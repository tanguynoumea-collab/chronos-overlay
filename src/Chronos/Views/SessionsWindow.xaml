<Window x:Class="Chronos.Views.SessionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        ShowInTaskbar="False" ShowActivated="False" Topmost="True"
        WindowStartupLocation="Manual" FontFamily="Segoe UI">
    <!-- Panneau CHROMELESS : ni titre, ni résumé, ni fond autour. Seules les pastilles flottent sur le
         bureau (comme le cadran). Glisser = déplacer. Clic droit sur une pastille = Archiver. -->
    <ItemsControl ItemsSource="{Binding Items}" Background="#01000000"
                  MouseLeftButtonDown="Drag" Padding="2">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border CornerRadius="9" Padding="10,7" Margin="0,3" MinWidth="180"
                        Background="#CC17151F">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.35" Color="Black"/>
                    </Border.Effect>
                    <Border.ContextMenu>
                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                            <MenuItem Header="Archiver (retirer de l'overlay)" Command="{Binding ArchiveCommand}"/>
                        </ContextMenu>
                    </Border.ContextMenu>
                    <Grid>
                        <Ellipse Width="10" Height="10" Fill="{Binding StateBrush}"
                                 HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <StackPanel Margin="20,0,0,0">
                            <TextBlock Text="{Binding Project}" Foreground="#F4F2EC" FontSize="12.5"
                                       FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                            <StackPanel Orientation="Horizontal" Margin="0,1,0,0">
                                <TextBlock Text="{Binding StateText}" Foreground="{Binding StateBrush}" FontSize="10.5" FontWeight="SemiBold"/>
                                <TextBlock Text="  ·  " Foreground="#8C8A98" FontSize="10.5"/>
                                <TextBlock Text="{Binding Detail}" Foreground="#8C8A98" FontSize="10.5"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</Window>
