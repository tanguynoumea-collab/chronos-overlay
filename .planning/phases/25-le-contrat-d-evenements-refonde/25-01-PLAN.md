---
phase: 25-le-contrat-d-evenements-refonde
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/CatalogueEvenementsHooks.cs
  - src/Chronos/Services/SessionHookInstaller.cs
  - src/Chronos/Services/SessionHookProcessor.cs
  - tests/Chronos.Tests/CatalogueEvenementsHooksTests.cs
  - tests/Chronos.Tests/SessionsTests.cs
  - tests/Chronos.Tests/ClaudeSettingsReconcilerTests.cs
autonomous: true
requirements: [EVT-01, EVT-02]

must_haves:
  truths:
    - "Une demande de permission réelle produit « à toi » : l'événement dédié PermissionRequest est câblé et routé."
    - "Une alerte d'absence (l'utilisateur n'a rien tapé depuis un moment) ne produit plus AUCUN état."
    - "Une authentification réussie et une reprise de quota ne produisent plus AUCUN état."
    - "Un nom d'événement mal orthographié ne peut plus être écrit dans settings.json : il est refusé avant l'écriture."
    - "Un matcher posé sur un événement qui n'en accepte pas n'est jamais écrit : une configuration morte ne s'installe pas."
    - "Les hooks d'un autre outil survivent intégralement à la réconciliation, y compris quand la purge Chronos devient large."
  artifacts:
    - path: "src/Chronos/Services/CatalogueEvenementsHooks.cs"
      provides: "Liste blanche des 33 noms d'événements + support du matcher, codée en dur"
      contains: "PermissionRequest"
    - path: "src/Chronos/Services/SessionHookInstaller.cs"
      provides: "Câblage déclaratif (événement + matcher + rôle), validé contre le catalogue AVANT écriture"
      contains: "Cablage"
    - path: "src/Chronos/Services/SessionHookProcessor.cs"
      provides: "Routage : PermissionRequest fonde l'attente ; le bus de notifications ne fabrique plus d'état hors demande"
      contains: "PermissionRequest"
    - path: "tests/Chronos.Tests/CatalogueEvenementsHooksTests.cs"
      provides: "Preuve que la liste blanche compte 33 noms distincts et refuse une faute de frappe"
  key_links:
    - from: "src/Chronos/Services/SessionHookInstaller.cs"
      to: "src/Chronos/Services/CatalogueEvenementsHooks.cs"
      via: "validation de chaque clé d'événement AVANT mutation du JSON"
      pattern: "CatalogueEvenementsHooks\\.(EstConnu|AccepteUnMatcher)"
    - from: "src/Chronos/Services/SessionHookInstaller.cs"
      to: "~/.claude/settings.json"
      via: "ClaudeSettingsReconciler.ReconcileJson → ApplyHooks (seul chemin autorisé)"
      pattern: "ApplyHooks\\("
---

<objective>
EVT-01 et EVT-02 — l'attente cesse de naître d'un proxy.

`PermissionRequest` (événement DÉDIÉ, confirmé par le relevé du 2026-09-12) devient la source de
« à toi ». Le bus `Notification` — douze types, pas une alerte d'absence — cesse d'être réduit à un seul
état : seuls les trois types qui sont de véritables DEMANDES l'atteignent encore, par un `matcher`, et un
veto secondaire écarte les neuf autres si le type est lisible sur stdin.

Ce plan pose aussi la parade au mode de défaillance le plus dangereux du contrat externe : **le sort d'un
nom d'événement inconnu n'est PAS documenté**, donc un nom mal orthographié pourrait laisser un hook mort
et muet. Une liste blanche des 33 noms, codée en dur, est consultée AVANT toute écriture.

Purpose: réparer la sémantique de la source, là où elle est fausse.
Output: `CatalogueEvenementsHooks`, un câblage déclaratif validé, un routage refondé, et les tests qui les
tiennent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-le-contrat-d-evenements-refonde/25-CONTRAT-EXTERNE.md
@.planning/phases/25-le-contrat-d-evenements-refonde/25-CONTEXT.md
@src/Chronos/Services/SessionHookProcessor.cs
@src/Chronos/Services/SessionHookInstaller.cs
@src/Chronos/Services/ClaudeSettingsJson.cs
@src/Chronos/Services/ClaudeSettingsReconciler.cs

<interfaces>
<!-- Contrats déjà en place que l'exécutant consomme. Aucune exploration du dépôt nécessaire. -->

From src/Chronos/Services/SessionHookProcessor.cs :
```csharp
public sealed record SessionHookResult(string? SessionId, bool Delete, string? StateJson, bool Ignore)
{
    public static SessionHookResult Ignored { get; }
}
public static class SessionHookProcessor
{
    public static SessionHookResult Process(string? eventName, string? stdinJson, long nowMs);
    public static string BuildStateJson(string sessionId, string project, SessionActivity activity,
                                        string? reason, long updatedAtMs);
    internal static string ProjectFromCwd(string? cwd);
}
```

From src/Chronos/Services/SessionHookInstaller.cs :
```csharp
public sealed class SessionHookInstaller
{
    public static readonly string[] Events;                       // aujourd'hui : 5 noms
    public static string HookCommand(string exePath, string ev);  // "\"C:/…/Chronos.exe\" --hook <ev>"
    public static void ApplyHooks(JsonObject root, string exePath, bool wanted);
    public static string? TransformForInstall(string? settingsJson, string exePath);
    public static string? TransformForUninstall(string? settingsJson);
}
```

From src/Chronos/Services/ClaudeSettingsJson.cs :
```csharp
public const string HookMarker = "--hook";
public static bool IsChronosGroup(JsonNode? group, string marker);
public static string? CommandOf(JsonNode? handler);
public static JsonObject? ParseOrNull(string? json);   // null == NE RIEN ÉCRIRE
```

From src/Chronos/Services/SessionSnapshot.cs :
```csharp
public enum SessionActivity { Working, WaitingAttention, WaitingTurn, Unknown }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: La liste blanche des 33 noms d'événements, codée en dur</name>
  <files>src/Chronos/Services/CatalogueEvenementsHooks.cs, tests/Chronos.Tests/CatalogueEvenementsHooksTests.cs</files>

  <read_first>
    - .planning/phases/25-le-contrat-d-evenements-refonde/25-CONTRAT-EXTERNE.md — sections D (le catalogue
      complet, 33 noms) et F (règles de matcher, liste des 10 événements SANS support)
    - src/Chronos/Services/SessionHookInstaller.cs (le tableau `Events`, l. 33)
    - tests/Chronos.Tests/NormalisationUniqueTests.cs (les motifs interdits dans `Services/`)
  </read_first>

  <behavior>
    - `CatalogueEvenementsHooks.Tous.Length == 33`, tous les noms distincts (comparaison ordinale).
    - `EstConnu("PermissionRequest") == true` ; `EstConnu("Notifcation") == false` (faute de frappe) ;
      `EstConnu("notification") == false` (les clés de `settings.json` sont sensibles à la casse) ;
      `EstConnu(null) == false` ; `EstConnu("") == false` ; `EstConnu("  Stop  ") == false`
      (aucun rognage implicite : une clé JSON n'a pas d'espaces).
    - `AccepteUnMatcher("Stop") == false`, `AccepteUnMatcher("MessageDisplay") == false`,
      `AccepteUnMatcher("UserPromptSubmit") == false` — les dix événements confirmés SANS support.
    - `AccepteUnMatcher("Notification") == true`, `AccepteUnMatcher("PermissionRequest") == true`.
    - `AccepteUnMatcher("Inconnu") == false` — ce qu'on ne connaît pas n'accepte rien.
    - Garde anti-mutisme : les cinq noms câblés aujourd'hui (`SessionStart`, `SessionEnd`,
      `UserPromptSubmit`, `Stop`, `Notification`) sont tous connus.
  </behavior>

  <action>
    Créer `src/Chronos/Services/CatalogueEvenementsHooks.cs` (namespace `Chronos.Services`, aucun type WPF,
    aucune E/S, aucune horloge) :

    ```csharp
    public sealed record EvenementHook(string Nom, bool AccepteUnMatcher);

    public static class CatalogueEvenementsHooks
    {
        public static readonly EvenementHook[] Tous = { /* 33 entrées */ };
        public static bool EstConnu(string? nom);
        public static bool AccepteUnMatcher(string? nom);
    }
    ```

    Les 33 noms, groupés comme dans le relevé (section D du contrat externe) :
    - Session : `SessionStart`, `Setup`, `SessionEnd`
    - Tour : `UserPromptSubmit`, `UserPromptExpansion`, `Stop`, `StopFailure`
    - Outils : `PreToolUse`, `PermissionRequest`, `PermissionDenied`, `PostToolUse`,
      `PostToolUseFailure`, `PostToolBatch`
    - Affichage : `Notification`, `MessageDisplay`
    - Sous-agents / tâches : `SubagentStart`, `SubagentStop`, `TaskCreated`, `TaskCompleted`, `TeammateIdle`
    - Contexte / config : `InstructionsLoaded`, `ConfigChange`, `PreCompact`, `PostCompact`,
      `PreModelSwitch`, `PostModelSwitch`
    - Fichiers : `CwdChanged`, `DirectoryAdded`, `FileChanged`, `WorktreeCreate`, `WorktreeRemove`
    - MCP : `Elicitation`, `ElicitationResult`

    `AccepteUnMatcher == false` pour exactement ces dix : `UserPromptSubmit`, `PostToolBatch`, `Stop`,
    `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `WorktreeCreate`, `WorktreeRemove`, `MessageDisplay`,
    `CwdChanged`. `true` pour les 23 autres.

    Comparaisons **ordinales et sensibles à la casse** (`StringComparer.Ordinal`) : une clé de
    `settings.json` est comparée telle quelle par Claude Code.

    XML-doc en tête, qui porte le POURQUOI et il n'est pas cosmétique :
    « Le sort d'un nom d'événement INCONNU n'est pas documenté (relevé du 2026-09-12, référence officielle
    des hooks). Les termes de rejet sont absents de la page, et le guide a rejeté sa propre affirmation
    d'un rejet silencieux comme fabriquée. Notre mode de défaillance le plus dangereux est donc un hook
    MORT et MUET : un nom mal orthographié qui ne produit aucune erreur visible. D'où cette liste blanche,
    consultée AVANT toute écriture dans le fichier de l'utilisateur — on ne compte jamais sur un
    avertissement de Claude Code. »

    **Consigne de rédaction (phase 24, obligatoire).** `NormalisationUniqueTests` balaie le TEXTE de
    `Services/` — commentaires et XML-doc compris — et ce fichier n'est PAS exempté. N'écrire nulle part,
    même en prose, une division ou une multiplication par cent, ni `FromUnixTimeSeconds`,
    `FromUnixTimeMilliseconds`, `DateTimeOffset.TryParse`. Écrire « cent » en toutes lettres si le nombre
    doit apparaître.

    Créer `tests/Chronos.Tests/CatalogueEvenementsHooksTests.cs` avec les cas du bloc `<behavior>`.
    Aucune E/S, aucun accès à `~/.claude`, aucun accès à `%APPDATA%`.

    Ordre TDD : écrire d'abord le fichier de tests contre un squelette compilable
    (`throw new NotImplementedException()`), mesurer l'étape ROUGE, puis implémenter.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~CatalogueEvenementsHooksTests"</automated>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~NormalisationUniqueTests|FullyQualifiedName~ServicesLayerPurityTests"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/Chronos/Services/CatalogueEvenementsHooks.cs` → vrai.
    - Le filtre `CatalogueEvenementsHooksTests` rend **0 échec** et **au moins 6 tests exécutés**.
    - `NormalisationUniqueTests` (3 tests) et `ServicesLayerPurityTests` (2 tests) : 0 échec.
    - `grep -c 'PermissionRequest' src/Chronos/Services/CatalogueEvenementsHooks.cs` ≥ 1.
    - `grep -c 'MessageDisplay' src/Chronos/Services/CatalogueEvenementsHooks.cs` ≥ 1.
    - Le test `Le_catalogue_compte_trente_trois_noms_distincts` existe et passe (c'est LUI qui fixe le
      nombre — aucun comptage par grep sur le fichier source, qui compterait des lignes et non des entrées).
    - Aucun type WPF, aucune E/S fichier, aucune horloge dans le nouveau fichier :
      `grep -E 'System\.Windows|System\.IO|DateTimeOffset\.UtcNow' src/Chronos/Services/CatalogueEvenementsHooks.cs`
      ne rend **aucune ligne**.
  </acceptance_criteria>

  <done>
    La liste blanche existe, compte 33 noms distincts, distingue les dix événements sans support de
    `matcher`, et refuse une faute de frappe comme une différence de casse. Aucune garde de projet ne
    régresse.
  </done>
</task>

<task type="auto">
  <name>Task 2: Le câblage devient déclaratif, validé contre le catalogue, et la purge devient large</name>
  <files>src/Chronos/Services/SessionHookInstaller.cs, tests/Chronos.Tests/SessionsTests.cs, tests/Chronos.Tests/ClaudeSettingsReconcilerTests.cs</files>

  <read_first>
    - src/Chronos/Services/SessionHookInstaller.cs (intégral — `Events` l. 33, `ApplyHooks` l. 92-130)
    - src/Chronos/Services/ClaudeSettingsJson.cs (`IsChronosGroup`, `ParseOrNull`, `Serialize`)
    - src/Chronos/Services/ClaudeSettingsReconciler.cs (`ReconcileJson` — le SEUL chemin vers settings.json)
    - tests/Chronos.Tests/SessionsTests.cs (l. 61-220, la section SessionHookInstaller)
    - tests/Chronos.Tests/ClaudeSettingsReconcilerTests.cs (l. 1-130 : fixture polluée, `CompteHooks`,
      `Purge_la_fixture_reelle_de_25_a_5_hooks_Chronos`, `Preserve_les_trois_hooks_GSD_…`)
    - .planning/phases/25-le-contrat-d-evenements-refonde/25-CONTRAT-EXTERNE.md — section F
  </read_first>

  <action>
    **(a) Rendre le câblage déclaratif.** Dans `SessionHookInstaller`, remplacer le tableau nu `Events` par
    une table qui porte le matcher et le rôle :

    ```csharp
    /// <summary>Un événement CÂBLÉ par Chronos : son nom, le matcher qui le filtre (null = tout), et ce
    /// qu'il produit. Le rôle n'est pas décoratif : c'est lui qu'on recopie dans docs/ (EVT-05).</summary>
    public sealed record EvenementCable(string Evenement, string? Matcher, string Role);

    public static readonly EvenementCable[] Cablage =
    {
        new("SessionStart",      null, "la session démarre → en cours"),
        new("UserPromptSubmit",  null, "un message est envoyé → en cours"),
        new("Stop",              null, "le tour se termine → tour fini"),
        new("SessionEnd",        null, "fin de session → le fichier d'état est supprimé"),
        new("PermissionRequest", null, "une permission est DEMANDÉE → à toi (EVT-01)"),
        new("Notification",      "agent_needs_input|elicitation_dialog|elicitation_url_dialog",
                                 "les TROIS types du bus qui sont de vraies demandes → à toi (EVT-02)"),
    };

    public static readonly string[] Events = Cablage.Select(c => c.Evenement).ToArray();
    ```

    `Events` est CONSERVÉ (le diagnostic et quatre tests le lisent) mais devient dérivé : une seule source
    de vérité.

    Justifier le matcher de `Notification` en XML-doc, en reprenant le relevé : le bus compte douze types
    (`permission_prompt`, `idle_prompt`, `auth_success`, quatre `elicitation_*`, `agent_needs_input`,
    `agent_completed`, trois `quota_auto_resume_*`). `permission_prompt` est volontairement ABSENT du
    matcher : `PermissionRequest` est le signal dédié, exact et immédiat, et deux chemins pour un même
    fait rendraient la source illisible. `idle_prompt` est une alerte d'ABSENCE : elle n'est au mieux
    qu'un indice, jamais une vérité sur ce que fait la session. Le matcher ne contient que des lettres,
    des tirets bas et des barres verticales : il est donc évalué comme une LISTE EXACTE et non comme une
    expression régulière non ancrée — c'est exactement le piège documenté (`Edit.*` attrape aussi
    `NotebookEdit`), et on n'y entre pas.

    **(b) Valider AVANT d'écrire, et ouvrir la validation aux tests.** Scinder `ApplyHooks` :

    ```csharp
    public static void ApplyHooks(JsonObject root, string exePath, bool wanted)
        => ApplyHooks(root, exePath, wanted, Cablage);

    /// <summary>Surcharge à câblage INJECTÉ : elle existe pour que la validation (liste blanche,
    /// support du matcher) soit prouvable sans jamais écrire un nom douteux dans le câblage réel.</summary>
    public static void ApplyHooks(JsonObject root, string exePath, bool wanted,
                                  IReadOnlyList<EvenementCable> cablage)
    ```

    Dans la surcharge, pour chaque entrée du câblage, AVANT toute mutation :
    - si `!CatalogueEvenementsHooks.EstConnu(c.Evenement)` → **ne rien écrire pour cette entrée**
      (un nom inconnu produirait un hook mort et muet) ;
    - si `c.Matcher is not null && !CatalogueEvenementsHooks.AccepteUnMatcher(c.Evenement)` →
      **ne rien écrire pour cette entrée** : un matcher posé là est silencieusement ignoré par Claude Code,
      donc le groupe ne ferait pas ce qu'il annonce. Une configuration morte ne s'installe pas.

    **(c) La purge devient LARGE, l'installation reste ciblée.** Aujourd'hui `ApplyHooks` ne parcourt que
    les événements câblés : un groupe Chronos posé sur un événement qu'on cesse de câbler survivrait pour
    toujours, invisible. Désormais :
    1. retirer les groupes Chronos de **TOUTES** les clés de `root["hooks"]`
       (`ClaudeSettingsJson.IsChronosGroup(g, HookMarker)`), parcours DESCENDANT par tableau ;

       Une clé de `hooks` dont la valeur **n'est pas un tableau** (fichier tiers mal formé) est **ignorée
       sans être écrite** : `if (hooks[k] is not JsonArray arr) continue;`. Et une clé dont le tableau
       était **déjà vide à l'entrée** n'est **pas** retirée : seules les clés **dont on a effectivement
       retiré un groupe Chronos** peuvent être supprimées si elles deviennent vides (étape 3). Retirer une
       clé vide qu'on n'a jamais touchée mutilerait la configuration d'un autre outil.
    2. si `wanted`, ajouter un groupe par entrée VALIDE du câblage, avec la clé `matcher` **présente
       uniquement** quand `c.Matcher is not null`, et `{"type":"command","command":HookCommand(...),
       "timeout":10}` comme aujourd'hui (chemin d'exe en slashes avant — leçon terrain conservée) ;
    3. retirer les clés d'événement devenues des tableaux vides, puis la clé `hooks` si elle est vide.

    **Piège de mise en œuvre :** on ne peut pas supprimer une clé d'un `JsonObject` pendant qu'on l'énumère.
    Figer d'abord la liste des clés (`hooks.Select(kv => kv.Key).ToList()`), puis muter. Ne jamais
    reparenter un `JsonNode` (les groupes tiers doivent garder leur instance, leur ordre, leur `matcher`,
    leur `timeout` et leurs clés inconnues).

    **(d) Adapter les tests existants DANS CETTE TÂCHE** (`tests/Chronos.Tests` a un `ProjectReference` vers
    `Chronos` : la compilabilité est exigée à chaque commit) :
    - `SessionsTests.Install_ajoute_les_5_hooks_en_slashes_avant` → renommer
      `Install_pose_un_groupe_par_evenement_cable_en_slashes_avant` ; la boucle sur `Events` reste valide.
    - `SessionsTests.Trois_chemins_dexe_successifs_ne_laissent_que_cinq_hooks` → renommer
      `Trois_chemins_dexe_successifs_ne_laissent_qu_un_groupe_par_evenement`.
    - `ClaudeSettingsReconcilerTests.Purge_la_fixture_reelle_de_25_a_5_hooks_Chronos` → renommer
      `Purge_la_fixture_reelle_de_25_groupes_a_un_seul_par_evenement_cable`, et remplacer le littéral `5`
      attendu par `SessionHookInstaller.Events.Length`. L'assertion d'entrée (`25` groupes dans la fixture)
      reste un littéral : c'est un fait figé du 2026-09-09, pas une conséquence du code.
    - Deux littéraux — et **exactement deux** — sont des comptages de hooks Chronos et doivent devenir
      `SessionHookInstaller.Events.Length` : `ClaudeSettingsReconcilerTests.cs:67` et
      `ClaudeSettingsReconcilerTests.cs:253`, tous deux `Assert.Equal(5, CompteHooks(…, HookMarker))`.
      **Tous les autres `5` de ces deux fichiers sont des `timeout` appartenant à un AUTRE OUTIL**
      (`SessionsTests.cs:135`, `:142`, `:181` ; `ClaudeSettingsReconcilerTests.cs:101`) **ou la rétention
      de sauvegardes** (`ClaudeSettingsReconcilerTests.cs:328`) : les toucher serait une régression de
      préservation des tiers. Ne les modifier sous aucun prétexte. `SessionsTests.cs` ne contient AUCUN
      comptage de hooks codé en dur — ses tests bouclent déjà sur `SessionHookInstaller.Events`.

    **(e) Nouveaux tests** dans `tests/Chronos.Tests/SessionsTests.cs` :
    - `Le_groupe_Notification_ne_laisse_passer_que_les_trois_vraies_demandes` — après
      `TransformForInstall(null, Exe)`, le groupe `Notification` porte la clé `matcher` égale à
      `agent_needs_input|elicitation_dialog|elicitation_url_dialog`.
    - `Le_cablage_installe_bien_le_groupe_PermissionRequest` — après `TransformForInstall(null, Exe)`, la
      clé `hooks["PermissionRequest"]` existe, porte **exactement un** groupe Chronos dont la commande vaut
      `SessionHookInstaller.HookCommand(Exe, "PermissionRequest")`, et ce groupe **ne porte PAS** de clé
      `matcher`. C'est la moitié « câblée » d'EVT-01 ; sans lui, la boucle sur `Events` est tautologique.
    - `Le_cablage_est_exactement_celui_que_la_phase_annonce` — le **contenu exact** du câblage, figé :
      ```csharp
      Assert.Equal(new[]{"SessionStart","UserPromptSubmit","Stop","SessionEnd","PermissionRequest","Notification"},
                   SessionHookInstaller.Events);
      ```
      Un comptage seul (`Events.Length == 6`) ne prouverait rien : `Purge_la_fixture_reelle_…` compare des
      handlers à `Events.Length` et reste vert quelle que soit cette valeur.
    - `Un_evenement_dont_le_nom_est_inconnu_n_est_jamais_ecrit` — via la surcharge à câblage injecté,
      avec `new EvenementCable("Notifcation", null, "faute de frappe")` : après `ApplyHooks`, la racine ne
      porte AUCUNE clé `hooks` (rien n'a été écrit).
    - `Un_matcher_sur_un_evenement_qui_n_en_accepte_pas_n_est_jamais_ecrit` — câblage injecté
      `new EvenementCable("Stop", "quelque_chose", "…")` : aucune clé `Stop` écrite.
    - `Un_groupe_Chronos_sur_un_evenement_qui_n_est_plus_cable_est_purge` — partir d'un settings qui porte
      un groupe Chronos sur `SubagentStop` (commande `"C:/Apps/Chronos.exe" --hook SubagentStop`) ; après
      `TransformForInstall`, la clé `SubagentStop` a disparu.
    - `La_purge_large_epargne_les_hooks_d_un_autre_outil` — partir d'un settings qui porte des groupes
      tiers sur `PreToolUse` et `PostToolUse` (`node gsd.js`, avec `matcher` et `timeout`) ; après
      `TransformForInstall`, asserter que le groupe tiers est **toujours à l'index 0**, avec sa commande,
      son `matcher` et son `timeout` d'origine — **ne jamais asserter la longueur du tableau** : le plan
      25-02 y ajoutera un groupe Chronos APRÈS lui, et ce test doit rester vert sans retouche.
    - `La_purge_large_ne_prend_pas_pour_nous_le_hook_d_un_tiers_qui_porte_le_meme_argument` — un groupe
      tiers sur `PreCompact` dont la commande est `node outil.js --hook PreCompact` (marqueur **présent**,
      exécutable **non Chronos**) et un groupe dont le handler est `{"type":"http","url":"…"}` (**sans**
      `command`) survivent tous deux intacts à `TransformForInstall` **ET** à `TransformForUninstall`.

    Aucun test de cette tâche ne touche le vrai `~/.claude/settings.json` : les cœurs purs travaillent sur
    des chaînes, et tout chemin de fichier vient de `Path.GetTempPath()`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~SessionsTests|FullyQualifiedName~ClaudeSettingsReconcilerTests|FullyQualifiedName~ClaudeSettingsJsonTests|FullyQualifiedName~StatusLineInstallerTests"</automated>
  </verify>

  <acceptance_criteria>
    - Le filtre ci-dessus rend **0 échec**.
    - Les huit tests nommés en (e) existent : `grep -c 'Le_groupe_Notification_ne_laisse_passer_que_les_trois_vraies_demandes\|Le_cablage_installe_bien_le_groupe_PermissionRequest\|Le_cablage_est_exactement_celui_que_la_phase_annonce\|Un_evenement_dont_le_nom_est_inconnu_n_est_jamais_ecrit\|Un_matcher_sur_un_evenement_qui_n_en_accepte_pas_n_est_jamais_ecrit\|Un_groupe_Chronos_sur_un_evenement_qui_n_est_plus_cable_est_purge\|La_purge_large_epargne_les_hooks_d_un_autre_outil\|La_purge_large_ne_prend_pas_pour_nous_le_hook_d_un_tiers_qui_porte_le_meme_argument' tests/Chronos.Tests/SessionsTests.cs`
      ≥ 8.
    - Le **contenu exact** du câblage est figé par le test dédié
      `Le_cablage_est_exactement_celui_que_la_phase_annonce`, qui asserte
      `Assert.Equal(new[]{"SessionStart","UserPromptSubmit","Stop","SessionEnd","PermissionRequest","Notification"}, SessionHookInstaller.Events)`.
      Un comptage seul (`Events.Length == 6`) ne prouverait rien : `Purge_la_fixture_reelle_…` compare des
      handlers à `Events.Length` et reste vert avec 5, 7 ou 12 entrées.
    - `grep -n 'CatalogueEvenementsHooks' src/Chronos/Services/SessionHookInstaller.cs` rend **au moins
      2 lignes** (les deux validations).
    - Les anciens noms de tests ont disparu :
      `grep -c 'Install_ajoute_les_5_hooks_en_slashes_avant\|Trois_chemins_dexe_successifs_ne_laissent_que_cinq_hooks\|Purge_la_fixture_reelle_de_25_a_5_hooks_Chronos' tests/Chronos.Tests/` (récursif, hors `bin/` et `obj/`) rend **0**.
    - `git diff --stat <SHA d'entrée de phase>..HEAD -- src/Chronos/App.xaml.cs
      src/Chronos/Services/ClaudeSettingsReconciler.cs src/Chronos/Services/SessionTreatmentTracker.cs
      src/Chronos/Services/TreatedStore.cs src/Chronos/Services/ArchiveStore.cs` est **vide**.
      `git diff --stat` nu est MUET après un commit : il ne compare que l'arbre de travail. Relever le SHA
      d'entrée de phase (`git rev-parse HEAD`, AVANT le premier commit de la phase) dans le SUMMARY 25-01,
      et le réutiliser à l'identique aux plans 25-02, 25-03 et 25-04.
  </acceptance_criteria>

  <done>
    Le câblage est déclaratif et validé contre la liste blanche avant toute écriture ; `Notification` est
    filtré par `matcher` sur les trois seules vraies demandes ; la purge Chronos couvre toutes les clés
    sans jamais toucher un groupe tiers ; `PermissionRequest` est installé.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Le routage — PermissionRequest fonde l'attente, le bus cesse d'en fabriquer une</name>
  <files>src/Chronos/Services/SessionHookProcessor.cs, tests/Chronos.Tests/SessionsTests.cs</files>

  <read_first>
    - src/Chronos/Services/SessionHookProcessor.cs (intégral, 104 lignes — la table de routage l. 59-72
      et la XML-doc l. 13-24 qui la décrit)
    - tests/Chronos.Tests/SessionsTests.cs (l. 17-59, la section SessionHookProcessor)
    - .planning/phases/25-le-contrat-d-evenements-refonde/25-CONTRAT-EXTERNE.md — correction n°1
      (les douze types du bus) et « Ce que ce relevé N'AUTORISE PAS »
    - src/Chronos/Services/SessionHookInstaller.cs (le `Cablage` posé à la tâche 2 : le matcher est le
      filtre PRIMAIRE, ce routage n'en est que le second)
  </read_first>

  <behavior>
    - `Process("PermissionRequest", {"session_id":"s","cwd":"C:\\dev\\MonProjet"}, 1000)` →
      `Ignore == false`, `activity == "WaitingAttention"`, `project == "MonProjet"`,
      `reason == "PermissionRequest"`, `updated_at == 1000`.
    - `Process("Notification", {"session_id":"s","notification_type":"agent_needs_input"}, 0)` →
      `WaitingAttention`, `reason == "agent_needs_input"`.
    - `Process("Notification", {…,"notification_type":"elicitation_dialog"})` → `WaitingAttention`.
    - `Process("Notification", {…,"notification_type":"elicitation_url_dialog"})` → `WaitingAttention`.
    - `Process("Notification", {…,"notification_type":"idle_prompt"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"auth_success"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"quota_auto_resume_fired"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"quota_auto_resume_stale"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"quota_auto_resume_disabled"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"agent_completed"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"elicitation_complete"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"elicitation_response"})` → `Ignore == true`.
    - `Process("Notification", {…,"notification_type":"permission_prompt"})` → `Ignore == true`
      (l'événement dédié en a la charge ; deux chemins pour un même fait rendraient la source illisible).
    - `Process("Notification", {"session_id":"s"})` — AUCUN type lisible → `WaitingAttention` :
      le `matcher` a déjà trié en amont, et on ne fait jamais dépendre la PRODUCTION d'un état de la
      présence d'un champ dont le nom n'est pas confirmé.
    - `Process("Notification", {…,"notification_type":"un_type_futur_inconnu"})` → `WaitingAttention` :
      même raison ; le veto ne porte que sur des valeurs RELEVÉES.
    - Les quatre routages existants sont inchangés : `Stop` → `WaitingTurn`, `UserPromptSubmit` → `Working`,
      `SessionStart` → `Working`, `SessionEnd` → suppression.
    - `Process("SubagentStop", …)` reste ignoré ; sans `session_id`, tout est ignoré.
  </behavior>

  <action>
    Dans `src/Chronos/Services/SessionHookProcessor.cs` :

    **(a)** Déclarer le veto, en nommant sa source :

    ```csharp
    /// <summary>Les types du bus de notifications qui ne disent RIEN de ce que fait la session.
    /// Relevé du 2026-09-12 : le bus en compte douze, et Chronos les réduisait TOUS à un seul état —
    /// une authentification réussie et une reprise de quota fabriquaient donc une attente.
    /// Le filtre PRIMAIRE est le matcher posé dans settings.json ; ceci n'en est que le VETO de
    /// second rideau, qui protège aussi les réglages d'une version antérieure, encore sans matcher.</summary>
    private static readonly string[] NotificationsSansEtat =
    {
        "permission_prompt", "idle_prompt", "auth_success",
        "elicitation_complete", "elicitation_response", "agent_completed",
        "quota_auto_resume_fired", "quota_auto_resume_stale", "quota_auto_resume_disabled",
    };
    ```

    **(b)** Dans `Process`, après la lecture de stdin et le garde `session_id` :

    ```csharp
    if (ev is "Notification"
        && notifType.Length > 0
        && System.Array.IndexOf(NotificationsSansEtat, notifType) >= 0)
        return SessionHookResult.Ignored;
    ```

    Le sens EXACT de ce test, à écrire en commentaire : **le veto ne produit jamais d'état, il n'en
    retire que.** On ne lit le champ que pour ÉCARTER ; jamais pour conclure. C'est ce qui rend le code
    robuste au fait que le nom de ce champ n'est pas confirmable — s'il changeait, on retomberait
    simplement sur le tri par matcher, sans jamais fabriquer d'attente.

    **(c)** Ajouter `"PermissionRequest" => SessionActivity.WaitingAttention` au `switch` de routage, en
    tête (c'est le signal dédié, exact et immédiat).

    **(d)** Le motif écrit dans le fichier d'état : pour `Notification`, le type quand il est lisible
    (comportement actuel, conservé) ; pour `PermissionRequest`, le NOM DE L'ÉVÉNEMENT
    (`"PermissionRequest"`). Ne lire AUCUN champ de contexte de permission : deux lectures du relevé se
    sont contredites sur son nom, donc rien ne doit s'y appuyer. Le nom de l'événement, lui, est un fait
    observé — c'est nous qui l'avons câblé.

    **(e)** Réécrire la table de sémantique de la XML-doc de tête pour qu'elle décrive ce qui est
    réellement produit :

    ```
    PermissionRequest                 → WaitingAttention (une permission est DEMANDÉE : exact, immédiat)
    Notification (3 types de demande) → WaitingAttention (filtré par matcher ; veto sur les neuf autres)
    Stop                              → WaitingTurn (le tour s'est terminé, c'est OBSERVÉ)
    UserPromptSubmit / SessionStart   → Working
    SessionEnd                        → suppression du fichier
    inconnu                           → ignoré
    ```

    **Consigne de rédaction (obligatoire).** Ce fichier est dans `Services/` et n'est PAS exempté de
    `NormalisationUniqueTests` : ne jamais écrire, même en commentaire, une division ou une
    multiplication par cent, ni `FromUnixTimeSeconds`, `FromUnixTimeMilliseconds`,
    `DateTimeOffset.TryParse`.

    Ajouter les cas du bloc `<behavior>` dans `tests/Chronos.Tests/SessionsTests.cs`, en `[Theory]` pour
    les neuf types vetoés et les trois types de demande. Conserver tels quels les quatre tests existants
    de routage. Ordre TDD : tests d'abord, étape ROUGE mesurée, puis implémentation.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~SessionsTests"</automated>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `SessionsTests` : **0 échec**.
    - La suite complète : **0 échec**, et un total **= total mesuré du plan précédent + nombre de cas
      ajoutés** — ici baseline d'entrée de phase (788) + les cas ajoutés par les trois tâches de ce plan —
      reporté dans `25-VALIDATION.md` ; tout écart, même positif, **justifié nominativement** dans le
      SUMMARY. Attendu indicatif : **816**.
    - `grep -c 'PermissionRequest' src/Chronos/Services/SessionHookProcessor.cs` ≥ 2
      (le routage + la table de la XML-doc).
    - `grep -c 'NotificationsSansEtat' src/Chronos/Services/SessionHookProcessor.cs` ≥ 2
      (la déclaration + son usage).
    - Le test `Une_alerte_d_absence_ne_fabrique_plus_aucun_etat` (ou le cas `idle_prompt` de la `[Theory]`
      de veto) existe et passe.
    - `git diff --stat <SHA d'entrée de phase>..HEAD -- src/Chronos/Services/SessionTreatmentTracker.cs
      src/Chronos/Services/TreatedStore.cs src/Chronos/Services/ArchiveStore.cs
      src/Chronos/Services/ArbitrageSessions.cs src/Chronos/Services/LectureSessions.cs` est **vide**
      (même SHA d'entrée de phase qu'à la tâche 2 : `git diff --stat` nu ne dirait rien après commit).
  </acceptance_criteria>

  <done>
    « À toi » naît d'une demande de permission réelle. Une alerte d'absence, une authentification réussie
    et une reprise de quota ne fabriquent plus aucun état. La suite reste verte.
  </done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug --nologo -v q` → 0 échec, total **= 788 (baseline d'entrée de phase)
  + cas ajoutés ici** (attendu indicatif : **816**), reporté dans `25-VALIDATION.md`, tout écart — même
  positif — justifié nominativement.
- Le **SHA d'entrée de phase** est relevé (`git rev-parse HEAD`, avant le premier commit) et consigné dans
  le SUMMARY : les plans 25-02 à 25-04 en dépendent pour que leurs `git diff --stat` disent quelque chose.
- Suites de gardes, chacune séparément, 0 échec : `GardesPerimetreTests` (10), `NormalisationUniqueTests` (3),
  `ServicesLayerPurityTests` (2), `CompositionRootTests` (5), `GardesDoctrineTests` (8).
- `git status` propre après les commits ; aucun fichier hors `files_modified` touché.
- Aucun test n'a écrit dans le vrai `~/.claude/` ni dans le vrai `%APPDATA%\Chronos\` : le test
  `Aucun_test_ne_cible_le_vrai_settings_du_profil` (ClaudeSettingsReconcilerTests) reste vert.
</verification>

<success_criteria>
- `PermissionRequest` est câblé ET routé vers « à toi ».
- Les neuf types du bus qui ne sont pas des demandes ne produisent plus aucun état, et le matcher
  installé fait qu'ils n'atteignent même plus Chronos.
- Un nom d'événement inconnu et un matcher sur un événement qui n'en accepte pas ne sont jamais écrits
  dans `settings.json` — prouvé par deux tests à câblage injecté.
- Un groupe de hooks d'un autre outil survit à la purge large — prouvé par un test nommé.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/25-le-contrat-d-evenements-refonde/25-01-SUMMARY.md`.

Le SUMMARY porte OBLIGATOIREMENT le **SHA d'entrée de phase** (`git rev-parse HEAD` relevé AVANT le premier
commit de la phase), en clair et sur sa propre ligne : les plans 25-02 à 25-04 s'en servent pour que leurs
`git diff --stat` restent parlants après commit.
</output>
