---
phase: 25-le-contrat-d-evenements-refonde
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - docs/hooks-contract.md
  - tests/Chronos.Tests/ContratHooksDocumenteTests.cs
  - tests/Chronos.Tests/Chronos.Tests.csproj
autonomous: true
requirements: [EVT-05]

must_haves:
  truths:
    - "docs/ décrit les événements câblés, leurs matchers, les champs lus et les états produits."
    - "docs/ porte les TROIS trous documentaires — Échap, SessionEnd sur crash, nom d'événement inconnu — avec leur date et leur source."
    - "Le document ne peut plus diverger du code en silence : un test compare la table documentée au câblage réel."
    - "Ce que Chronos a délibérément ÉCARTÉ est écrit, avec la raison — sinon un successeur le recâblerait."
  artifacts:
    - path: "docs/hooks-contract.md"
      provides: "Le contrat des hooks : ce qui est câblé, ce qui est lu, ce qui est produit, et ce qui n'est PAS garanti"
      contains: "2026-09-12"
      min_lines: 90
    - path: "tests/Chronos.Tests/ContratHooksDocumenteTests.cs"
      provides: "La garde de non-dérive : le document et le câblage disent la même chose"
  key_links:
    - from: "tests/Chronos.Tests/ContratHooksDocumenteTests.cs"
      to: "docs/hooks-contract.md"
      via: "chemin INJECTÉ par MSBuild (CheminDocsChronos), jamais deviné depuis la sortie de build"
      pattern: "CheminDocsChronos"
    - from: "tests/Chronos.Tests/ContratHooksDocumenteTests.cs"
      to: "src/Chronos/Services/SessionHookInstaller.cs"
      via: "comparaison de la table documentée à SessionHookInstaller.Cablage"
      pattern: "SessionHookInstaller\\.Cablage"
---

<objective>
EVT-05 — le seul critère qui protège contre la RÉPÉTITION de la panne.

Les quatre autres exigences réparent la dérive d'aujourd'hui ; celle-ci rend la prochaine **détectable**.
L'absence d'un tel document est précisément ce qui a laissé passer, pendant des mois, une sémantique fausse :
un bus de notifications réduit à un état, un proxy préféré à l'événement dédié, une interruption jamais
couverte. Rien ne le disait, donc rien ne pouvait le contredire.

Le document doit porter **ce qui n'est pas garanti** au moins autant que ce qui l'est, et nommer la DATE et
la SOURCE du relevé — pour qu'un écart futur se voie. Et pour qu'il ne devienne pas à son tour un texte
mort, un test le compare au câblage réel.

Purpose: rendre la prochaine dérive du contrat externe détectable.
Output: `docs/hooks-contract.md` et la garde qui l'empêche de mentir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-le-contrat-d-evenements-refonde/25-CONTRAT-EXTERNE.md
@.planning/phases/25-le-contrat-d-evenements-refonde/25-01-SUMMARY.md
@.planning/phases/25-le-contrat-d-evenements-refonde/25-02-SUMMARY.md
@.planning/phases/25-le-contrat-d-evenements-refonde/25-03-SUMMARY.md
@docs/data-sources.md
@src/Chronos/Services/SessionHookInstaller.cs
@src/Chronos/Services/CatalogueEvenementsHooks.cs
@src/Chronos/Services/SessionHookProcessor.cs

<interfaces>
<!-- Ce que le document doit décrire, et ce que la garde compare. -->

From src/Chronos/Services/SessionHookInstaller.cs (après 25-02, 8 entrées) :
```csharp
public sealed record EvenementCable(string Evenement, string? Matcher, string Role);
public static readonly EvenementCable[] Cablage;
public static readonly string[] Events;                       // dérivé
public static string HookCommand(string exePath, string ev);  // "\"…/Chronos.exe\" --hook <ev>"
```

From src/Chronos/Services/CatalogueEvenementsHooks.cs (25-01) :
```csharp
public sealed record EvenementHook(string Nom, bool AccepteUnMatcher);
public static readonly EvenementHook[] Tous;   // 33
public static bool EstConnu(string? nom);
public static bool AccepteUnMatcher(string? nom);
```

From tests/Chronos.Tests/Chronos.Tests.csproj — le précédent EXACT d'injection de chemin :
```xml
<AssemblyAttribute Include="System.Reflection.AssemblyMetadata">
  <_Parameter1>CheminSourcesChronos</_Parameter1>
  <_Parameter2>$(MSBuildProjectDirectory)\..\..\src\Chronos</_Parameter2>
</AssemblyAttribute>
```

From tests/Chronos.Tests/GardesPerimetreTests.cs — le motif de lecture du chemin injecté :
```csharp
internal static string CheminSources()
    => typeof(GardesPerimetreTests).Assembly
           .GetCustomAttributes<AssemblyMetadataAttribute>()
           .FirstOrDefault(a => a.Key == "CheminSourcesChronos")?.Value ?? "";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Le contrat des hooks, écrit — y compris ce qui n'est pas garanti</name>
  <files>docs/hooks-contract.md</files>

  <read_first>
    - .planning/phases/25-le-contrat-d-evenements-refonde/25-CONTRAT-EXTERNE.md (INTÉGRAL — c'est la
      matière première ; il fait foi)
    - docs/data-sources.md (l. 1-60 : le précédent de FORMAT — en-tête daté, avertissement sur la
      fragilité de la source, sections numérotées)
    - src/Chronos/Services/SessionHookInstaller.cs (le `Cablage` livré, 8 entrées, avec leurs `Role`)
    - src/Chronos/Services/SessionHookProcessor.cs (la table de sémantique de la XML-doc de tête)
    - src/Chronos/Services/SessionMonitor.cs (`SilenceDesBattements`, `DropAfter`)
    - src/Chronos/Services/AffichageSessions.cs (les cinq libellés d'état)
    - .planning/phases/25-le-contrat-d-evenements-refonde/25-01-SUMMARY.md, 25-02-SUMMARY.md,
      25-03-SUMMARY.md (ce qui a RÉELLEMENT été livré — ne rien documenter d'après le plan, tout d'après
      le code)
  </read_first>

  <action>
    Créer `docs/hooks-contract.md`, en français, sur le modèle de `docs/data-sources.md`.

    **En-tête obligatoire**, dans cet esprit :

    > **Relevé le 2026-09-12** — source : référence officielle des hooks Claude Code
    > (`code.claude.com/docs/en/hooks`, `/hooks-guide`, `/settings-reference`), lue et confirmée deux fois.
    > **⚠ Limite de la source :** la page de référence est TRONQUÉE à la récupération, juste avant les
    > sections par événement qui portent les schémas JSON de `stdin`. Les tables reprises ici (cycle de vie,
    > matchers, champs communs) ont été obtenues mot pour mot et identiques sur plusieurs lectures.
    > **Aucun nom de champ spécifique à un événement n'est confirmé.** Ne rien construire dessus.

    **§1 — Les événements câblés.** Une table encadrée par DEUX marqueurs de commentaire HTML, que le test
    de la tâche 2 utilisera comme délimiteurs :

    ```
    <!-- EVENEMENTS-CABLES:debut -->
    | Événement | `matcher` | Ce que Chronos en produit |
    |---|---|---|
    | `SessionStart` | (aucun) | … |
    …
    <!-- EVENEMENTS-CABLES:fin -->
    ```

    Une ligne par entrée de `SessionHookInstaller.Cablage` (huit aujourd'hui), dans le même ordre, avec le
    nom d'événement entre accents graves en première colonne, le matcher entre accents graves ou le
    littéral `(aucun)` en deuxième, et le `Role` du câblage en troisième. Ces valeurs se RECOPIENT du code,
    elles ne s'inventent pas.

    **§2 — Les champs lus sur `stdin`, et leur degré de confiance.** Distinguer explicitement :
    - **Confirmés comme communs à tous les événements** : `session_id`, `hook_event_name`, `cwd`,
      `transcript_path` (écrit de façon asynchrone, peut être en retard), `agent_id` / `agent_type`
      (présents UNIQUEMENT dans un sous-agent).
    - **Lu mais NON confirmé** : `notification_type`. Dire qu'il n'est utilisé que comme VETO — il peut
      écarter un état, jamais en produire un — et pourquoi : si Claude Code le renommait, Chronos
      retomberait sur le tri par `matcher` sans jamais fabriquer une attente.
    - **Jamais lus, délibérément** : tout champ spécifique à un événement. Deux lectures du relevé se sont
      contredites sur le nom du motif de fin de session et sur celui du contexte de permission.

    **§3 — Les états produits.** Une table `activity` → libellé affiché → ce que l'état AFFIRME, pour les
    cinq valeurs. Marquer sans ambiguïté laquelle est DÉDUITE et non observée.

    **Divergence connue, à écrire ici et à NE PAS corriger dans cette phase.**
    `SessionTreatmentTracker.cs:39` (`EstAttente`) ne reconnaît comme attentes que `WaitingTurn` et
    `WaitingAttention` : `WaitingDeduced` en est **exclue**, alors que le widget la compte dans
    `WaitingCount`. Conséquence : une session déduite n'ouvre pas d'épisode d'attente pour le suivi de
    « traité ». Cette phase **ne touche pas** au tracker (le diff de `SessionTreatmentTracker.cs` doit
    rester vide) : la divergence est documentée ici, telle quelle, et **signalée comme entrée de la
    phase 26**, qui redéfinit précisément ce que « traité » veut dire.

    **§4 — Le piège des sous-agents.** Les hooks d'un sous-agent portent le MÊME `session_id` que la session
    parente et ne s'en distinguent que par `agent_id` / `agent_type`. Sur cette machine, quatre-vingt-quatorze
    pour cent des transcripts sont des sous-agents. Dire la règle livrée : un sous-agent ne parle jamais de
    l'activité ni du cycle de vie de son parent ; il peut seulement réclamer une intervention.

    **§5 — CE QUI N'EST PAS GARANTI.** La section la plus importante du document. Les **trois trous
    documentaires**, chacun avec sa date de relevé (2026-09-12) et sa source :
    1. **L'interruption au clavier.** Aucun des 33 événements ne la mentionne ; « interrupt », « Escape »
       et « cancel » sont absents de la page. Que `Stop` soit muet sur Échap est **plausible et non
       confirmable**. Conséquence livrée : l'état est DÉDUIT du silence des battements, et le libellé le
       dit. Latence assumée : le seuil de silence (vingt minutes).
    2. **`SessionEnd` sur terminal tué, crash ou redémarrage.** `reason` a cinq valeurs — `clear`,
       `resume`, `logout`, `prompt_input_exit`, `other` — et aucune ne couvre ces trois cas. Rien ne
       documente une garantie d'émission. Conséquence livrée : `SessionEnd` reste un raccourci de
       nettoyage propre ; le balayage de la phase 23 et le seuil de huit heures sont le vrai filet.
    3. **Le sort d'un nom d'événement INCONNU.** « unrecognized », « unknown event » et « typo » sont
       absents de la page ; le guide a explicitement rejeté sa propre affirmation d'un rejet silencieux
       comme fabriquée. C'est le mode de défaillance le plus dangereux — un hook mort et muet.
       Conséquence livrée : liste blanche des 33 noms codée en dur, consultée AVANT toute écriture.

    **§6 — Ce que nous avons délibérément écarté, et pourquoi.** Sans cette section, un successeur
    recâblerait ce que nous avons refusé :
    - `MessageDisplay` — le battement le plus fin, mais déclenché pendant le streaming du texte : un
      processus par fragment affiché. Écarté pour son COÛT.
    - `permission_prompt` du bus `Notification` — l'événement dédié `PermissionRequest` en a la charge ;
      deux chemins pour un même fait rendraient la source illisible.
    - `idle_prompt` — une alerte d'ABSENCE, au mieux un indice, jamais un état.
    - Un `timeout` **uniforme** de dix secondes sur les battements — écarté : `PreToolUse` est BLOQUANT,
      et dix secondes de gel par appel d'outil coûteraient infiniment plus cher que le battement ne
      rapporte. Les deux battements portent `timeout: 3`, les six autres entrées `10`. Recopier la valeur
      **du code**, pas du plan.
    - Les ~25 autres événements du catalogue (`StopFailure`, `PreCompact`, `TeammateIdle`, `SubagentStart`…)
      — hors périmètre : cette phase câble ce que les cinq critères exigent, pas le catalogue.
    - Une sonde de capture installée dans `~/.claude/settings.json` pour relever les schémas réels de
      `stdin` — c'est la configuration VIVANTE de l'utilisateur. **Question ouverte, à lui poser : accepte-t-il
      qu'une sonde temporaire y soit posée ?** Tant que la réponse est non, les noms de champs
      spécifiques restent non confirmables et le code ne doit pas s'y appuyer.

    **§7 — Règles de `matcher`, telles que relevées.** Omis / `"*"` / `""` = tout ; lettres, chiffres,
    tiret bas, tiret, espaces, virgule et barre verticale = chaîne exacte ou liste ; **tout autre caractère
    = expression régulière JavaScript NON ANCRÉE** (`Edit.*` attrape aussi `NotebookEdit`). Un `matcher`
    posé sur un événement qui n'en accepte pas est **silencieusement ignoré** — d'où le refus d'écrire un
    tel groupe. Nommer les dix événements sans support. Nommer les deux pièges de version : séparateur
    virgule à partir de la version 2.1.191, tiret dans le jeu exact à partir de la 2.1.195.

    **§8 — Le catalogue complet des 33 événements**, groupé comme dans le relevé (session, tour, outils,
    affichage, sous-agents et tâches, contexte et config, fichiers, MCP). Chaque nom entre accents graves.

    **§9 — Comment détecter une dérive.** La procédure, en trois lignes : relire la page de référence,
    comparer à §1, §7 et §8, et **si un écart apparaît, le dater ici avant de toucher au code**. Rappeler
    que `ContratHooksDocumenteTests` échouera si §1 cesse de décrire le câblage réel, mais qu'**aucun test
    ne peut détecter une dérive de la source EXTERNE** : cela, seul un humain le peut, et c'est pour cela
    que la date du relevé figure en tête.

    **Rappel de terrain à écrire aussi** : la configuration des hooks est lue au DÉMARRAGE d'une session
    Claude Code. Seules les sessions lancées APRÈS une réconciliation de `settings.json` sont suivies — et
    la réconciliation n'a lieu qu'au lancement de l'overlay, en mode overlay uniquement.

    **NE PAS toucher** à `~/.claude/settings.json`, ni à `%APPDATA%\Chronos\sessions\`, ni à
    `archived.json`, ni à l'overlay en cours d'exécution. Ce document se rédige à partir du relevé et du
    code, pas d'une inspection de la machine.
  </action>

  <verify>
    <automated>test -f docs/hooks-contract.md && wc -l docs/hooks-contract.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/hooks-contract.md` → vrai, et `wc -l < docs/hooks-contract.md` ≥ **90**.
    - `grep -c '2026-09-12' docs/hooks-contract.md` ≥ 1.
    - `grep -c 'EVENEMENTS-CABLES:debut' docs/hooks-contract.md` == 1 et
      `grep -c 'EVENEMENTS-CABLES:fin' docs/hooks-contract.md` == 1.
    - Les neuf sections existent : `grep -c '^## ' docs/hooks-contract.md` ≥ 9.
    - Les trois trous documentaires sont nommés :
      `grep -ci 'Échap\|interruption' docs/hooks-contract.md` ≥ 1,
      `grep -c 'prompt_input_exit' docs/hooks-contract.md` ≥ 1,
      `grep -ci 'nom d.événement inconnu\|liste blanche' docs/hooks-contract.md` ≥ 1.
    - Le champ non confirmé est nommé : `grep -c 'notification_type' docs/hooks-contract.md` ≥ 1.
    - La question ouverte à l'utilisateur (sonde de capture) est présente :
      `grep -ci 'sonde' docs/hooks-contract.md` ≥ 1.
    - Aucun fichier de code n'est touché par cette tâche : `git diff --stat -- src/ tests/` est **vide**.
  </acceptance_criteria>

  <done>
    Le contrat des hooks est écrit : les événements câblés, les champs lus et leur degré de confiance, les
    états produits, le piège des sous-agents, les trois trous documentaires datés et sourcés, ce qui a été
    écarté et pourquoi, et la procédure de détection d'une dérive.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: La garde de non-dérive — le document et le câblage ne peuvent plus diverger en silence</name>
  <files>tests/Chronos.Tests/ContratHooksDocumenteTests.cs, tests/Chronos.Tests/Chronos.Tests.csproj</files>

  <read_first>
    - docs/hooks-contract.md (le document produit à la tâche 1, notamment §1 et ses deux marqueurs)
    - tests/Chronos.Tests/Chronos.Tests.csproj (l'`AssemblyAttribute` `CheminSourcesChronos` et son
      commentaire — le motif à recopier)
    - tests/Chronos.Tests/GardesPerimetreTests.cs (l. 255-264 : `CheminSources()`, le motif de lecture)
    - src/Chronos/Services/SessionHookInstaller.cs (`Cablage`)
    - src/Chronos/Services/CatalogueEvenementsHooks.cs (`Tous`)
  </read_first>

  <behavior>
    - `Le_chemin_du_document_est_injecte_et_le_fichier_existe` — l'attribut `CheminDocsChronos` est présent,
      non vide, et `docs/hooks-contract.md` existe à ce chemin. Aucune mise en sourdine possible : une
      garde qui se désarme quand son chemin manque ne garde rien.
    - `La_table_documentee_liste_EXACTEMENT_les_evenements_cables` — l'ensemble des noms extraits de la
      première colonne, entre les deux marqueurs, est **égal** (ni plus, ni moins) à l'ensemble
      `SessionHookInstaller.Cablage.Select(c => c.Evenement)`. Le message d'échec nomme les manquants ET
      les surnuméraires.
    - `Chaque_ligne_documentee_porte_le_matcher_reellement_installe` — pour chaque entrée du câblage, la
      deuxième colonne vaut le matcher entre accents graves, ou le littéral `(aucun)` quand il est nul.
    - `Chaque_ligne_documentee_porte_le_role_reellement_cable` — pour chaque entrée de `Cablage`, la
      **troisième** colonne de la ligne correspondante vaut `c.Role` (comparaison ORDINALE, cellules
      rognées). Sans elle, la colonne qui porte le SENS est la seule à pouvoir mentir : on vérifierait le
      nom et le filtre, et on laisserait la description dériver.
    - `Le_document_porte_les_trente_trois_noms_du_catalogue` — chaque `CatalogueEvenementsHooks.Tous[i].Nom`
      apparaît au moins une fois dans le document.
    - `Le_document_porte_les_trois_trous_documentaires_avec_leur_date` — les trois marqueurs des trous
      sont présents, **et la date `2026-09-12` apparaît DANS LA SECTION §5** (découper le texte entre
      l'en-tête `## ` du §5 et l'en-tête `## ` suivant, et chercher la date dans ce seul fragment). Une
      date présente ailleurs dans le fichier ne daterait pas les trous : c'est précisément ce qu'on veut
      pouvoir relire dans six mois.
    - Garde anti-mutisme : le document lu fait au moins 90 lignes et la table extraite n'est pas vide —
      sinon un fichier tronqué rendrait toutes les assertions vertes.
  </behavior>

  <action>
    **(a)** Dans `tests/Chronos.Tests/Chronos.Tests.csproj`, ajouter un second `AssemblyAttribute` à côté de
    l'existant, avec un commentaire qui dit le POURQUOI (même raison que pour les sources : `CLAUDE.md`
    interdit de localiser un assembly par son chemin de fichier, vide en publication mono-fichier, et une
    remontée de dossiers depuis `AppContext.BaseDirectory` casserait en silence) :

    ```xml
    <AssemblyAttribute Include="System.Reflection.AssemblyMetadata">
      <_Parameter1>CheminDocsChronos</_Parameter1>
      <_Parameter2>$(MSBuildProjectDirectory)\..\..\docs</_Parameter2>
    </AssemblyAttribute>
    ```

    **(b)** Créer `tests/Chronos.Tests/ContratHooksDocumenteTests.cs` avec les cas du bloc `<behavior>`.

    Extraction de la table : lire le texte, découper entre `EVENEMENTS-CABLES:debut` et
    `EVENEMENTS-CABLES:fin`, ne retenir que les lignes commençant par `|` qui ne sont ni l'en-tête ni la
    ligne de séparation, et prendre les deux premières cellules en retirant les accents graves et les
    espaces. Comparer des `HashSet<string>` en comparaison ORDINALE.

    **Écrire la XML-doc de classe qui porte la raison d'être**, dans l'esprit des gardes existantes :
    « Un document qui décrit un câblage qu'il ne décrit plus est PIRE que pas de document : il fait croire
    qu'on sait. C'est exactement le mécanisme de la panne que cette phase répare — une sémantique de
    source devenue fausse, que rien ne contredisait. Cette garde ne peut pas détecter une dérive de la
    source EXTERNE (seule une relecture humaine de la référence officielle le peut, d'où la date en tête
    du document) ; elle garantit la moitié qu'une machine peut garantir : que ce que nous écrivons
    corresponde à ce que nous installons. »

    Cette classe ne lit que des fichiers du dépôt : aucun réseau, aucun `%APPDATA%`, aucun `~/.claude`,
    aucun jeton.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~ContratHooksDocumenteTests"</automated>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - Le filtre `ContratHooksDocumenteTests` rend **0 échec** et **au moins 7 tests exécutés**
      (les six annoncés + `Chaque_ligne_documentee_porte_le_role_reellement_cable`).
    - La suite complète : **0 échec**, total **= total mesuré après 25-03 + nombre de cas ajoutés par ce
      plan**, reporté dans `25-VALIDATION.md` ; tout écart, même positif, **justifié nominativement** dans
      le SUMMARY. Attendu indicatif : **854**.
    - `grep -c 'CheminDocsChronos' tests/Chronos.Tests/Chronos.Tests.csproj` == 1.
    - `grep -c 'SessionHookInstaller.Cablage' tests/Chronos.Tests/ContratHooksDocumenteTests.cs` ≥ 1 et
      `grep -c 'CatalogueEvenementsHooks.Tous' tests/Chronos.Tests/ContratHooksDocumenteTests.cs` ≥ 1.
    - **Falsifiabilité prouvée avant commit** : dans un `git worktree` JETABLE (créé puis supprimé ; le
      dépôt principal reste propre), retirer une ligne de la table §1 du document et mesurer que
      `La_table_documentee_liste_EXACTEMENT_les_evenements_cables` ÉCHOUE — puis rétablir. Consigner la
      mesure (nombre d'échecs, nom du test) dans le SUMMARY. Une garde jamais vue rouge n'est pas une garde.
    - Suites de gardes, 0 échec : `GardesPerimetreTests` (10), `NormalisationUniqueTests` (3),
      `ServicesLayerPurityTests` (2), `CompositionRootTests` (5), `GardesDoctrineTests` (8).
    - `git diff --stat <SHA d'entrée de phase>..HEAD -- src/` est **vide** pour cette tâche : elle ne
      touche aucun code de production (SHA consigné au SUMMARY 25-01 ; `git diff --stat` nu est muet après
      commit).
    - La divergence `SessionTreatmentTracker.EstAttente` / `WaitingDeduced` est **écrite au §3** et
      **non corrigée** : `grep -c 'SessionTreatmentTracker' docs/hooks-contract.md` ≥ 1, et
      `git diff --stat <SHA d'entrée de phase>..HEAD -- src/Chronos/Services/SessionTreatmentTracker.cs`
      est vide. Elle est reportée comme **entrée de la phase 26**.
  </acceptance_criteria>

  <done>
    Le document ne peut plus diverger du câblage en silence : un test compare les deux, et il a été vu
    rouge dans un worktree jetable avant d'être admis vert.
  </done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug --nologo -v q` → 0 échec, total **= total mesuré après 25-03 + cas
  ajoutés ici** (attendu indicatif : **854**), reporté dans `25-VALIDATION.md`, tout écart justifié
  nominativement.
- `docs/hooks-contract.md` existe, ≥ 90 lignes, porte la date et la source du relevé, les trois trous
  documentaires, et la question ouverte sur la sonde de capture.
- La garde de non-dérive a été prouvée FALSIFIABLE dans un worktree jetable, et le dépôt principal est
  resté propre.
- Aucun test n'a touché `~/.claude/`, `%APPDATA%\Chronos\`, ni l'overlay en cours d'exécution.
</verification>

<success_criteria>
- Le contrat des hooks est écrit dans `docs/`, au même titre que les autres sources.
- Ce qui n'est PAS garanti y occupe autant de place que ce qui l'est, daté et sourcé.
- Une future dérive de NOTRE câblage fait échouer un test ; une future dérive de la source EXTERNE est
  rendue repérable par la date du relevé et la procédure du §9.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/25-le-contrat-d-evenements-refonde/25-04-SUMMARY.md`.
</output>
