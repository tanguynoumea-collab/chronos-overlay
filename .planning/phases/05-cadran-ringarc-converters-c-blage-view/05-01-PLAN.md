---
phase: 05-cadran-ringarc-converters-c-blage-view
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Rendering/ArcGeometry.cs
  - src/Chronos/Rendering/RampColor.cs
  - tests/Chronos.Tests/ArcGeometryTests.cs
  - tests/Chronos.Tests/RampColorTests.cs
autonomous: true
requirements: [CAD-04, CAD-07]

must_haves:
  truths:
    - "La géométrie d'arc se construit sans exception pour fraction 0, ≤0, NaN, 1 et intermédiaire"
    - "Un arc >180° bascule du bon côté (IsLargeArc calculé), jamais du mauvais"
    - "Un anneau plein (fraction ≥ 1) est un cercle continu sans micro-fente"
    - "La rampe couleur passe exactement par vert #7BB13C, ambre #EFA23A, rouge #D8503A"
  artifacts:
    - path: "src/Chronos/Rendering/ArcGeometry.cs"
      provides: "PointAt (repère Y-inversé) + Build (Empty/Ellipse/ArcSegment)"
      contains: "isLargeArc: sweep > 180.0"
    - path: "src/Chronos/Rendering/RampColor.cs"
      provides: "Interpolate(double) → Color, 3 stops linéaires"
      contains: "0x7B, 0xB1, 0x3C"
    - path: "tests/Chronos.Tests/ArcGeometryTests.cs"
      provides: "Preuve des cas limites géométriques (CAD-07)"
    - path: "tests/Chronos.Tests/RampColorTests.cs"
      provides: "Preuve des 3 stops exacts (CAD-04)"
  key_links:
    - from: "src/Chronos/Rendering/ArcGeometry.cs"
      to: "System.Windows.Media (PathGeometry/EllipseGeometry/ArcSegment)"
      via: "Build retourne une Geometry WPF"
      pattern: "return new (EllipseGeometry|PathGeometry)"
    - from: "src/Chronos/Rendering/RampColor.cs"
      to: "System.Windows.Media.Color"
      via: "Interpolate + Lerp par canal"
      pattern: "Color\\.FromRgb"
---

<objective>
Créer les DEUX briques de calcul PUR de la Phase 5 — géométrie angle→arc (`ArcGeometry`) et
interpolation RGB de la rampe utilization (`RampColor`) — avec leurs tests unitaires.

Ces fonctions statiques n'ont aucun état, aucun I/O, aucune dépendance de thread : elles sont
la seule vraie logique de la phase et doivent être blindées AVANT d'être enveloppées dans un
`Shape` (05-02) puis câblées en XAML (05-03).

Purpose: isoler la trigonométrie et l'interpolation dans du code testable trivialement, pour
que les enveloppes WPF (RingArc, converter) restent minces et sans logique.
Output: `src/Chronos/Rendering/ArcGeometry.cs`, `src/Chronos/Rendering/RampColor.cs` + 2 fichiers de tests verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-CONTEXT.md

<interfaces>
<!-- Framework de test déjà en place — l'exécuteur n'a rien à installer. -->
tests/Chronos.Tests/Chronos.Tests.csproj : cible net8.0-windows, UseWPF, xUnit 2.9.2 + Xunit.StaFact 1.1.11.
`[WpfFact]` (StaFact) = thread STA, REQUIS dès qu'on construit un type WPF (PathGeometry, EllipseGeometry, ArcSegment, SolidColorBrush).
Les structs `System.Windows.Point` / `System.Windows.Media.Color` peuvent passer en `[Fact]`, mais Build() construit des Geometry → `[WpfFact]`.

Repère verrouillé (05-CONTEXT « Rendu ») : Y-inversé, 0° = 12 h (haut), sens horaire.
point = centre + R·(sin θ, −cos θ). IsLargeArc = |sweep| > 180°. fraction ≥ 1 → anneau plein via EllipseGeometry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : ArcGeometry pure (PointAt + Build) + tests des cas limites</name>
  <files>src/Chronos/Rendering/ArcGeometry.cs, tests/Chronos.Tests/ArcGeometryTests.cs</files>
  <read_first>
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md § « Pattern 2 : ArcGeometry.Build » (code prêt à copier) + § « Common Pitfalls » 1 & 2
    - tests/Chronos.Tests/OverlayWindowConfigTests.cs (modèle d'usage de [WpfFact])
  </read_first>
  <behavior>
    - PointAt(centre(0,0), R=10, 0°) → (0, −10) à 1e-6 près (haut, 12 h).
    - PointAt(centre(0,0), R=10, 90°) → (10, 0) à 1e-6 près (droite, sens horaire).
    - Build(fraction = 0) → ReferenceEquals(Geometry.Empty) — aucune exception.
    - Build(fraction = −0.5) → Geometry.Empty. Build(fraction = double.NaN) → Geometry.Empty.
    - Build(fraction = 1.0) → est un EllipseGeometry (anneau plein, pas de cas dégénéré 360°).
    - Build(fraction = 1.5) → est un EllipseGeometry (clamp implicite du plein).
    - Build(fraction = 0.25) → PathGeometry ; l'ArcSegment a IsLargeArc == false.
    - Build(fraction = 0.75) → PathGeometry ; l'ArcSegment a IsLargeArc == true.
    - Build(fraction = 0.5) → ArcSegment IsLargeArc == false (sweep 180° : la condition est > 180, stricte).
  </behavior>
  <action>
    Créer `src/Chronos/Rendering/ArcGeometry.cs` (namespace `Chronos.Rendering`), classe statique
    copiée VERBATIM du Pattern 2 du RESEARCH — ne rien réinventer :

    ```csharp
    using System;
    using System.Windows;
    using System.Windows.Media;

    namespace Chronos.Rendering;

    /// <summary>
    /// Géométrie d'arc PURE (aucun état, aucun I/O). Repère WPF Y-inversé : 0° = 12 h (haut),
    /// sens horaire. point = centre + R·(sin θ, −cos θ).
    /// </summary>
    public static class ArcGeometry
    {
        /// <summary>Point sur le cercle à l'angle <paramref name="deg"/> (0° = 12 h, horaire).</summary>
        public static Point PointAt(Point center, double radius, double deg)
        {
            double r = deg * Math.PI / 180.0;
            return new Point(center.X + radius * Math.Sin(r),
                             center.Y - radius * Math.Cos(r)); // −cos compense Y vers le bas
        }

        /// <summary>
        /// Arc de <paramref name="fraction"/> (0..1) depuis <paramref name="startAngle"/>, horaire.
        /// fraction ≤ 0 ou NaN → Geometry.Empty (invisible, AUCUNE exception).
        /// fraction ≥ 1 → EllipseGeometry (anneau plein sans micro-fente). Sinon ArcSegment ouvert.
        /// </summary>
        public static Geometry Build(Point center, double radius, double startAngle, double fraction)
        {
            if (double.IsNaN(fraction) || fraction <= 0.0)
                return Geometry.Empty;                                   // sweep 0 : rien, pas d'exception
            if (fraction >= 1.0)
                return new EllipseGeometry(center, radius, radius);      // anneau plein sans micro-fente

            double sweep = 360.0 * fraction;                            // ∈ ]0, 360[
            var start = PointAt(center, radius, startAngle);
            var end   = PointAt(center, radius, startAngle + sweep);

            var fig = new PathFigure { StartPoint = start, IsClosed = false, IsFilled = false };
            fig.Segments.Add(new ArcSegment(
                point:          end,
                size:           new Size(radius, radius),
                rotationAngle:  0,
                isLargeArc:     sweep > 180.0,                           // PIÈGE #1 résolu
                sweepDirection: SweepDirection.Clockwise,
                isStroked:      true));
            return new PathGeometry(new[] { fig });
        }
    }
    ```

    Puis créer `tests/Chronos.Tests/ArcGeometryTests.cs` (namespace `Chronos.Tests`) couvrant
    exactement les cas de <behavior> en `[WpfFact]` (STA requis pour construire les Geometry).
    Pour lire IsLargeArc : caster `((PathGeometry)g).Figures[0].Segments[0]` en `ArcSegment`.
    Pour le type plein : `Assert.IsType<EllipseGeometry>(g)`. Pour le vide : `Assert.Same(Geometry.Empty, g)`.
    Comparer les Point avec tolérance (ex. `Assert.Equal(0.0, p.X, 6)`).
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~ArcGeometry"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~ArcGeometry"` : tous verts (≥ 8 assertions couvrant les cas ci-dessus).
    - grep `isLargeArc: sweep > 180.0` présent dans ArcGeometry.cs (piège #1 résolu).
    - grep `return new EllipseGeometry` présent (piège #2 résolu, pas de clamp 359.9°).
    - grep `return Geometry.Empty` présent (fraction ≤ 0 / NaN sans exception).
  </acceptance_criteria>
  <done>Build vert 0/1/plein/intermédiaire couverts par test ; aucun clamp 359.9° ; aucune exception sur fraction dégénérée.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : RampColor pure (interpolation 3 stops) + tests des bornes exactes</name>
  <files>src/Chronos/Rendering/RampColor.cs, tests/Chronos.Tests/RampColorTests.cs</files>
  <read_first>
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md § « Pattern 5 : RampColor.Interpolate » (code prêt à copier + bornes à asserter)
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-CONTEXT.md § tokens (rampe verrouillée)
  </read_first>
  <behavior>
    - Interpolate(0.0)  → Color.FromRgb(0x7B, 0xB1, 0x3C) exact (vert).
    - Interpolate(0.55) → Color.FromRgb(0xEF, 0xA2, 0x3A) exact (ambre, stop médian).
    - Interpolate(1.0)  → Color.FromRgb(0xD8, 0x50, 0x3A) exact (rouge).
    - Interpolate(−0.2) → vert (clamp bas). Interpolate(1.4) → rouge (clamp haut).
    - Interpolate(0.275) (mi-segment vert→ambre) → R strictement entre 0x7B et 0xEF, ni vert ni ambre exact.
  </behavior>
  <action>
    Créer `src/Chronos/Rendering/RampColor.cs` (namespace `Chronos.Rendering`) copié VERBATIM du
    Pattern 5 du RESEARCH. Stop ambre verrouillé à `AmberStop = 0.55` (constante unique, ajustable en UAT) :

    ```csharp
    using System;
    using System.Windows.Media;

    namespace Chronos.Rendering;

    /// <summary>
    /// Rampe utilization → couleur. 3 stops verrouillés :
    ///   0.00 → vert  #7BB13C   0.55 → ambre #EFA23A   1.00 → rouge #D8503A
    /// Interpolation LINÉAIRE par canal sur chaque segment. Fonction PURE (testable).
    /// </summary>
    public static class RampColor
    {
        private const double AmberStop = 0.55;
        private static readonly Color Green = Color.FromRgb(0x7B, 0xB1, 0x3C);
        private static readonly Color Amber = Color.FromRgb(0xEF, 0xA2, 0x3A);
        private static readonly Color Red   = Color.FromRgb(0xD8, 0x50, 0x3A);

        public static Color Interpolate(double u)
        {
            u = Math.Clamp(u, 0.0, 1.0);
            return u <= AmberStop
                ? Lerp(Green, Amber, u / AmberStop)
                : Lerp(Amber, Red, (u - AmberStop) / (1.0 - AmberStop));
        }

        private static Color Lerp(Color a, Color b, double t)
        {
            t = Math.Clamp(t, 0.0, 1.0);
            return Color.FromRgb(
                (byte)Math.Round(a.R + (b.R - a.R) * t),
                (byte)Math.Round(a.G + (b.G - a.G) * t),
                (byte)Math.Round(a.B + (b.B - a.B) * t));
        }
    }
    ```

    Puis `tests/Chronos.Tests/RampColorTests.cs` (namespace `Chronos.Tests`). `Color` est un struct →
    `[Fact]` suffit (pas de thread WPF). Asserter les 3 bornes exactes via
    `Assert.Equal(Color.FromRgb(0x7B,0xB1,0x3C), RampColor.Interpolate(0))` etc., les 2 clamps, et
    le point mi-segment (R entre les bornes).
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~RampColor"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~RampColor"` : tous verts (3 stops exacts + 2 clamps + mi-segment).
    - grep `0x7B, 0xB1, 0x3C` ET `0xEF, 0xA2, 0x3A` ET `0xD8, 0x50, 0x3A` présents dans RampColor.cs (tokens exacts).
    - grep `AmberStop = 0.55` présent (constante unique).
  </acceptance_criteria>
  <done>Les 3 couleurs de la maquette sont atteintes au pixel près ; clamp hors [0,1] correct ; interpolation continue prouvée.</done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug` : les 41 tests existants + les nouveaux (ArcGeometry, RampColor) tous verts.
- Aucune régression : aucun fichier hors `Rendering/` et `tests/` n'est touché.
</verification>

<success_criteria>
- ArcGeometry.Build gère Empty (0/≤0/NaN), EllipseGeometry (≥1), ArcSegment avec IsLargeArc correct — prouvé par test.
- RampColor.Interpolate passe exactement par les 3 stops verrouillés — prouvé par test.
- Deux fonctions PURES, réutilisables telles quelles par 05-02 (RingArc, TickRing, converter).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-01-SUMMARY.md`.
</output>
