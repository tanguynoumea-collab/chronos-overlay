---
phase: 05-cadran-ringarc-converters-c-blage-view
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/Chronos/App.xaml
  - src/Chronos/Views/MainWindow.xaml
  - tests/Chronos.Tests/CadranBindingTests.cs
autonomous: false
requirements: [CAD-01, CAD-02, CAD-03, CAD-05, CAD-06, DAT-08, ROB-01]

must_haves:
  truths:
    - "L'utilisateur voit un cadran sombre gradué avec deux arcs concentriques (5 h extérieur, hebdo intérieur)"
    - "La longueur de chaque arc reflète FractionRemaining et se vide vers le reset"
    - "Un compte à rebours texte des deux fenêtres s'affiche au centre"
    - "Les signaux « estimée » et « quota épuisé » sont PAR FENÊTRE : chaque anneau porte sa propre indication (5 h et hebdo indépendantes)"
    - "Une donnée périmée est signalée en texte secondaire (« données périmées »), lié à IsStale"
    - "Deux sources indisponibles → texte « données indisponibles », zéro crash"
  artifacts:
    - path: "src/Chronos/App.xaml"
      provides: "ResourceDictionary : tokens couleur verrouillés (dont les DEUX nuances secondaires #A9A8B2 et #C7C6D0) + converters instanciés"
      contains: "UtilizationToBrushConverter"
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "Composition complète du cadran (fond/rim/ticks/pistes/arcs/textes/badges par fenêtre/staleness)"
      contains: "FiveHour.FractionRemaining"
    - path: "tests/Chronos.Tests/CadranBindingTests.cs"
      provides: "Smoke [WpfFact] : MainWindow + fake VM 4 états (dont fiabilité mixte), aucun crash, éléments présents"
  key_links:
    - from: "src/Chronos/Views/MainWindow.xaml (arc extérieur)"
      to: "MainViewModel.FiveHour.FractionRemaining + .Utilization"
      via: "Binding Fraction + Stroke via converter"
      pattern: "FiveHour\\.(FractionRemaining|Utilization)"
    - from: "src/Chronos/Views/MainWindow.xaml (arc intérieur)"
      to: "MainViewModel.SevenDay.FractionRemaining + .Utilization"
      via: "Binding Fraction + Stroke via converter"
      pattern: "SevenDay\\.(FractionRemaining|Utilization)"
    - from: "src/Chronos/Views/MainWindow.xaml (badges 5 h)"
      to: "FiveHour.IsEstimated / FiveHour.Exhausted"
      via: "Binding Visibility (BoolToVis)"
      pattern: "FiveHour\\.(IsEstimated|Exhausted)"
    - from: "src/Chronos/Views/MainWindow.xaml (badges hebdo)"
      to: "SevenDay.IsEstimated / SevenDay.Exhausted"
      via: "Binding Visibility (BoolToVis)"
      pattern: "SevenDay\\.(IsEstimated|Exhausted)"
    - from: "src/Chronos/Views/MainWindow.xaml (centre)"
      to: "FiveHour.CountdownText / SevenDay.CountdownText / DataUnavailable / IsStale"
      via: "Binding TextBlock + Visibility"
      pattern: "CountdownText|DataUnavailable|IsStale"
---

<objective>
Assembler le cadran complet dans `MainWindow.xaml` en instanciant les contrôles de 05-02, en
bindant sur la surface déjà éprouvée du `MainViewModel` (Phase 4), et en centralisant les tokens
de design verrouillés dans `App.xaml`. Remplacer le placeholder Ellipse par le vrai cadran, puis
prouver par un smoke test que la fenêtre se construit dans quatre états (exact / estimé /
indisponible / fiabilité mixte) sans crash. Terminer par une vérification visuelle humaine
(fidélité maquette + arcs temps réel).

Les signaux de fiabilité et d'épuisement sont **PAR FENÊTRE** (le `CompositeUsageProvider` retient
la meilleure source par fenêtre — 5 h et hebdo peuvent avoir des provenances différentes). Chaque
anneau porte donc sa propre indication « estimée » / « quota épuisé », liée à ses propres
`FiveHour.IsEstimated/Exhausted` et `SevenDay.IsEstimated/Exhausted`. La staleness globale
(`MainViewModel.IsStale`, décision verrouillée « signalée en texte secondaire ») est affichée en
mention secondaire dédiée.

Aucune logique en code-behind : uniquement des bindings + converters. `MainWindow.xaml.cs`
(placement + StartClock, Phase 1/4) reste INCHANGÉ.

Purpose: livrer le cadran visible et temps réel — le cœur observable de la Phase 5.
Output: `App.xaml` (tokens + converters), `MainWindow.xaml` (cadran), `CadranBindingTests.cs` (smoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-CONTEXT.md
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-02-SUMMARY.md
@src/Chronos/Views/MainWindow.xaml
@src/Chronos/Views/MainWindow.xaml.cs

<interfaces>
<!-- Contrôles livrés 05-02 (Wave 2) — à instancier en XAML (xmlns:ctrl / xmlns:conv). -->
namespace Chronos.Controls;
  class RingArc : Shape  → DP double Fraction (0..1), double StartAngle, double Radius ; Stroke/StrokeThickness hérités.
  class TickRing : Shape → DP int Count, double Radius, double TickLength, double StartAngle ; Stroke/StrokeThickness hérités.
namespace Chronos.Converters;
  class UtilizationToBrushConverter : IValueConverter  → double? Utilization → Brush (rampe/gris/neutre).

<!-- Surface de binding du DataContext (MainViewModel, Phase 4) — code réel, NE RIEN ajouter côté VM. -->
MainViewModel : WindowGaugeViewModel FiveHour ; WindowGaugeViewModel SevenDay ;
                bool DataUnavailable ; DateTimeOffset? CapturedAt ; bool IsStale.
WindowGaugeViewModel (sur FiveHour ET SevenDay — signaux INDÉPENDANTS par fenêtre) :
                double FractionRemaining (0..1) → RingArc.Fraction ;
                double? Utilization → RingArc.Stroke via UtilizationToBrushConverter ;
                string CountdownText (FR, ex. « 3 h 42 ») → TextBlock centre ;
                bool Exhausted → « quota épuisé » (par fenêtre) ; bool IsEstimated → badge « estimée » (par fenêtre).
Toutes [ObservableProperty] → binding réactif ; le tick 1 s (StartClock, déjà câblé) rafraîchit sans I/O.

<!-- Tokens de design VERROUILLÉS (05-CONTEXT — valeurs EXACTES, à mettre en ressources) -->
Fond cadran #16151B (semi-transparent, ex. #E616151B), rim #2C2B34.
Ticks : mineurs #34333D, majeurs #46454F. Pistes : 5 h #2A2932, hebdo #26252E.
Texte principal #F4F2EC. Secondaire : DEUX nuances verrouillées, chacune un usage distinct —
  #C7C6D0 (le plus clair) → countdown hebdo secondaire ;
  #A9A8B2 (le plus sourd) → badges/mentions annexes (estimée, épuisé, indisponible, périmée).
Gris épuisé #5A5960 (via converter).

<!-- Modèles pour le smoke test (types immuables, ApplySnapshot est internal → InternalsVisibleTo OK) -->
UsageSnapshot { WindowState FiveHour; WindowState SevenDay; DateTimeOffset? SourceCapturedAt; }  + static Empty.
WindowState { WindowKind Kind; double? Utilization; DateTimeOffset? ResetsAt; SourceReliability Reliability; bool Exhausted; }
             + static Unavailable(WindowKind).
Construction VM (cf. OverlayWindowConfigTests / MainViewModelTests) :
  new MainViewModel(new RefreshOrchestrator(new FakeUsageProvider(), ChronosPaths.Default(), RefreshOptions.Default),
                    new FakeUiDispatcher(), new FakeClock(DateTimeOffset.UtcNow));
  vm.ApplySnapshot(snap); puis new MainWindow(vm, new TopmostGuard());
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Tokens dans App.xaml + composition du cadran dans MainWindow.xaml</name>
  <files>src/Chronos/App.xaml, src/Chronos/Views/MainWindow.xaml</files>
  <read_first>
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md § « Pattern 6 : Layout du cadran » (squelette empilé + recommandations rayons) + § « Binding — surface exacte disponible »
    - src/Chronos/Views/MainWindow.xaml (placeholder à remplacer) et src/Chronos/App.xaml (Application.Resources vide à remplir)
    - src/Chronos/Views/MainWindow.xaml.cs (NE PAS MODIFIER — juste comprendre que DataContext = MainViewModel)
  </read_first>
  <action>
    **1. App.xaml — tokens + converters en ressources globales (source unique de vérité).**
    Remplacer `<Application.Resources/>` par un ResourceDictionary contenant :
    - Les SolidColorBrush des tokens VERROUILLÉS avec ces clés exactes (valeurs hex ci-dessus) :
      `FondCadran` (#E616151B), `Rim` (#2C2B34), `TickMineur` (#34333D), `TickMajeur` (#46454F),
      `Piste5h` (#2A2932), `PisteHebdo` (#26252E), `TextePrincipal` (#F4F2EC),
      `TexteSecondaire` (#A9A8B2), `TexteSecondaireClair` (#C7C6D0).
      **Choix documenté (résout le token secondaire inutilisé) :** les DEUX nuances secondaires
      verrouillées de la maquette servent chacune un usage distinct — `TexteSecondaireClair`
      (#C7C6D0, plus lumineux) pour le countdown hebdo (donnée « vivante » mais secondaire), et
      `TexteSecondaire` (#A9A8B2, plus sourd) pour les badges/mentions annexes (estimée, épuisé,
      indisponible, périmée). Aucune nuance verrouillée n'est laissée orpheline.
    - Le converter : `<conv:UtilizationToBrushConverter x:Key="UtilBrush"/>` (déclarer `xmlns:conv="clr-namespace:Chronos.Converters"`).
    - `<BooleanToVisibilityConverter x:Key="BoolToVis"/>` (WPF intégré — ne PAS écrire de converter maison).

    **2. MainWindow.xaml — remplacer le `<Grid>` placeholder par le cadran complet** (empilement du
    Pattern 6, du fond vers le premier plan). Déclarer `xmlns:ctrl="clr-namespace:Chronos.Controls"`.
    Structure (Grid 220×220, centre 110,110 ; rayons discrétionnaires RExt≈90 / RInt≈70, thickness≈11) :

    - `<Ellipse>` Fill={StaticResource FondCadran}, Stroke={StaticResource Rim}, StrokeThickness=2 (fond + rim).
    - `<ctrl:TickRing Count="60" Radius="96" TickLength="5" Stroke="{StaticResource TickMineur}" StrokeThickness="1"/>` (mineurs).
    - `<ctrl:TickRing Count="12" Radius="96" TickLength="9" Stroke="{StaticResource TickMajeur}" StrokeThickness="2"/>` (majeurs).
    - Piste 5 h : `<ctrl:RingArc Fraction="1" Radius="90" Stroke="{StaticResource Piste5h}" StrokeThickness="11"/>`.
    - Piste hebdo : `<ctrl:RingArc Fraction="1" Radius="70" Stroke="{StaticResource PisteHebdo}" StrokeThickness="11"/>`.
    - Arc valeur 5 h (extérieur, CAD-02), nommé pour le smoke :
      ```xml
      <ctrl:RingArc x:Name="ArcCinqHeures" Radius="90" StrokeThickness="11"
                    StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                    Fraction="{Binding FiveHour.FractionRemaining}"
                    Stroke="{Binding FiveHour.Utilization, Converter={StaticResource UtilBrush}}"/>
      ```
    - Arc valeur hebdo (intérieur, CAD-03), `x:Name="ArcHebdo"`, `Radius="70"`, bindé sur `SevenDay.FractionRemaining` / `SevenDay.Utilization`.
    - Bloc central `<StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">` — **signaux PAR FENÊTRE** (la fiabilité/épuisement diffère entre 5 h et hebdo car le CompositeUsageProvider choisit la meilleure source par fenêtre) :
      - **Groupe 5 h :**
        - `<TextBlock x:Name="TexteCinqHeures" Text="{Binding FiveHour.CountdownText}" Foreground="{StaticResource TextePrincipal}" .../>` (CAD-06 principal).
        - Badge estimée 5 h (DAT-08) : `<TextBlock x:Name="BadgeEstimeeCinqHeures" Text="estimée" Visibility="{Binding FiveHour.IsEstimated, Converter={StaticResource BoolToVis}}" Foreground="{StaticResource TexteSecondaire}"/>`.
        - Épuisé 5 h (CAD-05) : `<TextBlock x:Name="TexteEpuiseCinqHeures" Text="quota épuisé" Visibility="{Binding FiveHour.Exhausted, Converter={StaticResource BoolToVis}}" Foreground="{StaticResource TexteSecondaire}"/>`.
      - **Groupe hebdo :**
        - `<TextBlock x:Name="TexteHebdo" Text="{Binding SevenDay.CountdownText}" Foreground="{StaticResource TexteSecondaireClair}" .../>` (CAD-06 secondaire — nuance #C7C6D0).
        - Badge estimée hebdo (DAT-08) : `<TextBlock x:Name="BadgeEstimeeHebdo" Text="estimée" Visibility="{Binding SevenDay.IsEstimated, Converter={StaticResource BoolToVis}}" Foreground="{StaticResource TexteSecondaire}"/>`.
        - Épuisé hebdo (CAD-05) : `<TextBlock x:Name="TexteEpuiseHebdo" Text="quota épuisé" Visibility="{Binding SevenDay.Exhausted, Converter={StaticResource BoolToVis}}" Foreground="{StaticResource TexteSecondaire}"/>`.
      - **Mentions globales :**
        - Staleness (DAT-08, décision verrouillée « staleness signalée en texte secondaire ») : `<TextBlock x:Name="TexteStale" Text="données périmées" Visibility="{Binding IsStale, Converter={StaticResource BoolToVis}}" Foreground="{StaticResource TexteSecondaire}"/>`.
        - Indisponible (ROB-01) : `<TextBlock x:Name="TexteIndisponible" Text="données indisponibles" Visibility="{Binding DataUnavailable, Converter={StaticResource BoolToVis}}" Foreground="{StaticResource TexteSecondaire}"/>`.

    Contraintes : AUCUN code-behind ajouté (MainWindow.xaml.cs inchangé) ; AUCUNE Storyboard / blur /
    shadow (rendu logiciel) ; tailles de police, regroupement visuel et position exacte des badges
    par fenêtre = discrétion (caler en UAT). Le Grid racine peut garder son fond transparent hérité
    (pas d'interaction en Phase 5).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` : succès (XAML compile, aucun binding vers un membre inexistant).
    - grep tokens exacts dans App.xaml : `16151B`, `2C2B34`, `34333D`, `46454F`, `2A2932`, `26252E`, `F4F2EC`, `A9A8B2`, `C7C6D0` (CAD-01 + les DEUX nuances secondaires utilisées).
    - grep MainWindow.xaml : `FiveHour.FractionRemaining` ET `SevenDay.FractionRemaining` (CAD-02/03).
    - grep MainWindow.xaml : `FiveHour.CountdownText` ET `SevenDay.CountdownText` (CAD-06).
    - grep MainWindow.xaml : `FiveHour.IsEstimated` ET `SevenDay.IsEstimated` (DAT-08 par fenêtre).
    - grep MainWindow.xaml : `FiveHour.Exhausted` ET `SevenDay.Exhausted` (CAD-05 par fenêtre).
    - grep MainWindow.xaml : `IsStale` (staleness en texte secondaire, décision verrouillée) ET `DataUnavailable` (ROB-01).
    - grep MainWindow.xaml : `TexteSecondaireClair` (countdown hebdo) ET `TexteSecondaire` (badges/mentions) — les deux nuances effectivement référencées.
    - grep MainWindow.xaml : `ctrl:RingArc` ET `ctrl:TickRing` présents ; plus aucune trace du placeholder `#CC1E1E1E`.
    - `MainWindow.xaml.cs` inchangé (aucune logique métier ajoutée).
  </acceptance_criteria>
  <done>Le cadran complet est composé : fond/rim, ticks, 2 pistes, 2 arcs de valeur bindés, textes centraux, badges « estimée »/« épuisé » PAR FENÊTRE (5 h et hebdo indépendants), mention staleness et indisponible ; les deux nuances secondaires #A9A8B2/#C7C6D0 servent chacune un usage ; build vert.</done>
</task>

<task type="auto">
  <name>Task 2 : Smoke test [WpfFact] — MainWindow dans 4 états, aucun crash (DAT-08, ROB-01)</name>
  <files>tests/Chronos.Tests/CadranBindingTests.cs</files>
  <read_first>
    - tests/Chronos.Tests/OverlayWindowConfigTests.cs (construction MainWindow + [WpfFact])
    - tests/Chronos.Tests/MainViewModelTests.cs (fabrication de WindowState/UsageSnapshot + ApplySnapshot)
    - tests/Chronos.Tests/Fakes/ (FakeUsageProvider, FakeUiDispatcher, FakeClock — réutiliser tels quels)
  </read_first>
  <behavior>
    - État EXACT (deux fenêtres lisibles, util 0.3/0.6) : MainWindow se construit, aucune exception ;
      DataContext est le VM ; FindName("ArcCinqHeures")/("ArcHebdo") non nuls ; DataUnavailable == false.
    - État ESTIMÉ (FiveHour Reliability=Estimated, Utilization=null) : construction sans crash ;
      vm.FiveHour.IsEstimated == true ; le converter sur Utilization null n'a levé aucune exception.
    - État INDISPONIBLE (UsageSnapshot.Empty : deux fenêtres Unavailable) : construction sans crash ;
      vm.DataUnavailable == true (ROB-01) ; FindName("TexteIndisponible") non nul.
    - État FIABILITÉ MIXTE (5 h Exact avec Utilization=0.4, hebdo Estimated avec Utilization=null) :
      construction sans crash ; vm.FiveHour.IsEstimated == false ET vm.SevenDay.IsEstimated == true —
      prouve que les badges « estimée » sont bien PAR FENÊTRE (affichage différencié). Les deux
      TextBlocks nommés BadgeEstimeeCinqHeures/BadgeEstimeeHebdo existent (FindName non nuls).
  </behavior>
  <action>
    Créer `tests/Chronos.Tests/CadranBindingTests.cs` (namespace `Chronos.Tests`), tests `[WpfFact]`
    (STA requis : construction de MainWindow + brushes). Réutiliser un helper local qui fabrique un
    `MainViewModel` (cf. OverlayWindowConfigTests : orchestrateur non démarré, aucun I/O), appelle
    `vm.ApplySnapshot(snap)` avec le snapshot voulu, construit `new MainWindow(vm, new TopmostGuard())`,
    force le rendu de layout si utile (`fenetre.Measure(new Size(220,220)); fenetre.Arrange(new Rect(0,0,220,220))`),
    puis asserte. Quatre snapshots :

    - Exact : deux `WindowState { Kind, Reliability = SourceReliability.Exact, Utilization = 0.3/0.6, ResetsAt = now + 2h/3j }`.
    - Estimé : `FiveHour = new WindowState { Kind = WindowKind.FiveHour, Reliability = SourceReliability.Estimated, Utilization = null, ResetsAt = null }`,
      `SevenDay = WindowState.Unavailable(WindowKind.SevenDay)`.
    - Indisponible : `UsageSnapshot.Empty`.
    - Fiabilité mixte : `FiveHour = new WindowState { Kind = WindowKind.FiveHour, Reliability = SourceReliability.Exact, Utilization = 0.4, ResetsAt = now + 2h }`,
      `SevenDay = new WindowState { Kind = WindowKind.SevenDay, Reliability = SourceReliability.Estimated, Utilization = null, ResetsAt = now + 3j }`.

    Asserter la non-exception (le simple fait d'arriver au bout), les propriétés VM (IsEstimated,
    Exhausted, DataUnavailable) et la présence des éléments nommés via `fenetre.FindName("ArcCinqHeures")`,
    `FindName("BadgeEstimeeCinqHeures")`, `FindName("BadgeEstimeeHebdo")`, etc. Pour le cas mixte,
    asserter explicitement `Assert.False(vm.FiveHour.IsEstimated)` ET `Assert.True(vm.SevenDay.IsEstimated)`
    (preuve que le signal est indépendant par fenêtre). Ce test verrouille DAT-08 (badge par fenêtre +
    converter tolère Utilization null sans mentir/crasher) et ROB-01 (deux Unavailable → cadran + texte,
    zéro crash).
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~Cadran"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~Cadran"` : tous verts (4 états, aucune exception).
    - Le test construit réellement `MainWindow` (grep `new MainWindow`) et asserte `FindName("ArcCinqHeures")` non nul.
    - Cas estimé avec `Utilization = null` couvert (preuve que le converter ne crashe pas sur donnée absente).
    - Cas `UsageSnapshot.Empty` couvert avec `Assert.True(vm.DataUnavailable)`.
    - Cas fiabilité mixte couvert avec `Assert.False(vm.FiveHour.IsEstimated)` ET `Assert.True(vm.SevenDay.IsEstimated)` (badges « estimée » différenciés par fenêtre) ; `FindName("BadgeEstimeeHebdo")` non nul.
  </acceptance_criteria>
  <done>Smoke vert : la fenêtre se construit et se bind dans les 4 états sans crash ; DAT-08 (signaux par fenêtre) et ROB-01 prouvés automatiquement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : Checkpoint visuel — fidélité maquette + cadran temps réel (UAT)</name>
  <files>(aucun — vérification humaine, aucune modification de code)</files>
  <action>
    Précondition automatisée : la suite complète doit être verte (tâches 1 & 2 livrées). Puis lancer
    l'overlay et guider l'utilisateur dans la vérification visuelle décrite dans <how-to-verify>.
    Cette tâche NE modifie aucun fichier : elle valide le rendu produit par les tâches 1 et 2 et
    recueille l'approbation humaine (fidélité maquette + comportement temps réel). En cas d'écart,
    consigner les retours pour une passe de gap-closure.
  </action>
  <what-built>
    Le cadran complet Chronos : cadran sombre gradué (ticks mineurs/majeurs), arc extérieur 5 h +
    arc intérieur hebdo dont la longueur reflète le temps restant, couleur vert→ambre→rouge selon
    utilization (gris si épuisé), compte à rebours central des deux fenêtres (hebdo en nuance claire
    #C7C6D0), badges « estimée » et « quota épuisé » INDÉPENDANTS par fenêtre, mention « données
    périmées » (staleness) et état « données indisponibles ». Build + smoke test (4 états) verts.
  </what-built>
  <how-to-verify>
    1. Lancer l'overlay : `dotnet run --project src/Chronos/Chronos.csproj -c Debug`.
    2. Le cadran apparaît en haut à droite du bureau (220×220), sombre et semi-transparent, gradué.
    3. Vérifier la FIDÉLITÉ MAQUETTE : fond sombre #16151B, rim discret, deux anneaux concentriques
       (extérieur = 5 h, intérieur = hebdo), couleurs conformes (vert/ambre/rouge selon la charge).
       Le countdown hebdo utilise la nuance secondaire claire (#C7C6D0), les badges/mentions la
       nuance plus sourde (#A9A8B2) — les deux se distinguent à l'œil.
    4. Observer ~10 s : le compte à rebours central décrémente à la seconde, et la longueur des arcs
       progresse (se vide vers le reset) sans saccade — CAD-02/03 + RAF-03.
    5. Fiabilité PAR FENÊTRE : si une seule source est en repli JSONL (ex. hebdo estimée, 5 h exacte),
       le badge « estimée » n'apparaît QUE près du countdown concerné, et l'arc de cette fenêtre
       N'invente PAS de couleur d'utilization (couleur neutre). L'autre anneau garde sa couleur de
       rampe. De même « quota épuisé » ne s'affiche que pour la fenêtre réellement épuisée — DAT-08/CAD-05.
    6. Staleness : si la donnée est périmée (> 2 min sans capture), la mention « données périmées »
       apparaît en texte secondaire. Si aucune source : « données indisponibles » s'affiche, le
       cadran reste visible (pistes vides), aucun crash — DAT-08 + ROB-01.
    7. Vérifier qu'aucun arc ne « saute » du mauvais côté quand il dépasse la moitié (piège IsLargeArc)
       et que l'anneau plein en début de fenêtre est continu (pas de micro-fente).
  </how-to-verify>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug</automated>
  </verify>
  <resume-signal>Répondre « approved » si le cadran est fidèle et temps réel (badges par fenêtre corrects, staleness visible), sinon décrire les écarts (couleurs, proportions, sens de vidage, badges par fenêtre, staleness).</resume-signal>
  <done>Utilisateur répond « approved » : cadran fidèle à la maquette, arcs et compte à rebours se mettant à jour à la seconde, signaux « estimée »/« épuisé » corrects par fenêtre, staleness signalée.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` : succès (XAML + bindings valides).
- `dotnet test Chronos.sln -c Debug` : suite complète verte (41 existants + ArcGeometry + RampColor + UtilizationToBrush + Cadran 4 états).
- Vérification humaine « approved » sur la fidélité maquette, les signaux par fenêtre, la staleness et le comportement temps réel.
</verification>

<success_criteria>
- Cadran sombre gradué à deux arcs (5 h extérieur, hebdo intérieur) dont la longueur reflète le temps restant — CAD-01/02/03.
- Couleur vert→ambre→rouge selon utilization, gris + « quota épuisé » à ≥1, PAR FENÊTRE — CAD-04/05.
- Compte à rebours central des deux fenêtres (hebdo en nuance secondaire claire #C7C6D0) — CAD-06.
- Repli marqué « estimée » PAR FENÊTRE sans couleur inventée ; staleness signalée en texte secondaire ; « données indisponibles » sans crash — DAT-08/ROB-01.
- Les deux nuances secondaires verrouillées (#A9A8B2, #C7C6D0) sont chacune utilisées (aucun token orphelin).
- Aucune logique en code-behind ; tout en bindings + converters.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-03-SUMMARY.md`.
</output>
</content>
</invoke>
