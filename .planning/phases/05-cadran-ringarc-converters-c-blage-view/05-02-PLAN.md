---
phase: 05-cadran-ringarc-converters-c-blage-view
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/Chronos/Controls/RingArc.cs
  - src/Chronos/Controls/TickRing.cs
  - src/Chronos/Converters/UtilizationToBrushConverter.cs
  - tests/Chronos.Tests/UtilizationToBrushConverterTests.cs
autonomous: true
requirements: [CAD-01, CAD-04, CAD-05, CAD-07]

must_haves:
  truths:
    - "RingArc est un Shape réutilisable dont la géométrie se redessine seule au changement de Fraction (AffectsRender)"
    - "RingArc à Fraction=1 sert de piste (anneau plein continu)"
    - "TickRing rend toutes les graduations en UN seul visuel (GeometryGroup), pas 60 éléments"
    - "Le converter renvoie la rampe pour [0,1[, gris #5A5960 pour ≥1, neutre pour null (jamais de couleur inventée)"
  artifacts:
    - path: "src/Chronos/Controls/RingArc.cs"
      provides: "Shape : DP Fraction/StartAngle/Radius (AffectsRender), DefiningGeometry → ArcGeometry.Build"
      contains: "FrameworkPropertyMetadataOptions.AffectsRender"
    - path: "src/Chronos/Controls/TickRing.cs"
      provides: "Shape : graduations en une passe (GeometryGroup de LineGeometry)"
      contains: "new GeometryGroup"
    - path: "src/Chronos/Converters/UtilizationToBrushConverter.cs"
      provides: "double? → Brush (rampe / gris épuisé / neutre null)"
      contains: "0x5A, 0x59, 0x60"
    - path: "tests/Chronos.Tests/UtilizationToBrushConverterTests.cs"
      provides: "Preuve des 3 branches sémantiques (CAD-04/05, honnêteté null)"
  key_links:
    - from: "src/Chronos/Controls/RingArc.cs"
      to: "src/Chronos/Rendering/ArcGeometry.cs"
      via: "DefiningGeometry appelle ArcGeometry.Build"
      pattern: "ArcGeometry\\.Build"
    - from: "src/Chronos/Converters/UtilizationToBrushConverter.cs"
      to: "src/Chronos/Rendering/RampColor.cs"
      via: "Convert appelle RampColor.Interpolate"
      pattern: "RampColor\\.Interpolate"
---

<objective>
Envelopper les fonctions pures de 05-01 dans trois enveloppes WPF minces et réutilisables :
`RingArc` (Shape d'arc, CAD-07), `TickRing` (Shape de graduations, CAD-01) et
`UtilizationToBrushConverter` (couleur d'arc, CAD-04/05 + honnêteté sur donnée absente).

Ces trois classes ne contiennent quasi aucune logique : elles déclarent des DependencyProperty
avec `AffectsRender` et délèguent à `ArcGeometry` / `RampColor`. Elles sont le vocabulaire visuel
que le XAML de 05-03 assemblera.

Purpose: fournir des contrôles paramétrés (angle, rayon, couleur) et un converter mono-entrée,
prêts à instancier dans MainWindow.xaml sans aucune logique en code-behind.
Output: `Controls/RingArc.cs`, `Controls/TickRing.cs`, `Converters/UtilizationToBrushConverter.cs` + tests du converter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md
@.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-01-SUMMARY.md

<interfaces>
<!-- Produits par 05-01 (Wave 1) — à consommer directement, ne pas réécrire. -->
namespace Chronos.Rendering;
public static class ArcGeometry {
    public static Point PointAt(Point center, double radius, double deg);
    public static Geometry Build(Point center, double radius, double startAngle, double fraction); // Empty / Ellipse / ArcSegment
}
public static class RampColor {
    public static Color Interpolate(double u); // 0→#7BB13C, 0.55→#EFA23A, 1→#D8503A (clamp)
}

<!-- Tokens verrouillés utilisés ici (05-CONTEXT) -->
Gris épuisé (CAD-05) : #5A5960 = (0x5A,0x59,0x60). Neutre null (piste douce) : #2A2932 = (0x2A,0x29,0x32).
Ticks : mineurs #34333D, majeurs #46454F (posés en XAML côté Stroke en 05-03, pas ici).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : RingArc — Shape dérivé, DP Fraction/StartAngle/Radius (CAD-07)</name>
  <files>src/Chronos/Controls/RingArc.cs</files>
  <read_first>
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md § « Pattern 1 : RingArc » (code prêt à copier) + § « State of the Art » (pourquoi DP Fraction 0..1 et non EndAngle)
    - src/Chronos/Rendering/ArcGeometry.cs (livré 05-01 — signature de Build)
  </read_first>
  <action>
    Créer `src/Chronos/Controls/RingArc.cs` (namespace `Chronos.Controls`) copié VERBATIM du Pattern 1 :

    ```csharp
    using System.Windows;
    using System.Windows.Media;
    using System.Windows.Shapes;
    using Chronos.Rendering;

    namespace Chronos.Controls;

    /// <summary>
    /// Arc d'anneau réutilisable (CAD-07). Dérive de Shape : la géométrie est le pur produit
    /// des DP (AffectsRender → redessin auto au changement de binding, pas d'animation).
    /// Fraction 0..1 = longueur d'arc (1 = anneau plein). StartAngle = origine (0° = 12 h).
    /// L'épaisseur vient de StrokeThickness (hérité), la couleur de Stroke (hérité).
    /// Sert AUSSI de « piste » : une instance Fraction=1, Stroke = couleur de piste.
    /// </summary>
    public sealed class RingArc : Shape
    {
        public static readonly DependencyProperty FractionProperty =
            DependencyProperty.Register(nameof(Fraction), typeof(double), typeof(RingArc),
                new FrameworkPropertyMetadata(0d, FrameworkPropertyMetadataOptions.AffectsRender));

        public static readonly DependencyProperty StartAngleProperty =
            DependencyProperty.Register(nameof(StartAngle), typeof(double), typeof(RingArc),
                new FrameworkPropertyMetadata(0d, FrameworkPropertyMetadataOptions.AffectsRender));

        public static readonly DependencyProperty RadiusProperty =
            DependencyProperty.Register(nameof(Radius), typeof(double), typeof(RingArc),
                new FrameworkPropertyMetadata(90d, FrameworkPropertyMetadataOptions.AffectsRender));

        public double Fraction   { get => (double)GetValue(FractionProperty);   set => SetValue(FractionProperty, value); }
        public double StartAngle { get => (double)GetValue(StartAngleProperty); set => SetValue(StartAngleProperty, value); }
        public double Radius     { get => (double)GetValue(RadiusProperty);     set => SetValue(RadiusProperty, value); }

        protected override Geometry DefiningGeometry =>
            ArcGeometry.Build(new Point(ActualWidth / 2, ActualHeight / 2), Radius, StartAngle, Fraction);
    }
    ```

    Ne PAS ajouter de logique, de converter d'angle, ni de géométrie donut : l'épaisseur = StrokeThickness
    (posé en XAML), la couleur = Stroke (via converter en XAML). Les trois DP portent `AffectsRender`
    pour que le tick 1 s (RAF-03) redessine sans animation.
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` : succès (0 erreur).
    - grep `class RingArc : Shape` présent (Shape, PAS UserControl — décision verrouillée).
    - grep `FrameworkPropertyMetadataOptions.AffectsRender` présent sur les 3 DP.
    - grep `ArcGeometry.Build` présent dans DefiningGeometry (délègue à la math testée 05-01).
    - Aucune DP `EndAngle` (on binde Fraction 0..1, pas un angle en degrés).
  </acceptance_criteria>
  <done>RingArc compile ; expose Fraction/StartAngle/Radius en DP AffectsRender ; DefiningGeometry délègue à ArcGeometry.Build.</done>
</task>

<task type="auto">
  <name>Task 2 : TickRing — Shape de graduations en une seule passe (CAD-01)</name>
  <files>src/Chronos/Controls/TickRing.cs</files>
  <read_first>
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md § « Pattern 3 : TickRing » (esquisse + rationale 1 visuel vs 60)
    - src/Chronos/Controls/RingArc.cs (modèle de Shape + pattern DP à copier)
  </read_first>
  <action>
    Créer `src/Chronos/Controls/TickRing.cs` (namespace `Chronos.Controls`). Compléter l'esquisse du
    Pattern 3 en un Shape complet avec 4 DP (Count, Radius, TickLength, StartAngle), toutes en
    `AffectsRender`. `DefiningGeometry` construit UN `GeometryGroup` de `LineGeometry` (une passe,
    un seul visuel — jamais 60 éléments), en réutilisant `ArcGeometry.PointAt` (déjà testé 05-01) :

    ```csharp
    using System.Windows;
    using System.Windows.Media;
    using System.Windows.Shapes;
    using Chronos.Rendering;

    namespace Chronos.Controls;

    /// <summary>
    /// Anneau de graduations (CAD-01) rendu en UNE passe géométrique (GeometryGroup) → un seul visuel,
    /// léger sous fenêtre layered (rendu logiciel, AllowsTransparency). Dérive de Shape comme RingArc.
    /// Deux instances empilées en XAML : mineurs (Count=60, court) et majeurs (Count=12, long/épais).
    /// Couleur = Stroke, épaisseur = StrokeThickness (posés en XAML avec les tokens).
    /// </summary>
    public sealed class TickRing : Shape
    {
        public static readonly DependencyProperty CountProperty =
            DependencyProperty.Register(nameof(Count), typeof(int), typeof(TickRing),
                new FrameworkPropertyMetadata(60, FrameworkPropertyMetadataOptions.AffectsRender));

        public static readonly DependencyProperty RadiusProperty =
            DependencyProperty.Register(nameof(Radius), typeof(double), typeof(TickRing),
                new FrameworkPropertyMetadata(96d, FrameworkPropertyMetadataOptions.AffectsRender));

        public static readonly DependencyProperty TickLengthProperty =
            DependencyProperty.Register(nameof(TickLength), typeof(double), typeof(TickRing),
                new FrameworkPropertyMetadata(6d, FrameworkPropertyMetadataOptions.AffectsRender));

        public static readonly DependencyProperty StartAngleProperty =
            DependencyProperty.Register(nameof(StartAngle), typeof(double), typeof(TickRing),
                new FrameworkPropertyMetadata(0d, FrameworkPropertyMetadataOptions.AffectsRender));

        public int    Count      { get => (int)GetValue(CountProperty);        set => SetValue(CountProperty, value); }
        public double Radius     { get => (double)GetValue(RadiusProperty);     set => SetValue(RadiusProperty, value); }
        public double TickLength { get => (double)GetValue(TickLengthProperty); set => SetValue(TickLengthProperty, value); }
        public double StartAngle { get => (double)GetValue(StartAngleProperty); set => SetValue(StartAngleProperty, value); }

        protected override Geometry DefiningGeometry
        {
            get
            {
                var center = new Point(ActualWidth / 2, ActualHeight / 2);
                var group = new GeometryGroup();
                if (Count <= 0) return group;
                for (int i = 0; i < Count; i++)
                {
                    double a = StartAngle + i * 360.0 / Count;
                    var outer = ArcGeometry.PointAt(center, Radius, a);
                    var inner = ArcGeometry.PointAt(center, Radius - TickLength, a);
                    group.Children.Add(new LineGeometry(inner, outer));
                }
                return group;
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` : succès.
    - grep `class TickRing : Shape` présent.
    - grep `new GeometryGroup` présent (une passe, un visuel — pas d'ItemsControl de 60 items).
    - grep `ArcGeometry.PointAt` présent (réutilise la math testée 05-01).
    - Garde `Count <= 0` présente (aucune division par zéro / boucle négative).
  </acceptance_criteria>
  <done>TickRing compile ; construit un GeometryGroup unique paramétré par Count/Radius/TickLength/StartAngle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : UtilizationToBrushConverter — rampe / gris épuisé / neutre null (CAD-04/05)</name>
  <files>src/Chronos/Converters/UtilizationToBrushConverter.cs, tests/Chronos.Tests/UtilizationToBrushConverterTests.cs</files>
  <read_first>
    - .planning/phases/05-cadran-ringarc-converters-c-blage-view/05-RESEARCH.md § « Pattern 4 : UtilizationToBrushConverter » (code prêt) + § « Pitfall 5 : couleur mensongère sur donnée absente »
    - src/Chronos/Rendering/RampColor.cs (livré 05-01)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (Utilization est double? — null = repli sans plafond, JAMAIS inventer)
  </read_first>
  <behavior>
    - Convert(0.3) → SolidColorBrush dont Color == RampColor.Interpolate(0.3) (branche rampe [0,1[).
    - Convert(0.0) → couleur == vert #7BB13C (borne rampe).
    - Convert(1.0) → gris #5A5960 (épuisé, CAD-05) — PAS la couleur rouge de la rampe.
    - Convert(1.4) → gris #5A5960 (tout ≥ 1 est épuisé).
    - Convert(null) → brush neutre #2A2932 (donnée absente : aucune couleur d'utilization inventée — honnêteté).
    - Convert("abc" / non-double) → brush neutre (tolérant, aucune exception).
    - Le brush retourné est gelé (IsFrozen == true) pour les couleurs statiques neutre/épuisé.
  </behavior>
  <action>
    Créer `src/Chronos/Converters/UtilizationToBrushConverter.cs` (namespace `Chronos.Converters`)
    copié VERBATIM du Pattern 4 :

    ```csharp
    using System;
    using System.Globalization;
    using System.Windows.Data;
    using System.Windows.Media;
    using Chronos.Rendering;

    namespace Chronos.Converters;

    /// <summary>
    /// utilization (double?) → Brush. null → neutre (donnée absente, jamais inventée) ;
    /// ≥ 1 → gris « épuisé » (CAD-05) ; [0,1[ → rampe vert→ambre→rouge (CAD-04).
    /// Brushes gelés (Freeze) = partageables et légers. Aucun MultiBinding (couleur = 1 seule valeur).
    /// </summary>
    public sealed class UtilizationToBrushConverter : IValueConverter
    {
        private static readonly SolidColorBrush Neutre = Frozen(0x2A, 0x29, 0x32); // teinte piste douce
        private static readonly SolidColorBrush Epuise = Frozen(0x5A, 0x59, 0x60); // #5A5960

        public object Convert(object? value, Type t, object? p, CultureInfo c)
        {
            if (value is not double u) return Neutre;      // null / non-double → neutre
            if (u >= 1.0)             return Epuise;        // quota épuisé
            var b = new SolidColorBrush(RampColor.Interpolate(u)); b.Freeze();
            return b;
        }
        public object ConvertBack(object v, Type t, object p, CultureInfo c) => Binding.DoNothing;

        private static SolidColorBrush Frozen(byte r, byte g, byte b)
        { var s = new SolidColorBrush(Color.FromRgb(r, g, b)); s.Freeze(); return s; }
    }
    ```

    Puis `tests/Chronos.Tests/UtilizationToBrushConverterTests.cs` (namespace `Chronos.Tests`),
    `[WpfFact]` (SolidColorBrush = DispatcherObject → STA). Instancier le converter, appeler
    `Convert(value, typeof(Brush), null, CultureInfo.InvariantCulture)`, caster en `SolidColorBrush`
    et asserter `.Color`. Couvrir tous les cas de <behavior>, dont explicitement `null` → neutre
    (Pitfall 5 : ne jamais inventer une couleur sur donnée absente).
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~UtilizationToBrush"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~UtilizationToBrush"` : tous verts (rampe / ≥1 gris / null neutre / non-double neutre).
    - grep `0x5A, 0x59, 0x60` présent (gris épuisé CAD-05, token exact).
    - grep `RampColor.Interpolate` présent (délègue à la math testée).
    - grep `value is not double u` présent (null / non-double → neutre, honnêteté DAT-08 préservée).
  </acceptance_criteria>
  <done>Converter mono-entrée prouvé sur les 3 branches ; gris exact à ≥1 ; neutre sur null (aucune couleur inventée).</done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug` : suite complète verte (41 existants + ArcGeometry + RampColor + UtilizationToBrush).
- Aucune logique métier hors des enveloppes minces ; RingArc et TickRing dérivent de Shape (pas UserControl).
</verification>

<success_criteria>
- RingArc réutilisable (Shape, DefiningGeometry, DP AffectsRender) — CAD-07.
- TickRing rend les graduations en un visuel unique — support CAD-01.
- Converter renvoie rampe/gris/neutre selon utilization, sans jamais mentir sur une donnée absente — CAD-04/05 + honnêteté.
- Les trois classes sont instanciables telles quelles dans MainWindow.xaml (05-03).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-cadran-ringarc-converters-c-blage-view/05-02-SUMMARY.md`.
</output>
