---
phase: 11-int-gration-composite-r-glage
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/Views/MainWindow.xaml
  - tests/Chronos.Tests/MainViewModelTests.cs
  - tests/Chronos.Tests/WindowGaugeViewModelTests.cs
autonomous: false
requirements: [INT-02, INT-03]
must_haves:
  truths:
    - "Une fenêtre Exact issue de l'OAuth a IsEstimated == false → le badge « estimée » est masqué et l'arc prend la couleur d'utilisation réelle (INT-02)."
    - "Le menu contient « Usage exact (OAuth) » cochable reflétant OAuthUsageEnabled ; le basculer persiste le flag (GAP-1) et redéclenche un rafraîchissement (INT-03)."
    - "En app réelle : le cadran affiche les vrais % (~74/93) SANS badge « estimée », arcs colorés ; décocher → repli estimé sans accès token."
  artifacts:
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "IsOAuthUsageEnabled + commande ToggleOAuthUsage (Load frais/with/Save + RequestRefresh)"
      contains: "ToggleOAuthUsage"
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "MenuItem cochable « Usage exact (OAuth) »"
      contains: "Usage exact"
  key_links:
    - from: "src/Chronos/Views/MainWindow.xaml"
      to: "MainViewModel.IsOAuthUsageEnabled / ToggleOAuthUsageCommand"
      via: "IsChecked binding + Command binding"
      pattern: "ToggleOAuthUsageCommand"
    - from: "src/Chronos/ViewModels/MainViewModel.cs"
      to: "SettingsService.Save + orchestrator.RequestRefresh"
      via: "Load DISQUE frais → with { OAuthUsageEnabled } → Save → RequestRefresh"
      pattern: "_orchestrator\\.RequestRefresh\\(\\)"
---

<objective>
Exposer le réglage on/off « Usage exact (OAuth) » dans le menu contextuel (commande VM persistée + item
cochable XAML), prouver par test que le badge « estimée » disparaît sur une fenêtre Exact (INT-02), puis
valider en app réelle que le cadran affiche les vrais pourcentages sans badge (checkpoint).

Purpose : donner à l'utilisateur le contrôle explicite de l'accès au token et confirmer l'honnêteté
bidirectionnelle du cadran (exact quand exact, estimé sinon).
Output : `MainViewModel.IsOAuthUsageEnabled` + `ToggleOAuthUsageCommand`, MenuItem dans MainWindow.xaml,
test INT-02 (WindowGaugeViewModel), tests toggle VM, et une vérification humaine finale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-int-gration-composite-r-glage/11-CONTEXT.md
@.planning/phases/11-int-gration-composite-r-glage/11-01-PLAN.md

# Code existant (patterns à copier — NE PAS réexplorer)
@src/Chronos/ViewModels/MainViewModel.cs
@src/Chronos/ViewModels/WindowGaugeViewModel.cs
@src/Chronos/Views/MainWindow.xaml

<interfaces>
<!-- MainViewModel injecte DÉJÀ tout le nécessaire : le toggle N'AJOUTE PAS de paramètre au ctor
     (donc AUCUN impact sur CadranBindingTests / OverlayWindowConfigTests — leçon phase 9 respectée). -->

// src/Chronos/ViewModels/MainViewModel.cs (collaborateurs déjà en champ privé) :
//   private readonly RefreshOrchestrator _orchestrator;   // _orchestrator.RequestRefresh() existe
//   private readonly SettingsService _settingsService;    // Load()/Save()
//   private ChronosSettings _settings;                     // état persisté courant
// Pattern GAP-1 déjà en place (voir CalibrateBudgets) : var courant = _settingsService.Load();
//   _settings = _settingsService.Load() with { ... }; _settingsService.Save(_settings); _orchestrator.RequestRefresh();
// Pattern toggle cochable déjà en place (voir ToggleBackground/ToggleAutostart) :
//   [ObservableProperty] private bool _isBackground;  + init dans le ctor depuis _settings.

// src/Chronos/ViewModels/WindowGaugeViewModel.cs :
//   public void Apply(WindowState s);   // met IsEstimated = (s.Reliability == Estimated)
//   [ObservableProperty] private bool _isEstimated;
//   [ObservableProperty] private double? _utilization;

// src/Chronos/Models/WindowState.cs :
//   new WindowState { Kind, Reliability, Utilization, ResetsAt }
//   SourceReliability { Exact, Estimated, Unavailable }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : commande VM ToggleOAuthUsage (persistée GAP-1 + RequestRefresh) + tests</name>
  <files>src/Chronos/ViewModels/MainViewModel.cs, tests/Chronos.Tests/MainViewModelTests.cs</files>
  <read_first>
    - src/Chronos/ViewModels/MainViewModel.cs (champs _settings/_settingsService/_orchestrator ; init des toggles dans le ctor ; CalibrateBudgets pour le pattern GAP-1 Load frais → with → Save → RequestRefresh)
    - tests/Chronos.Tests/MainViewModelTests.cs (helpers NewVmFull/Build ; tests ToggleBackground/CalibrateBudgets à copier ; GAP-1 « n_ecrase_pas_les_reglages_persistes_par_un_autre_writer »)
  </read_first>
  <behavior>
    - À l'init, IsOAuthUsageEnabled reflète _settings.OAuthUsageEnabled (donc true par défaut).
    - ToggleOAuthUsage inverse IsOAuthUsageEnabled, persiste le nouveau flag dans settings.json et appelle RequestRefresh.
    - GAP-1 : le toggle relit le DISQUE frais avant Save → il n'écrase PAS un autre réglage (ex. Corner) écrit après la construction du VM.
  </behavior>
  <action>
    1. Dans `src/Chronos/ViewModels/MainViewModel.cs` :
       - Ajouter la propriété observable près de `_isBackground`/`_isAutostart` :
         ```csharp
         // État reflété dans l'item « Usage exact (OAuth) » du menu (INT-03).
         [ObservableProperty] private bool _isOAuthUsageEnabled;
         ```
       - Dans le ctor, après `IsAutostart = _autostart.IsEnabled();`, initialiser depuis les settings :
         ```csharp
         IsOAuthUsageEnabled = _settings.OAuthUsageEnabled;
         ```
       - Ajouter la commande (calquée sur CalibrateBudgets pour GAP-1) :
         ```csharp
         /// <summary>INT-03 : active/désactive la source EXACTE OAuth. Persiste le flag (GAP-1 : Load DISQUE
         /// frais avant Save, pour ne pas écraser un réglage écrit par l'OverlayController) puis redéclenche
         /// l'orchestrateur → le portillon gated relit le flag frais au prochain GetAsync et bascule aussitôt.</summary>
         [RelayCommand]
         private void ToggleOAuthUsage()
         {
             IsOAuthUsageEnabled = !IsOAuthUsageEnabled;
             _settings = _settingsService.Load() with { OAuthUsageEnabled = IsOAuthUsageEnabled };
             _settingsService.Save(_settings);
             _orchestrator.RequestRefresh();   // application immédiate (le gated Load() frais à chaque GetAsync)
         }
         ```
    2. Dans `tests/Chronos.Tests/MainViewModelTests.cs`, ajouter :
       ```csharp
       // --- INT-03 : à l'init, IsOAuthUsageEnabled reflète le setting (défaut true) ---
       [Fact]
       public void Initialisation_IsOAuthUsageEnabled_reflete_le_setting_par_defaut_true()
       {
           var vm = NewVmFull(out _, out _, out _, out _, out _, out _, out _, out _);
           Assert.True(vm.IsOAuthUsageEnabled);   // défaut ChronosSettings = true
       }

       // --- INT-03 : le toggle bascule l'état ET persiste le flag dans settings.json ---
       [Fact]
       public void ToggleOAuthUsage_bascule_et_persiste_le_flag()
       {
           var vm = NewVmFull(out _, out _, out _, out _, out _, out _, out _, out var settings);
           Assert.True(vm.IsOAuthUsageEnabled);

           vm.ToggleOAuthUsageCommand.Execute(null);
           Assert.False(vm.IsOAuthUsageEnabled);
           Assert.False(settings.Load().OAuthUsageEnabled);   // persisté off

           vm.ToggleOAuthUsageCommand.Execute(null);
           Assert.True(vm.IsOAuthUsageEnabled);
           Assert.True(settings.Load().OAuthUsageEnabled);     // persisté on
       }

       // --- GAP-1 : le toggle n'écrase pas un réglage écrit sur disque par un autre writer ---
       [Fact]
       public void ToggleOAuthUsage_n_ecrase_pas_les_reglages_persistes_par_un_autre_writer()
       {
           var vm = NewVmFull(out _, out _, out _, out _, out _, out _, out _, out var settings);

           // Simule l'OverlayController : APRÈS construction du VM, un drag persiste un nouveau coin.
           settings.Save(settings.Load() with { Corner = OverlayCorner.BottomLeft });

           vm.ToggleOAuthUsageCommand.Execute(null);   // passe OAuthUsageEnabled à false

           var apres = settings.Load();
           Assert.False(apres.OAuthUsageEnabled);                 // flag bien persisté…
           Assert.Equal(OverlayCorner.BottomLeft, apres.Corner);  // …SANS écraser le coin du drag (GAP-1)
       }
       ```
    NE PAS ajouter de paramètre au ctor de MainViewModel (les collaborateurs sont déjà injectés) → aucun
    impact sur CadranBindingTests / OverlayWindowConfigTests / CompositionRootTests.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~MainViewModelTests"</automated>
  </verify>
  <done>IsOAuthUsageEnabled initialisé depuis settings ; ToggleOAuthUsageCommand bascule + persiste + RequestRefresh ; 3 tests verts ; ctor VM inchangé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : MenuItem « Usage exact (OAuth) » (XAML) + test INT-02 (badge masqué en Exact)</name>
  <files>src/Chronos/Views/MainWindow.xaml, tests/Chronos.Tests/WindowGaugeViewModelTests.cs</files>
  <read_first>
    - src/Chronos/Views/MainWindow.xaml (ContextMenu : MenuItem cochables « Arrière-plan »/« Lancer au démarrage » à copier ; badges BadgeEstimee* liés à FiveHour.IsEstimated / SevenDay.IsEstimated)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (Apply() met IsEstimated = Reliability==Estimated ; Utilization exposée)
  </read_first>
  <behavior>
    - WindowGaugeViewModel.Apply(fenêtre Exact) → IsEstimated == false ET Utilization == valeur exacte (badge masqué, couleur de rampe).
    - WindowGaugeViewModel.Apply(fenêtre Estimated) → IsEstimated == true (badge visible — l'honnêteté joue dans les deux sens).
  </behavior>
  <action>
    1. Dans `src/Chronos/Views/MainWindow.xaml`, ajouter un MenuItem cochable dans le `<ContextMenu>`
       (après « Lancer au démarrage », avant le `<Separator/>`), calqué sur les items cochables existants :
       ```xml
       <MenuItem Header="Usage exact (OAuth)" IsCheckable="True"
                 IsChecked="{Binding IsOAuthUsageEnabled}"
                 Command="{Binding ToggleOAuthUsageCommand}"/>
       ```
    2. Créer `tests/Chronos.Tests/WindowGaugeViewModelTests.cs` prouvant INT-02 au niveau VM (pas de
       nouveau binding — on vérifie que l'état Exact éteint le badge et porte l'utilisation réelle) :
       ```csharp
       using Chronos.Models;
       using Chronos.ViewModels;
       using Xunit;

       namespace Chronos.Tests;

       /// <summary>Prouve INT-02 : une fenêtre EXACT (issue de l'OAuth) éteint le badge « estimée »
       /// (IsEstimated == false) et porte l'utilisation réelle (couleur de rampe) ; une fenêtre ESTIMÉE
       /// rallume le badge. L'honnêteté joue dans les deux sens. Tests PURS ([Fact]).</summary>
       public class WindowGaugeViewModelTests
       {
           [Fact]
           public void Fenetre_exacte_masque_le_badge_estimee_et_porte_utilisation_reelle()
           {
               var vm = new WindowGaugeViewModel(TimeSpan.FromHours(5));
               vm.Apply(new WindowState
               {
                   Kind = WindowKind.FiveHour,
                   Reliability = SourceReliability.Exact,
                   Utilization = 0.74,
                   ResetsAt = DateTimeOffset.UtcNow + TimeSpan.FromHours(2),
               });

               Assert.False(vm.IsEstimated);              // badge « estimée » masqué (INT-02)
               Assert.Equal(0.74, vm.Utilization);        // arc en vraie couleur (utilization exacte)
               Assert.False(vm.HasTokens);                // pas de surfaçage tokens estimés en Exact
           }

           [Fact]
           public void Fenetre_estimee_rallume_le_badge()
           {
               var vm = new WindowGaugeViewModel(TimeSpan.FromDays(7));
               vm.Apply(new WindowState { Kind = WindowKind.SevenDay, Reliability = SourceReliability.Estimated });
               Assert.True(vm.IsEstimated);               // honnêteté dans l'autre sens
           }
       }
       ```
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~WindowGaugeViewModelTests"</automated>
  </verify>
  <done>MenuItem « Usage exact (OAuth) » présent et lié ; test INT-02 vert (Exact → IsEstimated false + Utilization exacte ; Estimated → true) ; le build XAML compile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : validation en app réelle (vrais % sans badge, toggle off = repli)</name>
  <files>(aucun — vérification manuelle en app réelle)</files>
  <action>Vérification HUMAINE : republier et lancer l'app, puis suivre les étapes de <how-to-verify>. Aucune modification de code — ce checkpoint confirme visuellement le résultat des tâches 1-2 et de la chaîne DI du plan 11-01 sur le vrai poste (endpoint + token réels non simulables).</action>
  <what-built>
    Chaîne composite à 3 (OAuth gated → statusLine → JSONL), réglage menu « Usage exact (OAuth) » persisté,
    badge « estimée » masqué automatiquement sur les fenêtres Exact. Tout est automatisé et testé ; il reste
    à confirmer visuellement sur le vrai poste (l'endpoint réel + le vrai token ne sont pas simulables en test).
  </what-built>
  <how-to-verify>
    1. Republier/lancer l'app :
       `dotnet build Chronos.sln -c Debug` puis lancer l'exécutable Chronos (ou `dotnet run --project src/Chronos`).
    2. OAuth ACTIVÉ (défaut) : observer le cadran → les deux arcs affichent les VRAIS pourcentages
       (ordre de grandeur attendu ~74 % / ~93 %, cf. app bureau `/usage`), SANS le texte « estimée »
       sous les compteurs, arcs en couleur d'utilisation réelle.
    3. Clic DROIT → menu → décocher « Usage exact (OAuth) ». Le cadran doit basculer sur l'estimation
       (le badge « estimée » réapparaît si la source de repli est estimée), SANS erreur ni gel.
    4. Recocher « Usage exact (OAuth) » → les vrais pourcentages reviennent au prochain rafraîchissement.
    5. Redémarrer l'app une fois pour confirmer que l'état coché/décoché est bien PERSISTÉ (settings.json).
  </how-to-verify>
  <resume-signal>
    Taper « approved » si les vrais % s'affichent sans badge et que le toggle bascule/persiste correctement ;
    sinon décrire l'écart observé (badge résiduel, mauvais chiffres, pas de bascule, non-persistance).
  </resume-signal>
  <verify>Confirmation humaine « approved » après observation du cadran (vrais % sans badge « estimée », toggle off → repli estimé, état persisté au redémarrage). Pré-requis automatisé : `dotnet test Chronos.sln -c Debug` vert.</verify>
  <done>L'utilisateur confirme : vrais pourcentages affichés sans badge « estimée » arcs colorés ; décocher bascule sur l'estimation sans erreur ; l'état coché/décoché survit au redémarrage.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` compile (XAML inclus).
- `dotnet test Chronos.sln -c Debug` : suite complète verte (MainViewModel toggle + WindowGaugeViewModel INT-02 + tout l'existant).
- Checkpoint humain « approved » : vrais % sans badge, toggle off → repli, état persisté au redémarrage.
</verification>

<success_criteria>
- INT-02 : fenêtre Exact → badge « estimée » masqué + arc en utilisation réelle (prouvé par test + confirmé en app).
- INT-03 (UI) : item de menu cochable « Usage exact (OAuth) » persiste le flag (GAP-1) et redéclenche un refresh ; off = v1.1 strict.
- Non-régression : ctor MainViewModel inchangé → CadranBindingTests / OverlayWindowConfigTests / CompositionRootTests restent verts.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/11-int-gration-composite-r-glage/11-02-SUMMARY.md`.
</output>
