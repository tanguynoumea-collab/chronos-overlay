---
phase: 11-int-gration-composite-r-glage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/ChronosSettings.cs
  - src/Chronos/Services/GatedOAuthUsageProvider.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/SettingsServiceTests.cs
  - tests/Chronos.Tests/GatedOAuthUsageProviderTests.cs
  - tests/Chronos.Tests/CompositeUsageProviderTests.cs
autonomous: true
requirements: [INT-01, INT-03]
must_haves:
  truths:
    - "La source OAuth (Exact) prime PAR FENÊTRE sur le pont statusLine puis le repli JSONL dans la chaîne composite à 3 niveaux."
    - "OAuthUsageEnabled == false → la source OAuth retourne UsageSnapshot.Empty SANS lire le token ni appeler l'endpoint (zéro accès)."
    - "OAuthUsageEnabled (défaut true) se persiste et se relit sans exception (round-trip settings.json)."
  artifacts:
    - path: "src/Chronos/Services/ChronosSettings.cs"
      provides: "Champ OAuthUsageEnabled (bool, défaut true)"
      contains: "OAuthUsageEnabled"
    - path: "src/Chronos/Services/GatedOAuthUsageProvider.cs"
      provides: "Portillon gated neutre : Empty sans accès token si désactivé"
      contains: "class GatedOAuthUsageProvider"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Chaîne DI imbriquée à 3 (OAuth gated → statusLine → JSONL)"
      contains: "GatedOAuthUsageProvider"
  key_links:
    - from: "src/Chronos/Services/GatedOAuthUsageProvider.cs"
      to: "SettingsService.Load()"
      via: "lecture FRAÎCHE du flag à chaque GetAsync"
      pattern: "_settings\\.Load\\(\\)\\.OAuthUsageEnabled"
    - from: "src/Chronos/App.xaml.cs"
      to: "CompositeUsageProvider imbriqué"
      via: "primary: GatedOAuthUsageProvider, fallback: CompositeUsageProvider(usageObject, jsonl)"
      pattern: "new CompositeUsageProvider\\("
---

<objective>
Brancher le provider OAuth (Phase 10) comme source PRIMAIRE exacte du composite existant, PAR IMBRICATION,
et introduire le réglage `OAuthUsageEnabled` avec un portillon « gated » qui garantit ZÉRO accès au token
quand la source est désactivée (INT-01, INT-03 côté backend).

Purpose : rendre l'overlay exact et automatique tout en préservant l'inviolabilité de la sécurité v1.2
(off = comportement v1.1 strict, le coffre n'est jamais ouvert).
Output : `ChronosSettings.OAuthUsageEnabled`, `GatedOAuthUsageProvider`, la chaîne DI à 3 niveaux dans
`App.xaml.cs`, et les tests prouvant round-trip + gated + priorité composite. 178 tests + nouveaux verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-int-gration-composite-r-glage/11-CONTEXT.md

# Code existant (contrats à réutiliser — NE PAS réexplorer le codebase, tout est ici)
@src/Chronos/Services/CompositeUsageProvider.cs
@src/Chronos/Services/ClaudeOAuthUsageProvider.cs
@src/Chronos/Services/ChronosSettings.cs
@src/Chronos/Services/SettingsService.cs
@src/Chronos/App.xaml.cs

<interfaces>
<!-- Contrats exacts que l'exécuteur doit utiliser DIRECTEMENT (extraits du codebase). -->

// src/Chronos/Services/IUsageProvider.cs
public interface IUsageProvider { Task<UsageSnapshot> GetAsync(CancellationToken ct = default); }

// src/Chronos/Models/UsageSnapshot.cs
public sealed record UsageSnapshot {
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
    public DateTimeOffset? SourceCapturedAt { get; init; }
    public static UsageSnapshot Empty { get; }   // deux fenêtres Unavailable
}

// src/Chronos/Services/IClaudeTokenReader.cs
public interface IClaudeTokenReader { string? TryReadAccessToken(out DateTimeOffset? expiresAt); }

// src/Chronos/Services/ClaudeTokenReader.cs
public sealed class ClaudeTokenReader : IClaudeTokenReader {
    public ClaudeTokenReader(string configJsonPath, string localStatePath);
    public static ClaudeTokenReader Default();   // cible %APPDATA%/Claude/config.json + Local State
}

// src/Chronos/Services/ClaudeOAuthUsageProvider.cs
public sealed class ClaudeOAuthUsageProvider : IUsageProvider {
    public ClaudeOAuthUsageProvider(IClaudeTokenReader tokenReader, HttpClient http, IClock clock);
}

// src/Chronos/Services/SettingsService.cs
public sealed class SettingsService {
    public ChronosSettings Load();          // tolérant : absent/corrompu → défauts
    public void Save(ChronosSettings s);    // atomique
}

// Fakes de test disponibles (tests/Chronos.Tests/Fakes/)
internal sealed class FakeClaudeTokenReader : IClaudeTokenReader { public string? Token; public DateTimeOffset? Expires; public int ReadCount { get; } }
internal sealed class FakeHttpMessageHandler : HttpMessageHandler { public int SendCount { get; } public static FakeHttpMessageHandler Json(HttpStatusCode s, string body); }
internal sealed class FakeClock : IClock { public FakeClock(DateTimeOffset now); public DateTimeOffset UtcNow { get; set; } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0) : champ OAuthUsageEnabled (défaut true) + round-trip settings.json</name>
  <files>src/Chronos/Services/ChronosSettings.cs, tests/Chronos.Tests/SettingsServiceTests.cs</files>
  <read_first>
    - src/Chronos/Services/ChronosSettings.cs (record NEUTRE, champs `init` avec défauts)
    - tests/Chronos.Tests/SettingsServiceTests.cs (patterns round-trip + défauts sur fichier absent)
  </read_first>
  <behavior>
    - Round-trip : un ChronosSettings avec OAuthUsageEnabled = false, sauvé puis relu, est ÉGAL à l'original.
    - Défaut : sur fichier absent, Load() retourne OAuthUsageEnabled == true (source exacte active dès l'install).
  </behavior>
  <action>
    1. Dans `src/Chronos/Services/ChronosSettings.cs`, ajouter un champ à la fin du record :
       ```csharp
       /// <summary>Active la source EXACTE OAuth (INT-03). Défaut TRUE : vrais chiffres dès l'installation.
       /// false → comportement v1.1 strict, AUCUN accès au token (le portillon gated court-circuite).</summary>
       public bool OAuthUsageEnabled { get; init; } = true;
       ```
    2. Dans `tests/Chronos.Tests/SettingsServiceTests.cs` :
       - Dans `Save_puis_Load_round_trip`, ajouter `OAuthUsageEnabled = false,` à l'initialiseur `original`
         (valeur ≠ défaut pour prouver la persistance ; l'assertion `Assert.Equal(original, relu)` la couvre).
       - Dans `Load_fichier_absent_redonne_les_defauts`, ajouter `Assert.True(s.OAuthUsageEnabled);`.
    NE PAS toucher aux autres champs ni à SettingsService (le round-trip JSON est générique).
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~SettingsServiceTests"</automated>
  </verify>
  <done>OAuthUsageEnabled présent (défaut true) ; round-trip + assertion de défaut verts ; suite Settings verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : GatedOAuthUsageProvider (Empty sans accès token si désactivé) + tests</name>
  <files>src/Chronos/Services/GatedOAuthUsageProvider.cs, tests/Chronos.Tests/GatedOAuthUsageProviderTests.cs</files>
  <read_first>
    - src/Chronos/Services/ClaudeOAuthUsageProvider.cs (le provider interne enveloppé)
    - src/Chronos/Services/SettingsService.cs (Load() frais)
    - tests/Chronos.Tests/ClaudeOAuthUsageProviderTests.cs (montage FakeClaudeTokenReader + FakeHttpMessageHandler + FakeClock ; corps NominalBody réutilisable)
    - tests/Chronos.Tests/SettingsServiceTests.cs (montage ChronosPaths temp + SettingsService)
  </read_first>
  <behavior>
    - OAuthUsageEnabled == false → GetAsync retourne UsageSnapshot.Empty ; reader.ReadCount == 0 ET handler.SendCount == 0 (zéro accès token/réseau).
    - OAuthUsageEnabled == true → GetAsync délègue au provider interne (fenêtres mappées Exact ; reader.ReadCount >= 1).
    - Le flag est relu FRAIS : basculer le setting entre deux GetAsync change le comportement sans reconstruire le provider.
  </behavior>
  <action>
    1. Créer `src/Chronos/Services/GatedOAuthUsageProvider.cs` :
       ```csharp
       using Chronos.Models;

       namespace Chronos.Services;

       /// <summary>
       /// Portillon (INT-03) autour du provider OAuth EXACT. À CHAQUE GetAsync, relit OAuthUsageEnabled
       /// FRAIS depuis settings.json (comme JsonlEstimationProvider relit ses plafonds). Si désactivé,
       /// retourne UsageSnapshot.Empty SANS JAMAIS appeler le provider interne — donc sans lire ni
       /// déchiffrer le token, sans le moindre appel réseau. Off = v1.1 strict, ZÉRO accès token.
       /// Type NEUTRE (aucun type WPF) : la garde de pureté Services/Models reste verte.
       /// </summary>
       public sealed class GatedOAuthUsageProvider : IUsageProvider
       {
           private readonly IUsageProvider _inner;      // ClaudeOAuthUsageProvider (Exact)
           private readonly SettingsService _settings;

           public GatedOAuthUsageProvider(IUsageProvider inner, SettingsService settings)
           {
               _inner = inner;
               _settings = settings;
           }

           public Task<UsageSnapshot> GetAsync(CancellationToken ct = default)
           {
               // Lecture FRAÎCHE : un toggle menu prend effet au prochain GetAsync sans redémarrage.
               if (!_settings.Load().OAuthUsageEnabled)
                   return Task.FromResult(UsageSnapshot.Empty);   // court-circuit : le token n'est JAMAIS touché
               return _inner.GetAsync(ct);
           }
       }
       ```
    2. Créer `tests/Chronos.Tests/GatedOAuthUsageProviderTests.cs` :
       - Helper : construire un `SettingsService` sur `ChronosPaths` temp (copier le pattern de `SettingsServiceTests`/`MainViewModelTests.TempPaths`), un `ClaudeOAuthUsageProvider` réel enveloppant un `FakeClaudeTokenReader { Token = "secret" }`, `new HttpClient(handler)` avec `FakeHttpMessageHandler.Json(HttpStatusCode.OK, corpsNominal)`, `FakeClock`.
       - Test `Desactive_retourne_Empty_sans_lire_le_token_ni_appeler_le_reseau` :
         `settings.Save(new ChronosSettings { OAuthUsageEnabled = false });` puis
         `var snap = await gated.GetAsync();`
         `Assert.Equal(SourceReliability.Unavailable, snap.FiveHour.Reliability);`
         `Assert.Equal(SourceReliability.Unavailable, snap.SevenDay.Reliability);`
         `Assert.Equal(0, reader.ReadCount);`   // PREUVE sécurité : coffre jamais ouvert
         `Assert.Equal(0, handler.SendCount);`  // PREUVE : aucun appel endpoint
       - Test `Active_delegue_au_provider_interne_exact` :
         `settings.Save(new ChronosSettings { OAuthUsageEnabled = true });` puis GetAsync →
         `Assert.Equal(SourceReliability.Exact, snap.FiveHour.Reliability);`
         `Assert.True(reader.ReadCount >= 1);` `Assert.True(handler.SendCount >= 1);`
       - (Corps JSON : réutiliser un corps minimal type `{ "five_hour": {"utilization":65,"resets_at":"2026-07-09T09:39:59+00:00"}, "seven_day": {"utilization":92,"resets_at":"2026-07-10T21:59:59+00:00"} }` ; horloge FakeClock avant ces resets.)
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~GatedOAuthUsageProviderTests"</automated>
  </verify>
  <done>GatedOAuthUsageProvider créé ; off → Empty + ReadCount==0 + SendCount==0 ; on → délègue Exact. Garde de pureté ServicesLayerPurityTests reste verte.</done>
</task>

<task type="auto">
  <name>Task 3 : chaîne DI à 3 niveaux dans App.xaml.cs + test de priorité composite imbriqué</name>
  <files>src/Chronos/App.xaml.cs, tests/Chronos.Tests/CompositeUsageProviderTests.cs</files>
  <read_first>
    - src/Chronos/App.xaml.cs (ConfigureServices, lignes ~76-82 : enregistrement actuel du composite ; lignes ~96-103 : BudgetAutoCalibrator dépend de JsonlEstimationProvider CONCRET — NE PAS casser)
    - src/Chronos/Services/CompositeUsageProvider.cs (best-par-fenêtre + staleness, réutilisé par imbrication)
    - tests/Chronos.Tests/CompositeUsageProviderTests.cs (patterns Assert.Same, FakeProvider local)
  </read_first>
  <action>
    1. Dans `src/Chronos/App.xaml.cs`, ajouter `using System.Net.Http;` en tête si absent.
    2. Remplacer le SEUL enregistrement `services.AddSingleton<IUsageProvider>(...)` (lignes ~80-82) par la
       chaîne à 3 niveaux, en gardant les enregistrements existants de `ClaudeUsageObjectProvider` et
       `JsonlEstimationProvider` (le calibrateur en dépend). Insérer AVANT l'enregistrement IUsageProvider :
       ```csharp
       // v1.2 (INT-01/03) : source EXACTE OAuth en tête de chaîne. Le reader cible le coffre de l'app
       // bureau (%APPDATA%/Claude) ; il n'est JAMAIS sollicité tant que le portillon gated est fermé.
       services.AddSingleton<IClaudeTokenReader>(_ => ClaudeTokenReader.Default());
       services.AddSingleton(sp => new ClaudeOAuthUsageProvider(
           sp.GetRequiredService<IClaudeTokenReader>(),
           new HttpClient(),                                    // long-lived, une seule destination (constante)
           sp.GetRequiredService<IClock>()));
       // Portillon gated : OAuthUsageEnabled==false → Empty sans toucher au token (INT-03).
       services.AddSingleton(sp => new GatedOAuthUsageProvider(
           sp.GetRequiredService<ClaudeOAuthUsageProvider>(),
           sp.GetRequiredService<SettingsService>()));
       ```
       puis remplacer l'enregistrement IUsageProvider par l'IMBRICATION (best-par-fenêtre réutilisé) :
       ```csharp
       // Chaîne à 3 par imbrication (INT-01) : OAuth (exact, gated) → pont statusLine (exact) → JSONL (estimé).
       // Le CompositeUsageProvider gère déjà « meilleure source PAR FENÊTRE » + staleness → aucune réécriture.
       services.AddSingleton<IUsageProvider>(sp => new CompositeUsageProvider(
           primary:  sp.GetRequiredService<GatedOAuthUsageProvider>(),
           fallback: new CompositeUsageProvider(
               primary:  sp.GetRequiredService<ClaudeUsageObjectProvider>(),
               fallback: sp.GetRequiredService<JsonlEstimationProvider>())));
       ```
    3. Dans `tests/Chronos.Tests/CompositeUsageProviderTests.cs`, ajouter un test prouvant la priorité de
       l'imbrication (mêmes FakeProvider/Win/Snap déjà présents dans le fichier) :
       ```csharp
       [Fact]
       public async Task Chaine_imbriquee_OAuth_prime_puis_statusLine_puis_JSONL_par_fenetre()
       {
           // OAuth : 5h Exact, 7j Unavailable ; statusLine : 5h Unavailable, 7j Exact ; JSONL : tout Estimated.
           var oauth      = Snap(Win(WindowKind.FiveHour, SourceReliability.Exact),
                                 Win(WindowKind.SevenDay, SourceReliability.Unavailable));
           var statusLine = Snap(Win(WindowKind.FiveHour, SourceReliability.Unavailable),
                                 Win(WindowKind.SevenDay, SourceReliability.Exact));
           var jsonl      = Snap(Win(WindowKind.FiveHour, SourceReliability.Estimated),
                                 Win(WindowKind.SevenDay, SourceReliability.Estimated));

           var chaine = new CompositeUsageProvider(
               new FakeProvider(oauth),
               new CompositeUsageProvider(new FakeProvider(statusLine), new FakeProvider(jsonl)));

           var snap = await chaine.GetAsync();

           Assert.Same(oauth.FiveHour, snap.FiveHour);       // 5h : OAuth Exact prime
           Assert.Same(statusLine.SevenDay, snap.SevenDay);  // 7j : OAuth indispo → bascule statusLine Exact
       }

       [Fact]
       public async Task Chaine_imbriquee_OAuth_indispo_bascule_sur_JSONL_estime()
       {
           var oauth      = Snap(Win(WindowKind.FiveHour, SourceReliability.Unavailable),
                                 Win(WindowKind.SevenDay, SourceReliability.Unavailable));
           var statusLine = Snap(Win(WindowKind.FiveHour, SourceReliability.Unavailable),
                                 Win(WindowKind.SevenDay, SourceReliability.Unavailable));
           var jsonl      = Snap(Win(WindowKind.FiveHour, SourceReliability.Estimated),
                                 Win(WindowKind.SevenDay, SourceReliability.Estimated));

           var chaine = new CompositeUsageProvider(
               new FakeProvider(oauth),
               new CompositeUsageProvider(new FakeProvider(statusLine), new FakeProvider(jsonl)));

           var snap = await chaine.GetAsync();
           Assert.Same(jsonl.FiveHour, snap.FiveHour);       // repli ultime : JSONL estimé
           Assert.Same(jsonl.SevenDay, snap.SevenDay);
       }
       ```
    NE PAS modifier `CompositeUsageProvider.cs` (l'imbrication réutilise le best-par-fenêtre tel quel → les
    Assert.Same existants restent intacts).
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~CompositeUsageProviderTests"</automated>
  </verify>
  <done>App.xaml.cs enregistre la chaîne imbriquée à 3 ; les 2 tests d'imbrication passent ; les 6 tests composite d'origine (Assert.Same) restent verts ; le build compile.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` compile sans erreur.
- `dotnet test Chronos.sln -c Debug` : 178 tests d'origine + nouveaux (Settings, Gated, Composite) verts.
- ServicesLayerPurityTests reste verte (GatedOAuthUsageProvider est NEUTRE, aucun type WPF).
- Grep de contrôle : `GatedOAuthUsageProvider` référencé dans App.xaml.cs ; `_settings.Load().OAuthUsageEnabled` présent dans le portillon.
</verification>

<success_criteria>
- INT-01 : la chaîne composite est OAuth (gated) → statusLine → JSONL, best-par-fenêtre, prouvée par test.
- INT-03 (backend) : OAuthUsageEnabled persisté (défaut true) ; désactivé → Empty avec ReadCount==0 ET SendCount==0 (zéro accès token/réseau).
- Non-régression : les 178 tests restent verts ; aucun Assert.Same du composite cassé.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/11-int-gration-composite-r-glage/11-01-SUMMARY.md`.
</output>
