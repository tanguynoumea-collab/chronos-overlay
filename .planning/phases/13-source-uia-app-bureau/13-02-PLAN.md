---
phase: 13-source-uia-app-bureau
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/Chronos/Chronos.csproj
  - src/Chronos/Services/WindowsUiaTreeProvider.cs
  - src/Chronos/Services/DesktopUiaPollService.cs
  - tests/Chronos.Tests/DesktopUiaPollServiceTests.cs
autonomous: true
requirements: [BUR-01, ROB-07]

must_haves:
  truths:
    - "L'implémentation réelle lit l'arbre d'accessibilité de la fenêtre Claude via System.Windows.Automation (COM managé, pas d'admin, pas de dépendance native de rendu)."
    - "L'élément racine (fenêtre Claude) est mis en cache entre les polls et réacquis s'il devient invalide."
    - "Un poll en arrière-plan appelle DesktopUiaSessionSource.Poll à ~1-2 s, JAMAIS sur le thread UI."
    - "Fenêtre Claude fermée / UIA indisponible → TryGetTree retourne null sans exception (dégradation)."
  artifacts:
    - path: "src/Chronos/Services/WindowsUiaTreeProvider.cs"
      provides: "IUiaTreeProvider réel (AutomationElement/TreeWalker → UiaNode neutre), racine cachée"
      contains: "IUiaTreeProvider"
    - path: "src/Chronos/Services/DesktopUiaPollService.cs"
      provides: "IHostedService : boucle de poll hors thread UI pilotant DesktopUiaSessionSource.Poll"
      contains: "IHostedService"
    - path: "src/Chronos/Chronos.csproj"
      provides: "Références UIAutomationClient / UIAutomationTypes"
      contains: "UIAutomation"
  key_links:
    - from: "DesktopUiaPollService"
      to: "DesktopUiaSessionSource.Poll"
      via: "timer/thread de fond ~1.5 s (hors UI)"
      pattern: "\\.Poll\\("
    - from: "WindowsUiaTreeProvider.TryGetTree"
      to: "AutomationElement (racine cachée)"
      via: "walk TreeWalker → UiaNode, try/catch → null"
      pattern: "AutomationElement"
---

<objective>
Fournir l'implémentation RÉELLE du seam (`WindowsUiaTreeProvider` via `System.Windows.Automation`) et le
moteur de poll non bloquant (`DesktopUiaPollService`) qui alimente le cache de `DesktopUiaSessionSource`
hors du thread UI. C'est la seule couche OS-dépendante ; elle est MINCE et sans logique métier (la logique
vit dans MapTree, Plan 01).

Purpose: Réaliser la lecture concrète de l'arbre Claude (BUR-01) et garantir que rien de coûteux ne touche
le thread UI (ROB-07), racine cachée entre polls.
Output: 2 fichiers source neufs + références csproj + tests sur la mécanique de poll (pump testable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-source-uia-app-bureau/13-CONTEXT.md

<interfaces>
<!-- Créés au Plan 01, consommés ici : -->
// public interface IUiaTreeProvider { UiaNode? TryGetTree(); }
// public sealed record UiaNode(string ControlType, string Name, string? AutomationId, bool Enabled, IReadOnlyList<UiaNode> Children);
// public sealed class DesktopUiaSessionSource : ISessionSource { public void Poll(DateTimeOffset now); public IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now); }

// À créer :
public sealed class WindowsUiaTreeProvider : IUiaTreeProvider { public UiaNode? TryGetTree(); }
public sealed class DesktopUiaPollService : Microsoft.Extensions.Hosting.IHostedService, System.IDisposable
{
    public DesktopUiaPollService(DesktopUiaSessionSource source, IClock clock);
    public System.Threading.Tasks.Task StartAsync(System.Threading.CancellationToken ct);
    public System.Threading.Tasks.Task StopAsync(System.Threading.CancellationToken ct);
    public void PollOnce(); // exposé pour test : un tick de poll (appelle source.Poll(clock.UtcNow))
}
</interfaces>

<!-- Patrons du dépôt à imiter : -->
@src/Chronos/Services/WpfUiDispatcher.cs
@src/Chronos/App.xaml.cs
@tests/Chronos.Tests/ServicesLayerPurityTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Références UIAutomation + WindowsUiaTreeProvider réel (AutomationElement → UiaNode, racine cachée)</name>
  <files>src/Chronos/Chronos.csproj, src/Chronos/Services/WindowsUiaTreeProvider.cs</files>
  <read_first>
    - src/Chronos/Chronos.csproj (où ajouter les &lt;Reference&gt; — net8.0-windows, UseWPF déjà actif)
    - src/Chronos/Services/UiaNode.cs, src/Chronos/Services/IUiaTreeProvider.cs (contrats Plan 01)
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (CONTRAINTE : la surface PUBLIQUE de ce type Services doit rester neutre — TryGetTree retourne UiaNode?, aucun AutomationElement en signature publique)
    - CLAUDE.md (§ « aucune dépendance native de rendu » : System.Windows.Automation = interop COM managé, autorisé ; pas de SkiaSharp/Direct2D)
  </read_first>
  <action>
    1. Dans `Chronos.csproj`, ajouter un `<ItemGroup>` avec `<Reference Include="UIAutomationClient" />` et
       `<Reference Include="UIAutomationTypes" />` (assemblies du Windows Desktop pack, résolues via UseWPF).
       Vérifier au build ; si la résolution échoue, essayer `<FrameworkReference Include="Microsoft.WindowsDesktop.App" />`
       n'est PAS requis (déjà tiré par UseWPF) — se limiter aux deux References. Aucune dépendance native de rendu.
    2. Créer `WindowsUiaTreeProvider.cs` (sealed class : IUiaTreeProvider, namespace Chronos.Services).
       `using System.Windows.Automation;` autorisé ICI (adaptateur OS, comme WpfUiDispatcher). La surface
       PUBLIQUE se limite à `UiaNode? TryGetTree()` — NE JAMAIS exposer `AutomationElement` en public
       (sinon ServicesLayerPurityTests casse). Champ privé `private AutomationElement? _root;` (racine cachée).
    3. `TryGetTree()` — tout dans un try/catch qui retourne null en cas de souci (JAMAIS d'exception) :
       a. Si `_root` est null ou invalide (accès à une propriété lève / `AutomationElement.RootElement` ne le
          retrouve plus), le réacquérir : chercher la fenêtre de l'app bureau Claude par ControlType Window
          dont le Name contient "Claude" (matching tolérant), sous `AutomationElement.RootElement` (bornée :
          `TreeScope.Children`). Mettre en cache dans `_root`.
       b. Convertir le sous-arbre en `UiaNode` via un walk récursif `TreeWalker.ControlViewWalker` avec une
          BORNE de profondeur (ex. maxDepth ~12) et de largeur raisonnable, pour rester léger (fenêtre layered,
          cf. CLAUDE.md coûts de composition). Pour chaque élément : ControlType.ProgrammaticName (ou LocalizedControlType),
          Name (Current.Name), AutomationId (Current.AutomationId), Enabled (Current.IsEnabled).
       c. Retourner le UiaNode racine (ou null si fenêtre Claude introuvable).
    4. Commentaires en français ; préciser que ce type N'EST PAS unit-testé (dépend de l'OS) et reste sans
       logique métier (le mapping est dans DesktopUiaSessionSource.MapTree).
  </action>
  <verify>
    <automated>dotnet build "src/Chronos/Chronos.csproj" -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` réussit (références UIAutomation résolues).
    - grep : `Chronos.csproj` contient `UIAutomationClient` et `UIAutomationTypes`.
    - grep : `WindowsUiaTreeProvider.cs` contient `IUiaTreeProvider`, `AutomationElement`, `_root`, `TreeWalker`.
    - grep : la signature publique n'expose pas AutomationElement (seul `UiaNode? TryGetTree()` est public).
    - `dotnet test --filter "FullyQualifiedName~ServicesLayerPurityTests"` reste vert.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DesktopUiaPollService — poll de fond non bloquant + PollOnce testable</name>
  <files>src/Chronos/Services/DesktopUiaPollService.cs, tests/Chronos.Tests/DesktopUiaPollServiceTests.cs</files>
  <read_first>
    - src/Chronos/Services/DesktopUiaSessionSource.cs (Plan 01 : méthode Poll(now) à piloter)
    - src/Chronos/App.xaml.cs (patron IHostedService : RefreshOrchestrator enregistré via AddHostedService)
    - src/Chronos/Services/IClock.cs (source de temps injectable ; voir IClock/SystemClock usage existant)
    - tests/Chronos.Tests/RefreshOrchestratorTests.cs (patron de test d'un service de fond, si présent)
  </read_first>
  <action>
    1. Créer `DesktopUiaPollService.cs` (sealed class : IHostedService, IDisposable ; namespace Chronos.Services ;
       NEUTRE — aucun type WPF). Ctor `(DesktopUiaSessionSource source, IClock clock)`.
    2. `StartAsync` : démarrer un `System.Threading.Timer` (ou une boucle sur un thread de fond) de période
       ~1.5 s qui appelle `PollOnce()`. Le timer .NET s'exécute sur un thread du pool → JAMAIS le thread UI
       (c'est le cœur de ROB-07). Retourner `Task.CompletedTask`.
    3. `public void PollOnce()` : try { `_source.Poll(_clock.UtcNow);` } catch { /* ne jamais remonter */ }.
       Exposé public pour permettre un test déterministe SANS attendre le timer.
    4. `StopAsync` / `Dispose` : arrêter et disposer le timer proprement (le host appelle StopAsync à l'arrêt,
       cf. App.OnExit). Idempotent.
    5. Créer `DesktopUiaPollServiceTests.cs` : un faux `IUiaTreeProvider` renvoyant un petit arbre valide
       (ancre RootWebArea + Text "Mode chat" + placeholder) ; construire `new DesktopUiaSessionSource(fake)` ,
       `new DesktopUiaPollService(source, new FixedClock(...))`. Avant `PollOnce`, `source.Read(now)` est vide ;
       après `PollOnce()`, `source.Read(now)` contient le snapshot mappé. Vérifier aussi que `PollOnce()` ne
       lève pas quand le provider retourne null (dégradation). Réutiliser le helper `FakeUiaNode` (le copier
       si le fichier de test du Plan 01 n'expose pas d'API partagée). Si un IClock fixe n'existe pas déjà dans
       les Fakes de test, en créer un minimal localement.
  </action>
  <verify>
    <automated>dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj" --filter "FullyQualifiedName~DesktopUiaPollServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Le test « avant PollOnce vide / après PollOnce peuplé » passe, et le cas provider=null ne lève pas.
    - grep : `DesktopUiaPollService.cs` contient `IHostedService`, `PollOnce`, `.Poll(`, `Timer` (ou thread de fond documenté).
    - grep : `DesktopUiaPollService.cs` n'utilise AUCUN `using System.Windows`.
    - Suite `dotnet test` complète verte (aucune régression).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build "src/Chronos/Chronos.csproj"` réussit avec les références UIAutomation.
- `dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj"` : suite entière verte.
- `ServicesLayerPurityTests` reste vert (WindowsUiaTreeProvider n'expose pas AutomationElement publiquement).
- Aucun câblage DI ni modification de SessionMonitor/SessionsViewModel ici (réservé au Plan 03).
</verification>

<success_criteria>
- `WindowsUiaTreeProvider` lit réellement l'arbre Claude via System.Windows.Automation, racine cachée,
  dégradation silencieuse (BUR-01, fondation).
- `DesktopUiaPollService` pilote `Poll` hors thread UI à ~1.5 s (ROB-07), avec un chemin `PollOnce` testé.
- Aucune régression ; frontière neutre préservée.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/13-source-uia-app-bureau/13-02-SUMMARY.md`.
</output>
