---
phase: 13-source-uia-app-bureau
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - src/Chronos/Services/SessionMonitor.cs
  - src/Chronos/App.xaml.cs
  - src/Chronos/ViewModels/SessionsViewModel.cs
  - src/Chronos/Views/SessionsWindow.xaml
  - tests/Chronos.Tests/SessionsTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
autonomous: true
requirements: [BUR-01, BUR-03]

must_haves:
  truths:
    - "SessionMonitor.Read fusionne les snapshots de la source bureau APRÈS transcripts + hooks, sans écraser les sessions CLI (clés `desktop:...` disjointes des session_id JSONL)."
    - "La source bureau est câblée dans le conteneur DI et son poll de fond démarre avec l'app (IHostedService)."
    - "Un test de résolution DI enregistre les 3 nouveaux services UIA et prouve que SessionMonitor ET DesktopUiaPollService se résolvent — garde réelle au-delà de `dotnet build` (une DI mal ordonnée ou un service non enregistré planterait au démarrage, pas au build)."
    - "Le widget affiche le TYPE de chaque session bureau (Chat / Code / Cowork) dans la pastille."
    - "App Claude fermée / source bureau null → aucune session bureau, aucun crash, sessions CLI intactes."
  artifacts:
    - path: "src/Chronos/Services/SessionMonitor.cs"
      provides: "Fusion de la source bureau (ISessionSource optionnelle) dans Read"
      contains: "ISessionSource"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Enregistrement DI : IUiaTreeProvider→WindowsUiaTreeProvider, DesktopUiaSessionSource, DesktopUiaPollService, injection dans SessionMonitor"
      contains: "WindowsUiaTreeProvider"
    - path: "src/Chronos/ViewModels/SessionsViewModel.cs"
      provides: "Libellé de type (Chat/Code/Cowork) sur SessionItemVm"
      contains: "Kind"
    - path: "src/Chronos/Views/SessionsWindow.xaml"
      provides: "Affichage du type dans la pastille"
      contains: "KindLabel"
    - path: "tests/Chronos.Tests/CompositionRootTests.cs"
      provides: "Garde DI réelle : les 3 services UIA + SessionMonitor + DesktopUiaPollService se résolvent"
      contains: "DesktopUiaPollService"
  key_links:
    - from: "SessionMonitor.Read"
      to: "_desktop.Read(now)"
      via: "fusion après transcripts + hooks, avant filtre archived"
      pattern: "_desktop"
    - from: "App.ConfigureServices"
      to: "SessionMonitor(desktop)"
      via: "injection de DesktopUiaSessionSource"
      pattern: "DesktopUiaSessionSource"
    - from: "SessionsViewModel.Refresh"
      to: "SessionItemVm.KindLabel"
      via: "mappage SessionKind → libellé fr"
      pattern: "KindLabel"
---

<objective>
Brancher la source bureau dans le pipeline vivant : fusion dans `SessionMonitor.Read`, câblage DI complet
dans `App.xaml.cs` (provider réel + source + poll de fond), et affichage du TYPE de session dans le widget.
À l'issue, une session de l'app bureau Claude apparaît réellement dans les pastilles, typée, à côté des
sessions Claude Code CLI.

Purpose: Rendre la chaîne visible de bout en bout (BUR-01) et exposer le type à l'utilisateur (BUR-03),
sans casser les sessions CLI existantes ni bloquer l'UI.
Output: 4 fichiers modifiés + tests d'intégration (fusion + libellé de type) via faux ISessionSource +
extension de la garde DI (CompositionRootTests) couvrant les nouveaux services UIA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-source-uia-app-bureau/13-CONTEXT.md

<interfaces>
<!-- Fournis par Plans 01/02, consommés ici : -->
// public interface ISessionSource { IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now); }
// public enum SessionKind { Unknown, Chat, Code, Cowork }
// SessionSnapshot a maintenant .Kind et .Origin
// public sealed class DesktopUiaSessionSource : ISessionSource
// public sealed class WindowsUiaTreeProvider : IUiaTreeProvider
// public sealed class DesktopUiaPollService : IHostedService, IDisposable

// SessionMonitor ctor ACTUEL (à étendre) :
//   public SessionMonitor(string? sessionsDir = null, TranscriptSessionSource? transcripts = null, ArchiveStore? archive = null)
</interfaces>

@src/Chronos/Services/SessionMonitor.cs
@src/Chronos/App.xaml.cs
@src/Chronos/ViewModels/SessionsViewModel.cs
@src/Chronos/Views/SessionsWindow.xaml
@tests/Chronos.Tests/SessionsTests.cs
@tests/Chronos.Tests/CompositionRootTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fusionner la source bureau dans SessionMonitor.Read (paramètre optionnel non cassant)</name>
  <files>src/Chronos/Services/SessionMonitor.cs, tests/Chronos.Tests/SessionsTests.cs</files>
  <read_first>
    - src/Chronos/Services/SessionMonitor.cs (ctor + méthode Read : ordre transcripts → hooks → filtre archived)
    - src/Chronos/Services/ISessionSource.cs (Plan 01)
    - tests/Chronos.Tests/SessionsTests.cs (patrons de test SessionMonitor : SessionMonitor(dir, transcripts, archive))
  </read_first>
  <behavior>
    - Un SessionMonitor avec une source bureau fournissant `desktop:foreground:chat` (WaitingTurn, Kind=Chat, Origin=Desktop) et un transcript CLI `cli-1` (Working) → Read retourne LES DEUX (2 snapshots), le CLI inchangé.
    - Si la source bureau et le CLI portaient la MÊME clé, la source bureau (fusionnée en dernier) primerait — mais en pratique les clés `desktop:...` sont disjointes des session_id JSONL.
    - Une source bureau null (défaut) → comportement identique à aujourd'hui (sessions CLI seules), aucun crash.
    - Les sessions bureau restent soumises au filtre `archived` (clic droit) comme les autres.
  </behavior>
  <action>
    1. Étendre le ctor `SessionMonitor` avec un paramètre optionnel EN FIN : `ISessionSource? desktop = null`
       (non cassant : tous les appels existants — App.xaml.cs, tests — restent valides). Stocker `_desktop`.
    2. Dans `Read(now)`, APRÈS la boucle transcripts (1) et la boucle hooks (2), et AVANT le filtre archived (3),
       ajouter une étape : `if (_desktop is not null) foreach (var d in _desktop.Read(now)) byId[d.SessionId] = d;`
       entourée d'un try/catch tolérant (une source bureau ne doit jamais casser la lecture). Commenter en français :
       « 2.b) Source BUREAU (UIA) : clés synthétiques `desktop:...` disjointes des session_id JSONL → fusion additive ;
       lecture non bloquante (cache du poll de fond, ROB-07) ».
    3. Le filtre archived (3) s'applique inchangé à l'ensemble fusionné (les clés `desktop:...` sont archivables).
    4. Étendre `SessionsTests.cs` : ajouter un faux `ISessionSource` (classe de test renvoyant une liste fixe)
       et un test `Monitor_fusionne_la_source_bureau_avec_les_sessions_cli` prouvant les 2 snapshots + non-régression
       quand desktop=null. Réutiliser le patron `WriteTranscript`/`WriteState` existant.
  </action>
  <verify>
    <automated>dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj" --filter "FullyQualifiedName~SessionsTests"</automated>
  </verify>
  <acceptance_criteria>
    - Le test de fusion bureau+CLI passe ; les tests SessionMonitor existants (staleness, archive, hook prioritaire) restent verts.
    - grep : `SessionMonitor.cs` contient `ISessionSource? desktop` et `_desktop`.
    - grep : la fusion est bien AVANT le filtre archived (le `Where(... archived ...)` reste la dernière étape).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Câblage DI dans App.xaml.cs (provider réel + source bureau + poll de fond + injection SessionMonitor) + garde DI réelle dans CompositionRootTests</name>
  <files>src/Chronos/App.xaml.cs, tests/Chronos.Tests/CompositionRootTests.cs</files>
  <read_first>
    - src/Chronos/App.xaml.cs (ConfigureServices : bloc « Widget de sessions » lignes ~174-184 ; enregistrement SessionMonitor ligne ~177 ; patron AddHostedService lignes ~237-238)
    - src/Chronos/Services/WindowsUiaTreeProvider.cs, DesktopUiaSessionSource.cs, DesktopUiaPollService.cs (Plans 01/02)
    - tests/Chronos.Tests/CompositionRootTests.cs (conteneur MIROIR reconstruit à la main : il n'inclut PAS les services du bloc « Widget de sessions » — c'est le trou à combler pour attraper une DI mal ordonnée au démarrage)
  </read_first>
  <action>
    1. Dans `ConfigureServices`, dans le bloc « Widget de sessions », AVANT l'enregistrement de `SessionMonitor`,
       ajouter (Singletons) :
       - `services.AddSingleton<IUiaTreeProvider>(_ => new WindowsUiaTreeProvider());`
       - `services.AddSingleton(sp => new DesktopUiaSessionSource(sp.GetRequiredService<IUiaTreeProvider>()));`
    2. Modifier l'enregistrement du `SessionMonitor` (actuellement `new SessionMonitor(null, null, archive)`) pour
       injecter la source bureau en 4e argument :
       `services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(), sp.GetRequiredService<DesktopUiaSessionSource>()));`
    3. Enregistrer le poll de fond comme IHostedService (démarré/arrêté par le host, cf. RefreshOrchestrator) :
       - `services.AddSingleton(sp => new DesktopUiaPollService(sp.GetRequiredService<DesktopUiaSessionSource>(), sp.GetRequiredService<IClock>()));`
       - `services.AddHostedService(sp => sp.GetRequiredService<DesktopUiaPollService>());`
    4. Commentaires en français expliquant la chaîne : provider réel → source (cache) → poll de fond (hors UI) →
       SessionMonitor (fusion) → widget. Ne toucher à AUCUN autre enregistrement.
    5. GARDE DI RÉELLE — étendre `CompositionRootTests.cs`. Le test actuel reconstruit un conteneur MIROIR à la
       main qui n'inclut PAS le bloc « Widget de sessions » : une DI mal ordonnée (ex. `SessionMonitor` résolu
       avant que `DesktopUiaSessionSource` soit enregistré, ou `IUiaTreeProvider` non enregistré) COMPILERAIT et
       ne planterait qu'au démarrage de l'app. Ajouter un NOUVEAU test (ne pas alourdir `Host_resout_et_dispose_les_singletons`) :
       `[Fact] public void Le_graphe_DI_resout_les_services_bureau_UIA()` qui, dans un `ServiceCollection` neuf,
       enregistre EXACTEMENT la sous-chaîne bureau telle que dans App.xaml.cs :
       `IClock`→SystemClock, `ArchiveStore`, `IUiaTreeProvider`→WindowsUiaTreeProvider, `DesktopUiaSessionSource`,
       `SessionMonitor(null, null, archive, desktop)` et `DesktopUiaPollService` (+ AddHostedService), puis
       `BuildServiceProvider()` et ASSERTE `Assert.NotNull(provider.GetRequiredService<SessionMonitor>())` ET
       `Assert.NotNull(provider.GetRequiredService<DesktopUiaPollService>())` (résolution sans exception = graphe
       câblé dans le bon ordre). Test [Fact] simple (aucun besoin de STA/WPF ici — pas de MainWindow). Commentaire
       fr : garde au-delà du build — attrape un service manquant / mal ordonné qui ne casserait qu'au runtime.
  </action>
  <verify>
    <automated>dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj" --filter "FullyQualifiedName~CompositionRootTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` réussit (graphe DI résolvable).
    - grep : `App.xaml.cs` contient `WindowsUiaTreeProvider`, `DesktopUiaSessionSource`, `DesktopUiaPollService` et `AddHostedService`.
    - grep : l'enregistrement `SessionMonitor` passe désormais `DesktopUiaSessionSource` en argument.
    - grep : `CompositionRootTests.cs` contient `DesktopUiaPollService` et `WindowsUiaTreeProvider` et un `GetRequiredService<SessionMonitor>()`.
    - `dotnet test --filter "FullyQualifiedName~CompositionRootTests"` passe (nouveau test de résolution bureau vert).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Afficher le type (Chat/Code/Cowork) dans le widget de sessions</name>
  <files>src/Chronos/ViewModels/SessionsViewModel.cs, src/Chronos/Views/SessionsWindow.xaml, tests/Chronos.Tests/SessionsTests.cs</files>
  <read_first>
    - src/Chronos/ViewModels/SessionsViewModel.cs (SessionItemVm : [ObservableProperty] ; Refresh remplit les items ; Describe(activity))
    - src/Chronos/Views/SessionsWindow.xaml (DataTemplate de la pastille : Project + StateText + Detail)
    - src/Chronos/Services/SessionSnapshot.cs (champ Kind)
    - tests/Chronos.Tests/SessionsTests.cs (comment tester le VM ; s'il n'y a pas de test VM, en créer via SessionMonitor + faux ISessionSource)
  </read_first>
  <behavior>
    - Un snapshot Kind=Code → SessionItemVm.KindLabel == "Code" ; Kind=Chat → "Chat" ; Kind=Cowork → "Cowork".
    - Un snapshot Kind=Unknown (sessions CLI) → KindLabel == "" (rien d'affiché, pas de bruit).
    - Après Refresh, l'item bureau porte bien son KindLabel.
  </behavior>
  <action>
    1. Dans `SessionItemVm` : ajouter `[ObservableProperty] private string _kindLabel = "";`.
    2. Dans `SessionsViewModel.Refresh`, lors de la construction de chaque `SessionItemVm`, définir
       `it.KindLabel = KindText(s.Kind);` où `KindText` est un helper statique privé :
       `SessionKind.Chat => "Chat", SessionKind.Code => "Code", SessionKind.Cowork => "Cowork", _ => ""`.
       (Rester en français des libellés courts ; ces mots sont des noms propres de modes, conservés tels quels.)
    3. Dans `SessionsWindow.xaml`, dans le `DataTemplate` de la pastille, ajouter un petit `TextBlock`
       lié à `KindLabel` (ex. à droite du Project ou en suffixe de la ligne d'état), masqué quand vide :
       utiliser un style/Trigger sur `KindLabel == ""` → `Visibility=Collapsed`, sinon visible. Couleur discrète
       (#8C8A98) cohérente avec le Detail. Ne PAS casser le layout chromeless existant (pastille compacte).
    4. Étendre `SessionsTests.cs` : test `Widget_affiche_le_type_de_session_bureau` — via un SessionMonitor
       alimenté par un faux `ISessionSource` renvoyant un snapshot Kind=Code (Origin=Desktop), construire un
       `SessionsViewModel`, appeler `Refresh(now)`, asserter `Items[0].KindLabel == "Code"` et qu'un snapshot
       Kind=Unknown donne `KindLabel == ""`. (Le VM référence des Brush WPF : ce test tourne déjà côté projet
       de tests qui référence WPF — voir tests VM existants comme WindowGaugeViewModelTests / SessionsViewModel.)
  </action>
  <verify>
    <automated>dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj" --filter "FullyQualifiedName~SessionsTests"</automated>
  </verify>
  <acceptance_criteria>
    - Le test « KindLabel = Code / vide pour Unknown » passe.
    - grep : `SessionsViewModel.cs` contient `KindLabel` et un mappage `SessionKind.Code => "Code"`.
    - grep : `SessionsWindow.xaml` contient `KindLabel`.
    - `dotnet build` du projet réussit et la suite complète `dotnet test` reste verte.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build "src/Chronos/Chronos.csproj"` réussit.
- `dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj"` : suite entière verte (existants + nouveaux tests de fusion, de résolution DI bureau et de type).
- `CompositionRootTests` : le nouveau test de résolution des services bureau (SessionMonitor + DesktopUiaPollService) passe.
- Vérification manuelle facultative (checkpoint non requis) : lancer l'app avec l'app bureau Claude ouverte → une pastille bureau typée apparaît ; app fermée → aucune pastille bureau, sessions CLI intactes.
</verification>

<success_criteria>
- Une session de l'app bureau Claude apparaît dans le widget, à côté des sessions CLI (BUR-01).
- Chaque session bureau affiche son type Chat/Code/Cowork (BUR-03).
- La source bureau est câblée (provider réel + poll de fond non bloquant) et son absence ne casse rien.
- Le câblage DI des 3 services UIA est prouvé par un test de résolution réel (pas seulement `dotnet build`).
- Aucune régression des ~215 tests.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/13-source-uia-app-bureau/13-03-SUMMARY.md`.
</output>
</content>
