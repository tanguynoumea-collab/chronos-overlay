---
phase: 13-source-uia-app-bureau
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/SessionSnapshot.cs
  - src/Chronos/Services/UiaNode.cs
  - src/Chronos/Services/IUiaTreeProvider.cs
  - src/Chronos/Services/ISessionSource.cs
  - src/Chronos/Services/UiaLabels.cs
  - src/Chronos/Services/DesktopUiaSessionSource.cs
  - tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs
autonomous: true
requirements: [BUR-02, BUR-03, BUR-04, BUR-05, ROB-06]

must_haves:
  truths:
    - "Un faux arbre UIA « Claude répond. » (ou bouton « Arrêter ») produit un snapshot Working."
    - "Un faux arbre avec « Ignorer les permissions » produit un snapshot WaitingAttention."
    - "Un faux arbre « Mode chat » + placeholder sans « Claude répond. » produit un snapshot WaitingTurn."
    - "Le type est dérivé : « Mode chat » → Chat ; panneaux Terminal/Diff/Aperçu/Actions de session → Code ; « Contrôle à distance » → Cowork."
    - "Une session Cowork VM distante (« Contrôle à distance ») est marquée Kind=Cowork, Activity=Unknown — jamais un état d'exécution certain."
    - "Chaque bouton sidebar « En cours d'exécution <nom> » produit une session active nommée <nom> (Working) ; les autres entrées ne sont PAS listées."
    - "L'ancre du foreground se reconnaît par `UiaNode.AutomationId == \"RootWebArea\"` (ControlType Document) — SEULE exception au principe « ne pas matcher par AutomationId » (rôle a11y stable, pas un `base-ui-_r_XXX_` volatil)."
    - "Un arbre racine null / ancre `RootWebArea` (AutomationId==\"RootWebArea\") absente produit une liste VIDE, jamais d'exception."
    - "Repli tracé : si la fenêtre Claude (Window Name contient « Claude ») est présente mais l'ancre `RootWebArea` absente, la dégradation vers liste vide est TRACÉE (Health=AnchorMissing) — jamais un zéro silencieux non distingué de « fenêtre absente »."
    - "Le matching est insensible à la casse et aux espaces, et reconnaît les libellés fr ET en."
  artifacts:
    - path: "src/Chronos/Services/SessionSnapshot.cs"
      provides: "SessionSnapshot étendu avec Kind/Origin (défauts non cassants) + enums SessionKind/SessionOrigin"
      contains: "SessionKind"
    - path: "src/Chronos/Services/UiaNode.cs"
      provides: "DTO neutre d'arbre UIA (dont AutomationId, porteur de l'ancre RootWebArea)"
      contains: "record UiaNode"
    - path: "src/Chronos/Services/IUiaTreeProvider.cs"
      provides: "Seam injectable retournant un UiaNode neutre"
      contains: "interface IUiaTreeProvider"
    - path: "src/Chronos/Services/ISessionSource.cs"
      provides: "Contrat commun de source de sessions"
      contains: "interface ISessionSource"
    - path: "src/Chronos/Services/UiaLabels.cs"
      provides: "Table de libellés fr/en + helpers de matching tolérant"
      contains: "class UiaLabels"
    - path: "src/Chronos/Services/DesktopUiaSessionSource.cs"
      provides: "Logique pure MapTree(arbre→snapshots) + ancre AutomationId==RootWebArea + Health (repli tracé) + cache + Read non bloquant"
      contains: "MapTree"
    - path: "tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs"
      provides: "Suite de tests via faux arbre (aucune fenêtre Claude réelle)"
      contains: "FakeUiaNode"
  key_links:
    - from: "DesktopUiaSessionSource.MapTree"
      to: "ancre RootWebArea"
      via: "descendant dont AutomationId == \"RootWebArea\" (ControlType Document)"
      pattern: "RootWebArea"
    - from: "DesktopUiaSessionSource.MapTree"
      to: "UiaLabels"
      via: "matching ControlType+Name fr/en tolérant"
      pattern: "UiaLabels\\."
    - from: "DesktopUiaSessionSource.Read"
      to: "_cache"
      via: "retour synchrone du dernier cache (aucune I/O UIA)"
      pattern: "_cache"
---

<objective>
Poser TOUTE la logique testable de la source bureau, sans aucune dépendance à Windows UI Automation :
le modèle étendu (`SessionSnapshot.Kind/Origin`), le seam `IUiaTreeProvider` + DTO neutre `UiaNode`, la
table de libellés fr/en (`UiaLabels`), et `DesktopUiaSessionSource` dont la méthode pure `MapTree` traduit
un arbre neutre en `SessionSnapshot`. Le gros des tests de la phase vit ici, tous alimentés par un FAUX
arbre — aucune fenêtre Claude réelle requise.

Purpose: Isoler la logique métier derrière une frontière testable (contrainte n°1 du CONTEXT). Les états
honnêtes (BUR-02/BUR-05), la distinction de type (BUR-03), l'énumération sidebar (BUR-04) et la robustesse
souple fr/en + dégradation (ROB-06) se prouvent ici par `dotnet test`.
Output: 6 fichiers source neufs/étendus + 1 fichier de tests neuf. Aucun câblage réel (Plans 02/03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-source-uia-app-bureau/13-CONTEXT.md

<interfaces>
<!-- Contrats à créer dans ce plan. L'exécuteur les implémente tels quels — aucune exploration requise. -->

// src/Chronos/Services/SessionSnapshot.cs (ÉTENDU — ajouts NON cassants en fin de record)
public enum SessionKind { Unknown, Chat, Code, Cowork }
public enum SessionOrigin { Cli, Desktop }
public sealed record SessionSnapshot(
    string SessionId,
    string Project,
    SessionActivity Activity,
    string? Reason,
    System.DateTimeOffset UpdatedAt,
    SessionKind Kind = SessionKind.Unknown,      // défaut Unknown → usages CLI existants inchangés
    SessionOrigin Origin = SessionOrigin.Cli);   // défaut Cli    → usages CLI existants inchangés

// src/Chronos/Services/UiaNode.cs
// NB ancre : c'est le champ AutomationId qui porte le littéral "RootWebArea" (ControlType="Document").
// Le WindowsUiaTreeProvider (Plan 02) DOIT donc renseigner AutomationId depuis Current.AutomationId.
public sealed record UiaNode(
    string ControlType,
    string Name,
    string? AutomationId,
    bool Enabled,
    System.Collections.Generic.IReadOnlyList<UiaNode> Children);

// src/Chronos/Services/IUiaTreeProvider.cs
public interface IUiaTreeProvider
{
    /// Arbre neutre de la fenêtre Claude, ou null si absente/indisponible. NE LÈVE JAMAIS.
    UiaNode? TryGetTree();
}

// src/Chronos/Services/ISessionSource.cs
public interface ISessionSource
{
    System.Collections.Generic.IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);
}

// src/Chronos/Services/DesktopUiaSessionSource.cs (surface publique)
public enum DesktopHealth { Unknown, Ok, WindowMissing, AnchorMissing } // repli tracé (pas de zéro silencieux)
public sealed class DesktopUiaSessionSource : ISessionSource
{
    public DesktopUiaSessionSource(IUiaTreeProvider provider);
    public DesktopHealth Health { get; }          // dernier état de santé du poll (diag/futur)
    public void Poll(System.DateTimeOffset now);   // appelé HORS thread UI (Plan 02) — remplit le cache
    public System.Collections.Generic.IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now); // rend le cache, non bloquant
    public static System.Collections.Generic.IReadOnlyList<SessionSnapshot> MapTree(UiaNode? root, System.DateTimeOffset now); // PUR, testable
}
</interfaces>

<!-- Patrons à imiter : source neutre, lecture tolérante, aucun type WPF. -->
@src/Chronos/Services/SessionSnapshot.cs
@src/Chronos/Services/TranscriptSessionSource.cs
@tests/Chronos.Tests/SessionsTests.cs
@tests/Chronos.Tests/ServicesLayerPurityTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre SessionSnapshot (Kind/Origin) + poser les seams neutres (UiaNode, IUiaTreeProvider, ISessionSource)</name>
  <files>src/Chronos/Services/SessionSnapshot.cs, src/Chronos/Services/UiaNode.cs, src/Chronos/Services/IUiaTreeProvider.cs, src/Chronos/Services/ISessionSource.cs</files>
  <read_first>
    - src/Chronos/Services/SessionSnapshot.cs (record + enum existants à étendre)
    - src/Chronos/Services/TranscriptSessionSource.cs (voir usages `new SessionSnapshot(...)` positionnels à ne pas casser)
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (les nouveaux types Chronos.Services doivent rester NEUTRES : aucun type WPF dans les signatures publiques)
  </read_first>
  <behavior>
    - Un `new SessionSnapshot("id","p", SessionActivity.Working, null, now)` (5 args positionnels existants) compile toujours et donne Kind=Unknown, Origin=Cli.
    - `snap with { Kind = SessionKind.Code, Origin = SessionOrigin.Desktop }` fonctionne.
    - `UiaNode`, `IUiaTreeProvider`, `ISessionSource` existent dans le namespace Chronos.Services.
  </behavior>
  <action>
    1. Dans `SessionSnapshot.cs` : ajouter `public enum SessionKind { Unknown, Chat, Code, Cowork }` et
       `public enum SessionOrigin { Cli, Desktop }`. Étendre le record avec DEUX paramètres AJOUTÉS EN FIN,
       avec valeurs par défaut : `SessionKind Kind = SessionKind.Unknown, SessionOrigin Origin = SessionOrigin.Cli`.
       Ne PAS réordonner les paramètres existants (SessionId, Project, Activity, Reason, UpdatedAt) — les
       usages positionnels de `TranscriptSessionSource` / `SessionMonitor` / tests doivent rester compilables.
       Commenter en français que les défauts garantissent la non-régression CLI.
    2. Créer `UiaNode.cs` : record `UiaNode(string ControlType, string Name, string? AutomationId, bool Enabled, IReadOnlyList<UiaNode> Children)`.
       Commentaire fr : DTO NEUTRE d'arbre d'accessibilité, testable par faux arbre, aucun type WPF. Préciser
       que `AutomationId` est significatif : il porte le littéral d'ancre `RootWebArea` (rôle a11y Chromium/IA2,
       ControlType="Document"), seul cas où l'AutomationId est matché (cf. Task 3).
    3. Créer `IUiaTreeProvider.cs` : interface avec `UiaNode? TryGetTree();`. Commentaire fr :
       « retourne l'arbre neutre de la fenêtre Claude, null si absente ; NE LÈVE JAMAIS (dégradation) ».
    4. Créer `ISessionSource.cs` : interface `IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now);`.
       Commentaire fr : contrat commun (transcripts + bureau), fusion dans SessionMonitor.
    5. Tous ces types dans le namespace `Chronos.Services`, aucun `using System.Windows.*`.
  </action>
  <verify>
    <automated>dotnet build "src/Chronos/Chronos.csproj" -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` du projet Chronos réussit (les usages positionnels existants compilent inchangés).
    - grep : `SessionSnapshot.cs` contient `SessionKind` et `SessionOrigin` et `Kind = SessionKind.Unknown`.
    - grep : `UiaNode.cs` contient `record UiaNode`, `IUiaTreeProvider.cs` contient `interface IUiaTreeProvider`, `ISessionSource.cs` contient `interface ISessionSource`.
    - grep : aucun `using System.Windows` dans les 4 fichiers.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: (Wave-0) Réconcilier l'état REPOS + Table de libellés fr/en (UiaLabels) + helpers de matching tolérant, testés</name>
  <files>src/Chronos/Services/UiaLabels.cs, tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs</files>
  <read_first>
    - .planning/phases/13-source-uia-app-bureau/13-CONTEXT.md (section « Mapping des états » — libellés vérifiés au spike ; § Specific Ideas : snapshot REPOS manquant)
    - scratchpad/uia-spike.ps1 (script de dump UIA déjà présent — sert à la capture REPOS ci-dessous)
    - src/Chronos/Services/UiaNode.cs (créé en Task 1)
    - tests/Chronos.Tests/SessionsTests.cs (style xUnit : [Fact]/[Theory], assertions, noms fr)
  </read_first>
  <behavior>
    - `UiaLabels.Matches("  claude RÉPOND.  ", UiaLabels.Responding)` == true (insensible casse/espaces).
    - `UiaLabels.Matches("Claude is responding", UiaLabels.Responding)` == true (variante en).
    - `UiaLabels.Matches(null, UiaLabels.Responding)` == false (jamais d'exception).
    - `UiaLabels.StartsWithAny("En cours d'exécution mon-projet", UiaLabels.RunningPrefix, out var nom)` == true, nom == "mon-projet".
    - `UiaLabels.StartsWithAny("Running my-proj", UiaLabels.RunningPrefix, out var n2)` == true, n2 == "my-proj".
  </behavior>
  <action>
    0. RÉCONCILIATION ÉTAT REPOS (Wave-0, court — le spike a été pris PENDANT une génération, donc l'état
       repos n'est pas confirmé). Lancer `scratchpad/uia-spike.ps1` sur la fenêtre Claude À L'ÉTAT REPOS
       (aucune génération en cours) pour dumper son arbre UIA, et VÉRIFIER/ajuster les littéraux de
       `UiaLabels.ChatMode`, `UiaLabels.ChatPlaceholder` et le signal du bouton d'ENVOI (présence/état du
       bouton quand « Claude répond. » a DISPARU) AVANT de figer la détection WaitingTurn (BUR-02). Consigner
       les libellés confirmés dans la mémoire `chronos-desktop-uia.md`. Si l'app est occupée / indisponible au
       moment de l'exécution : NE PAS bloquer la phase — s'appuyer sur le matching tolérant + la table fr/en
       extensible (ci-dessous) et noter le point à re-vérifier. Cette étape ne modifie aucun fichier source si
       les littéraux du spike initial sont confirmés ; elle protège contre un WaitingTurn mal détecté en prod.
    1. Créer `UiaLabels.cs` (static class, namespace Chronos.Services, aucun type WPF). Déclarer des tableaux
       `string[]` fr/en pour CHAQUE signal vérifié au spike (CONTEXT § Mapping, réconciliés à l'étape 0) :
       - `Responding` = { "Claude répond.", "Claude is responding" }
       - `StopButton` = { "Arrêter", "Stop" }
       - `PermissionButton` = { "Ignorer les permissions", "Skip permissions", "Autoriser", "Allow" }  (+ variantes autorisation)
       - `ChatMode` = { "Mode chat", "Chat mode" }
       - `ChatPlaceholder` = { "Tapez / pour les commandes", "Type / for commands" }
       - `RunningPrefix` = { "En cours d'exécution", "Running" }
       - `RemoteControl` = { "Contrôle à distance", "Remote control" }
       - `CodePanels` = { "Terminal", "Diff", "Aperçu", "Preview", "Actions de session", "Session actions" }
       Commenter en français que la table est EXTENSIBLE (une MAJ de l'app ajoute un libellé sans toucher au code de logique) — c'est le cœur de ROB-06 ; NE JAMAIS coder de `AutomationId` (`base-ui-_r_XXX_`).
    2. Helper `public static bool Matches(string? name, string[] variants)` : compare `name` (trim + casse
       insensible, culture-invariante) à chaque variante ; false si name null/vide. Utiliser
       `string.Equals(a, b, StringComparison.OrdinalIgnoreCase)` sur les valeurs trimées.
    3. Helper `public static bool StartsWithAny(string? name, string[] prefixes, out string remainder)` :
       si `name` (trimé) commence par un préfixe (casse insensible), remainder = reste trimé, retourne true ;
       sinon remainder = "", false. Sert à extraire le `<nom>` de « En cours d'exécution <nom> » (BUR-04).
    4. Créer `tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs` avec une classe `DesktopUiaSessionSourceTests`.
       Ajouter d'abord un petit helper de construction de faux arbre en haut du fichier (réutilisé en Task 3) :
       `private static UiaNode FakeUiaNode(string type, string name, params UiaNode[] children) => new(type, name, null, true, children);`
       et une surcharge portant un AutomationId (pour construire l'ancre) :
       `private static UiaNode FakeUiaNode(string type, string name, string? aid, params UiaNode[] children) => new(type, name, aid, true, children);`
       Écrire les [Fact]/[Theory] couvrant les 5 comportements ci-dessus (matching fr/en, casse/espaces, null, extraction de nom).
  </action>
  <verify>
    <automated>dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj" --filter "FullyQualifiedName~DesktopUiaSessionSourceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Les tests de matching (fr/en, casse, espaces, null, StartsWithAny) passent.
    - grep : `UiaLabels.cs` contient `Responding`, `RunningPrefix`, `RemoteControl`, `ChatMode`, `StopButton`, `PermissionButton`, `CodePanels`.
    - grep : `UiaLabels.cs` ne contient AUCUN `AutomationId` codé en dur ni `base-ui`.
    - grep : `DesktopUiaSessionSourceTests.cs` contient `FakeUiaNode`.
    - Étape 0 tracée : les libellés ChatMode/ChatPlaceholder/bouton d'envoi sont confirmés (ou le point à re-vérifier est noté dans la mémoire / le SUMMARY).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DesktopUiaSessionSource — MapTree pur (ancre RootWebArea via AutomationId, états, type, sidebar, clés synthétiques, santé/repli tracé) + cache, suite de tests par faux arbre</name>
  <files>src/Chronos/Services/DesktopUiaSessionSource.cs, tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs</files>
  <read_first>
    - .planning/phases/13-source-uia-app-bureau/13-CONTEXT.md (sections « Mapping des états », « Type de session », « Cowork-VM », « Identité des sessions », « Robustesse »)
    - src/Chronos/Services/UiaNode.cs, src/Chronos/Services/UiaLabels.cs, src/Chronos/Services/ISessionSource.cs, src/Chronos/Services/SessionSnapshot.cs
    - src/Chronos/Services/TranscriptSessionSource.cs (patron de source neutre + tolérance)
  </read_first>
  <behavior>
    - MapTree(null, now) == liste vide (santé : racine absente). Aucune exception.
    - MapTree(arbre AVEC fenêtre Claude mais SANS ancre `RootWebArea` (aucun descendant AutomationId=="RootWebArea"), now) == liste vide. Aucune exception. (Le repli est tracé côté source : Health=AnchorMissing après Poll — cf. test dédié.)
    - MapTree(arbre AVEC un descendant `AutomationId=="RootWebArea"` (ControlType "Document") et Text "Claude répond.", now) → 1 snapshot foreground Activity=Working, clé `desktop:foreground:<kind>`.
    - Arbre (ancre présente) avec Button "Arrêter" (sans "Claude répond.") → Working.
    - Arbre (ancre présente) avec Button "Ignorer les permissions" → WaitingAttention.
    - Arbre (ancre présente) avec Text "Mode chat" + Text placeholder "Tapez / pour les commandes", SANS "Claude répond." → WaitingTurn, Kind=Chat, clé `desktop:foreground:chat`.
    - Arbre (ancre présente) avec vue/onglet Code + panneaux "Terminal"/"Diff"/"Actions de session" → Kind=Code.
    - Arbre (ancre présente) avec "Contrôle à distance" → un snapshot Kind=Cowork, Activity=Unknown (BUR-05 : jamais un état d'exécution certain).
    - Arbre (ancre présente) avec 2 boutons sidebar "En cours d'exécution alpha" et "En cours d'exécution beta" → 2 snapshots Working nommés alpha/beta, clés `desktop:<kind>:alpha` / `desktop:<kind>:beta`.
    - Un bouton sidebar SANS le préfixe « En cours d'exécution » n'est PAS listé.
    - Ancre présente mais aucun signal exploitable dans le foreground → Activity=Unknown (jamais inventé), pas d'exception.
    - Poll : `Health` reflète le dernier diagnostic — Ok (ancre trouvée), AnchorMissing (fenêtre présente, ancre absente), WindowMissing (arbre null). Le zéro « fenêtre présente mais ancre absente » n'est donc PAS confondu avec « app fermée ».
  </behavior>
  <action>
    1. Créer `DesktopUiaSessionSource.cs` (sealed class : ISessionSource, namespace Chronos.Services, NEUTRE).
       Déclarer `public enum DesktopHealth { Unknown, Ok, WindowMissing, AnchorMissing }`.
       Champs : `private readonly IUiaTreeProvider _provider;`,
       `private volatile IReadOnlyList<SessionSnapshot> _cache = System.Array.Empty<SessionSnapshot>();`,
       et une propriété `public DesktopHealth Health { get; private set; } = DesktopHealth.Unknown;`.
       Ctor `DesktopUiaSessionSource(IUiaTreeProvider provider)`.
    2. `public void Poll(DateTimeOffset now)` : try { `var tree = _provider.TryGetTree(); Health = EvaluateHealth(tree); _cache = MapTree(tree, now);` } catch { /* dégradation : ne pas jeter, garder l'ancien cache */ }.
       `private static DesktopHealth EvaluateHealth(UiaNode? root)` : root null → WindowMissing ; sinon si un
       descendant a `AutomationId == "RootWebArea"` → Ok ; sinon (fenêtre présente mais ancre absente, ex.
       changement de version de l'app) → AnchorMissing. Commentaire fr : le repli AnchorMissing est TRACÉ pour
       le diag (pas un zéro silencieux) et distinct de WindowMissing (app fermée). Commentaire fr : Poll est
       appelé HORS thread UI par le poll (Plan 02).
    3. `public IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now) => _cache;` — NON bloquant (ROB-07),
       aucune I/O UIA dans ce chemin (c'est ce qu'appelle le timer 2 s de SessionsViewModel via SessionMonitor).
    4. `public static IReadOnlyList<SessionSnapshot> MapTree(UiaNode? root, DateTimeOffset now)` — PURE. Étapes :
       a. SANTÉ / ANCRE : si `root is null` → retourner liste vide. Parcourir l'arbre (helper récursif
          `IEnumerable<UiaNode> Descendants(UiaNode)`), et vérifier la présence de l'ANCRE `RootWebArea` par
          `n.AutomationId == "RootWebArea"` (comparaison Ordinal ; c'est un rôle d'accessibilité Chromium/IA2
          stable porté par l'AutomationId, ControlType attendu "Document" — NB : SEULE exception au principe
          « ne pas matcher par AutomationId » du CONTEXT, car ce littéral N'EST PAS un `base-ui-_r_XXX_`
          volatil). Ancre absente → retourner liste VIDE (le repli tracé Health=AnchorMissing est posé par
          Poll/EvaluateHealth, pas ici). JAMAIS d'exception (tout le parcours est sur le DTO neutre).
       b. ÉTAT foreground : chercher dans les descendants — `UiaLabels.Matches` sur Responding OU StopButton présent → Working ; sinon PermissionButton présent → WaitingAttention ; sinon ChatMode + ChatPlaceholder présents → WaitingTurn ; sinon Unknown.
       c. TYPE foreground : ChatMode présent → Chat ; un des CodePanels présents (Terminal/Diff/Aperçu/Actions de session) → Code ; RemoteControl présent → Cowork ; sinon Unknown.
       d. COWORK VM (BUR-05) : si RemoteControl présent, l'Activity du snapshot Cowork est FORCÉE à Unknown (sa présence est signalée, son état non), quelle que soit l'inférence de b.
       e. CLÉ foreground : `desktop:foreground:<kind>` où `<kind>` = nom d'enum en minuscules (chat/code/cowork/unknown). Project = libellé lisible (ex. "Claude (bureau)"). Origin=Desktop.
       f. SIDEBAR (BUR-04) : pour chaque descendant ControlType Button dont Name matche `UiaLabels.StartsWithAny(..., RunningPrefix, out nom)` → 1 snapshot Activity=Working, Kind hérité du type foreground (ou Code par défaut agentique), Project=`<nom>`, clé `desktop:<kind>:<nom>`, Origin=Desktop. Les boutons SANS ce préfixe ne produisent RIEN.
       g. Dédupliquer par SessionId (clé synthétique) ; retourner la liste. UpdatedAt = `now` pour tous (signal instantané de l'arbre).
    5. Étendre `DesktopUiaSessionSourceTests.cs` : compléter le helper `FakeUiaNode` (Task 2, dont la surcharge
       avec AutomationId) et couvrir TOUS les comportements ci-dessus. Construire des faux arbres englobant une
       ancre `AutomationId=="RootWebArea"` (ControlType "Document") pour les cas positifs. Aucune fenêtre Claude
       réelle, aucun appel à System.Windows.Automation.
    6. Ajouter un test de repli TRACÉ : un arbre avec une fenêtre Claude (Window Name="Claude") MAIS sans nœud
       AutomationId=="RootWebArea" → après `Poll(now)`, `Read(now)` est vide ET `Health == DesktopHealth.AnchorMissing` ;
       un provider retournant null → `Health == DesktopHealth.WindowMissing` ; un arbre avec ancre → `Health == DesktopHealth.Ok`.
       (Prouve qu'un « fenêtre présente / ancre absente » n'est pas un zéro silencieux confondu avec app fermée.)
    7. Ajouter un test de cache/non-blocage : un faux `IUiaTreeProvider` (implémenté dans le fichier de test)
       comptant ses appels — `Read(now)` AVANT tout Poll retourne vide ; après `Poll(now)`, `Read(now)`
       retourne le résultat mappé SANS rappeler le provider (compteur inchangé entre 2 Read).
  </action>
  <verify>
    <automated>dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj" --filter "FullyQualifiedName~DesktopUiaSessionSourceTests"</automated>
  </verify>
  <acceptance_criteria>
    - Tous les tests de comportement (santé/ancre, repli tracé Health=AnchorMissing/WindowMissing/Ok, Working/WaitingAttention/WaitingTurn/Unknown, type Chat/Code/Cowork, Cowork VM=Unknown, sidebar énumérée + non-préfixée ignorée, cache non bloquant) passent.
    - grep : `DesktopUiaSessionSource.cs` contient `MapTree`, `_cache`, `desktop:foreground:`, `desktop:`, `RootWebArea` et `DesktopHealth`.
    - grep : `DesktopUiaSessionSource.cs` matche l'ancre via `AutomationId` (ex. `AutomationId == "RootWebArea"`), pas par un `base-ui` volatil.
    - grep : `DesktopUiaSessionSource.cs` n'utilise AUCUN `using System.Windows` ni `AutomationElement`.
    - La suite complète `dotnet test` du projet reste verte (aucune régression des ~215 tests existants).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build "src/Chronos/Chronos.csproj"` réussit.
- `dotnet test "tests/Chronos.Tests/Chronos.Tests.csproj"` : suite entière verte (existants + nouveaux DesktopUiaSessionSourceTests).
- `ServicesLayerPurityTests.La_couche_Services_ne_reference_aucun_assembly_WPF` reste vert (nouveaux types Services neutres).
- Aucun fichier de Plan 02/03 n'est modifié ici (WindowsUiaTreeProvider, SessionMonitor, App.xaml.cs, SessionsViewModel intacts).
</verification>

<success_criteria>
- Modèle étendu non cassant (Kind/Origin) + seams neutres (UiaNode, IUiaTreeProvider, ISessionSource) en place.
- `DesktopUiaSessionSource.MapTree` traduit un faux arbre en snapshots honnêtes (BUR-02), typés (BUR-03),
  avec sidebar énumérée (BUR-04) et Cowork VM marqué indéterminé (BUR-05), le tout robuste fr/en + dégradation
  vers Unknown/liste vide sans exception (ROB-06). L'ancre foreground est reconnue par `AutomationId=="RootWebArea"`
  et le repli « fenêtre présente / ancre absente » est TRACÉ (Health=AnchorMissing) — jamais un zéro silencieux.
- `Read` rend un cache sans I/O (fondation de ROB-07, câblé au Plan 02).
- Toute la logique est prouvée sans fenêtre Claude réelle.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/13-source-uia-app-bureau/13-01-SUMMARY.md`.
</output>
</content>
</invoke>
