---
phase: 15-idempotence-des-int-grations
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/Chronos/Services/SessionHookInstaller.cs
  - src/Chronos/Services/StatusLineInstaller.cs
  - src/Chronos/Views/SessionsController.cs
  - src/Chronos/Views/StatusLineSetup.cs
  - tests/Chronos.Tests/SessionsTests.cs
  - tests/Chronos.Tests/StatusLineInstallerTests.cs
autonomous: true
requirements: [PUR-01, PUR-02]
user_setup: []

must_haves:
  truths:
    - "Trois installations successives depuis trois chemins d'exe différents laissent exactement 1 groupe Chronos par événement, soit 5 au total"
    - "L'installation ne touche jamais un groupe de hook non-Chronos (matcher, timeout et champs inconnus préservés)"
    - "L'entrée statusLine Chronos périmée est repointée sur l'exe courant sans dupliquer ni détruire padding"
    - "Une barre statusLine tierce n'est jamais écrasée par l'installation ni retirée par la désinstallation"
    - "Un settings.json inexploitable ne provoque aucune écriture (les deux cœurs purs renvoient null)"
  artifacts:
    - path: "src/Chronos/Services/SessionHookInstaller.cs"
      provides: "Installation retirer-puis-ajouter, prédicat large, abandon sur JSON inexploitable"
      contains: "public static void ApplyHooks"
    - path: "src/Chronos/Services/StatusLineInstaller.cs"
      provides: "Mutation ciblée de statusLine.command, prédicats appartenance/fraîcheur séparés"
      contains: "public static void ApplyStatusLine"
    - path: "tests/Chronos.Tests/SessionsTests.cs"
      provides: "Preuve du non-cumul multi-chemins et de la survie des hooks tiers"
    - path: "tests/Chronos.Tests/StatusLineInstallerTests.cs"
      provides: "Preuve du repointage et de la préservation de padding"
  key_links:
    - from: "src/Chronos/Services/SessionHookInstaller.cs"
      to: "src/Chronos/Services/ClaudeSettingsJson.cs"
      via: "prédicat d'identité partagé"
      pattern: "ClaudeSettingsJson\\.(IsChronosGroup|ParseOrNull|Serialize)"
    - from: "src/Chronos/Services/StatusLineInstaller.cs"
      to: "src/Chronos/Services/ClaudeSettingsJson.cs"
      via: "prédicat d'identité partagé"
      pattern: "ClaudeSettingsJson\\.(IsChronosCommand|PointsToExe|ParseOrNull|Serialize)"
---

<objective>
Corriger la **cause racine** dans les deux installateurs : `SessionHookInstaller.IsOurEntry` identifie une
entrée par le **chemin de l'exe courant**, donc le garde d'idempotence (ligne 69) ne voit jamais les
entrées des versions précédentes et empile un groupe de plus à chaque version — 5 versions = 25 groupes.
Symétriquement, `StatusLineInstaller.IsChronosCommand` accepte n'importe quelle commande contenant
« Chronos », donc `IsEnabled()` répond « oui » pour un exe périmé et le pont reste branché à vie sur
`Chronos-v2.8.1.exe` dans `Downloads`.

Purpose: PUR-01 (hooks non cumulatifs) et PUR-02 (statusLine repointée, jamais dupliquée) — l'avenir
est assaini. La machine **déjà** polluée est traitée par le plan 03.
Output: deux installateurs qui remplacent au lieu de cumuler, qui mutent au lieu de reconstruire, et qui
abandonnent au lieu d'effacer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-idempotence-des-int-grations/15-RESEARCH.md
@.planning/phases/15-idempotence-des-int-grations/15-01-SUMMARY.md

<interfaces>
<!-- Fourni par le plan 15-01. Utiliser DIRECTEMENT, ne pas réimplémenter. -->

```csharp
namespace Chronos.Services;
public static class ClaudeSettingsJson
{
    public const string HookMarker = "--hook";
    public const string StatusLineMarker = "--statusline";
    public static string ExtractExecutable(string? command);
    public static bool IsChronosCommand(string? command, string marker);
    public static bool PointsToExe(string? command, string exePath);
    public static string? CommandOf(JsonNode? handler);
    public static bool IsChronosGroup(JsonNode? group, string marker);
    public static JsonObject? ParseOrNull(string? json);   // null = INEXPLOITABLE → ne rien écrire
    public static string Serialize(JsonObject root);       // WriteIndented + UnsafeRelaxedJsonEscaping
}
```

<!-- Surface publique APRÈS ce plan (les signatures marquées CHANGE cassent des appelants listés dans l'action). -->

```csharp
public sealed class SessionHookInstaller
{
    public static readonly string[] Events;                                   // inchangé (5 événements)
    public SessionHookInstaller(string? settingsPath = null);                 // inchangé
    public string SettingsPath { get; }                                       // inchangé
    public static string HookCommand(string exePath, string ev);              // inchangé (slashes avant)
    public bool IsInstalled(string exePath);                                  // sémantique CHANGE : « CET exe »
    public void Install(string exePath);                                      // n'écrit plus si le JSON est inexploitable
    public void Uninstall();                                                  // CHANGE : plus de paramètre exePath
    public static void ApplyHooks(JsonObject root, string exePath, bool wanted);   // NOUVEAU (muté sur place)
    public static string? TransformForInstall(string? settingsJson, string exePath);  // CHANGE : string?
    public static string? TransformForUninstall(string? settingsJson);               // CHANGE : string?, plus d'exePath
}

public sealed class StatusLineInstaller
{
    public StatusLineInstaller(string? claudeSettingsPath = null);            // inchangé
    public string SettingsPath { get; }                                       // inchangé
    public static string ChronosCommand(string exePath);                      // CHANGE : slashes avant
    public bool IsInstalled(string exePath);                                  // sémantique CHANGE : appartenance ET fraîcheur
    public string? Install(string exePath);                                   // n'écrit plus si le JSON est inexploitable
    public void Uninstall(string? innerCommand);                              // CHANGE : plus de paramètre exePath
    public static void ApplyStatusLine(JsonObject root, string exePath);      // NOUVEAU (repointe uniquement)
    public static string? TransformForInstall(string? settingsJson, string exePath, out string? capturedInner);  // CHANGE : string?
    public static string? TransformForUninstall(string? settingsJson, string? innerCommand);                     // CHANGE : string?, plus d'exePath
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionHookInstaller — retirer-puis-ajouter inconditionnel, prédicat large, abandon sur JSON inexploitable</name>

  <read_first>
    - src/Chronos/Services/SessionHookInstaller.cs (fichier modifié — lire l'intégralité : `TransformForInstall` ligne 59, le garde ligne 69, `IsOurEntry` ligne 100, `Parse` ligne 110)
    - src/Chronos/Services/ClaudeSettingsJson.cs (socle du plan 01 : signatures exactes)
    - src/Chronos/Views/SessionsController.cs (appelants `Install` ligne ~43 et `Uninstall` ligne ~63)
    - tests/Chronos.Tests/SessionsTests.cs (lignes 61-110 : les 3 tests existants qui doivent rester verts ou être adaptés)
  </read_first>

  <files>src/Chronos/Services/SessionHookInstaller.cs, src/Chronos/Views/SessionsController.cs</files>

  <behavior>
    - `TransformForInstall(null, "C:/DL/Chronos-v2.5.exe")` → 5 événements, 1 groupe chacun, commande `"C:/DL/Chronos-v2.5.exe" --hook <Event>` avec `"timeout": 10`
    - Chaîner 3 chemins (`Chronos-v2.5.exe` → `Chronos-v2.6.exe` → `Chronos-v2.8.1.exe`) → chaque événement porte **exactement 1** groupe, pointant sur `Chronos-v2.8.1.exe` (et non 3)
    - Un groupe GSD `{ "matcher": "Write|Edit", "hooks": [ { "type":"command", "command":"node gsd.js", "timeout": 5 } ] }` présent sur `SessionStart` survit avec son `matcher` ET son `timeout`, et reste **avant** le groupe Chronos (ordre des survivants préservé)
    - `TransformForInstall` appliqué deux fois de suite avec le MÊME exe produit une sortie identique (point fixe)
    - `TransformForUninstall` retire **tous** les groupes Chronos, quel que soit le chemin d'exe (pas seulement le courant)
    - `TransformForUninstall` retire la clé d'événement devenue vide, et ne retire pas une clé qui garde un groupe tiers
    - `TransformForInstall("{\"a\":1,\"a\":2}", exe)` → **null** ; `TransformForInstall("[1,2]", exe)` → null ; `TransformForInstall("{ cassé", exe)` → null
    - Un handler `{"type":"http","url":"..."}` dans un groupe voisin ne fait lever aucune exception
    - `IsInstalled(exeCourant)` → false si le seul groupe présent pointe un autre chemin d'exe Chronos
  </behavior>

  <action>
Modifier `src/Chronos/Services/SessionHookInstaller.cs`.

**1. Supprimer** la méthode privée `IsOurEntry` (lignes 100-106) et la méthode privée `Parse`
(lignes 110-115) ainsi que le champ `Indented` (ligne 108). Ils sont remplacés par `ClaudeSettingsJson`.
Ajouter `using Chronos.Services;` n'est pas nécessaire (même namespace).

**2. Ajouter** le cœur de mutation, appelé aussi par le réconciliateur du plan 03 :

```csharp
/// <summary>
/// Amène l'arbre <paramref name="root"/> à l'ÉTAT DÉSIRÉ pour les 5 événements de Chronos :
/// retirer-puis-ajouter INCONDITIONNEL. Idempotent par construction — il ne dépend d'aucun prédicat
/// de présence exact, donc il ne peut plus empiler (cause du cumul de 25 groupes).
/// <paramref name="wanted"/> == false ⇒ ZÉRO groupe Chronos (désinstallation / purge).
/// Les groupes non-Chronos ne sont jamais touchés : leur ordre, leur « matcher », leur « timeout »
/// et leurs champs inconnus sont préservés (mutation en place, aucun reparentage de JsonNode).
/// </summary>
public static void ApplyHooks(JsonObject root, string exePath, bool wanted)
{
    if (root["hooks"] is not JsonObject hooks)
    {
        if (!wanted) return;                 // rien à purger, et on n'installe pas une clé « hooks » vide
        hooks = new JsonObject();
        root["hooks"] = hooks;
    }

    foreach (var ev in Events)
    {
        if (hooks[ev] is not JsonArray arr)
        {
            if (!wanted) continue;
            arr = new JsonArray();
            hooks[ev] = arr;                 // réassigner la MÊME instance ne lève pas
        }

        // Parcours DESCENDANT : les indices restants restent valides, aucun reparentage.
        for (int i = arr.Count - 1; i >= 0; i--)
            if (ClaudeSettingsJson.IsChronosGroup(arr[i], ClaudeSettingsJson.HookMarker))
                arr.RemoveAt(i);

        if (wanted)
            arr.Add(new JsonObject
            {
                ["hooks"] = new JsonArray(new JsonObject
                {
                    ["type"] = "command",
                    ["command"] = HookCommand(exePath, ev),   // slashes avant : leçon terrain, conservée
                    ["timeout"] = 10,
                }),
            });

        if (arr.Count == 0) hooks.Remove(ev);   // événement devenu vide → retirer la clé
    }

    if (hooks.Count == 0) root.Remove("hooks");
}
```

**3. Réécrire les cœurs purs** en `string?` (null = ne rien écrire) :

```csharp
public static string? TransformForInstall(string? settingsJson, string exePath)
{
    var root = ClaudeSettingsJson.ParseOrNull(settingsJson);
    if (root is null) return null;   // JAMAIS new JsonObject() : cela EFFACERAIT tout le fichier
    ApplyHooks(root, exePath, wanted: true);
    return ClaudeSettingsJson.Serialize(root);
}

public static string? TransformForUninstall(string? settingsJson)
{
    var root = ClaudeSettingsJson.ParseOrNull(settingsJson);
    if (root is null) return null;
    ApplyHooks(root, exePath: "", wanted: false);
    return ClaudeSettingsJson.Serialize(root);
}
```
Noter dans un commentaire français que `TransformForUninstall` ne prend plus de `exePath` : la purge
est volontairement LARGE (tous les Chronos), sinon `Disable()` depuis une nouvelle version laisserait
les hooks des anciennes.

**4. Adapter les méthodes d'E/S** :

```csharp
public void Install(string exePath)
{
    var current = File.Exists(_settingsPath) ? File.ReadAllText(_settingsPath) : null;
    var updated = TransformForInstall(current, exePath);
    if (updated is null) return;        // fichier inexploitable → on n'écrit RIEN (dégradation silencieuse)
    WriteAtomic(updated);
}

public void Uninstall()
{
    if (!File.Exists(_settingsPath)) return;
    var updated = TransformForUninstall(File.ReadAllText(_settingsPath));
    if (updated is null) return;
    WriteAtomic(updated);
}
```

**5. `IsInstalled(string exePath)`** — sépare appartenance et fraîcheur : renvoyer vrai seulement si un
groupe de `Notification` porte une commande à la fois Chronos ET pointant sur CET exe :
```csharp
public bool IsInstalled(string exePath)
{
    try
    {
        if (!File.Exists(_settingsPath)) return false;
        var root = ClaudeSettingsJson.ParseOrNull(File.ReadAllText(_settingsPath));
        if (root?["hooks"]?["Notification"] is not JsonArray arr) return false;
        return arr.Any(g => g is JsonObject go && go["hooks"] is JsonArray hs
            && hs.Any(h => ClaudeSettingsJson.CommandOf(h) is { } c
                && ClaudeSettingsJson.IsChronosCommand(c, ClaudeSettingsJson.HookMarker)
                && ClaudeSettingsJson.PointsToExe(c, exePath)));
    }
    catch { return false; }
}
```

**6. Mettre à jour l'appelant** `src/Chronos/Views/SessionsController.cs` : remplacer
`try { _installer.Uninstall(ExePath); } catch { }` par `try { _installer.Uninstall(); } catch { }`.
Ne rien changer d'autre dans ce fichier.

**7. Actualiser la XML-doc de classe** (lignes 7-16) : remplacer « NON DESTRUCTIF (ajoute à côté des
hooks existants) » par une formulation exacte — « REMPLACE inconditionnellement les groupes Chronos
(quel que soit le chemin d'exe : PUR-01) et ne touche jamais aux groupes des autres outils ».
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q --filter "FullyQualifiedName~SessionsTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "IsOurEntry" src/Chronos/Services/SessionHookInstaller.cs` renvoie 0
    - `grep -c "return new JsonObject()" src/Chronos/Services/SessionHookInstaller.cs` renvoie 0
    - Le fichier contient `public static void ApplyHooks(JsonObject root, string exePath, bool wanted)`
    - Le fichier contient `public static string? TransformForInstall(` et `public static string? TransformForUninstall(string? settingsJson)`
    - Le fichier contient `for (int i = arr.Count - 1; i >= 0; i--)` (parcours descendant)
    - `grep -c "arr.Any(e => IsOurEntry" src/Chronos/Services/SessionHookInstaller.cs` renvoie 0 (le garde d'idempotence conditionnel a disparu)
    - `grep -c "_installer.Uninstall(ExePath)" src/Chronos/Views/SessionsController.cs` renvoie 0
    - `dotnet build Chronos.sln --nologo -v q` sort en 0
  </acceptance_criteria>

  <done>L'installation de hooks remplace inconditionnellement, la purge est large, un JSON inexploitable ne provoque aucune écriture, et l'appelant compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusLineInstaller — muter command au lieu de reconstruire l'objet, séparer appartenance et fraîcheur</name>

  <read_first>
    - src/Chronos/Services/StatusLineInstaller.cs (fichier modifié — lire l'intégralité : `TransformForInstall` ligne 68 et sa reconstruction ligne 79, `IsChronosCommand` ligne 106, `ParseObject` ligne 116)
    - src/Chronos/Services/ClaudeSettingsJson.cs (socle du plan 01)
    - src/Chronos/Views/StatusLineSetup.cs (appelants : `IsEnabled` ligne 28, `Enable` ligne 34, `Disable` ligne 57, `OfferOnFirstRun` ligne 67)
    - tests/Chronos.Tests/StatusLineInstallerTests.cs (les 6 tests existants ; 3 appellent `TransformForUninstall(json, Exe, inner)` et devront perdre l'argument `Exe`)
  </read_first>

  <files>src/Chronos/Services/StatusLineInstaller.cs, src/Chronos/Views/StatusLineSetup.cs</files>

  <behavior>
    - `ChronosCommand(@"C:\DL\Chronos.exe")` → `"C:/DL/Chronos.exe" --statusline` (slashes AVANT, aligné sur `SessionHookInstaller.HookCommand`)
    - `TransformForInstall` sur `{"statusLine":{"type":"command","command":"\"C:/DL/Chronos-v2.8.1.exe\" --statusline","padding":2}}` avec l'exe courant `C:/Apps/Chronos.exe` → `command` repointée sur l'exe courant, `padding` **toujours présent et égal à 2**, `type` toujours `"command"`, `capturedInner` **null** (une barre Chronos n'est jamais chaînée sur elle-même)
    - `TransformForInstall` sur une barre tierce `{"statusLine":{"type":"command","command":"node autre.js"}}` → `capturedInner == "node autre.js"` et `command` repointée sur Chronos (comportement d'installation existant conservé)
    - `TransformForInstall(null, exe, out _)` → crée `{"type":"command","command":"..."}`
    - `TransformForInstall` deux fois de suite avec le même exe → sortie identique (point fixe), `capturedInner` null la seconde fois
    - `ApplyStatusLine(root, exe)` sur une barre tierce → **root inchangé** (aucune installation sans consentement)
    - `ApplyStatusLine(root, exe)` sans clé `statusLine` → root inchangé
    - `ApplyStatusLine(root, exe)` sur une barre Chronos périmée → seule `command` change ; `padding` et toute clé inconnue survivent
    - `TransformForUninstall` retire/restaure une barre Chronos **quel que soit** le chemin d'exe enregistré
    - `TransformForUninstall` ne touche pas une barre tierce
    - `TransformForInstall("{\"a\":1,\"a\":2}", exe, out _)` → null ; `TransformForInstall("[1,2]", exe, out _)` → null
    - `IsInstalled(exeCourant)` → false quand la commande enregistrée est `Chronos-v2.8.1.exe` alors qu'on tourne depuis un autre chemin
  </behavior>

  <action>
Modifier `src/Chronos/Services/StatusLineInstaller.cs`.

**1. Supprimer** `IsChronosCommand` (lignes 106-112), `ParseObject` (116-121), `ReadCommand` (123-127)
et le champ `Indented` (114). Remplacés par `ClaudeSettingsJson`.

**2. `ChronosCommand`** — aligner sur les slashes avant (décision de phase, Open Question 7 de
`15-RESEARCH.md` tranchée par le planner : OUI) :
```csharp
/// <summary>Commande statusLine attendue pour ce Chronos.exe (slashes AVANT, comme HookCommand :
/// des backslashes seraient avalés par le shell — leçon vérifiée sur la vraie machine).</summary>
public static string ChronosCommand(string exePath) => "\"" + exePath.Replace('\\', '/') + "\" --statusline";
```

**3. Ajouter** le cœur de mutation réutilisé par le réconciliateur du plan 03 :

```csharp
/// <summary>
/// REPOINTE la barre statusLine sur <paramref name="exePath"/> — et RIEN d'autre. Ne crée jamais
/// l'entrée : le consentement d'installation reste porté par le menu / StatusLinePromptDismissed.
/// Mutation CIBLÉE de « command » : « type », « padding » (champ officiel optionnel) et toute clé
/// future de l'objet survivent — les reconstruire les effacerait sans bruit.
/// </summary>
public static void ApplyStatusLine(JsonObject root, string exePath)
{
    if (root["statusLine"] is not JsonObject sl) return;
    var cmd = sl["command"] is JsonValue v && v.TryGetValue<string>(out var s) ? s : null;
    if (!ClaudeSettingsJson.IsChronosCommand(cmd, ClaudeSettingsJson.StatusLineMarker)) return;  // barre tierce → intouchée
    if (ClaudeSettingsJson.PointsToExe(cmd, exePath)) return;                                    // déjà à jour
    sl["command"] = ChronosCommand(exePath);
}
```

**4. Réécrire `TransformForInstall`** — mutation, plus de reconstruction, `string?` en retour :

```csharp
public static string? TransformForInstall(string? settingsJson, string exePath, out string? capturedInner)
{
    capturedInner = null;
    var root = ClaudeSettingsJson.ParseOrNull(settingsJson);
    if (root is null) return null;   // JAMAIS new JsonObject() : cela EFFACERAIT tout le fichier

    if (root["statusLine"] is JsonObject sl)
    {
        var existing = sl["command"] is JsonValue v && v.TryGetValue<string>(out var s) ? s : null;
        if (!string.IsNullOrWhiteSpace(existing)
            && !ClaudeSettingsJson.IsChronosCommand(existing, ClaudeSettingsJson.StatusLineMarker))
            capturedInner = existing;      // barre tierce → mémorisée pour le chaînage

        sl["command"] = ChronosCommand(exePath);   // MUTATION : type, padding, clés futures intacts
        if (sl["type"] is null) sl["type"] = "command";
    }
    else
    {
        root["statusLine"] = new JsonObject { ["type"] = "command", ["command"] = ChronosCommand(exePath) };
    }
    return ClaudeSettingsJson.Serialize(root);
}
```

**5. Réécrire `TransformForUninstall`** — prédicat large (n'importe quel Chronos), plus de `exePath` :

```csharp
public static string? TransformForUninstall(string? settingsJson, string? innerCommand)
{
    var root = ClaudeSettingsJson.ParseOrNull(settingsJson);
    if (root is null) return null;
    if (root["statusLine"] is JsonObject sl)
    {
        var cmd = sl["command"] is JsonValue v && v.TryGetValue<string>(out var s) ? s : null;
        if (ClaudeSettingsJson.IsChronosCommand(cmd, ClaudeSettingsJson.StatusLineMarker))
        {
            if (!string.IsNullOrWhiteSpace(innerCommand)) sl["command"] = innerCommand;  // mutation, pas reconstruction
            else root.Remove("statusLine");
        }
    }
    return ClaudeSettingsJson.Serialize(root);
}
```

**6. E/S** : `Install` n'écrit pas si `TransformForInstall` renvoie null (et renvoie alors `null` comme
`capturedInner`). `Uninstall(string? innerCommand)` perd son paramètre `exePath` et n'écrit pas si le
cœur renvoie null.

**7. `IsInstalled(string exePath)`** — appartenance ET fraîcheur :
```csharp
public bool IsInstalled(string exePath)
{
    try
    {
        if (!File.Exists(_claudeSettingsPath)) return false;
        var root = ClaudeSettingsJson.ParseOrNull(File.ReadAllText(_claudeSettingsPath));
        var cmd = root?["statusLine"] is JsonObject sl && sl["command"] is JsonValue v
            && v.TryGetValue<string>(out var s) ? s : null;
        return ClaudeSettingsJson.IsChronosCommand(cmd, ClaudeSettingsJson.StatusLineMarker)
            && ClaudeSettingsJson.PointsToExe(cmd, exePath);
    }
    catch { return false; }
}
```
Ajouter en XML-doc, en français, la raison du durcissement : `IsEnabled()` répondait « oui » pour un
exe périmé, donc `OfferOnFirstRun` sortait immédiatement et le pont restait branché à vie sur
`Chronos-v2.8.1.exe`. **Noter que le réconciliateur du plan 03 s'exécute AVANT `OfferOnFirstRun()` dans
`App.OnStartup` : après repointage, `IsInstalled` redevient vrai et aucun dialogue parasite n'apparaît.**

**8. Mettre à jour l'appelant** `src/Chronos/Views/StatusLineSetup.cs` ligne 57 :
`_installer.Uninstall(ExePath, s.InnerStatusLineCommand);` → `_installer.Uninstall(s.InnerStatusLineCommand);`.
Ne rien changer d'autre dans ce fichier (`Enable` continue de persister `InnerStatusLineCommand = inner ?? s.InnerStatusLineCommand`, ce qui garantit qu'une réconciliation ne l'écrase jamais avec null).
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q --filter "FullyQualifiedName~StatusLineInstallerTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "root\[\"statusLine\"\] = new JsonObject" src/Chronos/Services/StatusLineInstaller.cs` renvoie exactement 1 (uniquement la branche « clé absente »)
    - Le fichier contient `public static void ApplyStatusLine(JsonObject root, string exePath)`
    - Le fichier contient `exePath.Replace('\\', '/')` dans `ChronosCommand`
    - `grep -c "IndexOf(\"Chronos\"" src/Chronos/Services/StatusLineInstaller.cs` renvoie 0 (l'ancien prédicat laxiste a disparu)
    - `grep -c "return new JsonObject()" src/Chronos/Services/StatusLineInstaller.cs` renvoie 0
    - `grep -c "Uninstall(ExePath," src/Chronos/Views/StatusLineSetup.cs` renvoie 0
    - `dotnet build Chronos.sln --nologo -v q` sort en 0
  </acceptance_criteria>

  <done>La barre statusLine est repointée par mutation ciblée, `padding` survit, une barre tierce n'est jamais capturée par la réconciliation, et `IsInstalled` distingue « à Chronos » de « à CE Chronos ».</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Étendre les tests — non-cumul multi-chemins, survie des tiers, préservation de padding</name>

  <read_first>
    - tests/Chronos.Tests/SessionsTests.cs (section `--- SessionHookInstaller ---`, lignes 61-110)
    - tests/Chronos.Tests/StatusLineInstallerTests.cs (les 6 tests existants, à adapter aux nouvelles signatures)
    - src/Chronos/Services/SessionHookInstaller.cs et src/Chronos/Services/StatusLineInstaller.cs (versions issues des tâches 1 et 2)
  </read_first>

  <files>tests/Chronos.Tests/SessionsTests.cs, tests/Chronos.Tests/StatusLineInstallerTests.cs</files>

  <behavior>
    Dans `SessionsTests.cs`, ajouter :
    - `Trois_chemins_dexe_successifs_ne_laissent_que_cinq_hooks` — chaîner
      `C:\DL\Chronos-v2.5.exe` → `C:\DL\Chronos-v2.6.exe` → `C:\DL\Chronos-v2.8.1.exe` ;
      pour chacun des 5 `Events` : `Assert.Single(groupes)` et la commande contient
      `C:/DL/Chronos-v2.8.1.exe` (**critère de succès 1 de la ROADMAP**)
    - `Install_preserve_un_groupe_tiers_avec_son_matcher_et_son_timeout` — partir de
      `{"hooks":{"SessionStart":[{"matcher":"Write|Edit","hooks":[{"type":"command","command":"node gsd.js","timeout":5}]}]}}` ;
      après install : 2 groupes, le groupe tiers en **index 0** avec `matcher == "Write|Edit"` et
      `timeout == 5`
    - `Install_ne_leve_pas_sur_un_handler_sans_command` — un groupe voisin `{"hooks":[{"type":"http","url":"https://x"}]}` survit
    - `Install_sur_json_inexploitable_ne_produit_rien` — `[Theory]` sur `{"a":1,"a":2}`, `[1,2,3]`,
      `{ cassé`, `"scalaire"` → `Assert.Null(SessionHookInstaller.TransformForInstall(x, Exe))`
    - `Uninstall_retire_les_hooks_de_toutes_les_versions` — installer depuis 3 chemins puis
      `TransformForUninstall` → plus aucune commande contenant `--hook`, le groupe tiers survit
    - Adapter les 3 tests existants aux nouvelles signatures (`TransformForUninstall(json)` sans `Exe`)
      et au retour `string?` (déréférencer avec `!`)

    Dans `StatusLineInstallerTests.cs`, ajouter :
    - `Install_repointe_une_barre_Chronos_perimee_sans_dupliquer` — entrée
      `{"statusLine":{"type":"command","command":"\"C:/DL/Chronos-v2.8.1.exe\" --statusline","padding":2}}`,
      exe courant `C:\Apps\Chronos.exe` → `command == StatusLineInstaller.ChronosCommand(Exe)`,
      `capturedInner` null (**critère de succès 2**)
    - `Install_preserve_padding_et_les_cles_inconnues` — `padding == 2` et une clé
      `"futur": "valeur"` toujours présentes après install
    - `Install_est_un_point_fixe` — deux passes du même exe produisent la même chaîne
    - `Install_sur_json_inexploitable_ne_produit_rien` — `[Theory]` comme ci-dessus → `Assert.Null(...)`
    - `Uninstall_retire_une_barre_Chronos_dune_autre_version` — la commande enregistrée pointe
      `Chronos-v2.6.exe`, `Uninstall(innerCommand: null)` la retire quand même
    - `ChronosCommand_utilise_des_slashes_avant` — `Assert.DoesNotContain("\\", StatusLineInstaller.ChronosCommand(@"C:\Apps\Chronos.exe"))`
    - Adapter les 3 tests existants qui passaient `Exe` à `TransformForUninstall`
  </behavior>

  <action>
Étendre les deux fichiers de tests avec les cas listés dans `<behavior>`, en respectant le style
existant : noms de méthodes en français avec underscores, helpers `Root(json)` / `Cmd(json)` déjà
présents dans `StatusLineInstallerTests`, constante `Exe` déjà présente dans les deux fichiers.

Contraintes impératives :
- **Aucun test n'ouvre le vrai `~/.claude/settings.json`.** Tous les tests de ce plan appellent
  uniquement les cœurs PURS statiques (`TransformForInstall` / `TransformForUninstall` /
  `ApplyHooks` / `ApplyStatusLine`), aucun ne construit `new SessionHookInstaller()` ni
  `new StatusLineInstaller()` sans chemin. Si un test doit construire un installateur, il **doit**
  passer un chemin issu de `Path.GetTempPath()`.
- Pour les assertions sur la commande statusLine, comparer à
  `StatusLineInstaller.ChronosCommand(Exe)` et **jamais** à une chaîne littérale codée en dur — la
  forme du chemin (slashes avant) est décidée par la tâche 2.
- Les cœurs purs renvoient désormais `string?` : déréférencer avec `!` après avoir asserté non-null,
  ou asserter `Assert.Null` pour les cas d'abandon.
- Ajouter, en tête de chaque nouveau bloc, un commentaire français reliant le test à son exigence
  (`// PUR-01 — critère de succès 1 : ...`).
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Trois_chemins_dexe_successifs_ne_laissent_que_cinq_hooks" tests/Chronos.Tests/SessionsTests.cs` = 1
    - `grep -c "Install_repointe_une_barre_Chronos_perimee_sans_dupliquer" tests/Chronos.Tests/StatusLineInstallerTests.cs` = 1
    - `grep -c "padding" tests/Chronos.Tests/StatusLineInstallerTests.cs` ≥ 2
    - `grep -c "Assert.Null" tests/Chronos.Tests/SessionsTests.cs` ≥ 4
    - `grep -rc "new SessionHookInstaller()\|new StatusLineInstaller()" tests/Chronos.Tests/SessionsTests.cs tests/Chronos.Tests/StatusLineInstallerTests.cs` renvoie 0 sur les deux fichiers (aucune construction sans chemin injecté)
    - `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q` sort en 0, avec un total ≥ 328 + les tests des plans 01 et 02, 0 échec
  </acceptance_criteria>

  <done>Le non-cumul multi-chemins, la survie des groupes tiers avec matcher/timeout, le repointage de statusLine et la préservation de padding sont prouvés par des tests verts.</done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln --nologo -v q` → 0 erreur.
2. `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q` → ≥ 328 + nouveaux, 0 échec.
3. `dotnet test ... --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert.
4. `dotnet test ... --filter "FullyQualifiedName~CompositionRootTests"` → vert.
5. `grep -rn "IsOurEntry\|return new JsonObject()" src/Chronos/Services/` → aucune occurrence.
</verification>

<success_criteria>
- PUR-01 : trois installations depuis trois chemins d'exe laissent exactement 5 groupes Chronos.
- PUR-02 : la barre statusLine Chronos est repointée par mutation, `padding` et clés inconnues intacts, une barre tierce n'est jamais capturée.
- Les groupes non-Chronos (matcher, timeout, champs inconnus, ordre) survivent à l'installation comme à la désinstallation.
- Un `settings.json` inexploitable ne provoque aucune écriture (les 4 cœurs purs renvoient `null`).
- Suite complète verte, gardes de pureté et de composition comprises.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/15-idempotence-des-int-grations/15-02-SUMMARY.md`.
</output>

