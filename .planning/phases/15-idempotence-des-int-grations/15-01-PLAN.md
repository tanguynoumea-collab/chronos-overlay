---
phase: 15-idempotence-des-int-grations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/ClaudeSettingsJson.cs
  - tests/Chronos.Tests/ClaudeSettingsJsonTests.cs
  - tests/Chronos.Tests/TestData/claude-settings-pollue.json
autonomous: true
requirements: [PUR-01, PUR-02, PUR-03]
user_setup: []

must_haves:
  truths:
    - "Une commande de hook Chronos est reconnue quel que soit le chemin de l'exe (v2.5, v2.5.1, v2.6, v2.8.1, build Debug)"
    - "Une commande de hook GSD (node gsd-*.js) n'est JAMAIS reconnue comme Chronos"
    - "Un settings.json malformé (commentaire, virgule traînante, clé dupliquée, racine tableau) produit « ne rien écrire » et jamais un objet vide"
    - "Un chemin accentué (Téléchargements) ressort littéral après sérialisation, jamais en \\uXXXX"
  artifacts:
    - path: "src/Chronos/Services/ClaudeSettingsJson.cs"
      provides: "Prédicat d'identité Chronos + analyse tolérante + sérialisation fidèle"
      contains: "public static class ClaudeSettingsJson"
      min_lines: 90
    - path: "tests/Chronos.Tests/ClaudeSettingsJsonTests.cs"
      provides: "Preuves unitaires du socle"
      min_lines: 90
    - path: "tests/Chronos.Tests/TestData/claude-settings-pollue.json"
      provides: "Fixture figée reproduisant l'état réel pollué (25 groupes Chronos + 3 groupes GSD + statusLine périmée + agentPushNotifEnabled)"
      contains: "agentPushNotifEnabled"
  key_links:
    - from: "src/Chronos/Services/ClaudeSettingsJson.cs"
      to: "System.Text.Encodings.Web.JavaScriptEncoder"
      via: "JsonSerializerOptions.Encoder"
      pattern: "UnsafeRelaxedJsonEscaping"
    - from: "src/Chronos/Services/ClaudeSettingsJson.cs"
      to: "System.Text.Json.JsonDocumentOptions"
      via: "lecture tolérante"
      pattern: "AllowTrailingCommas"
---

<objective>
Poser le **socle neutre partagé** dont dépendent les deux plans suivants : un prédicat d'identité
« cette commande appartient-elle à Chronos ? » fondé sur le **marqueur d'argument + le nom de fichier
de l'exécutable** (jamais le chemin — cause racine du cumul de 25 hooks), une **analyse tolérante qui
abandonne au lieu d'effacer** (le défaut le plus grave du code actuel : `catch { return new JsonObject(); }`
suivi d'une écriture écrase intégralement `~/.claude/settings.json`), et une **sérialisation fidèle**
qui ne mutile pas les accents du fichier.

Purpose: PUR-01, PUR-02 et PUR-03 reposent tous les trois sur le même prédicat et la même discipline
d'écriture. Les factoriser une fois évite trois implémentations divergentes.
Output: `ClaudeSettingsJson` (statique, neutre, testé) + la fixture figée de l'état réel pollué qui
servira de preuve à la purge du plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-idempotence-des-int-grations/15-RESEARCH.md
@src/Chronos/Services/SessionHookInstaller.cs
@src/Chronos/Services/StatusLineInstaller.cs

<interfaces>
<!-- Contrat créé par ce plan. Les plans 02 et 03 codent CONTRE cette surface. -->

Nouveau fichier `src/Chronos/Services/ClaudeSettingsJson.cs`, namespace `Chronos.Services` :

```csharp
public static class ClaudeSettingsJson
{
    public const string HookMarker = "--hook";
    public const string StatusLineMarker = "--statusline";

    /// Premier jeton exécutable d'une chaîne de commande (segment entre guillemets, sinon jusqu'au premier espace).
    public static string ExtractExecutable(string? command);

    /// Vrai si la commande porte le MARQUEUR et que son exécutable s'appelle Chronos*.exe. Jamais de comparaison de CHEMIN.
    public static bool IsChronosCommand(string? command, string marker);

    /// Vrai si la commande référence CE chemin d'exe (backslashes et slashes avant tolérés). Sert à la fraîcheur, pas à l'appartenance.
    public static bool PointsToExe(string? command, string exePath);

    /// Lit un champ "command" sans jamais lever (TryGetValue, pas GetValue).
    public static string? CommandOf(JsonNode? handler);

    /// Vrai si le GROUPE { "hooks": [ {...} ] } contient au moins un handler dont la commande est à Chronos.
    public static bool IsChronosGroup(JsonNode? group, string marker);

    /// Analyse tolérante. new JsonObject() si json est null/vide. null si le contenu est INEXPLOITABLE
    /// (racine non-objet, clés dupliquées, JSON invalide) → l'appelant NE DOIT RIEN ÉCRIRE.
    public static JsonObject? ParseOrNull(string? json);

    /// Sérialisation fidèle : indentation 2 espaces + UnsafeRelaxedJsonEscaping (accents littéraux).
    public static string Serialize(JsonObject root);
}
```

Conventions du dépôt à respecter : identifiants de types/méthodes en **anglais** (comme
`SessionHookInstaller`, `TransformForInstall`), XML-doc et commentaires en **français**, noms de
méthodes de test en **français** avec underscores (comme `Install_ajoute_les_5_hooks_en_slashes_avant`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer ClaudeSettingsJson — prédicat d'identité, analyse tolérante, sérialisation fidèle</name>

  <read_first>
    - src/Chronos/Services/SessionHookInstaller.cs (lignes 99-115 : `IsOurEntry` fondé sur le chemin d'exe = la cause racine ; `Parse` avec repli destructif `new JsonObject()`)
    - src/Chronos/Services/StatusLineInstaller.cs (lignes 105-121 : `IsChronosCommand` qui accepte n'importe quoi contenant « Chronos » ; `ParseObject` destructif)
    - src/Chronos/Services/ChronosPaths.cs (style d'un type neutre du dossier Services : record/statique, XML-doc française)
    - .planning/phases/15-idempotence-des-int-grations/15-RESEARCH.md (sections « Pattern 1 », « Common Pitfalls » 3/5/9, « Code Examples »)
  </read_first>

  <files>src/Chronos/Services/ClaudeSettingsJson.cs</files>

  <behavior>
    - `ExtractExecutable("\"C:/DL/Chronos-v2.5.exe\" --hook Stop")` → `C:/DL/Chronos-v2.5.exe`
    - `ExtractExecutable("node \"C:/x/gsd.js\"")` → `node`
    - `ExtractExecutable("")` et `ExtractExecutable(null)` → `""`
    - `IsChronosCommand("\"C:/DL/Chronos-v2.6.exe\" --hook Stop", "--hook")` → true
    - `IsChronosCommand("\"C:/x/PROJET OVERLAY/src/Chronos/bin/Debug/net8.0-windows/Chronos.exe\" --hook Stop", "--hook")` → true (chemin avec espaces)
    - `IsChronosCommand("node \"C:/Users/Tanguy/.claude/hooks/gsd-check-update.js\"", "--hook")` → false (pas de marqueur, et pas Chronos*.exe)
    - `IsChronosCommand("\"C:/x/Chronos.exe\" --statusline", "--hook")` → false (mauvais marqueur)
    - `IsChronosCommand("\"C:/x/autre.exe\" --hook Stop", "--hook")` → false (marqueur oui, exe non-Chronos)
    - `IsChronosCommand(null, "--hook")` et `IsChronosCommand("   ", "--hook")` → false
    - `PointsToExe("\"C:/DL/Chronos.exe\" --statusline", @"C:\DL\Chronos.exe")` → true (backslashes normalisés)
    - `PointsToExe("\"C:/DL/Chronos-v2.5.exe\" --statusline", @"C:\DL\Chronos.exe")` → false
    - `CommandOf` sur `{"type":"command","command":"x"}` → `"x"` ; sur `{"type":"command","command":42}` → null (aucune levée) ; sur `{"type":"http","url":"…"}` → null ; sur null → null
    - `IsChronosGroup` sur `{"hooks":[{"type":"command","command":"\"C:/DL/Chronos-v2.5.exe\" --hook Stop"}]}` avec `"--hook"` → true
    - `IsChronosGroup` sur `{"matcher":"Write|Edit","hooks":[{"type":"command","command":"node gsd.js","timeout":5}]}` → false
    - `IsChronosGroup` sur `{"hooks":[{"type":"http","url":"https://x"}]}` → false (aucune levée)
    - `ParseOrNull(null)` et `ParseOrNull("   ")` → `JsonObject` vide (départ légitime, `Count == 0`)
    - `ParseOrNull("{ \"a\": 1, // note\n \"b\": 2, }")` → objet non nul (commentaire + virgule traînante tolérés)
    - `ParseOrNull("{\"a\":1,\"a\":2}")` → **null** (clés dupliquées : `Parse` réussit puis l'accès lève `ArgumentException`, qui n'est PAS une `JsonException`)
    - `ParseOrNull("[1,2,3]")` → null ; `ParseOrNull("\"txt\"")` → null ; `ParseOrNull("{ pas du json")` → null
    - `Serialize` sur un objet contenant `"C:/Users/Tanguy/Téléchargements/Chronos.exe"` → la sortie contient la chaîne littérale `Téléchargements`, et NE contient PAS `\u00E9`
    - `Serialize` sur `{"a":"x&y<z>w+v"}` → sortie contenant `&`, `<`, `>`, `+` littéraux
    - `Serialize` préserve l'ordre des clés et le texte brut des nombres (`5817635413` reste `5817635413`)
  </behavior>

  <action>
Créer `src/Chronos/Services/ClaudeSettingsJson.cs`, namespace `Chronos.Services`, `public static class ClaudeSettingsJson`.
XML-doc de classe en français expliquant : (a) pourquoi le repérage se fait sur le MARQUEUR + le NOM de
fichier et jamais sur le CHEMIN (les 5 chemins d'exe historiques ont produit 25 groupes de hooks au lieu
de 5) ; (b) pourquoi `ParseOrNull` renvoie `null` au lieu d'un objet vide (le repli `new JsonObject()`
du code actuel ÉCRASE tout le settings.json de l'utilisateur) ; (c) que les commentaires JSON sont
perdus à la réécriture — limite de System.Text.Json, couverte par la sauvegarde du plan 03.

`using System.IO; using System.Text.Json; using System.Text.Json.Nodes; using System.Text.Encodings.Web;`

Membres exactement comme suit :

```csharp
public const string HookMarker = "--hook";
public const string StatusLineMarker = "--statusline";

private static readonly JsonDocumentOptions LectureTolerante = new()
{
    CommentHandling = JsonCommentHandling.Skip,   // sinon « // » lève JsonReaderException
    AllowTrailingCommas = true,
};

private static readonly JsonSerializerOptions SortieFidele = new()
{
    WriteIndented = true,                                    // 2 espaces — style de Claude Code
    Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping,   // « Téléchargements » reste littéral
};
```

`ExtractExecutable(string? command)` : `TrimStart` ; chaîne vide → `""` ; si le premier caractère est
`"`, renvoyer le segment jusqu'au `"` fermant (`IndexOf('"', 1)`), sinon le reste ; sinon renvoyer
jusqu'au premier espace (`IndexOf(' ')`), sinon toute la chaîne.

`IsChronosCommand(string? command, string marker)` :
`if (string.IsNullOrWhiteSpace(command)) return false;`
`if (command.IndexOf(marker, StringComparison.OrdinalIgnoreCase) < 0) return false;`
`var nom = Path.GetFileName(ExtractExecutable(command).Replace('\\', '/'));`
`return nom.StartsWith("Chronos", StringComparison.OrdinalIgnoreCase) && nom.EndsWith(".exe", StringComparison.OrdinalIgnoreCase);`

`PointsToExe(string? command, string exePath)` : false si `command` vide ou `exePath` vide ; sinon vrai
si `command` contient `exePath` OU `exePath.Replace('\\','/')`, en `OrdinalIgnoreCase`.

`CommandOf(JsonNode? handler)` : `handler is JsonObject o && o["command"] is JsonValue v && v.TryGetValue<string>(out var s) ? s : null`.
**Ne jamais** utiliser `GetValue<string>()` : il lève `InvalidOperationException` sur un nombre, et le
schéma officiel autorise désormais des handlers `http`/`mcp_tool`/`prompt`/`agent` sans champ `command`.

`IsChronosGroup(JsonNode? group, string marker)` : `group is JsonObject g && g["hooks"] is JsonArray hs
&& hs.Any(h => IsChronosCommand(CommandOf(h), marker))`.

`ParseOrNull(string? json)` :
```csharp
if (string.IsNullOrWhiteSpace(json)) return new JsonObject();
try
{
    if (JsonNode.Parse(json, nodeOptions: null, LectureTolerante) is not JsonObject o) return null;
    _ = o.Count;   // force la matérialisation : lève ArgumentException si clés dupliquées
    return o;
}
catch { return null; }   // catch LARGE : ArgumentException n'est PAS une JsonException
```

`Serialize(JsonObject root)` : `root.ToJsonString(SortieFidele)`.

Aucun type WPF, aucune E/S fichier dans ce fichier (`ServicesLayerPurityTests` doit rester vert).
  </action>

  <verify>
    <automated>dotnet build src/Chronos/Chronos.csproj --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `src/Chronos/Services/ClaudeSettingsJson.cs` existe et contient `public static class ClaudeSettingsJson`
    - Le fichier contient `public const string HookMarker = "--hook";` et `public const string StatusLineMarker = "--statusline";`
    - Le fichier contient `JavaScriptEncoder.UnsafeRelaxedJsonEscaping`
    - Le fichier contient `CommentHandling = JsonCommentHandling.Skip` et `AllowTrailingCommas = true`
    - Le fichier contient `_ = o.Count;` (matérialisation forçant l'`ArgumentException` sur clés dupliquées)
    - `grep -c "GetValue<string>()" src/Chronos/Services/ClaudeSettingsJson.cs` renvoie 0
    - `grep -c "new JsonObject()" src/Chronos/Services/ClaudeSettingsJson.cs` renvoie exactement 1 (le cas json null/vide uniquement)
    - `grep -c "System.Windows" src/Chronos/Services/ClaudeSettingsJson.cs` renvoie 0
    - `dotnet build src/Chronos/Chronos.csproj --nologo -v q` sort en 0
  </acceptance_criteria>

  <done>La classe compile, expose les 7 membres publics du contrat, n'a aucune E/S ni type WPF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prouver le socle par tests unitaires (prédicat, tolérance, fidélité)</name>

  <read_first>
    - src/Chronos/Services/ClaudeSettingsJson.cs (le fichier créé en Task 1)
    - tests/Chronos.Tests/StatusLineInstallerTests.cs (style : classe publique, `[Fact]`, noms de tests en français, helpers `Root`/`Cmd`)
    - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs (lignes 25-26 : motif `[CallerFilePath]` pour résoudre `TestData/` sans toucher le csproj)
  </read_first>

  <files>tests/Chronos.Tests/ClaudeSettingsJsonTests.cs</files>

  <behavior>
    Un test (ou un `[Theory]`) par ligne de la section `<behavior>` de la Task 1. Au minimum ces
    assertions nommées :
    - `Reconnait_les_cinq_chemins_dexe_Chronos_historiques` — `[Theory]` sur les 5 commandes réelles
      (`Chronos-v2.5.exe`, `Chronos-v2.5.1.exe`, `Chronos-v2.6.exe`, `Chronos-v2.8.1.exe`,
      `.../src/Chronos/bin/Debug/net8.0-windows/Chronos.exe`) → toutes true avec `--hook`
    - `Ne_reconnait_pas_les_hooks_GSD` — les 3 commandes `node ".../gsd-check-update.js"`,
      `node ".../gsd-context-monitor.js"`, `node ".../gsd-prompt-guard.js"` → toutes false
    - `Ne_reconnait_pas_un_exe_tiers_portant_le_marqueur` — `"C:/x/autre.exe" --hook Stop` → false
    - `Marqueur_statusline_et_marqueur_hook_ne_se_confondent_pas`
    - `PointsToExe_normalise_les_backslashes`
    - `CommandOf_ne_leve_pas_sur_un_command_numerique` et `CommandOf_ne_leve_pas_sur_un_handler_http`
    - `IsChronosGroup_ignore_un_groupe_GSD_avec_matcher_et_timeout`
    - `ParseOrNull_tolere_commentaires_et_virgule_trainante`
    - `ParseOrNull_abandonne_sur_cles_dupliquees` → `Assert.Null(...)`
    - `ParseOrNull_abandonne_sur_racine_tableau` et `ParseOrNull_abandonne_sur_json_invalide`
    - `ParseOrNull_sur_fichier_absent_donne_un_objet_vide`
    - `Serialize_conserve_les_accents_litteraux` → `Assert.Contains("Téléchargements", sortie)` **et**
      `Assert.DoesNotContain("\\u00E9", sortie)`
    - `Serialize_conserve_les_caracteres_html_litteraux` → contient `&`, `<`, `>`, `+`
    - `Serialize_preserve_lordre_des_cles_et_le_texte_brut_des_nombres`
  </behavior>

  <action>
Créer `tests/Chronos.Tests/ClaudeSettingsJsonTests.cs`, namespace `Chronos.Tests`,
`public class ClaudeSettingsJsonTests`, `using Chronos.Services; using System.Text.Json.Nodes; using Xunit;`.
XML-doc de classe en français : « Prouve le socle d'identité et de tolérance sur lequel reposent PUR-01,
PUR-02 et PUR-03 : repérage par marqueur + nom d'exe (jamais le chemin), abandon au lieu d'effacement,
sérialisation sans mutilation des accents. »

Constantes de commandes réelles à réutiliser (copiées de l'état constaté sur la machine) :
```csharp
private const string ChronosV25   = "\"C:/Users/Tanguy/Downloads/Chronos-v2.5.exe\" --hook SessionStart";
private const string ChronosV251  = "\"C:/Users/Tanguy/Downloads/Chronos-v2.5.1.exe\" --hook SessionStart";
private const string ChronosV26   = "\"C:/Users/Tanguy/Downloads/Chronos-v2.6.exe\" --hook SessionStart";
private const string ChronosV281  = "\"C:/Users/Tanguy/Downloads/Chronos-v2.8.1.exe\" --hook SessionStart";
private const string ChronosDebug = "\"C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY/src/Chronos/bin/Debug/net8.0-windows/Chronos.exe\" --hook SessionStart";
private const string GsdUpdate    = "node \"C:/Users/Tanguy/.claude/hooks/gsd-check-update.js\"";
private const string GsdMonitor   = "node \"C:/Users/Tanguy/.claude/hooks/gsd-context-monitor.js\"";
private const string GsdGuard     = "node \"C:/Users/Tanguy/.claude/hooks/gsd-prompt-guard.js\"";
```

Écrire un test par élément de `<behavior>`. Pour `Serialize_preserve_lordre_des_cles_et_le_texte_brut_des_nombres`,
partir de `ClaudeSettingsJson.ParseOrNull("""{"z":5817635413,"a":1.0,"m":"x"}""")!` puis vérifier que
`ClaudeSettingsJson.Serialize(...)` place `"z"` avant `"a"` avant `"m"` (comparer les index de
`IndexOf`) et contient `5817635413` et `1.0`.

**Aucun test de ce fichier ne doit toucher le système de fichiers.** Le socle est purement en mémoire.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q --filter "FullyQualifiedName~ClaudeSettingsJsonTests"</automated>
  </verify>

  <acceptance_criteria>
    - `tests/Chronos.Tests/ClaudeSettingsJsonTests.cs` existe
    - `grep -c "Assert.Null" tests/Chronos.Tests/ClaudeSettingsJsonTests.cs` ≥ 3 (clés dupliquées, racine tableau, json invalide)
    - Le fichier contient `Assert.DoesNotContain("\\u00E9"` (ou l'équivalent littéral `\u00E9` échappé en C#)
    - Le fichier contient les 3 constantes `GsdUpdate`, `GsdMonitor`, `GsdGuard`
    - `grep -c "System.IO.File\|File.ReadAllText\|File.WriteAllText" tests/Chronos.Tests/ClaudeSettingsJsonTests.cs` renvoie 0
    - `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q --filter "FullyQualifiedName~ClaudeSettingsJsonTests"` sort en 0 avec au moins 15 tests exécutés
  </acceptance_criteria>

  <done>Tous les tests du socle passent ; le prédicat distingue les 5 exes Chronos des 3 hooks GSD ; les 4 formes de JSON inexploitable renvoient null.</done>
</task>

<task type="auto">
  <name>Task 3: Figer la fixture de l'état réel pollué (25 groupes Chronos + 3 groupes GSD)</name>

  <read_first>
    - tests/Chronos.Tests/TestData/usage-valid.json (format et style des fixtures existantes)
    - .planning/phases/15-idempotence-des-int-grations/15-RESEARCH.md (section « Runtime State Inventory » : inventaire exact du fichier réel)
    - .planning/phases/15-idempotence-des-int-grations/15-CONTEXT.md (section « Défaut constaté en production »)
  </read_first>

  <files>tests/Chronos.Tests/TestData/claude-settings-pollue.json</files>

  <action>
Créer `tests/Chronos.Tests/TestData/claude-settings-pollue.json` : JSON strict (aucun commentaire),
indentation 2 espaces, **reproduisant fidèlement la structure du `~/.claude/settings.json` réel constaté
le 2026-09-09**, avec exactement 3 clés racine dans cet ordre : `hooks`, `statusLine`,
`agentPushNotifEnabled`.

`hooks` contient exactement 7 clés d'événement, dans cet ordre :

1. `"SessionStart"` : tableau de **6** groupes —
   - groupe 0 (à préserver) : `{ "hooks": [ { "type": "command", "command": "node \"C:/Users/Tanguy/.claude/hooks/gsd-check-update.js\"" } ] }`
   - groupes 1 à 5 (Chronos, à purger) : un groupe `{ "hooks": [ { "type": "command", "command": "<CMD>", "timeout": 10 } ] }` par exe, où `<CMD>` = `"<chemin>" --hook SessionStart` pour les 5 chemins :
     `C:/Users/Tanguy/Downloads/Chronos-v2.5.exe`,
     `C:/Users/Tanguy/Downloads/Chronos-v2.5.1.exe`,
     `C:/Users/Tanguy/Downloads/Chronos-v2.6.exe`,
     `C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY/src/Chronos/bin/Debug/net8.0-windows/Chronos.exe`,
     `C:/Users/Tanguy/Downloads/Chronos-v2.8.1.exe`
2. `"PostToolUse"` (à préserver INTACT, `matcher` et `timeout` compris) :
   `[ { "matcher": "Bash|Edit|Write|MultiEdit|Agent|Task", "hooks": [ { "type": "command", "command": "node \"C:/Users/Tanguy/.claude/hooks/gsd-context-monitor.js\"", "timeout": 10 } ] } ]`
3. `"PreToolUse"` (à préserver INTACT) :
   `[ { "matcher": "Write|Edit", "hooks": [ { "type": "command", "command": "node \"C:/Users/Tanguy/.claude/hooks/gsd-prompt-guard.js\"", "timeout": 5 } ] } ]`
4. `"Notification"` : **5** groupes Chronos (les mêmes 5 chemins, `--hook Notification`)
5. `"Stop"` : **5** groupes Chronos (`--hook Stop`)
6. `"UserPromptSubmit"` : **5** groupes Chronos (`--hook UserPromptSubmit`)
7. `"SessionEnd"` : **5** groupes Chronos (`--hook SessionEnd`)

Total : **25 groupes Chronos** et **3 groupes non-Chronos**.

`statusLine` (Chronos périmée, backslashes, chemin `Downloads` — état réel) :
`{ "type": "command", "command": "\"C:\\\\Users\\\\Tanguy\\\\Downloads\\\\Chronos-v2.8.1.exe\" --statusline", "padding": 2 }`
Le champ `padding` est ajouté volontairement par rapport au fichier réel : c'est un champ **officiel et
optionnel** que la reconstruction actuelle de l'objet efface ; la fixture doit permettre de prouver
qu'il survit (Pitfall 4).

`agentPushNotifEnabled` : `true`.

Ajouter, dans `PostToolUse`, une clé racine supplémentaire inconnue de Chronos à l'intérieur du groupe
GSD : `"if": "always"` — pour prouver que les champs inconnus des groupes tiers survivent aussi.

Le fichier doit être un JSON valide (vérifiable par `python -c "import json;json.load(open(...))"`).
Aucune donnée secrète : ce sont des chemins et des noms de scripts publics.
  </action>

  <verify>
    <automated>python -c "import json;d=json.load(open('tests/Chronos.Tests/TestData/claude-settings-pollue.json',encoding='utf-8'));h=d['hooks'];c=sum(1 for g in sum(h.values(),[]) for e in g['hooks'] if '--hook' in e.get('command',''));assert c==25,c;assert len(h)==7;assert d['agentPushNotifEnabled'] is True;assert d['statusLine']['padding']==2;print('OK',c)"</automated>
  </verify>

  <acceptance_criteria>
    - `tests/Chronos.Tests/TestData/claude-settings-pollue.json` existe et est du JSON valide
    - Il contient exactement 25 handlers dont la commande contient `--hook` (vérifié par la commande ci-dessus)
    - `hooks` a exactement 7 clés d'événement
    - `grep -c "gsd-check-update.js" ...` = 1, `grep -c "gsd-context-monitor.js" ...` = 1, `grep -c "gsd-prompt-guard.js" ...` = 1
    - Le fichier contient `"matcher": "Bash|Edit|Write|MultiEdit|Agent|Task"` et `"matcher": "Write|Edit"`
    - Le fichier contient `"agentPushNotifEnabled": true` et `"padding": 2`
    - Le fichier contient `Chronos-v2.5.exe`, `Chronos-v2.5.1.exe`, `Chronos-v2.6.exe`, `Chronos-v2.8.1.exe` et `bin/Debug/net8.0-windows/Chronos.exe`
  </acceptance_criteria>

  <done>La fixture reproduit l'état réel (25 groupes Chronos, 3 groupes GSD avec matcher/timeout/if, statusLine périmée avec padding, agentPushNotifEnabled) et est consommable par les tests du plan 03.</done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln --nologo -v q` → 0 erreur.
2. `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --nologo -v q` → ≥ 328 + nouveaux tests, 0 échec.
3. `dotnet test ... --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert (le nouveau type neutre ne fuit aucun WPF).
4. Aucun test de ce plan ne lit ni n'écrit `~/.claude/settings.json` :
   `grep -rn "\.claude" tests/Chronos.Tests/ClaudeSettingsJsonTests.cs` ne renvoie que des chaînes littérales de commandes, jamais un accès fichier.
</verification>

<success_criteria>
- `ClaudeSettingsJson` expose le prédicat marqueur+nom d'exe, l'analyse tolérante « null = ne rien écrire », et la sérialisation `UnsafeRelaxedJsonEscaping`.
- Les 5 chemins d'exe Chronos historiques sont reconnus ; les 3 hooks GSD ne le sont pas.
- Les 4 formes de JSON inexploitable (clés dupliquées, racine tableau, JSON invalide, scalaire) renvoient `null`.
- La fixture `claude-settings-pollue.json` fige l'état réel à 25 groupes Chronos + 3 groupes GSD.
- Suite complète verte, `ServicesLayerPurityTests` compris.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/15-idempotence-des-int-grations/15-01-SUMMARY.md`.
</output>

