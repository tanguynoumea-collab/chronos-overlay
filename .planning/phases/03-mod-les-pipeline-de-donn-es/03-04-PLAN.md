---
phase: 03-mod-les-pipeline-de-donn-es
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/install-bridge.mjs
  - scripts/README.md
autonomous: false
requirements: [DAT-04]
user_setup:
  - service: claude-code-statusline
    why: "Brancher le pont Chronos dans la statusLine pour materialiser rate_limits dans usage.json (source primaire DAT-04)."
    dashboard_config:
      - task: "Autoriser la modification de ~/.claude/settings.json (statusLine.command), avec sauvegarde prealable"
        location: "~/.claude/settings.json (profil utilisateur, hors repo)"
must_haves:
  truths:
    - "Un script d'installation idempotent sauvegarde ~/.claude/settings.json, detecte la commande statusLine existante, et remplace statusLine.command par le wrapper qui chaine l'existant."
    - "Relance sur une config deja pontee = no-op (idempotent), jamais de double-wrapping ni de perte de l'existant."
    - "Une procedure manuelle documentee permet a l'utilisateur d'installer/desinstaller lui-meme."
    - "Apres installation et une session Claude Code reelle : la statusLine s'affiche intacte ET %APPDATA%\\Chronos\\usage.json se remplit."
  artifacts:
    - path: "scripts/install-bridge.mjs"
      provides: "Installeur idempotent non destructif (backup + chainage du wrapper)"
      contains: "settings.json"
    - path: "scripts/README.md"
      provides: "Procedure manuelle install/desinstall + role du pont"
      contains: "chronos-statusline-bridge"
  key_links:
    - from: "scripts/install-bridge.mjs"
      to: "~/.claude/settings.json"
      via: "lecture + backup + reecriture idempotente de statusLine.command"
      pattern: "statusLine"
---

<objective>
Deployer la source primaire : brancher le pont `chronos-statusline-bridge.js` (cree au plan 03-02) dans la statusLine de Claude Code, de facon NON DESTRUCTIVE et idempotente, puis verifier en session reelle que la statusLine existante n'est pas cassee et que `usage.json` se remplit.

Purpose : sans ce branchement, `rate_limits` ne transite jamais vers un fichier -> `ClaudeUsageObjectProvider` n'aurait aucune source primaire (uniquement le repli JSONL Estimated). C'est le SEUL point de la phase qui touche un fichier hors du repo (`~/.claude/settings.json`) -> traite avec precaution (sauvegarde + accord explicite + verification humaine).
Output : installeur idempotent + doc, et confirmation humaine que le pont fonctionne live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md
@docs/data-sources.md

<interfaces>
<!-- Cree au plan 03-02 : scripts/chronos-statusline-bridge.js (wrapper non destructif). -->
<!-- Config statusLine actuelle (LECTURE SEULE faite en recherche) :
     "statusLine": { "type": "command", "command": "node \"C:/Users/Tanguy/.claude/hooks/gsd-statusline.js\"" }
     Une SEULE commande possible -> le wrapper doit chainer l'existant. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Installeur idempotent + procedure manuelle documentee</name>
  <files>scripts/install-bridge.mjs, scripts/README.md</files>
  <read_first>
    - .planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md § « Installation — recommandation : automatique + documentee » + § « Design recommande du pont »
    - docs/data-sources.md § 1 (contrainte non destructive, une seule commande statusLine)
  </read_first>
  <action>
    A) `scripts/install-bridge.mjs` (Node ESM, idempotent, NON destructif). Comportement :
    1. Resoudre `settingsPath = path.join(os.homedir(), ".claude", "settings.json")`.
    2. Lire settings.json (si absent -> objet vide). Parser en JSON tolerant (try/catch ; si illisible -> ABANDONNER avec message clair, ne rien ecrire).
    3. SAUVEGARDE prealable : copier vers `settings.json.chronos.bak` (ne pas ecraser un backup existant -> suffixer timestamp si present).
    4. Detecter `settings.statusLine?.command`. Trois cas :
       - deja le wrapper Chronos (`command` contient `chronos-statusline-bridge`) -> NO-OP, afficher « deja installe », sortir 0.
       - une autre commande existe -> la MEMORISER (le wrapper doit la re-executer). Cible du pont deja code en dur sur `gsd-statusline.js` (plan 03-02) ; si la commande existante differe, l'afficher en avertissement et demander une edition manuelle plutot que reecrire aveuglement le wrapper.
       - aucune commande -> installer le wrapper seul (sans chainage).
    5. Ecrire `statusLine = { type: "command", command: 'node "<chemin absolu vers scripts/chronos-statusline-bridge.js avec slashes avant>"' }` (slashes avant, imperatif Git Bash). Ecriture atomique (temp + rename) du settings.json.
    6. Fournir un flag `--uninstall` : restaurer la commande memorisee / le backup.
    Commentaires FR. Le script ne modifie QUE la cle `statusLine` ; il preserve toutes les autres cles de settings.json.

    B) `scripts/README.md` (FR) : role du pont, procedure d'installation AUTO (`node scripts/install-bridge.mjs`), procedure MANUELLE (editer `~/.claude/settings.json`, remplacer `statusLine.command` par `node "<...>/scripts/chronos-statusline-bridge.js"`, garder l'ancienne commande car le wrapper la re-execute), et procedure de DESINSTALLATION (`--uninstall` ou restaurer `.chronos.bak`). Avertissement securite : le pont ne lit que `rate_limits`.
  </action>
  <verify>
    <automated>node --check scripts/install-bridge.mjs && node -e "const fs=require('fs');if(!fs.existsSync('scripts/README.md'))process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/install-bridge.mjs` : syntaxe valide.
    - `grep -q "settings.json" scripts/install-bridge.mjs` ET `grep -qi "bak" scripts/install-bridge.mjs` (sauvegarde).
    - `grep -q "chronos-statusline-bridge" scripts/install-bridge.mjs` ET `grep -q "chronos-statusline-bridge" scripts/README.md`
    - `grep -q "uninstall" scripts/install-bridge.mjs` (desinstallation possible).
    - Idempotence documentee : `grep -qi "deja install\|idempot" scripts/install-bridge.mjs`.
  </acceptance_criteria>
  <done>Installeur idempotent (backup, detection, chainage non destructif, uninstall) + README FR. AUCUNE modification de ~/.claude/settings.json n'est faite dans cette tache (script seulement).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 : Installation live du pont + verification humaine (statusLine intacte + usage.json rempli)</name>
  <action>Etape de verification humaine : apres accord explicite de l'utilisateur, executer `node scripts/install-bridge.mjs`, puis confirmer en session Claude Code reelle que la statusLine reste intacte et que %APPDATA%\Chronos\usage.json se remplit. Aucune ecriture automatique hors repo sans accord.</action>
  <what-built>
    Le pont `scripts/chronos-statusline-bridge.js` (plan 03-02) et l'installeur idempotent `scripts/install-bridge.mjs` (Task 1). Rien n'a encore ete ecrit dans `~/.claude/settings.json` : cette etape requiert ton accord explicite (fichier hors repo) et une session Claude Code reelle pour la verification.
  </what-built>
  <how-to-verify>
    1. (Accord) Autoriser la modification de `~/.claude/settings.json`. Lancer l'installeur : `node scripts/install-bridge.mjs`. Verifier le message « installe » (ou « deja installe » si relance = idempotent) et l'existence de `~/.claude/settings.json.chronos.bak`.
    2. Ouvrir (ou continuer) une session Claude Code reelle et laisser passer au moins une reponse de l'assistant (rate_limits n'apparait qu'apres la 1re reponse API, abonnes Pro/Max).
    3. Verifier que la statusLine s'affiche TOUJOURS normalement (model | task | dir | contexte) — le pont ne doit pas l'avoir cassee.
    4. Verifier que `%APPDATA%\Chronos\usage.json` existe et contient `five_hour`/`seven_day` (ou null) + `capturedAt`. Commande : `type "%APPDATA%\Chronos\usage.json"` (cmd) ou `cat "$APPDATA/Chronos/usage.json"` (bash).
    5. (Idempotence) Relancer `node scripts/install-bridge.mjs` : doit afficher « deja installe » sans re-modifier.
    En cas de statusLine cassee : `node scripts/install-bridge.mjs --uninstall` (ou restaurer le .bak) et decrire le symptome.
  </how-to-verify>
  <resume-signal>Taper « approuve » si statusLine intacte + usage.json rempli ; sinon decrire le probleme (statusLine cassee, usage.json vide/absent, rate_limits absent).</resume-signal>
</task>

</tasks>

<verification>
- Installeur : `node --check scripts/install-bridge.mjs` OK ; relance = idempotente.
- Verification humaine : statusLine existante intacte + `%APPDATA%\Chronos\usage.json` alimente en session reelle (maps aux Human Verification Items de 03-VALIDATION.md).
</verification>

<success_criteria>
- DAT-04 (deploiement) : le pont est branche de facon non destructive et idempotente ; usage.json se remplit en session reelle, sans casser la statusLine existante.
- Reversibilite garantie (backup + --uninstall).
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-mod-les-pipeline-de-donn-es/03-04-SUMMARY.md`.
</output>
