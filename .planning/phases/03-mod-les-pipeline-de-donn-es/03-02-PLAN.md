---
phase: 03-mod-les-pipeline-de-donn-es
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/chronos-statusline-bridge.js
  - scripts/fixtures/statusline-input.json
  - src/Chronos/Services/ClaudeUsageObjectProvider.cs
  - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs
  - tests/Chronos.Tests/TestData/usage-valid.json
  - tests/Chronos.Tests/TestData/usage-partial.json
  - tests/Chronos.Tests/TestData/usage-corrupt.json
autonomous: true
requirements: [DAT-04, ROB-02]
must_haves:
  truths:
    - "Le pont Node lit le stdin de la statusLine, ecrit %APPDATA%\\Chronos\\usage.json ATOMIQUEMENT (temp puis rename) AVANT de relancer la statusLine existante, et re-emet la sortie de gsd-statusline.js intacte."
    - "Le pont propage rate_limits fenetre par fenetre (null si absente), n'invente aucune valeur, et ne casse jamais la statusLine meme si l'ecriture echoue."
    - "ClaudeUsageObjectProvider lit usage.json et mappe used_percentage/100 -> Utilization, resets_at (epoch s) -> DateTimeOffset, capturedAt -> Age via IClock, avec Reliability = Exact."
    - "Fenetre absente -> WindowState.Unavailable ; fichier absent/corrompu -> UsageSnapshot.Empty, jamais d'exception (ROB-02)."
    - "Un champ manquant dans une fenetre presente (ex. resets_at absent) ne crashe pas : la fenetre reste partielle (Utilization connue, ResetsAt null)."
  artifacts:
    - path: "scripts/chronos-statusline-bridge.js"
      provides: "Pont wrapper non destructif statusLine -> usage.json"
      contains: "renameSync"
    - path: "src/Chronos/Services/ClaudeUsageObjectProvider.cs"
      provides: "Provider primaire (source Exact) lisant usage.json de facon tolerante"
      contains: "FromUnixTimeSeconds"
    - path: "tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs"
      provides: "Tests mapping + tolerance (valide/partiel/corrompu/absent)"
      min_lines: 40
  key_links:
    - from: "scripts/chronos-statusline-bridge.js"
      to: "%APPDATA%/Chronos/usage.json"
      via: "write temp + renameSync (atomique) AVANT spawnSync"
      pattern: "renameSync"
    - from: "src/Chronos/Services/ClaudeUsageObjectProvider.cs"
      to: "ChronosPaths.UsageFile"
      via: "FileStream FileShare.ReadWrite + JsonDocument"
      pattern: "FileShare.ReadWrite"
---

<objective>
Livrer la SOURCE PRIMAIRE (Fiable) : (1) le pont Node non destructif `chronos-statusline-bridge.js` qui materialise `rate_limits` dans `%APPDATA%\Chronos\usage.json`, et (2) `ClaudeUsageObjectProvider` qui lit ce fichier de facon tolerante en `UsageSnapshot` marque `Exact`.

Purpose : c'est l'unique moyen d'obtenir l'objet d'usage officiel (`rate_limits` ne transite que par le stdin de la statusLine, rien n'est persiste par Claude Code — cf. docs/data-sources.md). Le pont doit ENVELOPPER la statusLine existante (`gsd-statusline.js`) sans jamais la casser.
Output : script du pont + provider primaire teste (DAT-04), parsing tolerant (ROB-02).
Note : ce plan CREE le script mais ne l'INSTALLE PAS dans ~/.claude/settings.json — l'installation (fichier hors repo) est le plan 03-04 (checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md
@docs/data-sources.md

<interfaces>
<!-- Contrats produits par le plan 03-01 (deja en place). Utiliser directement. -->
From src/Chronos/Services/IUsageProvider.cs :
```csharp
public interface IUsageProvider {
    Task<UsageSnapshot> GetAsync(CancellationToken ct = default);
    event EventHandler<UsageSnapshot>? SnapshotChanged;
}
```
From src/Chronos/Models/WindowState.cs :
```csharp
public sealed record WindowState {
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public long? EstimatedTokens { get; init; }
    public required SourceReliability Reliability { get; init; }
    public bool Exhausted => Utilization is >= 1.0;
    public static WindowState Unavailable(WindowKind k);
    public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength);
}
```
From src/Chronos/Models/UsageSnapshot.cs : `FiveHour`, `SevenDay`, `SourceCapturedAt`, `Age`, `static UsageSnapshot Empty`.
From src/Chronos/Services/ChronosPaths.cs : `record ChronosPaths(string UsageFile, string ProjectsRoot)`.
From src/Chronos/Services/IClock.cs : `DateTimeOffset UtcNow`.
From tests/Chronos.Tests/Fakes/FakeClock.cs : `internal FakeClock(DateTimeOffset now)`.
</interfaces>

<!-- Config statusLine utilisateur (LECTURE SEULE, cf. RESEARCH) : commande enfant a re-executer =
     node "C:/Users/Tanguy/.claude/hooks/gsd-statusline.js" (slashes avant, imperatif sous Git Bash). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Pont Node non destructif + fixture d'entree</name>
  <files>scripts/chronos-statusline-bridge.js, scripts/fixtures/statusline-input.json</files>
  <read_first>
    - .planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md § « Le pont statusLine -> fichier » (design recommande, sequence 1-5) + § « Pitfall 3 »
    - docs/data-sources.md § 1 (mecanisme d'acces, contrainte non destructive, schema)
  </read_first>
  <action>
    A) Creer `scripts/fixtures/statusline-input.json` — echantillon d'entree stdin realiste (RESEARCH § echantillon) :
    ```json
    { "rate_limits": { "five_hour": { "used_percentage": 23.5, "resets_at": 1738425600 }, "seven_day": { "used_percentage": 41.2, "resets_at": 1738857600 } } }
    ```

    B) Creer `scripts/chronos-statusline-bridge.js` (CommonJS, Node v24). Commentaires FR en tete (role, non destructif, atomique-avant-spawn). Sequence EXACTE (RESEARCH § design recommande) :

    1. Bufferiser TOUT le stdin : `let input = ""; process.stdin.on('data', c => input += c); process.stdin.on('end', () => { ... });` — le stdin ne se lit qu'une fois.
    2. Dans 'end' : `let data = null; try { data = JSON.parse(input); } catch { data = null; }`. Extraire `data && data.rate_limits`.
    3. ECRIRE usage.json ATOMIQUEMENT AVANT tout spawn (survie a l'annulation en vol), le tout en try/catch pour NE JAMAIS casser la statusLine :
       - `const dir = path.join(process.env.APPDATA, "Chronos"); fs.mkdirSync(dir, { recursive: true });`
       - `const usageFinal = path.join(dir, "usage.json"); const tmp = path.join(dir, "usage.json.tmp-" + process.pid);`
       - Payload SANS invention, fenetre par fenetre :
         `const rl = data && data.rate_limits; const payload = { five_hour: (rl && rl.five_hour) || null, seven_day: (rl && rl.seven_day) || null, capturedAt: Date.now() };`
         (capturedAt = epoch MILLISECONDES, lu cote C# via FromUnixTimeMilliseconds.)
       - `fs.writeFileSync(tmp, JSON.stringify(payload)); fs.renameSync(tmp, usageFinal);`
    4. Re-executer la statusLine existante avec LE MEME stdin :
       `const child = require("child_process").spawnSync("node", ["C:/Users/Tanguy/.claude/hooks/gsd-statusline.js"], { input: input, encoding: "utf8" });`
    5. Re-emettre la sortie enfant TELLE QUELLE et propager le code :
       `if (child.stdout) process.stdout.write(child.stdout); if (child.stderr) process.stderr.write(child.stderr); process.exit(child.status || 0);`
    SECURITE : ne lire QUE `data.rate_limits` ; ne jamais lire `.credentials.json` ni le contenu des conversations.
  </action>
  <verify>
    <automated>node scripts/chronos-statusline-bridge.js < scripts/fixtures/statusline-input.json ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "renameSync" scripts/chronos-statusline-bridge.js` (ecriture atomique).
    - `grep -q "spawnSync" scripts/chronos-statusline-bridge.js` ET `grep -q "gsd-statusline.js" scripts/chronos-statusline-bridge.js` (re-execution non destructive).
    - L'ecriture de usage.json apparait AVANT l'appel spawnSync dans le fichier (verifiable : `renameSync` a une ligne inferieure a celle de `spawnSync`).
    - Apres `node scripts/chronos-statusline-bridge.js < scripts/fixtures/statusline-input.json`, le fichier `$APPDATA/Chronos/usage.json` existe et contient `five_hour.used_percentage == 23.5` et un `capturedAt` numerique (verifiable par `node -e` de lecture, ou inspection manuelle).
    - `grep -qi "credentials" scripts/chronos-statusline-bridge.js` renvoie FAUX (aucune lecture de credentials).
  </acceptance_criteria>
  <done>Le pont ecrit usage.json atomiquement avant de relancer gsd-statusline.js et re-emet sa sortie ; aucune valeur inventee, fenetres nulles propagees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : ClaudeUsageObjectProvider + fixtures + tests (DAT-04, ROB-02)</name>
  <files>src/Chronos/Services/ClaudeUsageObjectProvider.cs, tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs, tests/Chronos.Tests/TestData/usage-valid.json, tests/Chronos.Tests/TestData/usage-partial.json, tests/Chronos.Tests/TestData/usage-corrupt.json</files>
  <read_first>
    - .planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md § « Code Examples » (Lecture tolerante du fichier pont, verbatim) + § « Pitfall 1, 4 »
    - docs/data-sources.md § 3 (mapping) + § 1 (schema, epoch SECONDES)
    - tests/Chronos.Tests/CompositionRootTests.cs (style xUnit FR)
  </read_first>
  <behavior>
    ClaudeUsageObjectProviderTests ([Fact]/[Theory] classiques, chemins injectes vers TestData, FakeClock). Le csproj des tests doit copier TestData au build (CopyToOutputDirectory).
    - usage-valid.json (five_hour 23.5/1738425600, seven_day 41.2/1738857600, capturedAt ms) :
      * FiveHour.Utilization == 0.235 (± 1e-9), Reliability == Exact.
      * FiveHour.ResetsAt == DateTimeOffset.FromUnixTimeSeconds(1738425600) (== 2025-02-01T16:00:00Z — Pitfall 1).
      * SevenDay.Utilization == 0.412 ; SnapshotAge : avec FakeClock = capturedAt + 30 s, Age == 30 s.
      * FiveHour.FractionTimeRemaining non null (calcule via FractionRemaining, fenetre 5 h).
    - usage-partial.json (five_hour present SANS resets_at ; seven_day absent) :
      * FiveHour.Utilization connue, FiveHour.ResetsAt == null, FractionTimeRemaining == null (Pitfall 4, pas de NRE).
      * SevenDay == Unavailable.
    - usage-corrupt.json (JSON tronque/invalide) -> GetAsync renvoie UsageSnapshot.Empty, AUCUNE exception (ROB-02).
    - Fichier ABSENT (chemin vers un usage.json inexistant) -> UsageSnapshot.Empty, aucune exception.
  </behavior>
  <action>
    Ecrire d'ABORD les fixtures + tests (rouge), puis le provider (vert). Copier le provider VERBATIM du RESEARCH § « Lecture tolerante du fichier pont ».

    Fixtures TestData :
    - `usage-valid.json` : `{ "five_hour": {"used_percentage":23.5,"resets_at":1738425600}, "seven_day": {"used_percentage":41.2,"resets_at":1738857600}, "capturedAt": <epoch ms fixe, ex. 1751976000000> }`
    - `usage-partial.json` : `{ "five_hour": {"used_percentage":88.0} }` (pas de resets_at, pas de seven_day).
    - `usage-corrupt.json` : contenu volontairement invalide, ex. `{ "five_hour": {"used_perc` (JSON tronque).

    Provider `src/Chronos/Services/ClaudeUsageObjectProvider.cs` (namespace Chronos.Services) :
    - Ctor : `public ClaudeUsageObjectProvider(ChronosPaths paths, IClock clock)`. Champs `_paths`, `_clock`. Implemente `IUsageProvider` ; declarer `public event EventHandler<UsageSnapshot>? SnapshotChanged;` (emis en fin de GetAsync reussi, optionnel en Phase 3).
    - `JsonSerializerOptions Tolerant` (static readonly) : PropertyNameCaseInsensitive=true, ReadCommentHandling=Skip, AllowTrailingCommas=true, NumberHandling=AllowReadingFromString (verbatim RESEARCH).
    - `GetAsync` : ouvrir `_paths.UsageFile` en `FileStream(..., FileAccess.Read, FileShare.ReadWrite)`, `JsonDocument.ParseAsync`, lire `capturedAt` via `FromUnixTimeMilliseconds`, appeler `ReadWindow(root,"five_hour",FiveHour,TimeSpan.FromHours(5))` et `("seven_day",SevenDay,TimeSpan.FromDays(7))`. Age = capturedAt is null ? null : `_clock.UtcNow - capturedAt`.
    - `catch (Exception ex) when (ex is IOException or JsonException or FileNotFoundException or DirectoryNotFoundException)` -> `return UsageSnapshot.Empty;` (ROB-01/ROB-02, jamais de crash).
    - `ReadWindow` : `used_percentage` -> `/100.0` ; `resets_at` -> `DateTimeOffset.FromUnixTimeSeconds` (SECONDES, Pitfall 1) ; chaque champ en TryGetProperty (fenetre absente -> Unavailable, champ manquant -> null) ; Reliability=Exact ; FractionTimeRemaining via `WindowState.FractionRemaining(reset, _clock.UtcNow, len)`.
    Ajouter `[ItemGroup]` de copie TestData dans tests/Chronos.Tests/Chronos.Tests.csproj si besoin :
    `<Content Include="TestData\**"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></Content>` (verifier que ce fichier reste dans files_modified via le plan si modifie — sinon utiliser un chemin absolu resolu depuis AppContext.BaseDirectory dans les tests).
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --filter "FullyQualifiedName~ClaudeUsageObjectProvider"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~ClaudeUsageObjectProvider"` : tous verts (valide, partiel, corrompu, absent).
    - `grep -q "FromUnixTimeSeconds" src/Chronos/Services/ClaudeUsageObjectProvider.cs` (resets_at en secondes).
    - `grep -q "FileShare.ReadWrite" src/Chronos/Services/ClaudeUsageObjectProvider.cs`
    - `grep -q "return UsageSnapshot.Empty" src/Chronos/Services/ClaudeUsageObjectProvider.cs` (degradation sans crash).
    - `grep -c "System.Windows" src/Chronos/Services/ClaudeUsageObjectProvider.cs` == 0.
  </acceptance_criteria>
  <done>Provider primaire lit usage.json en Exact, mappe correctement (used_percentage/100, epoch s), tolere fenetre/champ absent et fichier corrompu/absent (Empty) ; tests verts -> DAT-04 + ROB-02.</done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug` : suite complete verte (dont ServicesLayerPurityTests, toujours vert).
- Pont : `node scripts/chronos-statusline-bridge.js < scripts/fixtures/statusline-input.json` produit `$APPDATA/Chronos/usage.json` avec `five_hour`/`seven_day`/`capturedAt` et re-emet une sortie (statusLine enfant).
</verification>

<success_criteria>
- DAT-04 : le pont materialise rate_limits dans usage.json (atomique, non destructif) et ClaudeUsageObjectProvider le lit en UsageSnapshot Exact.
- ROB-02 : fichier corrompu/absent, fenetre/champ manquant -> jamais d'exception, jamais de valeur inventee.
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-mod-les-pipeline-de-donn-es/03-02-SUMMARY.md`.
</output>
