---
phase: 03-mod-les-pipeline-de-donn-es
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/Chronos/Services/JsonlEstimationProvider.cs
  - src/Chronos/Services/CompositeUsageProvider.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/JsonlEstimationProviderTests.cs
  - tests/Chronos.Tests/CompositeUsageProviderTests.cs
  - tests/Chronos.Tests/TestData/sample-valid.jsonl
  - tests/Chronos.Tests/TestData/sample-tolerant.jsonl
autonomous: true
requirements: [DAT-05, DAT-06, ROB-02]
must_haves:
  truths:
    - "JsonlEstimationProvider somme input+output+cache_creation+cache_read des lignes assistant dont timestamp est dans la fenetre glissante (5 h / 7 j depuis now), en streaming FileShare.ReadWrite."
    - "L'estimation est TOUJOURS marquee Estimated, avec Utilization=null et ResetsAt=null (jamais de valeur inventee), EstimatedTokens = somme brute."
    - "Le parsing JSONL ignore lignes corrompues, derniere ligne partielle, champs manquants et lignes non-assistant (jamais matcher la prose 'five_hour'/'seven_day'), sans jamais lever d'exception."
    - "CompositeUsageProvider prend la meilleure source PAR FENETRE : Exact prioritaire, sinon Estimated, sinon Unavailable."
    - "App.xaml.cs enregistre IClock, ChronosPaths, les deux providers concrets et IUsageProvider=CompositeUsageProvider en Singleton, resolubles sans exception."
  artifacts:
    - path: "src/Chronos/Services/JsonlEstimationProvider.cs"
      provides: "Provider de repli : estimation par somme de tokens JSONL en streaming tolerant"
      contains: "FileShare.ReadWrite"
    - path: "src/Chronos/Services/CompositeUsageProvider.cs"
      provides: "Bascule primaire->repli par fenetre"
      contains: "SourceReliability.Exact"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Enregistrement DI Singleton du pipeline complet"
      contains: "CompositeUsageProvider"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "IUsageProvider"
      via: "AddSingleton<IUsageProvider>(sp => new CompositeUsageProvider(primary, fallback))"
      pattern: "CompositeUsageProvider"
    - from: "src/Chronos/Services/JsonlEstimationProvider.cs"
      to: "ChronosPaths.ProjectsRoot"
      via: "Directory.EnumerateFiles recursif + StreamReader.ReadLineAsync"
      pattern: "EnumerateFiles"
    - from: "src/Chronos/Services/CompositeUsageProvider.cs"
      to: "WindowState.Best"
      via: "selection par fenetre Exact>Estimated>Unavailable"
      pattern: "Reliability == SourceReliability.Exact"
---

<objective>
Fermer le pipeline : (1) `JsonlEstimationProvider` (repli honnete par somme de tokens JSONL, marque `Estimated`, sans Utilization/ResetsAt inventes), (2) `CompositeUsageProvider` (bascule primaire->repli PAR FENETRE), et (3) l'enregistrement DI complet dans `App.xaml.cs`.

Purpose : rendre le success criterion 1 de la phase VRAI — « un provider composite renvoie un snapshot : primaire si lisible, sinon estime depuis les JSONL ». C'est le point de cablage final : apres ce plan, `IUsageProvider` resout un `CompositeUsageProvider` operationnel.
Output : DAT-05 + DAT-06 + ROB-02 couverts, graphe DI cable et testable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md
@docs/data-sources.md
@src/Chronos/App.xaml.cs

<interfaces>
<!-- Contrats en place (plans 03-01 et 03-02). Utiliser directement, ne pas re-explorer. -->
From src/Chronos/Services/IUsageProvider.cs : `Task<UsageSnapshot> GetAsync(CancellationToken ct = default);` + `event EventHandler<UsageSnapshot>? SnapshotChanged;`
From src/Chronos/Services/ClaudeUsageObjectProvider.cs : `ClaudeUsageObjectProvider(ChronosPaths paths, IClock clock)` (source primaire, Exact).
From src/Chronos/Services/ChronosPaths.cs : `record ChronosPaths(string UsageFile, string ProjectsRoot)` + `static ChronosPaths Default()`.
From src/Chronos/Models/WindowState.cs : record nullable-safe + `static WindowState Unavailable(WindowKind)`.
From src/Chronos/Models/UsageSnapshot.cs : `FiveHour`, `SevenDay`, `SourceCapturedAt`, `Age`, `static Empty`.

<!-- App.xaml.cs actuel (Phase 1) — ConfigureServices a etendre, NE PAS casser l'existant : -->
```csharp
private static void ConfigureServices(IServiceCollection services)
{
    services.AddSingleton<IUiDispatcher>(_ => new WpfUiDispatcher(Current.Dispatcher));
    services.AddSingleton<TopmostGuard>();
    services.AddSingleton<MainViewModel>();
    services.AddSingleton<MainWindow>();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : JsonlEstimationProvider + fixtures + tests (DAT-05, ROB-02)</name>
  <files>src/Chronos/Services/JsonlEstimationProvider.cs, tests/Chronos.Tests/JsonlEstimationProviderTests.cs, tests/Chronos.Tests/TestData/sample-valid.jsonl, tests/Chronos.Tests/TestData/sample-tolerant.jsonl</files>
  <read_first>
    - .planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md § « Estimation JSONL honnete » (Option a) + § « Code Examples » (Streaming tolerant du JSONL, verbatim) + § « Pitfall 2 » + note « Faux positifs a eviter »
    - docs/data-sources.md § 2 (schema ligne assistant, message.usage, timestamp ISO 8601, streaming FileShare.ReadWrite)
  </read_first>
  <behavior>
    JsonlEstimationProviderTests ([Fact]/[Theory], chemins injectes vers TestData, FakeClock a un now fixe coherent avec les timestamps des fixtures).
    - sample-valid.jsonl : lignes assistant avec message.usage. Avec now fixe :
      * FiveHour.EstimatedTokens == somme des tokens des lignes assistant dans les 5 dernieres heures (input+output+cache_creation+cache_read).
      * SevenDay.EstimatedTokens >= FiveHour.EstimatedTokens (fenetre plus large).
      * Les DEUX fenetres : Reliability == Estimated, Utilization == null, ResetsAt == null, FractionTimeRemaining == null (jamais invente).
    - sample-tolerant.jsonl contient : une ligne assistant valide, une ligne JSON corrompue, une ligne non-assistant (type=user), une ligne de prose contenant le texte "five_hour" dans un champ content, et une derniere ligne TRONQUEE (sans \n final, JSON incomplet) :
      * GetAsync NE LEVE PAS d'exception (ROB-02).
      * Seule la ligne assistant valide est comptee ; la prose "five_hour" et la ligne user ne sont PAS comptees (faux positifs evites) ; la ligne corrompue et la partielle sont ignorees.
    - Dossier ProjectsRoot inexistant -> UsageSnapshot avec deux fenetres Estimated a 0 token (ou Empty), sans exception.
  </behavior>
  <action>
    Ecrire d'ABORD fixtures + tests (rouge), puis le provider (vert). Copier le corps de GetAsync VERBATIM du RESEARCH § « Streaming tolerant du JSONL ».

    Fixtures (timestamps ISO 8601 UTC 'Z', choisir un now de test, ex. FakeClock = 2026-07-08T12:00:00Z) :
    - sample-valid.jsonl : 2-3 lignes `{"type":"assistant","timestamp":"2026-07-08T11:30:00.000Z","message":{"role":"assistant","usage":{"input_tokens":1000,"output_tokens":200,"cache_creation_input_tokens":50,"cache_read_input_tokens":300}}}` (une recente <5h, une entre 5h et 7j ex. timestamp 2026-07-05T...).
    - sample-tolerant.jsonl : ligne assistant valide + `{ "type":"assistant", broken` (corrompue) + `{"type":"user","timestamp":"2026-07-08T11:00:00.000Z","message":{"role":"user","content":"parlons de five_hour et seven_day"}}` (prose, non comptee) + derniere ligne tronquee `{"type":"assistant","timestamp":"2026-07-08T11:5` (SANS newline final).

    Provider `src/Chronos/Services/JsonlEstimationProvider.cs` (namespace Chronos.Services, implemente IUsageProvider) :
    - Ctor : `JsonlEstimationProvider(ChronosPaths paths, IClock clock)`. Declarer `public event EventHandler<UsageSnapshot>? SnapshotChanged;`.
    - `GetAsync` : `now = _clock.UtcNow`. Iterer `EnumerateJsonl(_paths.ProjectsRoot)` (helper : `Directory.Exists` puis `Directory.EnumerateFiles(root, "*.jsonl", SearchOption.AllDirectories)`, le tout en try/catch — dossier absent -> sequence vide). Pour chaque fichier : `FileStream(..., FileAccess.Read, FileShare.ReadWrite)` en try/catch (IOException -> continue), `StreamReader`, boucle `ReadLineAsync` :
      * ligne vide -> continue ;
      * `try { using var doc = JsonDocument.Parse(line); ... } catch (JsonException) { /* ignoree ROB-02 */ }` ;
      * `IsAssistant(o)` : `o.TryGetProperty("type", out var t) && t.GetString()=="assistant"` ET `message.role == "assistant"` (objet structure, JAMAIS matcher une chaine dans content — note faux positifs) ;
      * `timestamp` -> `DateTimeOffset.TryParse(s, CultureInfo.InvariantCulture, DateTimeStyles.RoundtripKind, out when)` sinon continue ;
      * `SumUsageTokens(o)` = input_tokens+output_tokens+cache_creation_input_tokens+cache_read_input_tokens (chaque champ via TryGetProperty, defaut 0) ;
      * `if (when >= now - TimeSpan.FromHours(5)) five += tokens;` `if (when >= now - TimeSpan.FromDays(7)) week += tokens;`
    - Retour : `EstimatedWindow(WindowKind, total)` -> `new WindowState { Kind=k, EstimatedTokens=total, Utilization=null, ResetsAt=null, FractionTimeRemaining=null, Reliability=SourceReliability.Estimated }`. SourceCapturedAt=now, Age=TimeSpan.Zero.
    - Commentaire FR en tete : « Estimation HONNETE : somme de tokens exacte, mais % de quota INCONNU (plafonds non publies) -> toujours Estimated, Utilization null. »
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --filter "FullyQualifiedName~JsonlEstimationProvider"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~JsonlEstimationProvider"` : tous verts (somme fenetre, corrompue/partielle/prose ignorees, dossier absent).
    - `grep -q "FileShare.ReadWrite" src/Chronos/Services/JsonlEstimationProvider.cs`
    - `grep -q "SourceReliability.Estimated" src/Chronos/Services/JsonlEstimationProvider.cs` ET aucune affectation `Utilization =` non-null dans ce fichier (`grep -q "Utilization = null" ...`).
    - `grep -q "catch (JsonException)" src/Chronos/Services/JsonlEstimationProvider.cs` (tolerance par ligne).
    - `grep -c "System.Windows" src/Chronos/Services/JsonlEstimationProvider.cs` == 0.
  </acceptance_criteria>
  <done>Repli JSONL : somme de tokens en streaming tolerant, toujours Estimated, Utilization/ResetsAt null ; lignes invalides/partielles/prose/user ignorees -> DAT-05 + ROB-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : CompositeUsageProvider — bascule par fenetre (DAT-06)</name>
  <files>src/Chronos/Services/CompositeUsageProvider.cs, tests/Chronos.Tests/CompositeUsageProviderTests.cs</files>
  <read_first>
    - .planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md § « Pattern 3 : CompositeUsageProvider » (code verbatim, incl. helper Best)
  </read_first>
  <behavior>
    CompositeUsageProviderTests avec deux fakes IUsageProvider (renvoyant des UsageSnapshot fabriques) :
    - Primaire donne FiveHour Exact + SevenDay Unavailable ; repli donne les deux Estimated -> resultat : FiveHour == primaire (Exact), SevenDay == repli (Estimated).
    - Primaire tout Unavailable ; repli tout Estimated -> resultat : les deux Estimated.
    - Primaire tout Exact ; repli tout Estimated -> resultat : les deux Exact (primaire prioritaire).
    - Primaire et repli Unavailable pour une fenetre -> resultat Unavailable pour cette fenetre (pas de crash, ROB-01 en aval).
    - SnapshotChanged est emis une fois par GetAsync avec le snapshot final.
  </behavior>
  <action>
    Ecrire tests (rouge) puis provider (vert). Copier VERBATIM le RESEARCH § Pattern 3 :
    - Ctor : `CompositeUsageProvider(IUsageProvider primary, IUsageProvider fallback)`.
    - `GetAsync` : `var p = await _primary.GetAsync(ct); var f = await _fallback.GetAsync(ct);` puis snapshot avec `FiveHour = Best(p.FiveHour, f.FiveHour)`, `SevenDay = Best(p.SevenDay, f.SevenDay)`, `SourceCapturedAt = p.SourceCapturedAt ?? f.SourceCapturedAt`, `Age = p.Age ?? f.Age`. `SnapshotChanged?.Invoke(this, snap);` puis return.
    - `private static WindowState Best(WindowState primary, WindowState fallback) => primary.Reliability == SourceReliability.Exact ? primary : fallback.Reliability == SourceReliability.Estimated ? fallback : primary;`
    (Note perf RESEARCH Open Question 3 : le court-circuit paresseux du repli est un raffinement Phase 4 — ici, appeler les deux GetAsync suffit et reste teste.)
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --filter "FullyQualifiedName~CompositeUsageProvider"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~CompositeUsageProvider"` : tous verts (les 5 cas ci-dessus).
    - `grep -q "Reliability == SourceReliability.Exact" src/Chronos/Services/CompositeUsageProvider.cs`
    - `grep -q "SnapshotChanged?.Invoke" src/Chronos/Services/CompositeUsageProvider.cs`
    - `grep -c "System.Windows" src/Chronos/Services/CompositeUsageProvider.cs` == 0.
  </acceptance_criteria>
  <done>Composite selectionne la meilleure source par fenetre (Exact>Estimated>Unavailable) et emet SnapshotChanged -> DAT-06.</done>
</task>

<task type="auto">
  <name>Task 3 : Enregistrement DI du pipeline dans App.xaml.cs</name>
  <files>src/Chronos/App.xaml.cs</files>
  <read_first>
    - src/Chronos/App.xaml.cs (ConfigureServices existant — NE PAS casser les enregistrements Phase 1)
    - .planning/phases/03-mod-les-pipeline-de-donn-es/03-RESEARCH.md § « Enregistrement DI (App.xaml.cs — Singleton) » (verbatim)
  </read_first>
  <action>
    Etendre `ConfigureServices` (App.xaml.cs) SANS retirer les lignes Phase 1. Ajouter, apres les enregistrements existants, en Singleton (verbatim RESEARCH, adapte a ChronosPaths.Default()) :
    ```csharp
    services.AddSingleton<IClock, SystemClock>();
    services.AddSingleton(ChronosPaths.Default());
    services.AddSingleton<ClaudeUsageObjectProvider>();
    services.AddSingleton<JsonlEstimationProvider>();
    services.AddSingleton<IUsageProvider>(sp => new CompositeUsageProvider(
        primary:  sp.GetRequiredService<ClaudeUsageObjectProvider>(),
        fallback: sp.GetRequiredService<JsonlEstimationProvider>()));
    ```
    Ajouter les `using Chronos.Services;` / `using Chronos.Models;` deja presents ou requis. Commentaire FR : « Pipeline de donnees Phase 3 : primaire (pont usage.json) -> repli (JSONL), composite expose comme IUsageProvider. Chemins via Environment (jamais Assembly.Location, mono-fichier). »
    NE PAS demarrer de watcher/timer ici (Phase 4).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` reussit.
    - `grep -q "AddSingleton<IUsageProvider>" src/Chronos/App.xaml.cs` ET `grep -q "CompositeUsageProvider" src/Chronos/App.xaml.cs`
    - `grep -q "AddSingleton<IClock, SystemClock>" src/Chronos/App.xaml.cs` ET `grep -q "ChronosPaths.Default()" src/Chronos/App.xaml.cs`
    - Les 4 lignes Phase 1 (IUiDispatcher, TopmostGuard, MainViewModel, MainWindow) sont toujours presentes.
    - Optionnel : un test DI de resolution `IUsageProvider` peut etre ajoute a CompositionRootTests (non requis pour ce plan).
  </acceptance_criteria>
  <done>Le graphe DI resout IUsageProvider = CompositeUsageProvider(primaire, repli) en Singleton sans casser Phase 1 ; build vert.</done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug` : suite complete verte (Phase 1 + tous les tests Phase 3, dont ServicesLayerPurityTests).
- Success criterion 1 de la phase : un CompositeUsageProvider resolu renvoie un snapshot (primaire si usage.json lisible, sinon estime JSONL).
</verification>

<success_criteria>
- DAT-05 : estimation JSONL honnete (somme tokens, Estimated, sans utilization inventee) en streaming tolerant.
- DAT-06 : bascule composite par fenetre (Exact>Estimated>Unavailable).
- ROB-02 : parsing JSONL tolerant (corrompue, partielle, prose, dossier absent) sans exception.
- Pipeline cable en DI (Singleton), IUsageProvider operationnel.
</success_criteria>

<output>
Apres completion, creer `.planning/phases/03-mod-les-pipeline-de-donn-es/03-03-SUMMARY.md`.
</output>
