---
phase: 19-nouvelle-doctrine-du-composite
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/Chronos/Models/ProvenanceReleve.cs
  - src/Chronos/Models/WindowState.cs
  - src/Chronos/Models/UsageSnapshot.cs
  - src/Chronos/Services/DoctrineFraicheur.cs
  - src/Chronos/Services/CompositeUsageProvider.cs
  - tests/Chronos.Tests/DoctrineFraicheurTests.cs
  - tests/Chronos.Tests/GardesDoctrineTests.cs
autonomous: true
requirements: [EXA-02, EXA-04, DEL-03, DEL-04]

must_haves:
  truths:
    - "La doctrine a QUATRE états distinguables (frais / encore valide / plancher avec activité / indisponible) sans qu'aucun des 32 sites « new WindowState » du dépôt n'ait à être retouché."
    - "Un relevé dont l'âge est sous la limite est rendu exact tel quel, SANS qu'aucun journal d'activité ne soit consulté — le chemin nominal ne paie aucune E/S."
    - "Un relevé de trois heures sans aucune réponse assistant depuis sa capture reste EXACT : la limite d'âge est un laissez-passer, pas un couperet, et DEL-03 ré-habilite au-delà."
    - "Avec activité depuis la capture, le chiffre change de NATURE et non de valeur : Utilization reste rigoureusement inchangée, la fiabilité devient « plancher », et les tokens bruts accompagnent sans être convertis."
    - "Un relevé antérieur à l'horizon des transcripts, ou dépourvu d'horodatage, est déclaré indisponible : la doctrine refuse de produire un delta qu'elle ne peut pas garantir plutôt que d'en inventer un."
    - "Une démotion EFFACE le pourcentage : une fenêtre indisponible qui garderait sa valeur ferait encore peindre l'arc du cadran."
    - "Chacune des quatre branches possède au moins un test qui tombe si la branche est supprimée, prouvé par mutation réelle puis révocation."
    - "Aucun rapport entre un comptage de tokens et un plafond ne peut réapparaître dans Services/ ou Models/ : une garde balaie le texte source et nomme le fichier fautif."
    - "Aucune propriété de UsageSnapshot ne peut être ajoutée sans être nommée dans CompositeUsageProvider.cs : le piège de la recomposition par « new » cesse d'être silencieux."
  artifacts:
    - path: "src/Chronos/Models/ProvenanceReleve.cs"
      provides: "Enum à trois valeurs — Frais, EncoreValide, PlancherAvecActivite"
      contains: "PlancherAvecActivite"
    - path: "src/Chronos/Models/WindowState.cs"
      provides: "Provenance et TokensDepuisReleve, init + nullables, sans required"
      contains: "TokensDepuisReleve"
    - path: "src/Chronos/Models/UsageSnapshot.cs"
      provides: "UnExactADejaEteObtenu (bool?) posé AU-DESSUS du composite"
      contains: "UnExactADejaEteObtenu"
    - path: "src/Chronos/Services/DoctrineFraicheur.cs"
      provides: "Classe PURE : LimiteAge, Age, ABesoinDuJournal, Statuer — les 4 branches"
      contains: "LimiteAge"
      min_lines: 80
    - path: "tests/Chronos.Tests/DoctrineFraicheurTests.cs"
      provides: "Les 4 branches falsifiables + cohérence entre ABesoinDuJournal et Statuer"
      min_lines: 120
    - path: "tests/Chronos.Tests/GardesDoctrineTests.cs"
      provides: "Garde de recomposition de UsageSnapshot + garde de non-retour EXA-04"
  key_links:
    - from: "src/Chronos/Services/DoctrineFraicheur.cs"
      to: "RateLimitHeaderUsageProvider.CadenceNominale"
      via: "LimiteAge DÉRIVÉE de la cadence de la sonde, jamais recopiée"
      pattern: "CadenceNominale"
    - from: "src/Chronos/Services/DoctrineFraicheur.cs"
      to: "TranscriptActivityLog.Covers"
      via: "refus explicite de produire un delta hors de l'horizon connu"
      pattern: "Covers"
    - from: "src/Chronos/Services/CompositeUsageProvider.cs"
      to: "UsageSnapshot.UnExactADejaEteObtenu"
      via: "commentaire NOMMANT le champ : il est posé au-dessus du composite, donc jamais recomposé ici"
      pattern: "UnExactADejaEteObtenu"
    - from: "tests/Chronos.Tests/GardesDoctrineTests.cs"
      to: "src/Chronos/Services/CompositeUsageProvider.cs"
      via: "balayage du texte source : toute propriété de UsageSnapshot doit y être nommée"
      pattern: "CheminSourcesChronos"
---

<objective>
Écrire **la doctrine**, et seulement la doctrine : une classe **pure**, sans E/S, sans horloge propre,
sans type WPF — donc testable en `[Fact]` sur ses quatre branches, chacune supprimable pour prouver
qu'un test tombe. Rien n'est encore branché : le plan 19-03 s'en charge.

Purpose: trois décisions sont ici gravées, toutes issues de la recherche et toutes mesurées.

**1. Quatre états, mais trois valeurs de fiabilité.** `SourceReliability.Estimated` est **mort en
production** depuis la phase 16 (aucun provider ne le produit ; il ne subsiste que cinq **lectures**),
alors que son câblage de présentation est intact : `IsEstimated`, `HasTokens`, `Describe` disent déjà
« ce chiffre n'est pas un exact courant ». C'est exactement la sémantique du plancher DEL-04. On le
**réaffecte**, et on ajoute un champ nullable `Provenance` pour la quatrième distinction. Résultat :
**zéro rupture de compilation** sur les 32 sites `new WindowState`. Ajouter un membre d'enum aurait au
contraire traversé **silencieusement** `WindowGaugeViewModel.cs:69` (`IsEstimated = r == Estimated`
⇒ `false`) et fait afficher le plancher **comme un exact** — la pire issue possible pour ce milestone.

**2. L'ordre des règles : la limite d'âge est un LAISSEZ-PASSER, DEL-03 est une RÉ-HABILITATION.**
`utilization` est fonction de la consommation ; sans consommation elle ne change pas. Donc « zéro réponse
assistant depuis T » implique « l'utilisation à `now` égale celle de T » — ce n'est pas une
approximation, c'est une **déduction**. Déclarer périmé un chiffre qu'on peut **prouver** juste, ce n'est
pas de la prudence : c'est cacher une information vraie, et l'overlay deviendrait muet précisément quand
il est le plus utile (vous revenez le matin, rien n'a tourné). La limite d'âge dispense de la preuve en
deçà ; elle ne l'interdit pas au-delà. **Le « 10 % de deux mois » ne meurt donc pas par la limite d'âge,
il meurt par `Covers(T)`** — horizon de 8 jours, déjà dans le code depuis la phase 16.

**3. DEL-04 n'a pas de réponse numérique honnête, et n'en a pas besoin.** Convertir *N tokens* en
*Δ points de pourcentage* exige un taux points/token — c'est-à-dire un plafond, que EXA-04 interdit et
que trois faits rendent faux de toute façon : les limites Anthropic pondèrent par modèle ; les
transcripts ignorent l'app de bureau et Cowork, qui consomment le même pool ; et la mesure du
2026-09-12 donne **643 649 933 tokens sur 5 h** contre l'ancien plafond de 230 000 000, soit **280 %**.
À l'intérieur d'une fenêtre non remise à zéro, `utilization` est **monotone croissante** : le dernier
relevé exact est donc une **borne inférieure rigoureuse**. Le delta ne s'ajoute pas au chiffre, il change
sa **nature** : « 42 % » devient « **au moins** 42 % », incertitude **unilatérale**, borne supérieure
**inconnue** et déclarée telle. `Utilization` n'est **jamais** gonflée.

Output: `ProvenanceReleve`, deux champs sur `WindowState`, un champ sur `UsageSnapshot`,
`DoctrineFraicheur`, et **deux gardes de non-retour**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-CONTEXT.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-VALIDATION.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Tout ce qu'il faut, texte exact. Aucune exploration de dépôt nécessaire. -->

```csharp
// src/Chronos/Models/WindowState.cs — ÉTAT ACTUEL (record scellé, 46 lignes)
public sealed record WindowState
{
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public long? EstimatedTokens { get; init; }          // somme de l'estimation ABSOLUE supprimée (phase 16)
    public required SourceReliability Reliability { get; init; }
    public DateTimeOffset? CapturedAt { get; init; }
    public StatutServeur? StatutServeur { get; init; }
    public EtatDepassement? Depassement { get; init; }
    public bool Exhausted => Utilization is >= 1.0;
    public static WindowState Unavailable(WindowKind k);
    public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength);
}

// src/Chronos/Models/SourceReliability.cs — TROIS valeurs, INCHANGÉ par ce plan
public enum SourceReliability { Exact, Estimated, Unavailable }

// src/Chronos/Models/UsageSnapshot.cs — ÉTAT ACTUEL
public sealed record UsageSnapshot
{
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
    public DateTimeOffset? SourceCapturedAt { get; init; }
    public static UsageSnapshot Empty { get; }
}
```

```csharp
// src/Chronos/Services/ITranscriptActivitySource.cs (phase 16) — le contrat du journal
public sealed class TranscriptActivityLog
{
    public DateTimeOffset Now { get; }
    public DateTimeOffset Horizon { get; }
    public TranscriptActivityLog(DateTimeOffset now, DateTimeOffset horizon,
                                 IReadOnlyList<(DateTimeOffset Ts, long Tokens)> entries);
    public bool Covers(DateTimeOffset since);              // since >= Horizon
    public TranscriptActivity Since(DateTimeOffset since); // ]since ; Now] — borne basse EXCLUSIVE
}
public sealed record TranscriptActivity(bool HasActivity, long Tokens, DateTimeOffset? LastActivityAt);
```

```csharp
// src/Chronos/Services/RateLimitHeaderUsageProvider.cs — l. 140, la constante DONT on dérive la limite
public static readonly TimeSpan CadenceNominale = TimeSpan.FromSeconds(300);
```

```csharp
// src/Chronos/Services/CompositeUsageProvider.cs — l. 39-45, la recomposition PIÉGEUSE (à NE PAS changer)
return new UsageSnapshot
{
    FiveHour = fiveHour,
    SevenDay = sevenDay,
    SourceCapturedAt = primaryUsed ? (p.SourceCapturedAt ?? f.SourceCapturedAt)
                                   : (f.SourceCapturedAt ?? p.SourceCapturedAt),
};
```

```csharp
// tests/Chronos.Tests/NormalisationUniqueTests.cs — MOTIF de garde à recopier (chemin injecté par MSBuild)
private static string CheminSources()
    => typeof(NormalisationUniqueTests).Assembly
           .GetCustomAttributes<AssemblyMetadataAttribute>()
           .FirstOrDefault(a => a.Key == "CheminSourcesChronos")?.Value
       ?? "";
```

**Motifs déjà INTERDITS par `NormalisationUniqueTests` dans `Services/` et `Models/`** — ne les écris
nulle part, **même en commentaire**, dans les fichiers créés ou modifiés par ce plan :
`/ 100`, `* 100`, `FromUnixTimeSeconds`, `FromUnixTimeMilliseconds`, `DateTimeOffset.TryParse`.

**Hors périmètre :** `LastExactUsageProvider`, `App.xaml.cs`, tout ViewModel, tout XAML, `PercentFormatter`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Le modèle à quatre états — trois champs additifs, zéro site de construction retouché</name>

  <read_first>
    - src/Chronos/Models/WindowState.cs (INTÉGRALEMENT)
    - src/Chronos/Models/UsageSnapshot.cs (INTÉGRALEMENT)
    - src/Chronos/Models/SourceReliability.cs et src/Chronos/Models/StatutServeur.cs (style de la maison)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (lignes 60-90 — pour COMPRENDRE pourquoi Estimated est réaffecté et non remplacé)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md (§ Pattern 1, § Pitfall 5, § Pitfall 6)
  </read_first>

  <files>
src/Chronos/Models/ProvenanceReleve.cs
src/Chronos/Models/WindowState.cs
src/Chronos/Models/UsageSnapshot.cs
  </files>

  <action>
**A. Créer `src/Chronos/Models/ProvenanceReleve.cs`** :
```csharp
namespace Chronos.Models;

/// <summary>
/// Provenance FINE du chiffre affiché (phase 19). Complète <see cref="SourceReliability"/>, qui répond
/// « peut-on s'y fier », par « d'où vient-il et qu'a-t-on vérifié ». <c>null</c> = la doctrine n'a pas
/// statué : fenêtre indisponible, ou WindowState fabriqué hors doctrine (provider, magasin, test).
///
/// La phase 20 (EXA-03) bindera CES trois valeurs pour la distinction visuelle. Aucune géométrie de
/// cadran n'en dépend en phase 19 : le contrat est gravé avant que le dessin ne bouge.
/// </summary>
public enum ProvenanceReleve
{
    /// <summary>Âge sous la limite : exact, affiché tel quel, aucune E/S consultée pour le certifier.</summary>
    Frais,

    /// <summary>Plus vieux que la limite, MAIS on a prouvé qu'aucune réponse assistant n'est survenue
    /// depuis sa capture : l'utilisation n'a objectivement pas bougé, il est ENCORE exact (DEL-03).</summary>
    EncoreValide,

    /// <summary>De l'activité est survenue depuis la capture : le chiffre devient une BORNE INFÉRIEURE
    /// (« au moins X »), pas une valeur. Jamais gonflé, jamais confondu avec un exact (DEL-04).</summary>
    PlancherAvecActivite,
}
```

**B. `src/Chronos/Models/WindowState.cs`** — ajouter **après** `Depassement` (l. 30) deux propriétés,
`init` et nullables, **sans `required`** (les 32 sites `new WindowState` du dépôt compilent sans retouche) :
```csharp
    /// <summary>Phase 19 — provenance fine décidée par la doctrine. null = non statué. Porté par
    /// WindowState et NON par UsageSnapshot, pour la raison mécanique déjà établie pour StatutServeur :
    /// Best() rend l'INSTANCE gagnante PAR RÉFÉRENCE, donc ce champ traverse gratuitement les trois
    /// composites imbriqués, là où un champ de snapshot serait détruit par la recomposition.</summary>
    public ProvenanceReleve? Provenance { get; init; }

    /// <summary>Phase 19 (DEL-04) — tokens de réponses assistant observés depuis CapturedAt. BORNE
    /// INFÉRIEURE et matière première BRUTE, jamais exprimée en points de pourcentage (EXA-04) : les
    /// limites Anthropic pondèrent par modèle, et les transcripts ignorent l'app de bureau et Cowork,
    /// qui consomment pourtant le même pool. Champ DISTINCT d'EstimatedTokens, qui portait la somme de
    /// l'estimation absolue supprimée en phase 16 — réutiliser ce dernier rattacherait au nouveau chiffre
    /// la sémantique que le milestone a tuée.</summary>
    public long? TokensDepuisReleve { get; init; }
```

**C. `src/Chronos/Models/UsageSnapshot.cs`** — ajouter après `SourceCapturedAt` (l. 11) :
```csharp
    /// <summary>EXA-05 — un relevé exact a-t-il déjà été obtenu au moins une fois ? <c>null</c> = non
    /// évalué (tout snapshot produit SOUS la couche de doctrine, dont <see cref="Empty"/>). <c>false</c> =
    /// jamais : l'overlay doit inviter à se connecter et ne JAMAIS afficher de pourcentage.
    ///
    /// bool? et non bool : avec bool, Empty vaudrait false, c'est-à-dire « jamais eu d'exact » — une
    /// AFFIRMATION produite par une absence, exactement ce que le projet proscrit.
    ///
    /// Sur UsageSnapshot et non WindowState : c'est un fait de COMPTE, pas de fenêtre. Sans danger malgré
    /// la recomposition par « new » de CompositeUsageProvider.GetAsync, parce que ce champ est posé par la
    /// couche de doctrine, qui est AU-DESSUS du composite : aucun composite ne le reverra jamais.</summary>
    public bool? UnExactADejaEteObtenu { get; init; }
```
et **amender** le commentaire de `SourceCapturedAt` (l. 9-10) pour dire qu'il est **legacy** :
```csharp
    /// <summary>LEGACY (phase 19). Horodatage de capture au niveau SNAPSHOT. La vérité de la doctrine est
    /// désormais PAR FENÊTRE (WindowState.CapturedAt + Provenance) ; ce champ et le IsStale du ViewModel
    /// qui en dérive ne sont bindés nulle part et sont conservés tels quels pour ne rien casser. La
    /// phase 20 les retire en bindant la provenance par fenêtre. Ne pas le croire canonique.</summary>
```
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -c Release -v q --nologo</automated>
    <automated>dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**, et le nombre de tests est **identique** à
      celui de fin du plan 19-01 (ce sont trois ajouts purement additifs).
    - `src/Chronos/Models/ProvenanceReleve.cs` existe et contient les trois membres
      `Frais`, `EncoreValide`, `PlancherAvecActivite`.
    - `grep -c "required" src/Chronos/Models/WindowState.cs` == **2** (`Kind` et `Reliability`
      uniquement — aucun nouveau champ obligatoire).
    - `grep -c "public ProvenanceReleve? Provenance { get; init; }" src/Chronos/Models/WindowState.cs` == 1
      et `grep -c "public long? TokensDepuisReleve { get; init; }" src/Chronos/Models/WindowState.cs` == 1.
    - `grep -c "public bool? UnExactADejaEteObtenu { get; init; }" src/Chronos/Models/UsageSnapshot.cs` == 1.
    - `src/Chronos/Models/SourceReliability.cs` est **inchangé** : `git diff --name-only` ne le liste pas.
    - Aucun des motifs interdits par `NormalisationUniqueTests` n'apparaît dans les trois fichiers touchés :
      `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~NormalisationUniqueTests"` vert.
  </acceptance_criteria>

  <done>Le modèle distingue quatre états, aucun site de construction n'a été retouché, `SourceReliability`
  n'a pas bougé, et la suite est verte au même compte qu'avant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DoctrineFraicheur — les quatre branches, pures, déterministes et falsifiables une par une</name>

  <read_first>
    - src/Chronos/Models/WindowState.cs et src/Chronos/Models/ProvenanceReleve.cs (après la tâche 1)
    - src/Chronos/Services/ITranscriptActivitySource.cs (INTÉGRALEMENT — Covers, Since, Horizon, Now)
    - src/Chronos/Services/RateLimitHeaderUsageProvider.cs (ligne 140 : CadenceNominale)
    - src/Chronos/Services/WeeklyRecalibration.cs (INTÉGRALEMENT — le modèle de classe PURE de la maison)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (lignes 62-90 — pourquoi la démotion DOIT effacer Utilization)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md (§ Pattern 2, 3, 4, 5 ; § Code Examples 1)
  </read_first>

  <files>
src/Chronos/Services/DoctrineFraicheur.cs
tests/Chronos.Tests/DoctrineFraicheurTests.cs
  </files>

  <behavior>
    - **Branche 1 (EXA-02)** : candidat dont l'âge ≤ `LimiteAge` ⇒ `Exact` + `Frais`, `Utilization`
      conservée, **et le journal n'est même pas regardé** (passer `journal: null` donne le même résultat
      que passer un journal riche).
    - **Branche 2 (DEL-03)** : candidat de 3 h, journal couvrant sa capture et **sans** activité depuis ⇒
      `Exact` + `EncoreValide`, `Utilization` **inchangée**, `TokensDepuisReleve == 0`.
    - **Branche 3 (DEL-04)** : même candidat, journal **avec** activité depuis ⇒ `Estimated` +
      `PlancherAvecActivite`, `Utilization` **rigoureusement inchangée**, `TokensDepuisReleve` = somme brute.
    - **Branche 4a** : journal absent, ou `Covers(CapturedAt) == false` ⇒ `Unavailable`,
      `Utilization == null`, `Provenance == null`.
    - **Branche 4b (EXA-02)** : candidat `Exact` **sans** `CapturedAt` ⇒ `Unavailable`,
      `Utilization == null` — un exact sans horodatage est incertifiable, jamais « frais ».
    - **Candidat** : une réponse vivante `Exact` **porteuse d'une utilization ET d'un horodatage** prime
      toujours sur le magasin ; sinon c'est le relevé mémorisé ; sinon rien.
    - **Statut serveur et dépassement survivent** à une substitution par le magasin : ils décrivent des
      faits du tick courant, pas la fraîcheur d'un pourcentage.
    - `ABesoinDuJournal` est **cohérent** avec `Statuer` : quand il rend `false`, `Statuer` produit le même
      résultat avec ou sans journal.
  </behavior>

  <action>
**A. Créer `src/Chronos/Services/DoctrineFraicheur.cs`** — texte de référence, à reproduire :
```csharp
using Chronos.Models;

namespace Chronos.Services;

/// <summary>
/// LA DOCTRINE (EXA-02, EXA-04, DEL-03, DEL-04). Classe PURE : aucune E/S, aucune horloge propre, aucun
/// type WPF. Elle reçoit tout ce dont elle a besoin et rend un <see cref="WindowState"/> — donc
/// entièrement testable en [Fact], et chaque branche est supprimable pour prouver qu'un test tombe.
///
/// Ordre des règles, et POURQUOI cet ordre :
///   1. âge sous la limite                        -> Exact / Frais                (laissez-passer, 0 E/S)
///   2. journal couvre T et AUCUNE activité       -> Exact / EncoreValide         (DEL-03, ré-habilitation)
///   3. journal couvre T et activité              -> Estimated / Plancher         (DEL-04, borne inférieure)
///   4. sinon                                     -> Unavailable                  (jamais de chiffre inventé)
///
/// La limite d'âge n'est PAS un couperet : c'est un laissez-passer. Sans consommation, l'utilisation ne
/// change pas ; « zéro réponse assistant depuis T » implique donc « l'utilisation à now égale celle de T ».
/// Ce n'est pas une approximation, c'est une déduction — et déclarer périmé un chiffre qu'on peut PROUVER
/// juste reviendrait à cacher une information vraie. Le plafond absolu existe déjà et il est gratuit :
/// l'horizon de huit jours des transcripts. C'est par Covers(T), et non par la limite d'âge, que meurt
/// le relevé figé depuis deux mois qui a motivé ce milestone.
/// </summary>
public static class DoctrineFraicheur
{
    /// <summary>
    /// Âge maximal d'un relevé exact affiché SANS vérification d'activité (EXA-02). DÉRIVÉE de la cadence
    /// de la sonde, jamais recopiée : sous cette limite, le cache légitime de la sonde n'est jamais démoté,
    /// donc le chemin nominal ne paie JAMAIS la passe de transcripts (mesurée à 2,7-3,2 s et 536 Mo sur la
    /// machine cible). La limite d'âge est donc AUSSI un régulateur de débit, pas seulement un jugement.
    ///
    /// NON RÉGLABLE, et c'est délibéré : EXA-02 est une propriété de sûreté, pas une préférence. Un réglage
    /// permettrait de la porter à deux mois, c'est-à-dire de recréer le défaut que ce milestone éradique —
    /// le précédent est documenté dans ce dépôt (une source de plafond passée en « Manual » a gelé à vie
    /// un chiffre faux).
    /// </summary>
    public static readonly TimeSpan LimiteAge =
        RateLimitHeaderUsageProvider.CadenceNominale + TimeSpan.FromSeconds(60);

    /// <summary>Âge d'une fenêtre, ou null si elle ne dit pas QUAND elle a été capturée. Un exact sans
    /// horodatage est INCERTIFIABLE : on ne peut ni mesurer son âge, ni poser la question « activité
    /// depuis T ? ». Rendre null, et non TimeSpan.Zero, est le cœur de la correction.</summary>
    public static TimeSpan? Age(WindowState w, DateTimeOffset now)
        => w.CapturedAt is { } t ? now - t : null;

    /// <summary>
    /// Le journal d'activité est-il NÉCESSAIRE pour statuer sur cette fenêtre ? Permet à l'appelant de
    /// n'engager la passe disque que lorsqu'elle change quelque chose. Contrat : quand ceci rend false,
    /// <see cref="Statuer"/> produit le MÊME résultat avec ou sans journal — un test l'impose.
    /// </summary>
    public static bool ABesoinDuJournal(WindowState vivante, WindowState? memorisee, DateTimeOffset now)
    {
        var candidat = Candidat(vivante, memorisee);
        return candidat is not null && Age(candidat, now) is { } age && age > LimiteAge;
    }

    /// <summary>
    /// Statue sur UNE fenêtre. <paramref name="vivante"/> est ce que la chaîne vient de produire,
    /// <paramref name="memorisee"/> ce que le magasin détient (déjà validé : son reset est futur), et
    /// <paramref name="journal"/> le journal d'activité (null = source indisponible ou non consultée).
    /// </summary>
    public static WindowState Statuer(WindowState vivante, WindowState? memorisee,
                                      TranscriptActivityLog? journal, DateTimeOffset now)
    {
        var candidat = Candidat(vivante, memorisee);
        if (candidat is null) return Indisponible(vivante);

        // EXA-02, première moitié : pas d'horodatage, pas de certification possible. C'est ici que tombe
        // un relevé marqué exact dont personne ne sait quand il a été pris.
        if (Age(candidat, now) is not { } age) return Indisponible(vivante);

        // 1. FRAIS — laissez-passer : aucune E/S, aucune question posée aux transcripts.
        if (age <= LimiteAge) return Qualifier(candidat, vivante, SourceReliability.Exact,
                                               ProvenanceReleve.Frais, tokens: null);

        var t = candidat.CapturedAt!.Value;

        // 4a. Sans journal, ou journal qui NE COUVRE PAS T : rien ne peut être prouvé. On ne borne pas un
        // delta qu'on ne sait pas borner (le filtre mtime de huit jours sous-évaluerait en silence) — on
        // se déclare indisponible plutôt que d'inventer.
        if (journal is null || !journal.Covers(t)) return Indisponible(vivante);

        var activite = journal.Since(t);

        // 2. ENCORE EXACT (DEL-03) — aucune réponse assistant depuis T : l'utilisation n'a PAS bougé.
        if (!activite.HasActivity) return Qualifier(candidat, vivante, SourceReliability.Exact,
                                                    ProvenanceReleve.EncoreValide, tokens: 0L);

        // 3. PLANCHER (DEL-04) — il y a eu de l'activité : le chiffre devient une BORNE INFÉRIEURE.
        // Utilization INCHANGÉE : on ne l'augmente d'aucun delta (EXA-04). C'est la QUALIFICATION du
        // chiffre qui change, jamais sa valeur.
        return Qualifier(candidat, vivante, SourceReliability.Estimated,
                         ProvenanceReleve.PlancherAvecActivite, activite.Tokens);
    }

    // Exact vivant CERTIFIABLE -> lui. Sinon le magasin. Sinon rien. Un exact vivant sans utilization ou
    // sans horodatage ne vaut pas mieux que le magasin : c'est là que le relevé figé perd sa préséance.
    private static WindowState? Candidat(WindowState vivante, WindowState? memorisee)
        => vivante.Reliability == SourceReliability.Exact
           && vivante.Utilization is not null && vivante.CapturedAt is not null
            ? vivante
            : memorisee;

    // Le statut serveur et le dépassement décrivent le TICK COURANT (ce que le serveur vient de déclarer),
    // pas la fraîcheur d'un pourcentage : ils survivent donc à une substitution par le magasin, qui ne les
    // persiste pas. Sans ce report, HDR-03/HDR-04 disparaîtraient dès que le magasin prend la main.
    private static WindowState Qualifier(WindowState candidat, WindowState vivante,
                                         SourceReliability fiabilite, ProvenanceReleve provenance,
                                         long? tokens)
        => candidat with
        {
            Reliability = fiabilite,
            Provenance = provenance,
            TokensDepuisReleve = tokens,
            StatutServeur = candidat.StatutServeur ?? vivante.StatutServeur,
            Depassement = candidat.Depassement ?? vivante.Depassement,
        };

    // Démotion : on EFFACE le chiffre. WindowGaugeViewModel.Apply affecte Utilization SANS consulter
    // Reliability : une fenêtre indisponible qui conserverait 0,10 peindrait encore l'arc à dix pour cent
    // et le thème lui donnerait sa couleur. On CONSERVE en revanche l'instance vivante — donc son reset,
    // sa géométrie temporelle, son statut serveur et son dépassement : un reset connu reste un fait,
    // indépendant de l'âge du pourcentage qui l'accompagnait.
    private static WindowState Indisponible(WindowState vivante)
        => vivante with
        {
            Reliability = SourceReliability.Unavailable,
            Utilization = null,
            EstimatedTokens = null,
            TokensDepuisReleve = null,
            Provenance = null,
        };
}
```

**B. Créer `tests/Chronos.Tests/DoctrineFraicheurTests.cs`** — au minimum **onze** tests. Helpers :
```csharp
    private static readonly DateTimeOffset Now = new(2026, 9, 12, 12, 0, 0, TimeSpan.Zero);

    private static WindowState Exact(double util, DateTimeOffset captured, DateTimeOffset? resets = null)
        => new() { Kind = WindowKind.FiveHour, Reliability = SourceReliability.Exact,
                   Utilization = util, CapturedAt = captured, ResetsAt = resets ?? Now.AddHours(3) };

    private static TranscriptActivityLog Journal(params (DateTimeOffset Ts, long Tokens)[] e)
        => new(Now, Now - TimeSpan.FromDays(8), e);
```
Les tests, nommés en français et commentés par leur POURQUOI :
1. `Releve_sous_la_limite_est_exact_frais` — `Exact(0.42, Now.AddMinutes(-1))`, `journal: null` ⇒
   `Exact` + `Frais` + `0.42` + `TokensDepuisReleve == null`.
2. `La_branche_fraiche_ne_consulte_PAS_le_journal` — même entrée, comparer `Statuer(..., null, Now)` et
   `Statuer(..., Journal((Now.AddMinutes(-2), 9_000)), Now)` : **résultat identique**.
3. `Trois_heures_sans_aucune_activite_reste_EXACT_et_non_perime` — mémorisé `Exact(0.42, Now.AddHours(-3))`,
   vivante `WindowState.Unavailable(WindowKind.FiveHour)`, journal portant une entrée **antérieure** à T
   (`(Now.AddHours(-4), 5_000)`) ⇒ `Exact` + `EncoreValide` + `0.42` + `TokensDepuisReleve == 0`.
4. `Activite_depuis_le_releve_donne_un_plancher_marque` — journal `((Now.AddHours(-2), 120_000),
   (Now.AddMinutes(-5), 880_000))`, T = `Now.AddHours(-3)` ⇒ `Estimated` + `PlancherAvecActivite` +
   `TokensDepuisReleve == 1_000_000`.
5. `Le_plancher_ne_gonfle_JAMAIS_l_utilization` (EXA-04) — même cas que 4, mais l'assertion est
   `Assert.Equal(0.42, w.Utilization)` avec un commentaire citant la mesure : 643 649 933 tokens sur 5 h
   contre l'ancien plafond de 230 000 000 rendraient 280 %, donc aucun taux points-par-token n'est honnête.
6. `Releve_anterieur_a_l_horizon_des_transcripts_est_indisponible_et_non_sous_evalue` — T = `Now.AddDays(-60)`,
   journal vide ; asserter d'abord `Assert.False(journal.Covers(t))` (la prémisse, rendue explicite) puis
   `Unavailable` + `Utilization == null` + `Provenance == null`. **C'est ici que meurt le « 10 % ».**
7. `Un_exact_vivant_sans_CapturedAt_est_incertifiable_et_ne_passe_pas_pour_frais` — vivante `Exact`,
   `Utilization = 0.10`, `CapturedAt = null`, aucun mémorisé ⇒ `Unavailable` + `Utilization == null`.
8. `Sans_journal_un_releve_trop_vieux_est_indisponible` — T = `Now.AddHours(-3)`, `journal: null` ⇒
   `Unavailable`.
9. `Un_exact_vivant_certifiable_prime_toujours_sur_le_magasin` — vivante `Exact(0.20, Now)`, mémorisé
   `Exact(0.80, Now.AddHours(-2))` ⇒ `0.20`.
10. `La_demotion_conserve_le_reset_et_le_statut_serveur_mais_efface_le_pourcentage` — vivante `Exact`,
    `Utilization = 0.10`, `CapturedAt = null`, `ResetsAt = Now.AddHours(2)`,
    `StatutServeur = StatutServeur.Autorise` ⇒ `Utilization == null` **et** `ResetsAt == Now.AddHours(2)`
    **et** `StatutServeur == StatutServeur.Autorise`.
11. `ABesoinDuJournal_est_coherent_avec_Statuer` — pour une matrice d'au moins quatre cas (frais / vieux /
    sans horodatage / aucun candidat) : si `ABesoinDuJournal(...) == false`, alors
    `Statuer(vivante, memorisee, journalRiche, Now) == Statuer(vivante, memorisee, null, Now)`
    (égalité de record). C'est la garantie que la paresse du plan 19-03 ne change **rien** au verdict.
12. `Le_statut_serveur_du_tick_survit_a_une_substitution_par_le_magasin` — vivante `Unavailable` portant
    `StatutServeur = StatutServeur.Autorise`, mémorisé `Exact(0.42, Now.AddMinutes(-1))` ⇒ `Frais` **et**
    `StatutServeur == StatutServeur.Autorise`.

**C. FALSIFIABILITÉ — obligatoire, et à consigner.** Pour **chacune des quatre branches**, muter
réellement `DoctrineFraicheur.cs` (par exemple : supprimer le retour de la branche 1 ; inverser
`!activite.HasActivity` ; remplacer `!journal.Covers(t)` par `false` ; retirer `Utilization = null` de
`Indisponible`), constater l'échec, **noter le nom du ou des tests qui tombent**, puis **révoquer** la
mutation. Le SUMMARY doit porter les quatre mutations, leur effet, et la preuve de révocation
(`git diff -- src/Chronos/Services/DoctrineFraicheur.cs` vide en fin de tâche).
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~DoctrineFraicheurTests"</automated>
    <automated>dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~DoctrineFraicheurTests"` :
      **au moins 12 tests, 0 échec**.
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**.
    - `grep -c "System.Windows" src/Chronos/Services/DoctrineFraicheur.cs` == **0**.
    - `grep -c "DateTimeOffset.UtcNow" src/Chronos/Services/DoctrineFraicheur.cs` == **0** : la classe n'a
      pas d'horloge propre, `now` est toujours un paramètre.
    - `grep -c "CadenceNominale" src/Chronos/Services/DoctrineFraicheur.cs` == 1 : la limite est DÉRIVÉE,
      jamais une valeur littérale en minutes.
    - `grep -cE "(ReadAsync|File\.|Directory\.)" src/Chronos/Services/DoctrineFraicheur.cs` == **0** :
      aucune E/S dans la doctrine.
    - Le SUMMARY nomme les **quatre** mutations jouées, le test tombé pour chacune, et atteste la
      révocation (`git diff` vide sur le fichier).
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests|FullyQualifiedName~NormalisationUniqueTests"` vert.
  </acceptance_criteria>

  <done>Les quatre branches existent, sont pures, déterministes, et chacune a été **prouvée nécessaire**
  par une mutation réelle qui fait tomber un test nommé, puis révoquée.</done>
</task>

<task type="auto">
  <name>Task 3: Deux gardes de non-retour — le piège de la recomposition et l'interdiction EXA-04</name>

  <read_first>
    - src/Chronos/Services/CompositeUsageProvider.cs (INTÉGRALEMENT, 63 lignes)
    - src/Chronos/Models/UsageSnapshot.cs (après la tâche 1)
    - tests/Chronos.Tests/NormalisationUniqueTests.cs (INTÉGRALEMENT — le motif de garde par balayage de texte source, à imiter)
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (la garde de non-retour des plafonds, phase 16)
    - tests/Chronos.Tests/CompositeUsageProviderTests.cs (INTÉGRALEMENT — les 6 tests de caractérisation de la phase 18, dont les COMMENTAIRES sont à amender)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md (§ Pitfall 5, § Pitfall 8, § Code Examples 4)
  </read_first>

  <files>
src/Chronos/Services/CompositeUsageProvider.cs
tests/Chronos.Tests/GardesDoctrineTests.cs
tests/Chronos.Tests/CompositeUsageProviderTests.cs
  </files>

  <action>
**A. `src/Chronos/Services/CompositeUsageProvider.cs` — modification MINIMALE et justifiée.** Ni horloge,
ni magasin, ni transcripts n'y entrent : la doctrine vit au-dessus. Deux choses seulement.

1. Compléter l'en-tête XML-doc de la classe (l. 5-10) par :
```csharp
/// <para><b>Phase 19 — où vit la doctrine, et pourquoi pas ici.</b> Ce composite ne juge PAS la
/// fraîcheur : il classe par fiabilité et rien d'autre, délibérément. La doctrine (DoctrineFraicheur)
/// vit dans la couche de tête, LastExactUsageProvider, pour trois raisons mécaniques : Best() ne reçoit
/// que deux WindowState nus (ni horloge, ni magasin, ni journal d'activité) ; la chaîne réelle est faite
/// de TROIS composites imbriqués, donc une doctrine placée ici s'exécuterait trois fois par tick et
/// statuerait sur une information partielle ; et la seule source à ancienneté non bornée est le repli
/// le plus interne, qui ne peut gagner que lorsque tout ce qui est au-dessus est indisponible —
/// appliquer la porte d'âge AU-DESSUS du composite y est donc strictement équivalent.</para>
/// <para><b>Corollaire à ne jamais enfreindre :</b> Best() ne doit JAMAIS arbitrer par récence entre deux
/// sources exactes. La porte d'âge est BINAIRE (certifiable ou non), pas un classement. Un arbitrage par
/// récence ferait gagner l'endpoint OAuth contre le cache légitime de la sonde, et emporterait avec lui
/// le statut serveur et le dépassement, dont la sonde est l'unique porteuse.</para>
```
2. Compléter le commentaire de la recomposition (l. 32-35) par une phrase **nommant explicitement** le
   champ posé au-dessus — c'est cette mention que la garde B.1 exige :
```csharp
        // Phase 19 : UnExactADejaEteObtenu n'est PAS recomposé ici, et c'est correct — ce champ est posé
        // par la couche de doctrine, qui est AU-DESSUS de tous les composites. Aucun composite ne le voit
        // jamais. Toute AUTRE propriété ajoutée à UsageSnapshot devra en revanche être nommée dans ce
        // « new », sans quoi elle sera détruite en silence à chaque passage : une garde l'impose.
```
**Ne pas** remplacer le `new UsageSnapshot` par un `with` : `p with { … }` ferait hériter silencieusement
du **primaire** tout champ futur, c'est-à-dire produire une valeur fausse là où le `new` produit un `null`.

**B. Créer `tests/Chronos.Tests/GardesDoctrineTests.cs`** — trois tests.

1. `Toute_propriete_de_UsageSnapshot_est_nommee_dans_la_recomposition_du_composite` : lire
   `Path.Combine(CheminSources(), "Services", "CompositeUsageProvider.cs")`, et asserter que **toutes** les
   `typeof(UsageSnapshot).GetProperties().Select(p => p.Name)` apparaissent dans le texte. Le message
   d'échec doit nommer les propriétés manquantes et expliquer que le record est reconstruit par `new`, donc
   qu'un champ non nommé est détruit en silence. Recopier le `CheminSources()` de `NormalisationUniqueTests`
   (attribut `AssemblyMetadata("CheminSourcesChronos")`, jamais `Assembly.Location`).

2. `Aucun_rapport_entre_un_comptage_de_tokens_et_un_plafond_dans_Services_et_Models` (EXA-04) : balayer
   les `*.cs` de `Services/` et `Models/` avec
   `@"(Tokens?\w*\s*[/*]\s*\w*(Plafond|Budget|Limite|Capacite))|((Plafond|Budget)\w*\s*[/*]\s*\w*Tokens?)"`,
   `RegexOptions.IgnoreCase`, et asserter la liste vide, en nommant les fichiers fautifs.
   **PIÈGE À ÉVITER, impératif :** cette expression régulière matcherait un COMMENTAIRE qui écrirait les
   deux mots séparés d'une barre oblique. Dans **tout** fichier de `Services/` et `Models/`, y compris les
   commentaires que tu écris toi-même, formule toujours « un rapport entre un comptage de tokens et un
   plafond » — jamais les deux termes accolés par un opérateur. Ajoute cette consigne en commentaire du
   test lui-même, formulée de la même manière.
   Le POURQUOI à écrire dans la XML-doc : la phase 16 a déjà interdit aux transcripts d'implémenter
   `IUsageProvider` ; cette garde ferme l'autre voie, celle d'un taux points-par-token — qui n'est qu'un
   plafond auto-calibré, c'est-à-dire le calibrateur de plafonds supprimé en phase 16.
   **Falsifiabilité obligatoire** : introduire réellement la mutation (par exemple, dans
   `DoctrineFraicheur.cs`, une ligne `var taux = tokensDepuisReleve / plafondTokens;` — nom de variable au
   choix, pourvu qu'il déclenche le motif), constater l'échec **nommant le fichier**, puis révoquer.
   Consigner dans le SUMMARY, et vérifier `git diff` vide.

3. `Le_repli_le_plus_interne_est_bien_celui_a_anciennete_non_bornee` : preuve de comportement, dans l'esprit
   de la garde de position de la phase 18. Construire
   `new CompositeUsageProvider(primary: A, fallback: new CompositeUsageProvider(primary: B, fallback: C))`
   avec trois `FakeUsageProvider` produisant des `Exact` **aux chiffres différents** (0,11 / 0,22 / 0,33),
   et asserter que `0.11` sort. Le POURQUOI : l'équivalence « porte d'âge au-dessus du composite ≡ porte
   d'âge dans Best() » est une propriété de **l'ordre actuel** de la chaîne ; un réordonnancement futur la
   casserait en silence. Ce test transforme cette fragilité en signal.

**C. `tests/Chronos.Tests/CompositeUsageProviderTests.cs` — amender les COMMENTAIRES, ne rien supprimer.**
Les 14 tests restent verts et les 6 de caractérisation de la phase 18 gardent leur sens (ils prouvent que
le statut serveur et le dépassement traversent la chaîne par référence, donc que HDR-03/HDR-04 sont
vivants). Mais ils décrivent désormais un composite qui **ignore délibérément** l'âge : ajouter en tête de
fichier une note de deux à quatre lignes disant où la doctrine est passée et pourquoi le composite n'arbitre
pas la fraîcheur. **Aucune assertion n'est modifiée, aucun test n'est supprimé.**
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~GardesDoctrineTests|FullyQualifiedName~CompositeUsageProviderTests"</automated>
    <automated>dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~CompositeUsageProviderTests"` :
      **exactement 14 tests**, 0 échec (aucune suppression, aucun ajout).
    - `tests/Chronos.Tests/GardesDoctrineTests.cs` existe et contient les trois noms de tests ci-dessus.
    - `grep -c "UnExactADejaEteObtenu" src/Chronos/Services/CompositeUsageProvider.cs` ≥ 1 — c'est ce qui
      fait passer la garde B.1 tout en documentant pourquoi ce champ n'est pas recomposé.
    - Aucune injection n'est entrée dans le composite — le constructeur et les champs sont INTACTS.
      Critères portant sur le CODE et non sur le texte (les commentaires imposés ci-dessus nomment
      légitimement `DoctrineFraicheur` et `LastExactUsageProvider`, il ne faut donc surtout pas compter
      ces noms à zéro) :
      `grep -c "public CompositeUsageProvider(IUsageProvider primary, IUsageProvider fallback)" src/Chronos/Services/CompositeUsageProvider.cs` == **1**,
      et `grep -cE "^\s*private readonly" src/Chronos/Services/CompositeUsageProvider.cs` == **2**.
    - `grep -c "new UsageSnapshot" src/Chronos/Services/CompositeUsageProvider.cs` == **1** et
      `grep -c "p with" src/Chronos/Services/CompositeUsageProvider.cs` == **0**.
    - Le chemin des sources vient de MSBuild et de rien d'autre :
      `grep -c "CheminSourcesChronos" tests/Chronos.Tests/GardesDoctrineTests.cs` >= **1** et
      `grep -cE "typeof\(.*\)\.Assembly\.Location" tests/Chronos.Tests/GardesDoctrineTests.cs` == **0**.
      *(Critère portant sur l'APPEL, pas sur le mot : si tu veux expliquer en commentaire pourquoi cette
      propriété est proscrite en publication mono-fichier, décris-la comme « le chemin de fichier de
      l'assembly » plutôt que de l'invoquer.)*
    - Le SUMMARY consigne la mutation EXA-04 jouée, le message d'échec obtenu (avec le nom du fichier
      dénoncé), et la révocation.
  </acceptance_criteria>

  <done>Le composite documente où vit la doctrine et nomme le champ posé au-dessus de lui ; deux gardes de
  non-retour existent, l'une prouvée falsifiable par mutation réelle ; les 14 tests de caractérisation sont
  intacts et leurs commentaires disent la vérité du moment.</done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -v q --nologo` — **0 échec**. Le total augmente d'environ 16 tests (12 de
   doctrine + 3 de gardes) ; toute variation est justifiée nominativement dans le SUMMARY.
2. Gardes permanentes vertes :
   `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests|FullyQualifiedName~NormalisationUniqueTests|FullyQualifiedName~CompositionRootTests"`.
3. Garde de position de la sonde intacte :
   `dotnet test Chronos.sln -v q --nologo --filter "La_sonde_d_en_tetes_est_le_PRIMAIRE"`.
4. Aucun type WPF dans les nouveaux fichiers :
   `grep -rc "System.Windows" src/Chronos/Services/DoctrineFraicheur.cs src/Chronos/Models/ProvenanceReleve.cs` == 0 partout.
5. `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`.
6. `grep -rl "RefreshAsync" src/Chronos --include=*.cs | wc -l` == **2**.
</verification>

<success_criteria>
- Quatre états modélisés sans une seule rupture de compilation ni un seul `required` de plus.
- `DoctrineFraicheur` est pure, dérive sa limite d'âge de la cadence de la sonde, et ses quatre branches
  sont chacune prouvées nécessaires par mutation réelle puis révocation.
- `Utilization` n'est gonflée dans aucune branche ; aucun pourcentage n'est inventé nulle part.
- Un relevé hors de l'horizon des transcripts est déclaré indisponible, jamais sous-évalué.
- Une démotion efface le pourcentage mais conserve le reset, le statut serveur et le dépassement.
- Deux gardes de non-retour existent ; `CompositeUsageProvider` n'a reçu aucune injection.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/19-nouvelle-doctrine-du-composite/19-02-SUMMARY.md`.

Y consigner impérativement :
- les **quatre mutations** de branche jouées, le test tombé pour chacune, et la révocation ;
- la **mutation EXA-04** jouée sur la garde de balayage, le fichier dénoncé, et la révocation ;
- la décision de conserver `SourceCapturedAt` et `IsStale` en LEGACY plutôt que de les supprimer (coût :
  4 tests à supprimer pour aucune exigence de cette phase ; la phase 20 les retirera en bindant
  `Provenance`) ;
- le statut des requirements : **EXA-02, EXA-04, DEL-03 et DEL-04 restent Pending** — la doctrine existe
  mais **rien ne l'appelle** à ce commit. C'est intentionnel : le câblage est le plan 19-03.
</output>
