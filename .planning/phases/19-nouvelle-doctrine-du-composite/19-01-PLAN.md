---
phase: 19-nouvelle-doctrine-du-composite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/ChronosOAuthUsageProvider.cs
  - src/Chronos/Services/ClaudeOAuthUsageProvider.cs
  - src/Chronos/Services/ClaudeUsageObjectProvider.cs
  - src/Chronos/Services/LastExactStore.cs
  - src/Chronos/Services/SourceActiviteMemoisee.cs
  - tests/Chronos.Tests/TestData/usage-ancien.json
  - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs
  - tests/Chronos.Tests/LastExactStoreTests.cs
  - tests/Chronos.Tests/Fakes/FakeTranscriptActivitySource.cs
  - tests/Chronos.Tests/SourceActiviteMemoiseeTests.cs
autonomous: true
requirements: [EXA-02, EXA-05]

must_haves:
  truths:
    - "Chacun des quatre providers exacts horodate ses fenêtres : la limite d'âge d'EXA-02 devient applicable PAR FENÊTRE, alors qu'elle était jusqu'ici inapplicable pour trois d'entre eux."
    - "L'âge d'un relevé lu dans usage.json est celui du FICHIER, jamais celui de la lecture — un fichier écrit il y a deux mois porte deux mois d'âge."
    - "Le magasin refuse désormais de persister une fenêtre exacte sans horodatage de capture : plus aucun captured_at null incertifiable n'est écrit."
    - "Le magasin sait distinguer « aucun relevé exact n'a JAMAIS été obtenu » de « un relevé existait mais sa fenêtre a roulé » — les deux rendent null à Load, et seul le premier justifie d'inviter à se connecter (EXA-05)."
    - "Une source d'activité mémoïsée rend le même journal pendant sa durée de validité : deux interrogations rapprochées ne paient qu'UNE passe disque."
    - "L'inventaire RÉEL des tests cassés par l'ajout de CapturedAt est établi par exécution de la suite complète, pas par hypothèse."
  artifacts:
    - path: "src/Chronos/Services/ClaudeUsageObjectProvider.cs"
      provides: "CapturedAt par fenêtre = horodatage du FICHIER (capturedAt epoch ms), null si absent"
      contains: "CapturedAt"
    - path: "src/Chronos/Services/ChronosOAuthUsageProvider.cs"
      provides: "CapturedAt par fenêtre = instant de la réponse HTTP"
      contains: "CapturedAt"
    - path: "src/Chronos/Services/ClaudeOAuthUsageProvider.cs"
      provides: "CapturedAt par fenêtre = instant de la réponse HTTP"
      contains: "CapturedAt"
    - path: "src/Chronos/Services/LastExactStore.cs"
      provides: "UnExactADejaEteObtenu() + exigence de CapturedAt à la persistance"
      contains: "UnExactADejaEteObtenu"
    - path: "src/Chronos/Services/SourceActiviteMemoisee.cs"
      provides: "Décorateur ITranscriptActivitySource à durée de validité, non levant"
      contains: "ValiditeParDefaut"
    - path: "tests/Chronos.Tests/TestData/usage-ancien.json"
      provides: "Fixture reproduisant le fichier RÉEL de production (10 %, écrit le 2026-07-10)"
      contains: "capturedAt"
    - path: "tests/Chronos.Tests/Fakes/FakeTranscriptActivitySource.cs"
      provides: "Journal programmable + compteur de passes + mode panne"
      contains: "Lectures"
  key_links:
    - from: "src/Chronos/Services/ClaudeUsageObjectProvider.cs"
      to: "WindowState.CapturedAt"
      via: "capturedAt du fichier passé en paramètre à ReadWindow — JAMAIS _clock.UtcNow"
      pattern: "CapturedAt = capturedAt"
    - from: "src/Chronos/Services/LastExactStore.cs"
      to: "Entry.CapturedAt"
      via: "Convertir exige Utilization ET ResetsAt ET CapturedAt"
      pattern: "CapturedAt is \\{ \\} c"
    - from: "src/Chronos/Services/SourceActiviteMemoisee.cs"
      to: "ITranscriptActivitySource.ReadAsync"
      via: "délégation derrière un SemaphoreSlim + double vérification de validité"
      pattern: "SemaphoreSlim"
---

<objective>
Rendre la limite d'âge d'EXA-02 **applicable**, et donner au magasin les deux facultés dont la doctrine
aura besoin. Aucune doctrine n'est encore écrite dans ce plan : on pose les **faits** qu'elle consommera.

Purpose: la recherche a établi le défaut de forme qui rendait EXA-02 impossible :
`WindowState.CapturedAt` n'est renseigné que par **un** des quatre providers exacts (la sonde
d'en-têtes, l. 431). `ChronosOAuthUsageProvider`, `ClaudeOAuthUsageProvider` et
`ClaudeUsageObjectProvider` ne posent que `UsageSnapshot.SourceCapturedAt`, au niveau **snapshot**.
Conséquence : une limite d'âge par fenêtre n'a rien à mesurer, et le magasin persiste des
`captured_at: null` définitivement incertifiables.

**Le piège le plus grave de toute la phase est dans ce plan.** Le réflexe, pour
`ClaudeUsageObjectProvider`, est d'écrire l'instant de **lecture**. Or le fichier a été écrit par le pont
statusLine **il y a deux mois** (constat machine : `{"five_hour":{"used_percentage":10,"resets_at":9},
"capturedAt":1783666519131}`). Avec l'instant de lecture, ce relevé paraîtrait avoir **zéro seconde**
d'âge, franchirait la porte de fraîcheur, et « 10 % » reviendrait — cette fois avec l'autorité d'une
limite d'âge prétendument appliquée. **Pire que le bug d'origine.** Un test le verrouille par une
fixture.

Output: quatre providers exacts qui horodatent, un magasin qui refuse l'incertifiable et qui sait dire
« je n'ai JAMAIS rien vu », un décorateur de mémoïsation prêt à être câblé (plan 19-03), le faux manquant
sans lequel rien de la doctrine n'est testable, et **l'inventaire réel** des tests impactés.

Ce plan ne crée **aucun** type de doctrine, ne touche **ni** `LastExactUsageProvider`, **ni**
`CompositeUsageProvider`, **ni** `App.xaml.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-CONTEXT.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md
@.planning/phases/19-nouvelle-doctrine-du-composite/19-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contrats EXACTS à consommer ou modifier. L'exécuteur n'a aucune exploration à faire. -->

```csharp
// src/Chronos/Models/WindowState.cs — le champ existe DÉJÀ (phase 16), il n'est simplement pas rempli.
public DateTimeOffset? CapturedAt { get; init; }
```

```csharp
// src/Chronos/Services/ClaudeUsageObjectProvider.cs — ÉTAT ACTUEL
// l. 48-49 : capturedAt du FICHIER, déjà parsé (epoch MILLISECONDES) puis posé en SourceCapturedAt seulement
DateTimeOffset? capturedAt = root.TryGetProperty("capturedAt", out var c) && c.TryGetInt64(out var ms)
    ? UsageNormalization.InstantDepuisEpochMillisecondes(ms) : null;
// l. 51-52 : les deux appels
var five = ReadWindow(root, "five_hour", WindowKind.FiveHour, TimeSpan.FromHours(5));
var week = ReadWindow(root, "seven_day", WindowKind.SevenDay, TimeSpan.FromDays(7));
// l. 73 : signature actuelle
private WindowState ReadWindow(JsonElement root, string name, WindowKind kind, TimeSpan len)
```

```csharp
// src/Chronos/Services/ChronosOAuthUsageProvider.cs — ÉTAT ACTUEL
// l. 127-131 : le SEUL site d'appel, « now » est en portée (posé l. 63)
FiveHour = Read(root, "five_hour", WindowKind.FiveHour, TimeSpan.FromHours(5)),
SevenDay = Read(root, "seven_day", WindowKind.SevenDay, TimeSpan.FromDays(7)),
SourceCapturedAt = now,
// l. 168 : signature actuelle
private WindowState Read(JsonElement root, string name, WindowKind kind, TimeSpan len)
```

```csharp
// src/Chronos/Services/ClaudeOAuthUsageProvider.cs — ÉTAT ACTUEL
// l. 98-102 : le SEUL site d'appel, « now » est en portée (posé l. 56)
// l. 125 : signature actuelle, identique à celle ci-dessus
private WindowState Read(JsonElement root, string name, WindowKind kind, TimeSpan len)
```

```csharp
// src/Chronos/Services/LastExactStore.cs — ÉTAT ACTUEL
public const int SchemaVersion = 1;                 // NE PAS BUMPER (3 fixtures épinglent 1 / 999)
private Payload? LireBrut();                        // non-null SSI fichier présent + lisible + version == 1
public LastExactWindows? Load(DateTimeOffset now);  // null si rien d'exploitable OU toutes fenêtres roulées

// l. 135-138 — la garde de persistance ACTUELLE
private static Entry? Convertir(WindowState w)
    => w.Reliability == SourceReliability.Exact && w.Utilization is { } u && w.ResetsAt is { } r
        ? new Entry { Utilization = u, ResetsAt = r, CapturedAt = w.CapturedAt }
        : null;

private sealed record Payload  { int Version; Entry? FiveHour; Entry? SevenDay; }
private sealed record Entry    { double Utilization; DateTimeOffset? ResetsAt; DateTimeOffset? CapturedAt; }
```

```csharp
// src/Chronos/Services/ITranscriptActivitySource.cs (phase 16) — à DÉCORER, jamais à modifier
public interface ITranscriptActivitySource
{
    Task<TranscriptActivityLog> ReadAsync(CancellationToken ct = default);
}
public sealed class TranscriptActivityLog
{
    public DateTimeOffset Now { get; }        // instant de la passe disque
    public DateTimeOffset Horizon { get; }    // borne basse de connaissance (filtre mtime 8 j)
    public TranscriptActivityLog(DateTimeOffset now, DateTimeOffset horizon,
                                 IReadOnlyList<(DateTimeOffset Ts, long Tokens)> entries);
    public bool Covers(DateTimeOffset since);                 // since >= Horizon
    public TranscriptActivity Since(DateTimeOffset since);    // ]since ; Now]
}
public sealed record TranscriptActivity(bool HasActivity, long Tokens, DateTimeOffset? LastActivityAt);
```

```csharp
// src/Chronos/Services/IClock.cs + tests/Chronos.Tests/Fakes/FakeClock.cs
public interface IClock { DateTimeOffset UtcNow { get; } }
internal sealed class FakeClock : IClock { public DateTimeOffset UtcNow { get; set; } public FakeClock(DateTimeOffset now); }
```

**Harnais de test déjà en place, à réutiliser :**
- `tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs` — fixtures sous `TestData/`, chemin résolu par
  `TestDataPath(string file, [CallerFilePath] string thisFile = "")`, provider construit par
  `ProviderFor(string usageFile, IClock clock)`. Constante existante `CapturedAtMs = 1751976000000L`.
- `tests/Chronos.Tests/LastExactStoreTests.cs` — dossier temp par test (`IDisposable`), helper
  `Exact(WindowKind k, double util, DateTimeOffset resets, DateTimeOffset captured)` : **les 15 appels
  passent déjà un `captured` non-null**, donc exiger `CapturedAt` à la persistance ne casse aucun des 20 tests.

**Hors périmètre de ce plan :** `DoctrineFraicheur`, `ProvenanceReleve`, `LastExactUsageProvider`,
`CompositeUsageProvider`, `App.xaml.cs`, tout ViewModel, tout XAML.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CapturedAt par fenêtre dans les trois providers exacts qui l'omettent, + inventaire réel</name>

  <read_first>
    - src/Chronos/Services/ClaudeUsageObjectProvider.cs (INTÉGRALEMENT, 92 lignes)
    - src/Chronos/Services/ChronosOAuthUsageProvider.cs (lignes 40-190)
    - src/Chronos/Services/ClaudeOAuthUsageProvider.cs (lignes 30-150)
    - src/Chronos/Services/RateLimitHeaderUsageProvider.cs (lignes 420-440 — le SEUL provider qui fait déjà bien, à imiter)
    - src/Chronos/Models/WindowState.cs (INTÉGRALEMENT)
    - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs (INTÉGRALEMENT)
    - tests/Chronos.Tests/TestData/usage-valid.json
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md (§ Pitfall 1, § Pitfall 2, § Open Question 4)
  </read_first>

  <files>
src/Chronos/Services/ClaudeUsageObjectProvider.cs
src/Chronos/Services/ChronosOAuthUsageProvider.cs
src/Chronos/Services/ClaudeOAuthUsageProvider.cs
tests/Chronos.Tests/TestData/usage-ancien.json
tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs
  </files>

  <behavior>
    - Un `usage.json` dont `capturedAt` vaut **2026-07-10T08:55:00Z** (epoch ms `1783673700000`), lu à
      **2026-09-12T12:00:00Z**, produit `FiveHour.CapturedAt == 2026-07-10T08:55:00Z` — et
      **surtout pas** l'instant de lecture.
    - Un `usage.json` **sans** clé `capturedAt` produit `CapturedAt == null` (incertifiable assumé), jamais
      l'instant de lecture : c'est `TestData/usage-partial.json` ou `usage-corrupt.json` selon le cas réel
      constaté à la lecture du fichier.
    - Les deux providers OAuth posent `CapturedAt == now` sur chaque fenêtre lue, `now` étant l'instant de
      la réponse HTTP déjà utilisé pour `SourceCapturedAt`.
    - Une fenêtre absente reste `WindowState.Unavailable(kind)` : aucun horodatage sur une fenêtre qui
      n'existe pas.
  </behavior>

  <action>
**A. `src/Chronos/Services/ClaudeUsageObjectProvider.cs` — LE piège de la phase.**

1. Changer la signature l. 73 en :
```csharp
private WindowState ReadWindow(JsonElement root, string name, WindowKind kind, TimeSpan len,
                               DateTimeOffset? capturedAt)
```
2. Dans le `new WindowState` (l. 83-90), ajouter **exactement** :
```csharp
            CapturedAt = capturedAt,
```
3. Adapter les deux sites d'appel (l. 51-52) en passant la variable `capturedAt` déjà calculée l. 48-49 :
```csharp
            var five = ReadWindow(root, "five_hour", WindowKind.FiveHour, TimeSpan.FromHours(5), capturedAt);
            var week = ReadWindow(root, "seven_day", WindowKind.SevenDay, TimeSpan.FromDays(7), capturedAt);
```
4. Ajouter au-dessus de `ReadWindow` un commentaire XML-doc dont la teneur est imposée, **formulé
   exactement comme ci-dessous** (contrainte de critère : ne reproduis PAS l'expression fautive, décris-la) :
```csharp
    /// <summary>
    /// EXA-02 — l'horodatage porté par la fenêtre est celui du FICHIER (clé capturedAt, epoch
    /// millisecondes), et JAMAIS l'instant de lecture. Le pont statusLine peut avoir écrit ce fichier
    /// il y a deux mois : lui attribuer l'heure courante le ferait paraître vieux de zéro seconde,
    /// il franchirait la porte de fraîcheur de la doctrine, et le « 10 % » figé de cette machine
    /// reviendrait — avec l'autorité d'une limite d'âge prétendument appliquée. Absent du fichier
    /// ⇒ null (incertifiable), ce qui est strictement plus honnête qu'une date inventée.
    /// </summary>
```

**B. `src/Chronos/Services/ChronosOAuthUsageProvider.cs`** — signature l. 168 :
```csharp
private WindowState Read(JsonElement root, string name, WindowKind kind, TimeSpan len,
                         DateTimeOffset capturedAt)
```
ajouter `CapturedAt = capturedAt,` dans le `new WindowState` (l. 178-185) et passer `now` aux deux appels
l. 129-130. Commentaire : `// EXA-02 — instant de la RÉPONSE HTTP : ici la lecture EST la capture. Le cache
sert l'instance d'origine, donc son horodatage d'origine vieillit honnêtement.`

**C. `src/Chronos/Services/ClaudeOAuthUsageProvider.cs`** — même transformation, signature l. 125, ajout
dans le `new WindowState` l. 136-143, appels l. 100-101 avec `now`. Même commentaire.

**D. Nouvelle fixture `tests/Chronos.Tests/TestData/usage-ancien.json`** — copie de forme du fichier RÉEL
de production, avec des epochs explicites et vérifiés :
```json
{ "five_hour": { "used_percentage": 10, "resets_at": 1783684500 }, "capturedAt": 1783673700000 }
```
(`1783673700` = 2026-07-10T08:55:00Z ; `1783684500` = 2026-07-10T11:55:00Z ; les deux sont au-dessus du
plancher d'epoch 2020-01-01 de `UsageNormalization`, donc non neutralisés.)

**E. `tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs`** — ajouter **deux** tests :
```csharp
    /// <summary>
    /// EXA-02 — LE test qui interdit la résurrection du bug des « 10 % ». La fixture reproduit la forme
    /// du fichier RÉEL de cette machine (10 %, écrit le 2026-07-10) ; on le lit deux mois plus tard.
    /// Si quelqu'un horodate la LECTURE, la seconde assertion tombe.
    /// </summary>
    [Fact]
    public async Task Un_usage_json_vieux_de_deux_mois_porte_l_age_du_FICHIER_et_non_celui_de_la_lecture()
    {
        var ecritLe = new DateTimeOffset(2026, 7, 10, 8, 55, 0, TimeSpan.Zero);
        var luLe    = new DateTimeOffset(2026, 9, 12, 12, 0, 0, TimeSpan.Zero);

        var snap = await ProviderFor(TestDataPath("usage-ancien.json"), new FakeClock(luLe)).GetAsync();

        Assert.Equal(ecritLe, snap.FiveHour.CapturedAt);
        Assert.NotEqual(luLe, snap.FiveHour.CapturedAt);
        Assert.Equal(ecritLe, snap.SourceCapturedAt);   // cohérence des deux niveaux
    }

    /// <summary>
    /// EXA-02, l'autre moitié : sans clé capturedAt, l'âge est INCONNU — et inconnu ne vaut JAMAIS
    /// « zéro seconde ». null est ici la réponse honnête ; la doctrine en tirera « incertifiable ».
    /// </summary>
    [Fact]
    public async Task Un_usage_json_sans_capturedAt_laisse_l_age_INCONNU()
    {
        var luLe = new DateTimeOffset(2026, 9, 12, 12, 0, 0, TimeSpan.Zero);

        var snap = await ProviderFor(TestDataPath("usage-partial.json"), new FakeClock(luLe)).GetAsync();

        Assert.Null(snap.FiveHour.CapturedAt);
        Assert.Null(snap.SevenDay.CapturedAt);
    }
```
Si `usage-partial.json` contient en réalité un `capturedAt`, crée `TestData/usage-sans-capture.json`
(`{ "five_hour": { "used_percentage": 10, "resets_at": 1783684500 } }`) et cible-la — **lis la fixture
avant d'écrire le test**.

**F. INVENTAIRE RÉEL — la raison d'être de la position de cette tâche.** Lancer la **suite complète** :
```
dotnet test Chronos.sln -v q --nologo
```
La recherche n'a pas lu exhaustivement les ~120 tests des quatre providers ; un
`Assert.Equal(attenduWindowState, obtenu)` comparant des **records entiers** casserait du fait de l'ajout
de `CapturedAt`. **Consigner dans le SUMMARY la liste NOMINATIVE des tests qui ont échoué** (même si elle
est vide — c'est alors une mesure, pas une hypothèse), puis les corriger en ajustant l'**attendu** (jamais
en retirant l'horodatage du provider). 2 min 15 de mesure contre une journée d'hypothèses.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ClaudeUsageObjectProviderTests|FullyQualifiedName~ChronosOAuthUsageProviderTests|FullyQualifiedName~ClaudeOAuthUsageProviderTests|FullyQualifiedName~RateLimitHeaderUsageProviderTests"</automated>
    <automated>dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**. Toute variation du nombre de tests est
      justifiée nominativement dans le SUMMARY.
    - Les deux tests nommés ci-dessus existent et passent :
      `grep -c "Un_usage_json_vieux_de_deux_mois_porte_l_age_du_FICHIER_et_non_celui_de_la_lecture" tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs` ≥ 1.
    - `grep -cE "CapturedAt\s*=\s*_clock" src/Chronos/Services/ClaudeUsageObjectProvider.cs` == **0**
      (le commentaire imposé en A.4 décrit le piège sans écrire cette expression — ne l'écris nulle part
      dans ce fichier, pas même en commentaire).
    - `grep -c "CapturedAt" src/Chronos/Services/ChronosOAuthUsageProvider.cs` ≥ 1 et
      `grep -c "CapturedAt" src/Chronos/Services/ClaudeOAuthUsageProvider.cs` ≥ 1.
    - `tests/Chronos.Tests/TestData/usage-ancien.json` existe et contient `1783673700000`.
    - Le SUMMARY contient la liste nominative (éventuellement vide) des tests cassés par l'ajout.
    - Contrôle coffre inchangé : `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`.
  </acceptance_criteria>

  <done>Les quatre providers exacts horodatent leurs fenêtres ; l'âge d'un relevé de `usage.json` est
  celui du fichier, prouvé par fixture ; la suite complète est verte et l'inventaire des impacts est
  une mesure écrite, pas une supposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Le magasin refuse l'incertifiable et sait dire « je n'ai JAMAIS rien vu » (EXA-05)</name>

  <read_first>
    - src/Chronos/Services/LastExactStore.cs (INTÉGRALEMENT, 178 lignes)
    - tests/Chronos.Tests/LastExactStoreTests.cs (INTÉGRALEMENT — en particulier le helper `Exact` l. 36-44 et les 3 fixtures de version)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md (§ Pattern 6, § Runtime State Inventory)
  </read_first>

  <files>
src/Chronos/Services/LastExactStore.cs
tests/Chronos.Tests/LastExactStoreTests.cs
  </files>

  <behavior>
    - `UnExactADejaEteObtenu()` rend **false** quand le fichier est absent, illisible, de version inconnue,
      ou présent mais sans aucune des deux fenêtres.
    - Il rend **true** quand le fichier porte `version: 1` et au moins une fenêtre — **même si**
      `Load(now)` rend `null` parce que toutes les fenêtres ont roulé. C'est exactement la distinction
      qu'EXA-05 exige : « jamais rien vu » appelle une invitation à se connecter, « vu mais la fenêtre a
      tourné » non.
    - Une fenêtre exacte **sans** `CapturedAt` n'est plus persistée : `Save` la traite comme non
      mémorisable, au même titre qu'une fenêtre sans `ResetsAt`.
    - `SchemaVersion` reste **1**. Aucun champ persisté n'est ajouté ni renommé.
  </behavior>

  <action>
**A. `src/Chronos/Services/LastExactStore.cs` — durcir `Convertir` (l. 135-138).** Remplacer par :
```csharp
    /// <summary>
    /// Extrait l'entrée persistable d'une fenêtre. Trois conditions, pas deux : exacte, porteuse d'un
    /// pourcentage, d'un instant de reset ET d'un instant de CAPTURE. Le troisième est l'ajout de la
    /// phase 19 : un relevé sans instant de capture est définitivement incertifiable — ni son âge ni la
    /// question « de l'activité depuis ? » ne peuvent lui être posés. Le persister reviendrait à graver
    /// sur disque un chiffre que la doctrine devra rejeter à chaque lecture.
    /// </summary>
    private static Entry? Convertir(WindowState w)
        => w.Reliability == SourceReliability.Exact && w.Utilization is { } u
           && w.ResetsAt is { } r && w.CapturedAt is { } c
            ? new Entry { Utilization = u, ResetsAt = r, CapturedAt = c }
            : null;
```

**B. Ajouter la méthode publique** juste après `Load` (après l. 106) :
```csharp
    /// <summary>
    /// EXA-05 — un relevé exact a-t-il DÉJÀ été obtenu et mémorisé, au moins une fois ? Distinct de
    /// <see cref="Load"/>, qui rend null aussi bien pour « jamais rien vu » que pour « quelque chose a
    /// été vu, mais sa fenêtre a roulé depuis ». Les deux cas ne se traitent PAS pareil : le premier
    /// appelle une invitation à se connecter, le second non — l'utilisateur EST connecté, sa fenêtre a
    /// simplement tourné. Lui crier « connecte-toi » serait une fausse alerte.
    ///
    /// Aucune évolution de schéma : <see cref="SchemaVersion"/> reste 1 (trois fixtures épinglent
    /// "version":1 et "version":999). C'est une LECTURE de plus, pas un champ de plus.
    /// Tolérant par construction : s'appuie sur LireBrut, qui ne lève jamais.
    /// </summary>
    public bool UnExactADejaEteObtenu()
        => LireBrut() is { } p && (p.FiveHour is not null || p.SevenDay is not null);
```

**C. `tests/Chronos.Tests/LastExactStoreTests.cs` — ajouter quatre tests.** Le helper `Exact` existant
exige un `captured` non-null : ne le modifie pas, les 20 tests existants restent verts.
```csharp
    // --- EXA-05 : « jamais rien vu » n'est PAS « vu, mais la fenêtre a roulé » ---

    [Fact]
    public void Aucun_fichier_signifie_qu_aucun_exact_n_a_JAMAIS_ete_obtenu()
    {
        Assert.False(new LastExactStore(_fichier).UnExactADejaEteObtenu());
    }

    [Fact]
    public void Un_releve_dont_la_fenetre_a_roule_prouve_tout_de_meme_qu_un_exact_a_ete_obtenu()
    {
        var magasin = new LastExactStore(_fichier);
        magasin.Save(Snap(
            Exact(WindowKind.FiveHour, 0.42, Now.AddMinutes(-1), Now.AddHours(-6)),   // reset DÉJÀ passé
            WindowState.Unavailable(WindowKind.SevenDay)));

        Assert.Null(magasin.Load(Now));                  // plus rien de servable…
        Assert.True(magasin.UnExactADejaEteObtenu());    // …mais l'utilisateur N'EST PAS « jamais connecté »
    }

    [Fact]
    public void Un_fichier_illisible_ne_permet_d_affirmer_aucun_exact_obtenu()
    {
        File.WriteAllText(_fichier, "{ pas du JSON ]");
        Assert.False(new LastExactStore(_fichier).UnExactADejaEteObtenu());
    }

    [Fact]
    public void Une_fenetre_exacte_SANS_instant_de_capture_n_est_pas_persistee()
    {
        var sansCapture = new WindowState
        {
            Kind = WindowKind.FiveHour, Reliability = SourceReliability.Exact,
            Utilization = 0.42, ResetsAt = Now.AddHours(3), CapturedAt = null,
        };

        new LastExactStore(_fichier).Save(Snap(sansCapture, WindowState.Unavailable(WindowKind.SevenDay)));

        Assert.False(File.Exists(_fichier));   // rien de certifiable ⇒ pas même un fichier
    }
```
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~LastExactStoreTests"</automated>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~LastExactUsageProviderTests|FullyQualifiedName~CompositionRootTests"</automated>
  </verify>

  <acceptance_criteria>
    - Les deux commandes ci-dessus : **0 échec**.
    - `grep -c "public const int SchemaVersion = 1;" src/Chronos/Services/LastExactStore.cs` == **1**
      (aucun bump : les 3 fixtures de version restent valides).
    - `grep -c "UnExactADejaEteObtenu" src/Chronos/Services/LastExactStore.cs` ≥ 1.
    - `grep -cE "w\.CapturedAt is \{ \} c" src/Chronos/Services/LastExactStore.cs` == **1**
      (la troisième condition de persistance est bien en place).
    - Aucun `[JsonPropertyName]` ajouté ni modifié : `git diff -- src/Chronos/Services/LastExactStore.cs`
      ne contient aucune ligne touchant `JsonPropertyName`.
    - Aucun test écrit sous le vrai `%APPDATA%\Chronos\` : tous les chemins de ce fichier de tests
      restent sous `Path.GetTempPath()`.
  </acceptance_criteria>

  <done>`UnExactADejaEteObtenu()` distingue les deux silences du magasin, prouvé par un test où `Load`
  rend null et où le bit rend pourtant true ; plus aucun `captured_at: null` n'est écrit ; schéma v1
  intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Le faux manquant et le mémoïseur — sans quoi la doctrine coûterait 536 Mo par minute</name>

  <read_first>
    - src/Chronos/Services/ITranscriptActivitySource.cs (INTÉGRALEMENT, 85 lignes)
    - src/Chronos/Services/TranscriptActivityProvider.cs (la signature de ReadAsync et la constante d'horizon)
    - src/Chronos/Services/ChronosTokenAuthority.cs (le motif SemaphoreSlim + double vérification, déjà éprouvé phase 17)
    - src/Chronos/Services/IClock.cs et tests/Chronos.Tests/Fakes/FakeClock.cs
    - tests/Chronos.Tests/Fakes/FakeEtatServeur.cs (style des faux du projet)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-RESEARCH.md (§ Pattern 7, § Pitfall 3, § Open Question 5)
  </read_first>

  <files>
src/Chronos/Services/SourceActiviteMemoisee.cs
tests/Chronos.Tests/Fakes/FakeTranscriptActivitySource.cs
tests/Chronos.Tests/SourceActiviteMemoiseeTests.cs
  </files>

  <behavior>
    - Deux `ReadAsync` consécutifs dans la durée de validité ⇒ **une seule** délégation à l'inner, et le
      **même** objet journal rendu.
    - Un `ReadAsync` après expiration de la validité ⇒ **une deuxième** délégation.
    - La validité se mesure sur `journal.Now` (l'instant de la passe disque), pas sur l'instant d'appel :
      un journal né vieux expire vite, ce qui est honnête.
    - Une panne de l'inner (exception) **ne lève pas** : le dernier journal connu est rendu s'il existe,
      sinon l'exception remonte (l'appelant, lui, la convertira en branche « indisponible »).
    - Aucun type WPF : `ServicesLayerPurityTests` reste vert.
  </behavior>

  <action>
**A. `tests/Chronos.Tests/Fakes/FakeTranscriptActivitySource.cs`** (n'existe pas — rien de la doctrine
n'est testable sans lui) :
```csharp
using Chronos.Services;

namespace Chronos.Tests;

/// <summary>
/// Faux <see cref="ITranscriptActivitySource"/> : journal PROGRAMMABLE + compteur de passes + mode panne.
/// Le compteur est l'instrument de la contrainte de coût de la phase 19 : une passe RÉELLE coûte 2,7 à
/// 3,2 s et lit 536 Mo (mesuré le 2026-09-12 sur la machine cible : 474 fichiers, 155 171 lignes).
/// Compter les passes est donc une assertion de CONCEPTION, pas une micro-optimisation.
/// </summary>
internal sealed class FakeTranscriptActivitySource : ITranscriptActivitySource
{
    private int _lectures;

    /// <summary>Nombre de délégations réellement effectuées (thread-safe).</summary>
    public int Lectures => Volatile.Read(ref _lectures);

    /// <summary>Journal rendu. null = la source ÉCHOUE (disque absent, E/S) — la doctrine doit dégrader.</summary>
    public TranscriptActivityLog? Journal;

    public Task<TranscriptActivityLog> ReadAsync(CancellationToken ct = default)
    {
        Interlocked.Increment(ref _lectures);
        return Journal is null
            ? Task.FromException<TranscriptActivityLog>(new System.IO.IOException("source indisponible"))
            : Task.FromResult(Journal);
    }
}
```

**B. `src/Chronos/Services/SourceActiviteMemoisee.cs`** :
```csharp
namespace Chronos.Services;

/// <summary>
/// Décorateur d'<see cref="ITranscriptActivitySource"/> à DURÉE DE VALIDITÉ. Raison d'être mesurée : une
/// passe réelle coûte 2,7 à 3,2 s et lit 536 Mo sur la machine cible ; au tick de 60 s, une lecture par
/// tick serait 536 Mo par minute d'E/S permanente sur un overlay. Le journal rendu par la phase 16 est
/// PUR et interrogeable N fois : le réutiliser est donc gratuit et sans perte.
///
/// La durée de validité est AUSSI la tolérance de certification : un journal dont le <c>Now</c> a 45 s ne
/// dit rien de ces 45 s, donc un « aucune activité » qu'il fonde n'est valable qu'à 45 s près. C'est
/// pourquoi elle est courte et pourquoi elle ne doit PAS être allongée par confort de performance.
///
/// Verrou : le motif éprouvé de ChronosTokenAuthority (SemaphoreSlim(1,1) + double vérification). Deux
/// passes concurrentes liraient 1 072 Mo simultanément ; le coût du verrou est nul en comparaison.
/// Type NEUTRE : aucun type WPF, la garde de pureté Services/Models reste verte.
/// </summary>
public sealed class SourceActiviteMemoisee : ITranscriptActivitySource
{
    /// <summary>Durée de validité par défaut. Alignée sur l'intervalle de rafraîchissement nominal (60 s) :
    /// un tick ne paie jamais deux passes, deux ticks n'en partagent jamais une.</summary>
    public static readonly TimeSpan ValiditeParDefaut = TimeSpan.FromSeconds(60);

    private readonly ITranscriptActivitySource _inner;
    private readonly IClock _horloge;
    private readonly TimeSpan _validite;
    private readonly SemaphoreSlim _verrou = new(1, 1);
    private TranscriptActivityLog? _journal;

    public SourceActiviteMemoisee(ITranscriptActivitySource inner, IClock horloge, TimeSpan? validite = null)
    {
        _inner = inner;
        _horloge = horloge;
        _validite = validite ?? ValiditeParDefaut;
    }

    public async Task<TranscriptActivityLog> ReadAsync(CancellationToken ct = default)
    {
        if (EncoreValide(_journal, _horloge.UtcNow) && _journal is { } rapide) return rapide;

        await _verrou.WaitAsync(ct).ConfigureAwait(false);
        try
        {
            // Double vérification : N demandeurs entrés ensemble ne produisent qu'UNE passe disque.
            if (EncoreValide(_journal, _horloge.UtcNow) && _journal is { } dejaFait) return dejaFait;

            try
            {
                _journal = await _inner.ReadAsync(ct).ConfigureAwait(false);
            }
            catch when (_journal is not null)
            {
                // Une panne de la source ne doit pas effacer ce qu'on savait : le dernier journal connu
                // reste servable, et sa propre péremption le retirera du jeu le moment venu.
            }

            // Ici _journal est forcément non-null : soit la passe vient de réussir, soit le filtre du
            // catch ci-dessus n'a laissé passer l'exception que parce qu'un journal connu existait.
            return _journal!;
        }
        finally { _verrou.Release(); }
    }

    // La validité se mesure sur l'instant de la PASSE DISQUE et non sur l'instant de mise en cache :
    // un journal né vieux doit expirer vite.
    private bool EncoreValide(TranscriptActivityLog? j, DateTimeOffset now)
        => j is not null && now - j.Now <= _validite;
}
```

**C. `tests/Chronos.Tests/SourceActiviteMemoiseeTests.cs`** — cinq tests au minimum :
1. `Deux_lectures_rapprochees_ne_paient_QU_UNE_passe_disque` : validité 60 s, deux `ReadAsync` à la même
   heure ⇒ `Assert.Equal(1, faux.Lectures)` et `Assert.Same(j1, j2)`.
2. `Une_lecture_apres_peremption_repaie_une_passe` : avancer `FakeClock.UtcNow` de 61 s ⇒
   `Assert.Equal(2, faux.Lectures)`.
3. `La_validite_se_mesure_sur_l_instant_de_la_passe_disque` : journal dont `Now` est déjà vieux de 120 s ⇒
   la deuxième lecture repaie immédiatement.
4. `Une_panne_de_la_source_rend_le_dernier_journal_connu` : première lecture OK, puis `faux.Journal = null`
   et horloge avancée ⇒ aucune exception, journal précédent rendu.
5. `Une_panne_des_la_premiere_lecture_remonte_a_l_appelant` :
   `await Assert.ThrowsAsync<IOException>(() => memo.ReadAsync())` — c'est l'appelant (plan 19-03) qui
   convertit cela en « indisponible », pas ce décorateur.

Construire les journaux avec
`new TranscriptActivityLog(now, now - TimeSpan.FromDays(8), Array.Empty<(DateTimeOffset, long)>())`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~SourceActiviteMemoiseeTests|FullyQualifiedName~ServicesLayerPurityTests|FullyQualifiedName~NormalisationUniqueTests"</automated>
    <automated>dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**, et le nombre total de tests a augmenté
      (nouveaux tests des tâches 1 à 3) — toute variation est justifiée nominativement dans le SUMMARY.
    - `src/Chronos/Services/SourceActiviteMemoisee.cs` et
      `tests/Chronos.Tests/Fakes/FakeTranscriptActivitySource.cs` existent.
    - `grep -c "System.Windows" src/Chronos/Services/SourceActiviteMemoisee.cs` == **0**.
    - `grep -c "SemaphoreSlim" src/Chronos/Services/SourceActiviteMemoisee.cs` ≥ 1.
    - `grep -c "Lectures" tests/Chronos.Tests/Fakes/FakeTranscriptActivitySource.cs` ≥ 1.
    - `grep -rc "SourceActiviteMemoisee" src/Chronos/App.xaml.cs` == **0** : l'enregistrement DI est
      **volontairement** le plan 19-03, en un seul commit avec le 4ᵉ argument du décorateur. Consigner ce
      choix dans le SUMMARY (précédent 17-03 : « un service que le host ne démarre jamais ne fait rien »).
    - Contrôle coffre inchangé : `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`.
  </acceptance_criteria>

  <done>Le faux existe et compte les passes ; le mémoïseur ne paie qu'une passe par durée de validité,
  mesure la validité sur l'instant de la passe disque, ne perd pas le journal connu en cas de panne, et
  reste un type neutre non encore câblé.</done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -v q --nologo` — **0 échec**. Le total passe de 652 à ~662 ; toute variation
   est justifiée nominativement dans le SUMMARY.
2. Gardes permanentes vertes :
   `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests|FullyQualifiedName~NormalisationUniqueTests|FullyQualifiedName~CompositionRootTests"`.
3. Garde de position de la sonde intacte :
   `dotnet test Chronos.sln -v q --nologo --filter "La_sonde_d_en_tetes_est_le_PRIMAIRE"`.
4. `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147` — avant ET après le plan.
5. Aucune requête réseau : `grep -rn "api.anthropic.com" tests/Chronos.Tests/SourceActiviteMemoiseeTests.cs`
   ne rend rien.
6. Les fichiers SOURCE contenant `RefreshAsync` restent exactement **2** :
   `grep -rl "RefreshAsync" src/Chronos --include=*.cs | wc -l` == 2.
</verification>

<success_criteria>
- `WindowState.CapturedAt` est posé par les **quatre** providers exacts, avec la bonne valeur pour chacun.
- Un test par **fixture** prouve qu'un `usage.json` de deux mois porte l'âge du fichier, et un autre qu'un
  fichier sans `capturedAt` laisse l'âge inconnu.
- `LastExactStore.UnExactADejaEteObtenu()` distingue les deux silences de `Load`, et `Convertir` exige
  désormais trois champs au lieu de deux.
- `SourceActiviteMemoisee` et `FakeTranscriptActivitySource` existent, testés, neutres, non câblés.
- Suite complète verte, inventaire nominatif des impacts écrit, coffre intact.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/19-nouvelle-doctrine-du-composite/19-01-SUMMARY.md`.

Y consigner impérativement :
- la **liste nominative** des tests cassés par l'ajout de `CapturedAt` (même vide) ;
- la décision de ne PAS câbler `SourceActiviteMemoisee` dans ce plan, et pourquoi ;
- le statut des requirements : **EXA-02 et EXA-05 restent Pending** — ce plan livre les faits qu'ils
  exigent, pas la doctrine qui les applique. Ils seront cochés par les plans 19-03 et 19-04.
</output>
