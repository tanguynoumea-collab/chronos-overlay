---
phase: 09-calibration-des-plafonds-surfa-age
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/Chronos/Services/IBudgetPrompt.cs
  - src/Chronos/Views/BudgetPrompt.cs
  - src/Chronos/Views/BudgetDialog.xaml
  - src/Chronos/Views/BudgetDialog.xaml.cs
  - src/Chronos/ViewModels/BudgetDialogViewModel.cs
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/Services/RefreshOrchestrator.cs
  - src/Chronos/Views/MainWindow.xaml
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs
  - tests/Chronos.Tests/MainViewModelTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
  - tests/Chronos.Tests/CadranBindingTests.cs
  - tests/Chronos.Tests/OverlayWindowConfigTests.cs
autonomous: true
requirements: [CAL-01, CAL-02]

must_haves:
  truths:
    - "Le menu contextuel offre « Calibrer les plafonds… » qui ouvre un dialogue à deux champs numériques pré-remplis (vide = null)"
    - "CalibrateBudgetsCommand persiste les deux plafonds via Load frais → with → Save (GAP-1) en marquant source=Manual pour les champs saisis (None si laissé vide)"
    - "Après enregistrement, l'orchestrateur est re-déclenché (RequestRefresh) → l'utilization/couleur des arcs est recalculée aussitôt avec les nouveaux plafonds"
    - "L'annulation du dialogue ne persiste rien"
    - "Le conteneur DI résout IBudgetPrompt et instancie BudgetAutoCalibrator (abonné à l'orchestrateur) au démarrage"
  artifacts:
    - path: "src/Chronos/Services/IBudgetPrompt.cs"
      provides: "Contrat neutre IBudgetPrompt + record BudgetSelection(long?, long?)"
      contains: "interface IBudgetPrompt"
    - path: "src/Chronos/ViewModels/BudgetDialogViewModel.cs"
      provides: "VM du dialogue plafonds (parse texte→long?, CloseRequested)"
      contains: "class BudgetDialogViewModel"
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "CalibrateBudgetsCommand (Load frais→Save→RequestRefresh)"
      contains: "CalibrateBudgets"
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "MenuItem « Calibrer les plafonds… » lié à CalibrateBudgetsCommand"
      contains: "CalibrateBudgetsCommand"
  key_links:
    - from: "src/Chronos/Views/MainWindow.xaml"
      to: "MainViewModel.CalibrateBudgetsCommand"
      via: "Command binding du MenuItem"
      pattern: "CalibrateBudgetsCommand"
    - from: "src/Chronos/ViewModels/MainViewModel.cs"
      to: "SettingsService.Load/Save + RefreshOrchestrator.RequestRefresh"
      via: "Load frais → with plafonds+source → Save → RequestRefresh"
      pattern: "RequestRefresh"
    - from: "src/Chronos/App.xaml.cs"
      to: "BudgetAutoCalibrator"
      via: "AddSingleton + résolution eager au démarrage (abonnement)"
      pattern: "BudgetAutoCalibrator"
---

<objective>
Livrer la CALIBRATION MANUELLE (CAL-01) : item de menu « Calibrer les plafonds… » → dialogue
minimal à deux champs (plafond 5 h, plafond hebdo), persistance immédiate (leçon GAP-1) et
application aussitôt via un re-déclenchement de l'orchestrateur. Câbler aussi en DI le
`BudgetAutoCalibrator` (09-01) pour activer la calibration auto (CAL-02) au runtime.

Purpose: donner à l'utilisateur le contrôle des plafonds et rendre l'auto-calibration vivante.
Output: contrat/dialogue de plafonds (miroir du pattern RecalibrationPrompt), commande VM,
`RefreshOrchestrator.RequestRefresh`, MenuItem, câblage DI, tests VM + composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-des-plafonds-surfa-age/09-CONTEXT.md
@.planning/phases/09-calibration-des-plafonds-surfa-age/09-01-SUMMARY.md

<interfaces>
<!-- Pattern EXISTANT à dupliquer/adapter pour les plafonds. Reproduire la structure, pas explorer. -->

Le prompt hebdo (à imiter) — src/Chronos/Services/IRecalibrationPrompt.cs :
```csharp
public interface IRecalibrationPrompt { DateTimeOffset? Ask(DateTimeOffset? current); }
```
src/Chronos/Views/RecalibrationPrompt.cs (implémentation WPF, hors garde de pureté) :
```csharp
public sealed class RecalibrationPrompt : IRecalibrationPrompt {
    public DateTimeOffset? Ask(DateTimeOffset? current) {
        var vm = new RecalibrationViewModel(current ?? DateTimeOffset.Now);
        var dlg = new RecalibrationDialog { DataContext = vm, Owner = Application.Current?.MainWindow };
        return dlg.ShowDialog() == true ? new DateTimeOffset(vm.SelectedDate) : null;
    }
}
```
src/Chronos/ViewModels/RecalibrationViewModel.cs : `event Action<bool>? CloseRequested;` + commandes
`[RelayCommand] Validate/Cancel` → `CloseRequested?.Invoke(bool)`.
src/Chronos/Views/RecalibrationDialog.xaml.cs : code-behind minimal, `DataContextChanged` → s'abonne à
`CloseRequested` → `DialogResult = accepte; Close();`.

MainViewModel (à étendre) — ctor et pattern GAP-1 existants — src/Chronos/ViewModels/MainViewModel.cs :
```csharp
public MainViewModel(RefreshOrchestrator orchestrator, IUiDispatcher ui, IClock clock,
    IWindowController controller, IAutostartService autostart, IRecalibrationPrompt prompt, SettingsService settings)
// Recalibrate() illustre GAP-1 : _settings = _settingsService.Load() with { ... }; _settingsService.Save(_settings);
```
Champs disponibles : `_settingsService`, `_settings`, `_clock`. L'orchestrateur n'est PAS mémorisé
aujourd'hui (seulement `orchestrator.SnapshotChanged += ...`) → il faudra le stocker.

Métadonnées settings (créées en 09-01) — src/Chronos/Services/ChronosSettings.cs :
`FiveHourTokenBudget`, `WeeklyTokenBudget` (long?), `FiveHourBudgetSource`/`WeeklyBudgetSource`
(enum `BudgetSource {None,Manual,Auto}`), `FiveHourBudgetCalibratedAt`/`WeeklyBudgetCalibratedAt`.

BudgetAutoCalibrator (créé en 09-01) — ctor :
`BudgetAutoCalibrator(RefreshOrchestrator orchestrator, IUsageProvider tokenSource, SettingsService settings, IClock clock)`.

DI existant — src/Chronos/App.xaml.cs : `IRecalibrationPrompt` mappé sur `RecalibrationPrompt` ;
`JsonlEstimationProvider` enregistré en singleton ; `RefreshOrchestrator` singleton + résolution eager
du `MainViewModel` avant `StartAsync` (pour forcer l'abonnement).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Contrat + dialogue WPF des plafonds (miroir de RecalibrationPrompt)</name>
  <files>src/Chronos/Services/IBudgetPrompt.cs, src/Chronos/ViewModels/BudgetDialogViewModel.cs, src/Chronos/Views/BudgetDialog.xaml, src/Chronos/Views/BudgetDialog.xaml.cs, src/Chronos/Views/BudgetPrompt.cs</files>
  <read_first>
    - src/Chronos/Services/IRecalibrationPrompt.cs
    - src/Chronos/Views/RecalibrationPrompt.cs
    - src/Chronos/Views/RecalibrationDialog.xaml
    - src/Chronos/Views/RecalibrationDialog.xaml.cs
    - src/Chronos/ViewModels/RecalibrationViewModel.cs
  </read_first>
  <action>
    1. `src/Chronos/Services/IBudgetPrompt.cs` — NEUTRE, `namespace Chronos.Services` :
       ```csharp
       /// <summary>Sélection de plafonds retournée par le dialogue (null par champ = pas de plafond).</summary>
       public sealed record BudgetSelection(long? FiveHour, long? Weekly);
       /// <summary>Contrat neutre pour demander les deux plafonds. Retourne null si l'utilisateur annule.</summary>
       public interface IBudgetPrompt { BudgetSelection? Ask(long? currentFiveHour, long? currentWeekly); }
       ```

    2. `src/Chronos/ViewModels/BudgetDialogViewModel.cs` — `sealed partial`, ObservableObject,
       `namespace Chronos.ViewModels`. Deux `[ObservableProperty] string _fiveHourText / _weeklyText`
       pré-remplis depuis les valeurs courantes (null → chaîne vide). `event Action<bool>? CloseRequested;`.
       Commandes `[RelayCommand] Validate()` → `CloseRequested?.Invoke(true)` ; `Cancel()` → `Invoke(false)`.
       Exposer un parseur pur `public long? ParsedFiveHour => Parse(FiveHourText);` (idem hebdo) où
       `Parse` : chaîne vide/espaces → null ; sinon retirer espaces et séparateurs de milliers puis
       `long.TryParse` (invariant) → valeur si > 0, sinon null. Ctor `(long? five, long? weekly)`.

    3. `src/Chronos/Views/BudgetDialog.xaml` — Window ToolWindow sombre, calquée sur RecalibrationDialog
       (même ResourceDictionary DesignTokens, `WindowStartupLocation="CenterOwner"`, fond #16151B) :
       titre « Calibrer les plafonds », un texte d'aide court « Laissez vide pour ne pas colorer l'arc »,
       deux `TextBox` labellisés (« Plafond 5 h (tokens) », « Plafond hebdo (tokens) ») liés à
       `FiveHourText`/`WeeklyText` (Mode=TwoWay, UpdateSourceTrigger=PropertyChanged), boutons
       Annuler (IsCancel) / Valider (IsDefault) liés à Cancel/Validate.

    4. `src/Chronos/Views/BudgetDialog.xaml.cs` — code-behind minimal identique au pattern :
       `DataContextChanged` → si `BudgetDialogViewModel vm` alors `vm.CloseRequested += a => { DialogResult = a; Close(); }`.

    5. `src/Chronos/Views/BudgetPrompt.cs` — `sealed class BudgetPrompt : IBudgetPrompt` (namespace
       Chronos.Views, hors garde de pureté) :
       ```csharp
       public BudgetSelection? Ask(long? currentFiveHour, long? currentWeekly) {
           var vm = new BudgetDialogViewModel(currentFiveHour, currentWeekly);
           var dlg = new BudgetDialog { DataContext = vm, Owner = System.Windows.Application.Current?.MainWindow };
           return dlg.ShowDialog() == true ? new BudgetSelection(vm.ParsedFiveHour, vm.ParsedWeekly) : null;
       }
       ```
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <done>Le projet compile ; IBudgetPrompt + BudgetSelection neutres ; dialogue WPF présent, DialogResult piloté par CloseRequested.</done>
</task>

<task type="auto">
  <name>Task 2 : CalibrateBudgetsCommand (GAP-1) + RefreshOrchestrator.RequestRefresh + tests VM</name>
  <files>src/Chronos/ViewModels/MainViewModel.cs, src/Chronos/Services/RefreshOrchestrator.cs, tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs, tests/Chronos.Tests/MainViewModelTests.cs, tests/Chronos.Tests/CadranBindingTests.cs, tests/Chronos.Tests/OverlayWindowConfigTests.cs</files>
  <read_first>
    - src/Chronos/ViewModels/MainViewModel.cs (ctor, Recalibrate = pattern GAP-1)
    - src/Chronos/Services/RefreshOrchestrator.cs (Trigger/TryTrigger privés)
    - tests/Chronos.Tests/MainViewModelTests.cs (Build/NewVmFull, tests Recalibrate)
    - tests/Chronos.Tests/Fakes/FakeRecalibrationPrompt.cs (pattern de fake)
  </read_first>
  <action>
    1. `RefreshOrchestrator` : ajouter `public void RequestRefresh() => _triggers.Writer.TryWrite(true);`
       avec commentaire fr (« déclenche un recalcul immédiat — ex. après calibration manuelle des plafonds »).
       Type de retour void/neutre → garde de pureté inchangée.

    2. `MainViewModel` :
       - Ajouter un champ `private readonly RefreshOrchestrator _orchestrator;` et l'assigner dans le ctor
         (`_orchestrator = orchestrator;`) — l'ordre d'abonnement existant reste inchangé.
       - Ajouter `IBudgetPrompt _budgetPrompt` au ctor (nouveau paramètre) + champ ; le stocker.
       - Nouvelle commande :
       ```csharp
       /// <summary>CAL-01 : demande les deux plafonds, les persiste (GAP-1 : Load DISQUE frais avant Save)
       /// en marquant source=Manual pour les champs saisis, puis redéclenche l'orchestrateur → couleur
       /// des arcs recalculée aussitôt avec les nouveaux plafonds. Annulation → aucun changement.</summary>
       [RelayCommand]
       private void CalibrateBudgets()
       {
           var courant = _settingsService.Load();
           var sel = _budgetPrompt.Ask(courant.FiveHourTokenBudget, courant.WeeklyTokenBudget);
           if (sel is null) return;

           var now = _clock.UtcNow;
           _settings = _settingsService.Load() with   // GAP-1 : relire l'état disque le plus récent
           {
               FiveHourTokenBudget = sel.FiveHour,
               FiveHourBudgetSource = sel.FiveHour is not null ? BudgetSource.Manual : BudgetSource.None,
               FiveHourBudgetCalibratedAt = sel.FiveHour is not null ? now : null,
               WeeklyTokenBudget = sel.Weekly,
               WeeklyBudgetSource = sel.Weekly is not null ? BudgetSource.Manual : BudgetSource.None,
               WeeklyBudgetCalibratedAt = sel.Weekly is not null ? now : null,
           };
           _settingsService.Save(_settings);
           _orchestrator.RequestRefresh(); // application immédiate (le provider Load() frais à chaque GetAsync)
       }
       ```
       (Ajouter `using Chronos.Services;` déjà présent ; BudgetSource y vit.)

    3. `tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs` — `internal sealed class FakeBudgetPrompt : IBudgetPrompt`
       avec `BudgetSelection? Result`, `int AskCount`, capture de `LastFiveHour/LastWeekly`, renvoyant Result.

    4bis. [FIX plan-checker] Le ctor de MainViewModel gagne un paramètre IBudgetPrompt : mettre à jour AUSSI
    les DEUX constructions directes hors helpers — tests/Chronos.Tests/CadranBindingTests.cs (~ligne 31) et
    tests/Chronos.Tests/OverlayWindowConfigTests.cs (~ligne 20) — en passant `new FakeBudgetPrompt()`
    (même position d'argument que dans les helpers). Sans cela le build casse.
    4. `tests/Chronos.Tests/MainViewModelTests.cs` : adapter les helpers `Build`/`NewVmFull` pour injecter
       le `FakeBudgetPrompt` dans le ctor (nouveau paramètre) — mettre à jour TOUTES les constructions de VM.
       Ajouter des [Fact] :
       - Saisie {5h=2_000_000, hebdo=null} → `settings.Load()` : FiveHourTokenBudget=2_000_000,
         FiveHourBudgetSource=Manual ; WeeklyTokenBudget=null, WeeklyBudgetSource=None.
       - Annulation (Result=null) → settings inchangés (plafonds restent null).
       - GAP-1 : un autre writer persiste `Corner=BottomLeft` APRÈS construction ; après CalibrateBudgets,
         `Corner` reste BottomLeft (non écrasé) ET le plafond est bien enregistré.
       (Le re-déclenchement RequestRefresh sur orchestrateur non démarré est sans effet observable en test —
       ne pas asserter dessus ; il est couvert par l'intégration.)
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "MainViewModel"</automated>
  </verify>
  <done>MainViewModelTests vert (anciens tests Recalibrate inclus, ctor mis à jour) ; nouveaux [Fact] CalibrateBudgets verts ; RequestRefresh public présent.</done>
</task>

<task type="auto">
  <name>Task 3 : Câblage DI (IBudgetPrompt + BudgetAutoCalibrator) + MenuItem</name>
  <files>src/Chronos/App.xaml.cs, src/Chronos/Views/MainWindow.xaml, tests/Chronos.Tests/CompositionRootTests.cs</files>
  <read_first>
    - src/Chronos/App.xaml.cs (ConfigureServices, résolution eager du VM avant StartAsync)
    - src/Chronos/Views/MainWindow.xaml (ContextMenu existant)
    - tests/Chronos.Tests/CompositionRootTests.cs (miroir de ConfigureServices)
    - src/Chronos/Services/BudgetAutoCalibrator.cs (ctor)
  </read_first>
  <action>
    1. `App.xaml.cs` / `ConfigureServices` :
       - `services.AddSingleton<IBudgetPrompt, BudgetPrompt>();` (à côté de IRecalibrationPrompt).
       - Enregistrer le calibrateur avec le `JsonlEstimationProvider` concret comme source de tokens :
         ```csharp
         services.AddSingleton(sp => new BudgetAutoCalibrator(
             sp.GetRequiredService<RefreshOrchestrator>(),
             sp.GetRequiredService<JsonlEstimationProvider>(),   // source portant EstimatedTokens
             sp.GetRequiredService<SettingsService>(),
             sp.GetRequiredService<IClock>()));
         ```
       - Dans `OnStartup`, AVANT `await _host.StartAsync();` (à côté de la résolution eager du VM,
         même raison — forcer l'abonnement à SnapshotChanged avant la charge initiale) :
         `_ = _host.Services.GetRequiredService<BudgetAutoCalibrator>();`
       - S'assurer que `BudgetAutoCalibrator` (IDisposable) est disposé par le host à l'arrêt : étant
         un Singleton résolu par le conteneur, il est disposé par `_host.Dispose()` — vérifier qu'aucune
         disposition manuelle n'est requise (commenter le point).

    2. `MainWindow.xaml` : ajouter dans le `<ContextMenu>` un `MenuItem` (après « Recalibrer le reset
       hebdo… ») : `<MenuItem Header="Calibrer les plafonds…" Command="{Binding CalibrateBudgetsCommand}"/>`.

    3. `CompositionRootTests.cs` : mettre à jour le conteneur de test miroir pour refléter le nouveau
       graphe — enregistrer `IBudgetPrompt` (via `FakeBudgetPrompt` ou `BudgetPrompt`), ajuster le ctor du
       `MainViewModel` (nouveau paramètre IBudgetPrompt), et ajouter la résolution/enregistrement du
       `BudgetAutoCalibrator`. Le test doit continuer à prouver résolution sans exception + disposition.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "CompositionRoot"</automated>
  </verify>
  <done>CompositionRootTests vert ; le graphe DI résout MainWindow/VM/BudgetAutoCalibrator ; MenuItem « Calibrer les plafonds… » présent et lié à CalibrateBudgetsCommand.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit.
- `dotnet test Chronos.sln -c Debug` : suite complète verte (119 existants + Phase 9).
- Smoke manuel (hors CI) : lancer l'app, menu contextuel → « Calibrer les plafonds… » ouvre le dialogue,
  saisir un plafond 5 h → l'arc 5 h se colore ; réouvrir → valeur pré-remplie et persistée.
</verification>

<success_criteria>
- CAL-01 : dialogue de plafonds accessible au menu, valeurs persistées (GAP-1) et appliquées aussitôt (RequestRefresh).
- CAL-02 : BudgetAutoCalibrator instancié et abonné à l'orchestrateur au démarrage (calibration auto active au runtime).
- Aucune régression (tests VM + composition + pureté verts).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/09-calibration-des-plafonds-surfa-age/09-02-SUMMARY.md`
(dialogue plafonds, CalibrateBudgetsCommand, RequestRefresh, câblage DI du calibrateur).
</output>
