---
phase: 09-calibration-des-plafonds-surfa-age
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/ChronosSettings.cs
  - src/Chronos/Services/BudgetSource.cs
  - src/Chronos/Services/BudgetCalibration.cs
  - src/Chronos/Services/BudgetAutoCalibrator.cs
  - src/Chronos/Text/TokenFormatter.cs
  - tests/Chronos.Tests/BudgetCalibrationTests.cs
  - tests/Chronos.Tests/TokenFormatterTests.cs
  - tests/Chronos.Tests/BudgetAutoCalibratorTests.cs
  - tests/Chronos.Tests/JsonlEstimationProviderTests.cs
autonomous: true
requirements: [CAL-02, CAL-03, NET-02]

must_haves:
  truths:
    - "BudgetCalibration.Deduce(util, tokens) rend tokens/util arrondi (plafond déduit), null si util<=0 ou tokens<=0"
    - "BudgetCalibration.ApplyAuto n'écrit un plafond auto QUE si la source de ce plafond est None ou Auto — jamais Manual (une saisie manuelle n'est jamais écrasée)"
    - "TokenFormatter.Format abrège en français : 62 484 658 → « ≈ 62,5 M tokens », 12 345 → « ≈ 12,3 k tokens », 500 → « ≈ 500 tokens »"
    - "BudgetAutoCalibrator est INERTE (aucun appel disque, aucune écriture) tant qu'aucune fenêtre Exact avec Utilization>0 n'apparaît"
    - "Un plafond calibré ne change PAS la Reliability : une fenêtre estimée reste Estimated (badge « estimée » conservé) — CAL-03"
    - "BudgetAutoCalibrator et BudgetCalibration sont neutres (aucun type WPF) — ServicesLayerPurityTests reste verte"
  artifacts:
    - path: "src/Chronos/Services/BudgetSource.cs"
      provides: "Enum neutre BudgetSource { None, Manual, Auto }"
      contains: "enum BudgetSource"
    - path: "src/Chronos/Services/BudgetCalibration.cs"
      provides: "Fonctions pures Deduce + ApplyAuto (règle de priorité manuel/auto)"
      contains: "public static long? Deduce"
    - path: "src/Chronos/Services/BudgetAutoCalibrator.cs"
      provides: "Service neutre écoutant RefreshOrchestrator.SnapshotChanged, calibration opportuniste"
      contains: "class BudgetAutoCalibrator"
    - path: "src/Chronos/Text/TokenFormatter.cs"
      provides: "Formateur pur fr abrégé des tokens (M/k)"
      contains: "public static string Format"
    - path: "tests/Chronos.Tests/BudgetCalibrationTests.cs"
      provides: "Couverture Deduce + priorité manuel/auto"
      min_lines: 40
  key_links:
    - from: "src/Chronos/Services/BudgetAutoCalibrator.cs"
      to: "RefreshOrchestrator.SnapshotChanged"
      via: "abonnement dans le ctor + désabonnement dans Dispose"
      pattern: "SnapshotChanged \\+="
    - from: "src/Chronos/Services/BudgetAutoCalibrator.cs"
      to: "BudgetCalibration.Deduce / ApplyAuto"
      via: "appel après lecture fraîche des settings"
      pattern: "BudgetCalibration\\."
---

<objective>
Poser les FONDATIONS NEUTRES et testables de la Phase 9, sans aucune UI ni câblage DI :
la métadonnée de source des plafonds (Manual/Auto/None), la logique pure de calibration
(déduction d'un plafond + règle de priorité manuel/auto), le formateur de tokens français
abrégé, et le service `BudgetAutoCalibrator` qui écoute l'orchestrateur pour calibrer
opportunément — inerte tant qu'aucune source Exact n'existe.

Purpose: isoler toute la logique vérifiable en couche Services/Text (garde de pureté) pour que
les plans UI (09-02, 09-03) ne fassent que du câblage. Couvre la substance de CAL-02 (déduction +
priorité + service), CAL-03 (l'estimé reste estimé malgré un plafond) et NET-02 (formateur).
Output: 3 sources neutres + 1 enum + 1 formateur + 3 fichiers de tests + 1 test de régression CAL-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-des-plafonds-surfa-age/09-CONTEXT.md
@.planning/phases/09-calibration-des-plafonds-surfa-age/09-VALIDATION.md

<interfaces>
<!-- Contrats existants que l'exécuteur consomme directement — pas d'exploration nécessaire. -->

Depuis src/Chronos/Services/ChronosSettings.cs (record NEUTRE, déjà présent — Phase 8) :
```csharp
public sealed record ChronosSettings
{
    // ... Corner, MonitorDeviceName, X, Y, Background, RefreshIntervalSeconds, WeeklyAnchor ...
    public long? FiveHourTokenBudget { get; init; } // plafond 5 h calibrable ; null = pas de plafond
    public long? WeeklyTokenBudget { get; init; }   // plafond hebdo calibrable ; null = pas de plafond
}
```

Depuis src/Chronos/Services/SettingsService.cs :
```csharp
public ChronosSettings Load();          // lecture tolérante (défauts si absent/corrompu)
public void Save(ChronosSettings s);    // écriture atomique
```

Depuis src/Chronos/Services/RefreshOrchestrator.cs :
```csharp
public event EventHandler<UsageSnapshot>? SnapshotChanged; // émis (thread pool) après chaque GetAsync
```

Depuis src/Chronos/Models/WindowState.cs et UsageSnapshot.cs :
```csharp
public sealed record WindowState {
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }        // 0..1 ; util exacte quand Reliability==Exact
    public long? EstimatedTokens { get; init; }      // somme brute JSONL (repli) ; info honnête
    public required SourceReliability Reliability { get; init; } // Exact | Estimated | Unavailable
}
public sealed record UsageSnapshot {
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
}
```

Depuis src/Chronos/Services/IUsageProvider.cs :
```csharp
public interface IUsageProvider { Task<UsageSnapshot> GetAsync(CancellationToken ct = default); }
```
Note : `JsonlEstimationProvider` implémente `IUsageProvider` et renvoie TOUJOURS des fenêtres
`Estimated` PORTANT `EstimatedTokens` (la somme réelle), quelle que soit la présence d'un plafond.
C'est cette source de tokens que consomme le calibrateur.

Pattern formateur pur existant à imiter — src/Chronos/Text/CountdownFormatter.cs :
```csharp
public static class CountdownFormatter { public static string Format(TimeSpan reste) { ... } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Métadonnée de source des plafonds + logique pure de calibration</name>
  <files>src/Chronos/Services/BudgetSource.cs, src/Chronos/Services/ChronosSettings.cs, src/Chronos/Services/BudgetCalibration.cs, tests/Chronos.Tests/BudgetCalibrationTests.cs</files>
  <read_first>
    - src/Chronos/Services/ChronosSettings.cs (record à étendre)
    - src/Chronos/Models/WindowKind.cs (enum FiveHour/SevenDay)
    - tests/Chronos.Tests/SettingsServiceTests.cs (pattern de test round-trip)
  </read_first>
  <behavior>
    - Deduce : util=0.5, tokens=1_000_000 → 2_000_000 ; util=0.25, tokens=750_000 → 3_000_000
    - Deduce : util<=0 → null ; tokens<=0 → null ; util>1 accepté (retourne tokens/util)
    - ApplyAuto sur plafond 5 h avec source None → écrit budget, source=Auto, CalibratedAt=now
    - ApplyAuto avec source Auto → écrase (met à jour budget + timestamp, reste Auto)
    - ApplyAuto avec source Manual → renvoie les settings INCHANGÉS (référence/valeur identiques)
    - ApplyAuto ne touche QUE la fenêtre visée (5 h) et laisse l'autre plafond (hebdo) intact
  </behavior>
  <action>
    1. Créer `src/Chronos/Services/BudgetSource.cs` — enum neutre dans `namespace Chronos.Services` :
       `public enum BudgetSource { None, Manual, Auto }` avec commentaire fr (None=jamais calibré,
       Manual=saisie utilisateur, Auto=déduit opportunément).

    2. Étendre `ChronosSettings` (ne PAS toucher aux champs existants) en ajoutant 4 propriétés
       `init` avec commentaires fr :
       ```csharp
       public BudgetSource FiveHourBudgetSource { get; init; } = BudgetSource.None;
       public DateTimeOffset? FiveHourBudgetCalibratedAt { get; init; }
       public BudgetSource WeeklyBudgetSource { get; init; } = BudgetSource.None;
       public DateTimeOffset? WeeklyBudgetCalibratedAt { get; init; }
       ```
       (Le JsonStringEnumConverter déjà configuré dans SettingsService sérialise l'enum en texte.)

    3. Créer `src/Chronos/Services/BudgetCalibration.cs` — classe statique PURE (aucun I/O, aucun WPF) :
       ```csharp
       public static class BudgetCalibration
       {
           /// <summary>Plafond déduit = tokens / utilization_exacte, arrondi. null si mesure inexploitable.</summary>
           public static long? Deduce(double exactUtilization, long jsonlTokens)
               => exactUtilization > 0 && jsonlTokens > 0
                   ? (long)Math.Round(jsonlTokens / exactUtilization, MidpointRounding.AwayFromZero)
                   : null;

           /// <summary>Applique un plafond AUTO à la fenêtre <paramref name="kind"/> SEULEMENT si sa source
           /// n'est pas Manual (l'auto n'écrase que l'auto ou l'absence de valeur — CAL-02). Sinon renvoie
           /// les settings inchangés.</summary>
           public static ChronosSettings ApplyAuto(ChronosSettings current, WindowKind kind, long deducedBudget, DateTimeOffset now)
           {
               if (kind == WindowKind.FiveHour)
                   return current.FiveHourBudgetSource == BudgetSource.Manual ? current
                       : current with { FiveHourTokenBudget = deducedBudget, FiveHourBudgetSource = BudgetSource.Auto, FiveHourBudgetCalibratedAt = now };
               return current.WeeklyBudgetSource == BudgetSource.Manual ? current
                   : current with { WeeklyTokenBudget = deducedBudget, WeeklyBudgetSource = BudgetSource.Auto, WeeklyBudgetCalibratedAt = now };
           }
       }
       ```
       Ajouter `using Chronos.Models;` pour `WindowKind`.

    4. Écrire `tests/Chronos.Tests/BudgetCalibrationTests.cs` couvrant tous les cas du bloc <behavior>
       (Deduce nominal + bornes ; ApplyAuto None/Auto/Manual pour 5 h ET hebdo ; non-contamination de
       l'autre fenêtre). Tests [Fact] purs.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter BudgetCalibration</automated>
  </verify>
  <done>BudgetCalibrationTests vert ; ChronosSettings compile avec les 4 nouveaux champs ; enum BudgetSource neutre présent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Formateur de tokens fr abrégé (NET-02) + régression CAL-03</name>
  <files>src/Chronos/Text/TokenFormatter.cs, tests/Chronos.Tests/TokenFormatterTests.cs, tests/Chronos.Tests/JsonlEstimationProviderTests.cs</files>
  <read_first>
    - src/Chronos/Text/CountdownFormatter.cs (pattern formateur pur à imiter)
    - src/Chronos/Services/JsonlEstimationProvider.cs (BuildFiveHour/BuildSevenDay : Reliability toujours Estimated même avec budget)
    - tests/Chronos.Tests/JsonlEstimationProviderTests.cs (pattern de test provider)
  </read_first>
  <behavior>
    - Format(62_484_658) → "≈ 62,5 M tokens"
    - Format(1_000_000) → "≈ 1 M tokens"   (décimale ,0 supprimée)
    - Format(12_345) → "≈ 12,3 k tokens"
    - Format(500) → "≈ 500 tokens"
    - Format(0) → "≈ 0 tokens" ; Format(négatif) traité comme 0
    - CAL-03 : JsonlEstimationProvider avec un budget défini → FiveHour.Reliability == Estimated
      (jamais Exact) ET Utilization != null (couleur), le badge « estimée » reste donc dû à l'UI
  </behavior>
  <action>
    1. Créer `src/Chronos/Text/TokenFormatter.cs` — classe statique PURE dans `namespace Chronos.Text` :
       - `public static string Format(long tokens)` ; borne `if (tokens < 0) tokens = 0;`
       - Seuils : `>= 1_000_000` → valeur = tokens/1_000_000d, unité "M " ; `>= 1_000` →
         valeur = tokens/1_000d, unité "k " ; sinon → valeur entière, unité "".
       - Abréviation à 1 décimale, virgule française, décimale ,0 supprimée : formater la valeur
         via `value.ToString("0.#", System.Globalization.CultureInfo.InvariantCulture).Replace('.', ',')`
         (déterministe, indépendant de la culture machine — comme CountdownFormatter reste neutre).
       - Résultat : `$"≈ {texteValeur} {unite}tokens"`. Pour le cas brut (<1000) : `$"≈ {tokens} tokens"`.
         Vérifier que « 62,5 M tokens » et « 500 tokens » sortent EXACTEMENT (un seul espace, pas d'unité vide).

    2. Écrire `tests/Chronos.Tests/TokenFormatterTests.cs` couvrant chaque ligne du <behavior> (égalité de chaîne exacte).

    3. Ajouter dans `tests/Chronos.Tests/JsonlEstimationProviderTests.cs` un [Fact] de régression CAL-03 :
       provider avec `FiveHourTokenBudget` défini dans les settings + un JSONL produisant des tokens →
       assert `snap.FiveHour.Reliability == SourceReliability.Estimated` ET `snap.FiveHour.Utilization is not null`.
       (Réutiliser les helpers/TestData existants du fichier ; ne pas casser les tests présents.)
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "TokenFormatter|JsonlEstimation"</automated>
  </verify>
  <done>TokenFormatterTests vert ; nouveau [Fact] CAL-03 vert ; aucun test JSONL existant cassé.</done>
</task>

<task type="auto">
  <name>Task 3 : BudgetAutoCalibrator (service neutre écoutant l'orchestrateur, CAL-02)</name>
  <files>src/Chronos/Services/BudgetAutoCalibrator.cs, tests/Chronos.Tests/BudgetAutoCalibratorTests.cs</files>
  <read_first>
    - src/Chronos/Services/RefreshOrchestrator.cs (event SnapshotChanged, ctor)
    - src/Chronos/ViewModels/MainViewModel.cs (pattern abonnement + leçon GAP-1 : Load frais avant Save)
    - tests/Chronos.Tests/CompositeUsageProviderTests.cs (FakeProvider retournant un snapshot fabriqué)
    - src/Chronos/Services/BudgetCalibration.cs (créé Task 1)
  </read_first>
  <action>
    1. Créer `src/Chronos/Services/BudgetAutoCalibrator.cs` — `sealed class`, `IDisposable`, NEUTRE
       (aucun `using System.Windows.*`) dans `namespace Chronos.Services`. Documenter en fr le rôle
       (calibration opportuniste CAL-02, inerte sans Exact).
       Dépendances ctor : `(RefreshOrchestrator orchestrator, IUsageProvider tokenSource, SettingsService settings, IClock clock)`.
       > Décision verrouillée : `tokenSource` reçoit en DI le `JsonlEstimationProvider` concret (source
       > qui porte toujours `EstimatedTokens`), PAS le composite (qui, sur fenêtre Exact, perd les tokens).
       Dans le ctor : `orchestrator.SnapshotChanged += OnSnapshot;` et mémoriser l'orchestrateur pour Dispose.

    2. Handler + logique :
       ```csharp
       private async void OnSnapshot(object? sender, UsageSnapshot snap)
       {
           try { await CalibrateAsync(snap); }
           catch { /* best-effort : la calibration ne doit jamais faire tomber le pipeline */ }
       }

       // Testable directement. INERTE si aucune fenêtre Exact avec util>0 (aucun GetAsync, aucune écriture).
       internal async Task CalibrateAsync(UsageSnapshot snap, CancellationToken ct = default)
       {
           bool fiveExact  = snap.FiveHour.Reliability == SourceReliability.Exact && snap.FiveHour.Utilization is > 0;
           bool weekExact  = snap.SevenDay.Reliability == SourceReliability.Exact && snap.SevenDay.Utilization is > 0;
           if (!fiveExact && !weekExact) return; // chemin app-bureau : zéro coût

           var est = await tokenSource.GetAsync(ct);   // tokens JSONL mesurés sur les mêmes fenêtres
           var now = clock.UtcNow;
           var current = settings.Load();              // GAP-1 : état DISQUE frais avant écriture
           var updated = current;

           if (fiveExact && est.FiveHour.EstimatedTokens is > 0 &&
               BudgetCalibration.Deduce(snap.FiveHour.Utilization!.Value, est.FiveHour.EstimatedTokens.Value) is { } b5)
               updated = BudgetCalibration.ApplyAuto(updated, WindowKind.FiveHour, b5, now);

           if (weekExact && est.SevenDay.EstimatedTokens is > 0 &&
               BudgetCalibration.Deduce(snap.SevenDay.Utilization!.Value, est.SevenDay.EstimatedTokens.Value) is { } b7)
               updated = BudgetCalibration.ApplyAuto(updated, WindowKind.SevenDay, b7, now);

           if (!ReferenceEquals(updated, current)) settings.Save(updated); // n'écrit que si un plafond a changé
       }
       ```
       `Dispose()` : `orchestrator.SnapshotChanged -= OnSnapshot;`.
       (Champs privés `readonly` pour tokenSource/settings/clock/orchestrator.)

    3. Écrire `tests/Chronos.Tests/BudgetAutoCalibratorTests.cs` (temp ChronosPaths + SettingsService réel,
       FakeClock, un FakeProvider comme `tokenSource` renvoyant un snapshot Estimated avec EstimatedTokens ;
       orchestrateur NON démarré — seule la source d'abonnement compte). Cas :
       - Snapshot avec FiveHour Exact util=0.5 + tokenSource EstimatedTokens=1_000_000 → après `CalibrateAsync`,
         `settings.Load().FiveHourTokenBudget == 2_000_000`, `FiveHourBudgetSource == Auto`.
       - Settings pré-remplis avec `FiveHourBudgetSource = Manual, FiveHourTokenBudget = 999` → `CalibrateAsync`
         ne change RIEN (saisie manuelle jamais écrasée).
       - Snapshot 100% Estimated (aucune fenêtre Exact) → `CalibrateAsync` ne modifie pas settings
         (assert budget inchangé) — preuve d'inertie.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "BudgetAutoCalibrator|ServicesLayerPurity"</automated>
  </verify>
  <done>BudgetAutoCalibratorTests vert ; ServicesLayerPurityTests reste verte (service neutre) ; le calibrateur s'abonne/désabonne proprement.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit (après chaque tâche).
- `dotnet test Chronos.sln -c Debug` : les 119 tests existants restent verts + les nouveaux.
- ServicesLayerPurityTests verte : BudgetCalibration et BudgetAutoCalibrator n'exposent aucun type WPF.
</verification>

<success_criteria>
- Déduction de plafond (tokens/util) et règle de priorité manuel/auto pures et testées (CAL-02).
- Calibrateur opportuniste inerte sans source Exact, écrivant via Load frais → ApplyAuto → Save (CAL-02, GAP-1).
- Un plafond calibré laisse la fenêtre Estimated (CAL-03, régression verte).
- Formateur fr abrégé des tokens prêt à consommer par l'UI (NET-02).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/09-calibration-des-plafonds-surfa-age/09-01-SUMMARY.md`
(artefacts créés, décisions : tokenSource = JsonlEstimationProvider concret, priorité Manual > Auto/None).
</output>
