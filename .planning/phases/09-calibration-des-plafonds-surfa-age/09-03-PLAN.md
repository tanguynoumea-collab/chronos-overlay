---
phase: 09-calibration-des-plafonds-surfa-age
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/Chronos/ViewModels/WindowGaugeViewModel.cs
  - src/Chronos/Views/MainWindow.xaml
  - tests/Chronos.Tests/CadranBindingTests.cs
autonomous: true
requirements: [NET-02]

must_haves:
  truths:
    - "En source Estimated avec des tokens > 0, WindowGaugeViewModel expose TokensText = « ≈ N M/k tokens » et HasTokens = true"
    - "En source Exact, ou sans tokens, HasTokens = false et aucun texte de tokens n'est affiché"
    - "Le cadran affiche, par fenêtre, les tokens estimés en texte secondaire discret (couleur #A9A8B2), visible seulement si HasTokens"
  artifacts:
    - path: "src/Chronos/ViewModels/WindowGaugeViewModel.cs"
      provides: "Propriétés TokensText + HasTokens dérivées de EstimatedTokens/Reliability"
      contains: "TokensText"
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "TextBlocks tokens estimés (5 h + hebdo) liés à TokensText/HasTokens"
      contains: "TokensText"
  key_links:
    - from: "src/Chronos/Views/MainWindow.xaml"
      to: "WindowGaugeViewModel.TokensText / HasTokens"
      via: "Text binding + Visibility via BoolToVis"
      pattern: "TokensText"
    - from: "src/Chronos/ViewModels/WindowGaugeViewModel.cs"
      to: "Chronos.Text.TokenFormatter.Format"
      via: "appel dans Apply quand Estimated + tokens>0"
      pattern: "TokenFormatter\\.Format"
---

<objective>
Surfacer la matière première de l'estimation (NET-02) : quand une fenêtre est en source Estimated,
afficher ses tokens estimés en texte secondaire discret (« ≈ 62,5 M tokens ») près du countdown,
via le `TokenFormatter` pur créé en 09-01. Rien en source Exact ni sans donnée.

Purpose: l'utilisateur voit d'où vient l'estimation, sans jamais qu'un chiffre estimé soit présenté
comme exact (le badge « estimée » reste). Output: propriétés VM TokensText/HasTokens + deux TextBlocks
XAML discrets + test de binding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-calibration-des-plafonds-surfa-age/09-CONTEXT.md
@.planning/phases/09-calibration-des-plafonds-surfa-age/09-01-SUMMARY.md

<interfaces>
<!-- Contrats existants — consommer directement. -->

src/Chronos/ViewModels/WindowGaugeViewModel.cs (à étendre) — patron ObservableProperty + Apply :
```csharp
[ObservableProperty] private bool _isEstimated;   // déjà là : provenance
public void Apply(WindowState s) {
    _state = s;
    Utilization = s.Utilization; Exhausted = s.Exhausted; Reliability = s.Reliability;
    IsEstimated = s.Reliability == SourceReliability.Estimated;
}
```
src/Chronos/Models/WindowState.cs : `long? EstimatedTokens` (somme brute JSONL, portée par le repli) ;
`SourceReliability Reliability` (Exact|Estimated|Unavailable).

src/Chronos/Text/TokenFormatter.cs (créé en 09-01) : `public static string Format(long tokens)`
→ « ≈ 62,5 M tokens », « ≈ 12,3 k tokens », « ≈ 500 tokens ».

src/Chronos/Views/MainWindow.xaml — StackPanel central, badges existants utilisent déjà
`Converter={StaticResource BoolToVis}` (bool→Visibility) et le token `TexteSecondaire` (#A9A8B2).
Groupes existants : « Groupe 5 h » (TexteCinqHeures + BadgeEstimeeCinqHeures + TexteEpuiseCinqHeures)
et « Groupe hebdo » (TexteHebdo + BadgeEstimeeHebdo + TexteEpuiseHebdo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : WindowGaugeViewModel — TokensText + HasTokens (NET-02)</name>
  <files>src/Chronos/ViewModels/WindowGaugeViewModel.cs, tests/Chronos.Tests/CadranBindingTests.cs</files>
  <read_first>
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (Apply, propriétés existantes)
    - src/Chronos/Text/TokenFormatter.cs (Format)
    - tests/Chronos.Tests/CadranBindingTests.cs (pattern d'assertion sur le sous-VM)
  </read_first>
  <action>
    1. Dans `WindowGaugeViewModel`, ajouter :
       ```csharp
       [ObservableProperty] private string _tokensText = "";   // « ≈ N M/k tokens » ; vide si masqué
       [ObservableProperty] private bool _hasTokens;           // vrai SSI Estimated + tokens>0 (pilote la visibilité)
       ```
       Ajouter `using Chronos.Text;` si absent.
    2. Dans `Apply(WindowState s)`, après le bloc existant, dériver :
       ```csharp
       HasTokens = s.Reliability == SourceReliability.Estimated && s.EstimatedTokens is > 0;
       TokensText = HasTokens ? TokenFormatter.Format(s.EstimatedTokens!.Value) : "";
       ```
       (Honnêteté : jamais de tokens affichés en Exact — les pourcentages exacts suffisent — ni sans donnée.)
    3. Dans `tests/Chronos.Tests/CadranBindingTests.cs`, ajouter des [Fact] :
       - Apply d'un WindowState Estimated avec EstimatedTokens=62_484_658 → HasTokens=true,
         TokensText=="≈ 62,5 M tokens".
       - Apply d'un WindowState Exact (util renseignée, EstimatedTokens=null) → HasTokens=false, TokensText=="".
       - Apply Estimated avec EstimatedTokens=0 → HasTokens=false.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "CadranBinding"</automated>
  </verify>
  <done>CadranBindingTests vert ; TokensText/HasTokens dérivés correctement ; aucun affichage en Exact.</done>
</task>

<task type="auto">
  <name>Task 2 : MainWindow.xaml — TextBlocks tokens discrets + smoke suite complète</name>
  <files>src/Chronos/Views/MainWindow.xaml</files>
  <read_first>
    - src/Chronos/Views/MainWindow.xaml (StackPanel central, groupes 5 h / hebdo, usage de BoolToVis + TexteSecondaire)
  </read_first>
  <action>
    1. Dans le « Groupe 5 h », après `BadgeEstimeeCinqHeures`, ajouter un TextBlock discret :
       ```xml
       <TextBlock x:Name="TokensCinqHeures" HorizontalAlignment="Center"
                  FontSize="9"
                  Text="{Binding FiveHour.TokensText}"
                  Visibility="{Binding FiveHour.HasTokens, Converter={StaticResource BoolToVis}}"
                  Foreground="{StaticResource TexteSecondaire}"/>
       ```
    2. Dans le « Groupe hebdo », après `BadgeEstimeeHebdo`, ajouter l'équivalent lié à
       `SevenDay.TokensText` / `SevenDay.HasTokens` (`x:Name="TokensHebdo"`).
       Rester discret : FontSize 9, couleur TexteSecondaire (#A9A8B2), pas de gras. Ne PAS déplacer les
       éléments existants (le layout du cadran reste stable).
    3. Vérifier visuellement dans le XAML que les bindings pointent bien sur les sous-VM `FiveHour`/`SevenDay`
       (mêmes DataContext que les badges voisins).
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug</automated>
  </verify>
  <done>Le build XAML passe ; suite COMPLÈTE verte (119 existants + Phase 9) ; deux TextBlocks tokens présents, visibles seulement si HasTokens.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit (XAML compilé).
- `dotnet test Chronos.sln -c Debug` : suite complète verte.
- Smoke manuel (hors CI) : en mode repli (app bureau), le cadran affiche « ≈ N M tokens » sous chaque
  fenêtre estimée ; en source Exact, aucun texte de tokens.
</verification>

<success_criteria>
- NET-02 : tokens estimés surfacés en texte secondaire discret, uniquement en source Estimated avec tokens>0.
- Honnêteté préservée : le badge « estimée » et l'absence de tokens en Exact restent intacts.
- Aucune régression (suite complète verte).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/09-calibration-des-plafonds-surfa-age/09-03-SUMMARY.md`
(TokensText/HasTokens, TextBlocks tokens ; clôture Phase 9 / milestone v1.1).
</output>
