---
phase: 26-traite-veut-enfin-dire-quelque-chose
plan: 04
type: execute
wave: 4
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - docs/hooks-contract.md
  - tests/Chronos.Tests/ContratHooksDocumenteTests.cs
  - .planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TRT-01, TRT-02, TRT-03, TRT-04]

must_haves:
  truths:
    - "Le contrat des hooks ne transmet plus de divergence à une phase future : elle est refermée, et le document le dit."
    - "Ce que le document écrit de la règle de traitement correspond à ce que le code applique — une garde le tient."
    - "La carte de validation de la phase porte des chiffres MESURÉS, pas des chiffres attendus."
    - "Les quatre exigences TRT sont tracées comme couvertes, chacune par une preuve nommée."
  artifacts:
    - path: "docs/hooks-contract.md"
      provides: "Le §3 dit la règle de traitement réellement câblée ; la divergence transmise est close"
      contains: "TRT-01"
    - path: "tests/Chronos.Tests/ContratHooksDocumenteTests.cs"
      provides: "Garde de non-dérive entre le §3 et le détecteur"
      contains: "SessionTreatmentTracker"
    - path: ".planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md"
      provides: "Carte de validation close, tous chiffres mesurés"
      contains: "Total mesuré après 26-04"
  key_links:
    - from: "docs/hooks-contract.md"
      to: "src/Chronos/Services/SessionTreatmentTracker.cs"
      via: "la garde lit les DEUX et exige qu'ils disent la même règle"
      pattern: "SessionTreatmentTracker"
---

<objective>
Le document suit le câblage, et la phase se clôt sur des chiffres mesurés.

`docs/hooks-contract.md` §3 porte encore, en toutes lettres, une **divergence connue transmise à la
phase 26, NON corrigée ici** : `SessionTreatmentTracker` ne reconnaissait pas `WaitingDeduced` comme une
attente. Les plans 26-01 à 26-03 l'ont refermée — et ont changé la règle de traitement de fond en comble.
Un document qui décrit un câblage qu'il ne décrit plus est **pire que pas de document** : il fait croire
qu'on sait. C'est le mécanisme exact de la panne que la phase 25 a réparée ; le répéter une octave plus bas
serait la faute la plus bête de tout le milestone.

La garde de non-dérive (EVT-05) tient déjà la moitié qu'une machine peut tenir : que ce que nous ÉCRIVONS
corresponde à ce que nous INSTALLONS. On l'étend ici à la règle de traitement.

Purpose: qu'aucun lecteur — utilisateur ou futur planificateur — ne puisse croire que le défaut est encore là.
Output: un §3 exact, une garde qui le tient, et la carte de validation close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-CONTEXT.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-01-SUMMARY.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-02-SUMMARY.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-03-SUMMARY.md
@docs/hooks-contract.md
@tests/Chronos.Tests/ContratHooksDocumenteTests.cs

<interfaces>
<!-- Ce que la garde EXISTANTE tient déjà, et qu'il ne faut surtout pas casser. -->

From tests/Chronos.Tests/ContratHooksDocumenteTests.cs :
```csharp
private const string MarqueurDebut  = "EVENEMENTS-CABLES:debut";   // encadrent la table du §1
private const string MarqueurFin    = "EVENEMENTS-CABLES:fin";
private const string DateDuReleve   = "2026-09-12";
private const string TitreNonGaranti = "## 5.";                    // section découpée avant contrôle de date
private const int    LignesMinimum  = 90;                          // garde anti-document vidé
private static readonly (string Marqueur, string Trou)[] TrousDocumentaires =
{
    ("Échap", …), ("prompt_input_exit", …), ("liste blanche", …),   // les trois trous documentaires
};
```
**Aucun de ces marqueurs ne doit disparaître du document.** Le §3 se modifie sans toucher au §1, au §5,
ni aux trois marqueurs de trous.

From tests/Chronos.Tests/GardesPerimetreTests.cs :
```csharp
internal static string CheminSources();   // chemin de src/Chronos, INJECTÉ par MSBuild — réutilisable ici
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Le §3 dit la règle réellement câblée, et une garde le tient</name>
  <files>docs/hooks-contract.md, tests/Chronos.Tests/ContratHooksDocumenteTests.cs</files>

  <read_first>
    - docs/hooks-contract.md — le §3 **intégral**, y compris le bloc
      « ⚠ Divergence connue — transmise à la phase 26, NON corrigée ici »
    - tests/Chronos.Tests/ContratHooksDocumenteTests.cs (intégral — constantes l. 36-50, `LireDocument`
      l. 72-85, et le motif des tests existants)
    - tests/Chronos.Tests/GardesPerimetreTests.cs (l. 258-264 : `CheminSources()`, `internal static`)
    - src/Chronos/Services/SessionTreatmentTracker.cs (tel que laissé par 26-01)
    - .planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-01-SUMMARY.md
  </read_first>

  <behavior>
    Deux cas ajoutés, **verts dès leur écriture** — ce sont des gardes de non-dérive, pas des tests de
    développement, et leur valeur est d'attraper la PROCHAINE divergence.

    1. `Le_document_ne_transmet_plus_de_divergence_a_la_phase_26` — le texte du document ne contient plus
       `"transmise à la phase 26"` ni `"NON corrigée ici"`, et contient `"TRT-01"`. Assertion anti-muette
       préalable : le document compte au moins `LignesMinimum` lignes et contient encore `"## 3."`.
    2. `Le_document_dit_la_regle_de_traitement_reellement_cablee` — garde croisée DOCUMENT ↔ CODE.
       Lire `docs/hooks-contract.md` **et** `src/Chronos/Services/SessionTreatmentTracker.cs` (via
       `GardesPerimetreTests.CheminSources()`), puis exiger que les trois affirmations du document aient
       leur contrepartie dans le code :
       • le document contient **la chaîne exacte `transition observée sur la MÊME source`** — et non
         `"même source"`, qui figure **déjà deux fois** au §5 (l. 213 et 227, « Relevé le 2026-09-12, même
         source ») dans un sens sans rapport : une garde qui s'en contenterait serait **verte avant
         l'édition**, donc vacueuse — **et** le code contient `prec.Source == v.Source` ;
       • le document nomme `WaitingDeduced` comme une attente **et** le code contient `WaitingDeduced`
         dans `EstAttente` ;
       • le document dit que l'épisode est daté par le signal **et** le code contient `InstantDuSignal`.
       Assertions anti-muettes : le fichier de code contient `public void Observe(` et n'est pas vide.
  </behavior>

  <action>
    **(a) `docs/hooks-contract.md` §3 — remplacer le bloc de divergence.**

    Supprimer intégralement la sous-section « ⚠ Divergence connue — transmise à la phase 26, NON corrigée
    ici » et la remplacer par une sous-section qui décrit la règle **telle qu'elle est câblée** après la
    phase 26. Elle doit dire, dans cet ordre, et sans un mot de plus :

    ```
    ### Ce que « traité » veut dire (phase 26 — TRT-01, TRT-02)

    Une session quitte le widget pour deux raisons seulement : un geste de l'utilisateur, ou une
    **transition observée sur la MÊME source**. Concrètement, `SessionTreatmentTracker` n'inscrit une
    session comme traitée que si la source qui a parlé au cycle précédent disait une attente, que c'est
    **la même** qui parle maintenant, et qu'elle dit `Working`.

    - **`WaitingDeduced` est une attente pour le détecteur.** L'exclure revenait à lire « l'attente est
      devenue déduite » comme « l'utilisateur a répondu ». La divergence signalée par la vérification de
      la phase 25 est refermée ici.
    - **`Unknown` ne ferme aucun épisode.** Un signal illisible ou indéterminé n'affirme rien ; le lire
      comme une réponse, c'est conclure d'une absence de lecture.
    - **Une bascule de source n'est pas une transition.** Un fichier de hook qui franchit `DropAfter`
      pendant qu'un transcript reprend la main, ce n'est pas quelqu'un qui répond : c'est une source qui
      se tait pendant qu'une autre parle. Le relevé du 2026-09-12 en donne la mesure — une attente
      enregistrée quatre cent soixante-dix-huit minutes avant un seuil de quatre cent quatre-vingts, puis
      la bascule, et six heures de masquage sur une session réellement en attente de permission.
    - **L'épisode d'attente est daté par l'instant que le SIGNAL porte**, jamais par l'horloge du
      guetteur, et il n'avance que lorsque la source affirme quelque chose de plus récent. C'est ce qui
      fait survivre le « traité » à un redémarrage de l'overlay sans lui interdire de revenir.
    ```

    **Ne rien changer d'autre dans le document** : ni le tableau des cinq états, ni le rang d'urgence 3,
    ni la phrase « `WaitingDeduced` n'est JAMAIS écrite dans un fichier d'état », ni le §1 et ses deux
    marqueurs, ni le §5, ni les trois marqueurs de trous documentaires (`Échap`, `prompt_input_exit`,
    `liste blanche`), ni la date du relevé.

    **(b) `tests/Chronos.Tests/ContratHooksDocumenteTests.cs`** — ajouter les deux cas du bloc
    `<behavior>`, en réutilisant `LireDocument()` (qui refuse de se mettre en sourdine si le chemin
    manque) et `GardesPerimetreTests.CheminSources()`. Ne **modifier aucun** test existant de ce fichier.

    **Consigne de rédaction (obligatoire).** Le document est du Markdown et n'est pas balayé par
    `NormalisationUniqueTests` ; en revanche la garde compte des lignes et cherche des marqueurs — vérifier
    après édition que le compte de lignes reste **≥ 90** et que les marqueurs sont intacts. Écrire les
    grandeurs en toutes lettres dans la prose, comme le fait déjà le document. Français.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~ContratHooksDocumente"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -c Debug --nologo -v q` : **0 échec**, total = **886 + 2 = 888**.
      Tout écart justifié **nominativement** au SUMMARY.
    - **Tous** les tests préexistants de `ContratHooksDocumenteTests` sont verts — en particulier ceux qui
      contrôlent la table du §1, la date du relevé et les trois trous documentaires.
    - `grep -c "transmise à la phase 26" docs/hooks-contract.md` = **0** ;
      `grep -c "NON corrigée ici" docs/hooks-contract.md` = **0**.
    - `grep -c "TRT-01" docs/hooks-contract.md` **≥ 1**.
    - `grep -c "transition observée sur la MÊME source" docs/hooks-contract.md` = **1**.
    - `grep -c "même source" docs/hooks-contract.md` = **2**, *inchangé* : ce sont les deux
      « **Relevé le 2026-09-12**, même source » du §5 (l. 213 et 227), sans aucun rapport avec la règle de
      traitement. C'est la preuve que la garde croisée ne s'appuie pas sur eux. *(La chaîne ajoutée porte
      `MÊME` en capitales ; `grep` est sensible à la casse, ce compte ne bouge donc pas.)*
    - `grep -c "WaitingDeduced" docs/hooks-contract.md` **≥ 3** (le tableau des états, la phrase de
      non-persistance, la nouvelle sous-section) — mesurer la valeur d'entrée avant édition et la reporter
      si elle diffère.
    - `wc -l docs/hooks-contract.md` **≥ 90**.
    - `grep -c "EVENEMENTS-CABLES:debut\|EVENEMENTS-CABLES:fin\|## 5\." docs/hooks-contract.md` **≥ 3** :
      les marqueurs de la garde existante sont intacts.
    - `git diff --stat <SHA de fin du plan 26-03>..HEAD -- src/Chronos` est **vide** : cette tâche n'écrit
      que du Markdown et un fichier de tests. **Mesurer ce SHA en début de tâche** (`git rev-parse HEAD`)
      et le reporter au SUMMARY. `07ee784` ne convient PAS ici : il montrerait tous les fichiers des plans
      26-01 à 26-03.
  </acceptance_criteria>

  <done>
    Le contrat des hooks ne transmet plus rien à personne : il décrit la règle qui tourne, et une garde
    croisée refuse désormais que le document et le détecteur se contredisent en silence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clore la carte de validation sur des chiffres mesurés</name>
  <files>.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md (intégral — toutes les
      cases « Mesuré » à remplir)
    - Les quatre SUMMARY de la phase : 26-01, 26-02, 26-03, et celui de la tâche 1 ci-dessus
    - .planning/REQUIREMENTS.md (l. 60-70 : les quatre TRT ; l. 113-118 : la table de traçabilité)
    - .planning/phases/25-le-contrat-d-evenements-refonde/25-VALIDATION.md (le modèle de clôture :
      chaque case porte un chiffre MESURÉ et tout écart est justifié nominativement)
  </read_first>

  <action>
    **(a) `26-VALIDATION.md`** — remplir **toutes** les cases restées vides :
    - les totaux mesurés après chacun des quatre plans, avec le temps de suite, et **deux exécutions
      consécutives** à la clôture ;
    - la couverture des quatre exigences TRT, chacune avec sa **preuve nommée** (nom exact du test) ;
    - la liste des tests **rouges avant / verts après**, avec les noms relevés dans les SUMMARY des plans
      26-01 et 26-02 ;
    - les renommages effectivement survenus, **nominativement** ;
    - les déviations par rapport aux attendus indicatifs, chacune expliquée.

    **Règle absolue, héritée du milestone : ne rien écrire ici qui n'ait été mesuré.** Une case remplie
    « d'après le plan » plutôt que d'après une exécution est exactement la faute que ce milestone corrige.
    Un attendu indicatif qui ne tombe pas juste se **justifie**, il ne s'**ajuste** pas.

    **(b) `.planning/REQUIREMENTS.md`** — passer les quatre cases `- [ ] **TRT-0x**` à `- [x]`, et les
    quatre lignes de la table de traçabilité de `Pending` à `Complete`. **Rien d'autre.** Si une exigence
    n'est pas réellement couverte par une preuve nommée dans `26-VALIDATION.md`, **ne pas la cocher** et le
    dire au SUMMARY : une case cochée sans preuve est une affirmation non observée, et c'est le seul
    interdit absolu de ce milestone.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -c Debug --nologo -v q` : **0 échec**, total **888**, **deux exécutions
      consécutives** avec le même total, toutes deux reportées dans `26-VALIDATION.md`.
    - Aucune case « Mesuré » de `26-VALIDATION.md` n'est vide ; aucun chiffre n'y figure sans avoir été
      observé dans une sortie de test.
    - `grep -c "TRT-0" .planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md` **≥ 4** :
      les quatre exigences y sont nommées.
    - `grep -c "| TRT-0. | Phase 26 | Complete |" .planning/REQUIREMENTS.md` = **4**
      (et `… | Pending |` = **0** pour les lignes TRT).
    - `grep -c "^- \[x\] \*\*TRT-0" .planning/REQUIREMENTS.md` = **4**.
    - Cette tâche **n'ajoute aucun test** et ne modifie **aucun** fichier de `src/` ni de `tests/` :
      `git diff --stat 07ee784..HEAD -- src tests` est identique à la mesure de fin de tâche 1.
  </acceptance_criteria>

  <done>
    La phase se clôt sur des chiffres relevés dans une sortie de test, pas sur des chiffres attendus. Les
    quatre exigences TRT portent chacune le nom du test qui les prouve — et celles qui n'en auraient pas
    restent ouvertes, en toutes lettres.
  </done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, total **888**, suite ≤ 8 s, deux
  exécutions consécutives.
- `docs/hooks-contract.md` décrit la règle de traitement **réellement câblée**, et une garde croisée
  document ↔ code refuse la prochaine dérive.
- Les acquis des phases 20 à 25 sont intacts, vérifiés par leurs gardes respectives, toutes vertes.
- Aucun fichier de l'utilisateur n'est touché : `%APPDATA%\Chronos\sessions\`, `archived.json`,
  `treated.json`, `~/.claude/settings.json`, `oauth.dat`. L'overlay (pid 119412) n'est ni lancé ni tué.
- Aucune requête réseau, aucune dépendance NuGet ajoutée.
</verification>

<success_criteria>
- Le document ne transmet plus de divergence : elle est refermée et décrite.
- Une garde tient la correspondance entre ce que le §3 écrit et ce que le détecteur applique.
- La carte de validation est close, tous chiffres mesurés, tous écarts justifiés nominativement.
- Les quatre exigences TRT sont tracées comme couvertes, chacune par une preuve nommée.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-04-SUMMARY.md`.
</output>
