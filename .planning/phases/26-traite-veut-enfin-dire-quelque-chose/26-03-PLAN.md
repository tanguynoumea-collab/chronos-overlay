---
phase: 26-traite-veut-enfin-dire-quelque-chose
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - src/Chronos/ViewModels/SessionsViewModel.cs
  - src/Chronos/ViewModels/SessionsPreviewViewModel.cs
  - src/Chronos/Views/SessionsController.cs
  - src/Chronos/Views/SessionsWindow.xaml.cs
  - src/Chronos/Resources/SessionStyles.xaml
  - src/Chronos/Services/DiagnosticService.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/GesteTraiteTests.cs
  - tests/Chronos.Tests/AffichageSessionsTests.cs
  - tests/Chronos.Tests/SessionStylesBindingTests.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
autonomous: true
requirements: [TRT-03, TRT-04]

must_haves:
  truths:
    - "Je peux marquer une session traitée d'un geste direct, et elle disparaît immédiatement."
    - "Je peux vider un widget saturé de déductions en UN geste, pas en vingt."
    - "Le geste est RÉVERSIBLE et le libellé le dit : la session revient dès qu'elle me redemande quelque chose."
    - "Le geste destructif est séparé du geste ordinaire et annoncé comme définitif — jamais ambigu, jamais à portée d'un clic distrait."
    - "Les trois entrées du menu existent sur les 8 styles de session et les 9 thèmes, sans casser la compacité du widget."
    - "Le libellé « Archiver » dit enfin la vérité : ce qui est archivé ne revient jamais."
  artifacts:
    - path: "src/Chronos/ViewModels/SessionsViewModel.cs"
      provides: "Deux commandes de traitement explicite, et l'instant du signal porté par chaque ligne"
      contains: "MarquerTraitee"
    - path: "src/Chronos/Resources/SessionStyles.xaml"
      provides: "Huit menus contextuels à trois entrées, libellés qui disent la vérité"
      contains: "MarquerToutTraiteCommand"
    - path: "tests/Chronos.Tests/GesteTraiteTests.cs"
      provides: "Preuve de la disparition immédiate, du geste de masse et de la réversibilité annoncée"
      contains: "MarquerTraiteeCommand"
  key_links:
    - from: "src/Chronos/ViewModels/SessionsViewModel.cs"
      to: "src/Chronos/Services/TreatedStore.cs"
      via: "le geste explicite écrit l'instant du signal dans le magasin réversible"
      pattern: "_treated\\.Set\\("
    - from: "src/Chronos/Resources/SessionStyles.xaml"
      to: "src/Chronos/ViewModels/SessionsViewModel.cs"
      via: "les huit menus contextuels lient les trois commandes de SessionItemVm"
      pattern: "Binding MarquerTraiteeCommand"
---

<objective>
TRT-03 — le geste explicite. Et le volet UI de TRT-04 : que le libellé dise la vérité.

Le focus ne peut pas fournir l'acquittement en terminal — NET-02 a été retirée en phase 21 parce qu'elle
exigeait une origine « app bureau » qu'une session Claude Code n'a jamais. Il faut donc un geste DIRECT, et
il passera par le chemin déjà éprouvé : les **huit** menus contextuels de `SessionStyles.xaml`, un par
style, liés aux commandes de `SessionItemVm`.

Deux exigences pèsent sur sa conception, et elles sont mesurées.

**L'effet de masse.** Sur un corpus vivant de 66 états, **20 sessions sur 54** basculent ensemble en « à
toi ? déduit » (relevé 25-03). Si le geste explicite est le seul moyen de vider un widget saturé, **un
geste par session sur vingt sessions n'est pas un geste**. D'où une seconde entrée : tout marquer d'un coup,
avec le nombre affiché dans le libellé.

**La prudence UI, verrou établi dans ce projet.** `LoginClaudeCommand` bascule, et un clic effacerait le
coffre de jetons ; `ReconnecterCommand` est la commande dédiée. Ici : le geste ordinaire est RÉVERSIBLE et
le dit ; le geste définitif est séparé, nommé définitif, et placé en dernier — jamais à portée d'un clic
distrait.

Purpose: donner à l'utilisateur le moyen de dire lui-même « c'est traité », sans jamais lui faire signer
autre chose que ce qu'il lit.
Output: trois entrées de menu sur huit styles et neuf thèmes, deux commandes, et un libellé qui ne ment plus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-CONTEXT.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-VALIDATION.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-01-SUMMARY.md
@.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-02-SUMMARY.md
@src/Chronos/ViewModels/SessionsViewModel.cs
@src/Chronos/Resources/SessionStyles.xaml
@src/Chronos/Views/SessionsController.cs
@tests/Chronos.Tests/SessionStylesBindingTests.cs

<interfaces>
<!-- Contrats EXISTANTS lus le 2026-09-12, plus ceux posés par 26-01 et 26-02. -->

From src/Chronos/ViewModels/SessionsViewModel.cs (avant ce plan) :
```csharp
public sealed partial class SessionItemVm : ObservableObject
{
    public string SessionId { get; }
    public SessionItemVm(string sessionId, System.Action<string> archive);   // ← change en tâche 1
    [ObservableProperty] private string _project, _stateText, _detail;
    [ObservableProperty] private Brush _stateBrush;
    [ObservableProperty] private bool _isWaiting, _isAttention, _isTurn, _isWorking, _isGhost;
    [RelayCommand] private void Archive();                                   // → ArchiveCommand
}

public sealed partial class SessionsViewModel : ObservableObject
{
    public SessionsViewModel(SessionMonitor monitor, IClock clock, ArchiveStore archive);  // ← change en tâche 1
    public ObservableCollection<SessionItemVm> Items { get; }
    public void Refresh(System.DateTimeOffset now);        // l. 132-163 : reconstruit Items à chaque appel
    public void SetTheme(ChronosTheme theme);
    [ObservableProperty] private SessionStyle _style;      // 8 valeurs, 8 booléens IsStyleX
}
```

From src/Chronos/Services/TreatedStore.cs (après 26-01) :
```csharp
public TreatedStore(string? path = null, IClock? clock = null);
public IReadOnlyDictionary<string, long> Load();
public void Set(string sessionId, long treatedWaitingTs);   // treatedWaitingTs = l'instant de l'ÉPISODE
public void Remove(string sessionId);
```

From src/Chronos/Services/SessionTreatmentTracker.cs (après 26-01) — la règle qui rend le geste réversible :
```
NET-03 : une session en attente dont l'épisode courant (l'instant que le SIGNAL porte, borné par
         maintenant) est STRICTEMENT plus récent que le treatedWaitingTs mémorisé est retirée du magasin.
```
Conséquence dont dépend le libellé : si le geste écrit `treatedWaitingTs = UpdatedAt` du signal courant,
la session reste masquée tant que sa source ne dit rien de neuf — et **revient au premier signal plus
récent**. « Revient si elle me redemande » est donc une description, pas une promesse.

From src/Chronos/Resources/SessionStyles.xaml (état actuel, mesuré) :
```
8 DataTemplate (x:Key=Tpl…), 8 blocs <ContextMenu>, 8 <MenuItem> — un par menu — tous liés à ArchiveCommand.
Libellés actuels : « Archiver (retirer de l'overlay) » ×2 (TplPastilles l. 56, TplMarge l. 88),
                   « Archiver » ×6 (Jetons 125, Sonar 165, Facade 216, Etagere 245, Annonciateur 313, Veilleurs 345).
Le ContextMenu porte toujours : DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"
→ le DataContext du menu est le SessionItemVm de la ligne. C'est le chemin à réutiliser tel quel.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deux commandes — traiter celle-ci, ou toutes</name>
  <files>src/Chronos/ViewModels/SessionsViewModel.cs, src/Chronos/ViewModels/SessionsPreviewViewModel.cs, src/Chronos/Views/SessionsController.cs, src/Chronos/Services/DiagnosticService.cs, src/Chronos/App.xaml.cs, tests/Chronos.Tests/GesteTraiteTests.cs, tests/Chronos.Tests/AffichageSessionsTests.cs, tests/Chronos.Tests/SessionStylesBindingTests.cs</files>

  <read_first>
    - src/Chronos/ViewModels/SessionsViewModel.cs (intégral, 187 l. — `SessionItemVm` l. 14-41,
      `ArchiveSession` l. 116-120, `Refresh` l. 132-163)
    - src/Chronos/ViewModels/SessionsPreviewViewModel.cs (l. 25-50 : le SEUL autre site de construction
      de `SessionItemVm`, l. 37)
    - src/Chronos/Views/SessionsController.cs (l. 14-32 : champs et constructeur ; l. 96-104 : la
      construction du ViewModel)
    - src/Chronos/App.xaml.cs (l. 244-282 : magasins, moniteur, enregistrement de `ISessionsController`)
    - tests/Chronos.Tests/AffichageSessionsTests.cs (l. 125-135 et l. 190-200 : les deux sites
      `new SessionsViewModel(...)`)
    - tests/Chronos.Tests/SessionStylesBindingTests.cs (l. 49-63 : le troisième site)
    - src/Chronos/Services/SessionTreatmentTracker.cs (après 26-01, pour la règle NET-03)
  </read_first>

  <behavior>
    Quatre cas, dans un fichier neuf `tests/Chronos.Tests/GesteTraiteTests.cs`. **Aucun n'est annoncé
    rouge** : ils portent sur du code qui n'existe pas encore et sont écrits dans la même tâche que lui —
    le projet interdit un commit qui ne compile pas, et un test qui ne compile pas n'est pas un test rouge.

    1. `Marquer_traitee_fait_disparaitre_la_session_immediatement` — cinq sessions visibles ; exécuter
       `Items[0].MarquerTraiteeCommand` → `vm.Items` n'en contient plus que **quatre**, et l'identifiant
       retiré est présent dans `treated.Load()`.
    2. `Marquer_tout_traite_vide_le_widget_en_un_geste` — cinq sessions ; exécuter
       `Items[0].MarquerToutTraiteCommand` → `vm.Items` est **vide**, et `treated.Load()` contient les
       **cinq** identifiants. (L'effet de masse mesuré : vingt sessions ne se traitent pas une à une.)
    3. `Le_libelle_du_geste_de_masse_porte_le_nombre_de_sessions` — avec cinq sessions,
       `vm.Items[0].ToutTraiterLibelle` contient `"5"`. Un geste de masse qui ne dit pas sur combien il
       porte est exactement le genre d'ambiguïté que ce projet s'interdit.
    4. `Le_geste_est_reversible_et_le_libelle_ne_ment_pas` — moniteur muni du détecteur ; une session en
       attente à `t0`, geste explicite → masquée ; la même session réaffirme son attente avec un signal
       daté `t1 > t0` → elle **réapparaît** dans `vm.Items`, et son entrée a quitté `treated.Load()`.
       C'est la preuve littérale du libellé « revient si elle me redemande ».
  </behavior>

  <action>
    **(a) `SessionItemVm` — porter l'instant du signal et les deux nouvelles commandes.**

    ```csharp
    public string SessionId { get; }

    /// <summary>Instant que le SIGNAL de cette session portait au dernier rafraîchissement, en
    /// millisecondes. C'est ce que le geste explicite écrit dans le magasin réversible : marquer traité,
    /// c'est dire « j'ai vu CET épisode-là », pas « ne me montre plus jamais cette session ». Un signal
    /// plus récent la ramènera (NET-03).</summary>
    public long UpdatedAtMs { get; }

    private readonly System.Action<string> _archive;
    private readonly System.Action<SessionItemVm> _traiter;
    private readonly System.Action _traiterTout;

    /// <summary>Libellé du geste de masse, porté par chaque ligne parce que le menu contextuel a pour
    /// DataContext la ligne et non la liste. Il porte le NOMBRE : un geste qui agit sur vingt sessions
    /// doit le dire avant d'être cliqué.</summary>
    [ObservableProperty] private string _toutTraiterLibelle = "Tout marquer traité";

    public SessionItemVm(string sessionId, long updatedAtMs, System.Action<string> archive,
                         System.Action<SessionItemVm> traiter, System.Action traiterTout)
    {
        SessionId = sessionId;
        UpdatedAtMs = updatedAtMs;
        _archive = archive;
        _traiter = traiter;
        _traiterTout = traiterTout;
    }

    /// <summary>Clic droit → Archiver : DÉFINITIF, la session ne revient jamais (TRT-04).</summary>
    [RelayCommand] private void Archive() => _archive(SessionId);

    /// <summary>Clic droit → Marquer traitée : RÉVERSIBLE, la session revient si elle me redemande
    /// quelque chose (TRT-03).</summary>
    [RelayCommand] private void MarquerTraitee() => _traiter(this);

    /// <summary>Clic droit → Tout marquer traité : le même geste, sur toutes les lignes visibles.</summary>
    [RelayCommand] private void MarquerToutTraite() => _traiterTout();
    ```

    **(b) `SessionsViewModel` — le magasin réversible entre dans le ViewModel.**

    - Champ `private readonly TreatedStore _treated;` et constructeur
      `public SessionsViewModel(SessionMonitor monitor, IClock clock, ArchiveStore archive, TreatedStore treated)`.
    - Deux méthodes privées, sur le modèle exact d'`ArchiveSession` (l. 116-120) :

      ```csharp
      // Geste explicite, RÉVERSIBLE : on inscrit l'épisode que l'utilisateur vient de voir, pas l'instant
      // du clic. Écrire « maintenant » masquerait aussi les demandes arrivées entre-temps — et c'est
      // précisément la confusion que cette phase supprime.
      private void MarquerSessionTraitee(SessionItemVm item)
      {
          _treated.Set(item.SessionId, item.UpdatedAtMs);
          Refresh(_clock.UtcNow);
      }

      // L'effet de masse mesuré le 2026-09-12 : vingt sessions sur cinquante-quatre basculent ensemble en
      // attente déduite. Un geste par session n'en serait pas un. La copie de la collection est
      // indispensable : Refresh vide Items.
      private void MarquerToutTraite()
      {
          foreach (var it in Items.ToList()) _treated.Set(it.SessionId, it.UpdatedAtMs);
          Refresh(_clock.UtcNow);
      }
      ```

    - Dans `Refresh` (l. 141) :
      ```csharp
      var it = new SessionItemVm(s.SessionId, s.UpdatedAt.ToUnixTimeMilliseconds(),
                                 ArchiveSession, MarquerSessionTraitee, MarquerToutTraite)
               { Project = s.Project, ToutTraiterLibelle = $"Tout marquer traité ({snaps.Count})" };
      ```
      Le reste de `Refresh` **ne change pas** : ni l'ordre venu de `AffichageSessions.Ordonner`, ni les
      drapeaux, ni le compteur d'attente, ni le résumé.

    **(c) Sites d'appel — tous adaptés dans CETTE tâche** (`tests/Chronos.Tests` a un `ProjectReference`
    vers `Chronos` : une signature changée sans ses appelants casse le build) :
    - `src/Chronos/ViewModels/SessionsPreviewViewModel.cs` l. 37 →
      `new SessionItemVm(project, 0, _ => { }, _ => { }, () => { })` (prévisualisation de la galerie :
      les trois gestes y sont sans effet, et c'était déjà le cas de l'archivage).
    - `src/Chronos/Views/SessionsController.cs` : champ `private readonly TreatedStore _treated;`,
      paramètre de constructeur ajouté **en dernier**, et l. 98
      `new SessionsViewModel(_monitor, _clock, _archive, _treated)`.
    - `src/Chronos/App.xaml.cs` l. 275-280 : ajouter `sp.GetRequiredService<TreatedStore>()` en dernier
      argument de `new Views.SessionsController(...)`.
    - `tests/Chronos.Tests/AffichageSessionsTests.cs` (2 sites) et
      `tests/Chronos.Tests/SessionStylesBindingTests.cs` (1 site) : ajouter un quatrième argument
      `new TreatedStore(Path.Combine(TempDir(), "t.json"), new FakeClock(Maintenant))`.
      **Chemin temporaire obligatoire** : aucun test ne doit pouvoir viser le vrai `%APPDATA%\Chronos`.

    **(c bis) Le rapport de diagnostic ne doit pas mentir non plus.**
    `src/Chronos/Services/DiagnosticService.cs` l. 600 libelle le motif « traité » ainsi :
    `"treated.json (hystérésis « traité » — posée automatiquement)"`. À partir de cette tâche, l'entrée
    peut aussi venir d'un geste de l'utilisateur : écrire
    `"treated.json (« traité » — hystérésis automatique OU geste explicite ; réversible)"`.
    **Une seule chaîne modifiée**, sur une seule ligne : `grep -c "MotifMasquage" src/Chronos/Services/DiagnosticService.cs`
    reste à **3**, et la garde `Le_diagnostic_ne_fabrique_aucun_moniteur_de_sessions` reste verte.

    **(d) Écrire `tests/Chronos.Tests/GesteTraiteTests.cs`** avec les quatre cas du bloc `<behavior>`.
    Motif de montage, à recopier de `SessionStylesBindingTests` (l. 30-63) : `SourceFixe` en source de
    base, dossier de sessions temporaire, `FakeClock`, et **un `TreatedStore` PARTAGÉ entre le moniteur et
    le ViewModel** — sans ce partage, le filtre du moniteur ne verrait pas ce que le geste écrit. Les cas 1
    à 3 construisent le moniteur **sans** détecteur (`tracker: null`) pour isoler le geste ; le cas 4 le
    construit **avec**, puisque c'est la réversibilité NET-03 qu'il éprouve.

    **Consigne de rédaction (obligatoire).** `Services/` et `Models/` sont balayés au TEXTE par
    `NormalisationUniqueTests` — ce plan n'y touche pas, mais la consigne reste : ne jamais y écrire une
    division ou une multiplication par cent, ni `FromUnixTimeSeconds`, `FromUnixTimeMilliseconds`,
    `DateTimeOffset.TryParse`. `ViewModels/` et `Views/` ne sont pas balayés par cette garde, mais
    `ServicesLayerPurityTests` interdit toujours tout type WPF dans `Services/` et `Models/` — et rien de
    ce plan ne doit descendre dans ces dossiers. UI et commentaires en **français**.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug --nologo` : **0 erreur**.
    - `dotnet test Chronos.sln -c Debug --nologo -v q` : **0 échec**, total = **878 + 4 = 882**.
      Tout écart justifié **nominativement** au SUMMARY.
    - Les quatre cas de `GesteTraiteTests` sont **verts**, nommément listés dans la sortie
      `--filter "FullyQualifiedName~GesteTraite"`.
    - `grep -c "MarquerTraitee\b" src/Chronos/ViewModels/SessionsViewModel.cs` **≥ 1** et
      `grep -c "MarquerToutTraite" src/Chronos/ViewModels/SessionsViewModel.cs` **≥ 3**
      (la déclaration de la commande sur la ligne, la méthode du ViewModel, son passage au constructeur).
    - `grep -c "_treated.Set(" src/Chronos/ViewModels/SessionsViewModel.cs` = **2**
      (le geste unitaire, le geste de masse). `grep` compte des LIGNES : recompter sur le code écrit.
    - `grep -c "new SessionItemVm(" src/Chronos/` = **2** (`SessionsViewModel.cs`,
      `SessionsPreviewViewModel.cs`) — aucun troisième site n'a été créé.
    - `grep -c "GetFolderPath\|APPDATA" tests/Chronos.Tests/GesteTraiteTests.cs` = **0**.
    - `grep -c "MotifMasquage" src/Chronos/Services/DiagnosticService.cs` = **3** (inchangé) et
      `grep -c "posée automatiquement" src/Chronos/Services/DiagnosticService.cs` = **0**.
    - `git diff --stat 07ee784..HEAD -- src/Chronos/Resources` est **vide** : le XAML est la tâche 2.
    - Suites de gardes, **0 échec** : `ServicesLayerPurityTests`, `NormalisationUniqueTests`,
      `CompositionRootTests`, `GardesPerimetreTests`, `ReglagesBindingTests`, `AffichageSessionsTests`,
      `SessionStylesBindingTests`, `TreatedSessionsTests`, `ArchiveStorePurgeTests`.
  </acceptance_criteria>

  <done>
    L'utilisateur dispose de deux commandes : traiter cette session, ou toutes. La première écrit l'épisode
    qu'il vient de voir, pas l'instant de son clic — et c'est ce détail qui rend le geste réversible au sens
    exact où le libellé va le promettre.
  </done>
</task>

<task type="auto">
  <name>Task 2: Huit menus, trois entrées, et des libellés qui disent la vérité</name>
  <files>src/Chronos/Resources/SessionStyles.xaml, src/Chronos/Views/SessionsWindow.xaml.cs</files>

  <read_first>
    - src/Chronos/Resources/SessionStyles.xaml (intégral, 383 l. — les huit blocs `<ContextMenu>` :
      l. 54-58 Pastilles, 86-90 Marge, 123-127 Jetons, 163-167 Sonar, 214-218 Facade, 243-247 Etagere,
      311-315 Annonciateur, 343-347 Veilleurs)
    - src/Chronos/Views/SessionsWindow.xaml.cs (l. 7-11 : la XML-doc de tête annonce « clic droit sur une
      pastille = Archiver » — elle devient fausse)
    - src/Chronos/ViewModels/SessionsViewModel.cs (après la tâche 1, pour les noms EXACTS des commandes)
  </read_first>

  <action>
    **(a)** Remplacer le contenu des **huit** blocs `<ContextMenu>` par ce bloc, à l'identique, **une
    entrée par ligne** (les critères d'acceptation comptent des LIGNES) :

    ```xml
    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
        <MenuItem Header="Marquer traitée (revient si elle me redemande)" Command="{Binding MarquerTraiteeCommand}"/>
        <MenuItem Header="{Binding ToutTraiterLibelle}" Command="{Binding MarquerToutTraiteCommand}"/>
        <Separator/>
        <MenuItem Header="Archiver définitivement (ne revient jamais)" Command="{Binding ArchiveCommand}"/>
    </ContextMenu>
    ```

    L'ordre et le séparateur ne sont pas décoratifs. Le verrou UI de ce projet : `LoginClaudeCommand`
    bascule, et un clic effacerait le coffre de jetons ; `ReconnecterCommand` est la commande dédiée. On
    applique la même prudence — **le geste réversible est en tête, le geste définitif est en queue, séparé,
    et son libellé dit ce qu'il fait.** Aucun des trois libellés n'est ambigu : chacun annonce s'il revient
    ou non.

    Les DEUX anciens libellés disparaissent : « Archiver (retirer de l'overlay) » (Pastilles, Marge) et
    « Archiver » seul (les six autres). Le premier promettait déjà, dans son infobulle de menu, ce que le
    code ne tenait pas ; c'est exactement ce que le plan 26-02 vient de rendre vrai côté magasin.

    **(b)** Ne toucher à **rien d'autre** dans ce fichier : ni les huit `DataTemplate`, ni les
    `DataTrigger`, ni les `Storyboard`, ni les tailles, marges et polices. La compacité du widget est un
    acquis, et un menu contextuel ne participe pas à la mise en page (il n'est pas mesuré tant qu'il n'est
    pas ouvert) — c'est précisément pourquoi ce geste passe par là.

    **(c)** `src/Chronos/Views/SessionsWindow.xaml.cs` l. 7-11 : corriger la XML-doc de tête, qui annonce
    « clic droit sur une pastille = Archiver ». Écrire ce qui est : clic droit = marquer traitée (réversible),
    tout marquer traité, ou archiver définitivement. Aucun changement de code dans ce fichier.

    **Consigne de rédaction (obligatoire).** `Services/` et `Models/` ne sont pas touchés par cette tâche ;
    la consigne de non-écriture des motifs interdits y reste valable pour les tâches suivantes. UI en
    français, accents compris — les libellés sont lus par l'utilisateur.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~SessionStylesBinding|FullyQualifiedName~GardesPerimetre"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug --nologo` : **0 erreur** (un XAML mal formé casse le build).
    - `dotnet test Chronos.sln -c Debug --nologo -v q` : **0 échec**, total **882** — cette tâche
      **n'ajoute aucun test** (les gardes sont la tâche 3).
    - `Les_8_styles_et_les_9_themes_se_chargent_se_mesurent_et_se_disposent` est **VERT** : aucune des 72
      combinaisons n'a de taille dégénérée. C'est la preuve non destructive que la compacité tient.
    - `Le_style_Pastilles_n_a_plus_qu_un_seul_separateur_visible_par_session` est **VERT** : le
      `<Separator/>` du menu n'est pas un `TextBlock` et ne peut pas être compté par ce test — vérifier que
      le compte reste **5**.
    - Comptes exacts dans `src/Chronos/Resources/SessionStyles.xaml` (`grep -c`, donc des LIGNES) :
      `<MenuItem` = **24** ; `<Separator/>` = **8** ; `<ContextMenu ` = **8** ;
      `MarquerTraiteeCommand` = **8** ; `MarquerToutTraiteCommand` = **8** ; `ArchiveCommand` = **8** ;
      `ToutTraiterLibelle` = **8** ; `ne revient jamais` = **8** ;
      `retirer de l'overlay` = **0** ; `DataTemplate x:Key=` = **8** (inchangé).
    - `Aucun_style_de_session_ne_binde_plus_un_libelle_de_type` (garde phase 21) reste **VERT** : les huit
      `DataTemplate` sont toujours là et `KindLabel` n'est jamais réapparu.
    - `git diff --stat 07ee784..HEAD -- src/Chronos/Services` ne montre **que** les fichiers des plans
      26-01 et 26-02 : aucune logique n'est descendue dans la couche neutre par ce geste d'interface.
  </acceptance_criteria>

  <done>
    Les huit menus offrent le même triptyque : traiter celle-ci, tout traiter, archiver définitivement. Le
    réversible est en tête, le définitif en queue derrière un séparateur, et chacun annonce s'il revient.
    Le menu ne promet plus rien que le code ne tienne.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deux gardes — le câblage des huit menus, et la matrice 8 × 9</name>
  <files>tests/Chronos.Tests/GardesPerimetreTests.cs, tests/Chronos.Tests/SessionStylesBindingTests.cs</files>

  <read_first>
    - tests/Chronos.Tests/GardesPerimetreTests.cs (l. 54-74 :
      `Aucun_style_de_session_ne_binde_plus_un_libelle_de_type`, le motif EXACT de contrôle de SOURCE du
      XAML, avec son assertion anti-muette sur les huit `DataTemplate`)
    - tests/Chronos.Tests/SessionStylesBindingTests.cs (intégral, 137 l. — `Monter` l. 72-85 et
      `TextesVisibles` l. 87-97 : le parcours d'arbre visuel à recopier)
    - src/Chronos/Resources/SessionStyles.xaml (tel que laissé par la tâche 2)
  </read_first>

  <behavior>
    Deux cas ajoutés. **Aucun n'est annoncé rouge** : la tâche 2 a déjà livré le câblage qu'ils gardent.
    Leur valeur est d'empêcher la PROCHAINE dérive, comme la garde de la phase 21 sur `KindLabel`.

    1. `Les_huit_menus_offrent_le_geste_explicite_et_disent_la_verite` (dans `GardesPerimetreTests`) —
       contrôle de SOURCE sur `SessionStyles.xaml`, parce que la réflexion ne voit pas un binding XAML.
       Assertion anti-muette d'abord : **8** occurrences de `DataTemplate x:Key=`. Puis les comptes de la
       tâche 2 : `<MenuItem` = 24, `MarquerTraiteeCommand` = 8, `MarquerToutTraiteCommand` = 8,
       `ArchiveCommand` = 8, `<Separator/>` = 8, et `Assert.DoesNotContain("retirer de l'overlay", …)`.
       **Pourquoi cette garde et pas seulement la matrice :** une commande peut être parfaitement testée
       et n'être liée nulle part ; le défaut ne se verrait alors que chez l'utilisateur, en silence — c'est
       le motif exact des gardes de câblage des phases 21 et 23.
    2. `Les_trois_gestes_sont_offerts_sur_les_8_styles_et_les_9_themes` (dans
       `SessionStylesBindingTests`, `[WpfFact]`, collection « XAML WPF ») — pour chacune des 72
       combinaisons : monter la fenêtre comme le fait déjà `Monter`, parcourir l'arbre VISUEL, collecter
       les `FrameworkElement.ContextMenu` non nuls, et exiger que **chacun** porte exactement **3**
       `MenuItem`. Anti-muet : au moins **un** menu trouvé par style, et le nombre de styles ayant produit
       au moins un menu vaut **8**.
  </behavior>

  <action>
    **(a) `tests/Chronos.Tests/GardesPerimetreTests.cs`** — ajouter le test 1 en réutilisant
    `CheminSources()` et `System.Text.RegularExpressions.Regex.Matches(texte, …).Count`, exactement comme
    le fait `Aucun_style_de_session_ne_binde_plus_un_libelle_de_type`. Ne **pas** modifier les tests
    existants de ce fichier.

    **(b) `tests/Chronos.Tests/SessionStylesBindingTests.cs`** — ajouter le parcours et le test 2 :

    ```csharp
    // Parcourt l'arbre VISUEL et rend les menus contextuels attachés. Un ContextMenu n'est PAS dans
    // l'arbre visuel de la fenêtre (il a le sien, créé à l'ouverture) : il est la VALEUR d'une propriété,
    // et c'est là qu'on va le chercher — ce qui permet de le vérifier sans jamais ouvrir de menu, donc
    // sans afficher quoi que ce soit.
    private static IEnumerable<ContextMenu> MenusContextuels(DependencyObject racine)
    {
        var n = VisualTreeHelper.GetChildrenCount(racine);
        for (var i = 0; i < n; i++)
        {
            var enfant = VisualTreeHelper.GetChild(racine, i);
            if (enfant is FrameworkElement fe && fe.ContextMenu is { } menu) yield return menu;
            foreach (var petit in MenusContextuels(enfant)) yield return petit;
        }
    }
    ```

    et le test, sur le modèle de la boucle existante l. 108-119 :

    ```csharp
    [WpfFact]
    public void Les_trois_gestes_sont_offerts_sur_les_8_styles_et_les_9_themes()
    {
        var styles = Enum.GetValues<SessionStyle>();
        var themes = ThemeCatalog.All;
        Assert.Equal(8, styles.Length);
        Assert.Equal(9, themes.Count);

        var stylesCouverts = new HashSet<SessionStyle>();

        foreach (var theme in themes)
        foreach (var style in styles)
        {
            var vm = Vm();
            vm.Style = style;
            vm.SetTheme(theme);
            var (_, racine) = Monter(vm, theme);

            var menus = MenusContextuels(racine).ToList();
            Assert.True(menus.Count >= 1,
                $"aucun menu contextuel pour le style {style} et le thème {theme.Key} : le geste explicite "
                + "n'y est pas offert");
            foreach (var menu in menus)
                Assert.Equal(3, menu.Items.OfType<MenuItem>().Count());
            stylesCouverts.Add(style);
        }

        Assert.Equal(8, stylesCouverts.Count);   // la matrice n'a pas été traversée à vide
    }
    ```

    **Note d'exécution.** Si un style ne produisait AUCUN menu (gabarit dont la racine d'élément ne porte
    pas de `ContextMenu`), **ne pas affaiblir l'assertion** : c'est un défaut de la tâche 2, et le menu
    manquant doit être ajouté au gabarit concerné. Si à l'inverse le nombre de menus par style diffère de
    cinq (cinq lignes, cinq menus), **ne rien en conclure et ne rien asserter dessus** : le comptage exact
    dépend de la réalisation des conteneurs, et l'assertion qui compte est « chaque menu trouvé porte
    trois entrées ». Reporter le nombre MESURÉ au SUMMARY.

    Ce fichier est déjà dans la collection `[Collection("XAML WPF")]` : le chargeur BAML n'est pas sûr en
    accès concurrent, ne pas créer une seconde classe de test WPF pour ces cas.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -c Debug --nologo -v q` : **0 échec**, total = **882 + 2 = 884**.
      Tout écart justifié **nominativement** au SUMMARY.
    - Les deux nouveaux tests sont **verts dès leur écriture** — et c'est annoncé : ce sont des gardes, pas
      des tests de développement.
    - `Les_8_styles_et_les_9_themes_se_chargent_se_mesurent_et_se_disposent` et
      `Le_style_Pastilles_n_a_plus_qu_un_seul_separateur_visible_par_session` restent **verts**.
    - `grep -c "MenusContextuels" tests/Chronos.Tests/SessionStylesBindingTests.cs` = **3**
      (la déclaration, l'appel récursif, l'appel dans le test).
    - `grep -c "\[Fact\]" tests/Chronos.Tests/GardesPerimetreTests.cs` = **11** (10 à l'entrée de phase,
      plus celui-ci). Mesurer la valeur d'entrée avant d'écrire et la reporter si elle diffère de 10.
    - La suite complète tourne en **≤ 8 s** (elle en prend 4 à 6 à l'entrée de phase ; les 72 montages
      supplémentaires sont le poste le plus coûteux de la phase — si le seuil est franchi, le dire au
      SUMMARY plutôt que de retirer des combinaisons).
    - Aucun test n'affiche de fenêtre : `grep -c "\.Show()\|ShowDialog" tests/Chronos.Tests/SessionStylesBindingTests.cs` = **0**.
  </acceptance_criteria>

  <done>
    Le geste explicite est tenu par deux gardes complémentaires : l'une lit le texte du XAML et compte le
    câblage, l'autre monte les 72 combinaisons et vérifie que chaque ligne, dans chaque style et chaque
    thème, offre bien les trois entrées. Ni l'une ni l'autre n'ouvre de fenêtre.
  </done>
</task>

</tasks>

<verification>
- `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, total **884**, suite ≤ 8 s. Deux
  exécutions consécutives reportées au SUMMARY.
- Les trois entrées de menu existent sur les **8** styles et les **9** thèmes, prouvé par une garde de
  SOURCE (le câblage) et une garde d'ARBRE VISUEL (les 72 combinaisons montées). La compacité est prouvée
  par la non-régression de la matrice de mise en page.
- Aucun libellé ambigu ne subsiste : chacun des trois annonce s'il revient ou non.
- L'overlay (pid 119412) n'est **ni lancé ni tué** ; aucun test n'affiche de fenêtre.
- Aucun test n'écrit dans le vrai `%APPDATA%\Chronos\` : `sessions\`, `archived.json` et `treated.json`
  sont intouchés. Le dossier `sessions\` bouge tout seul (hooks v3.0.2 vivants) — ne pas le « réparer ».
- `oauth.dat` n'est pas approché. Aucune requête réseau. Aucune dépendance NuGet ajoutée.
</verification>

<success_criteria>
- Un geste explicite existe, il fait disparaître la session immédiatement, et il est offert sur les 8
  styles et les 9 thèmes sans casser la compacité (TRT-03).
- Un widget saturé se vide en UN geste, et ce geste dit sur combien de sessions il porte (TRT-03,
  effet de masse).
- Le geste ordinaire est réversible et l'annonce ; le geste définitif est séparé et l'annonce (TRT-03).
- Le libellé « Archiver » dit enfin la vérité (TRT-04, volet UI).
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/26-traite-veut-enfin-dire-quelque-chose/26-03-SUMMARY.md`.
</output>
