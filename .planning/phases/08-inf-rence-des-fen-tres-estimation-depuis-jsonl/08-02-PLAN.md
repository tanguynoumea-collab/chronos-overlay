---
phase: 08-inf-rence-des-fen-tres-estimation-depuis-jsonl
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/Chronos/Services/JsonlEstimationProvider.cs
  - src/Chronos/Services/IUsageProvider.cs
  - src/Chronos/Services/ClaudeUsageObjectProvider.cs
  - src/Chronos/Services/CompositeUsageProvider.cs
  - src/Chronos/Models/UsageSnapshot.cs
  - tests/Chronos.Tests/JsonlEstimationProviderTests.cs
  - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs
  - tests/Chronos.Tests/CompositeUsageProviderTests.cs
  - tests/Chronos.Tests/Fakes/FakeUsageProvider.cs
autonomous: true
requirements: [EST-01, EST-02, EST-03, EST-04, EST-05, NET-01]

must_haves:
  truths:
    - "En repli JSONL, une fenêtre 5 h active porte ResetsAt=début+5h et FractionTimeRemaining calculé, marquée Estimated (arc retrouve une longueur)"
    - "Sans activité dans la fenêtre courante, la fenêtre 5 h est pleine : FractionTimeRemaining=1, EstimatedTokens=0 (jamais un arc vide)"
    - "Utilization 5 h/hebdo = tokens de la fenêtre inférée / plafond quand le budget est défini ; null sans budget (couleur neutre v1.0), sans clamp haut"
    - "La somme 5 h ne compte QUE la fenêtre inférée [start, now] ; la somme hebdo utilise WeeklyWindow.CurrentStart (ancrée ou 7 j glissants)"
    - "IUsageProvider.SnapshotChanged et UsageSnapshot.Age sont retirés ; la suite complète reste verte"
    - "Le reset hebdo reste géré par WeeklyRecalibration côté VM (provider laisse SevenDay.ResetsAt=null) — non-régression EST-05"
  artifacts:
    - path: "src/Chronos/Services/JsonlEstimationProvider.cs"
      provides: "GetAsync enrichi : collecte (ts,tokens) une passe, inférence 5 h, sommes bornées, utilization par plafonds, SettingsService injecté"
      contains: "FiveHourWindowInference.InferWindowStart"
    - path: "src/Chronos/Services/IUsageProvider.cs"
      provides: "Contrat épuré sans SnapshotChanged"
      contains: "Task<UsageSnapshot> GetAsync"
    - path: "src/Chronos/Models/UsageSnapshot.cs"
      provides: "Snapshot sans champ Age (staleness dérivée de SourceCapturedAt)"
      contains: "SourceCapturedAt"
  key_links:
    - from: "src/Chronos/Services/JsonlEstimationProvider.cs"
      to: "FiveHourWindowInference.InferWindowStart"
      via: "appel après tri des timestamps collectés"
      pattern: "InferWindowStart"
    - from: "src/Chronos/Services/JsonlEstimationProvider.cs"
      to: "SettingsService.Load()"
      via: "lecture fraîche des plafonds à chaque GetAsync"
      pattern: "_settings\\.Load\\(\\)"
    - from: "src/Chronos/Services/JsonlEstimationProvider.cs"
      to: "WeeklyWindow.CurrentStart"
      via: "borne de somme hebdo ancrée"
      pattern: "WeeklyWindow\\.CurrentStart"
---

<objective>
Rendre le repli JSONL réellement utile et nettoyer le contrat, en consommant la logique pure de 08-01.

Deux tâches, chacune build-green :
1. Enrichir `JsonlEstimationProvider.GetAsync` : une seule passe disque matérialisant `(timestamp, tokens)`, tri, inférence de la fenêtre 5 h (`InferWindowStart`), sommes bornées à la fenêtre courante, utilization estimée par plafonds (`SettingsService` injecté, `Load()` frais), fenêtre hebdo ancrée (`WeeklyWindow`). Adapter les tests JSONL (les champs 5 h ne sont plus null en fenêtre active).
2. NET-01 : retirer l'événement mort `IUsageProvider.SnapshotChanged` (contrat + 3 providers + fakes + test) et le champ inerte `UsageSnapshot.Age` (modèle + 3 providers + test), suite verte.

Purpose: les arcs retrouvent longueur (EST-01/02) et couleur si plafond (EST-03/04) tout en restant « estimée » ; le contrat cesse de porter du code mort.
Output: provider enrichi, contrat épuré, tests adaptés — 107 tests (moins 1 retiré + assertions adaptées) verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-CONTEXT.md
@.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-RESEARCH.md
@.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-01-SUMMARY.md

<interfaces>
<!-- Contrats livrés par 08-01 (Wave 1) + existants — pas d'exploration nécessaire. -->

Livré par 08-01 :
```csharp
public static class FiveHourWindowInference {
    public static readonly TimeSpan Window;                                 // 5 h
    public static DateTimeOffset? InferWindowStart(IReadOnlyList<DateTimeOffset> tsAsc, DateTimeOffset now);
}
public static class WeeklyWindow {
    public static DateTimeOffset CurrentStart(DateTimeOffset? anchor, DateTimeOffset now);
}
// ChronosSettings.FiveHourTokenBudget (long?), ChronosSettings.WeeklyTokenBudget (long?)
```

Existants (à réutiliser tels quels) :
```csharp
// src/Chronos/Models/WindowState.cs
public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength); // clampé [0..1], null si reset null
public bool Exhausted => Utilization is >= 1.0;                             // gris épuisé géré en aval — NE PAS clamper l'utilization à 1

// src/Chronos/Services/SettingsService.cs
public ChronosSettings Load();                                             // tolérant → défauts

// src/Chronos/Services/ChronosPaths.cs
public sealed record ChronosPaths(string UsageFile, string ProjectsRoot);
public string SettingsFile => Path.Combine(Path.GetDirectoryName(UsageFile)!, "settings.json");
```

DI : `services.AddSingleton<JsonlEstimationProvider>()` (App.xaml.cs:72) résout le ctor par réflexion ;
`SettingsService` est déjà enregistré (App.xaml.cs:57). Ajouter le paramètre au ctor SUFFIT — aucune
modification d'App.xaml.cs requise (vérifier le build WPF malgré tout).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Enrichir JsonlEstimationProvider (inférence + sommes bornées + utilization)</name>
  <files>src/Chronos/Services/JsonlEstimationProvider.cs, tests/Chronos.Tests/JsonlEstimationProviderTests.cs</files>
  <read_first>
    - src/Chronos/Services/JsonlEstimationProvider.cs (GetAsync actuel, EnumerateJsonl, SumUsageTokens, IsAssistant)
    - tests/Chronos.Tests/JsonlEstimationProviderTests.cs (Now=2026-07-08T12:00:00Z, ProviderFor, fixtures sample-valid/sample-tolerant)
    - tests/Chronos.Tests/TestData/sample-valid.jsonl (timestamps 11:30 / 07-05 / 06-01)
    - 08-RESEARCH.md § Code Example 1/2/3, § Q2/Q3/Q4, § Pitfall 1/2/3/5
  </read_first>
  <behavior>
    - Fenêtre 5 h ACTIVE (sample-valid, now=12:00, dernier msg 11:30) : EstimatedTokens=1550 (inchangé), ResetsAt=16:30, FractionTimeRemaining≈0.9, Utilization=null (aucun plafond), Reliability=Estimated.
    - Fenêtre 5 h avec plafond : construire un ChronosPaths dont le SettingsFile pointe un dossier temp où l'on écrit `{"FiveHourTokenBudget":3100}` → Utilization=1550/3100=0.5 ; sans plafond → null. (Pas de clamp haut : un budget < tokens donne util > 1.)
    - Fenêtre 5 h INACTIVE (fixture nouvelle : dernier message > 5 h avant now) : FractionTimeRemaining=1.0, EstimatedTokens=0, ResetsAt=null, Utilization = plafond? 0.0 : null.
    - Somme hebdo : sans WeeklyAnchor → 7 j glissants (sample-valid : 1550+600=2150, inchangé) ; SevenDay.ResetsAt reste null (rempli par le VM — non-régression EST-05).
    - Non-régression : sample-tolerant (700), subagents (800), dossier absent (0) restent verts.
  </behavior>
  <action>
    Injecter SettingsService et refactorer GetAsync (structure VERBATIM du RESEARCH § Code Example 1) :

    1. Ctor : `public JsonlEstimationProvider(ChronosPaths paths, IClock clock, SettingsService settings)` et stocker `_settings`. (DI et le ctor résolvent automatiquement — voir <interfaces>.)

    2. Dans GetAsync : au lieu d'accumuler `five/week`, collecter en une passe :
    ```csharp
    var now = _clock.UtcNow;
    var settings = _settings.Load();                         // Q4 : frais à chaque refresh
    var entries = new List<(DateTimeOffset Ts, long Tokens)>();
    // ... boucle de lecture streaming tolérante INCHANGÉE (FileShare.ReadWrite, try/catch JsonException,
    //     IsAssistant, TryParse RoundtripKind) — remplacer `five += ...; week += ...;` par :
    if (when <= now) entries.Add((when, tokens));            // Pitfall 3 : filtrer timestamps futurs
    // Retirer entièrement l'ancien `if (when >= now - 5h)` / `if (when >= now - 7d)` (Pitfall 2).
    ```
    Recommandé (perf, § Q « mtime ») : dans EnumerateJsonl, ne garder que les fichiers `LastWriteTimeUtc >= now - TimeSpan.FromDays(8)` — un fichier append-only plus vieux ne contient pas de message < 7 j. Ne pas casser les fixtures (copiées → mtime récente).

    3. Après la boucle, calculer en mémoire :
    ```csharp
    entries.Sort((a, b) => a.Ts.CompareTo(b.Ts));
    var tsAsc = entries.Select(e => e.Ts).ToList();
    var start = FiveHourWindowInference.InferWindowStart(tsAsc, now);

    var five  = BuildFiveHour(entries, start, now, settings.FiveHourTokenBudget);
    var seven = BuildSevenDay(entries, settings.WeeklyAnchor, now, settings.WeeklyTokenBudget);

    return new UsageSnapshot { FiveHour = five, SevenDay = seven, SourceCapturedAt = now };
    // NB : `Age = TimeSpan.Zero` retiré ici — Age est supprimé du modèle en Task 2 ; à ce stade,
    // GARDER `Age = TimeSpan.Zero` si le champ existe encore pour rester build-green, Task 2 le retirera.
    ```
    Garder pour l'instant l'événement `SnapshotChanged` + son `Invoke` (retirés en Task 2, atomiquement avec le contrat).

    4. Ajouter les helpers privés statiques (VERBATIM RESEARCH § Code Example 2 et 3) :
    ```csharp
    private static WindowState BuildFiveHour(
        List<(DateTimeOffset Ts, long Tokens)> entries, DateTimeOffset? start, DateTimeOffset now, long? budget)
    {
        if (start is null)                                        // EST-02 : inactive → arc plein
            return new WindowState {
                Kind = WindowKind.FiveHour, EstimatedTokens = 0,
                Utilization = budget is > 0 ? 0.0 : null,
                ResetsAt = null, FractionTimeRemaining = 1.0,
                Reliability = SourceReliability.Estimated };

        var reset = start.Value + FiveHourWindowInference.Window;
        long tokens = entries.Where(e => e.Ts >= start.Value && e.Ts <= now).Sum(e => e.Tokens);
        return new WindowState {
            Kind = WindowKind.FiveHour, EstimatedTokens = tokens,
            Utilization = budget is > 0 ? Math.Max(0.0, (double)tokens / budget.Value) : null,   // EST-03, pas de clamp haut
            ResetsAt = reset,                                                                     // EST-01
            FractionTimeRemaining = WindowState.FractionRemaining(reset, now, FiveHourWindowInference.Window),
            Reliability = SourceReliability.Estimated };
    }

    private static WindowState BuildSevenDay(
        List<(DateTimeOffset Ts, long Tokens)> entries, DateTimeOffset? anchor, DateTimeOffset now, long? budget)
    {
        var start = WeeklyWindow.CurrentStart(anchor, now);                                       // EST-04
        long tokens = entries.Where(e => e.Ts >= start && e.Ts <= now).Sum(e => e.Tokens);
        return new WindowState {
            Kind = WindowKind.SevenDay, EstimatedTokens = tokens,
            Utilization = budget is > 0 ? Math.Max(0.0, (double)tokens / budget.Value) : null,   // EST-04
            ResetsAt = null,                          // EST-05 : rempli par WeeklyRecalibration côté VM
            FractionTimeRemaining = null,
            Reliability = SourceReliability.Estimated };
    }
    ```
    Supprimer l'ancien helper `EstimatedWindow` s'il n'est plus référencé.

    5. Adapter JsonlEstimationProviderTests :
    - `Valide_somme_par_fenetre_et_marque_Estimated` : la boucle `foreach` asserte null sur les DEUX fenêtres. La SÉPARER :
      * FiveHour : `Assert.Equal(1550L, snap.FiveHour.EstimatedTokens)` (inchangé) ; `Assert.Equal(new DateTimeOffset(2026,07,08,16,30,00,TimeSpan.Zero), snap.FiveHour.ResetsAt)` ; `Assert.NotNull(snap.FiveHour.FractionTimeRemaining)` et ≈0.9 (précision) ; `Assert.Null(snap.FiveHour.Utilization)` (aucun plafond) ; `Estimated`.
      * SevenDay : reste `Assert.Null(Utilization/ResetsAt/FractionTimeRemaining)` + `Estimated` (rempli par le VM).
    - Ajouter un `[Fact]` utilization AVEC plafond : construire un dossier temp, y copier `sample-valid.jsonl` ET écrire un `settings.json` colocalisé avec le UsageFile (`{"FiveHourTokenBudget":3100}`), instancier le provider avec ce ChronosPaths → `Assert.Equal(0.5, snap.FiveHour.Utilization!.Value, 9)`.
    - Ajouter une fixture `sample-inactive.jsonl` (un seul message assistant à 2026-07-08T06:00:00Z, soit 6 h avant now) + un `[Fact]` : `FractionTimeRemaining==1.0`, `EstimatedTokens==0`, `ResetsAt==null`.
    - `ProviderFor` : ajouter le 3e argument `new SettingsService(new ChronosPaths(...))` (SettingsFile absent → plafonds null, cohérent avec les assertions Utilization null existantes).
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests -c Debug --filter JsonlEstimation</automated>
  </verify>
  <acceptance_criteria>
    - Fenêtre 5 h active : ResetsAt=16:30 et FractionTimeRemaining≈0.9 peuplés ; somme=1550.
    - Utilization=0.5 avec plafond 3100 ; null sans plafond ; pas de clamp haut.
    - Fenêtre inactive : Fraction=1, tokens=0, ResetsAt=null.
    - SevenDay.ResetsAt reste null (VM le remplira) ; sommes hebdo/tolerant/subagents/dossier-absent inchangées.
    - Build WPF vert sans toucher App.xaml.cs (DI auto-résout le nouveau paramètre).
  </acceptance_criteria>
  <done>Le provider infère la fenêtre 5 h, borne ses sommes à la fenêtre courante et colore selon les plafonds, tout restant Estimated.</done>
</task>

<task type="auto">
  <name>Task 2 : NET-01 — retirer SnapshotChanged (contrat + providers) et UsageSnapshot.Age</name>
  <files>src/Chronos/Services/IUsageProvider.cs, src/Chronos/Services/JsonlEstimationProvider.cs, src/Chronos/Services/ClaudeUsageObjectProvider.cs, src/Chronos/Services/CompositeUsageProvider.cs, src/Chronos/Models/UsageSnapshot.cs, tests/Chronos.Tests/Fakes/FakeUsageProvider.cs, tests/Chronos.Tests/CompositeUsageProviderTests.cs, tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs</files>
  <read_first>
    - 08-RESEARCH.md § Runtime State Inventory (liste ligne à ligne SnapshotChanged / Age)
    - src/Chronos/Services/IUsageProvider.cs, CompositeUsageProvider.cs, ClaudeUsageObjectProvider.cs
    - src/Chronos/Models/UsageSnapshot.cs
    - tests/Chronos.Tests/CompositeUsageProviderTests.cs (test `SnapshotChanged_emis_une_fois...` + FakeProvider interne)
    - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs (test `Valide_mappe_utilization_reset_et_age`, assertion Age ligne 56)
  </read_first>
  <action>
    RETRAIT DE SnapshotChanged (event mort — jamais souscrit ; l'orchestrateur porte le sien, à NE PAS toucher) :
    - IUsageProvider.cs : supprimer la ligne `event EventHandler<UsageSnapshot>? SnapshotChanged;` (+ xmldoc).
    - JsonlEstimationProvider.cs : supprimer la déclaration de l'event + `SnapshotChanged?.Invoke(this, snap);` (le `return snap;` subsiste).
    - ClaudeUsageObjectProvider.cs : supprimer la déclaration de l'event + le `SnapshotChanged?.Invoke(this, snap);`.
    - CompositeUsageProvider.cs : supprimer la déclaration de l'event + le `SnapshotChanged?.Invoke(this, snap);`.
    - tests/Fakes/FakeUsageProvider.cs : supprimer `public event EventHandler<UsageSnapshot>? SnapshotChanged;` et l'appel `SnapshotChanged?.Invoke(this, Next);` dans GetAsync.
    - CompositeUsageProviderTests.cs : SUPPRIMER entièrement le test `SnapshotChanged_emis_une_fois_avec_le_snapshot_final` ; dans le `FakeProvider` interne, retirer l'event et le bloc `#pragma warning disable/restore CS0067`.
    - NE PAS toucher RefreshOrchestrator.SnapshotChanged ni MainViewModel (event DISTINCT, vivant).

    RETRAIT DE UsageSnapshot.Age (champ inerte — IsStale dérive de SourceCapturedAt) :
    - UsageSnapshot.cs : supprimer la propriété `TimeSpan? Age` (+ xmldoc).
    - ClaudeUsageObjectProvider.cs : supprimer `Age = capturedAt is null ? null : _clock.UtcNow - capturedAt,` (garder `SourceCapturedAt = capturedAt,`).
    - CompositeUsageProvider.cs : supprimer `Age = p.Age ?? f.Age,` (garder `SourceCapturedAt = p.SourceCapturedAt ?? f.SourceCapturedAt,`).
    - JsonlEstimationProvider.cs : supprimer `Age = TimeSpan.Zero,` du `new UsageSnapshot` (si encore présent après Task 1).
    - ClaudeUsageObjectProviderTests.cs : dans `Valide_mappe_utilization_reset_et_age`, retirer `Assert.Equal(TimeSpan.FromSeconds(30), snap.Age);` (ligne ~56) ; CONSERVER `Assert.Equal(..., snap.SourceCapturedAt)`. Renommer le test en `Valide_mappe_utilization_reset_et_capturedAt` et ajuster le commentaire de classe qui mentionne « Age ».
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L "SnapshotChanged" src/Chronos/Services/IUsageProvider.cs` renvoie le fichier (symbole absent du contrat).
    - Aucune occurrence de `Age` dans UsageSnapshot.cs ni d'affectation `Age =` dans les 3 providers.
    - RefreshOrchestrator.SnapshotChanged et MainViewModel intacts (pipeline temps réel non cassé).
    - Suite complète verte : 107 tests moins le test SnapshotChanged retiré, assertions Age/5 h adaptées.
  </acceptance_criteria>
  <done>Le contrat IUsageProvider et UsageSnapshot sont épurés du code mort ; la suite complète est verte.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` vert après chaque tâche (chaque tâche est build-green isolément).
- `dotnet test Chronos.sln -c Debug` : suite complète verte en fin de plan.
- Non-régression EST-05 : WeeklyRecalibration inchangé, WeeklyRecalibrationTests verts ; sans ancre, SevenDay.ResetsAt reste null (countdown « — »).
- Pureté : ServicesLayerPurityTests verte (le provider n'introduit aucun type WPF).
</verification>

<success_criteria>
- EST-01 : arc 5 h retrouve une longueur (ResetsAt=début+5h, Fraction calculé), marquée Estimated.
- EST-02 : fenêtre inactive → arc plein (Fraction=1, tokens=0), jamais vide.
- EST-03/04 : utilization = tokens fenêtre / plafond quand budget défini, null sinon (pas de clamp haut).
- EST-05 : reset hebdo via WeeklyRecalibration/WeeklyAnchor inchangé (non-régression).
- NET-01 : SnapshotChanged et Age retirés, suite complète verte.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-02-SUMMARY.md`.
</output>
