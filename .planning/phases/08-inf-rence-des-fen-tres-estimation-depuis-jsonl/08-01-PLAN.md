---
phase: 08-inf-rence-des-fen-tres-estimation-depuis-jsonl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/FiveHourWindowInference.cs
  - src/Chronos/Services/WeeklyWindow.cs
  - src/Chronos/Services/ChronosSettings.cs
  - tests/Chronos.Tests/FiveHourWindowInferenceTests.cs
  - tests/Chronos.Tests/WeeklyWindowTests.cs
  - tests/Chronos.Tests/SettingsServiceTests.cs
autonomous: true
requirements: [EST-01, EST-02, EST-03, EST-04]

must_haves:
  truths:
    - "InferWindowStart renvoie le début de la fenêtre 5 h courante = plus ancien message sans trou ≥ 5 h en remontant, ou null si expirée/inactive"
    - "WeeklyWindow.CurrentStart calcule la borne de fenêtre hebdo ancrée [ancre+k·7j] si ancre, sinon 7 j glissants"
    - "ChronosSettings persiste FiveHourTokenBudget/WeeklyTokenBudget (long?, null par défaut) et les relit en round-trip"
    - "Les classes d'inférence sont pures (aucun type WPF) — ServicesLayerPurityTests reste verte"
  artifacts:
    - path: "src/Chronos/Services/FiveHourWindowInference.cs"
      provides: "Fonction pure InferWindowStart(tsAsc, now) + constante Window = 5 h"
      contains: "public static DateTimeOffset? InferWindowStart"
    - path: "src/Chronos/Services/WeeklyWindow.cs"
      provides: "Fonction pure CurrentStart(anchor, now) de la fenêtre hebdo ancrée"
      contains: "public static DateTimeOffset CurrentStart"
    - path: "src/Chronos/Services/ChronosSettings.cs"
      provides: "Deux plafonds long? nullable par défaut"
      contains: "FiveHourTokenBudget"
    - path: "tests/Chronos.Tests/FiveHourWindowInferenceTests.cs"
      provides: "Couverture EST-01/EST-02 (vide, unique <5h/≥5h, trou exact 5h, activité continue >5h, timestamps futurs)"
      min_lines: 40
  key_links:
    - from: "tests/Chronos.Tests/FiveHourWindowInferenceTests.cs"
      to: "FiveHourWindowInference.InferWindowStart"
      via: "appel direct avec now déterministe"
      pattern: "InferWindowStart"
    - from: "tests/Chronos.Tests/SettingsServiceTests.cs"
      to: "ChronosSettings.FiveHourTokenBudget / WeeklyTokenBudget"
      via: "round-trip Save/Load"
      pattern: "TokenBudget"
---

<objective>
Poser la LOGIQUE PURE et les SETTINGS de la phase 8, sans toucher au provider (qui les consommera en 08-02).

Trois briques indépendantes du disque, toutes testables en `[Fact]` avec un `now` déterministe :
1. `FiveHourWindowInference.InferWindowStart` — inférence du début de la fenêtre 5 h courante (EST-01/EST-02, algorithme « A » verrouillé).
2. `WeeklyWindow.CurrentStart` — borne de fenêtre hebdo ancrée `[ancre+k·7j]` ou 7 j glissants (math EST-04).
3. `ChronosSettings.FiveHourTokenBudget` / `WeeklyTokenBudget` (`long?`, null par défaut) + round-trip (infra EST-03/EST-04).

Purpose: séparer la logique pure (rapide à tester, RED→GREEN) de l'enrichissement I/O du provider — qualité maintenue, contexte < 50 %.
Output: 2 classes pures neutres, 2 champs de settings, 3 fichiers de tests (2 nouveaux + 1 étendu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-CONTEXT.md
@.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-RESEARCH.md

<interfaces>
<!-- Contrats existants que l'exécuteur consomme — pas d'exploration nécessaire. -->

Depuis src/Chronos/Services/ChronosSettings.cs (record NEUTRE, à ÉTENDRE — ne rien retirer) :
```csharp
public sealed record ChronosSettings
{
    public OverlayCorner Corner { get; init; } = OverlayCorner.TopRight;
    public string? MonitorDeviceName { get; init; }
    public double? X { get; init; }
    public double? Y { get; init; }
    public bool Background { get; init; }
    public double RefreshIntervalSeconds { get; init; } = 60;
    public DateTimeOffset? WeeklyAnchor { get; init; }  // ancre hebdo existante (Phase 6)
}
```

Depuis src/Chronos/Services/SettingsService.cs (persistance atomique/tolérante, INCHANGÉE) :
```csharp
public ChronosSettings Load();          // fichier absent/corrompu -> défauts
public void Save(ChronosSettings s);    // écriture atomique
// JsonSerializerOptions : PropertyNameCaseInsensitive, WriteIndented, JsonStringEnumConverter.
// System.Text.Json sérialise/désérialise automatiquement les long? ajoutés (aucune option à changer).
```

Depuis tests/Chronos.Tests/Fakes/FakeClock.cs : `new FakeClock(DateTimeOffset now)` expose `UtcNow`.

Miroir de pureté à imiter — src/Chronos/Services/WeeklyRecalibration.cs :
classe statique, `now` en paramètre, uniquement DateTimeOffset/TimeSpan, aucun I/O ni type WPF.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Plafonds de tokens dans ChronosSettings + round-trip</name>
  <files>src/Chronos/Services/ChronosSettings.cs, tests/Chronos.Tests/SettingsServiceTests.cs</files>
  <read_first>
    - src/Chronos/Services/ChronosSettings.cs (record complet)
    - tests/Chronos.Tests/SettingsServiceTests.cs (test `Save_puis_Load_round_trip`, l'objet `original`)
  </read_first>
  <behavior>
    - Round-trip : un ChronosSettings avec FiveHourTokenBudget=88_000 et WeeklyTokenBudget=1_200_000 relu à l'identique.
    - Défauts : sur fichier absent, les deux plafonds valent null (déjà couvert par `Load_fichier_absent_redonne_les_defauts` — y ajouter les deux assertions null).
  </behavior>
  <action>
    Ajouter DEUX propriétés à la fin du record `ChronosSettings` (commentaires FR, style xmldoc existant) :
    ```csharp
    /// <summary>Plafond de tokens de la fenêtre 5 h (calibrable, Phase 9). null = pas de plafond → utilization estimée null.</summary>
    public long? FiveHourTokenBudget { get; init; }

    /// <summary>Plafond de tokens de la fenêtre hebdo (calibrable, Phase 9). null = pas de plafond → utilization estimée null.</summary>
    public long? WeeklyTokenBudget { get; init; }
    ```
    Ne rien retirer ni réordonner (round-trip par égalité de record).

    Dans SettingsServiceTests :
    - `Save_puis_Load_round_trip` : ajouter `FiveHourTokenBudget = 88_000,` et `WeeklyTokenBudget = 1_200_000,` à l'objet `original`. L'assertion `Assert.Equal(original, relu)` valide alors les deux nouveaux champs.
    - `Load_fichier_absent_redonne_les_defauts` : ajouter `Assert.Null(s.FiveHourTokenBudget);` et `Assert.Null(s.WeeklyTokenBudget);`.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests -c Debug --filter SettingsService</automated>
  </verify>
  <acceptance_criteria>
    - Les 5 tests SettingsService passent, dont le round-trip incluant les deux plafonds non-null.
    - Aucune option JsonSerializer modifiée (les long? passent nativement).
  </acceptance_criteria>
  <done>ChronosSettings porte deux plafonds long? null par défaut, persistés et relus sans perte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : FiveHourWindowInference (fonction pure, algorithme « A »)</name>
  <files>src/Chronos/Services/FiveHourWindowInference.cs, tests/Chronos.Tests/FiveHourWindowInferenceTests.cs</files>
  <read_first>
    - src/Chronos/Services/WeeklyRecalibration.cs (patron de classe statique pure à imiter)
    - 08-RESEARCH.md § Pattern 1 (pseudocode verrouillé) et § Q1 (cas limites)
  </read_first>
  <behavior>
    RED d'abord — écrire ces `[Fact]` (now de référence = 2026-07-08T12:00:00Z, cohérent avec l'existant) :
    - Liste vide → null (inactive).
    - Message unique à now-1h → start = ce message ; reset = start+5h > now.
    - Message unique à now-6h (≥ 5 h) → null (reset ≤ now, EST-02).
    - Deux messages espacés d'exactement 5 h → trou ≥ 5 h STRICT = borne : start = message le plus récent seulement.
    - Rafale contiguë de 4 messages sur ~2 h (aucun trou ≥ 5 h) < 5 h totales → start = le plus ancien de la rafale.
    - Activité continue > 5 h (start ancien tel que start+5h ≤ now) → null (inactive, fraction=1 en aval — comportement verrouillé, Open Question 1).
    - Timestamps NON triés en entrée : la fonction suppose tsAsc trié ; ajouter un test documentant que l'appelant trie (le provider trie en 08-02) — passer une liste déjà triée.
  </behavior>
  <action>
    Créer la classe pure (aucun type WPF → ServicesLayerPurityTests la couvre). Code VERBATIM du RESEARCH § Pattern 1 :
    ```csharp
    namespace Chronos.Services;

    /// <summary>
    /// Inférence PURE du début de la fenêtre 5 h glissante à partir de timestamps triés croissant.
    /// Algorithme « A » verrouillé (CONTEXT) : début = le plus ancien message M tel qu'AUCUN trou
    /// inter-messages ≥ 5 h n'existe entre M et le message le plus récent, en remontant depuis le
    /// plus récent. Classe NEUTRE (DateTimeOffset/TimeSpan uniquement), now en paramètre → testable.
    /// </summary>
    public static class FiveHourWindowInference
    {
        public static readonly TimeSpan Window = TimeSpan.FromHours(5);

        /// <summary>Début de la fenêtre 5 h courante, ou null si inactive/expirée (reset ≤ now).</summary>
        public static DateTimeOffset? InferWindowStart(IReadOnlyList<DateTimeOffset> tsAsc, DateTimeOffset now)
        {
            if (tsAsc.Count == 0) return null;                    // aucune activité → inactive

            var start = tsAsc[^1];                                 // message le plus récent
            for (int i = tsAsc.Count - 2; i >= 0; i--)
            {
                if (start - tsAsc[i] >= Window) break;             // trou ≥ 5 h STRICT → borne atteinte
                start = tsAsc[i];                                  // pas de trou → on recule le début
            }

            var reset = start + Window;
            return reset > now ? start : null;                     // reset ≤ now → fenêtre expirée (EST-02)
        }
    }
    ```
    Puis écrire FiveHourWindowInferenceTests.cs couvrant les cas `<behavior>`. Utiliser des `DateTimeOffset` littéraux relatifs à `Now = new(2026,07,08,12,00,00, TimeSpan.Zero)`.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests -c Debug --filter FiveHourWindowInference</automated>
  </verify>
  <acceptance_criteria>
    - Tous les cas limites du `<behavior>` verts.
    - Trou d'EXACTEMENT 5 h traité comme borne (>=), message unique ≥ 5 h → null.
    - Aucun type WPF référencé (build de Chronos.csproj et ServicesLayerPurityTests verts).
  </acceptance_criteria>
  <done>InferWindowStart renvoie le début de fenêtre courant ou null selon l'algorithme « A », prouvé sur tous les cas limites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : WeeklyWindow.CurrentStart (fenêtre hebdo ancrée, pure)</name>
  <files>src/Chronos/Services/WeeklyWindow.cs, tests/Chronos.Tests/WeeklyWindowTests.cs</files>
  <read_first>
    - 08-RESEARCH.md § Q6 (math de la fenêtre roulante ancrée) et § Code Example 3
    - src/Chronos/Services/WeeklyRecalibration.cs (cohérence avec NextReset : même ancre)
  </read_first>
  <behavior>
    - Sans ancre (null) → CurrentStart = now - 7 j (fallback glissant, comportement v1.0).
    - Avec ancre = now - 10 j → k = floor(10/7) = 1 → CurrentStart = ancre + 7 j (= now - 3 j).
    - Avec ancre exactement = now → k=0 → CurrentStart = ancre (= now).
    - Avec ancre = now - 7 j (frontière exacte) → k=1 → CurrentStart = now (robustesse à la frontière).
  </behavior>
  <action>
    Créer la fonction pure (dérivée VERBATIM du RESEARCH § Code Example 3, extraite comme fonction réutilisable § Q6) :
    ```csharp
    namespace Chronos.Services;

    /// <summary>
    /// Borne de début de la fenêtre HEBDO courante. Avec ancre : fenêtre roulante [ancre+k·7j]
    /// contenant now, k = floor((now-ancre)/7j). Sans ancre : 7 j glissants (comportement v1.0).
    /// Cohérent avec WeeklyRecalibration.NextReset (même ancre) → longueur et couleur de l'arc alignées.
    /// Classe NEUTRE (DateTimeOffset/TimeSpan), now en paramètre.
    /// </summary>
    public static class WeeklyWindow
    {
        public static readonly TimeSpan Week = TimeSpan.FromDays(7);

        /// <summary>Début de la fenêtre hebdo courante : ancrée si anchor défini, sinon 7 j glissants.</summary>
        public static DateTimeOffset CurrentStart(DateTimeOffset? anchor, DateTimeOffset now)
            => anchor is { } a
                ? a + TimeSpan.FromTicks(Week.Ticks * (long)Math.Floor((now - a) / Week))
                : now - Week;
    }
    ```
    Puis WeeklyWindowTests.cs couvrant les cas `<behavior>`.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests -c Debug --filter WeeklyWindow</automated>
  </verify>
  <acceptance_criteria>
    - Fallback 7 j glissants sans ancre ; fenêtre ancrée correcte (k=floor) y compris à la frontière exacte.
    - Classe pure, ServicesLayerPurityTests verte.
  </acceptance_criteria>
  <done>WeeklyWindow.CurrentStart fournit la borne de fenêtre hebdo cohérente avec l'ancre existante, prouvée.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` vert après chaque tâche.
- `dotnet test Chronos.sln -c Debug` : suite complète verte (nouveaux tests inclus, aucun test existant cassé — le provider n'est pas encore touché, donc les tests JSONL restent inchangés à ce stade).
- ServicesLayerPurityTests verte (les deux nouvelles classes sont neutres).
</verification>

<success_criteria>
- FiveHourWindowInference.InferWindowStart et WeeklyWindow.CurrentStart existent, purs, testés sur tous les cas limites.
- ChronosSettings expose FiveHourTokenBudget/WeeklyTokenBudget (long?, null par défaut), round-trip prouvé.
- Aucune régression : suite complète verte.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-inf-rence-des-fen-tres-estimation-depuis-jsonl/08-01-SUMMARY.md`.
</output>
