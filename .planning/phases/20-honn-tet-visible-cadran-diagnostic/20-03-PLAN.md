---
phase: 20-honn-tet-visible-cadran-diagnostic
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - src/Chronos/ViewModels/WindowGaugeViewModel.cs
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/ViewModels/CadranPreviewViewModel.cs
  - src/Chronos/Views/Cadrans/CadranBraisesView.xaml
  - src/Chronos/Views/Cadrans/CadranFusibleView.xaml
  - src/Chronos/Views/Cadrans/CadranMareeView.xaml
  - src/Chronos/Views/Cadrans/CadranVoletsView.xaml
  - tests/Chronos.Tests/WindowGaugeViewModelTests.cs
  - tests/Chronos.Tests/MainViewModelTests.cs
  - tests/Chronos.Tests/CadranBindingTests.cs
  - tests/Chronos.Tests/GardesDoctrineTests.cs
autonomous: true
requirements: [EXA-03]

must_haves:
  truths:
    - "Il ne reste qu'UNE notion de « périmé » dans le projet : celle de la doctrine. Le ViewModel ne calcule plus aucun seuil d'ancienneté"
    - "Une propriété du cadran qui dit « plancher » s'appelle « plancher » — plus « estimé », concept supprimé en phase 19"
    - "Le ViewModel expose « ce chiffre est daté » comme un fait RAPPORTÉ par la doctrine, jamais recalculé"
    - "L'utilisateur peut lire, en une infobulle, quelle source alimente chaque fenêtre et depuis quand"
  artifacts:
    - path: "src/Chronos/ViewModels/WindowGaugeViewModel.cs"
      provides: "EstPlancher (ex-IsEstimated), EstDate, SourceDuReleve, InstantDuReleve"
      contains: "EstDate"
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "AfficherReleveDate + InfobulleReleve, recomposés ; IsStale et CapturedAt supprimés"
      contains: "AfficherReleveDate"
    - path: "tests/Chronos.Tests/GardesDoctrineTests.cs"
      provides: "Garde de non-retour : aucun seuil d'ancienneté dans les VM de la doctrine"
      contains: "seuil"
  key_links:
    - from: "WindowState.Provenance"
      to: "WindowGaugeViewModel.EstDate"
      via: "affectation directe dans Apply, sans aucune arithmétique d'horodatage"
      pattern: "EstDate = s.Provenance is"
    - from: "FiveHour.EstDate / SevenDay.EstDate"
      to: "MainViewModel.AfficherReleveDate"
      via: "recomposition dans MajPastilles — règle « le plus vieux des deux », jamais le plus jeune"
      pattern: "AfficherReleveDate ="
---

<objective>
Le ViewModel cesse d'avoir son avis sur la fraîcheur. `IsStale` (seuil de 2 min, bindé nulle part) meurt ;
`IsEstimated` (nom d'un concept supprimé en phase 19) devient `EstPlancher` ; et deux signaux nouveaux —
« ce chiffre est daté » et « voici qui l'alimente et depuis quand » — sont **rapportés** par la doctrine,
jamais recalculés.

Purpose : deux notions de « périmé » qui coexistent, c'est exactement l'incohérence que ce milestone
ferme. La bonne forme n'est pas d'aligner 2 min sur 6 min — c'est de **supprimer le calcul concurrent**,
et de poser une garde pour qu'il ne repousse pas au prochain refactor.

Output : le contrat de présentation complet dont le plan 04 a besoin pour dessiner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-RESEARCH.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-02-SUMMARY.md

<interfaces>
<!-- Contrats disponibles à l'entrée de ce plan. Ne PAS explorer le dépôt pour les retrouver. -->

```csharp
// Livrés par le plan 20-02 :
public enum SourceUsage { SondeEnTetes, EndpointOAuthChronos, EndpointOAuthClaude,
                          PontStatusLine, MagasinDernierExact }
public SourceUsage? WindowState.Source { get; init; }

public static class Chronos.Text.LibelleSource
{
    public static string Format(SourceUsage? source);                                  // null → « non renseignée »
    public static string Anciennete(DateTimeOffset? capturedAt, DateTimeOffset now);   // null → « de date inconnue »
    public static string Provenance(ProvenanceReleve? p);                              // null → «»
}
```

```csharp
// src/Chronos/ViewModels/WindowGaugeViewModel.cs — état ACTUEL des propriétés concernées
[ObservableProperty] private bool _isEstimated;      // ← renommée par ce plan
[ObservableProperty] private string _tokensText = "";
[ObservableProperty] private bool _hasTokens;
public void Apply(WindowState s) { … IsEstimated = s.Reliability == SourceReliability.Estimated; … }
```

```csharp
// src/Chronos/ViewModels/MainViewModel.cs — les lignes qui meurent
[ObservableProperty] private DateTimeOffset? _capturedAt;   // l. 44
[ObservableProperty] private bool _isStale;                 // l. 45
CapturedAt = snap.SourceCapturedAt;                          // l. 349, dans ApplySnapshot
IsStale = CapturedAt is { } c && (now - c) > TimeSpan.FromMinutes(2);   // l. 396, dans Interpolate

// …et le point de recomposition UNIQUE qui accueille le nouveau signal (l. 320-325)
private void MajPastilles()
{
    AfficherPastilleDeconnexion = _etatAuth == EtatAuthentification.Deconnecte;
    AfficherPastilleHorsLigne   = _etatAuth == EtatAuthentification.HorsLigne;
    AfficherInvitationConnexion = _jamaisDExactEtRienAAfficher && !AfficherPastilleDeconnexion;
}
```

Les **8 bindings XAML** à renommer (2 par fichier) :
`CadranBraisesView.xaml` l. 16/21 · `CadranFusibleView.xaml` l. 26/41 · `CadranMareeView.xaml` l. 17/32
(attribut `Estimated="{Binding X.IsEstimated}"`) · `CadranVoletsView.xaml` l. 40/72
(`Visibility="{Binding X.IsEstimated, Converter={StaticResource BoolToVis}}"`).
⚠️ Le nom de la **DP** des contrôles (`Estimated`) ne change PAS dans ce plan : seule la source du
binding change. Le renommage des DP est explicitement **optionnel et différé** (à sacrifier en premier).

Les **11 assertions de test** à réécrire : `CadranBindingTests` l. 81, 118, 119 ·
`MainViewModelTests` l. 283, 291, 379, 385 · `WindowGaugeViewModelTests` l. 25, 35, 104, 117
(+ le commentaire de classe l. 9 et les noms de test « badge estimée », périmés).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : IsEstimated → EstPlancher (le nom ment depuis la phase 19)</name>
  <files>src/Chronos/ViewModels/WindowGaugeViewModel.cs, src/Chronos/ViewModels/CadranPreviewViewModel.cs, src/Chronos/Views/Cadrans/CadranBraisesView.xaml, src/Chronos/Views/Cadrans/CadranFusibleView.xaml, src/Chronos/Views/Cadrans/CadranMareeView.xaml, src/Chronos/Views/Cadrans/CadranVoletsView.xaml, tests/Chronos.Tests/WindowGaugeViewModelTests.cs, tests/Chronos.Tests/MainViewModelTests.cs, tests/Chronos.Tests/CadranBindingTests.cs</files>

  <read_first>
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (INTÉGRALEMENT)
    - src/Chronos/ViewModels/CadranPreviewViewModel.cs (l. 55-72 : `Push`, qui pilote la galerie `--cadrans`)
    - src/Chronos/Views/Cadrans/CadranBraisesView.xaml, CadranFusibleView.xaml, CadranMareeView.xaml,
      CadranVoletsView.xaml (les 8 bindings)
    - tests/Chronos.Tests/WindowGaugeViewModelTests.cs (INTÉGRALEMENT)
    - tests/Chronos.Tests/MainViewModelTests.cs (l. 275-300 et l. 370-395)
    - tests/Chronos.Tests/CadranBindingTests.cs (l. 68-125)
  </read_first>

  <action>
Depuis la phase 19, `SourceReliability.Estimated` n'est produit qu'en **un seul endroit** de la
production (branche 3 de la doctrine) : `IsEstimated` signifie déjà exactement « **plancher** ». Dans une
phase intitulée « honnêteté visible », laisser une propriété dont le nom désigne un concept supprimé est
une contradiction interne.

1. `WindowGaugeViewModel` : renommer le champ `_isEstimated` en `_estPlancher` (la propriété générée
   devient `EstPlancher`) et l'affectation dans `Apply`. Amender le commentaire de ligne : la propriété
   vaut « borne inférieure », pas « estimation ».
2. `CadranPreviewViewModel.Push` : `g.IsEstimated = estimated;` devient `g.EstPlancher = estimated;`
   (1 ligne). **Ne pas** renommer les paramètres `estimated` / `FiveEstimated` / `SevenEstimated` de la
   galerie : ce sont des booléens d'**aperçu** sans provenance, et
   `PercentFormatter.Format(double?, bool)` — la surcharge historique au « ~ » — reste volontairement à
   leur service. La supprimer casserait la galerie pour rien.
3. Les **8** bindings XAML : `IsEstimated` → `EstPlancher`.
4. Réécrire les **11** assertions de test listées dans `<interfaces>`, et renommer les deux tests de
   `WindowGaugeViewModelTests` dont le libellé parle de « badge estimée » (vocabulaire périmé : le badge
   n'existe plus depuis le centre épuré de la v1.3) en un libellé qui dit ce qu'ils prouvent, p. ex.
   `Fenetre_exacte_ne_porte_PAS_la_marque_de_plancher_et_porte_l_utilisation_reelle` et
   `Fenetre_plancher_rallume_la_marque`. Amender le commentaire de classe l. 9 dans le même mouvement.
5. Tout cela **dans la même tâche** : `tests/Chronos.Tests` a un `ProjectReference` vers `Chronos`, une
   seule erreur de compilation fait échouer TOUTE l'invocation `dotnet test`. Aucun « échec de build
   attendu » n'est acceptable entre deux commits.

**Consigne de rédaction** (elle rend le critère ci-dessous non contradictoire) : ne pas écrire le mot
`IsEstimated` dans un commentaire de `src/` ni de `tests/`. Pour parler du renommage, écrire
« l'ancienne marque d'estimation ».
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~WindowGaugeViewModelTests|FullyQualifiedName~MainViewModelTests|FullyQualifiedName~CadranBindingTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn --include=*.cs --include=*.xaml "IsEstimated" src/Chronos tests/Chronos.Tests` rend
      **0 ligne**
    - `grep -rc --include=*.xaml "EstPlancher" src/Chronos/Views/Cadrans` rend un total de **8**
    - `grep -c "EstPlancher" src/Chronos/ViewModels/CadranPreviewViewModel.cs` rend **1**
    - `grep -c "Format(double?, bool)\|double? utilization, bool" src/Chronos/Text/PercentFormatter.cs`
      rend **au moins 1** (la surcharge historique de la galerie est CONSERVÉE)
    - Le test `Etat_fiabilite_mixte_la_marque_du_pourcentage_est_par_fenetre` de `CadranBindingTests`
      passe **sans modification de sa logique** (il verrouille la portée PAR FENÊTRE de la marque de
      plancher : seule la propriété change de nom)
    - 0 échec sur les trois classes filtrées
  </acceptance_criteria>

  <done>
La propriété qui pilote la texture des quatre styles alternatifs porte enfin le nom du fait qu'elle
décrit. Aucun comportement n'a changé — c'est un renommage, et il est prouvé par les 11 assertions
conservées.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : mort d'IsStale — une seule notion de « périmé », et une garde pour qu'elle le reste</name>
  <files>src/Chronos/ViewModels/WindowGaugeViewModel.cs, src/Chronos/ViewModels/MainViewModel.cs, src/Chronos/Models/UsageSnapshot.cs, tests/Chronos.Tests/MainViewModelTests.cs, tests/Chronos.Tests/WindowGaugeViewModelTests.cs, tests/Chronos.Tests/GardesDoctrineTests.cs</files>

  <read_first>
    - src/Chronos/ViewModels/MainViewModel.cs (l. 43-45, l. 340-360 `ApplySnapshot`, l. 390-400 `Interpolate`)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (`Apply`, après la tâche 1)
    - src/Chronos/Services/DoctrineFraicheur.cs (l. 24-40 : `LimiteAge`, DÉRIVÉE de la cadence de la
      sonde — 300 s + 60 s = 6 min — et le commentaire qui explique pourquoi elle n'est PAS réglable)
    - src/Chronos/Models/ProvenanceReleve.cs
    - src/Chronos/Models/UsageSnapshot.cs (le champ `SourceCapturedAt`, déclaré LEGACY en phase 19)
    - tests/Chronos.Tests/MainViewModelTests.cs (l. 185-206 : `IsStale_vrai_quand_la_capture_depasse_deux_minutes`)
    - tests/Chronos.Tests/GardesDoctrineTests.cs (le motif `CheminSources()` et le format des gardes de source)
    - src/Chronos/ViewModels/SessionsViewModel.cs (l. 185-195 — à LIRE avant d'écrire la garde : ce
      fichier compare légitimement des durées ; la garde ne doit PAS le balayer)
  </read_first>

  <behavior>
    - `EstDate` vaut **false** pour une fenêtre `Frais`, **true** pour `EncoreValide` et pour
      `PlancherAvecActivite`, **false** pour une provenance `null` (fenêtre née hors doctrine).
    - Un `EncoreValide` porte `EstDate = true` et `EstPlancher = false` : il est daté ET juste
      (non-régression d'honnêteté — le griser serait mentir sur une preuve positive).
    - Un `PlancherAvecActivite` porte `EstDate = true` ET `EstPlancher = true` : les deux marques se
      composent, et leur composition est littéralement vraie (`plancher ⊂ daté`).
    - La garde de source échoue si un seuil d'ancienneté réapparaît dans `MainViewModel` ou
      `WindowGaugeViewModel`.
  </behavior>

  <action>
Le point n'est pas « laquelle des deux valeurs est la bonne » (2 min contre 6 min), c'est **qui a
l'autorité**. La doctrine décide de la provenance ; un ViewModel qui recalcule sa propre notion de
« périmé » rouvre l'incohérence que ce milestone ferme.

1. `WindowGaugeViewModel` : ajouter
   ```csharp
   /// <summary>EXA-03 — « ce chiffre a été relevé il y a un moment ». RAPPORTÉ par la doctrine, jamais
   /// recalculé : aucune comparaison d'horodatage ne vit dans ce ViewModel, et c'est la garantie
   /// STRUCTURELLE qu'un second seuil de « périmé » ne peut pas réapparaître à côté de LimiteAge.
   /// Frais → false. EncoreValide → true ET juste : la doctrine est ALLÉE VÉRIFIER qu'aucune réponse
   /// assistant n'est survenue depuis la capture. Provenance null (fenêtre née hors doctrine) → false :
   /// on ne salit pas un chiffre sur lequel la doctrine ne s'est pas prononcée.</summary>
   [ObservableProperty] private bool _estDate;
   ```
   et, dans `Apply`, juste après l'affectation d'`EstPlancher` :
   ```csharp
   EstDate = s.Provenance is ProvenanceReleve.EncoreValide or ProvenanceReleve.PlancherAvecActivite;
   ```
   Ajouter aussi les deux propriétés simples (non observables — elles ne sont pas bindées, elles sont
   lues par `MainViewModel` pour composer l'infobulle en tâche 3) :
   ```csharp
   public SourceUsage? SourceDuReleve { get; private set; }
   public DateTimeOffset? InstantDuReleve { get; private set; }
   ```
   alimentées dans `Apply` par `s.Source` et `s.CapturedAt`.

2. `MainViewModel` : **supprimer** `[ObservableProperty] private DateTimeOffset? _capturedAt;` (l. 44),
   `[ObservableProperty] private bool _isStale;` (l. 45), la ligne `CapturedAt = snap.SourceCapturedAt;`
   (l. 349) et la ligne `IsStale = …` (l. 396). Aucune de ces quatre lignes n'a d'autre consommateur que
   le test supprimé en 3.

3. **CONSERVER** `UsageSnapshot.SourceCapturedAt` : il est encore asserté par 6 tests de providers, et le
   supprimer coûterait des tests pour zéro exigence. Amender son commentaire XML : il n'est plus « en
   attente de retrait par la phase 20 » — il est le staleness **de source**, distinct de la fraîcheur
   **par fenêtre** que porte `WindowState.CapturedAt` + `Provenance`.

4. `MainViewModelTests` : supprimer `IsStale_vrai_quand_la_capture_depasse_deux_minutes`. **Jamais sans
   remplaçant** — le remplaçant est la garde du point 5, plus deux `[Fact]` de comportement sur
   `WindowGaugeViewModelTests` couvrant les quatre cas de `EstDate` décrits en `<behavior>`, dont
   explicitement `EncoreValide ⇒ EstDate && !EstPlancher`.

5. Ajouter dans `GardesDoctrineTests` :
   `Aucun_seuil_d_anciennete_n_est_calcule_dans_les_ViewModels_de_la_doctrine`.
   - Balaie **exactement deux fichiers**, via `CheminSources()` :
     `ViewModels/MainViewModel.cs` et `ViewModels/WindowGaugeViewModel.cs`.
   - Motif : `[<>]=?\s*(System\.)?TimeSpan\.From` **ou** `TimeSpan\.From\w+\([^)]*\)\s*[<>]=?`
     (les deux sens de la comparaison).
   - Asserte que les deux fichiers existent et sont non vides (une garde qui ne lit rien est muette).
   - Message d'échec : la limite d'âge appartient à `DoctrineFraicheur.LimiteAge`, elle est DÉRIVÉE de
     la cadence de la sonde et non réglable ; un second seuil dans un ViewModel recréerait le défaut
     que ce milestone corrige (précédent documenté : une source de plafond passée en « Manual » a gelé
     à vie un chiffre faux).
   - **Portée délibérément restreinte à ces deux fichiers, et le commentaire doit le dire** :
     `SessionsViewModel` compare légitimement des durées (l. 190-191) pour formater l'ancienneté d'une
     session du widget — sujet distinct de la doctrine d'usage. Élargir la garde au dossier entier la
     rendrait rouge sur du code correct, donc inexploitable.

Vérifier par **mutation réelle** que la garde mord : réintroduire temporairement
`IsStale = CapturedAt is { } c && (now - c) > TimeSpan.FromMinutes(2);` dans `MainViewModel`, constater
le rouge, révoquer, constater que `git diff src/Chronos/ViewModels/MainViewModel.cs` ne contient plus la
mutation.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~GardesDoctrineTests|FullyQualifiedName~MainViewModelTests|FullyQualifiedName~WindowGaugeViewModelTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn --include=*.cs --include=*.xaml "IsStale" src/Chronos tests/Chronos.Tests` rend **0 ligne**
    - `grep -c "CapturedAt" src/Chronos/ViewModels/MainViewModel.cs` rend **0**
    - `grep -c "EstDate = s.Provenance is" src/Chronos/ViewModels/WindowGaugeViewModel.cs` rend **1**
    - `grep -rn "TimeSpan.From" src/Chronos/ViewModels/MainViewModel.cs` : aucune des lignes rendues ne
      contient un opérateur `<`, `>`, `<=` ou `>=` adjacent (les 3 occurrences restantes sont deux
      constructions de fenêtre et un `Interval =`)
    - `grep -c "SourceCapturedAt" src/Chronos/Models/UsageSnapshot.cs` rend **1** (le champ est CONSERVÉ,
      seul son commentaire change)
    - Le test `Aucun_seuil_d_anciennete_n_est_calcule_dans_les_ViewModels_de_la_doctrine` passe, et la
      mutation décrite dans l'action le fait échouer (constaté, puis révoqué)
    - `MainViewModelTests` : 41 tests au lieu de 42, l'écart justifié nominativement dans le SUMMARY
  </acceptance_criteria>

  <done>
Il ne reste qu'une notion de « périmé » dans Chronos, elle vit dans la doctrine, elle est dérivée et non
réglable — et une garde de source empêche la seconde de repousser.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : le signal d'âge et l'infobulle qui nomme la source (EXA-03 / EXA-06 au cadran)</name>
  <files>src/Chronos/ViewModels/MainViewModel.cs, src/Chronos/ViewModels/WindowGaugeViewModel.cs, tests/Chronos.Tests/MainViewModelTests.cs</files>

  <read_first>
    - src/Chronos/ViewModels/MainViewModel.cs (l. 310-360 : `_etatAuth`, `_jamaisDExactEtRienAAfficher`,
      `MajPastilles`, `ApplySnapshot` ; et l. 375-390 `MajTexteEtatSonde`, le modèle exact d'un texte
      recomposé à partir des jauges plutôt que remappé)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (après la tâche 2 : `EstDate`, `SourceDuReleve`,
      `InstantDuReleve`, `TokensText`, `HasTokens`)
    - src/Chronos/Text/LibelleSource.cs (livré par le plan 20-02)
    - tests/Chronos.Tests/MainViewModelTests.cs (le montage de VM utilisé par les tests existants)
  </read_first>

  <behavior>
    - 5 h `Frais` + hebdo `Frais` ⇒ `AfficherReleveDate == false` (le cas nominal ne porte AUCUNE marque).
    - 5 h `Frais` + hebdo `EncoreValide` ⇒ `AfficherReleveDate == true` (« le plus vieux des deux »,
      jamais le plus jeune : une synthèse conservatrice n'est pas un mensonge, une synthèse optimiste
      en serait un).
    - Deux fenêtres `Unavailable` ⇒ `AfficherReleveDate == false` (rien à dater : il n'y a pas de chiffre).
    - `InfobulleReleve` nomme les DEUX fenêtres, chacune avec son libellé de source et son ancienneté.
    - Une fenêtre plancher fait apparaître son compte de tokens brut dans l'infobulle, jamais converti
      en points de pourcentage.
    - Une fenêtre sans source rend « non renseignée » dans l'infobulle, jamais un nom par défaut.
  </behavior>

  <action>
`TokensText` / `HasTokens` sont calculés, testés, et bindés **nulle part** : c'est la dette n° 1 léguée par
la phase 19. Décision, tranchée et écrite ici : la **marque** (« ≥ » + texture) reste permanente ; la
**matière brute** devient visible **à la demande**, en infobulle — réinstaller une ligne de tokens
permanente au centre annulerait la décision de design « centre épuré » de la v1.3 pour satisfaire une
exigence qui ne le demande pas.

1. Ajouter à `MainViewModel` :
   ```csharp
   /// <summary>EXA-03 — au moins une des deux fenêtres porte un chiffre DATÉ (relevé au-delà de la
   /// limite d'âge, réhabilité ou dégradé en plancher par la doctrine). Marque INFORMATIVE et ADDITIVE :
   /// elle s'ajoute À CÔTÉ du chiffre, elle ne lui retire ni luminance ni netteté — un EncoreValide est
   /// un chiffre PROUVÉ juste, le voiler serait une régression d'honnêteté.
   /// PORTÉE GLOBALE assumée : l'âge n'est pas une propriété de la fenêtre, c'est une propriété du
   /// PIPELINE (les deux fenêtres viennent de la même chaîne de sources). Règle de composition :
   /// on montre la marque dès que l'UNE des deux est datée — le plus vieux des deux, jamais le plus
   /// jeune. Le détail par fenêtre est dans l'infobulle.</summary>
   [ObservableProperty] private bool _afficherReleveDate;

   /// <summary>EXA-06 au cadran — le COUPLE (qui alimente, depuis quand) pour chaque fenêtre, plus la
   /// matière brute de DEL-04 quand elle existe. Le vocabulaire n'est PAS remappé ici : il vient de
   /// Chronos.Text.LibelleSource, un seul point pour tout le projet.</summary>
   [ObservableProperty] private string _infobulleReleve = "";
   ```
2. Dans `MajPastilles()` — le point de recomposition UNIQUE, jamais un empilement de `Visibility`
   indépendantes — ajouter :
   ```csharp
   AfficherReleveDate = FiveHour.EstDate || SevenDay.EstDate;
   ```
   Renommer le `<summary>` de la méthode : elle recompose désormais **quatre** marques, pas trois.
3. Ajouter `MajInfobulleReleve()`, appelée depuis `ApplySnapshot` **après** `FiveHour.Apply` /
   `SevenDay.Apply` et à côté de `MajTexteEtatSonde()` (même frontière de thread, même instant) :
   ```csharp
   private void MajInfobulleReleve()
   {
       var now = _clock.UtcNow;
       InfobulleReleve = Ligne("5 h", FiveHour, now) + "\n" + Ligne("hebdo", SevenDay, now);

       static string Ligne(string etiquette, WindowGaugeViewModel g, DateTimeOffset now)
       {
           var morceaux = new List<string>
           {
               LibelleSource.Format(g.SourceDuReleve),
               "relevé " + LibelleSource.Anciennete(g.InstantDuReleve, now),
           };
           if (LibelleSource.Provenance(…) is { Length: > 0 } p) morceaux.Add(p);
           if (g.HasTokens) morceaux.Add(g.TokensText);
           return etiquette + " : " + string.Join(" · ", morceaux);
       }
   }
   ```
   Pour la provenance, exposer aussi `public ProvenanceReleve? ProvenanceDuReleve { get; private set; }`
   sur `WindowGaugeViewModel` (alimentée dans `Apply` par `s.Provenance`), sur le même modèle que
   `SourceDuReleve` — pas d'`[ObservableProperty]`, elle n'est pas bindée.
   **Aucune arithmétique d'horodatage ici** : `LibelleSource.Anciennete` fait tout le travail, et la
   garde de la tâche 2 reste verte.
4. `MajInfobulleReleve` est appelée **uniquement** depuis `ApplySnapshot` (une fois par rafraîchissement,
   ≈ 60 s) et **pas** depuis `Interpolate` (une fois par seconde) : recomposer une chaîne chaque seconde
   pour une infobulle qui n'est visible qu'au survol serait du travail permanent pour un affichage
   occasionnel. L'ancienneté affichée peut donc retarder d'un tick de rafraîchissement — écrire ce fait
   en commentaire plutôt que le laisser deviner.

Tests dans `MainViewModelTests` (écrits d'abord) : les six cas de `<behavior>`, chacun avec un snapshot
explicite. Pour l'infobulle, asserter la présence des libellés attendus
(`Assert.Contains("sonde d'en-têtes de rate-limit", vm.InfobulleReleve)`,
`Assert.Contains("non renseignée", …)`, `Assert.Contains("5 h :", …)` et `Assert.Contains("hebdo :", …)`),
jamais l'égalité stricte de toute la chaîne — une assertion d'égalité sur un texte composé se casse au
premier ajout de séparateur et n'apprend rien de plus.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~WindowGaugeViewModelTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "AfficherReleveDate =" src/Chronos/ViewModels/MainViewModel.cs` rend **1** (posée dans le
      SEUL point de recomposition, jamais ailleurs)
    - `grep -c "MajInfobulleReleve()" src/Chronos/ViewModels/MainViewModel.cs` rend **2**
      (la déclaration et l'unique appel depuis `ApplySnapshot`)
    - `grep -n "MajInfobulleReleve" src/Chronos/ViewModels/MainViewModel.cs` : aucune occurrence ne se
      trouve dans le corps de `Interpolate`
    - `grep -c "LibelleSource" src/Chronos/ViewModels/MainViewModel.cs` rend **au moins 3**
      (source, ancienneté, provenance — aucun mapping FR local n'est réécrit)
    - `grep -rn --include=*.cs "TokensDepuisReleve.*100\|TokensText.*%" src/Chronos/ViewModels` rend
      **0 ligne** (la matière brute n'est jamais convertie en points de pourcentage — EXA-04)
    - La garde `Aucun_seuil_d_anciennete_n_est_calcule_dans_les_ViewModels_de_la_doctrine` reste **verte**
    - Les 6 nouveaux tests passent
  </acceptance_criteria>

  <done>
Le ViewModel expose tout ce dont le dessin a besoin : une marque d'âge globale et conservatrice, et un
texte qui nomme la source et l'ancienneté de chaque fenêtre. Rien n'est encore à l'écran — c'est le
plan 04.
  </done>
</task>

</tasks>

<verification>
1. Suite complète verte : `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q`.
   Bilan attendu : **−1** (`IsStale…`), **+ ≈ 12** (4 sur `EstDate`, 6 sur la marque d'âge et
   l'infobulle, 1 garde de source, ±1 de scission). **0 échec + chaque écart justifié nominativement.**
2. Les cinq gardes permanentes restent vertes — en particulier `ServicesLayerPurityTests` : le signal
   visuel passe par les ViewModels, aucun type WPF n'est entré dans `Services/` ni `Models/`.
3. `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`. Overlay non lancé.
</verification>

<success_criteria>
- Une seule notion de « périmé » subsiste, gardée structurellement.
- `EstPlancher` / `EstDate` sont deux booléens indépendants, décrivant deux faits différents, et leur
  composition (`plancher ⊂ daté`) est testée.
- `TokensText` / `HasTokens` ne sont plus du code orphelin : ils arrivent à l'écran par l'infobulle.
- Aucun converter enum→Brush, aucune couleur en dur, aucune dépendance NuGet.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-03-SUMMARY.md`
</output>
