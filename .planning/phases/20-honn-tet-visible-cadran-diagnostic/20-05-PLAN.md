---
phase: 20-honn-tet-visible-cadran-diagnostic
plan: 05
type: execute
wave: 5
depends_on: ["20-04"]
files_modified:
  - src/Chronos/Services/DiagnosticService.cs
  - tests/Chronos.Tests/DiagnosticServiceTests.cs
  - .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [EXA-03, EXA-06]

must_haves:
  truths:
    - "Le diagnostic nomme la source qui alimente RÉELLEMENT chaque fenêtre, et depuis quand"
    - "Le diagnostic et le cadran parlent le MÊME vocabulaire : « ≥ » des deux côtés, plus jamais un « ~ » qui suggère une incertitude symétrique"
    - "Une source absente se dit « non renseignée » et jamais par un nom de source par défaut"
    - "La porte de phase est franchie sur preuves : suite verte deux fois, cinq gardes, coffre de jetons intact"
    - "Ce qu'aucun test ne peut produire est DÉLÉGUÉ à l'utilisateur sous forme de protocole, jamais simulé"
  artifacts:
    - path: "src/Chronos/Services/DiagnosticService.cs"
      provides: "Describe() rend le COUPLE (qui, depuis quand) + l'état de chaque fenêtre"
      contains: "LibelleSource"
    - path: ".planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md"
      provides: "Carte de vérification par tâche remplie, sign-off, relevé de porte daté"
      contains: "Per-Task Verification Map"
  key_links:
    - from: "WindowState.Source + WindowState.CapturedAt"
      to: "le rapport de diagnostic"
      via: "Chronos.Text.LibelleSource — un seul mapping FR pour tout le projet"
      pattern: "LibelleSource\\.(Format|Anciennete|Provenance)"
---

<objective>
Fermer EXA-06 par sa moitié visible — le rapport de diagnostic nomme la source et son ancienneté — et
franchir la porte de la **dernière phase du milestone v1.5**, en délivrant à l'utilisateur, sous forme de
protocole, les vérifications visuelles qu'aucun robot ne peut produire ici : y compris celles héritées
des phases 17, 18 et 19.

Purpose : l'utilisateur doit pouvoir constater **seul** une panne de source, sans instrumenter le code.
C'est exactement ce qui a manqué pendant deux mois — un 401 muet, un `usage.json` figé, et aucun endroit
où le lire.

Output : un rapport qui dit « sonde d'en-têtes de rate-limit · relevé il y a 12 min · borne inférieure »,
une porte de phase documentée, et un protocole de constat remis à l'utilisateur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-RESEARCH.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-04-SUMMARY.md

<interfaces>
```csharp
// Disponible depuis le plan 20-02 :
public static class Chronos.Text.LibelleSource
{
    public static string Format(SourceUsage? source);                                // null → « non renseignée »
    public static string Anciennete(DateTimeOffset? capturedAt, DateTimeOffset now); // null → « de date inconnue »
    public static string Provenance(ProvenanceReleve? p);                            // null → «»
}
```

```csharp
// src/Chronos/Services/DiagnosticService.cs — état ACTUEL, l. 585-599. Méthode STATIQUE, appelée
// depuis BuildReportAsync l. 359-360 : « sb.AppendLine("  5 h   : " + Describe(snap.FiveHour)); »
private static string Describe(WindowState w)
{
    var baseTexte = w.Reliability switch
    {
        SourceReliability.Exact     => "EXACT — " + (w.Utilization is { } u ? UsageNormalization.PourcentagePourAffichage(u) : "?"),
        SourceReliability.Estimated => "estimé — " + (w.Utilization is { } u ? "~" + UsageNormalization.PourcentagePourAffichage(u) : "% inconnu"),
        _                           => "indisponible",
    };
    if (w.StatutServeur is { } st) baseTexte += " · serveur : " + LibelleStatutServeur(st);
    if (w.Depassement?.Utilization is { } d) baseTexte += " · dépassement " + UsageNormalization.PourcentagePourAffichage(d);
    return baseTexte;
}
```

Deux tests existants assertent littéralement `Assert.Contains("estimé", report)` :
`Rapport_sans_token_conseille_et_n_expose_jamais_le_token` (l. 46) et
`Un_diagnostic_sans_sonde_reste_lisible` (l. 191). **Tous deux montent une fenêtre `Estimated` SANS
`Provenance` ni `Source`** : le nouveau libellé doit rester correct quand les deux sont `null`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : le diagnostic nomme la source et l'ancienneté (EXA-06)</name>
  <files>src/Chronos/Services/DiagnosticService.cs, tests/Chronos.Tests/DiagnosticServiceTests.cs</files>

  <read_first>
    - src/Chronos/Services/DiagnosticService.cs (l. 350-365 la section « résultat affiché », l. 500-515
      `LibelleStatutServeur`, l. 585-599 `Describe`)
    - src/Chronos/Text/LibelleSource.cs (livré par 20-02)
    - src/Chronos/Text/PercentFormatter.cs (l. 35-50 : le POURQUOI du « ≥ » — incertitude UNILATÉRALE ;
      « un tilde dirait *autour de 80*, ce qui autoriserait la lecture *peut-être 75* »)
    - tests/Chronos.Tests/DiagnosticServiceTests.cs (INTÉGRALEMENT — les 7 tests, leurs `Assert.Contains`
      littéraux et les deux `Assert.DoesNotContain`)
  </read_first>

  <behavior>
    - Une fenêtre exacte fraîche produite par la sonde rend :
      `EXACT — 42 % · source : sonde d'en-têtes de rate-limit · relevé il y a 12 min · frais`
    - Un plancher rend `PLANCHER — ≥ 42 % · … · borne inférieure (activité depuis)` — « ≥ », **jamais** « ~ ».
    - `Source = null` rend « source : non renseignée », jamais un nom de source par défaut.
    - `CapturedAt = null` rend « relevé de date inconnue », jamais une durée fabriquée.
    - `Provenance = null` n'ajoute **aucun** segment (la doctrine ne s'est pas prononcée).
    - Les suffixes existants (statut serveur, dépassement) restent rendus **mot pour mot** : des tests les
      assertent littéralement (`REJETÉ`, `34 %`).
  </behavior>

  <action>
Deux défauts à corriger d'un coup, parce qu'ils sont dans la même méthode :

1. **Le diagnostic dit encore « ~ » là où le cadran dit « ≥ »** depuis la phase 19 — deux vocabulaires
   pour le même fait, et le tilde véhicule une incertitude **symétrique** que 19-04 a explicitement
   rejetée. Ce tilde-là est un **oubli**, pas une conservation volontaire (à la différence de la
   surcharge `PercentFormatter.Format(double?, bool)`, qui sert la galerie et reste).
2. **Le rapport ne dit ni QUI alimente la fenêtre, ni DEPUIS QUAND** — c'est littéralement l'énoncé
   d'EXA-06.

Transformer `Describe` en méthode **d'instance** (retirer `static` ; elle a besoin de `_clock`). Les deux
sites d'appel l. 359-360 sont dans la même instance : **leur texte ne change pas**.

```csharp
// EXA-06 — le rapport rend le COUPLE (QUI alimente, DEPUIS QUAND) et l'état de la fenêtre. Le
// vocabulaire FR vient de Chronos.Text.LibelleSource : UN seul mapping pour tout le projet, partagé
// avec l'infobulle du cadran — deux mappings divergeraient au premier changement de vocabulaire.
private string Describe(WindowState w)
{
    var baseTexte = w.Reliability switch
    {
        SourceReliability.Exact     => "EXACT — " + (w.Utilization is { } u ? UsageNormalization.PourcentagePourAffichage(u) : "?"),
        // « ≥ » et NON « ~ » (19-04) : l'incertitude d'un plancher est UNILATÉRALE. On sait la borne
        // inférieure, c'est la borne supérieure qui est inconnue.
        SourceReliability.Estimated => "PLANCHER — " + (w.Utilization is { } u ? "≥ " + UsageNormalization.PourcentagePourAffichage(u) : "% inconnu"),
        _                           => "indisponible",
    };

    baseTexte += " · source : " + LibelleSource.Format(w.Source);
    baseTexte += " · relevé " + LibelleSource.Anciennete(w.CapturedAt, _clock.UtcNow);
    if (LibelleSource.Provenance(w.Provenance) is { Length: > 0 } p) baseTexte += " · " + p;

    if (w.StatutServeur is { } st) baseTexte += " · serveur : " + LibelleStatutServeur(st);
    if (w.Depassement?.Utilization is { } d)
        baseTexte += " · dépassement " + UsageNormalization.PourcentagePourAffichage(d);

    return baseTexte;
}
```

⚠️ Aucune conversion d'unité locale : tout passe par `UsageNormalization` — `NormalisationUniqueTests`
balaie le TEXTE source et échoue sur tout `/ 100` posé ailleurs.

Tests, dans `DiagnosticServiceTests` :
- **Réécrire** les deux `Assert.Contains("estimé", report)` (l. 46 et l. 191) : le mot n'existe plus.
  Les remplacer par `Assert.Contains("PLANCHER", report)` **et** `Assert.DoesNotContain("~", report)`.
  Vérifier avant de figer cette seconde assertion que le rapport complet ne contient aucun autre tilde
  (le lire en entier une fois) ; s'il en contient un ailleurs, restreindre l'assertion à la ligne
  « 5 h » extraite du rapport plutôt que d'affaiblir la preuve.
  ⚠️ Ces deux tests montent une fenêtre `Estimated` **sans** `Provenance` ni `Source` : le rendu attendu
  y est donc `PLANCHER — % inconnu · source : non renseignée · relevé de date inconnue`, sans segment
  de provenance. C'est la forme dégradée, et elle doit rester lisible.
- **Nouveau** `Le_rapport_nomme_la_SOURCE_qui_alimente_chaque_fenetre` : snapshot avec 5 h portant
  `SourceUsage.SondeEnTetes` et hebdo `SourceUsage.MagasinDernierExact` ; asserter les deux libellés FR.
- **Nouveau** `Le_rapport_donne_l_ANCIENNETE_du_releve_de_chaque_fenetre` : `CapturedAt = now - 12 min`
  avec une `FakeClock` à `now` ; asserter `Assert.Contains("relevé il y a 12 min", report)`.
- **Nouveau** `Une_source_absente_se_dit_non_renseignee_et_JAMAIS_un_nom_par_defaut` : `Source = null` ;
  asserter `Assert.Contains("source : non renseignée", report)` et
  `Assert.DoesNotContain("sonde d'en-têtes de rate-limit · relevé", report)` — la seconde assertion vise
  la LIGNE de résultat et non la section de la sonde, qui porte légitimement le même libellé entre
  crochets (`[Source exacte — sonde d'en-têtes de rate-limit]`). **Ne pas** écrire
  `DoesNotContain("sonde d'en-têtes de rate-limit")` tout court : ce serait rouge pour une bonne raison.
- **Nouveau** `Un_plancher_est_decrit_avec_superieur_ou_egal_et_sa_provenance` : `Estimated` +
  `PlancherAvecActivite` + `Utilization = 0.42` ; asserter `"PLANCHER — ≥ 42 %"` et
  `"borne inférieure (activité depuis)"`.

Les 7 montages passent déjà `machine: new FakeInventaireMachine()` depuis le plan 20-01 : les nouveaux
tests doivent faire de même, sans quoi ils rendraient à eux seuls 18 s chacun.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~DiagnosticServiceTests|FullyQualifiedName~NormalisationUniqueTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"estimé — "' src/Chronos/Services/DiagnosticService.cs` rend **0**
    - `grep -c 'PLANCHER' src/Chronos/Services/DiagnosticService.cs` rend **1**
    - `grep -c 'LibelleSource\.' src/Chronos/Services/DiagnosticService.cs` rend **3**
      (source, ancienneté, provenance — aucun mapping FR local réécrit)
    - `grep -c 'private static string Describe' src/Chronos/Services/DiagnosticService.cs` rend **0**
      et `grep -c 'private string Describe' …` rend **1**
    - `grep -c 'new FakeInventaireMachine()' tests/Chronos.Tests/DiagnosticServiceTests.cs` rend **11**
      (7 montages préexistants + 4 nouveaux)
    - Les 11 tests de `DiagnosticServiceTests` passent en **moins de 15 s** au total
    - `NormalisationUniqueTests` reste vert
  </acceptance_criteria>

  <done>
Le rapport répond aux deux moitiés d'EXA-06 — qui alimente, depuis quand — et il parle le même français
que le cadran.
  </done>
</task>

<task type="auto">
  <name>Task 2 : porte de phase — preuves rassemblées, comptage réconcilié, carte remplie</name>
  <files>.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <read_first>
    - .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md (INTÉGRALEMENT — la carte
      à remplir, les gardes permanentes, la section « Manual-Only Verifications », le sign-off)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-05-SUMMARY.md (le protocole de porte de
      phase à reproduire : comptage réconcilié NOMINATIVEMENT, relevés `stat` encadrants, statut ⏳
      distinct pour une vérification déléguée)
    - .planning/REQUIREMENTS.md (lignes EXA-03 et EXA-06, et la table de traçabilité)
    - .planning/ROADMAP.md (la table de progression et la ligne « Phase 20 »)
    - Les SUMMARY 20-01 à 20-04
  </read_first>

  <action>
1. **Suite complète, DEUX exécutions consécutives** :
   `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q`, deux fois de suite.
   La phase ajoute une dizaine de `[WpfFact]` ; le chargeur BAML de WPF a un précédent de course sous
   parallélisme xUnit (décision 16-03), et le symptôme est **indétectable par relance isolée**.
   Consigner les deux totaux ET les deux durées.
2. **Les cinq gardes permanentes**, chacune par sa commande, chacune avec son résultat :
   `ServicesLayerPurityTests`, `CompositionRootTests`, `NormalisationUniqueTests`, `GardesDoctrineTests`,
   et `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte`.
3. **Bilan de comptage réconcilié NOMINATIVEMENT** depuis la baseline **699**, plan par plan, dans le
   format de 19-05 : chaque écart (+ et −) porté par un nom de test ou de classe. Le critère de sortie
   est **0 échec + chaque écart justifié nominativement**, et **jamais** « ≥ 699 » : cette phase supprime
   deux tests (`IsStale_…`, `EstimatedTokens_seul_…`) et les remplace par des gardes de niveau supérieur.
4. **Sécurité et intégrité**, relevés AVANT et APRÈS le travail de la phase :
   - `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` → attendu `518 1783863147` ;
   - nombre de fichiers SOURCE contenant `RefreshAsync` → attendu exactement **2** ;
   - `~/.claude/settings.json` intact — **l'overlay n'a pas été lancé** ;
   - aucune requête réseau réelle depuis un test, aucune écriture dans le vrai `%APPDATA%\Chronos\`.
5. **Remplir `20-VALIDATION.md`** : la « Per-Task Verification Map » (une ligne par tâche des 5 plans,
   avec sa commande automatisée réelle et son statut), cocher les 4 cases de « Wave 0 Requirements » et
   les 6 de « Validation Sign-Off », passer le frontmatter `status: planned` → `status: validated`,
   `nyquist_compliant: true`, `wave_0_complete: true`, et **dater le relevé de porte**.
   La ligne des vérifications manuelles (20-05 T3) porte DÉJÀ le statut **DISTINCT** `⏳ à vérifier par
   l'utilisateur`, ni ✅ ni ⬜ : le conserver tel quel. Une porte qui se déclare verte sur une preuve qu'elle n'a pas produite est exactement le
   défaut que ce milestone corrige.
6. **Cocher EXA-03 et EXA-06** dans `.planning/REQUIREMENTS.md` (les deux listes `- [ ]` et la table de
   traçabilité : `Pending` → `Complete`), et mettre la ligne « Phase 20 » de `.planning/ROADMAP.md` à
   `5/5 · Complete` avec la date. **Ne rien décocher d'autre** : les exigences des phases 15 à 19 sont
   déjà closes, y toucher contredirait des décisions documentées.
7. Si un écart de la carte ne peut pas être justifié, **l'écrire** plutôt que d'arrondir : le SUMMARY de
   cette tâche doit pouvoir être lu comme un constat, pas comme une déclaration de victoire.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - Les **deux** exécutions de la suite complète rendent **0 échec** et **le même total** ; les deux
      totaux et les deux durées sont consignés dans le SUMMARY
    - Les 5 gardes permanentes sont vertes, chacune avec sa commande et son compte dans le SUMMARY
    - `grep -c "⬜ pending" .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md`
      rend **0**
    - `grep -c "⏳" .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md` rend
      **au moins 1** (la vérification déléguée porte un statut distinct)
    - `grep -c "status: validated" .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-VALIDATION.md`
      rend **1**
    - `grep -c "| EXA-03 | Phase 20 | Complete |" .planning/REQUIREMENTS.md` rend **1** et
      `grep -c "| EXA-06 | Phase 20 | Complete |" .planning/REQUIREMENTS.md` rend **1**
    - `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`
    - `grep -rlc --include=*.cs "RefreshAsync" src/Chronos | wc -l` rend **2**
  </acceptance_criteria>

  <done>
La porte est franchie sur des preuves nommées, et ce qui n'a pas été prouvé est identifié comme tel.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : constat visuel — la phase 20 ET les vérifications héritées du milestone v1.5</name>
  <files>(aucun fichier modifié — vérification humaine sur la machine réelle)</files>

  <read_first>
    - .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-04-SUMMARY.md (ce qui a réellement été
      dessiné, et les écarts éventuels au plan — le protocole doit décrire le code livré, pas le prévu)
    - .planning/phases/19-nouvelle-doctrine-du-composite/19-05-SUMMARY.md (le constat de bascule resté
      en souffrance, et le rappel que l'exe publié porte encore le comportement d'avant la phase 19)
    - .planning/phases/20-honn-tet-visible-cadran-diagnostic/20-CONTEXT.md (section `<deferred>` : la
      liste nominative des vérifications héritées des phases 17, 18 et 19 à solder ici)
    - src/Chronos/Views/MainWindow.xaml (état final : noms des éléments à citer dans le protocole)
  </read_first>

  <action>
Point d'arrêt HUMAIN. L'agent ne lance PAS l'overlay et ne republie PAS l'exe : les deux gestes touchent
la machine réelle de l'utilisateur (la republication écrase l'exe d'autostart, le lancement purge les
entrées Chronos de `~/.claude/settings.json`). L'agent PRÉSENTE le protocole ci-dessous, attend la
réponse, puis consigne le verdict — point par point — dans le SUMMARY du plan, avec le statut `⏳` si
l'utilisateur diffère une partie des constats. Ne JAMAIS simuler un constat visuel ni le déclarer
satisfait par défaut : le précédent 19-05 a documenté la substitution comme telle, et c'est la règle.
  </action>

  <what-built>
**Phase 20 (cette phase) :**
- Le style **Anneaux** rend l'arc de valeur en **pointillé** quand la fenêtre est un plancher (les quatre
  autres styles le faisaient déjà par leur grain). Un exact daté mais vérifié reste un trait **plein**.
- Une **rangée** de pastilles en bas à droite remplace trois pastilles libres qui pouvaient se
  superposer. La quatrième — un **anneau creux gris** — dit « au moins un des deux chiffres est daté »,
  et son **infobulle** nomme la source et l'ancienneté de chaque fenêtre.
- Le mot **« indisponible »** apparaît en bas à gauche quand il n'y a plus rien à afficher.
- Le **diagnostic** (clic droit → Diagnostic…) dit désormais, pour chaque fenêtre :
  `EXACT — 42 % · source : sonde d'en-têtes de rate-limit · relevé il y a 12 min · frais`.
- La grille des **réglages** n'a plus de cellule vide : « Diagnostic… » occupe toute la ligne du bas.

**Rappel important :** tant que l'exe n'est pas **republié**, l'overlay lancé par l'autostart porte encore
le comportement d'avant la phase 19 — vous ne verrez **rien** de tout cela.
  </what-built>

  <how-to-verify>
⚠️ **AVANT TOUT — sauvegarde obligatoire.** Lancer l'overlay déclenche la réconciliation de la phase 15,
qui **purge les entrées Chronos de `~/.claude/settings.json`** (état constaté : 25 groupes de hooks au
lieu de 5). C'est le comportement voulu, mais il doit se faire sous votre supervision :
```
cp ~/.claude/settings.json ~/.claude/settings.json.avant-phase20
```

**Republier, puis lancer :**
```
dotnet publish src/Chronos/Chronos.csproj -c Release -r win-x64 -p:PublishSingleFile=true
```
puis lancer l'exe produit sous `bin/Release/net8.0-windows/win-x64/publish/`.

**1. Le pointillé des Anneaux (le point n° 1 — il n'a JAMAIS été rendu à l'écran)**
   Le rendu de `StrokeDashArray` sur `RingArc` aux rayons 38 / 52 / 54 n'a pas pu être exécuté pendant
   la conception. À constater : le pointillé se distingue-t-il du trait plein **à distance de lecture
   normale**, sans zoomer ? Est-il lisible **sans être bruyant** ? Si les tirets paraissent trop longs
   (anneau « cassé ») ou trop fins (invisibles), c'est la valeur `0.55 0.45` qu'il faut ajuster —
   en multiples de l'épaisseur, jamais en pixels.

**2. Les quatre marques, sur fond d'écran RÉEL**
   Balayer quelques combinaisons représentatives (≈ 10 captures suffisent) : les **5 styles** × les
   **2 modes**, et 2 ou 3 des **9 thèmes** dont un clair s'il y en a un.
   - Le cas nominal (exact frais) porte-t-il bien **aucune** marque ?
   - L'anneau creux gris se distingue-t-il des pastilles pleines voisines d'un coup d'œil ?
   - Le mot « indisponible » est-il lisible sans être envahissant ?
   ⚠️ Défaut connu et **hors périmètre** : les 4 `UserControl` de styles alternatifs codent leurs
   couleurs en dur — sur un thème clair, leurs textes internes sont déjà peu lisibles, indépendamment
   de cette phase.

**3. La rangée de pastilles n'empiète sur aucun anneau**
   Dans les 9 thèmes, et surtout en mode Étendu (3 anneaux, le plus dense).

**4. L'infobulle de la pastille d'âge**
   Survoler l'anneau creux : elle doit nommer la source et l'ancienneté des **deux** fenêtres.

**5. Le diagnostic**
   Clic droit → Diagnostic… : la ligne « 5 h » nomme-t-elle une source plausible ? L'ancienneté
   correspond-elle à ce que vous savez de votre usage ? Si la source affichée n'est pas celle attendue,
   **c'est une information, pas un bug** : c'est exactement le service que rend EXA-06.

**— VÉRIFICATIONS HÉRITÉES DU MILESTONE, à solder ici (dernière phase de v1.5) —**

**6. Phase 19 — la bascule visuelle réelle** : l'ancien « 10 % » figé depuis deux mois doit avoir cédé
   la place, soit à un chiffre frais, soit à « indisponible » accompagné de l'invitation à se connecter.

**7. Phase 18 (HDR-02) — le 429 réel** : si l'API vous renvoie un 429, le cadran doit continuer
   d'afficher des chiffres **exacts et à jour** au lieu de basculer en « indisponible ». C'est
   l'instant où l'overlay sert le plus, et ce cas ne peut être constaté que sur une saturation réelle.

**8. Phase 17 — le parcours de reconnexion de bout en bout** : si la pastille ambre apparaît, un clic
   doit relancer le login et la pastille doit disparaître dès qu'un chiffre exact revient. Vérifier
   aussi le **contraste des pastilles** sur votre fond d'écran.

**Après la vérification :** comparer `~/.claude/settings.json` à la sauvegarde — Chronos doit y avoir
**5** groupes de hooks, pas 25, et les entrées non-Chronos doivent être intactes.
  </how-to-verify>

  <verify>
    <automated>node -e "const fs=require('fs'),os=require('os'),p=os.homedir()+'/.claude/settings.json';const j=JSON.parse(fs.readFileSync(p,'utf8'));const n=JSON.stringify(j).split('--hook').length-1;console.log('hooks Chronos:',n);if(n>5){process.exit(1)}"</automated>
  </verify>

  <acceptance_criteria>
    - Le protocole présenté cite la sauvegarde de `~/.claude/settings.json` **avant** toute instruction
      de lancement (ordre vérifiable à la lecture : la ligne `cp ~/.claude/settings.json …` précède la
      ligne `dotnet publish`)
    - L'agent n'a lancé NI `dotnet publish` NI l'exe : `git status --short` ne montre aucun artefact de
      publication, et `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend toujours `518 1783863147`
    - Après le constat de l'utilisateur : `~/.claude/settings.json` contient **au plus 5** groupes de
      hooks Chronos (commande du bloc `<verify>`), et les entrées non-Chronos sont intactes
    - Le SUMMARY consigne un verdict **point par point** (8 points), chacun avec son statut ✅ / ⚠️ / ⏳ —
      aucun point n'est déclaré satisfait sans réponse de l'utilisateur
    - Si l'utilisateur signale un défaut sur le point n° 1 (longueur du pointillé), la valeur corrigée
      est exprimée en **multiples de `StrokeThickness`** et jamais en pixels, et le test
      `Le_plancher_hebdo_rend_l_arc_des_Anneaux_en_pointille` reste vert après ajustement
  </acceptance_criteria>

  <resume-signal>
Répondre « approuvé » si les huit points sont satisfaisants, ou décrire ce qui cloche — en particulier
pour le point n° 1 (longueur et espacement du pointillé) et le point n° 2 (lisibilité des marques),
qui sont les deux seuls arbitrages de cette phase que la conception n'a **pas pu** trancher par la mesure.
  </resume-signal>

  <done>
L'utilisateur a constaté de ses yeux ce que les tests ne peuvent pas produire, et le milestone v1.5 n'a
plus de vérification humaine en souffrance.
  </done>
</task>

</tasks>

<verification>
1. Suite complète verte, **deux exécutions**, 0 échec.
2. Les cinq gardes permanentes vertes.
3. `20-VALIDATION.md` : carte remplie, aucune ligne `⬜ pending`, sign-off coché, frontmatter
   `status: validated`, vérification déléguée marquée `⏳`.
4. `REQUIREMENTS.md` : EXA-03 et EXA-06 cochées ; couverture v1.5 = **24 / 24**.
5. `ROADMAP.md` : Phase 20 à `5/5 · Complete`.
6. Coffre de jetons intact (`518 1783863147`), 2 fichiers source avec `RefreshAsync`, aucune requête
   réseau depuis un test, aucune écriture dans le vrai `%APPDATA%\Chronos\`.
</verification>

<success_criteria>
- EXA-06 est satisfaite des deux côtés : au cadran (infobulle) et dans le rapport (source + ancienneté).
- Le projet n'a plus qu'un vocabulaire pour dire « borne inférieure » : « ≥ », partout.
- La porte de phase est un **constat**, pas une déclaration : chaque écart de comptage porte un nom.
- Les vérifications visuelles héritées des phases 17, 18 et 19 sont délivrées à l'utilisateur en un seul
  protocole, avec la sauvegarde préalable qu'elles exigent — et non simulées.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-05-SUMMARY.md`
</output>
