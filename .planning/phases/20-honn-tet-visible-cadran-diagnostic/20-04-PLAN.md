---
phase: 20-honn-tet-visible-cadran-diagnostic
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - src/Chronos/Views/MainWindow.xaml
  - tests/Chronos.Tests/CadranBindingTests.cs
  - tests/Chronos.Tests/ThemingTests.cs
autonomous: true
requirements: [EXA-03]

must_haves:
  truths:
    - "Sans lire de texte, l'utilisateur distingue un chiffre exact frais (aucune marque), un chiffre daté (marque d'âge), un plancher (marque d'âge + texture) et un état indisponible"
    - "Le style Arcs a enfin la texture de plancher que les quatre autres styles avaient déjà"
    - "Les pastilles ne peuvent plus se recouvrir : le non-recouvrement est STRUCTUREL, pas conventionnel"
    - "L'état « indisponible » se distingue de l'état « en attente », qui produit aujourd'hui le même visuel neutre"
  artifacts:
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "StrokeDashArray sur les 3 arcs de valeur, rangée de 4 pastilles, mot « indisponible »"
      contains: "StrokeDashArray"
  key_links:
    - from: "SevenDay.EstPlancher / FiveHour.EstPlancher"
      to: "RingArc.StrokeDashArray"
      via: "DataTrigger dans un Style local, unités en multiples de StrokeThickness"
      pattern: "DataTrigger Binding=.*EstPlancher"
    - from: "MainViewModel.AfficherReleveDate"
      to: "PastilleReleveDate"
      via: "Visibility + BoolToVis dans la rangée bas-droite"
      pattern: "AfficherReleveDate"
    - from: "MainViewModel.DataUnavailable"
      to: "MotIndisponible"
      via: "Visibility + BoolToVis, couche racine, style-agnostique"
      pattern: "DataUnavailable"
---

<objective>
Le dessin. Trois marques, quatre apparences — parce que deux marques se composent :

| # | Apparence | Marques | Ce que ça dit |
|---|---|---|---|
| 1 | Exact frais | **aucune** | Le cas nominal. L'absence de marque EST la marque de la fraîcheur : un overlay qui décore son cas normal fabrique du bruit permanent |
| 2 | Exact daté (`EncoreValide`) | M-âge | « Relevé il y a un moment, et j'ai VÉRIFIÉ qu'il est toujours juste » |
| 3 | Plancher (`PlancherAvecActivite`) | M-âge + M-plancher | « Vieux, ET il s'est passé des choses depuis : c'est un minimum » |
| 4 | Indisponible | M-indispo | « Je ne sais pas » |

Purpose : c'est la seule phase du milestone dont le livrable est visuel. Les phases 16 à 19 ont livré la
doctrine et son état ; ici il arrive à l'œil.

Output : `MainWindow.xaml` porte les trois marques, une fois, pour 5 styles × 9 thèmes × 2 modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-RESEARCH.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-03-SUMMARY.md

<interfaces>
<!-- Ce qui est disponible à l'entrée de ce plan. Ne PAS explorer le dépôt pour le retrouver. -->

ViewModel (livré par 20-03) :
```csharp
WindowGaugeViewModel : EstPlancher (bool), EstDate (bool), UtilizationText, CountdownText, ValueBrush, …
MainViewModel : AfficherReleveDate (bool), InfobulleReleve (string), DataUnavailable (bool),
                AfficherPastilleDeconnexion, AfficherPastilleHorsLigne, AfficherInvitationConnexion,
                ReconnecterCommand, IsStyleArcs/…/IsStyleVolets, IsModeNormal/IsModeEtendu
```

`RingArc : Shape` (src/Chronos/Controls/RingArc.cs) — il **hérite** donc `Stroke`, `StrokeThickness`,
`StrokeDashArray`, `StrokeDashCap`. Aucune géométrie nouvelle à écrire.

Les **trois arcs de VALEUR** de `MainWindow.xaml` (les `Fraction="1"` sont des pistes, à ne pas toucher) :
| x:Name | Radius | StrokeThickness | Fenêtre portée | Mode |
|---|---|---|---|---|
| `ArcHebdo` (l. 36) | 38 | 8 | `SevenDay` | les DEUX modes |
| `ArcCinqHeures` (l. 46) | 54 | 8 | `FiveHour` | Étendu seulement |
| `ArcTimelineNormal` (l. 69) | 52 | 7 | géométrie = jour, **couleur = `FiveHour.ValueBrush`** | Normal seulement |
| `ArcVingtQuatreHeures` (l. 54) | 64 | 4 | idem, anneau FIN | Étendu seulement |

Couche racine (l. 135-203) — son commentaire de production, à respecter :
« DERNIERS enfants du Grid racine => au-dessus de TOUS les styles de cadran : uniques pour les 5 styles
et les 2 modes, sans duplication ». C'est **la seule couche style-agnostique du projet**, et la seule en
`{DynamicResource}` : les 4 `UserControl` de `Views/Cadrans/` codent leurs couleurs **en dur**
(`#F4F2EC`, `#C7C6D0`, `#A9A8B2`, `#211D2A`, `#161019`, `#55000000`).

Tokens disponibles (`Resources/DesignTokens.xaml`, tous écrasés par thème via `ChronosTheme.BrushTokens()`) :
`FondCadran, Rim, TickMineur, TickMajeur, TickVisible, TickReset, Piste5h, PisteHebdo, Piste24h,
TextePrincipal, TexteSecondaireClair, TexteSecondaire, Alerte`.
`TexteSecondaire` est DÉJÀ le token des mentions annexes (estimée, épuisé, indisponible, périmée).

Helpers de test disponibles (`tests/Chronos.Tests/CadranBindingTests.cs`) :
```csharp
private static MainWindow BuildWindow(UsageSnapshot snap, out MainViewModel vm);           // + TempPaths (20-01)
private static (MainWindow fenetre, FrameworkElement racine) MonterPastille(
    EtatAuthentification etat, UsageSnapshot snap, out MainViewModel vm);
// MonterPastille pose le DataContext sur la RACINE DU CONTENU puis PURGE la file du Dispatcher
// (DispatcherPriority.ApplicationIdle) avant Measure/Arrange sur 170×170.
```
</interfaces>

<pieges>
- **Piège 1 — jamais un hex.** Toute marque nouvelle utilise un `{DynamicResource}` de `DesignTokens.xaml`.
  Un `#` dans un diff de cette phase hors `DesignTokens.xaml` / `ChronosTheme.cs` est un défaut.
  `MainWindow.xaml` n'en contient **aucun aujourd'hui** : ce compte doit rester à 0.
- **Piège 4 — un binding qui ne s'évalue jamais rend un test vert pour une mauvaise raison.** Une `Window`
  jamais affichée n'a pas de template : son `Content` n'a aucun parent visuel, le `DataContext` ne se
  propage pas, `Visibility` reste à son défaut `Visible` et `Command` reste `null`. Tout nouveau test de
  ce plan passe par `MonterPastille` (DataContext sur la racine + purge du Dispatcher + Measure/Arrange),
  **jamais** par `BuildWindow` seul.
- **Piège 5 — marquer « exact daté » comme dégradé serait une régression d'honnêteté.** `EncoreValide`
  signifie que la doctrine est ALLÉE VÉRIFIER et a PROUVÉ que l'utilisation n'a pas bougé. M-âge est donc
  **informative et additive** (elle s'ajoute à côté), jamais **soustractive** (elle ne retire ni
  luminance, ni saturation, ni netteté au chiffre).
- **Aucun `Storyboard`, `BlurEffect` ni `DropShadowEffect` sur `MainWindow`** : `AllowsTransparency=True`
  force le rendu LOGICIEL, et le tick d'une seconde rendrait le coût permanent. Un `StrokeDashArray` est
  de la géométrie : gratuit.
- **`[Collection("XAML WPF")]` obligatoire** pour toute classe chargeant du BAML. En pratique :
  **étendre `CadranBindingTests`**, ne pas créer de classe.
- **Contrainte dure héritée de la phase 19 : PAS d'arc de delta, PAS de barre d'erreur.** Un delta chiffré
  serait une fiction (280 % d'erreur mesurée). La sémiologie exprime « au moins », jamais « environ ± x ».
</pieges>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : la texture de plancher aux Anneaux — le cinquième style enfin marqué</name>
  <files>src/Chronos/Views/MainWindow.xaml, tests/Chronos.Tests/CadranBindingTests.cs</files>

  <read_first>
    - src/Chronos/Views/MainWindow.xaml (l. 26-110 : le groupe « ANNEAUX », les pistes et les 4 arcs de valeur)
    - src/Chronos/Controls/RingArc.cs (INTÉGRALEMENT — constater `: Shape`, donc `Stroke*` hérités)
    - src/Chronos/Controls/Cadrans/EmberRingControl.cs (l. 70-95 : `DashStyle{1.4, 1.4}` sur un `Pen` de
      1,4 — c'est le grain de référence auquel le pointillé des Anneaux doit être comparable)
    - tests/Chronos.Tests/CadranBindingTests.cs (helpers `BuildWindow` et `MonterPastille`)
  </read_first>

  <behavior>
    - Hebdo en plancher ⇒ `ArcHebdo.StrokeDashArray` contient 2 valeurs ; hebdo frais ⇒ elle est vide.
    - 5 h en plancher, mode Étendu ⇒ `ArcCinqHeures.StrokeDashArray` contient 2 valeurs.
    - 5 h en plancher, mode Normal ⇒ `ArcTimelineNormal.StrokeDashArray` contient 2 valeurs (c'est le
      seul anneau qui porte la couleur du 5 h dans ce mode).
    - Une fenêtre `EncoreValide` ⇒ trait **PLEIN** : un exact daté mais vérifié n'est pas un plancher.
    - Une fenêtre `Frais` ⇒ trait plein.
  </behavior>

  <action>
Le canal **texture** existe déjà dans 4 styles sur 5 : `EmberRingControl`, `FuseBar`, `TideColumn` et
`CadranVoletsView` portent une DP `Estimated`, désormais bindée sur `EstPlancher` (plan 20-03), qui rend
un grain ou un pointillé. Seul le style **Anneaux** n'a aucun canal texture. On le lui donne, et c'est
tout le travail de M-plancher.

Ajouter à chacun des **trois** arcs de valeur `ArcHebdo`, `ArcCinqHeures` et `ArcTimelineNormal` un
`Style` local avec un `DataTrigger` :

```xml
<ctrl:RingArc.Style>
    <Style TargetType="ctrl:RingArc">
        <Style.Triggers>
            <!-- M-PLANCHER : le pointillé dit « borne inférieure », jamais « vieux ». Un exact daté
                 mais VÉRIFIÉ (EncoreValide) reste un trait PLEIN — la doctrine a prouvé qu'il est juste.
                 StrokeDashArray est hérité de Shape : aucune géométrie nouvelle, aucun effet, donc
                 gratuit sous rendu logiciel (AllowsTransparency).
                 UNITÉS : multiples de StrokeThickness, PAS des pixels. À 8 px d'épaisseur,
                 « 0.55 0.45 » donne des tirets de 4,4 px espacés de 3,6 px — comparable au grain des
                 Braises (DashStyle{1.4,1.4} sur un Pen de 1,4). Une valeur en pixels (« 4 3 »)
                 produirait des tirets de 32 px : l'anneau paraîtrait cassé. -->
            <DataTrigger Binding="{Binding SevenDay.EstPlancher}" Value="True">
                <Setter Property="StrokeDashArray" Value="0.55 0.45"/>
                <Setter Property="StrokeDashCap" Value="Round"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ctrl:RingArc.Style>
```

- `ArcHebdo` (R38, épaisseur 8) → binding sur `SevenDay.EstPlancher`.
- `ArcCinqHeures` (R54, épaisseur 8) → binding sur `FiveHour.EstPlancher`.
- `ArcTimelineNormal` (R52, épaisseur 7) → binding sur `FiveHour.EstPlancher`, avec un commentaire
  expliquant POURQUOI : en mode Normal il n'y a pas d'anneau 5 h dédié, l'usage 5 h passe dans la
  COULEUR de cet anneau — c'est donc lui qui doit porter la marque du plancher 5 h.

**NE PAS** marquer `ArcVingtQuatreHeures` (R64, épaisseur 4, mode Étendu) : en mode Étendu le 5 h a déjà
son arc épais marqué juste en dessous ; répéter la marque sur un anneau de 4 px produirait un pointillé
de 2,2 px, illisible, et une redondance bruyante. Écrire cette décision en commentaire — sinon elle
passera pour un oubli au prochain passage.
**NE PAS** toucher aux `RingArc Fraction="1"` (les pistes) : une piste en pointillé serait un mensonge
géométrique.

Tests (écrits d'abord) dans `CadranBindingTests`, tous en `[WpfFact]` :
- Ajouter un helper `MonterCadran(UsageSnapshot snap, out MainViewModel vm)` =
  `MonterPastille(EtatAuthentification.Connecte, snap, out vm)` — le montage qui propage réellement le
  `DataContext` et purge la file du Dispatcher. Un test qui utiliserait `BuildWindow` seul serait vert
  pour une mauvaise raison (Piège 4).
- `Le_plancher_hebdo_rend_l_arc_des_Anneaux_en_pointille` : poser explicitement
  `vm.CadranStyle = CadranStyle.Arcs`, snapshot hebdo `Estimated` + `PlancherAvecActivite`,
  asserter `Assert.Equal(2, arc.StrokeDashArray.Count)` sur `ArcHebdo`.
- `Un_exact_frais_laisse_l_arc_des_Anneaux_PLEIN` : contre-épreuve, `Assert.Empty(arc.StrokeDashArray)`.
- `Un_EncoreValide_laisse_l_arc_PLEIN_car_il_n_est_PAS_un_plancher` : la non-régression d'honnêteté du
  Piège 5, en une assertion.
- `Le_plancher_5h_marque_l_anneau_du_mode_NORMAL` : `vm.IsModeEtendu = false`, assertion sur
  `ArcTimelineNormal`.
- `Le_plancher_5h_marque_l_anneau_du_mode_ETENDU` : `vm.IsModeEtendu = true`, assertion sur
  `ArcCinqHeures`, **et** `Assert.Empty(((RingArc)f.FindName("ArcVingtQuatreHeures")).StrokeDashArray)`
  — la décision de ne pas marquer l'anneau fin est verrouillée par un test, pas par un commentaire seul.

⚠️ Les réglages de style et de mode se posent **explicitement dans chaque test** : ne jamais dépendre du
persisté (la migration vers `TempPaths()` du plan 20-01 a supprimé la dépendance au profil réel, mais
l'explicite reste la règle).
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~CadranBindingTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "StrokeDashArray" src/Chronos/Views/MainWindow.xaml` rend **3**
    - `grep -c "EstPlancher" src/Chronos/Views/MainWindow.xaml` rend **3**
      (`SevenDay` ×1, `FiveHour` ×2)
    - `grep -c "#" src/Chronos/Views/MainWindow.xaml` rend **0** (aucune couleur en dur introduite)
    - `grep -c "Storyboard\|BlurEffect\|DropShadowEffect" src/Chronos/Views/MainWindow.xaml` rend **0**
    - `grep -c "MonterCadran" tests/Chronos.Tests/CadranBindingTests.cs` rend **au moins 6**
      (le helper + les 5 tests)
    - Les 5 nouveaux `[WpfFact]` passent ; les 14 tests préexistants de `CadranBindingTests` restent verts
  </acceptance_criteria>

  <done>
Les cinq styles marquent désormais le plancher, chacun dans son vocabulaire visuel, tous depuis le même
booléen. Reste à constater à l'œil que le pointillé se lit à distance normale — c'est le point n° 1 de
la vérification humaine du plan 05.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : la rangée de pastilles — le non-recouvrement devient structurel, et M-âge y prend place</name>
  <files>src/Chronos/Views/MainWindow.xaml, tests/Chronos.Tests/CadranBindingTests.cs</files>

  <read_first>
    - src/Chronos/Views/MainWindow.xaml (l. 135-203 INTÉGRALEMENT : les 3 pastilles, leurs commentaires
      de justification, leurs `Margin` — 8,8 pour l'Ellipse de 10 px, 6,6 pour les Buttons de 14 px)
    - src/Chronos/ViewModels/MainViewModel.cs (`MajPastilles` après le plan 20-03 : les QUATRE marques)
    - tests/Chronos.Tests/CadranBindingTests.cs (les 6 tests de pastilles, l. 250-395 — DEUX d'entre eux
      assertent `HorizontalAlignment.Right` / `VerticalAlignment.Bottom` **sur le Button lui-même**)
    - src/Chronos/Resources/DesignTokens.xaml
    - src/Chronos/Theming/ChronosTheme.cs (l. 58-78 : `BrushTokens()`, qui écrase les tokens par thème)
  </read_first>

  <behavior>
    - Quand `AfficherPastilleHorsLigne` et `AfficherInvitationConnexion` sont vraies EN MÊME TEMPS (cas
      réellement atteignable : `HorsLigne` ⇒ `Deconnexion = false` ⇒ l'invitation peut valoir `true`),
      les deux pastilles sont visibles et leurs rectangles mis en page **ne s'intersectent pas**.
    - Avec les QUATRE marques allumées, aucune pastille n'est à moins de **71,5 px** du centre (85,85),
      et l'empreinte de la fenêtre reste **170 × 170**.
    - La pastille d'âge est visible ssi `AfficherReleveDate`, et son `ToolTip` rend `InfobulleReleve`.
    - La pastille d'âge n'est **pas** actionnable : elle n'a aucune `Command`.
  </behavior>

  <action>
Défaut existant, prouvé par lecture de `MajPastilles` : `PastilleHorsLigne` (Ellipse 10 px,
`Margin="0,0,8,8"`) et `PastilleInvitationConnexion` (Button 14 px, `Margin="0,0,6,6"`) peuvent être
visibles simultanément et **occupent le même coin**. La phase 20 y ajouterait une quatrième pastille.
La correction n'est donc pas un embellissement : c'est la réparation d'un défaut, et elle doit être
**structurelle** — un `StackPanel` horizontal, où le non-recouvrement ne dépend plus d'une convention
entre deux `Margin`.

1. Envelopper les **trois** pastilles existantes, **dans l'ordre et sans changer leur `x:Name`, leur
   `ToolTip`, leur `Command`, leur `Background` ni leur `Template`**, dans :
   ```xml
   <!-- DERNIERS enfants du Grid racine => au-dessus de TOUS les styles de cadran : uniques pour les 5
        styles et les 2 modes, sans duplication. Rangée et non superposition : deux pastilles peuvent
        légitimement être allumées ensemble (hors ligne + invitation), et deux Margin conventionnelles
        ne garantissent rien. Mesure : 4 pastilles de 14 px avec 4 px d'écart et 6 px de marge placent
        le coin gauche de la rangée à x = 96, soit 72,8 px du centre (seuil des anneaux : 71,5 px).
        QUATRE tiennent ; CINQ seraient la limite — borne dure, à ne pas franchir sans remesurer. -->
   <StackPanel x:Name="RangeePastilles" Orientation="Horizontal"
               HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,6,6">
   ```
   Retirer de chaque pastille `HorizontalAlignment`, `VerticalAlignment` et sa `Margin` propre ; poser à
   la place `VerticalAlignment="Center"` et `Margin="0,0,4,0"` sur chacune sauf la dernière (écart de
   4 px entre voisines, aucun après la dernière).
2. Ajouter en **premier** enfant de la rangée la pastille d'âge :
   ```xml
   <!-- M-ÂGE (EXA-03) : au moins une des deux fenêtres porte un chiffre DATÉ. INFORMATIVE et INERTE —
        aucune Command : ne jamais crier « répare-moi » pour un fait qui n'appelle aucun geste (même
        règle que la pastille « hors ligne »). ADDITIVE et non soustractive : elle s'ajoute À CÔTÉ du
        chiffre, elle ne lui retire ni luminance ni netteté — un EncoreValide est un chiffre PROUVÉ
        juste (Piège 5).
        ANNEAU CREUX et non disque plein : la forme la distingue au premier coup d'œil des deux
        pastilles pleines voisines, et un cadran vide dit « du temps a passé » sans mot.
        Fill="Transparent" (et non {x:Null}) : hit-testable sur fenêtre AllowsTransparency, donc son
        ToolTip s'ouvre — c'est lui qui porte EXA-06 au cadran. -->
   <Ellipse x:Name="PastilleReleveDate" Width="12" Height="12" VerticalAlignment="Center"
            Margin="0,0,4,0" Fill="Transparent"
            Stroke="{DynamicResource TexteSecondaire}" StrokeThickness="2"
            ToolTip="{Binding InfobulleReleve}"
            Visibility="{Binding AfficherReleveDate, Converter={StaticResource BoolToVis}}"/>
   ```
   Ordre final de la rangée, de gauche à droite : **âge, hors ligne, invitation, déconnexion** —
   informatives d'abord, actionnables au plus près du coin.
   `TexteSecondaire` est déjà le token des mentions annexes et il est écrasé par `BrushTokens()` dans
   les 9 thèmes : **aucun token nouveau, aucun hex**.
3. **Réécrire les deux assertions devenues fausses** — `La_pastille_est_bindee_sur_ReconnecterCommand_…`
   et `L_invitation_est_bindee_sur_ReconnecterCommand_…` assertent aujourd'hui
   `HorizontalAlignment.Right` / `VerticalAlignment.Bottom` **sur le Button**. Dans une rangée, ces
   propriétés appartiennent au `StackPanel`. Porter les deux assertions sur
   `fenetre.FindName("RangeePastilles")`, et **conserver intactes** toutes les autres assertions de ces
   deux tests (identité de commande par instance, `Background` non-null, `Visibility` résolue) : c'est
   le piège de sécurité de la phase 17, il ne doit pas s'affaiblir en passant.
4. Nouveaux `[WpfFact]` dans `CadranBindingTests` :
   - `Hors_ligne_et_invitation_allumees_ensemble_ne_se_RECOUVRENT_pas` : monter avec
     `EtatAuthentification.HorsLigne` et le snapshot « jamais d'exact » (`JamaisDExact()`, déjà présent
     dans la classe) ; asserter les deux `Visibility.Visible`, puis calculer les deux rectangles par
     `TransformToAncestor(racine)` + `ActualWidth/ActualHeight` et asserter
     `Rect.Intersect(r1, r2).IsEmpty`. **C'est la reproduction du bug, puis sa preuve de correction.**
   - `Les_QUATRE_pastilles_allumees_tiennent_hors_de_toute_geometrie_d_anneau` : forcer les quatre
     booléens directement sur le VM (`vm.AfficherPastilleDeconnexion = true;` etc.) — c'est un test de
     GÉOMÉTRIE, pas de sémantique : la recomposition garantit ce qui s'allume ensemble, la rangée doit
     garantir que la géométrie tient **même si la recomposition change demain**. Purger la file du
     Dispatcher après les affectations, ré-`Measure`/`Arrange` en 170×170, puis asserter pour chacune
     des 4 une distance au centre `> 71,5` px, et `Assert.Equal(170d, fenetre.Width)`.
   - `La_pastille_d_age_est_inerte_et_porte_l_infobulle_de_source` : `Assert.IsType<Ellipse>`,
     `Assert.Equal(vm.InfobulleReleve, pastille.ToolTip)`, et **aucune** propriété `Command` (l'`Ellipse`
     n'en a pas : c'est précisément pourquoi le type est choisi — l'asserter par `IsType` suffit).
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~CadranBindingTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'x:Name="RangeePastilles"' src/Chronos/Views/MainWindow.xaml` rend **1**
    - `grep -c 'x:Name="PastilleReleveDate"' src/Chronos/Views/MainWindow.xaml` rend **1**
    - `grep -c 'HorizontalAlignment="Right"' src/Chronos/Views/MainWindow.xaml` rend **1**
      (seule la rangée porte l'alignement ; les pastilles ne l'ont plus)
    - `grep -c "LoginClaudeCommand" src/Chronos/Views/MainWindow.xaml` rend **0** — la commande qui
      BASCULE et supprimerait le coffre de jetons n'est bindée nulle part
    - `grep -c "ReconnecterCommand" src/Chronos/Views/MainWindow.xaml` rend **2**
      (déconnexion + invitation ; la pastille d'âge n'en a PAS)
    - `grep -c "#" src/Chronos/Views/MainWindow.xaml` rend **0**
    - Les 3 nouveaux `[WpfFact]` passent ; les 6 tests de pastilles préexistants restent verts après
      réécriture des deux assertions d'alignement
  </acceptance_criteria>

  <done>
Deux pastilles ne peuvent plus se superposer, parce qu'elles ne peuvent plus occuper la même case. Et le
quatrième signal — l'âge — a sa place mesurée, son token de thème et son infobulle.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : le mot « indisponible » — lever l'ambiguïté avec l'état « en attente »</name>
  <files>src/Chronos/Views/MainWindow.xaml, tests/Chronos.Tests/CadranBindingTests.cs</files>

  <read_first>
    - src/Chronos/Views/MainWindow.xaml (l. 88-135 : les deux blocs centraux et `CentreHit`)
    - src/Chronos/ViewModels/MainViewModel.cs (`DataUnavailable`, posé dans `ApplySnapshot`)
    - src/Chronos/Controls/Cadrans/FuseBar.cs, TideColumn.cs (le rendu `HasData == false` → `WaitFill` :
      c'est l'état « en attente », visuellement neutre — le même neutre que l'indisponible)
    - tests/Chronos.Tests/CadranBindingTests.cs
  </read_first>

  <behavior>
    - Deux fenêtres `Unavailable` ⇒ le mot est `Visible`.
    - Au moins une fenêtre exploitable ⇒ le mot est `Collapsed`.
    - Le mot ne capte aucun clic : la zone centrale (`CentreHit`) continue de basculer % / temps.
    - Le rectangle du mot n'intersecte pas celui de la rangée de pastilles, et son centre est à plus de
      71,5 px du centre du cadran (aucun empiètement sur les anneaux).
  </behavior>

  <action>
M-indispo est aujourd'hui **à moitié livrée** : `Utilization = null` ⇒ arc neutre et `UtilizationText`
vide. Mais l'absence de chiffre est **ambiguë** avec l'état « en attente » (`HasData == false` ⇒ `WaitFill`
dans les 4 contrôles alternatifs), qui produit le même visuel neutre. Le mot lève l'ambiguïté ; sans lui,
EXA-03 n'est pas satisfaite. `DataUnavailable` est calculé, testé, et bindé nulle part — il le devient ici.

Ajouter dans la couche racine, **après** `CentreHit` et **avant** `RangeePastilles` :

```xml
<!-- M-INDISPO (EXA-03) : « je ne sais pas ». Le MOT, et pas seulement l'absence de chiffre — l'absence
     seule est ambiguë avec l'état « en attente » (HasData=false), qui rend le même neutre dans les
     quatre styles alternatifs.
     Couche racine => un seul exemplaire pour 5 styles × 2 modes × 9 thèmes, et le seul endroit du
     projet qui soit en {DynamicResource} (les UserControl de Views/Cadrans sont en couleurs dures).
     BAS-GAUCHE : hors de la géométrie des anneaux (> 71,5 px du centre) ET hors de la colonne de la
     rangée de pastilles, quelle que soit la taille de celle-ci — un mot centré en bas entrerait en
     collision avec la rangée dès trois pastilles.
     IsHitTestVisible=False : le clic continue d'atteindre CentreHit, donc la bascule % / temps
     fonctionne même quand il n'y a rien à afficher. -->
<TextBlock x:Name="MotIndisponible" Text="indisponible"
           HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="8,0,0,6"
           FontSize="10.5" IsHitTestVisible="False"
           Foreground="{DynamicResource TexteSecondaire}"
           Visibility="{Binding DataUnavailable, Converter={StaticResource BoolToVis}}"/>
```

Tests `[WpfFact]` dans `CadranBindingTests` :
- `Deux_fenetres_indisponibles_allument_le_mot_indisponible` : monter `UsageSnapshot.Empty` via
  `MonterCadran`, asserter `Visibility.Visible` **résolue** sur `MotIndisponible` et `vm.DataUnavailable`.
- `Un_chiffre_exploitable_eteint_le_mot_indisponible` : contre-épreuve, `Visibility.Collapsed`.
- `Le_mot_indisponible_n_empiete_ni_sur_les_anneaux_ni_sur_la_rangee_de_pastilles` : monter l'état
  « jamais d'exact » (le mot ET l'invitation sont alors allumés ensemble — c'est le cas réel), calculer
  les deux rectangles mis en page et asserter `Rect.Intersect(mot, rangee).IsEmpty`, puis que le centre
  du mot est à plus de 71,5 px de (85,85).
- Étendre le test existant `Etat_indisponible_deux_fenetres_Unavailable_ne_crashe_pas_et_centre_vide`
  d'une assertion sur `MotIndisponible` plutôt que d'en créer un doublon.

**Ne pas** ajouter de token de design nouveau : `TexteSecondaire` est déjà, par son commentaire d'origine,
le token des mentions annexes « estimée, épuisé, indisponible, périmée ».
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~CadranBindingTests|FullyQualifiedName~ThemingTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'x:Name="MotIndisponible"' src/Chronos/Views/MainWindow.xaml` rend **1**
    - `grep -c 'IsHitTestVisible="False"' src/Chronos/Views/MainWindow.xaml` rend **1**
    - `grep -c "DataUnavailable" src/Chronos/Views/MainWindow.xaml` rend **1**
    - `grep -c "#" src/Chronos/Views/MainWindow.xaml` rend **0**
    - `git diff --name-only` ne contient **ni** `src/Chronos/Resources/DesignTokens.xaml` **ni**
      `src/Chronos/Theming/ChronosTheme.cs` pour cette tâche (aucun token nouveau n'a été nécessaire ;
      si l'exécution en ajoute un, elle DOIT le justifier dans le SUMMARY et ajouter l'assertion
      correspondante dans `ThemingTests`, qui vérifie que chaque thème publie tous les tokens)
    - Les 3 nouveaux `[WpfFact]` passent ; `ThemingTests` reste vert
  </acceptance_criteria>

  <done>
« Je ne sais pas » se lit sur le cadran, dans les cinq styles et les neuf thèmes, sans voler un seul clic
à la zone centrale.
  </done>
</task>

</tasks>

<verification>
1. Suite complète : `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q`,
   **0 échec**, exécutée **DEUX fois** — cette phase ajoute une dizaine de `[WpfFact]` et le chargeur
   BAML de WPF a un précédent de course sous parallélisme xUnit (décision 16-03).
2. Les cinq gardes permanentes restent vertes, `ServicesLayerPurityTests` en tête : aucune de ces trois
   tâches n'a fait entrer un type WPF dans `Services/` ni `Models/`.
3. `grep -rn "#" src/Chronos/Views/MainWindow.xaml` rend 0 ligne : aucune couleur en dur dans la couche
   qui doit suivre les 9 thèmes.
4. `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`. **L'overlay n'est PAS lancé** :
   le constat visuel est délégué à l'utilisateur (plan 05).
</verification>

<success_criteria>
- Les quatre apparences sont distinguables **sans lire de texte** pour les trois premières ; la
  quatrième porte le mot parce que l'absence de chiffre, seule, est ambiguë.
- Une seule implémentation par marque, dans la couche racine ou sur les arcs — pas dix copies dans les
  `UserControl`.
- Le bug de superposition des pastilles est corrigé **structurellement**, et sa reproduction est un test.
- Zéro hex, zéro converter de couleur, zéro effet, zéro `Storyboard`, zéro dépendance NuGet.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-04-SUMMARY.md`
</output>
