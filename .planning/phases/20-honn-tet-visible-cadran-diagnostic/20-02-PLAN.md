---
phase: 20-honn-tet-visible-cadran-diagnostic
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/Chronos/Models/SourceUsage.cs
  - src/Chronos/Models/WindowState.cs
  - src/Chronos/Services/DoctrineFraicheur.cs
  - src/Chronos/Services/RateLimitHeaderUsageProvider.cs
  - src/Chronos/Services/ChronosOAuthUsageProvider.cs
  - src/Chronos/Services/ClaudeOAuthUsageProvider.cs
  - src/Chronos/Services/ClaudeUsageObjectProvider.cs
  - src/Chronos/Services/LastExactStore.cs
  - src/Chronos/ViewModels/WindowGaugeViewModel.cs
  - src/Chronos/Text/LibelleSource.cs
  - tests/Chronos.Tests/LibelleSourceTests.cs
  - tests/Chronos.Tests/GardesDoctrineTests.cs
  - tests/Chronos.Tests/CadranBindingTests.cs
autonomous: true
requirements: [EXA-06]

must_haves:
  truths:
    - "Chaque fenêtre d'usage sait NOMMER qui l'a produite, et ce nom survit aux trois composites imbriqués"
    - "Une fenêtre indisponible n'est alimentée par personne : elle ne nomme aucune source"
    - "Le vocabulaire français des sources est calculé en UN seul point du projet"
    - "Le champ mort EstimatedTokens a disparu du code, et son absence est gardée structurellement"
  artifacts:
    - path: "src/Chronos/Models/SourceUsage.cs"
      provides: "Les 5 sources de la chaîne DI réelle, sans membre « Inconnue »"
      contains: "enum SourceUsage"
    - path: "src/Chronos/Models/WindowState.cs"
      provides: "Source { get; init; } nullable — motif StatutServeur / Provenance"
      contains: "SourceUsage? Source"
    - path: "src/Chronos/Text/LibelleSource.cs"
      provides: "Vocabulaire FR UNIQUE : nom de source, ancienneté, provenance"
      contains: "class LibelleSource"
  key_links:
    - from: "les 5 producteurs de Services/"
      to: "WindowState.Source"
      via: "une ligne « Source = SourceUsage.X » par producteur"
      pattern: "Source = SourceUsage\\."
    - from: "src/Chronos/Services/DoctrineFraicheur.cs"
      to: "Source = null"
      via: "branche Indisponible, à côté de Provenance = null"
      pattern: "Source = null"
---

<objective>
La moitié manquante d'EXA-06 : le **nom** de la source qui alimente réellement l'affichage. `ProvenanceReleve`
répond « qu'a-t-on vérifié » ; aucun champ ne répond « qui l'a produit ». On pose ce champ là où il survit
(sur `WindowState`, rendu **par référence** par `Best()`), on lui donne son vocabulaire français en un point
unique, et on profite du passage sur `WindowState` pour tuer le champ mort `EstimatedTokens`.

Purpose : sans le nom de la source, le diagnostic du plan 05 ne peut pas faire son travail — constater
seul une panne de source. Et un champ mort surveillé par un test de comportement est une garantie plus
faible qu'un champ absent.

Output : `SourceUsage`, `WindowState.Source`, 5 producteurs qui la posent, `LibelleSource`, deux gardes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-RESEARCH.md
@.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-01-SUMMARY.md

<interfaces>
<!-- Contrats existants. Ne PAS explorer le dépôt pour les retrouver. -->

```csharp
// src/Chronos/Models/WindowState.cs — DEUX membres « required » SEULEMENT (Kind, Reliability).
// Tous les sites de construction sont des initialiseurs d'objet : 6 en src/, 42 en tests/.
// Une propriété « init » nullable de plus casse 0 site sur 48. Mesuré.
public sealed record WindowState
{
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public long? EstimatedTokens { get; init; }          // ← SUPPRIMÉ par la tâche 3
    public required SourceReliability Reliability { get; init; }
    public DateTimeOffset? CapturedAt { get; init; }
    public StatutServeur? StatutServeur { get; init; }
    public EtatDepassement? Depassement { get; init; }
    public ProvenanceReleve? Provenance { get; init; }
    public long? TokensDepuisReleve { get; init; }
    public bool Exhausted => Utilization is >= 1.0;
    public static WindowState Unavailable(WindowKind k) => …;
}

public enum ProvenanceReleve { Frais, EncoreValide, PlancherAvecActivite }
public enum SourceReliability { Exact, Estimated, Unavailable }
```

```csharp
// src/Chronos/Services/DoctrineFraicheur.cs — Qualifier hérite la source par « candidat with { … } » :
// AUCUNE ligne à ajouter là. Seul Indisponible doit effacer la source.
private static WindowState Qualifier(WindowState candidat, WindowState vivante,
                                     SourceReliability fiabilite, ProvenanceReleve provenance, long? tokens)
    => candidat with { Reliability = fiabilite, Provenance = provenance, TokensDepuisReleve = tokens,
                       StatutServeur = candidat.StatutServeur ?? vivante.StatutServeur,
                       Depassement  = candidat.Depassement  ?? vivante.Depassement };

private static WindowState Indisponible(WindowState vivante)
    => vivante with { Reliability = SourceReliability.Unavailable, Utilization = null,
                      EstimatedTokens = null, TokensDepuisReleve = null, Provenance = null };
```

Chaîne DI RÉELLE (`src/Chronos/App.xaml.cs`, l. 347-357), de l'externe vers l'interne — c'est elle qui
détermine les 5 membres de l'enum :
```
LastExactUsageProvider(couche de doctrine)
  └─ Composite(primary: RateLimitHeaderUsageProvider,
       fallback: Composite(primary: ChronosOAuthUsageProvider,
         fallback: Composite(primary: GatedOAuthUsageProvider → ClaudeOAuthUsageProvider,
                             fallback: ClaudeUsageObjectProvider)))
  store: LastExactStore
```

Motif de garde à réutiliser tel quel (`tests/Chronos.Tests/GardesDoctrineTests.cs`) — le chemin des
sources est INJECTÉ par MSBuild, jamais deviné (`Assembly.Location` est vide en mono-fichier) :
```csharp
private static string CheminSources()
    => typeof(GardesDoctrineTests).Assembly
           .GetCustomAttributes<AssemblyMetadataAttribute>()
           .FirstOrDefault(a => a.Key == "CheminSourcesChronos")?.Value ?? "";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : SourceUsage, posée là où elle survit — les 5 producteurs et la doctrine</name>
  <files>src/Chronos/Models/SourceUsage.cs, src/Chronos/Models/WindowState.cs, src/Chronos/Services/DoctrineFraicheur.cs, src/Chronos/Services/RateLimitHeaderUsageProvider.cs, src/Chronos/Services/ChronosOAuthUsageProvider.cs, src/Chronos/Services/ClaudeOAuthUsageProvider.cs, src/Chronos/Services/ClaudeUsageObjectProvider.cs, src/Chronos/Services/LastExactStore.cs, tests/Chronos.Tests/GardesDoctrineTests.cs</files>

  <read_first>
    - src/Chronos/Models/WindowState.cs (INTÉGRALEMENT — le commentaire de `StatutServeur` explique
      POURQUOI le champ vit sur `WindowState` et pas sur `UsageSnapshot` : c'est le modèle à recopier)
    - src/Chronos/Models/ProvenanceReleve.cs
    - src/Chronos/Services/DoctrineFraicheur.cs (INTÉGRALEMENT — `Qualifier` et `Indisponible`)
    - src/Chronos/Services/RateLimitHeaderUsageProvider.cs (l. 410-435)
    - src/Chronos/Services/ChronosOAuthUsageProvider.cs (l. 175-195)
    - src/Chronos/Services/ClaudeOAuthUsageProvider.cs (l. 133-152)
    - src/Chronos/Services/ClaudeUsageObjectProvider.cs (l. 85-105)
    - src/Chronos/Services/LastExactStore.cs (l. 155-180 — `Reconstruire`)
    - src/Chronos/Services/CompositeUsageProvider.cs (pour constater que `Best()` rend l'INSTANCE)
    - tests/Chronos.Tests/GardesDoctrineTests.cs
  </read_first>

  <behavior>
    - `WindowState.Source` vaut `SourceUsage.SondeEnTetes` quand la sonde d'en-têtes a produit la fenêtre.
    - Une fenêtre produite par `LastExactStore.Reconstruire` porte `SourceUsage.MagasinDernierExact`.
    - Après la doctrine, un plancher CONSERVE la source du relevé mémorisé (héritage par `candidat with`).
    - Une fenêtre `Indisponible` porte `Source = null` — personne ne l'alimente.
    - La source SURVIT aux trois composites imbriqués : une source interne qui gagne parce que les deux
      externes sont indisponibles fait arriver son nom jusqu'au snapshot final.
  </behavior>

  <action>
Créer `src/Chronos/Models/SourceUsage.cs` — **5 membres, calqués sur la chaîne DI réelle**, et
**aucun membre « Inconnue »** : `null` porte déjà « non renseigné », et le projet proscrit les
affirmations nées d'une absence.

```csharp
namespace Chronos.Models;

/// <summary>
/// EXA-06 — QUI a produit un chiffre d'usage. Les cinq membres sont calqués sur la chaîne DI réelle
/// (App.xaml.cs), de l'externe vers l'interne.
///
/// Orthogonal à <see cref="ProvenanceReleve"/>, qui dit ce qu'on a VÉRIFIÉ : un plancher a une SOURCE
/// (celle du relevé mémorisé) ET un ÉTAT (« borne inférieure »). Les fusionner produirait un enum
/// produit cartésien de 15 membres — et rendrait le diagnostic incapable de dire lequel des deux
/// endpoints OAuth répond, c'est-à-dire de faire son travail.
///
/// Pas de membre « Inconnue » : l'absence de source se dit par « null ».
/// </summary>
public enum SourceUsage
{
    SondeEnTetes,            // RateLimitHeaderUsageProvider — en-têtes anthropic-ratelimit-unified-*
    EndpointOAuthChronos,    // ChronosOAuthUsageProvider   — /api/oauth/usage, jeton du login Chronos
    EndpointOAuthClaude,     // ClaudeOAuthUsageProvider    — /api/oauth/usage, coffre app bureau / CLI
    PontStatusLine,          // ClaudeUsageObjectProvider   — usage.json écrit par le pont statusLine
    MagasinDernierExact,     // LastExactStore.Reconstruire — dernier relevé exact persisté sur disque
}
```

Dans `WindowState`, ajouter **après** `Provenance` :

```csharp
/// <summary>Phase 20 (EXA-06) — QUI a produit ce chiffre. Porté par WindowState et NON par
/// UsageSnapshot, pour la raison mécanique déjà établie deux fois (StatutServeur, Provenance) :
/// Best() rend l'INSTANCE gagnante PAR RÉFÉRENCE, donc ce champ traverse gratuitement les trois
/// composites imbriqués, là où un champ de snapshot serait détruit par le « new » de la recomposition.
/// La garde Toute_propriete_de_UsageSnapshot_… ne surveille que UsageSnapshot : elle n'est pas
/// concernée.
///
/// null = non renseigné. Pas de membre « Inconnue » dans l'enum : une absence ne doit jamais produire
/// d'affirmation.</summary>
public SourceUsage? Source { get; init; }
```

Poser la source dans les **5** producteurs, une ligne chacun, dans l'initialiseur d'objet, avec un
commentaire d'une ligne rappelant que le champ voyage par référence à travers `Best()` :

| Fichier | Site | Ligne à ajouter |
|---|---|---|
| `RateLimitHeaderUsageProvider.cs` | l. 422, le `new WindowState` **Exact** | `Source = SourceUsage.SondeEnTetes,` |
| `ChronosOAuthUsageProvider.cs` | l. 181 | `Source = SourceUsage.EndpointOAuthChronos,` |
| `ClaudeOAuthUsageProvider.cs` | l. 139 | `Source = SourceUsage.EndpointOAuthClaude,` |
| `ClaudeUsageObjectProvider.cs` | l. 92 | `Source = SourceUsage.PontStatusLine,` |
| `LastExactStore.cs` | l. 165, `Reconstruire` | `Source = SourceUsage.MagasinDernierExact,` |

⚠️ **NE PAS** poser de source sur le `new WindowState { … Reliability = Unavailable, Depassement = … }`
de `RateLimitHeaderUsageProvider` (l. 419, forme « dépassement seul ») : une fenêtre indisponible n'est
alimentée par personne. Il y a donc **6** `new WindowState` en `src/` mais **5** lignes `Source =`.

Dans `DoctrineFraicheur.Indisponible`, ajouter `Source = null,` juste après `Provenance = null,` avec un
commentaire : cohérence avec la provenance — une fenêtre indisponible ne nomme personne.
**Ne rien ajouter dans `Qualifier`** : `candidat with { … }` hérite la source du candidat gagnant, y
compris quand le magasin prend la main. C'est exactement le comportement voulu, et la tâche le prouve.

Tests (écrits AVANT l'implémentation, ils doivent d'abord échouer) — dans `GardesDoctrineTests` :

1. `La_source_survit_aux_TROIS_composites_imbriques` : monter
   `new CompositeUsageProvider(externe, new CompositeUsageProvider(median, interne))` où `externe` et
   `median` rendent deux fenêtres `Unavailable` et `interne` un snapshot `Exact` portant
   `Source = SourceUsage.PontStatusLine` ; asserter que le snapshot final porte encore
   `SourceUsage.PontStatusLine` sur les deux fenêtres. C'est la preuve **par référence**, jumelle de
   `Le_repli_le_plus_interne_est_bien_celui_a_anciennete_non_bornee` déjà présent dans ce fichier.
2. `Une_fenetre_indisponible_ne_nomme_AUCUNE_source` : appeler la doctrine (ou construire directement le
   cas qui tombe en branche 4) et asserter `Source is null` **et** `Provenance is null` sur la fenêtre
   rendue — les deux ensemble, parce que c'est leur cohérence qu'on garde.
3. `Un_plancher_conserve_la_source_du_releve_memorise` : une fenêtre mémorisée portant
   `SourceUsage.MagasinDernierExact`, hors limite d'âge, avec activité depuis ⇒ la fenêtre rendue est
   `SourceReliability.Estimated`, `ProvenanceReleve.PlancherAvecActivite`, et **conserve**
   `SourceUsage.MagasinDernierExact`.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~GardesDoctrineTests|FullyQualifiedName~DoctrineFraicheurTests|FullyQualifiedName~CompositeUsageProviderTests|FullyQualifiedName~LastExactStoreTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rc "Source = SourceUsage\." --include=*.cs src/Chronos/Services` rend un total de **5**
      (une ligne par producteur, aucune sur la forme « dépassement seul »)
    - `grep -c "SourceUsage? Source" src/Chronos/Models/WindowState.cs` rend **1**
    - `grep -c "Source = null" src/Chronos/Services/DoctrineFraicheur.cs` rend **1**
    - `grep -c "Source" src/Chronos/Services/CompositeUsageProvider.cs` : le fichier n'est **PAS**
      modifié par cette tâche — `git diff --name-only` ne doit pas le contenir
    - `grep -c "Inconnue" src/Chronos/Models/SourceUsage.cs` rend **0**
    - Les 3 nouveaux tests de `GardesDoctrineTests` passent ; les 3 préexistants restent verts
    - `dotnet build Chronos.sln -c Debug` réussit (les 48 sites `new WindowState` compilent sans retouche)
  </acceptance_criteria>

  <done>
Le snapshot qui arrive au ViewModel sait nommer sa source, cette information traverse les trois
composites, et une fenêtre indisponible ne ment pas en nommant un producteur qu'elle n'a pas.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : le vocabulaire français du relevé, calculé UNE fois (Chronos.Text)</name>
  <files>src/Chronos/Text/LibelleSource.cs, tests/Chronos.Tests/LibelleSourceTests.cs</files>

  <read_first>
    - src/Chronos/Text/PercentFormatter.cs (le motif exact du dossier : pur, aucun WPF, aucun I/O,
      aucune CultureInfo, deux surcharges documentées)
    - src/Chronos/Text/CountdownFormatter.cs (formate une durée RESTANTE — sujet distinct de l'ancienneté)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (l. 95-110 : `LibelleStatut` et son commentaire
      « vocabulaire FR UNIQUE … deux mappings divergeraient » — c'est la règle du projet)
    - src/Chronos/Models/SourceUsage.cs (créé en tâche 1)
  </read_first>

  <behavior>
    - `Format(SourceUsage.SondeEnTetes)` rend « sonde d'en-têtes de rate-limit ».
    - `Format(null)` rend « non renseignée » — jamais un nom de source par défaut.
    - `Anciennete(null, now)` rend « de date inconnue ».
    - `Anciennete(now - 30 s, now)` rend « à l'instant » ; `now - 12 min` rend « il y a 12 min » ;
      `now - 3 h 12` rend « il y a 3 h 12 » ; `now - 2 j` rend « il y a 2 j ».
    - Un horodatage POSTÉRIEUR à `now` (horloge qui recule) rend « à l'instant », jamais une durée négative.
    - `Provenance(ProvenanceReleve.EncoreValide)` rend « encore valide (aucune activité depuis) ».
    - `Provenance(null)` rend la chaîne VIDE.
  </behavior>

  <action>
Créer `src/Chronos/Text/LibelleSource.cs` — classe statique **pure** (aucun `using System.Windows*`,
aucun I/O, aucune `CultureInfo`), sur le modèle de `PercentFormatter` :

```csharp
namespace Chronos.Text;

/// <summary>
/// EXA-06 — vocabulaire FR UNIQUE du relevé : QUI l'a produit, DEPUIS QUAND, et ce qu'on a vérifié.
/// Un seul point, consommé par DiagnosticService ET par l'infobulle du ViewModel. Règle déjà posée par
/// WindowGaugeViewModel.LibelleStatut : deux mappings divergeraient le jour où le vocabulaire bouge.
///
/// Pur : aucune dépendance WPF, aucun I/O, aucune CultureInfo (motif du dossier Text/).
/// </summary>
public static class LibelleSource
{
    public static string Format(Chronos.Models.SourceUsage? source) => source switch
    {
        Chronos.Models.SourceUsage.SondeEnTetes         => "sonde d'en-têtes de rate-limit",
        Chronos.Models.SourceUsage.EndpointOAuthChronos => "endpoint OAuth (login Chronos)",
        Chronos.Models.SourceUsage.EndpointOAuthClaude  => "endpoint OAuth (jeton app bureau / CLI)",
        Chronos.Models.SourceUsage.PontStatusLine       => "pont statusLine Claude Code",
        Chronos.Models.SourceUsage.MagasinDernierExact  => "dernier exact persisté",
        _                                               => "non renseignée",
    };

    /// <summary>Ancienneté d'un relevé, destinée à suivre le mot « relevé » : « relevé il y a 12 min ».
    /// Un instant postérieur à « now » (horloge qui recule, source qui horodate en avance) rend
    /// « à l'instant » et JAMAIS une durée négative : un chiffre du futur n'est pas une information.</summary>
    public static string Anciennete(System.DateTimeOffset? capturedAt, System.DateTimeOffset now) { … }

    public static string Provenance(Chronos.Models.ProvenanceReleve? p) => p switch
    {
        Chronos.Models.ProvenanceReleve.Frais                => "frais",
        Chronos.Models.ProvenanceReleve.EncoreValide         => "encore valide (aucune activité depuis)",
        Chronos.Models.ProvenanceReleve.PlancherAvecActivite => "borne inférieure (activité depuis)",
        _                                                    => "",
    };
}
```

Paliers exacts d'`Anciennete` (à respecter, ils sont testés) :
- `capturedAt is null` → `"de date inconnue"`
- `age < 0` → `"à l'instant"`
- `age < 60 s` → `"à l'instant"`
- `age < 60 min` → `"il y a {N} min"` (N entier tronqué)
- `age < 24 h` → `"il y a {H} h {MM}"` (minutes sur 2 chiffres, ex. « il y a 3 h 12 »)
- sinon → `"il y a {J} j"`

**Ne pas** réutiliser `CountdownFormatter` (durée RESTANTE, vocabulaire différent) ni toucher au
formateur privé de `SessionsViewModel` (widget de sessions, sujet distinct — la fusion des deux
vocabulaires est explicitement hors périmètre de cette phase).

Créer `tests/Chronos.Tests/LibelleSourceTests.cs` (classe `[Fact]` pure, **aucun BAML** — ne PAS la
rattacher à la collection « XAML WPF ») couvrant : les **5** membres de `SourceUsage` nommés un par un,
`null` → « non renseignée », et les **6** paliers d'`Anciennete` dont le cas de l'horloge qui recule,
plus les 4 cas de `Provenance`.

Un test supplémentaire, exhaustif par réflexion, qui tombera si un membre est ajouté à l'enum sans
libellé : parcourir `Enum.GetValues<SourceUsage>()` et asserter que `Format(v)` n'est jamais vide **ni**
égal à « non renseignée ».
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~LibelleSourceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "using System.Windows" src/Chronos/Text/LibelleSource.cs` rend **0**
    - `grep -c "non renseignée" src/Chronos/Text/LibelleSource.cs` rend **1** (un seul repli, pas un par cas)
    - `grep -c "de date inconnue" src/Chronos/Text/LibelleSource.cs` rend **1**
    - Le test exhaustif par réflexion existe :
      `grep -c "Enum.GetValues" tests/Chronos.Tests/LibelleSourceTests.cs` rend **au moins 1**
    - `grep -c "Collection(" tests/Chronos.Tests/LibelleSourceTests.cs` rend **0** (classe pure, pas de BAML :
      la rattacher à la collection sérialisée ralentirait la suite pour rien)
    - Tous les tests de `LibelleSourceTests` passent
  </acceptance_criteria>

  <done>
Le projet a un seul endroit qui sait dire « sonde d'en-têtes de rate-limit » et « il y a 3 h 12 ». Le
diagnostic (plan 05) et l'infobulle du cadran (plan 03) y puiseront tous deux.
  </done>
</task>

<task type="auto">
  <name>Task 3 : mort d'EstimatedTokens — un champ absent vaut mieux qu'un champ mort surveillé</name>
  <files>src/Chronos/Models/WindowState.cs, src/Chronos/Services/DoctrineFraicheur.cs, src/Chronos/ViewModels/WindowGaugeViewModel.cs, tests/Chronos.Tests/GardesDoctrineTests.cs, tests/Chronos.Tests/CadranBindingTests.cs</files>

  <read_first>
    - src/Chronos/Models/WindowState.cs (les DEUX occurrences d'`EstimatedTokens` : la déclaration et
      le commentaire de `TokensDepuisReleve` qui s'y réfère)
    - src/Chronos/Services/DoctrineFraicheur.cs (`Indisponible` : `EstimatedTokens = null,`)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (le commentaire de `Apply` qui s'y réfère)
    - tests/Chronos.Tests/CadranBindingTests.cs (l. 186-203 : le test
      `EstimatedTokens_seul_ne_surface_PLUS_rien_au_cadran`, qui est une GARDE DE NON-RETOUR)
    - tests/Chronos.Tests/GardesDoctrineTests.cs (le motif `CheminSources()` et le format des messages
      d'échec des gardes de source)
    - tests/Chronos.Tests/WindowStateTests.cs (vérifier qu'aucune assertion ne porte sur le champ)
  </read_first>

  <action>
`EstimatedTokens` portait la somme de l'estimation ABSOLUE supprimée en phase 16. Il est mort en
production. Deux options honnêtes existaient : garder le champ avec sa garde de comportement, ou
supprimer le champ et remplacer la garde par une garde **structurelle**. On retient la seconde : un champ
absent est une garantie plus forte qu'un champ mort surveillé.

1. Supprimer la propriété `public long? EstimatedTokens { get; init; }` de `WindowState`.
2. Supprimer la ligne `EstimatedTokens = null,` de `DoctrineFraicheur.Indisponible`.
3. Réécrire les **commentaires** qui s'y réfèrent — celui de `WindowState.TokensDepuisReleve` (« Champ
   DISTINCT d'EstimatedTokens… ») et celui de `WindowGaugeViewModel.Apply` (« et non EstimatedTokens,
   qui portait… ») — en nommant le concept, pas le champ : « la somme de l'estimation absolue supprimée
   en phase 16 ». C'est ce qui rend le critère « 0 occurrence dans `src/` » atteignable.
4. Dans `CadranBindingTests`, **supprimer** le test `EstimatedTokens_seul_ne_surface_PLUS_rien_au_cadran`
   (il ne compile plus) — mais **jamais sans remplaçant**.
5. Ajouter le remplaçant dans `GardesDoctrineTests` :
   `Aucun_champ_nomme_EstimatedTokens_ne_reapparait_dans_Models_ni_Services` — balayage du TEXTE source
   par `CheminSources()`, sur `Models/` et `Services/` (dossiers plats, non récursif, motif recopié de
   `Aucun_rapport_entre_un_comptage_de_tokens_et_un_plafond_…`), avec :
   - la même garde de non-muettitude (`Assert.True(fichiers.Count >= 40, …)`) ;
   - un message d'échec qui dit POURQUOI : ce champ portait l'estimation absolue supprimée en phase 16 ;
     le chiffre de DEL-04 est `TokensDepuisReleve`, les tokens observés DEPUIS le relevé, matière d'une
     borne inférieure et jamais d'un pourcentage.
6. Vérifier qu'aucun autre site ne référence le champ : `tests/` inclus. Toute assertion de test qui
   l'utilisait est réécrite **dans cette tâche** (compilabilité à chaque commit — `tests/Chronos.Tests`
   a un `ProjectReference` vers `Chronos`, une seule erreur fait échouer toute l'invocation `dotnet test`).

**Consigne de rédaction** (elle rend le critère ci-dessous non contradictoire) : le nom `EstimatedTokens`
ne doit plus apparaître dans `src/Chronos/**` — ni en code, ni en commentaire. Il apparaît en revanche
légitimement dans `tests/Chronos.Tests/GardesDoctrineTests.cs`, en littéral de chaîne : c'est la garde.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~GardesDoctrineTests|FullyQualifiedName~CadranBindingTests|FullyQualifiedName~WindowStateTests|FullyQualifiedName~DoctrineFraicheurTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn --include=*.cs --include=*.xaml "EstimatedTokens" src/Chronos` rend **0 ligne**
      (le `--include` évite les binaires de `bin/` et `obj/`)
    - `grep -c "EstimatedTokens" tests/Chronos.Tests/GardesDoctrineTests.cs` rend **au moins 1**
      (la garde structurelle existe, en littéral de chaîne)
    - `grep -c "EstimatedTokens" tests/Chronos.Tests/CadranBindingTests.cs` rend **0**
    - Le test `Aucun_champ_nomme_EstimatedTokens_ne_reapparait_dans_Models_ni_Services` passe, et il
      ÉCHOUE si on réintroduit le champ (le vérifier par mutation réelle : ajouter la propriété,
      constater le rouge, révoquer, constater que `git diff` sur `src/` est vide)
    - `dotnet build Chronos.sln -c Debug` réussit
  </acceptance_criteria>

  <done>
Le champ n'existe plus, sa garde est devenue structurelle, et la preuve n'a pas été perdue en route :
elle a changé de niveau.
  </done>
</task>

</tasks>

<verification>
1. Suite complète verte : `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q`.
   Bilan de comptage attendu : **−1** (le test comportemental d'`EstimatedTokens`) **+ ≈ 20**
   (3 gardes de source/composite, ≈ 16 de `LibelleSourceTests`, 1 garde structurelle). Critère :
   **0 échec + chaque écart justifié nominativement**, jamais « ≥ N ».
2. Les cinq gardes permanentes restent vertes.
3. `stat -c '%s %Y' "$APPDATA/Chronos/oauth.dat"` rend `518 1783863147`. Aucune requête réseau, aucun
   test n'écrit dans le vrai `%APPDATA%\Chronos\`, l'overlay n'est pas lancé.
</verification>

<success_criteria>
- EXA-06 a sa matière : le nom de la source existe, il est posé par les 5 producteurs, il survit à la
  chaîne, il s'efface quand personne n'alimente la fenêtre.
- Le vocabulaire FR existe en **un** point, prêt pour les deux consommateurs (diagnostic + infobulle).
- `UsageSnapshot` n'a **pas** été touché, `CompositeUsageProvider.cs` non plus : la garde nominative de la
  recomposition n'avait pas à bouger, et elle n'a pas bougé.
- Zéro site de construction `new WindowState` retouché (48 sites, 0 cassé) — la mesure de la recherche
  est confirmée ou infirmée explicitement dans le SUMMARY.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/20-honn-tet-visible-cadran-diagnostic/20-02-SUMMARY.md`
</output>
