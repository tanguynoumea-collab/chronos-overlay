---
phase: 18-source-exacte-par-en-t-tes-de-rate-limit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Models/StatutServeur.cs
  - src/Chronos/Models/EtatDepassement.cs
  - src/Chronos/Models/WindowState.cs
  - src/Chronos/Services/ResultatSonde.cs
  - src/Chronos/Services/IEtatServeur.cs
  - tests/Chronos.Tests/StatutServeurTests.cs
  - tests/Chronos.Tests/WindowStateTests.cs
  - tests/Chronos.Tests/CompositeUsageProviderTests.cs
  - tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs
  - tests/Chronos.Tests/EnTetesDeReference.cs
autonomous: true
requirements: [HDR-03, HDR-04]

must_haves:
  truths:
    - "Un statut serveur inconnu est nommé « non reconnu » et JAMAIS rangé d'autorité dans « autorisé » : le vocabulaire allowed/allowed_warning/rejected est communautaire, pas officiel."
    - "Un en-tête de statut absent rend l'inconnu (null), ce qui est différent d'un statut présent mais non reconnu — deux faits distincts, deux valeurs distinctes."
    - "Le statut serveur et le dépassement voyagent sur l'instance de WindowState, la seule chose que CompositeUsageProvider transmet PAR RÉFÉRENCE — un champ posé sur UsageSnapshot serait silencieusement détruit, et ce fichier est interdit d'édition."
    - "Les 21 sites de construction `new WindowState { … }` des tests compilent sans une retouche : les nouveaux champs sont init, nullable, et sans required."
    - "Une réponse HTTP de test peut porter des en-têtes sur N'IMPORTE QUEL code de statut, 429 compris — sans quoi HDR-02 n'est pas testable du tout."
    - "Les jeux d'en-têtes de référence couvrent les sept formes réelles attendues : nominal, avertissement, refus, statut inconnu, dépassement seul, en-têtes absents, en-têtes illisibles."
  artifacts:
    - path: "src/Chronos/Models/StatutServeur.cs"
      provides: "Vocabulaire OUVERT du statut serveur (4 membres dont NonReconnu) + lecture tolérante depuis une valeur d'en-tête"
      exports: ["StatutServeur", "StatutServeurTexte"]
      min_lines: 40
    - path: "src/Chronos/Models/EtatDepassement.cs"
      provides: "Fait de compte : utilization de dépassement, reset, statut — tous optionnels, jamais inventés"
      exports: ["EtatDepassement"]
      min_lines: 25
    - path: "src/Chronos/Services/IEtatServeur.cs"
      provides: "Canal LATÉRAL neutre du dépassement et de l'observabilité de la sonde (motif IAuthStatus)"
      exports: ["IEtatServeur"]
      min_lines: 35
    - path: "src/Chronos/Services/ResultatSonde.cs"
      provides: "Vocabulaire d'issue de la sonde, SANS aucune chaîne (un corps d'erreur ne peut donc pas fuir)"
      exports: ["ResultatSonde"]
      min_lines: 25
    - path: "tests/Chronos.Tests/EnTetesDeReference.cs"
      provides: "Les sept jeux d'en-têtes de référence de la phase, en un point unique réutilisable par 18-03 et 18-04"
      min_lines: 70
  key_links:
    - from: "src/Chronos/Models/WindowState.cs"
      to: "StatutServeur? / EtatDepassement?"
      via: "deux champs init NULLABLE sans required — l'instance de WindowState est ce que Best() rend par référence"
      pattern: "public (StatutServeur|EtatDepassement)\\? \\w+ \\{ get; init; \\}"
    - from: "tests/Chronos.Tests/CompositeUsageProviderTests.cs"
      to: "CompositeUsageProvider.Best"
      via: "Assert.Same sur l'instance gagnante + Assert.Equal sur le statut qu'elle emporte"
      pattern: "StatutServeur"
    - from: "tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs"
      to: "HttpResponseMessage.Headers.TryAddWithoutValidation"
      via: "fabrique AvecEnTetes : des en-têtes sur n'importe quel code de statut"
      pattern: "TryAddWithoutValidation"
---

<objective>
Poser les **contrats** du statut serveur et du dépassement, et la **Wave 0 de transport** sans laquelle
HDR-02 n'est pas testable.

Purpose: trois contraintes mécaniques du code existant dictent entièrement la conception, et elles ont
été établies par lecture ligne à ligne — ce ne sont pas des préférences.

1. **`CompositeUsageProvider.GetAsync` (lignes 39-45) reconstruit le snapshot** par
   `new UsageSnapshot { FiveHour, SevenDay, SourceCapturedAt }` — **pas** par `p with { … }`. Tout
   nouveau champ posé sur `UsageSnapshot` est donc **silencieusement détruit** au premier composite
   traversé (il y en a deux aujourd'hui, trois après le plan 18-05), et `CompositeUsageProvider.cs` est
   **interdit d'édition** (sa refonte est la phase 19). En revanche `Best()` (lignes 54-55) retourne
   l'**instance** de `WindowState`, donc par référence : un champ optionnel posé là voyage gratuitement.
   Le statut serveur (per-fenêtre) va donc sur `WindowState`.
2. **Quand la forme « dépassement » est seule**, les deux fenêtres sont légitimement `Unavailable` et
   `Best()` peut retenir l'instance d'une AUTRE source `Exact` — le `Depassement` porté par la fenêtre
   écartée disparaîtrait. Le champ sur `WindowState` ne suffit donc pas : il faut **doubler par un canal
   latéral**, exactement sur le motif que le projet a déjà tranché pour l'état d'authentification
   (`IAuthStatus`, dont la XML-doc porte mot pour mot la justification : « il n'existe aucune règle
   sensée pour fusionner deux états issus de branches différentes »).
3. **Les en-têtes `anthropic-ratelimit-unified-*` ne sont documentés nulle part** chez Anthropic, et
   deux écarts entre `18-CONTEXT.md` et le code d'origine ont déjà été relevés (le statut global est
   `anthropic-ratelimit-unified-status`, **sans** segment de fenêtre — pas `-overage-status` ; et la
   forme overage est **alternative**, liée au type de compte, pas additive). Chaque nom est donc une
   **hypothèse** : vocabulaire de statut **ouvert** avec un quatrième membre `NonReconnu`, et jeux
   d'en-têtes de référence centralisés pour que les plans 18-03/18-04 ne les redéclarent pas.

Output: 4 fichiers de contrat (2 modèles, 2 services), l'extension de `FakeHttpMessageHandler` sans
laquelle on ne peut pas fabriquer un 429 porteur d'en-têtes, les sept jeux d'en-têtes de référence, et
deux gardes étendues.

**Aucune sonde dans ce plan, aucun câblage DI, aucune touche à `CompositeUsageProvider.cs`.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-RESEARCH.md
@.planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contrats EXISTANTS. À consommer et à ÉTENDRE, jamais à réécrire. -->

```csharp
// src/Chronos/Models/WindowState.cs — ÉTAT ACTUEL (à étendre de 2 champs, rien d'autre)
public sealed record WindowState
{
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public long? EstimatedTokens { get; init; }
    public required SourceReliability Reliability { get; init; }
    public DateTimeOffset? CapturedAt { get; init; }
    public bool Exhausted => Utilization is >= 1.0;
    public static WindowState Unavailable(WindowKind k) =>
        new() { Kind = k, Reliability = SourceReliability.Unavailable };
    public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength);
}

// src/Chronos/Models/UsageSnapshot.cs — INTOUCHÉ (tout ajout serait détruit par le composite)
// src/Chronos/Services/CompositeUsageProvider.cs — INTERDIT D'ÉDITION (phase 19)

// src/Chronos/Services/IAuthStatus.cs (phase 17) — LE MOTIF à copier pour le canal latéral
public interface IAuthStatus
{
    EtatAuthentification Etat { get; }
    event EventHandler<EtatAuthentification>? EtatChange;   // thread du POOL, sur TRANSITION
    void ReinitialiserApresLogin();
}

// tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs — ÉTAT ACTUEL (additif uniquement)
internal sealed class FakeHttpMessageHandler : HttpMessageHandler
{
    public int SendCount { get; }
    public HttpRequestMessage? LastRequest { get; }
    public FakeHttpMessageHandler(Func<HttpRequestMessage, HttpResponseMessage> responder);
    public static FakeHttpMessageHandler Json(HttpStatusCode status, string body);
    public static FakeHttpMessageHandler Throws(Exception ex);
}

// tests/Chronos.Tests/CompositeUsageProviderTests.cs — harnais existant à RÉUTILISER
private sealed class FakeProvider : IUsageProvider { public FakeProvider(UsageSnapshot snap); }
private static WindowState Win(WindowKind k, SourceReliability r) => new() { Kind = k, Reliability = r };
private static UsageSnapshot Snap(WindowState five, WindowState seven) => new() { FiveHour = five, SevenDay = seven };
```

**Noms d'en-tête retenus** (constantes de la phase ; chaque nom est une HYPOTHÈSE, pas un fait) :

| Constante | Nom exact | Unité / valeurs |
|---|---|---|
| 5 h utilization | `anthropic-ratelimit-unified-5h-utilization` | fraction 0..1, texte à point décimal |
| 5 h reset | `anthropic-ratelimit-unified-5h-reset` | epoch **secondes**, texte |
| 5 h statut | `anthropic-ratelimit-unified-5h-status` | `allowed` / `allowed_warning` / `rejected` |
| 7 j utilization | `anthropic-ratelimit-unified-7d-utilization` | fraction 0..1 |
| 7 j reset | `anthropic-ratelimit-unified-7d-reset` | epoch secondes |
| 7 j statut | `anthropic-ratelimit-unified-7d-status` | NON confirmé — lecture optionnelle |
| dépassement utilization | `anthropic-ratelimit-unified-overage-utilization` | fraction 0..1 |
| dépassement reset | `anthropic-ratelimit-unified-overage-reset` | epoch secondes |
| statut GLOBAL | `anthropic-ratelimit-unified-status` | **sans** segment de fenêtre (code d'origine) |
| statut dépassement | `anthropic-ratelimit-unified-overage-status` | annoncé par CONTEXT.md, **non confirmé** — lire quand même |
| réclamation | `anthropic-ratelimit-unified-representative-claim` | ex. `five_hour` — optionnel |

**Valeurs réelles observées** (dump indépendant, `steipete/CodexBar` issue #1894) :
`-5h-utilization: 0.01`, `-5h-reset: 1783180800`, `-7d-utilization: 0.63`, `-7d-reset: 1783713600`,
`-5h-status: allowed`, `-representative-claim: five_hour`.

**Fait .NET vérifié** : les en-têtes non standard atterrissent dans `resp.Headers` (**pas** dans
`resp.Content.Headers`), la recherche est **insensible à la casse**, et `SendAsync` **ne lève pas** sur
un 429 — la réponse arrive intacte, en-têtes compris.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Le vocabulaire — StatutServeur ouvert, EtatDepassement, ResultatSonde, et 2 champs sur WindowState</name>

  <read_first>
    - src/Chronos/Models/WindowState.cs (intégralement)
    - src/Chronos/Models/UsageSnapshot.cs (intégralement — comprendre pourquoi RIEN n'y sera ajouté)
    - src/Chronos/Services/CompositeUsageProvider.cs (lignes 22-62, LECTURE SEULE — la démonstration mécanique)
    - src/Chronos/Services/EtatAuthentification.cs (motif d'enum d'état de la phase 17)
    - tests/Chronos.Tests/WindowStateTests.cs (lignes 35-62)
    - .planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-RESEARCH.md (§ Pattern 3, § Pitfall 4, § Pitfall 5, § Q8)
  </read_first>

  <files>
src/Chronos/Models/StatutServeur.cs
src/Chronos/Models/EtatDepassement.cs
src/Chronos/Services/ResultatSonde.cs
src/Chronos/Models/WindowState.cs
tests/Chronos.Tests/StatutServeurTests.cs
tests/Chronos.Tests/WindowStateTests.cs
  </files>

  <behavior>
    - `StatutServeurTexte.DepuisEnTete("allowed")` → `StatutServeur.Autorise`.
    - `("allowed_warning")` → `AutoriseAvertissement` ; `("rejected")` → `Rejete`.
    - `("ALLOWED")`, `("  allowed  ")`, `("Allowed_Warning")` → mêmes valeurs (Trim + OrdinalIgnoreCase).
    - `("active")`, `("blocked")`, `("n'importe quoi")` → `StatutServeur.NonReconnu` — **jamais**
      `Autorise`. C'est le test qui refuse de deviner.
    - `(null)`, `("")`, `("   ")` → `null` : « rien rapporté » n'est PAS « rapporté mais inconnu ».
    - `new EtatDepassement()` → `EstRenseigne == false`.
    - `new EtatDepassement { Utilization = 0.12 }` → `EstRenseigne == true` ; idem avec `ResetsAt`
      seul, idem avec `Statut` seul.
    - Égalité structurelle de record : deux `EtatDepassement` aux mêmes champs sont égaux.
    - `WindowState.Unavailable(WindowKind.FiveHour)` → `StatutServeur` **null** ET `Depassement`
      **null** (deux `Assert.Null` ajoutés au test existant
      `Unavailable_neutralise_les_champs_et_garde_la_fenetre`, qui n'est PAS réécrit).
    - Deux `WindowState` identiques sans les nouveaux champs restent **égaux** (`null == null`) : les
      21 comparaisons structurelles existantes ne changent pas de résultat.
    - Un `WindowState` porteur d'un `StatutServeur` n'est PAS égal au même sans — le champ participe
      bien à l'identité du record.
  </behavior>

  <action>
**A. `src/Chronos/Models/StatutServeur.cs`** (namespace `Chronos.Models`) :

```csharp
/// <summary>Statut déclaré par le SERVEUR pour une fenêtre de limite (HDR-03) — un fait rapporté, pas
/// une déduction locale à partir d'un pourcentage. Vocabulaire OUVERT : allowed / allowed_warning /
/// rejected sont rapportés par la communauté et par le code d'origine, mais la famille d'en-têtes
/// anthropic-ratelimit-unified-* est ABSENTE de la documentation publique Anthropic. D'où
/// <see cref="NonReconnu"/> : on dit « statut non reconnu » plutôt que de ranger d'autorité une valeur
/// inconnue dans « autorisé ». Même honnêteté que « null n'est pas 0 ».</summary>
public enum StatutServeur { Autorise, AutoriseAvertissement, Rejete, NonReconnu }
```

Dans le même fichier, `public static class StatutServeurTexte` avec
`public static StatutServeur? DepuisEnTete(string? valeur)` :
- `string.IsNullOrWhiteSpace(valeur)` → `null` (**rien rapporté**) ;
- sinon comparer `valeur.Trim()` avec `StringComparison.OrdinalIgnoreCase` à `"allowed"`,
  `"allowed_warning"`, `"rejected"` ;
- aucune correspondance → `StatutServeur.NonReconnu`.
XML-doc obligatoire distinguant explicitement les deux inconnus : `null` = absent, `NonReconnu` =
présent mais hors de l'ensemble connu.

**B. `src/Chronos/Models/EtatDepassement.cs`** (namespace `Chronos.Models`) :

```csharp
/// <summary>Usage en DÉPASSEMENT (overage, HDR-04) : un fait de COMPTE, pas une troisième fenêtre.
/// Le code d'origine lit la famille overage dans une branche ALTERNATIVE (elif) liée au type de compte
/// (« ent » contre « pro ») : cette forme de réponse n'a ni 5 h ni 7 j. UsageSnapshot n'ayant que deux
/// emplacements required et aucun champ de snapshot ne survivant au composite, une troisième WindowKind
/// n'aurait littéralement nulle part où vivre en phase 18. Tous les champs sont optionnels : rien n'est
/// jamais inventé.</summary>
public sealed record EtatDepassement
{
    public double? Utilization { get; init; }        // 0..1 (les en-têtes sont en fraction)
    public DateTimeOffset? ResetsAt { get; init; }
    public StatutServeur? Statut { get; init; }
    public bool EstRenseigne => Utilization is not null || ResetsAt is not null || Statut is not null;
}
```

**C. `src/Chronos/Services/ResultatSonde.cs`** (namespace `Chronos.Services`) — vocabulaire d'issue,
placé à côté du canal latéral comme `EtatAuthentification` l'est à côté d'`IAuthStatus` :

```csharp
/// <summary>Issue du dernier passage de la sonde d'en-têtes (observabilité HDR-01/HDR-02/HDR-06).
/// AUCUN membre porteur de chaîne, par construction : un corps d'erreur ne doit jamais pouvoir
/// remonter jusqu'au rapport de diagnostic. Précédent verrouillé en phase 17 par
/// ResultatRafraichissement, dont un test réflexif interdit toute propriété string.</summary>
public enum ResultatSonde
{
    JamaisSondee,            // aucun passage depuis le démarrage de l'exe
    Desactivee,              // interrupteur de réglage à false : zéro accès réseau
    PasDeJeton,              // coffre vide : rien à présenter, ce n'est pas une panne
    FreinActif,              // cadence bornée : refus local, aucune requête émise
    SuccesEnTetesLus,        // 2xx + famille unified reconnue
    SuccesSansEnTetes,       // 2xx SANS famille unified -> signal que les noms ont changé
    SaturationEnTetesLus,    // 429 porteur des chiffres : LA raison d'être de la phase
    SaturationSansEnTetes,   // 429 muet (ex. plafond de dépense) : ne rien inventer
    RefusServeur,            // 401 après rejeu, ou 403 : dégrader, aucun en-tête à espérer
    ModeleRefuse,            // 400/404 : panne de CONFIGURATION, pas de quota
    PanneReseau,             // réseau/timeout/5xx
}
```

**D. `src/Chronos/Models/WindowState.cs`** — ajouter **exactement deux** propriétés, juste après
`CapturedAt`, avec leur XML-doc :

```csharp
/// <summary>HDR-03 — statut déclaré par le serveur pour CETTE fenêtre. null = non rapporté (JAMAIS
/// inventé). Porté par WindowState et NON par UsageSnapshot : CompositeUsageProvider.GetAsync
/// reconstruit le snapshot par « new UsageSnapshot { … } » et non par « with », donc tout champ de
/// snapshot est détruit au passage — et ce fichier est interdit d'édition jusqu'à la phase 19. Best()
/// rend en revanche l'instance de WindowState PAR RÉFÉRENCE : ce champ voyage gratuitement.</summary>
public StatutServeur? StatutServeur { get; init; }

/// <summary>HDR-04 — usage en dépassement rapporté avec cette fenêtre. null = aucun dépassement
/// rapporté. Doublé par le canal latéral IEtatServeur : quand la forme overage est SEULE, les deux
/// fenêtres sont Unavailable et Best() peut retenir l'instance d'une autre source Exact, ce qui
/// écarterait ce champ.</summary>
public EtatDepassement? Depassement { get; init; }
```

Contraintes non négociables : `init`, **nullable**, **sans `required`** — les 21 sites
`new WindowState { … }` des tests doivent compiler sans retouche (inventaire nominatif en
`18-RESEARCH.md`, § Q8). **Ne JAMAIS référencer le TYPE `StatutServeur` ailleurs dans
`WindowState.cs`** : la propriété porte le même nom que le type (motif légal « Color Color »), mais
écrire `StatutServeur.Autorise` dans ce fichier deviendrait ambigu. `Unavailable` reste inchangé —
les nouveaux champs prennent leur défaut `null`.

**E. Tests.** Créer `tests/Chronos.Tests/StatutServeurTests.cs` (classe publique, namespace
`Chronos.Tests`) couvrant tout le `<behavior>` relatif à `StatutServeurTexte` et à
`EtatDepassement` — préférer des `[Theory]` + `[InlineData]` pour les mappings.
**ÉTENDRE** `tests/Chronos.Tests/WindowStateTests.cs` : ajouter les deux `Assert.Null` dans le test
existant `Unavailable_neutralise_les_champs_et_garde_la_fenetre` (lignes 40-49, ne pas le réécrire), et
ajouter un test `Les_champs_de_statut_participent_a_l_identite_du_record` couvrant les deux dernières
puces du `<behavior>`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~StatutServeurTests|FullyQualifiedName~WindowStateTests"</automated>
  </verify>

  <acceptance_criteria>
    - Les 3 fichiers `src/Chronos/Models/StatutServeur.cs`, `src/Chronos/Models/EtatDepassement.cs`, `src/Chronos/Services/ResultatSonde.cs` existent.
    - `grep -c "NonReconnu" src/Chronos/Models/StatutServeur.cs` >= 2 (le membre + sa justification).
    - `grep -c "public enum StatutServeur { Autorise, AutoriseAvertissement, Rejete, NonReconnu }" src/Chronos/Models/StatutServeur.cs` == 1 — les 4 membres, dans cet ordre.
    - `grep -c "OrdinalIgnoreCase" src/Chronos/Models/StatutServeur.cs` >= 1.
    - `grep -c "public .*? \w+ { get; init; }" src/Chronos/Models/WindowState.cs` : le fichier contient exactement 2 lignes de plus qu'avant correspondant à `grep -cE "public (StatutServeur|EtatDepassement)\? \w+ \{ get; init; \}" src/Chronos/Models/WindowState.cs` == 2.
    - `grep -c "required" src/Chronos/Models/WindowState.cs` == 2 — toujours les deux seuls champs requis d'origine (`Kind`, `Reliability`).
    - `git diff -- src/Chronos/Models/UsageSnapshot.cs` est VIDE.
    - `git diff -- src/Chronos/Services/CompositeUsageProvider.cs` est VIDE.
    - `grep -cE "^\s*(public|private|internal).*string" src/Chronos/Services/ResultatSonde.cs` == 0 — aucune chaîne dans le vocabulaire d'issue.
    - `git diff -- tests/Chronos.Tests/WindowStateTests.cs | grep -c "^-"` <= 1 (extension, pas réécriture : seule la ligne de contexte finale du test peut bouger).
    - `dotnet build Chronos.sln` : **0 erreur** — preuve que les 21 sites `new WindowState` compilent.
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**.
  </acceptance_criteria>

  <done>
Le vocabulaire existe, il refuse de deviner, et il est posé au SEUL endroit qui survive au composite —
sans casser un seul des 21 sites de construction existants.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IEtatServeur — le canal latéral, et la preuve que le statut survit à Best()</name>

  <read_first>
    - src/Chronos/Services/IAuthStatus.cs (intégralement — le motif à copier, y compris sa justification)
    - src/Chronos/Services/CompositeUsageProvider.cs (lignes 22-62, LECTURE SEULE)
    - src/Chronos/Services/ResultatSonde.cs et src/Chronos/Models/EtatDepassement.cs (livrés par la tâche 1)
    - tests/Chronos.Tests/CompositeUsageProviderTests.cs (intégralement — harnais FakeProvider/Win/Snap)
    - .planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-RESEARCH.md (§ Pattern 4, § Q4)
  </read_first>

  <files>
src/Chronos/Services/IEtatServeur.cs
tests/Chronos.Tests/CompositeUsageProviderTests.cs
  </files>

  <behavior>
    - **Le statut voyage avec la fenêtre gagnante** : primaire 5 h `Exact` porteur de
      `StatutServeur.AutoriseAvertissement`, repli 5 h `Exact` sans statut → `Assert.Same(pFive, snap.FiveHour)`
      ET `Assert.Equal(StatutServeur.AutoriseAvertissement, snap.FiveHour.StatutServeur)`.
    - **Le statut du perdant ne contamine pas** : primaire 5 h `Unavailable` porteur d'un statut, repli
      5 h `Exact` sans statut → `Assert.Same(fFive, snap.FiveHour)` ET
      `Assert.Null(snap.FiveHour.StatutServeur)`.
    - **Le statut du repli remonte quand le repli gagne** : primaire `Unavailable` sans statut, repli
      `Exact` porteur de `StatutServeur.Rejete` → `Assert.Equal(StatutServeur.Rejete, snap.FiveHour.StatutServeur)`.
    - **Le dépassement suit la même règle** : champ `Depassement` présent sur la fenêtre gagnante →
      présent en sortie ; présent seulement sur la fenêtre écartée → absent en sortie. **Ce dernier cas
      est la démonstration mécanique de la nécessité du canal latéral**, et le test le dit en commentaire.
    - **Le composite transmet par RÉFÉRENCE** : `Assert.Same(pFive, snap.FiveHour)` sur un cas nominal —
      c'est la raison pour laquelle un champ sur `WindowState` survit là où un champ sur `UsageSnapshot`
      serait détruit.
    - **Imbrication à deux niveaux** (la chaîne réelle après le plan 18-05) : composite externe
      (primaire porteur du statut) au-dessus d'un composite interne → le statut traverse **les deux**.
  </behavior>

  <action>
**A. Créer `src/Chronos/Services/IEtatServeur.cs`** (namespace `Chronos.Services`), en copiant la forme
ET l'esprit d'`IAuthStatus.cs` :

```csharp
/// <summary>
/// Canal LATÉRAL de ce que seule la sonde d'en-têtes sait (HDR-04, et observabilité HDR-01/02/06).
/// Type NEUTRE : aucun type WPF.
///
/// Motif IDENTIQUE à <see cref="IAuthStatus"/> : le service expose l'événement, l'abonné marshalle
/// lui-même via IUiDispatcher (frontière de thread unique, RAF-04).
///
/// POURQUOI un canal latéral et pas seulement un champ de WindowState : quand la forme « dépassement »
/// est SEULE (autre type de compte, aucune fenêtre 5 h ni 7 j), les deux fenêtres de la sonde sont
/// légitimement Unavailable — Best() retient alors l'instance d'une AUTRE source Exact et le
/// Depassement porté par la fenêtre écartée disparaît. Le dépassement est un fait de COMPTE, produit
/// par UNE seule source, sans règle sensée de fusion par fenêtre : exactement l'argument déjà écrit
/// dans IAuthStatus pour l'état d'authentification.
/// </summary>
public interface IEtatServeur
{
    /// <summary>Dernier dépassement rapporté. null = rien rapporté (JAMAIS un zéro fabriqué).</summary>
    EtatDepassement? Depassement { get; }

    /// <summary>Issue du dernier passage de la sonde. Aucune chaîne : un corps d'erreur ne peut pas fuir.</summary>
    ResultatSonde DernierResultat { get; }

    /// <summary>NOMS des en-têtes de limite reconnus au dernier passage — JAMAIS leurs valeurs.
    /// La famille unified n'étant pas documentée, c'est le seul moyen honnête de constater sur la
    /// machine de l'utilisateur qu'elle a été renommée : « 200, aucun en-tête unified reconnu ».
    /// Les noms rendus proviennent TOUJOURS de l'ensemble de constantes de la sonde, jamais du serveur.</summary>
    IReadOnlyList<string> NomsEnTetesRecus { get; }

    /// <summary>Émis sur un thread du POOL, sur TRANSITION uniquement (jamais un événement par tick).
    /// L'abonné marshalle lui-même vers le thread UI.</summary>
    event EventHandler<EtatDepassement?>? DepassementChange;
}
```

`using Chronos.Models;` en tête (le contrat référence `EtatDepassement`). Aucune implémentation dans ce
plan : la sonde l'implémentera au plan 18-04, et le câblage DI est le plan 18-05 — exactement le
précédent de `IAuthStatus`, contrat sans implémentation au commit 17-02.

**B. ÉTENDRE `tests/Chronos.Tests/CompositeUsageProviderTests.cs`** — **ne réécrire aucun test
existant**. Ajouter en fin de fichier une région
`// --- HDR-03/HDR-04 : ce que WindowState emporte à travers Best() (phase 18) ---` puis les tests
couvrant les six puces du `<behavior>`.

Ajouter un helper local à côté de `Win` (sans modifier `Win`) :

```csharp
private static WindowState WinStatut(WindowKind k, SourceReliability r,
                                     StatutServeur? statut = null, EtatDepassement? dep = null)
    => new() { Kind = k, Reliability = r, StatutServeur = statut, Depassement = dep };
```

Le test du dépassement écarté porte un commentaire explicite :
`// C'est CE cas qui justifie IEtatServeur : le dépassement porté par la fenêtre perdante est jeté par`
`// Best(). Le canal latéral est le seul moyen de le faire survivre sans toucher CompositeUsageProvider.cs.`

**INTERDIT** : modifier `src/Chronos/Services/CompositeUsageProvider.cs`. Ces tests prouvent une
propriété du code EXISTANT ; si l'un échoue, c'est la conception du plan qui est fausse, pas le
composite — remonter le problème au lieu de toucher au fichier.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~CompositeUsageProviderTests"</automated>
  </verify>

  <acceptance_criteria>
    - `src/Chronos/Services/IEtatServeur.cs` existe et `grep -c "interface IEtatServeur" src/Chronos/Services/IEtatServeur.cs` == 1.
    - `grep -cE "(Depassement|DernierResultat|NomsEnTetesRecus|DepassementChange)" src/Chronos/Services/IEtatServeur.cs` >= 4 — les 4 membres sont présents.
    - `grep -c "System.Windows" src/Chronos/Services/IEtatServeur.cs` == 0.
    - `git diff -- src/Chronos/Services/CompositeUsageProvider.cs` est VIDE.
    - `git diff -- tests/Chronos.Tests/CompositeUsageProviderTests.cs | grep -c "^-"` == 0 — extension PURE, aucune ligne supprimée.
    - `grep -c "Assert.Same" tests/Chronos.Tests/CompositeUsageProviderTests.cs` >= 4 — la transmission par référence est assertée.
    - `grep -c "WinStatut" tests/Chronos.Tests/CompositeUsageProviderTests.cs` >= 6.
    - `dotnet test … --filter "FullyQualifiedName~CompositeUsageProviderTests"` : 0 échec, au moins 6 tests de plus qu'avant le plan.
  </acceptance_criteria>

  <done>
Le canal latéral est un contrat, et un test prouve **mécaniquement** pourquoi il est nécessaire :
le dépassement d'une fenêtre perdante est réellement jeté par `Best()`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 de transport — un 429 qui porte des en-têtes, et les sept jeux de référence</name>

  <read_first>
    - tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs (intégralement)
    - tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs (lignes 40-100 — le helper Sequence et le motif de fixture)
    - .planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-RESEARCH.md (§ Code Examples 1 et 4, § Pitfall 3)
    - .planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-VALIDATION.md (§ Wave 0 Requirements)
  </read_first>

  <files>
tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs
tests/Chronos.Tests/EnTetesDeReference.cs
  </files>

  <action>
**A. ÉTENDRE `FakeHttpMessageHandler` — additif STRICT** (aucun appelant existant ne doit changer ;
`git diff | grep "^-"` doit rendre 0). Ajouter deux fabriques :

```csharp
/// <summary>Réponse porteuse d'EN-TÊTES, sur N'IMPORTE QUEL code de statut. Le corps n'a aucune
/// importance : la sonde d'en-têtes ne le lit JAMAIS (contrainte de sécurité). Sert le cas décisif de
/// HDR-02 : un 429 qui livre quand même les chiffres. Vérifié empiriquement : SendAsync ne lève pas
/// sur 4xx/5xx et les en-têtes non standard atterrissent dans resp.Headers.</summary>
public static FakeHttpMessageHandler AvecEnTetes(
    HttpStatusCode statut, IReadOnlyDictionary<string, string> enTetes, string corps = "{}") =>
    new(_ =>
    {
        var r = new HttpResponseMessage(statut) { Content = new StringContent(corps) };
        foreach (var (k, v) in enTetes) r.Headers.TryAddWithoutValidation(k, v);
        return r;
    });

/// <summary>Séquence de réponses PORTEUSES D'EN-TÊTES (une par envoi, puis la dernière se répète).
/// Indispensable pour le rejeu unique sur 401 et pour prouver le frein de cadence.</summary>
public static FakeHttpMessageHandler SequenceAvecEnTetes(
    params (HttpStatusCode statut, IReadOnlyDictionary<string, string> enTetes)[] etapes)
{
    var i = 0;
    return new FakeHttpMessageHandler(_ =>
    {
        var (s, h) = etapes[Math.Min(i, etapes.Length - 1)];
        i++;
        var r = new HttpResponseMessage(s) { Content = new StringContent("{}") };
        foreach (var (k, v) in h) r.Headers.TryAddWithoutValidation(k, v);
        return r;
    });
}
```

`TryAddWithoutValidation` est **obligatoire** : `Add` rejette les noms non standard.
`using System.Collections.Generic;` si nécessaire (`ImplicitUsings` est activé, vérifier avant
d'ajouter).

**B. Créer `tests/Chronos.Tests/EnTetesDeReference.cs`** — `internal static class EnTetesDeReference`,
namespace `Chronos.Tests`. Point UNIQUE des jeux d'en-têtes de la phase : les plans 18-03 et 18-04 les
consomment et ne les redéclarent pas.

Constantes de noms (copier depuis le tableau « Noms d'en-tête retenus » de la section `<context>` de ce
plan) : `H5hUtil`, `H5hReset`, `H5hStatut`, `H7dUtil`, `H7dReset`, `H7dStatut`, `HOverUtil`,
`HOverReset`, `HOverStatut`, `HStatutGlobal`, `HClaim`.

Sept jeux, chacun un `IReadOnlyDictionary<string, string>` rendu par une propriété statique, avec en
XML-doc ce que le jeu prouve. Valeurs EXACTES (reprises du dump réel, épochs postérieurs au plancher de
sanité 2020-01-01) :

1. `Nominal` — `H5hUtil="0.01"`, `H5hReset="1783180800"`, `H5hStatut="allowed"`,
   `H7dUtil="0.63"`, `H7dReset="1783713600"`, `HClaim="five_hour"`.
   *Prouve HDR-01 : deux fenêtres exactes, fractions en 0..1, resets en epoch secondes.*
2. `Avertissement` — `Nominal` avec `H5hStatut="allowed_warning"`. *Prouve HDR-03.*
3. `Refus` — `Nominal` avec `H5hStatut="rejected"` et `H5hUtil="1.0"`. *Le cas 429 : refusé ET épuisé.*
4. `StatutInconnu` — `Nominal` avec `H5hStatut="active"`. *Prouve `NonReconnu`, jamais `Autorise`.*
5. `DepassementSeul` — **uniquement** `HOverUtil="0.34"`, `HOverReset="1783800000"`,
   `HStatutGlobal="allowed_warning"` — **aucune** fenêtre 5 h ni 7 j.
   *Prouve HDR-04 dans sa forme alternative : les deux fenêtres sont légitimement Unavailable.*
6. `Absents` — dictionnaire **vide**. *Le cas de première classe : plan sans famille `unified`, 429 de
   plafond de dépense, ou famille renommée. La bonne réponse est `UsageSnapshot.Empty`, jamais 0 %.*
7. `Illisibles` — `H5hUtil="pas-un-nombre"`, `H5hReset="9"` (l'epoch 1970 RÉEL de cette machine),
   `H7dUtil="0,63"` (virgule : la forme qu'un serveur n'envoie pas mais qu'une culture produirait),
   `H5hStatut=""`. *Prouve la tolérance : chaque en-tête se lit indépendamment, une valeur illisible
   n'invalide pas les autres, et rien ne devient 0.*

Ajouter aussi un jeu `NominalCasseMelangee` : `Nominal` avec les noms écrits
`Anthropic-RateLimit-Unified-5h-Utilization` etc. *Prouve la recherche insensible à la casse (vérifiée
empiriquement) — aucune normalisation de casse à écrire dans la sonde.*

Ajouter un unique test de fumée dans ce même fichier — classe publique `EnTetesDeReferenceTests` —
prouvant que la Wave 0 tient : un `HttpClient` monté sur
`FakeHttpMessageHandler.AvecEnTetes(HttpStatusCode.TooManyRequests, EnTetesDeReference.Nominal)`
rend une réponse dont `resp.StatusCode == 429` (aucune exception levée par `SendAsync`), dont
`resp.Headers.TryGetValues(EnTetesDeReference.H5hUtil, out _)` est **true**, et dont
`resp.Content.Headers.TryGetValues(EnTetesDeReference.H5hUtil, out _)` est **false** — les trois faits
.NET vérifiés sur lesquels repose toute la phase. Y ajouter le cas casse mélangée :
`resp.Headers.TryGetValues("anthropic-ratelimit-unified-5h-utilization", out _)` est **true** même
quand l'en-tête a été posé en casse mélangée.

SÉCURITÉ : aucun test de ce plan ne joint le réseau (tout par `FakeHttpMessageHandler`), aucun n'écrit
dans `%APPDATA%\Chronos\`, aucun n'ouvre `oauth.dat`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~EnTetesDeReferenceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `git diff -- tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs | grep -c "^-"` == 0 — extension additive stricte.
    - `grep -c "TryAddWithoutValidation" tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs` == 2.
    - `grep -cE "static FakeHttpMessageHandler (AvecEnTetes|SequenceAvecEnTetes)" tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs` == 2.
    - `tests/Chronos.Tests/EnTetesDeReference.cs` existe.
    - `grep -cE "anthropic-ratelimit-unified-(5h|7d|overage)-(utilization|reset|status)" tests/Chronos.Tests/EnTetesDeReference.cs` >= 8.
    - `grep -c "anthropic-ratelimit-unified-status" tests/Chronos.Tests/EnTetesDeReference.cs` >= 1 — le statut GLOBAL, sans segment de fenêtre (correction de CONTEXT.md).
    - `grep -cE "public static IReadOnlyDictionary<string, ?string>" tests/Chronos.Tests/EnTetesDeReference.cs` == 8 — les 7 jeux + `NominalCasseMelangee`.
    - `grep -c "\"9\"" tests/Chronos.Tests/EnTetesDeReference.cs` == 1 — l'epoch 1970 réel est bien dans le jeu `Illisibles`.
    - `grep -c "Content.Headers" tests/Chronos.Tests/EnTetesDeReference.cs` >= 1 — le fait « pas dans Content.Headers » est assertée, pas supposée.
    - `dotnet test … --filter "FullyQualifiedName~EnTetesDeReferenceTests"` : 0 échec.
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**.
  </acceptance_criteria>

  <done>
On peut désormais fabriquer un 429 porteur d'en-têtes en une ligne, et les sept formes réelles de
réponse sont déclarées une seule fois pour toute la phase.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -v q --nologo` → **0 échec**, **0 test supprimé**.
2. `dotnet build Chronos.sln` → 0 erreur : preuve que les 21 sites `new WindowState { … }` compilent.
3. `git diff --name-only -- src/Chronos/Services/CompositeUsageProvider.cs src/Chronos/Models/UsageSnapshot.cs` → **VIDE**.
4. `grep -c "SchemaVersion = 1" src/Chronos/Services/LastExactStore.cs` → **1** : aucun bump (3 fixtures
   de test épinglent `"version":1` / `"version":999`, et le statut serveur est une assertion VOLATILE
   qu'il serait mensonger de persister).
5. `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert.
6. `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~LastExactStoreTests"` → vert.
7. Coffre intact : `%APPDATA%\Chronos\oauth.dat` = **518 octets, mtime 1783863147** avant et après.
</verification>

<success_criteria>
- HDR-03 : le statut serveur a un vocabulaire OUVERT (`NonReconnu`), il distingue « absent » de
  « présent mais inconnu », et il est posé au seul endroit qui survive au composite.
- HDR-04 : le dépassement existe comme fait de compte, porté par **deux** canaux, et un test prouve
  pourquoi un seul ne suffit pas.
- La Wave 0 de transport est en place : un 429 porteur d'en-têtes est fabricable, les sept jeux de
  référence sont déclarés.
- 0 échec, 0 test supprimé, aucun fichier interdit touché, aucun bump de `SchemaVersion`.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/18-source-exacte-par-en-t-tes-de-rate-limit/18-02-SUMMARY.md`.
Y consigner : le nombre exact de tests avant/après, la confirmation que
`git diff -- src/Chronos/Services/CompositeUsageProvider.cs` est vide, et **les noms d'en-tête retenus
avec leur niveau de confiance** (confirmé par le code d'origine / observé dans un dump indépendant /
annoncé par CONTEXT.md mais non confirmé) — les plans 18-03 et 18-04 en dépendent.
</output>
