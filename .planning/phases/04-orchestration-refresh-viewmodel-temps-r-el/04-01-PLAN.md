---
phase: 04-orchestration-refresh-viewmodel-temps-r-el
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/RefreshOptions.cs
  - src/Chronos/Services/RefreshOrchestrator.cs
  - tests/Chronos.Tests/Fakes/FakeUiDispatcher.cs
  - tests/Chronos.Tests/Fakes/FakeUsageProvider.cs
  - tests/Chronos.Tests/RefreshOrchestratorTests.cs
autonomous: true
requirements: [RAF-01, RAF-02]

must_haves:
  truths:
    - "Une écriture sur usage.json déclenche (watcher débouncé) un GetAsync puis l'émission de SnapshotChanged"
    - "Le PeriodicTimer relit les données à intervalle configurable même sans aucun événement watcher (filet de sécurité)"
    - "Une rafale de déclencheurs est coalescée : pas de GetAsync concurrents, un seul rattrapage"
    - "L'événement Error du watcher entraîne sa recréation sans perdre la capacité de rafraîchir"
    - "RefreshOrchestrator ne référence aucun type WPF (ServicesLayerPurityTests reste vert sans allow-list)"
  artifacts:
    - path: "src/Chronos/Services/RefreshOrchestrator.cs"
      provides: "BackgroundService neutre : watcher + PeriodicTimer + Channel, event SnapshotChanged"
      contains: "class RefreshOrchestrator"
      min_lines: 60
    - path: "src/Chronos/Services/RefreshOptions.cs"
      provides: "Record d'options (PeriodicInterval, Debounce) + Default"
      contains: "record RefreshOptions"
    - path: "tests/Chronos.Tests/Fakes/FakeUsageProvider.cs"
      provides: "Fake IUsageProvider comptant GetAsync et émettant SnapshotChanged"
      contains: "class FakeUsageProvider"
    - path: "tests/Chronos.Tests/Fakes/FakeUiDispatcher.cs"
      provides: "Fake IUiDispatcher comptant les Post (partagé avec 04-02)"
      contains: "class FakeUiDispatcher"
    - path: "tests/Chronos.Tests/RefreshOrchestratorTests.cs"
      provides: "Tests RAF-01 (coalescence, Error→recréation) + RAF-02 (PeriodicTimer)"
      contains: "RefreshOrchestratorTests"
  key_links:
    - from: "src/Chronos/Services/RefreshOrchestrator.cs"
      to: "IUsageProvider.GetAsync"
      via: "appel dans la boucle consommateur unique"
      pattern: "_provider\\.GetAsync"
    - from: "FileSystemWatcher + PeriodicTimer"
      to: "boucle consommateur unique"
      via: "Channel<bool> capacité 1 DropWrite"
      pattern: "Channel\\.CreateBounded"
---

<objective>
Construire l'horloge DONNÉES : un `RefreshOrchestrator` neutre (couche Services, zéro WPF) qui
possède le `FileSystemWatcher` débouncé sur `usage.json` (RAF-01) ET un `PeriodicTimer` de secours
configurable (RAF-02). Les deux se contentent d'écrire un déclencheur dans un `Channel<bool>`
capacité 1 `DropWrite` ; une boucle consommateur unique lit le channel et appelle
`IUsageProvider.GetAsync` un à la fois (jamais de lecture concurrente), puis émet
`SnapshotChanged`. Le VM (plan 04-02) s'abonnera à cet événement.

Purpose: transformer le pipeline Phase 3 (tiré à la demande) en flux poussé en continu, sans
franchir aucun thread WPF (l'orchestrateur reste neutre ; le marshaling est fait côté VM en 04-02).
Output: `RefreshOrchestrator.cs`, `RefreshOptions.cs`, les fakes de test partagés, et
`RefreshOrchestratorTests.cs` vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-CONTEXT.md
@.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-RESEARCH.md

<interfaces>
<!-- Contrats déjà en place (Phase 3). L'exécuteur les CONSOMME tels quels, ne les recrée pas. -->

Depuis src/Chronos/Services/IUsageProvider.cs :
```csharp
public interface IUsageProvider
{
    Task<UsageSnapshot> GetAsync(CancellationToken ct = default);
    event EventHandler<UsageSnapshot>? SnapshotChanged;
}
```

Depuis src/Chronos/Services/ChronosPaths.cs (injecté en Singleton) :
```csharp
public sealed record ChronosPaths(string UsageFile, string ProjectsRoot);
// UsageFile = %APPDATA%\Chronos\usage.json  → le watcher surveille son RÉPERTOIRE, filtre "usage.json"
```

Depuis src/Chronos/Models/UsageSnapshot.cs :
```csharp
public sealed record UsageSnapshot
{
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
    public DateTimeOffset? SourceCapturedAt { get; init; }
    public TimeSpan? Age { get; init; }
    public static UsageSnapshot Empty { get; }   // deux fenêtres Unavailable
}
```

Garde de neutralité — tests/Chronos.Tests/ServicesLayerPurityTests.cs :
tout type de `Chronos.Services` hors allow-list {WpfUiDispatcher, TopmostGuard} NE DOIT exposer
aucun type des assemblies WPF (PresentationCore/PresentationFramework/WindowsBase) dans ses
signatures publiques. `RefreshOrchestrator` NE DOIT PAS être ajouté à l'allow-list : il reste neutre.

InternalsVisibleTo("Chronos.Tests") est déjà actif (Chronos.csproj) → les membres `internal`
du RefreshOrchestrator sont visibles aux tests (utile pour des seams déterministes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Wave 0 — RefreshOptions + fakes de test partagés</name>
  <read_first>
    - src/Chronos/Services/ChronosPaths.cs (pattern record + Default())
    - src/Chronos/Services/IUsageProvider.cs (contrat à faker)
    - src/Chronos/Services/IUiDispatcher.cs (contrat à faker)
    - tests/Chronos.Tests/Fakes/FakeClock.cs (style des fakes internes)
  </read_first>
  <files>
    src/Chronos/Services/RefreshOptions.cs,
    tests/Chronos.Tests/Fakes/FakeUiDispatcher.cs,
    tests/Chronos.Tests/Fakes/FakeUsageProvider.cs
  </files>
  <action>
    Créer les briques d'infrastructure (aucune logique métier — pas de TDD).

    1. `src/Chronos/Services/RefreshOptions.cs` — record neutre, valeurs VERBATIM du RESEARCH :
    ```csharp
    namespace Chronos.Services;

    /// <summary>Réglages des horloges données. Injecté en Singleton (pattern ChronosPaths).
    /// La persistance settings.json arrive en Phase 6 ; ici, valeurs par défaut pré-câblées.</summary>
    public sealed record RefreshOptions(TimeSpan PeriodicInterval, TimeSpan Debounce)
    {
        public static RefreshOptions Default => new(
            PeriodicInterval: TimeSpan.FromSeconds(60),        // filet de sécurité (RAF-02)
            Debounce:         TimeSpan.FromMilliseconds(300)); // coalescence/settle (RAF-01)
    }
    ```

    2. `tests/Chronos.Tests/Fakes/FakeUiDispatcher.cs` (partagé avec 04-02) — VERBATIM RESEARCH :
    ```csharp
    using Chronos.Services;

    namespace Chronos.Tests.Fakes;

    internal sealed class FakeUiDispatcher : IUiDispatcher
    {
        public int PostCount { get; private set; }
        public bool OnUiThread { get; set; }
        public bool CheckAccess() => OnUiThread;
        public void Post(Action a) { PostCount++; a(); }   // exécute inline pour l'assertion
    }
    ```

    3. `tests/Chronos.Tests/Fakes/FakeUsageProvider.cs` — compteur GetAsync + gate optionnel pour
    tester la coalescence (le RESEARCH donne la base ; ajouter un gate `ManualResetEventSlim`
    injectable pour bloquer un GetAsync en vol pendant qu'on empile des déclencheurs) :
    ```csharp
    using Chronos.Models;
    using Chronos.Services;

    namespace Chronos.Tests.Fakes;

    internal sealed class FakeUsageProvider : IUsageProvider
    {
        private int _getCount;
        public int GetCount => Volatile.Read(ref _getCount);
        public UsageSnapshot Next = UsageSnapshot.Empty;
        /// <summary>Si posé, chaque GetAsync attend ce gate (test de coalescence).</summary>
        public ManualResetEventSlim? Gate;
        public event EventHandler<UsageSnapshot>? SnapshotChanged;

        public Task<UsageSnapshot> GetAsync(CancellationToken ct = default)
        {
            Interlocked.Increment(ref _getCount);
            Gate?.Wait(ct);
            SnapshotChanged?.Invoke(this, Next);
            return Task.FromResult(Next);
        }
    }
    ```
    Note : le namespace des fakes existants est `Chronos.Tests` (voir FakeClock). Vérifier le
    namespace réellement utilisé par FakeClock et rester COHÉRENT (Chronos.Tests vs
    Chronos.Tests.Fakes) pour que les tests référencent les fakes sans using superflu.
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` réussit (0 erreur).
    - `RefreshOptions.Default` a bien PeriodicInterval=60 s et Debounce=300 ms (grep sur les littéraux).
    - Les deux fakes compilent et implémentent respectivement IUiDispatcher / IUsageProvider.
  </acceptance_criteria>
  <done>RefreshOptions + FakeUiDispatcher + FakeUsageProvider compilent ; la suite Phases 1-3 reste verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : RefreshOrchestrator (BackgroundService neutre) + tests RAF-01/RAF-02</name>
  <read_first>
    - .planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-RESEARCH.md (Pattern 1 : esquisse complète du RefreshOrchestrator lignes 124-198 ; Pitfalls 2 et 5)
    - src/Chronos/Services/CompositeUsageProvider.cs (l'IUsageProvider réel appelé en prod)
    - src/Chronos/Services/ChronosPaths.cs (UsageFile → répertoire à surveiller)
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (garde de neutralité à respecter)
  </read_first>
  <files>
    src/Chronos/Services/RefreshOrchestrator.cs,
    tests/Chronos.Tests/RefreshOrchestratorTests.cs
  </files>
  <behavior>
    Écrire les tests AVANT l'implémentation (RED → GREEN). Comportements à prouver :
    - RAF-02 (filet périodique) : avec `RefreshOptions(PeriodicInterval: 50 ms, Debounce: TimeSpan.Zero)`,
      démarrer via `StartAsync` SANS aucun événement watcher → `FakeUsageProvider.GetCount` croît
      (≥ 2 en ~500 ms). Prouve que le PeriodicTimer déclenche GetAsync seul.
    - RAF-01 (déclenchement watcher) : ChronosPaths pointant vers un répertoire temporaire ;
      démarrer l'orchestrateur ; écrire/renommer `usage.json` dans ce répertoire → GetCount
      s'incrémente (poll avec timeout généreux ~2 s ; tolérant à la nature best-effort du watcher).
    - RAF-01 (coalescence) : poser `FakeUsageProvider.Gate` (bloque le 1er GetAsync en vol) ;
      empiler N déclencheurs (via le seam interne de trigger) pendant le blocage ; libérer le gate →
      GetCount RESTE borné (≤ 2), PAS N. Prouve le Channel(1, DropWrite) + consommateur unique.
    - RAF-01 (Error → recréation) : après avoir provoqué la recréation du watcher (via seam interne
      `RecreateWatcher()`), un déclencheur ultérieur produit toujours un snapshot (GetCount croît).
    - SnapshotChanged émis : chaque GetAsync fait remonter un `UsageSnapshot` via l'event de
      l'orchestrateur (capturer l'event, asserter au moins une émission).
    Utiliser `CancellationTokenSource` + `StopAsync` pour un arrêt propre à la fin de chaque test.
  </behavior>
  <action>
    Implémenter `src/Chronos/Services/RefreshOrchestrator.cs` en suivant Pattern 1 du RESEARCH
    (esquisse quasi complète, lignes 124-198). Points VERROUILLÉS / concrets :

    - Hériter de `Microsoft.Extensions.Hosting.BackgroundService`. Namespace `Chronos.Services`.
      NE PAS ajouter à l'allow-list de ServicesLayerPurityTests → rester 100 % neutre (aucun
      `using System.Windows.*`).
    - Dépendances ctor : `(IUsageProvider provider, ChronosPaths paths, RefreshOptions options)`.
    - Champ `Channel<bool> _triggers = Channel.CreateBounded<bool>(new BoundedChannelOptions(1)
      { FullMode = BoundedChannelFullMode.DropWrite });` (System.Threading.Channels).
    - `public event EventHandler<UsageSnapshot>? SnapshotChanged;` (l'orchestrateur EST le point
      d'exposition — décision verrouillée ; le VM s'abonnera ICI, pas au composite).
    - `ExecuteAsync(stoppingToken)` : CreateWatcher() ; lancer `RunPeriodicAsync(stoppingToken)`
      (fire-and-forget) ; `_triggers.Writer.TryWrite(true)` pour la charge initiale ; puis
      `await foreach (var _ in _triggers.Reader.ReadAllAsync(stoppingToken))` → si
      `_options.Debounce > TimeSpan.Zero` faire `await Task.Delay(_options.Debounce, stoppingToken)`
      → `var snap = await _provider.GetAsync(stoppingToken)` → `SnapshotChanged?.Invoke(this, snap)`.
      Envelopper dans try/catch(OperationCanceledException) pour un arrêt silencieux.
    - `RunPeriodicAsync` : `using var timer = new PeriodicTimer(_options.PeriodicInterval);`
      boucle `while (await timer.WaitForNextTickAsync(ct)) _triggers.Writer.TryWrite(true);`
      catch OperationCanceledException.
    - `CreateWatcher()` : `dir = Path.GetDirectoryName(_paths.UsageFile)!` ;
      `Directory.CreateDirectory(dir)` (le pont crée usage.json, mais le dossier doit exister pour
      démarrer le watcher) ; `new FileSystemWatcher(dir, "usage.json")` avec
      `NotifyFilter = NotifyFilters.FileName | NotifyFilters.LastWrite` et
      `EnableRaisingEvents = true` ; s'abonner à **Changed + Created + Renamed** (tous → même
      handler qui fait `_triggers.Writer.TryWrite(true)`) — Renamed OBLIGATOIRE car le pont écrit
      atomiquement (renameSync), sinon écritures ratées (Pitfall 2). S'abonner à `Error` → handler
      qui dispose + recrée le watcher + `TryWrite(true)` (rescan).
    - `StopAsync(ct)` : dispose du watcher puis `await base.StopAsync(ct)` (signale stoppingToken →
      la boucle et le PeriodicTimer sortent). Disposal propre = décision verrouillée.
    - SEAMS DE TEST (internal, visibles via InternalsVisibleTo) pour des tests déterministes sans
      dépendre du timing réel du FileSystemWatcher :
        `internal bool TryTrigger() => _triggers.Writer.TryWrite(true);`
        `internal void RecreateWatcher();`  // = corps du handler Error, appelable par les tests
      Ces seams n'exposent que des types neutres (bool, void) → la garde de pureté reste verte.

    Anti-patterns interdits (RESEARCH) : ne JAMAIS appeler GetAsync directement dans un handler
    watcher/timer (seul le consommateur unique appelle GetAsync — Pitfall 5) ; aucun type WPF.
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~RefreshOrchestrator|FullyQualifiedName~ServicesLayerPurity"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test ... --filter RefreshOrchestrator` : tous verts (RAF-01 coalescence + Error, RAF-02 périodique, SnapshotChanged).
    - `ServicesLayerPurityTests` reste vert SANS ajout de RefreshOrchestrator à l'allow-list (preuve de neutralité).
    - grep `Channel\.CreateBounded` et `BoundedChannelFullMode.DropWrite` présents dans RefreshOrchestrator.cs.
    - grep `w.Renamed` (ou `Renamed +=`) et `Error +=` présents (Pitfall 2 couvert).
    - Aucun `using System.Windows` dans RefreshOrchestrator.cs.
  </acceptance_criteria>
  <done>RefreshOrchestrator pousse des snapshots via watcher débouncé (RAF-01) + PeriodicTimer (RAF-02), coalesce les rafales, se recrée sur Error, reste neutre. Tests verts.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` : 0 erreur.
- `dotnet test Chronos.sln -c Debug` : suite complète verte (27 tests Phases 1-3 + nouveaux RefreshOrchestratorTests + ServicesLayerPurityTests inchangé).
- RefreshOrchestrator absent de l'allow-list WPF → neutralité prouvée automatiquement.
</verification>

<success_criteria>
- RAF-01 : un FileSystemWatcher débouncé (Changed/Created/Renamed + Error→recréation) déclenche la relecture, rafales coalescées via Channel(1, DropWrite).
- RAF-02 : un PeriodicTimer à intervalle configurable (RefreshOptions, défaut 60 s) relit en filet de sécurité sans événement watcher.
- Consommateur unique : jamais de GetAsync concurrents.
- Couche neutre : zéro type WPF, ServicesLayerPurityTests vert.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-01-SUMMARY.md`
</output>
