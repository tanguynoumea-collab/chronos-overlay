---
phase: 04-orchestration-refresh-viewmodel-temps-r-el
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/Chronos/Text/CountdownFormatter.cs
  - src/Chronos/ViewModels/WindowGaugeViewModel.cs
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/App.xaml.cs
  - src/Chronos/Views/MainWindow.xaml.cs
  - tests/Chronos.Tests/CountdownFormatterTests.cs
  - tests/Chronos.Tests/MainViewModelTests.cs
autonomous: true
requirements: [RAF-03, RAF-04]

must_haves:
  truths:
    - "Un snapshot poussé par l'orchestrateur (hors thread UI) est appliqué au VM via IUiDispatcher.Post exactement une fois"
    - "Interpolate(now) recalcule fraction d'arc + compte à rebours SANS aucun I/O disque (GetAsync jamais appelé au tick)"
    - "Le compte à rebours est formaté en français (ex. 2 h 05, 3 j 14 h, 45 min)"
    - "DataUnavailable est vrai quand les deux fenêtres sont Unavailable ; les propriétés de fenêtre reflètent le snapshot"
    - "Le MainViewModel est résolu AVANT _host.StartAsync() : le snapshot initial n'est pas perdu"
  artifacts:
    - path: "src/Chronos/Text/CountdownFormatter.cs"
      provides: "Formatage FR pur d'un TimeSpan (2 h 05 / 3 j 14 h / 45 min / 0 min)"
      contains: "CountdownFormatter"
    - path: "src/Chronos/ViewModels/WindowGaugeViewModel.cs"
      provides: "Sous-VM par fenêtre : fraction, utilization, countdown, provenance"
      contains: "WindowGaugeViewModel"
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "Abonnement orchestrateur + marshaling + Interpolate + StartClock"
      contains: "RefreshOrchestrator"
      min_lines: 40
    - path: "src/Chronos/App.xaml.cs"
      provides: "DI orchestrateur + hostedservice même instance + pré-résolution VM avant StartAsync"
      contains: "GetRequiredService<MainViewModel>"
    - path: "tests/Chronos.Tests/MainViewModelTests.cs"
      provides: "Tests RAF-04 (marshaling PostCount) + RAF-03 (Interpolate sans I/O)"
      contains: "MainViewModelTests"
    - path: "tests/Chronos.Tests/CountdownFormatterTests.cs"
      provides: "Tests formatage FR"
      contains: "CountdownFormatterTests"
  key_links:
    - from: "src/Chronos/ViewModels/MainViewModel.cs"
      to: "RefreshOrchestrator.SnapshotChanged"
      via: "abonnement dans le ctor"
      pattern: "SnapshotChanged \\+="
    - from: "OnSnapshotChanged (thread pool)"
      to: "ApplySnapshot (thread UI)"
      via: "IUiDispatcher.Post"
      pattern: "_ui\\.Post"
    - from: "src/Chronos/App.xaml.cs"
      to: "MainViewModel (abonnement)"
      via: "GetRequiredService avant StartAsync"
      pattern: "GetRequiredService<MainViewModel>\\(\\)"
    - from: "DispatcherTimer 1 s (StartClock)"
      to: "Interpolate(now)"
      via: "Tick côté UI, aucun I/O"
      pattern: "Interpolate\\("
---

<objective>
Enrichir la couche présentation pour un affichage temps réel : (1) un `CountdownFormatter` pur
(FR) ; (2) un `WindowGaugeViewModel` par fenêtre (5 h / hebdo) qui recalcule fraction d'arc +
compte à rebours ; (3) un `MainViewModel` qui s'abonne à `RefreshOrchestrator.SnapshotChanged`,
franchit la frontière de thread EN UN SEUL POINT via `IUiDispatcher.Post` (RAF-04), et expose
`Interpolate(now)` (pur, sans I/O — RAF-03) piloté par un `DispatcherTimer` 1 s créé côté UI
(`StartClock`) ; (4) le câblage `App.xaml.cs` (DI orchestrateur en Singleton + IHostedService même
instance, VM pré-résolu AVANT `StartAsync`) et `MainWindow` (appel `StartClock` au chargement).

Purpose: rendre les données visibles et vivantes (compte à rebours qui décroît chaque seconde)
sans jamais provoquer d'InvalidOperationException ni relire le disque au tick UI.
Output: 3 fichiers source neufs, 2 fichiers câblés (App, MainWindow), 2 fichiers de tests verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-CONTEXT.md
@.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-RESEARCH.md
@.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-01-SUMMARY.md

<interfaces>
<!-- Contrats CONSOMMÉS. L'orchestrateur vient du plan 04-01 (même wave amont). -->

Depuis src/Chronos/Services/RefreshOrchestrator.cs (livré en 04-01) :
```csharp
public sealed class RefreshOrchestrator : BackgroundService
{
    public RefreshOrchestrator(IUsageProvider provider, ChronosPaths paths, RefreshOptions options);
    public event EventHandler<UsageSnapshot>? SnapshotChanged;   // ← le VM s'abonne ICI
}
```

Depuis src/Chronos/Services/IUiDispatcher.cs (point de marshaling unique — RAF-04) :
```csharp
public interface IUiDispatcher { bool CheckAccess(); void Post(Action action); }
// WpfUiDispatcher.Post : if (CheckAccess()) action(); else BeginInvoke(action);
```

Depuis src/Chronos/Services/IClock.cs :
```csharp
public interface IClock { DateTimeOffset UtcNow { get; } }   // SystemClock en prod, FakeClock en test
```

Depuis src/Chronos/Models/WindowState.cs (cœur de l'interpolation, PURE, prend `now`) :
```csharp
public sealed record WindowState
{
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }            // 0..1 ou null
    public DateTimeOffset? ResetsAt { get; init; }        // null si inconnu
    public bool Exhausted => Utilization is >= 1.0;
    public required SourceReliability Reliability { get; init; }  // Exact | Estimated | Unavailable
    public static WindowState Unavailable(WindowKind k);
    public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength);
}
```

Depuis src/Chronos/Models/UsageSnapshot.cs :
```csharp
public sealed record UsageSnapshot
{
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
    public DateTimeOffset? SourceCapturedAt { get; init; }
    public TimeSpan? Age { get; init; }
    public static UsageSnapshot Empty { get; }
}
```

État actuel à modifier :
- src/Chronos/ViewModels/MainViewModel.cs : VIDE (`public sealed partial class MainViewModel : ObservableObject {}`).
- src/Chronos/App.xaml.cs : `OnStartup` résout MainWindow APRÈS `await _host.StartAsync()` (Pitfall 3 à corriger). ConfigureServices enregistre déjà IUiDispatcher, IClock, ChronosPaths, IUsageProvider (composite), MainViewModel, MainWindow (tous Singleton).
- src/Chronos/Views/MainWindow.xaml.cs : ctor `(MainViewModel viewModel, TopmostGuard topmostGuard)`, `Loaded += PlacerCoinSuperieurDroit`.

Fakes disponibles (livrés 04-01) : FakeUiDispatcher (PostCount, OnUiThread), FakeUsageProvider ;
FakeClock existe déjà. Vérifier leur namespace réel avant `using`.
CommunityToolkit.Mvvm 8.4.2 : `[ObservableProperty]` sur champ privé `_camelCase` génère la propriété publique `PascalCase`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : CountdownFormatter (formatage FR pur) + tests</name>
  <read_first>
    - .planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-RESEARCH.md (Pattern 5, tableau de formatage FR lignes 318-326)
    - src/Chronos/Models/WindowState.cs (style records/méthodes pures neutres)
  </read_first>
  <files>
    src/Chronos/Text/CountdownFormatter.cs,
    tests/Chronos.Tests/CountdownFormatterTests.cs
  </files>
  <behavior>
    Fonction pure `static string Format(TimeSpan reste)`. Cas (tests AVANT implémentation) :
    - `3 j 14 h` de reste (≥ 1 jour)      → `"3 j 14 h"`     (règle : `{j} j {h} h`)
    - `2 h 5 min` de reste (≥ 1 h)        → `"2 h 05"`       (minutes sur 2 chiffres)
    - `45 min` de reste (< 1 h)           → `"45 min"`
    - `TimeSpan.Zero` ou négatif          → `"0 min"`
    Littéraux FR fixes (pas de CultureInfo). Aucune dépendance WPF (type neutre).
  </behavior>
  <action>
    Créer `src/Chronos/Text/CountdownFormatter.cs`, namespace `Chronos.Text` :
    ```csharp
    namespace Chronos.Text;

    /// <summary>Formatage FR pur d'un compte à rebours (aucune dépendance WPF, aucun I/O).</summary>
    public static class CountdownFormatter
    {
        public static string Format(TimeSpan reste)
        {
            if (reste <= TimeSpan.Zero) return "0 min";
            if (reste.TotalDays >= 1)  return $"{(int)reste.TotalDays} j {reste.Hours} h";
            if (reste.TotalHours >= 1) return $"{(int)reste.TotalHours} h {reste.Minutes:D2}";
            return $"{(int)reste.TotalMinutes} min";
        }
    }
    ```
    Écrire `CountdownFormatterTests.cs` couvrant les 4 cas du <behavior> (un `[Theory]`/`[Fact]`
    par ligne du tableau). Ajuster les littéraux si un cas limite l'exige, mais garder la forme FR.
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~CountdownFormatter"</automated>
  </verify>
  <acceptance_criteria>
    - Les 4 cas passent (`3 j 14 h`, `2 h 05`, `45 min`, `0 min`).
    - `CountdownFormatter` est neutre (aucun `using System.Windows`) → ne casse pas ServicesLayerPurityTests si placé sous Chronos (namespace Chronos.Text hors scope Services/Models, donc non concerné, mais rester neutre par principe).
  </acceptance_criteria>
  <done>CountdownFormatter.Format produit les chaînes FR attendues, testé, pur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : WindowGaugeViewModel + MainViewModel (marshaling + interpolation) + tests</name>
  <read_first>
    - .planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-RESEARCH.md (Pattern 4 lignes 223-280 : MainViewModel complet ; Pattern 5 lignes 282-315 : WindowGaugeViewModel complet ; Pitfalls 1 et 4)
    - src/Chronos/ViewModels/MainViewModel.cs (état actuel : vide)
    - src/Chronos/Models/WindowState.cs (FractionRemaining, Reliability, Exhausted)
  </read_first>
  <files>
    src/Chronos/ViewModels/WindowGaugeViewModel.cs,
    src/Chronos/ViewModels/MainViewModel.cs,
    tests/Chronos.Tests/MainViewModelTests.cs
  </files>
  <behavior>
    Tests en `[Fact]` SIMPLE (pas `[WpfFact]` — le DispatcherTimer n'est PAS créé dans le ctor).
    Utiliser FakeUiDispatcher + FakeClock + FakeUsageProvider. Comportements :
    - RAF-04 (marshaling) : instancier MainViewModel(orchestrator, fakeUi, fakeClock) ; déclencher
      l'orchestrateur (ou invoquer directement l'event SnapshotChanged via un fake orchestrateur /
      via FakeUsageProvider selon le seam retenu) avec `fakeUi.OnUiThread = false` → après
      application, `fakeUi.PostCount == 1` (frontière franchie EXACTEMENT une fois) et les
      propriétés des sous-VM reflètent le snapshot.
    - RAF-04 (état global) : snapshot avec FiveHour + SevenDay Unavailable → `DataUnavailable == true`.
      Snapshot avec au moins une fenêtre lisible → `DataUnavailable == false`.
    - RAF-03 (interpolation pure, aucun I/O) : appliquer un snapshot (ResetsAt = now + 2 h) ;
      avancer `FakeClock.UtcNow` de +1 h ; appeler `Interpolate(clock.UtcNow)` → `FractionRemaining`
      décroît et `CountdownText` change ; ASSERTER que `FakeUsageProvider.GetCount` n'a PAS augmenté
      pendant les Interpolate (0 I/O au tick — Pitfall 1).
    - RAF-03 (staleness) : CapturedAt vieux de > 2 min vs now → `IsStale == true`.
  </behavior>
  <action>
    Implémenter les deux VM en suivant VERBATIM les Patterns 4 et 5 du RESEARCH (esquisses quasi
    complètes). Points concrets :

    1. `src/Chronos/ViewModels/WindowGaugeViewModel.cs` (namespace `Chronos.ViewModels`,
       `public sealed partial class ... : ObservableObject`) — RESEARCH lignes 287-315 :
       - ctor `(TimeSpan windowLength)` ; champ `_windowLength`, `_state = WindowState.Unavailable(default)`.
       - `[ObservableProperty]` : `_fractionRemaining` (double), `_utilization` (double?),
         `_countdownText` (string = "—"), `_exhausted` (bool), `_reliability` (SourceReliability),
         `_isEstimated` (bool).
       - `Apply(WindowState s)` : mémorise `_state`, met à jour Utilization/Exhausted/Reliability,
         `IsEstimated = s.Reliability == SourceReliability.Estimated` (pré-câble DAT-08 Phase 5).
       - `Interpolate(DateTimeOffset now)` PUR : `FractionRemaining = WindowState.FractionRemaining(
         _state.ResetsAt, now, _windowLength) ?? 0.0` ; `CountdownText = _state.ResetsAt is { } r ?
         CountdownFormatter.Format(r - now) : "—"` (using Chronos.Text).

    2. `src/Chronos/ViewModels/MainViewModel.cs` (remplace la classe vide) — RESEARCH lignes 231-278 :
       - Dépendances ctor `(RefreshOrchestrator orchestrator, IUiDispatcher ui, IClock clock)`.
         S'abonner `orchestrator.SnapshotChanged += OnSnapshotChanged;` dans le ctor.
       - `public WindowGaugeViewModel FiveHour { get; } = new(TimeSpan.FromHours(5));`
         `public WindowGaugeViewModel SevenDay { get; } = new(TimeSpan.FromDays(7));`
       - `[ObservableProperty]` : `_dataUnavailable` (bool), `_capturedAt` (DateTimeOffset?),
         `_isStale` (bool).
       - `OnSnapshotChanged(object? s, UsageSnapshot snap) => _ui.Post(() => ApplySnapshot(snap));`
         ← FRONTIÈRE DE THREAD, franchie UNE seule fois (RAF-04).
       - `internal void ApplySnapshot(UsageSnapshot snap)` (thread UI) : `FiveHour.Apply(snap.FiveHour)`;
         `SevenDay.Apply(snap.SevenDay)`; `CapturedAt = snap.SourceCapturedAt`;
         `DataUnavailable = snap.FiveHour.Reliability == SourceReliability.Unavailable &&
         snap.SevenDay.Reliability == SourceReliability.Unavailable`; puis `Interpolate(_clock.UtcNow)`
         (premier rendu immédiat).
       - `internal void Interpolate(DateTimeOffset now)` PUR (RAF-03, aucun I/O) :
         `FiveHour.Interpolate(now)`; `SevenDay.Interpolate(now)`;
         `IsStale = CapturedAt is { } c && (now - c) > TimeSpan.FromMinutes(2)`.
       - `public void StartClock()` : créer `new System.Windows.Threading.DispatcherTimer { Interval =
         TimeSpan.FromSeconds(1) }`, `timer.Tick += (_, _) => Interpolate(_clock.UtcNow)`, `timer.Start()`.
         NE JAMAIS créer ce timer dans le ctor (Pitfall 4 : garde les tests en [Fact] simple).

    Rappels interdits (RESEARCH) : aucun `IUsageProvider` injecté dans le VM ; aucun `GetAsync`/`await`
    dans un handler Tick ; ne jamais muter une [ObservableProperty] hors du Post (toujours passer par
    IUiDispatcher). Le tick ne fait QUE de l'interpolation (texte + valeurs), pas d'animation continue.

    Note test : si asseoir le test de marshaling sur le vrai RefreshOrchestrator est trop lourd,
    l'exécuteur peut extraire une petite interface/seam OU piloter l'event via FakeUsageProvider
    branché dans un RefreshOrchestrator à intervalle court. Le format exact des tests est à la
    discrétion (RESEARCH) tant que PostCount==1 et GetCount==0-au-tick sont prouvés.
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~MainViewModel|FullyQualifiedName~CountdownFormatter"</automated>
  </verify>
  <acceptance_criteria>
    - RAF-04 : `FakeUiDispatcher.PostCount == 1` après application d'un snapshot hors thread UI ; propriétés à jour.
    - RAF-04 : `DataUnavailable` vrai ssi les deux fenêtres Unavailable.
    - RAF-03 : après `Interpolate(now avancé)`, FractionRemaining décroît, CountdownText change, `FakeUsageProvider.GetCount` inchangé (0 I/O au tick).
    - Les tests du VM sont en `[Fact]` (pas `[WpfFact]`) → preuve que le DispatcherTimer n'est pas dans le ctor.
    - grep `_ui.Post` et `SnapshotChanged +=` présents dans MainViewModel.cs.
  </acceptance_criteria>
  <done>MainViewModel marshalle un snapshot en un point unique (RAF-04) et interpole à la seconde sans I/O (RAF-03) ; WindowGaugeViewModel expose fraction + countdown FR. Tests verts.</done>
</task>

<task type="auto">
  <name>Task 3 : Câblage App.xaml.cs (DI + ordre de démarrage) + StartClock côté MainWindow</name>
  <read_first>
    - .planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-RESEARCH.md (Pattern 2 DI lignes 205-211 ; Pitfall 3 ordre de démarrage lignes 361-373)
    - src/Chronos/App.xaml.cs (OnStartup + ConfigureServices actuels)
    - src/Chronos/Views/MainWindow.xaml.cs (ctor + Loaded)
    - tests/Chronos.Tests/CompositionRootTests.cs (garantir que le graphe DI résout toujours)
  </read_first>
  <files>
    src/Chronos/App.xaml.cs,
    src/Chronos/Views/MainWindow.xaml.cs
  </files>
  <action>
    Glue d'intégration (pas de TDD ; couvert par build + CompositionRootTests + vérification humaine).

    1. `App.xaml.cs` → `ConfigureServices` : ajouter (Pattern 2) —
       ```csharp
       services.AddSingleton(RefreshOptions.Default);
       services.AddSingleton<RefreshOrchestrator>();
       services.AddHostedService(sp => sp.GetRequiredService<RefreshOrchestrator>()); // MÊME instance
       ```
       Le `MainViewModel` (déjà Singleton) prendra `RefreshOrchestrator` + `IUiDispatcher` + `IClock`
       par ctor — tous déjà enregistrés. NE PAS créer une seconde instance d'orchestrateur (le
       AddHostedService résout l'instance Singleton existante).

    2. `App.xaml.cs` → `OnStartup` : corriger l'ORDRE (Pitfall 3). Après `_host = builder.Build();`
       et AVANT `await _host.StartAsync();`, forcer la résolution (donc l'abonnement) du VM :
       ```csharp
       _host = builder.Build();
       _ = _host.Services.GetRequiredService<MainViewModel>();   // force l'abonnement à SnapshotChanged
       await _host.StartAsync();                                 // charge initiale → atteint le VM (marshalé via BeginInvoke)
       var window = _host.Services.GetRequiredService<MainWindow>();
       window.Show();
       ```
       Le snapshot initial part pendant StartAsync mais le VM est déjà abonné ; le Post est mis en
       file (BeginInvoke) et s'appliquera dès que le Dispatcher tourne → aucune InvalidOperationException,
       aucun overlay vide au lancement.

    3. `MainWindow.xaml.cs` : démarrer l'horloge UI côté vue (jamais dans le ctor du VM). Dans le
       ctor, après `DataContext = viewModel;`, ajouter un abonnement Loaded qui appelle StartClock :
       ```csharp
       Loaded += (_, _) => viewModel.StartClock();   // crée le DispatcherTimer 1 s sur le thread UI (RAF-03)
       ```
       Conserver le `Loaded += PlacerCoinSuperieurDroit` existant (les deux handlers Loaded coexistent).
       Garder une référence au `MainViewModel` si nécessaire pour l'appel (le ctor le reçoit déjà).
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test Chronos.sln -c Debug` : suite complète verte (Phases 1-3 + 04-01 + 04-02), CompositionRootTests inclus.
    - grep `GetRequiredService<MainViewModel>()` présent AVANT `StartAsync` dans App.xaml.cs (ordre corrigé).
    - grep `AddHostedService` + `RefreshOrchestrator` présents dans ConfigureServices.
    - grep `StartClock()` présent dans MainWindow.xaml.cs, déclenché par Loaded.
  </acceptance_criteria>
  <done>L'orchestrateur est enregistré (Singleton + IHostedService même instance), le VM est abonné AVANT StartAsync (snapshot initial préservé), et l'horloge UI 1 s démarre au chargement de la fenêtre. Build + suite complète verts.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` : 0 erreur.
- `dotnet test Chronos.sln -c Debug` : suite complète verte (nouveaux CountdownFormatterTests + MainViewModelTests + tous les tests amont).
- Vérification humaine (VALIDATION.md) : lancer l'app, observer le compte à rebours décroître à la seconde (RAF-03) ; une écriture usage.json met à jour l'affichage (RAF-01 bout-en-bout).
</verification>

<success_criteria>
- RAF-03 : DispatcherTimer 1 s (StartClock, côté UI) appelle Interpolate(now) pur ; arcs et compte à rebours FR progressent sans I/O disque.
- RAF-04 : tout franchissement thread pool → UI passe par IUiDispatcher.Post exactement une fois ; aucune InvalidOperationException.
- Ordre de démarrage corrigé : le snapshot initial atteint le VM (pas d'overlay vide au lancement).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-orchestration-refresh-viewmodel-temps-r-el/04-02-SUMMARY.md`
</output>
