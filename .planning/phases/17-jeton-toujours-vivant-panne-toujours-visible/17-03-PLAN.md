---
phase: 17-jeton-toujours-vivant-panne-toujours-visible
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - src/Chronos/Services/ChronosTokenAuthority.cs
  - src/Chronos/Services/TokenRefreshService.cs
  - tests/Chronos.Tests/ChronosTokenAuthorityTests.cs
  - tests/Chronos.Tests/TokenRefreshServiceTests.cs
autonomous: true
requirements: [TOK-01, TOK-02]

must_haves:
  truths:
    - "Un seul objet du processus a le droit d'appeler RefreshAsync et d'écrire dans le coffre : N demandes concurrentes de jeton produisent UNE seule rotation, jamais N."
    - "Le nouveau couple de jetons est persisté AVANT d'être rendu à l'appelant ; si l'écriture disque échoue, les jetons neufs restent en mémoire et la session courante continue de fonctionner."
    - "L'autorité n'efface JAMAIS le coffre : un faux positif serveur ne peut pas détruire le login de l'utilisateur."
    - "Un refus d'identifiants verrouille l'état et coupe tout réessai automatique ; un échec temporaire pose un recul exponentiel plafonné, remis à zéro au premier succès."
    - "La décision de rafraîchir se prend par comparaison d'HORLOGE MURALE à chaque tick, jamais par un réveil calculé : un portable qui dort huit heures rattrape naturellement, et un jeton expiré depuis deux mois est traité au premier tick."
    - "Le service de fond fait un premier tick IMMÉDIAT et ne lève jamais : une exception sur un thread du pool tuerait la boucle, donc le rafraîchissement préventif, donc TOK-01."
    - "L'état n'est publié que sur TRANSITION : pas un événement par minute."
  artifacts:
    - path: "src/Chronos/Services/ChronosTokenAuthority.cs"
      provides: "Autorité UNIQUE de jeton : sémaphore, double-vérification, rotation persistée, classification, recul, état"
      exports: ["GetAccessTokenAsync", "InvaliderAccessToken", "SignalerSucces", "SignalerRefusServeur", "ReinitialiserApresLogin", "Etat", "EtatChange"]
      min_lines: 130
    - path: "src/Chronos/Services/TokenRefreshService.cs"
      provides: "IHostedService à tick fixe 60 s, premier tick immédiat, TickAsync public et déterministe"
      exports: ["StartAsync", "TickAsync", "StopAsync", "Dispose"]
      min_lines: 50
    - path: "tests/Chronos.Tests/ChronosTokenAuthorityTests.cs"
      provides: "Le cœur de preuve de la phase : concurrence, rotation, classification, recul, non-effacement"
      min_lines: 220
    - path: "tests/Chronos.Tests/TokenRefreshServiceTests.cs"
      provides: "Preuve TOK-01 : premier tick immédiat, aucun appel si le jeton est valide, ne lève jamais"
      min_lines: 80
  key_links:
    - from: "src/Chronos/Services/ChronosTokenAuthority.cs"
      to: "SemaphoreSlim(1,1)"
      via: "section critique unique autour de charger/décider/rafraîchir/persister/publier"
      pattern: "SemaphoreSlim\\(1, ?1\\)"
    - from: "src/Chronos/Services/ChronosTokenAuthority.cs"
      to: "ChronosOAuthStore.Save"
      via: "persistance de la rotation AVANT le retour du jeton"
      pattern: "_coffre\\.Save\\("
    - from: "src/Chronos/Services/TokenRefreshService.cs"
      to: "ChronosTokenAuthority.GetAccessTokenAsync"
      via: "tick de fond ; l'autorité décide seule s'il faut rafraîchir"
      pattern: "GetAccessTokenAsync"
    - from: "src/Chronos/Services/TokenRefreshService.cs"
      to: "TimeSpan.Zero"
      via: "dueTime du Timer — premier tick immédiat (motif DesktopUiaPollService.StartAsync:44)"
      pattern: "TimeSpan\\.Zero"
---

<objective>
Créer l'**autorité unique de jeton** et son horloge de fond : le cœur de TOK-01.

Purpose: aujourd'hui le rafraîchissement est *paresseux* — il n'a lieu qu'au moment d'un `GetAsync`,
c'est-à-dire quand quelqu'un regarde le cadran. Ajouter un rafraîchisseur de fond **à côté** de celui-ci
serait pire que le mal : le flux OAuth fait **tourner** le refresh token, deux rafraîchisseurs présentant
le même refresh token produisent un `invalid_grant` sur le second, donc — puisque TOK-02 classera
`invalid_grant` comme « déconnecté » — une **fausse déconnexion sur un compte parfaitement sain**. Ce
n'est pas théorique : c'est le bug ouvert `anthropics/claude-code#25609`, « no coordination (file lock,
mutex, or leader election) during token refresh… a classic refresh token rotation race condition ».
Claude Code lui-même s'y est fait prendre.

La réponse est de **retirer le droit de rafraîchir à tout le monde sauf un**. `ChronosTokenAuthority`
possède le coffre, le client, un `SemaphoreSlim(1,1)`, l'état et la politique de recul. Tous les autres
(le provider d'usage au plan 17-04, la sonde d'en-têtes de la phase 18) deviennent de simples clients de
`GetAccessTokenAsync`, sérialisés par construction.

Output: `ChronosTokenAuthority.cs`, `TokenRefreshService.cs` et leurs deux fichiers de test.

**Aucun câblage DI dans ce plan** : `App.xaml.cs` est modifié au plan 17-04, en même temps que le
rebranchement du provider — un seul commit pour un seul état cohérent du graphe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-VALIDATION.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrats DISPONIBLES après le plan 17-02. À consommer tels quels : ne rien réinventer. -->

```csharp
// src/Chronos/Services/EtatAuthentification.cs (plan 17-02)
public enum EtatAuthentification { NonConnecte, Connecte, HorsLigne, Deconnecte }

// src/Chronos/Services/IAuthStatus.cs (plan 17-02)
public interface IAuthStatus
{
    EtatAuthentification Etat { get; }
    event EventHandler<EtatAuthentification>? EtatChange;
    void ReinitialiserApresLogin();
}

// src/Chronos/Services/ChronosOAuthClient.cs (plan 17-02)
public enum IssueRafraichissement { Succes, IdentifiantsRejetes, EchecTemporaire }
public sealed record ResultatRafraichissement(IssueRafraichissement Issue, OAuthTokens? Jetons);
public sealed record OAuthTokens(string AccessToken, string RefreshToken, DateTimeOffset ExpiresAt);
public sealed class ChronosOAuthClient
{
    public ChronosOAuthClient(HttpClient http, IClock? clock = null);   // horloge injectable => ExpiresAt déterministe
    public Task<ResultatRafraichissement> RefreshAsync(string refreshToken, CancellationToken ct = default);
}

// src/Chronos/Services/ChronosOAuthStore.cs (INCHANGÉ — n'y toucher sous aucun prétexte)
public sealed class ChronosOAuthStore
{
    public ChronosOAuthStore(string? path = null);
    public bool Exists { get; }
    public void Save(OAuthTokens tokens);   // écriture atomique tmp+Move ; peut lever (disque plein, fichier verrouillé)
    public OAuthTokens? Load();             // ne lève jamais
    public void Clear();                    // <- INTERDIT D'APPEL depuis l'autorité
}

// src/Chronos/Services/IClock.cs
public interface IClock { DateTimeOffset UtcNow { get; } }
```

**Motif de service de fond à COPIER** — `src/Chronos/Services/DesktopUiaPollService.cs` (86 lignes,
lire intégralement). Points saillants :

```csharp
public sealed class DesktopUiaPollService : IHostedService, IDisposable
{
    private static readonly TimeSpan Period = TimeSpan.FromSeconds(1.5);
    private readonly object _gate = new();
    private Timer? _timer;
    private bool _disposed;

    public Task StartAsync(CancellationToken cancellationToken)
    {
        lock (_gate)
        {
            if (_disposed) return Task.CompletedTask;
            // dueTime 0 : un premier poll immédiat pour peupler le cache dès le démarrage.
            _timer ??= new Timer(_ => PollOnce(), null, TimeSpan.Zero, Period);
        }
        return Task.CompletedTask;
    }

    /// <summary>PUBLIC pour test déterministe. Ne LÈVE JAMAIS.</summary>
    public void PollOnce() { try { _source.Poll(_clock.UtcNow); } catch { } }

    public Task StopAsync(CancellationToken ct) { lock (_gate) { _timer?.Dispose(); _timer = null; } return Task.CompletedTask; }
    public void Dispose() { lock (_gate) { _disposed = true; _timer?.Dispose(); _timer = null; } }
}
```

**Faux disponibles** : `FakeHttpMessageHandler` (`.Json(status, body)`, `.Throws(ex)`, `SendCount`,
`LastRequest`), `FakeClock` (`UtcNow` settable), `FakeAuthStatus` (plan 17-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChronosTokenAuthority — un seul rafraîchisseur, une rotation persistée, une cause honnête</name>

  <read_first>
    - src/Chronos/Services/ChronosOAuthClient.cs (tel que livré par 17-02, en particulier RefreshAsync)
    - src/Chronos/Services/ChronosOAuthStore.cs (intégralement)
    - src/Chronos/Services/IAuthStatus.cs et EtatAuthentification.cs (plan 17-02)
    - src/Chronos/Services/RefreshOrchestrator.cs (lignes 25-45 — motif d'événement neutre)
    - .planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md
      (§ Pattern 1 invariants 1-6, § Pattern 3 table de décision, § Pitfalls 4 et 5, § Code Example 1)
  </read_first>

  <files>
src/Chronos/Services/ChronosTokenAuthority.cs
tests/Chronos.Tests/ChronosTokenAuthorityTests.cs
  </files>

  <behavior>
    - **Prédicat pur** `DoitRafraichir(expiresAt, now, marge)` : `null` → vrai ; `now + marge` avant
      l'expiration → faux ; `now + marge` exactement à l'expiration → vrai ; expiration deux mois dans
      le passé → vrai. Quatre cas en `[Theory]`, aucun I/O.
    - Coffre vide → `GetAccessTokenAsync` rend `null`, `Etat == NonConnecte`, `handler.SendCount == 0`
      (rien à rafraîchir, ce n'est pas une panne).
    - Jeton encore valide (expiration à `now + 1 h`, marge 12 min) → rend l'access token du coffre,
      `handler.SendCount == 0` (**aucun appel réseau**), `Etat == Connecte`.
    - Jeton expiré → un refresh 200 → rend le **nouvel** access token, `Etat == Connecte`.
    - **Rotation persistée** : après ce refresh, un `new ChronosOAuthStore(memeChemin).Load()!.RefreshToken`
      vaut `"REF-2"` (le NOUVEAU) — la persistance a lieu avant le retour.
    - **Échec d'écriture non fatal** : si `Save` lève, `GetAccessTokenAsync` rend quand même le nouvel
      access token et `Etat == Connecte` (la session courante survit ; l'ancien refresh est déjà mort
      côté serveur).
    - **CONCURRENCE — le test le plus important de la phase** : 10 appels parallèles
      (`Task.WhenAll`) sur un jeton expiré → `handlerJeton.SendCount == 1`, et les 10 résultats sont
      identiques et non nuls.
    - Classification : 400 `invalid_grant` → `Deconnecte` + `null` ; 401 → `Deconnecte` ;
      **429 → `HorsLigne`** ; 500 → `HorsLigne` ; `HttpRequestException` → `HorsLigne` ;
      `TaskCanceledException` → `HorsLigne` ; 200 tronqué → `Deconnecte`.
    - **Le coffre n'est JAMAIS effacé** : après un 400 `invalid_grant`, `File.Exists(cheminCoffre)` est
      toujours vrai et `Load()` rend encore les jetons d'origine.
    - **Verrou définitif** : après un 400 `invalid_grant`, cinq appels supplémentaires à
      `GetAccessTokenAsync` rendent `null` et `handler.SendCount` reste à **1** (aucun réessai
      automatique sur l'irréparable).
    - **Recul exponentiel** : après un 500, un appel immédiat (même instant d'horloge) rend `null` sans
      requête (`SendCount` reste 1) ; en avançant l'horloge de 3 min (> 2 min de recul initial), un
      nouvel appel émet une requête ; après un second échec le recul double (4 min) ; il est plafonné à
      30 min ; un succès le remet à 2 min.
    - **Événement sur TRANSITION seulement** : trois échecs 500 consécutifs (horloge avancée entre
      chacun) produisent **un seul** `EtatChange` vers `HorsLigne`, pas trois.
    - `InvaliderAccessToken()` : un jeton pourtant valide est rafraîchi au prochain
      `GetAccessTokenAsync` (`SendCount` passe de 0 à 1).
    - `ReinitialiserApresLogin()` après un `Deconnecte` : relâche le verrou ET le recul ; le prochain
      `GetAccessTokenAsync` relit le coffre (jetons réécrits par le login) et rafraîchit à nouveau.
    - `SignalerSucces()` publie `Connecte` même après `Deconnecte` (critère de succès 3 : la pastille
      disparaît dès qu'un chiffre exact est de nouveau obtenu). `SignalerRefusServeur()` publie
      `Deconnecte`.
    - **Sécurité** : `GetAccessTokenAsync` est la seule sortie de jeton, et elle ne rend qu'un `string`
      d'access token — jamais un `OAuthTokens`, jamais le refresh token.
  </behavior>

  <action>
1. Créer `src/Chronos/Services/ChronosTokenAuthority.cs`. Type **neutre** (aucun `using System.Windows.*`),
   `public sealed class ChronosTokenAuthority : IAuthStatus, IDisposable`. XML-doc et commentaires
   **en français**. Constantes :

```csharp
    /// <summary>Marge de rafraîchissement préventif. STRICTEMENT supérieure à la période de tick du
    /// service de fond (60 s) : c'est ce qui garantit que le préventif gagne toujours la course contre
    /// un chemin paresseux — définition même de « préventif ». Volontairement large : la durée de vie
    /// réelle de l'access token Claude Code n'est pas documentée et ne doit être codée en dur nulle part.</summary>
    private static readonly TimeSpan Marge = TimeSpan.FromMinutes(12);

    /// <summary>Recul initial après un échec TEMPORAIRE. Hors ligne, un tick de 60 s tenterait 1 440
    /// rafraîchissements par jour et transformerait une panne bénigne en 429 (constaté en direct).</summary>
    private static readonly TimeSpan ReculInitial = TimeSpan.FromMinutes(2);
    private static readonly TimeSpan ReculMax = TimeSpan.FromMinutes(30);
```

2. Champs et constructeur :

```csharp
    private readonly SemaphoreSlim _verrou = new(1, 1);
    private readonly ChronosOAuthStore _coffre;
    private readonly ChronosOAuthClient _client;
    private readonly IClock _horloge;

    private OAuthTokens? _jetons;          // copie mémoire : survit à un Save en échec
    private bool _forcerRafraichissement;  // posé par InvaliderAccessToken() après un 401 sur l'usage
    private bool _refusDefinitif;          // VERROU : identifiants rejetés => plus AUCUN réessai auto
    private DateTimeOffset _prochainEssai; // fenêtre de recul (échecs temporaires)
    private TimeSpan _recul = ReculInitial;

    public EtatAuthentification Etat { get; private set; } = EtatAuthentification.NonConnecte;
    public event EventHandler<EtatAuthentification>? EtatChange;   // émis sur un thread du POOL

    public ChronosTokenAuthority(ChronosOAuthStore coffre, ChronosOAuthClient client, IClock horloge)
    {
        _coffre = coffre;
        _client = client;
        _horloge = horloge;
    }
```

3. `GetAccessTokenAsync` — **toute** la séquence (charger → décider → rafraîchir → persister → publier)
   est sous le sémaphore. Ne jamais utiliser `lock` autour d'un `await` (CS1996) :

```csharp
    /// <summary>
    /// Rend un access token VALIDE, en rafraîchissant si nécessaire ; null si indisponible.
    /// SÉCURITÉ : c'est la SEULE sortie d'un jeton hors de cette classe, et jamais le refresh token.
    /// Le refresh token ne quitte jamais l'autorité autrement que vers ChronosOAuthClient.RefreshAsync.
    /// </summary>
    public async Task<string?> GetAccessTokenAsync(CancellationToken ct = default)
    {
        await _verrou.WaitAsync(ct);
        try
        {
            var now = _horloge.UtcNow;
            _jetons ??= _coffre.Load();
            if (_jetons is null) { Publier(EtatAuthentification.NonConnecte); return null; }

            // VERROU DÉFINITIF : le serveur a refusé les identifiants. Seul un login répare ; réessayer
            // ne ferait que consommer du rate-limit sur le point de terminaison de jeton.
            if (_refusDefinitif) { Publier(EtatAuthentification.Deconnecte); return null; }

            // DOUBLE-VÉRIFICATION : un appelant concurrent vient peut-être de rafraîchir pendant
            // l'attente du sémaphore. Sans elle, N appelants en attente déclenchent N rotations en
            // série, donc N-1 invalid_grant — une fausse déconnexion fabriquée par nous-mêmes.
            if (!_forcerRafraichissement && !DoitRafraichir(_jetons.ExpiresAt, now, Marge))
            {
                Publier(EtatAuthentification.Connecte);
                return _jetons.AccessToken;
            }

            // Fenêtre de recul : ne jamais marteler le point de terminaison.
            if (now < _prochainEssai) return null;

            var res = await _client.RefreshAsync(_jetons.RefreshToken, ct);
            switch (res.Issue)
            {
                case IssueRafraichissement.Succes:
                    _jetons = res.Jetons!;
                    // ROTATION : persister AVANT de rendre le jeton. Un Save en échec ne doit PAS faire
                    // croire à un échec de refresh : les jetons neufs restent en mémoire, la session
                    // courante continue — l'ancien refresh token est de toute façon déjà mort côté serveur.
                    try { _coffre.Save(_jetons); } catch { /* dégradation : session courante préservée */ }
                    _forcerRafraichissement = false;
                    _recul = ReculInitial;
                    _prochainEssai = default;
                    Publier(EtatAuthentification.Connecte);
                    return _jetons.AccessToken;

                case IssueRafraichissement.IdentifiantsRejetes:
                    // AUCUN réessai automatique. Et surtout AUCUN _coffre.Clear() : un faux positif
                    // serveur (cas documenté claude-code#54443) effacerait définitivement un login
                    // parfaitement récupérable. On attend un login utilisateur.
                    _refusDefinitif = true;
                    Publier(EtatAuthentification.Deconnecte);
                    return null;

                default: // EchecTemporaire : réseau, timeout, 429, 5xx
                    _prochainEssai = now + _recul;
                    _recul = _recul < ReculMax ? Doubler(_recul) : ReculMax;
                    Publier(EtatAuthentification.HorsLigne);
                    return null;
            }
        }
        finally { _verrou.Release(); }
    }

    private static TimeSpan Doubler(TimeSpan t) => t + t > ReculMax ? ReculMax : t + t;
```

4. Les quatre méthodes de pilotage + le prédicat pur + la publication sur transition :

```csharp
    /// <summary>Après un 401 sur l'endpoint d'usage : le prochain appel forcera un rafraîchissement.
    /// Le serveur peut révoquer un jeton AVANT son ExpiresAt local (cas documenté) — le préventif seul
    /// ne suffit donc pas.</summary>
    public void InvaliderAccessToken() => _forcerRafraichissement = true;

    /// <summary>TOK-03 — après un login réussi : relâche le verrou « Deconnecte » ET le recul, et
    /// oublie les jetons mémorisés pour relire le coffre que le login vient de réécrire. Sans cet
    /// appel, le jeton tout neuf ne serait pas utilisé et la pastille survivrait à sa réparation.</summary>
    public void ReinitialiserApresLogin()
    {
        _jetons = null;
        _forcerRafraichissement = false;
        _refusDefinitif = false;
        _recul = ReculInitial;
        _prochainEssai = default;
        Publier(EtatAuthentification.NonConnecte);   // sera relevé au premier GetAccessTokenAsync
    }

    /// <summary>Un 2xx exploité déverrouille l'état : la pastille disparaît dès qu'un chiffre exact est
    /// de nouveau obtenu (critère de succès 3 de la phase).</summary>
    public void SignalerSucces()
    {
        _refusDefinitif = false;
        _recul = ReculInitial;
        _prochainEssai = default;
        Publier(EtatAuthentification.Connecte);
    }

    /// <summary>Le serveur a refusé un access token FRAÎCHEMENT rafraîchi (401/403 sur l'usage après
    /// rejeu) : ce n'est pas un problème de fraîcheur, c'est un refus de compte.</summary>
    public void SignalerRefusServeur()
    {
        _refusDefinitif = true;
        Publier(EtatAuthentification.Deconnecte);
    }

    /// <summary>PUR, aucun I/O. Comparaison d'HORLOGE MURALE et non de temps écoulé : un portable qui
    /// dort huit heures rattrape naturellement au tick suivant, et un jeton expiré depuis deux mois
    /// est traité au premier tick. Un réveil calculé (Task.Delay jusqu'à ExpiresAt) casse à la veille.</summary>
    internal static bool DoitRafraichir(DateTimeOffset? expiresAt, DateTimeOffset now, TimeSpan marge)
        => expiresAt is null || expiresAt.Value - marge <= now;

    // N'émettre que sur TRANSITION : sinon un événement par tick, soit 1 440 par jour pour rien.
    private void Publier(EtatAuthentification e)
    {
        if (Etat == e) return;
        Etat = e;
        EtatChange?.Invoke(this, e);
    }

    public void Dispose() => _verrou.Dispose();
```

   `DoitRafraichir` est `internal` : `InternalsVisibleTo` est déjà en place pour le projet de tests
   (motif « seams de test internes » de `RefreshOrchestrator.cs:100`). Vérifier sa présence dans
   `src/Chronos/Chronos.csproj` ou un `AssemblyInfo` ; si elle manque, rendre la méthode `public` plutôt
   que d'ajouter un attribut.

5. Créer `tests/Chronos.Tests/ChronosTokenAuthorityTests.cs`, classe publique, **sans**
   `[Collection("XAML WPF")]` (aucun BAML). Helpers :

```csharp
    private static readonly DateTimeOffset Maintenant = new(2026, 9, 9, 12, 0, 0, TimeSpan.Zero);

    private const string CorpsRefresh200 = """{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}""";

    // SÉCURITÉ : coffre TOUJOURS sous %TEMP%. Le refresh token réel de %APPDATA%\Chronos\oauth.dat
    // n'est ni lu, ni déchiffré, ni envoyé : l'endpoint le FAIT TOURNER, un appel dont le résultat
    // n'est pas re-sauvegardé invaliderait définitivement le login de l'utilisateur.
    private static string CheminCoffreTemp()
    {
        var dir = Path.Combine(Path.GetTempPath(), "ChronosAutoriteTest_" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(dir);
        var chemin = Path.Combine(dir, "oauth.dat");
        Assert.StartsWith(Path.GetTempPath(), chemin);
        return chemin;
    }

    private static (ChronosTokenAuthority autorite, FakeHttpMessageHandler handler, FakeClock horloge, string chemin)
        Autorite(DateTimeOffset? expiration, FakeHttpMessageHandler? handler = null)
    {
        var h = handler ?? FakeHttpMessageHandler.Json(HttpStatusCode.OK, CorpsRefresh200);
        var horloge = new FakeClock(Maintenant);
        var chemin = CheminCoffreTemp();
        var coffre = new ChronosOAuthStore(chemin);
        if (expiration is { } e) coffre.Save(new OAuthTokens("ACC-1", "REF-1", e));
        var autorite = new ChronosTokenAuthority(coffre, new ChronosOAuthClient(new HttpClient(h), horloge), horloge);
        return (autorite, h, horloge, chemin);
    }
```

6. Écrire les tests d'après `<behavior>`. Formes obligatoires pour les trois plus critiques :

```csharp
    /// <summary>
    /// LE test de la phase. Deux rafraîchissements concurrents présentant le MÊME refresh token
    /// produisent un invalid_grant sur le second — donc, avec la table TOK-02, une FAUSSE déconnexion
    /// sur un compte parfaitement sain (bug claude-code#25609). Le sémaphore + la double-vérification
    /// rendent cette situation structurellement impossible dans le processus.
    /// </summary>
    [Fact]
    public async Task Dix_demandes_concurrentes_ne_declenchent_QU_UNE_rotation()
    {
        var (autorite, handler, _, _) = Autorite(Maintenant.AddMinutes(-1));   // jeton expiré

        var resultats = await Task.WhenAll(Enumerable.Range(0, 10)
            .Select(_ => autorite.GetAccessTokenAsync()));

        Assert.Equal(1, handler.SendCount);                    // UNE seule rotation
        Assert.All(resultats, r => Assert.Equal("ACC-2", r));  // tous servis par le même jeton frais
    }

    /// <summary>Pitfall 4 : un faux positif serveur ne doit JAMAIS détruire un login récupérable.</summary>
    [Fact]
    public async Task Un_refus_d_identifiants_n_efface_JAMAIS_le_coffre()
    {
        var (autorite, _, _, chemin) = Autorite(Maintenant.AddMinutes(-1),
            FakeHttpMessageHandler.Json(HttpStatusCode.BadRequest, """{"error":"invalid_grant"}"""));

        Assert.Null(await autorite.GetAccessTokenAsync());
        Assert.Equal(EtatAuthentification.Deconnecte, autorite.Etat);
        Assert.True(File.Exists(chemin));
        Assert.Equal("REF-1", new ChronosOAuthStore(chemin).Load()!.RefreshToken);
    }

    [Fact]
    public async Task Une_transition_n_emet_qu_UN_evenement_meme_apres_trois_echecs()
    {
        var (autorite, _, horloge, _) = Autorite(Maintenant.AddMinutes(-1),
            FakeHttpMessageHandler.Json(HttpStatusCode.InternalServerError, ""));
        var emis = new List<EtatAuthentification>();
        autorite.EtatChange += (_, e) => emis.Add(e);

        for (var i = 0; i < 3; i++)
        {
            await autorite.GetAccessTokenAsync();
            horloge.UtcNow = horloge.UtcNow.AddMinutes(40);   // dépasse tout recul
        }

        Assert.Single(emis);
        Assert.Equal(EtatAuthentification.HorsLigne, emis[0]);
    }
```

   Le test d'échec d'écriture utilise un chemin verrouillé : créer un **dossier** au chemin du coffre
   (`Directory.CreateDirectory(chemin)`) après avoir enregistré les jetons via un autre chemin, ou plus
   simplement construire l'autorité avec un `ChronosOAuthStore` dont le chemin pointe un fichier
   ouvert en écriture exclusive par un `FileStream` maintenu pendant l'appel. Choisir la variante qui
   fait effectivement lever `Save` sur la machine, et asserter que `GetAccessTokenAsync` rend
   quand même `"ACC-2"` et publie `Connecte`.

**INTERDITS de cette tâche :** aucun `.Clear()` dans `ChronosTokenAuthority.cs`. Aucun `lock` autour
d'un `await`. Aucun `Task.Delay` fondé sur `ExpiresAt`. Aucun `DateTimeOffset.UtcNow` en dur (toujours
`_horloge.UtcNow`). Aucun `using System.Windows.*`. Aucun câblage DI. Ne pas toucher à
`ChronosOAuthStore.cs`, `ChronosOAuthUsageProvider.cs`, `App.xaml.cs`. Aucun test ne lit
`%APPDATA%\Chronos\oauth.dat`.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo && dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosTokenAuthorityTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -v q --nologo` → **0 erreur**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosTokenAuthorityTests"` →
      **0 échec**, **≥ 18** tests exécutés (le `[Theory]` du prédicat comptant pour 4).
    - `grep -c "\.Clear()" src/Chronos/Services/ChronosTokenAuthority.cs` → **0**.
    - `grep -c "SemaphoreSlim(1, 1)\|SemaphoreSlim(1,1)" src/Chronos/Services/ChronosTokenAuthority.cs` → **1**.
    - `grep -c "_verrou.WaitAsync" src/Chronos/Services/ChronosTokenAuthority.cs` → **1**
      (section critique UNIQUE) ; `grep -c "_verrou.Release" ` → **1**.
    - `grep -c "DateTimeOffset.UtcNow" src/Chronos/Services/ChronosTokenAuthority.cs` → **0**
      (l'horloge est toujours injectée).
    - `grep -cE "Task\.Delay|PeriodicTimer" src/Chronos/Services/ChronosTokenAuthority.cs` → **0**.
    - `grep -n "System.Windows" src/Chronos/Services/ChronosTokenAuthority.cs` → **aucun résultat**.
    - `grep -c "Task<string?> GetAccessTokenAsync" src/Chronos/Services/ChronosTokenAuthority.cs` → **1**
      (aucune méthode publique ne rend un `OAuthTokens`) ; `grep -c "public .*OAuthTokens" src/Chronos/Services/ChronosTokenAuthority.cs` → **0**.
    - `grep -c "Dix_demandes_concurrentes_ne_declenchent_QU_UNE_rotation" tests/Chronos.Tests/ChronosTokenAuthorityTests.cs` → **1**.
    - `grep -c "Un_refus_d_identifiants_n_efface_JAMAIS_le_coffre" tests/Chronos.Tests/ChronosTokenAuthorityTests.cs` → **1**.
    - `grep -n "new ChronosOAuthStore()" tests/Chronos.Tests/ChronosTokenAuthorityTests.cs` → **aucun résultat**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests"` → **vert**.
  </acceptance_criteria>

  <done>
Un seul objet du processus peut rafraîchir ; dix demandes concurrentes ne produisent qu'une rotation ;
la rotation est persistée avant d'être rendue ; le coffre n'est jamais effacé ; un 429 ne dit jamais
« déconnecté » ; l'état ne s'émet que sur transition.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TokenRefreshService — l'horloge de fond qui rend le rafraîchissement PRÉVENTIF</name>

  <read_first>
    - src/Chronos/Services/DesktopUiaPollService.cs (intégralement, 86 lignes — motif à copier)
    - tests/Chronos.Tests/DesktopUiaPollServiceTests.cs (intégralement — motif de test d'IHostedService)
    - src/Chronos/Services/ChronosTokenAuthority.cs (livré par la tâche 1)
    - .planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md (§ Pattern 2, § Pitfall 1)
  </read_first>

  <files>
src/Chronos/Services/TokenRefreshService.cs
tests/Chronos.Tests/TokenRefreshServiceTests.cs
  </files>

  <behavior>
    - Jeton **déjà expiré** dans le coffre → un seul `TickAsync()` déclenche exactement **un**
      rafraîchissement (`handler.SendCount == 1`) et l'autorité passe à `Connecte`.
    - Jeton **encore valide** (expiration à `now + 1 h`) → `TickAsync()` n'émet **aucune** requête
      (`handler.SendCount == 0`) : l'autorité décide seule, le service ne fait que la solliciter.
    - **Premier tick immédiat** : après `StartAsync(CancellationToken.None)` sur un jeton expiré, une
      requête part **sans attendre 60 s** — vérifié par sondage borné (≤ 2 s) du `SendCount`, motif
      `WaitUntilAsync` déjà présent dans le dépôt. C'est le cas réel de l'utilisateur : un jeton mort
      depuis deux mois ; attendre 60 s afficherait une pastille de déconnexion transitoire au lancement.
    - **Ne lève JAMAIS** : avec un transport qui lève une exception non gérée, `TickAsync()` se termine
      normalement (`Record.ExceptionAsync` → `null`). Une exception sur un thread du pool tuerait la
      boucle de fond, donc le rafraîchissement préventif, donc TOK-01.
    - `StopAsync` puis `StartAsync` puis `StopAsync` : aucune exception, idempotent.
    - `Dispose()` appelé deux fois : aucune exception.
    - Après `Dispose()`, `StartAsync` ne recrée pas de minuteur.
  </behavior>

  <action>
1. Créer `src/Chronos/Services/TokenRefreshService.cs`, calqué **structurellement** sur
   `DesktopUiaPollService` (même `lock (_gate)`, même champ `_disposed`, même idempotence) :

```csharp
using System;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.Extensions.Hosting;

namespace Chronos.Services;

/// <summary>
/// Horloge de fond du JETON (TOK-01). Sollicite <see cref="ChronosTokenAuthority.GetAccessTokenAsync"/>
/// à cadence fixe ; c'est l'autorité — et elle seule — qui décide s'il faut réellement rafraîchir.
///
/// Tick FIXE + prédicat d'horloge murale plutôt que réveil calculé : un portable qui dort huit heures
/// ferait mentir tout minuteur fondé sur le temps écoulé, alors qu'une comparaison
/// « expiration − marge ≤ maintenant » rattrape naturellement la veille, les changements d'heure et
/// une expiration vieille de deux mois.
///
/// Type NEUTRE : aucun type WPF. Le <see cref="Timer"/> .NET exécute son rappel sur un thread du POOL,
/// donc par construction hors thread UI.
/// </summary>
public sealed class TokenRefreshService : IHostedService, IDisposable
{
    /// <summary>Période du tick. Doit rester STRICTEMENT inférieure à la marge de l'autorité (12 min),
    /// sans quoi le rafraîchissement cesserait d'être préventif.</summary>
    private static readonly TimeSpan Periode = TimeSpan.FromSeconds(60);

    private readonly ChronosTokenAuthority _autorite;
    private readonly object _gate = new();

    private Timer? _timer;
    private bool _disposed;

    public TokenRefreshService(ChronosTokenAuthority autorite)
        => _autorite = autorite ?? throw new ArgumentNullException(nameof(autorite));

    public Task StartAsync(CancellationToken cancellationToken)
    {
        lock (_gate)
        {
            if (_disposed) return Task.CompletedTask;
            // dueTime ZÉRO : premier tick IMMÉDIAT. Le cas réel de l'utilisateur est un jeton déjà
            // expiré (2026-07-12) ; attendre 60 s afficherait une pastille de déconnexion transitoire
            // au lancement — exactement le faux signal que TOK-02 doit éviter.
            _timer ??= new Timer(_ => _ = TickAsync(), null, TimeSpan.Zero, Periode);
        }
        return Task.CompletedTask;
    }

    /// <summary>
    /// Un tick. PUBLIC pour permettre un test déterministe sans attendre le minuteur.
    /// Ne LÈVE JAMAIS : une exception non gérée sur un thread du pool tuerait la boucle de fond,
    /// donc le rafraîchissement préventif, donc TOK-01.
    /// </summary>
    public async Task TickAsync()
    {
        try
        {
            await _autorite.GetAccessTokenAsync();   // l'autorité décide seule s'il faut rafraîchir
        }
        catch
        {
            // Dégradation silencieuse : un tick raté ne doit pas interrompre la boucle.
        }
    }

    public Task StopAsync(CancellationToken cancellationToken)
    {
        lock (_gate) { _timer?.Dispose(); _timer = null; }
        return Task.CompletedTask;
    }

    public void Dispose()
    {
        lock (_gate) { _disposed = true; _timer?.Dispose(); _timer = null; }
    }
}
```

2. Créer `tests/Chronos.Tests/TokenRefreshServiceTests.cs`, classe publique, **sans**
   `[Collection("XAML WPF")]`. Réutiliser le helper `Autorite(...)` de la tâche 1 en le recopiant
   localement (ou l'extraire dans un helper partagé si l'exécuteur le préfère — dans ce cas, le placer
   dans le fichier de test de l'autorité et le rendre `internal static`).

   Le test du premier tick immédiat, avec sondage borné (motif du dépôt, jamais de `Thread.Sleep` nu) :

```csharp
    private static async Task<bool> AttendreAsync(Func<bool> condition, int msMax = 2000)
    {
        var fin = DateTime.UtcNow.AddMilliseconds(msMax);
        while (DateTime.UtcNow < fin)
        {
            if (condition()) return true;
            await Task.Delay(20);
        }
        return condition();
    }

    /// <summary>
    /// TOK-01 — cas RÉEL de l'utilisateur : jeton expiré depuis deux mois au démarrage de l'exe.
    /// Le premier tick doit partir IMMÉDIATEMENT (dueTime zéro), pas au bout de 60 s.
    /// </summary>
    [Fact]
    public async Task Un_jeton_deja_expire_est_rafraichi_des_le_demarrage_sans_attendre_le_tick()
    {
        var (autorite, handler, _, _) = Autorite(Maintenant.AddMonths(-2));   // expiré depuis deux mois
        using var service = new TokenRefreshService(autorite);

        await service.StartAsync(CancellationToken.None);
        try
        {
            Assert.True(await AttendreAsync(() => handler.SendCount == 1),
                "le premier tick doit être immédiat (dueTime zéro)");
            Assert.Equal(EtatAuthentification.Connecte, autorite.Etat);
        }
        finally { await service.StopAsync(CancellationToken.None); }
    }
```

   Et le test de non-levée :

```csharp
    [Fact]
    public async Task Un_tick_ne_leve_JAMAIS_meme_si_le_transport_explose()
    {
        var (autorite, _, _, _) = Autorite(Maintenant.AddMinutes(-1),
            FakeHttpMessageHandler.Throws(new InvalidOperationException("transport en vrac")));
        using var service = new TokenRefreshService(autorite);

        var ex = await Record.ExceptionAsync(() => service.TickAsync());

        Assert.Null(ex);
    }
```

   Note : `ChronosOAuthClient.RefreshAsync` filtre déjà les exceptions attendues ;
   `InvalidOperationException` n'en fait **pas** partie et remonte donc jusqu'au service — c'est
   précisément ce que ce test doit prouver que le service absorbe.

**INTERDITS de cette tâche :** ne PAS étendre `RefreshOrchestrator` (c'est l'horloge DONNÉES : lui
greffer le jeton lui donnerait deux raisons de changer et casserait `RefreshOrchestratorTests`).
Ne PAS utiliser `PeriodicTimer.Period` en cadence adaptative. Ne PAS calculer de `dueTime` à partir
d'`ExpiresAt`. Aucun câblage DI ici. Aucun `Thread.Sleep` nu dans les tests.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo && dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~TokenRefreshServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -v q --nologo` → **0 erreur**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~TokenRefreshServiceTests"` →
      **0 échec**, **≥ 6** tests exécutés.
    - `grep -c "TimeSpan.Zero" src/Chronos/Services/TokenRefreshService.cs` → **1** (premier tick immédiat).
    - `grep -c "TimeSpan.FromSeconds(60)" src/Chronos/Services/TokenRefreshService.cs` → **1**.
    - `grep -c "public async Task TickAsync" src/Chronos/Services/TokenRefreshService.cs` → **1**
      (méthode publique et déterministe pour les tests).
    - `grep -c "IHostedService" src/Chronos/Services/TokenRefreshService.cs` → **1**.
    - `grep -n "System.Windows\|PeriodicTimer" src/Chronos/Services/TokenRefreshService.cs` → **aucun résultat**.
    - `git diff --name-only -- src/Chronos/Services/RefreshOrchestrator.cs src/Chronos/App.xaml.cs` → **vide**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~RefreshOrchestratorTests"` → **vert**.
  </acceptance_criteria>

  <done>
Le rafraîchissement est PRÉVENTIF : une horloge de fond sollicite l'autorité toutes les 60 s, dès le
démarrage, et un jeton expiré depuis deux mois est renouvelé sans qu'aucun utilisateur n'ait regardé
le cadran. Le service ne peut pas mourir d'une exception.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln -v q --nologo` → **0 erreur**.
2. Suite complète : `dotnet test Chronos.sln -v q --nologo` → **0 échec**, total **≥ 465**
   (baseline de fin de 17-02 + ≥ 24 nouveaux). Aucun test supprimé.
3. Pureté : `--filter "FullyQualifiedName~ServicesLayerPurityTests"` → **vert**, fichier non modifié.
4. Composition : `--filter "FullyQualifiedName~CompositionRootTests"` → **vert** (aucun câblage n'a
   encore bougé — c'est le plan 17-04 qui l'ouvre).
5. Non-régression des voisins : `--filter "FullyQualifiedName~RefreshOrchestratorTests|FullyQualifiedName~DesktopUiaPollServiceTests|FullyQualifiedName~ChronosOAuthStoreTests"` → **vert**.
6. Aucun effacement de coffre nulle part dans le nouveau code :
   `grep -rn "\.Clear()" src/Chronos/Services/ChronosTokenAuthority.cs src/Chronos/Services/TokenRefreshService.cs`
   → **aucun résultat**.
7. Aucun jeton en clair :
   `grep -rniE "accessToken|refreshToken" src/Chronos --include=*.cs | grep -iE "log|Console|WriteLine|Append"`
   → **aucun résultat**.
8. Le coffre réel est intact : `ls -l "$APPDATA/Chronos/oauth.dat"` avant/après la suite → identique.
9. Aucun paquet ajouté : `git diff -- src/Chronos/Chronos.csproj tests/Chronos.Tests/Chronos.Tests.csproj` → **vide**.
</verification>

<success_criteria>
- `ChronosTokenAuthority` existe, implémente `IAuthStatus` et `IDisposable`, sérialise par
  `SemaphoreSlim(1,1)` avec double-vérification, persiste la rotation avant de rendre le jeton,
  n'appelle jamais `Clear()`, verrouille sur refus d'identifiants et recule exponentiellement sur
  échec temporaire.
- Le test de concurrence (10 appels → 1 rotation) est vert.
- `TokenRefreshService` est un `IHostedService` à tick fixe 60 s, premier tick immédiat, `TickAsync`
  public, et ne lève jamais.
- Aucun câblage DI n'a été touché ; les gardes de pureté et de composition sont vertes.
- Aucun test supprimé, suite complète verte.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-03-SUMMARY.md`.
</output>
