---
phase: 17-jeton-toujours-vivant-panne-toujours-visible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Chronos.Tests/ChronosOAuthClientTests.cs
  - tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs
autonomous: true
requirements: [TOK-01, TOK-02]

must_haves:
  truths:
    - "Le chemin d'appel de GET /api/oauth/usage est couvert par des tests AVANT d'être réécrit : en-têtes envoyés, unité 0..100 → 0..1, resets_at ISO, 429 → recul, 5xx → cache, exception réseau → aucune remontée."
    - "Les quatre modes d'échec réels du refresh (400 invalid_grant, 429 rate_limit_error, 200 à corps tronqué, exception réseau) sont exécutés au moins une fois, et le test NOMME le défaut : ils se confondent tous en un unique null."
    - "Le 401 sur l'endpoint d'usage est prouvé MUET aujourd'hui : aucun signal, aucun état, aucune trace."
    - "Aucun test ne joint le réseau réel et aucun n'ouvre le coffre réel : tout passe par FakeHttpMessageHandler et un ChronosOAuthStore sur un chemin issu de Path.GetTempPath()."
  artifacts:
    - path: "tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs"
      provides: "Première couverture dédiée du provider central de la phase (aucune n'existait)"
      min_lines: 180
      contains: "ChronosOAuthUsageProviderTests"
    - path: "tests/Chronos.Tests/ChronosOAuthClientTests.cs"
      provides: "Couverture de RefreshAsync (jamais testé) sur sa signature ACTUELLE"
      contains: "RefreshAsync"
  key_links:
    - from: "tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs"
      to: "FakeHttpMessageHandler"
      via: "aucun HttpClient réel n'est construit sans handler"
      pattern: "new HttpClient\\("
    - from: "tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs"
      to: "Path.GetTempPath()"
      via: "coffre de test isolé + garde anti-accident"
      pattern: "GetTempPath"
---

<objective>
**Wave 0 de la phase 17 : poser la couverture AVANT de toucher au code.** Deux zones vont être réécrites
en profondeur par les plans 17-02 et 17-04, et aucune des deux n'a de test aujourd'hui :
`ChronosOAuthUsageProvider` n'a **aucun fichier de test dédié** (vérifié : le dossier de tests contient
`ClaudeOAuthUsageProviderTests` et `GatedOAuthUsageProviderTests`, mais pas celui-là) et
`ChronosOAuthClient.RefreshAsync` n'est **pas testé** (les 4 tests existants ne couvrent que `CreatePkce`,
`BuildAuthorizeUrl` et `SplitCodeState`).

Purpose: on s'apprête à modifier du code non testé sur le chemin le plus sensible du produit — celui qui
manipule un refresh token à rotation. Sans filet, une régression silencieuse sur cette chaîne se traduit
par exactement le symptôme que la phase 17 doit éradiquer : une source exacte morte, sans un mot.
Ce plan ne modifie **aucun fichier de production**.

Output: `tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` (créé) et `ChronosOAuthClientTests.cs`
(étendu). Aucune modification sous `src/`.

**Deux tests documentent volontairement un DÉFAUT** (`Toutes_les_causes_d_echec_se_confondent_en_un_seul_null`
et `Un_401_est_aujourd_hui_totalement_muet`). Ils sont verts aujourd'hui, et les plans 17-02 et 17-04 les
**réécrivent** (jamais ne les suppriment) pour asserter le comportement corrigé. C'est la trace exécutable
du bug avant/après.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contrats EXISTANTS que l'exécuteur doit consommer tels quels. Aucun n'est modifié par ce plan. -->

```csharp
// src/Chronos/Services/ChronosOAuthClient.cs — signatures ACTUELLES (inchangées par ce plan)
public sealed record OAuthTokens(string AccessToken, string RefreshToken, DateTimeOffset ExpiresAt);

public sealed class ChronosOAuthClient
{
    public const string ClientId = "9d1c250a-e61b-44d9-88ed-5944d1962f5e";
    // TokenUrl (privé) = https://console.anthropic.com/v1/oauth/token
    public ChronosOAuthClient(HttpClient http);
    public static (string verifier, string challenge, string state) CreatePkce();
    public static string BuildAuthorizeUrl(string challenge, string state);
    public Task<OAuthTokens?> ExchangeCodeAsync(string pastedCode, string verifier, string state, CancellationToken ct = default);
    public Task<OAuthTokens?> RefreshAsync(string refreshToken, CancellationToken ct = default);  // <- NON TESTÉ
}

// src/Chronos/Services/ChronosOAuthStore.cs — coffre DPAPI, chemin INJECTABLE (c'est la clé de l'isolation)
public sealed class ChronosOAuthStore
{
    public ChronosOAuthStore(string? path = null);   // null => %APPDATA%\Chronos\oauth.dat  — NE JAMAIS PASSER null EN TEST
    public bool Exists { get; }
    public void Save(OAuthTokens tokens);
    public OAuthTokens? Load();
    public void Clear();
}

// src/Chronos/Services/ChronosOAuthUsageProvider.cs — signature ACTUELLE
public sealed class ChronosOAuthUsageProvider : IUsageProvider
{
    // UsageUrl = https://api.anthropic.com/api/oauth/usage ; UserAgent = "claude-code/2.1.30"
    // MinInterval = 120 s ; Backoff429 = 5 min ; CacheUsable = 15 min ; RefreshMargin = 5 min
    public ChronosOAuthUsageProvider(ChronosOAuthStore store, ChronosOAuthClient client, HttpClient http, IClock clock);
    public Task<UsageSnapshot> GetAsync(CancellationToken ct = default);
}

// src/Chronos/Models — cibles d'assertion
public enum SourceReliability { Exact, Estimated, Unavailable }
public enum WindowKind { FiveHour, SevenDay }
public sealed record WindowState { WindowKind Kind; double? Utilization; DateTimeOffset? ResetsAt;
                                   double? FractionTimeRemaining; DateTimeOffset? CapturedAt;
                                   SourceReliability Reliability; static WindowState Unavailable(WindowKind); }
public sealed record UsageSnapshot { WindowState FiveHour; WindowState SevenDay;
                                     DateTimeOffset? SourceCapturedAt; static UsageSnapshot Empty; }

// tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs — DÉJÀ DISPONIBLE, ne rien réécrire
internal sealed class FakeHttpMessageHandler : HttpMessageHandler
{
    public int SendCount { get; }
    public HttpRequestMessage? LastRequest { get; }
    public FakeHttpMessageHandler(Func<HttpRequestMessage, HttpResponseMessage> responder);
    public static FakeHttpMessageHandler Json(HttpStatusCode status, string body);
    public static FakeHttpMessageHandler Throws(Exception ex);
}

// tests/Chronos.Tests/Fakes/FakeClock.cs
internal sealed class FakeClock : IClock { public DateTimeOffset UtcNow { get; set; } public FakeClock(DateTimeOffset now); }
```

Motif d'isolation de chemin CANONIQUE du dépôt (`MainViewModelTests.cs:37-41`, `CompositionRootTests.cs:109`) :

```csharp
private static string CheminTemp()
{
    var dir = Path.Combine(Path.GetTempPath(), "ChronosOAuthTest_" + Guid.NewGuid().ToString("N"));
    Directory.CreateDirectory(dir);
    return Path.Combine(dir, "oauth.dat");
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Couvrir ChronosOAuthClient.RefreshAsync sur sa signature actuelle — et nommer le défaut</name>

  <read_first>
    - src/Chronos/Services/ChronosOAuthClient.cs (intégralement — en particulier PostTokenAsync, lignes 95-120)
    - tests/Chronos.Tests/ChronosOAuthClientTests.cs (intégralement, 71 lignes)
    - tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs
    - .planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md (§ Pattern 3, § Pitfall 3)
  </read_first>

  <files>
tests/Chronos.Tests/ChronosOAuthClientTests.cs
  </files>

  <behavior>
    - 200 nominal : corps `{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}` →
      résultat non null, `AccessToken == "ACC-2"`, `RefreshToken == "REF-2"` (**rotation** : le refresh
      renvoyé n'est PAS celui envoyé), `ExpiresAt` dans ]now+50 min ; now+70 min[.
    - 200 sans `refresh_token` → null (le champ est obligatoire dans le code actuel, ligne 114).
    - 200 à corps tronqué (`{"access_token":"ACC-2"` sans accolade fermante) → null, aucune exception.
    - 400 `{"error":"invalid_grant"}` → null.
    - 401 `{"error":"invalid_client"}` → null.
    - 429 `{"error":{"type":"rate_limit_error"}}` → null.
    - 500 corps vide → null.
    - `HttpRequestException` levée par le transport → null, aucune exception ne remonte.
    - `TaskCanceledException` levée par le transport → null, aucune exception ne remonte.
    - **Défaut nommé** : un `[Theory]` unique parcourt 400 / 429 / 200-tronqué / exception réseau et
      prouve que les quatre produisent **le même** `null` — la cause est détruite.
    - **Sécurité** : sur un 200 nominal, `handler.LastRequest!.RequestUri!.ToString()` ne contient
      **jamais** la chaîne du refresh token, et l'URL est exactement
      `https://console.anthropic.com/v1/oauth/token`.
    - **Sécurité** : aucun test de ce fichier ne construit `new ChronosOAuthStore()` ni ne lit
      `%APPDATA%` — le client n'a pas besoin du coffre.
  </behavior>

  <action>
1. Ouvrir `tests/Chronos.Tests/ChronosOAuthClientTests.cs`. **Ne toucher à aucun des 4 tests existants**
   (`CreatePkce_challenge_est_bien_le_S256_du_verifier`, l'URL d'autorisation, la découpe `code#state`).
   Ajouter les `using` manquants : `System.Net`, `System.Threading.Tasks`.

2. Ajouter en tête de classe le helper de construction UNIQUE — c'est lui qui rendra le plan 17-02
   bon marché (une seule ligne à changer quand le type de retour deviendra `ResultatRafraichissement`) :

```csharp
    // Helper UNIQUE de construction : aucun réseau réel, aucun coffre. Le plan 17-02 ne changera
    // que la ligne de retour quand RefreshAsync deviendra typé.
    private static async Task<OAuthTokens?> RefreshAvec(FakeHttpMessageHandler handler)
    {
        var client = new ChronosOAuthClient(new HttpClient(handler));
        return await client.RefreshAsync("REF-VIEUX-BIDON");
    }
```

   **SÉCURITÉ — non négociable** : la chaîne `"REF-VIEUX-BIDON"` est une valeur inventée. Aucun test de
   ce dépôt ne doit lire, déchiffrer ni envoyer le refresh token réel de `%APPDATA%\Chronos\oauth.dat` :
   l'endpoint fait TOURNER le refresh token, un appel dont le résultat n'est pas re-sauvegardé
   **invaliderait définitivement le login de l'utilisateur**.

3. Écrire les tests, XML-doc et noms **en français** (convention du dépôt) :

```csharp
    [Fact]
    public async Task Refresh_200_rend_des_jetons_ET_fait_tourner_le_refresh_token()
    {
        var handler = FakeHttpMessageHandler.Json(HttpStatusCode.OK,
            """{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}""");

        var jetons = await RefreshAvec(handler);

        Assert.NotNull(jetons);
        Assert.Equal("ACC-2", jetons!.AccessToken);
        Assert.Equal("REF-2", jetons.RefreshToken);            // ROTATION : ce n'est plus REF-VIEUX-BIDON
        Assert.InRange(jetons.ExpiresAt,
            DateTimeOffset.UtcNow.AddMinutes(50), DateTimeOffset.UtcNow.AddMinutes(70));
    }
```

   Puis, sur le même moule :
   - `Refresh_200_sans_refresh_token_est_rejete` → `Assert.Null`.
   - `Refresh_200_a_corps_tronque_ne_leve_pas` → corps `"""{"access_token":"ACC-2\""""`, `Assert.Null`.
   - `Refresh_exception_reseau_ne_leve_pas` → `FakeHttpMessageHandler.Throws(new HttpRequestException("réseau"))`,
     encadré par `var ex = await Record.ExceptionAsync(() => RefreshAvec(handler)); Assert.Null(ex);`
     **et** `Assert.Null(await RefreshAvec(FakeHttpMessageHandler.Throws(new HttpRequestException("réseau"))))`.
   - `Refresh_timeout_ne_leve_pas` → `Throws(new TaskCanceledException())`.

4. Écrire le test qui NOMME le défaut. C'est le test que le plan 17-02 réécrira :

```csharp
    /// <summary>
    /// DÉFAUT DOCUMENTÉ (état AVANT la phase 17). Le `catch { return null; }` de PostTokenAsync
    /// (ChronosOAuthClient.cs:119) confond quatre causes qui appellent quatre réactions opposées :
    /// « refresh token révoqué » (seule une reconnexion répare), « rate limité » (attendre suffit),
    /// « corps illisible après une rotation déjà faite côté serveur » (irréparable), « wifi coupé »
    /// (ne rien dire). Sans cause, TOK-02 est mécaniquement impossible.
    /// Le plan 17-02 RÉÉCRIT ce test (il ne le supprime pas) avec IssueRafraichissement.
    /// </summary>
    [Theory]
    [InlineData(400, """{"error":"invalid_grant"}""")]         // identifiants révoqués
    [InlineData(401, """{"error":"invalid_client"}""")]        // identifiants rejetés
    [InlineData(429, """{"error":{"type":"rate_limit_error"}}""")] // TEMPORAIRE — mode d'échec RÉEL et vérifié
    [InlineData(500, "")]                                       // panne serveur
    [InlineData(200, """{"access_token":"ACC-2\"""")]          // 200 à corps tronqué (rotation déjà faite !)
    public async Task Toutes_les_causes_d_echec_se_confondent_en_un_seul_null(int statut, string corps)
    {
        var jetons = await RefreshAvec(FakeHttpMessageHandler.Json((HttpStatusCode)statut, corps));
        Assert.Null(jetons);   // <- LE DÉFAUT : aucune de ces cinq situations n'est distinguable
    }
```

5. Ajouter la garde de sécurité sur l'URL :

```csharp
    [Fact]
    public async Task Le_refresh_token_ne_transite_JAMAIS_par_l_URL()
    {
        var handler = FakeHttpMessageHandler.Json(HttpStatusCode.OK,
            """{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}""");

        await RefreshAvec(handler);

        var url = handler.LastRequest!.RequestUri!.ToString();
        Assert.Equal("https://console.anthropic.com/v1/oauth/token", url);
        Assert.DoesNotContain("REF-VIEUX-BIDON", url);
        Assert.DoesNotContain("ACC-2", url);
    }
```

**INTERDITS de cette tâche :** ne modifier AUCUN fichier sous `src/`. Ne pas construire
`new ChronosOAuthStore()` sans chemin. Ne pas ajouter de paquet NuGet.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthClientTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthClientTests"` :
      **0 échec**, et le nombre de tests exécutés est **≥ 13** (4 existants + 9 nouveaux, le `[Theory]`
      comptant pour 5 cas).
    - `git diff --name-only -- src/` → **vide** (aucun fichier de production touché).
    - `grep -c "RefreshAsync" tests/Chronos.Tests/ChronosOAuthClientTests.cs` → **≥ 1**
      (avant ce plan : 0).
    - `grep -c "Toutes_les_causes_d_echec_se_confondent_en_un_seul_null" tests/Chronos.Tests/ChronosOAuthClientTests.cs` → **1**.
    - `grep -n "new ChronosOAuthStore()" tests/Chronos.Tests/ChronosOAuthClientTests.cs` → **aucun résultat**.
    - `grep -niE "oauth\.dat|GetFolderPath|ApplicationData" tests/Chronos.Tests/ChronosOAuthClientTests.cs` → **aucun résultat**.
    - Les 4 tests pré-existants sont intacts : `git diff -U0 tests/Chronos.Tests/ChronosOAuthClientTests.cs`
      ne montre que des **ajouts** (aucune ligne supprimée hors les `using`).
  </acceptance_criteria>

  <done>
`RefreshAsync` passe de 0 à ≥ 9 cas couverts, dont les quatre modes d'échec réels, et un test nommé
prouve que les causes sont aujourd'hui indistinguables. Aucun fichier de production n'a bougé.
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer ChronosOAuthUsageProviderTests — la classe centrale de la phase n'avait aucun test</name>

  <read_first>
    - src/Chronos/Services/ChronosOAuthUsageProvider.cs (intégralement, 144 lignes)
    - src/Chronos/Services/ChronosOAuthStore.cs (intégralement — ctor à chemin injectable)
    - src/Chronos/Models/WindowState.cs et UsageSnapshot.cs
    - tests/Chronos.Tests/ClaudeOAuthUsageProviderTests.cs (modèle de test de provider HTTP le plus proche)
    - tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs, Fakes/FakeClock.cs
  </read_first>

  <files>
tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs
  </files>

  <behavior>
    - Coffre vide (fichier absent) → `UsageSnapshot.Empty`, et **aucune** requête d'usage
      (`usage.SendCount == 0`).
    - 200 nominal (`{"five_hour":{"utilization":42,"resets_at":"2026-09-09T18:30:00+00:00"},
      "seven_day":{"utilization":63,"resets_at":"2026-09-14T09:00:00+00:00"}}`) →
      `FiveHour.Utilization == 0.42` (**normalisation 0..100 → 0..1**), `SevenDay.Utilization == 0.63`,
      les deux `Reliability == Exact`, `ResetsAt` parsés, `SourceCapturedAt == clock.UtcNow`.
    - En-têtes envoyés : `Authorization: Bearer ACC-VALIDE`, `anthropic-beta: oauth-2025-04-20`,
      `User-Agent` commençant par `claude-code/`.
    - **Sécurité** : `usage.LastRequest!.RequestUri!.ToString()` ne contient ni `ACC-VALIDE` ni
      `REF-VALIDE` ; l'URL est exactement `https://api.anthropic.com/api/oauth/usage`.
    - Fenêtre absente du JSON (`{"five_hour":{...}}` seul) → `SevenDay.Reliability == Unavailable`.
    - 429 → `UsageSnapshot.Empty`, puis un second `GetAsync` immédiat (même instant d'horloge)
      n'émet **aucune** nouvelle requête : `usage.SendCount` reste à **1** (recul en vigueur).
    - 500 → `UsageSnapshot.Empty`.
    - Cache : un 200 puis (horloge avancée de 3 min, > MinInterval de 120 s, < CacheUsable de 15 min)
      un 500 → le snapshot **caché** est resservi, `FiveHour.Utilization == 0.42`.
    - `HttpRequestException` du transport → `UsageSnapshot.Empty`, **aucune exception ne remonte**
      (`Record.ExceptionAsync` → null).
    - **Défaut nommé** : 401 → `UsageSnapshot.Empty` et **rien d'autre** — aucun état exposé, aucun
      signal, aucune propriété observable. C'est le 401 muet de deux mois.
    - **Défaut nommé** : jeton expiré dans le coffre → le provider déclenche lui-même un refresh
      *paresseux* au moment du `GetAsync` (`token.SendCount == 1`), c'est-à-dire seulement quand
      quelqu'un regarde — l'absence de TOK-01.
    - **Isolation** : le chemin du coffre de test commence par `Path.GetTempPath()`.
  </behavior>

  <action>
1. Créer `tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs`, classe **publique**
   `ChronosOAuthUsageProviderTests`, namespace `Chronos.Tests`. XML-doc de classe **en français**
   expliquant que ce fichier est la Wave 0 de la phase 17 : il couvre un provider jusqu'ici NON testé,
   avant sa réécriture par le plan 17-04.

   `using` requis : `System.Net`, `System.Net.Http`, `System.IO`, `System.Threading.Tasks`,
   `Chronos.Models`, `Chronos.Services`, `Xunit`.

   **Ne PAS ajouter `[Collection("XAML WPF")]`** : cette classe ne charge aucun BAML, elle doit rester
   parallélisable.

2. Écrire les trois helpers. Le helper `Provider(...)` est le **point unique** que le plan 17-04
   modifiera (le ctor passera à `(ChronosTokenAuthority, HttpClient, IClock)`) — d'où l'unicité :

```csharp
    private static readonly DateTimeOffset Maintenant = new(2026, 9, 9, 12, 0, 0, TimeSpan.Zero);

    // SÉCURITÉ : chemin de coffre TOUJOURS sous %TEMP%. Aucun test n'ouvre %APPDATA%\Chronos\oauth.dat :
    // le refresh token réel de l'utilisateur ne doit être ni lu, ni déchiffré, ni envoyé (rotation !).
    private static string CheminCoffreTemp()
    {
        var dir = Path.Combine(Path.GetTempPath(), "ChronosOAuthProviderTest_" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(dir);
        return Path.Combine(dir, "oauth.dat");
    }

    // Coffre isolé, éventuellement pré-rempli. expiresIn null => aucun jeton enregistré.
    private static ChronosOAuthStore CoffreAvec(DateTimeOffset? expiration)
    {
        var chemin = CheminCoffreTemp();
        Assert.StartsWith(Path.GetTempPath(), chemin);   // garde anti-accident (motif CompositionRootTests:109)
        var coffre = new ChronosOAuthStore(chemin);
        if (expiration is { } e) coffre.Save(new OAuthTokens("ACC-VALIDE", "REF-VALIDE", e));
        return coffre;
    }

    // POINT UNIQUE de construction : le plan 17-04 ne réécrit QUE ce corps.
    private static ChronosOAuthUsageProvider Provider(
        ChronosOAuthStore coffre, FakeHttpMessageHandler usage, FakeClock horloge,
        FakeHttpMessageHandler? jeton = null)
        => new(coffre,
               new ChronosOAuthClient(new HttpClient(jeton ?? FakeHttpMessageHandler.Json(HttpStatusCode.BadRequest, "{}"))),
               new HttpClient(usage),
               horloge);

    private const string CorpsNominal = """
        {"five_hour":{"utilization":42,"resets_at":"2026-09-09T18:30:00+00:00"},
         "seven_day":{"utilization":63,"resets_at":"2026-09-14T09:00:00+00:00"}}
        """;
```

   Note : `expiration` est toujours passée à `Maintenant.AddHours(1)` sauf dans le test de refresh
   paresseux, où elle vaut `Maintenant.AddMinutes(-1)` (jeton expiré).

3. Écrire les tests d'après la liste `<behavior>`, noms **en français**. Exemples de forme exacte :

```csharp
    [Fact]
    public async Task Sans_jeton_le_provider_rend_Empty_sans_appeler_le_reseau()
    {
        var usage = FakeHttpMessageHandler.Json(HttpStatusCode.OK, CorpsNominal);
        var p = Provider(CoffreAvec(null), usage, new FakeClock(Maintenant));

        var snap = await p.GetAsync();

        Assert.Equal(SourceReliability.Unavailable, snap.FiveHour.Reliability);
        Assert.Equal(SourceReliability.Unavailable, snap.SevenDay.Reliability);
        Assert.Equal(0, usage.SendCount);
    }

    [Fact]
    public async Task Un_200_normalise_l_utilization_de_0_100_vers_0_1()
    {
        var usage = FakeHttpMessageHandler.Json(HttpStatusCode.OK, CorpsNominal);
        var horloge = new FakeClock(Maintenant);
        var p = Provider(CoffreAvec(Maintenant.AddHours(1)), usage, horloge);

        var snap = await p.GetAsync();

        Assert.Equal(0.42, snap.FiveHour.Utilization!.Value, 6);
        Assert.Equal(0.63, snap.SevenDay.Utilization!.Value, 6);
        Assert.Equal(SourceReliability.Exact, snap.FiveHour.Reliability);
        Assert.Equal(Maintenant, snap.SourceCapturedAt);
    }

    [Fact]
    public async Task Un_429_recule_et_le_rappel_immediat_n_emet_aucune_requete()
    {
        var usage = FakeHttpMessageHandler.Json((HttpStatusCode)429, """{"error":{"type":"rate_limit_error"}}""");
        var horloge = new FakeClock(Maintenant);
        var p = Provider(CoffreAvec(Maintenant.AddHours(1)), usage, horloge);

        await p.GetAsync();
        await p.GetAsync();                 // même instant : le recul doit tenir

        Assert.Equal(1, usage.SendCount);
    }
```

4. Écrire les **deux tests qui nomment les défauts**. Ce sont ceux que le plan 17-04 réécrira :

```csharp
    /// <summary>
    /// DÉFAUT DOCUMENTÉ (état AVANT la phase 17) — TOK-02. Un 401 est traité EXACTEMENT comme un 500
    /// ou une coupure de wifi : recul silencieux, repli sur le cache, aucun état exposé. C'est ainsi
    /// que l'utilisateur a vécu deux mois avec une source exacte morte sans le savoir.
    /// Le plan 17-04 RÉÉCRIT ce test (invalidation + un seul rejeu + état « Deconnecte »).
    /// </summary>
    [Fact]
    public async Task Un_401_est_aujourd_hui_totalement_muet()
    {
        var usage = FakeHttpMessageHandler.Json(HttpStatusCode.Unauthorized, """{"error":"unauthorized"}""");
        var p = Provider(CoffreAvec(Maintenant.AddHours(1)), usage, new FakeClock(Maintenant));

        var snap = await p.GetAsync();

        Assert.Equal(SourceReliability.Unavailable, snap.FiveHour.Reliability);
        Assert.Equal(1, usage.SendCount);   // un seul essai, aucun rejeu, aucun signal
    }

    /// <summary>
    /// DÉFAUT DOCUMENTÉ (état AVANT la phase 17) — TOK-01. Le rafraîchissement est PARESSEUX : il n'a
    /// lieu qu'au moment d'un GetAsync, c'est-à-dire quand quelqu'un regarde le cadran. Un exe laissé
    /// tourner voit son jeton mourir sans qu'aucune horloge de fond ne le renouvelle.
    /// Le plan 17-04 RÉÉCRIT ce test : le provider ne rafraîchira plus lui-même.
    /// </summary>
    [Fact]
    public async Task Le_rafraichissement_est_aujourd_hui_paresseux_declenche_par_le_GetAsync()
    {
        var jeton = FakeHttpMessageHandler.Json(HttpStatusCode.OK,
            """{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}""");
        var usage = FakeHttpMessageHandler.Json(HttpStatusCode.OK, CorpsNominal);
        var p = Provider(CoffreAvec(Maintenant.AddMinutes(-1)), usage, new FakeClock(Maintenant), jeton);

        Assert.Equal(0, jeton.SendCount);   // avant : rien ne rafraîchit
        await p.GetAsync();
        Assert.Equal(1, jeton.SendCount);   // le refresh n'arrive QUE parce qu'on a demandé un chiffre
    }
```

**INTERDITS de cette tâche :** ne modifier AUCUN fichier sous `src/`. Ne jamais construire
`new ChronosOAuthStore()` sans chemin explicite. Ne jamais écrire une valeur lue depuis
`%APPDATA%\Chronos\oauth.dat`. Ne pas ajouter `[Collection("XAML WPF")]`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthUsageProviderTests"</automated>
  </verify>

  <acceptance_criteria>
    - Le fichier `tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` **existe** (il n'existait pas).
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthUsageProviderTests"` :
      **0 échec**, **≥ 10** tests exécutés.
    - `grep -c "Provider(" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → le helper est le
      point unique : `grep -c "new ChronosOAuthUsageProvider(" ` sur ce fichier → **exactement 1**.
    - `grep -c "GetTempPath" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → **≥ 2**
      (construction + garde `Assert.StartsWith`).
    - `grep -n "new ChronosOAuthStore()" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → **aucun résultat**.
    - `grep -c "Un_401_est_aujourd_hui_totalement_muet" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → **1**.
    - `grep -c "Le_rafraichissement_est_aujourd_hui_paresseux_declenche_par_le_GetAsync" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → **1**.
    - `git diff --name-only -- src/` → **vide**.
    - Après exécution : `ls "$APPDATA/Chronos/oauth.dat"` est **inchangé** (même date de modification
      qu'avant le test — le coffre réel n'a pas été touché).
  </acceptance_criteria>

  <done>
Le provider central de la phase 17 passe de 0 à ≥ 10 tests, dont deux qui nomment explicitement les
défauts que les plans 17-02 et 17-04 corrigeront. Aucun accès au coffre réel, aucun accès réseau réel.
  </done>
</task>

</tasks>

<verification>
1. Suite complète : `dotnet test Chronos.sln -v q --nologo` → **0 échec**, total **≥ 419 + 19 = 438**
   (baseline 419 ; ce plan **n'enlève aucun test**).
2. Aucun fichier de production modifié : `git diff --name-only -- src/` → **vide**.
3. Garde de pureté inchangée :
   `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert.
4. Garde de composition inchangée :
   `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~CompositionRootTests"` → vert.
5. Aucun jeton en clair :
   `grep -rniE "accessToken|refreshToken" src/Chronos --include=*.cs | grep -iE "log|Console|WriteLine|Append"`
   → **aucun résultat**.
6. Le coffre réel est intact : `ls -l "$APPDATA/Chronos/oauth.dat"` avant et après la suite → même
   taille et même horodatage.
</verification>

<success_criteria>
- `ChronosOAuthUsageProviderTests.cs` créé, ≥ 10 tests, 0 échec.
- `ChronosOAuthClientTests.cs` étendu, ≥ 9 cas nouveaux couvrant `RefreshAsync`, 0 échec.
- Deux tests nomment explicitement les défauts (401 muet, refresh paresseux) et un `[Theory]` prouve
  que les causes d'échec du refresh sont indistinguables.
- Aucun octet modifié sous `src/`, aucun appel réseau réel, aucun accès au coffre réel.
- Suite complète verte, ≥ 438 tests.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-01-SUMMARY.md`.
</output>
