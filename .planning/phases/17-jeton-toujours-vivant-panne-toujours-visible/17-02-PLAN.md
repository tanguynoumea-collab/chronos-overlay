---
phase: 17-jeton-toujours-vivant-panne-toujours-visible
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/Chronos/Services/EtatAuthentification.cs
  - src/Chronos/Services/IAuthStatus.cs
  - src/Chronos/Services/ChronosOAuthClient.cs
  - src/Chronos/Services/ChronosOAuthUsageProvider.cs
  - tests/Chronos.Tests/Fakes/FakeAuthStatus.cs
  - tests/Chronos.Tests/ChronosOAuthClientTests.cs
autonomous: true
requirements: [TOK-01, TOK-02]

must_haves:
  truths:
    - "Un échec de rafraîchissement porte désormais sa CAUSE : « identifiants rejetés » (seule une reconnexion répare) et « échec temporaire » (réseau, 429, 5xx, timeout) sont deux issues distinctes."
    - "Un 429 rate_limit_error — mode d'échec RÉEL et vérifié du point de terminaison de jeton — est classé EchecTemporaire, jamais « déconnecté » : crier « reconnecte-toi » sur un rate-limit serait une fausse alerte."
    - "Un 200 dont le corps est inexploitable est classé IdentifiantsRejetes : la rotation a déjà eu lieu côté serveur, l'ancien refresh token est mort et le nouveau est perdu — réessayer est vain."
    - "Le résultat du rafraîchissement ne transporte JAMAIS le corps HTTP, ni les en-têtes, ni un message d'exception : un corps d'erreur peut échoïser la requête, donc le refresh token."
    - "L'état d'authentification est un type NEUTRE de Chronos.Services, observable par événement, sans aucun type WPF — ServicesLayerPurityTests reste vert sans modification de son allow-list."
    - "La solution compile et la suite reste verte à ce commit : l'unique appelant de RefreshAsync est adapté d'une seule ligne."
  artifacts:
    - path: "src/Chronos/Services/EtatAuthentification.cs"
      provides: "Les 4 états d'authentification exposés à l'overlay"
      contains: "enum EtatAuthentification"
    - path: "src/Chronos/Services/IAuthStatus.cs"
      provides: "Canal d'état neutre lu par le ViewModel (propriété + événement + réarmement après login)"
      exports: ["Etat", "EtatChange", "ReinitialiserApresLogin"]
    - path: "src/Chronos/Services/ChronosOAuthClient.cs"
      provides: "RefreshAsync typé — la cause survit à l'échec"
      contains: "ResultatRafraichissement"
    - path: "tests/Chronos.Tests/Fakes/FakeAuthStatus.cs"
      provides: "IAuthStatus en mémoire, déclencheur manuel — requis par les 5 sites new MainViewModel"
      contains: "FakeAuthStatus"
  key_links:
    - from: "src/Chronos/Services/ChronosOAuthClient.cs"
      to: "IssueRafraichissement"
      via: "type de retour de RefreshAsync"
      pattern: "Task<ResultatRafraichissement> RefreshAsync"
    - from: "src/Chronos/Services/ChronosOAuthUsageProvider.cs"
      to: "ChronosOAuthClient.RefreshAsync"
      via: "adaptation minimale d'un seul appelant (.Jetons), supprimée au plan 17-04"
      pattern: "RefreshAsync\\(.*\\)\\)\\.Jetons"
    - from: "src/Chronos/Services/IAuthStatus.cs"
      to: "EtatAuthentification"
      via: "propriété + EventHandler<T>, types du même assembly (motif RefreshOrchestrator.SnapshotChanged)"
      pattern: "EventHandler<EtatAuthentification>"
---

<objective>
Poser les deux contrats sans lesquels TOK-02 est **mécaniquement impossible** : un vocabulaire d'états
d'authentification neutre, et un rafraîchissement qui rend sa CAUSE au lieu d'un `null` muet.

Purpose: `ChronosOAuthClient.PostTokenAsync` se termine ligne 119 par `catch { return null; }`. Ce seul
`catch` confond « refresh token révoqué » (seule une reconnexion répare), « rate limité » (attendre
suffit), « corps illisible après une rotation déjà faite côté serveur » (irréparable) et « wifi coupé »
(ne rien dire). Le plan 17-01 l'a prouvé par un `[Theory]`. Tant que la cause est détruite, aucune
pastille honnête n'est constructible : soit on crie « reconnecte-toi » quand c'est le wifi, soit on se
tait quand le compte est révoqué.

Output: `EtatAuthentification.cs`, `IAuthStatus.cs`, `ResultatRafraichissement` / `IssueRafraichissement`,
`RefreshAsync` typé, `FakeAuthStatus`, et la réécriture des assertions du `[Theory]` de la Wave 0.

**Ce plan ne crée AUCUNE autorité, AUCUN service de fond, AUCUNE pastille.** Il ne fait que rendre le
vocabulaire disponible. L'autorité arrive au plan 17-03, le câblage au 17-04, l'affichage au 17-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-VALIDATION.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrats à MODIFIER (état actuel) et motifs à copier. -->

```csharp
// src/Chronos/Services/ChronosOAuthClient.cs — ÉTAT ACTUEL, lignes 80-120
public Task<OAuthTokens?> RefreshAsync(string refreshToken, CancellationToken ct = default);   // -> devient typé
private async Task<OAuthTokens?> PostTokenAsync(object body, CancellationToken ct)
{
    try
    {
        using var req = new HttpRequestMessage(HttpMethod.Post, TokenUrl) { Content = /* JSON */ };
        using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
        cts.CancelAfter(TimeSpan.FromSeconds(15));
        using var resp = await _http.SendAsync(req, cts.Token);
        if (!resp.IsSuccessStatusCode) return null;
        // ... lecture du flux, access_token / refresh_token / expires_in (repli 3600) ...
        return new OAuthTokens(access!, refresh!, DateTimeOffset.UtcNow.AddSeconds(expiresIn));
    }
    catch { return null; }   // <- LE DÉFAUT : couvre à la fois l'envoi ET le parsing
}

// L'UNIQUE appelant de RefreshAsync dans tout le dépôt (vérifié par grep) :
// src/Chronos/Services/ChronosOAuthUsageProvider.cs:63
var refreshed = await _client.RefreshAsync(tokens.RefreshToken, ct);

// ExchangeCodeAsync garde sa signature : src/Chronos/Views/OAuthLogin.cs en dépend.

// src/Chronos/Services/IClock.cs + SystemClock.cs — horloge injectable du dépôt
public interface IClock { DateTimeOffset UtcNow { get; } }
public sealed class SystemClock : IClock { public DateTimeOffset UtcNow => DateTimeOffset.UtcNow; }

// MOTIF DE RÉFÉRENCE de l'événement neutre — src/Chronos/Services/RefreshOrchestrator.cs:30-32
// « Émis (thread pool) après chaque GetAsync… Le VM s'abonne ICI et marshalle via IUiDispatcher —
//   décision verrouillée "le service expose l'event" »
public event EventHandler<UsageSnapshot>? SnapshotChanged;
// C'est la preuve vivante qu'un EventHandler<T> d'un type du même assembly passe ServicesLayerPurityTests.
```

**Garde de pureté à ne PAS toucher** (`tests/Chronos.Tests/ServicesLayerPurityTests.cs`) : elle inspecte
les types de retour/paramètres des méthodes publiques et les types de propriétés de `Chronos.Services` /
`Chronos.Models` et refuse `PresentationCore`, `PresentationFramework`, `WindowsBase`. Un `enum` local et
un `EventHandler<T>` (`System.Runtime`) passent. **Aucune entrée n'est à ajouter à son allow-list.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vocabulaire d'état d'authentification — enum neutre, contrat observable, faux de test</name>

  <read_first>
    - src/Chronos/Services/RefreshOrchestrator.cs (lignes 25-45 — le motif d'événement neutre à copier)
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (intégralement — comprendre ce qui est inspecté)
    - tests/Chronos.Tests/Fakes/FakeOAuthLogin.cs (forme d'un faux PUBLIC du dépôt)
    - src/Chronos/Services/IOAuthLogin.cs (forme d'un contrat neutre du dépôt)
  </read_first>

  <files>
src/Chronos/Services/EtatAuthentification.cs
src/Chronos/Services/IAuthStatus.cs
tests/Chronos.Tests/Fakes/FakeAuthStatus.cs
  </files>

  <action>
1. Créer `src/Chronos/Services/EtatAuthentification.cs`. Commentaires **en français**, aucun `using`
   d'assembly WPF :

```csharp
namespace Chronos.Services;

/// <summary>
/// État d'authentification de la source exacte OAuth de Chronos, du point de vue de l'overlay (TOK-02).
/// Quatre valeurs et pas trois : confondre « hors ligne » et « déconnecté » ferait crier
/// « reconnecte-toi » à un utilisateur dont le wifi est simplement coupé — il relancerait un login,
/// échouerait, et perdrait confiance dans le signal.
/// </summary>
public enum EtatAuthentification
{
    /// <summary>Aucun jeton dans le coffre : l'utilisateur ne s'est jamais connecté. N'allume RIEN en
    /// phase 17 — l'invite « jamais connecté » est EXA-05, phase 19.</summary>
    NonConnecte,

    /// <summary>Jeton valide, chiffres exacts en circulation.</summary>
    Connecte,

    /// <summary>Réseau ou serveur momentanément indisponible (429, 5xx, timeout, DNS). Le jeton est
    /// peut-être parfaitement bon : état INFORMATIF, jamais actionnable.</summary>
    HorsLigne,

    /// <summary>Le serveur a REFUSÉ les identifiants (invalid_grant, invalid_client, 200 inexploitable
    /// après rotation). Seule une reconnexion répare : état ACTIONNABLE.</summary>
    Deconnecte,
}
```

2. Créer `src/Chronos/Services/IAuthStatus.cs` :

```csharp
namespace Chronos.Services;

/// <summary>
/// Canal d'état d'authentification exposé aux ViewModels (TOK-02/TOK-03). Type NEUTRE : aucun type WPF.
///
/// Motif IDENTIQUE à <see cref="RefreshOrchestrator.SnapshotChanged"/> : le service expose l'événement,
/// l'abonné marshalle lui-même via IUiDispatcher (frontière de thread unique, RAF-04). L'état d'auth
/// n'est délibérément PAS un champ d'UsageSnapshot : ce dernier traverse deux composites imbriqués dont
/// Best() choisit PAR FENÊTRE, et il n'existe aucune règle sensée pour fusionner deux états d'auth issus
/// de branches différentes.
/// </summary>
public interface IAuthStatus
{
    /// <summary>État courant. Lisible à tout instant, y compris avant la première transition.</summary>
    EtatAuthentification Etat { get; }

    /// <summary>Émis sur un thread du POOL à chaque TRANSITION (jamais un événement par tick).
    /// L'abonné marshalle lui-même vers le thread UI.</summary>
    event EventHandler<EtatAuthentification>? EtatChange;

    /// <summary>TOK-03 — après un login réussi : relâche le verrou « Deconnecte » ET le recul, et oublie
    /// les jetons mémorisés pour relire le coffre fraîchement écrit. Sans cet appel, le jeton tout neuf
    /// ne serait pas utilisé et la pastille survivrait à sa propre réparation.</summary>
    void ReinitialiserApresLogin();
}
```

3. Créer `tests/Chronos.Tests/Fakes/FakeAuthStatus.cs` — **public** (comme `FakeOAuthLogin`), car il sera
   consommé par 5 sites `new MainViewModel(...)` au plan 17-05 :

```csharp
using Chronos.Services;

namespace Chronos.Tests;

/// <summary>Faux d'<see cref="IAuthStatus"/> : état en mémoire, déclencheur manuel de transition,
/// et comptage des réarmements après login (preuve TOK-03).</summary>
public sealed class FakeAuthStatus : IAuthStatus
{
    /// <summary>Réglable AVANT construction du VM pour tester l'application de l'état initial.</summary>
    public EtatAuthentification Etat { get; set; } = EtatAuthentification.NonConnecte;

    /// <summary>Nombre d'appels à <see cref="ReinitialiserApresLogin"/>.</summary>
    public int ReinitCount { get; private set; }

    public event EventHandler<EtatAuthentification>? EtatChange;

    public void ReinitialiserApresLogin() => ReinitCount++;

    /// <summary>Provoque une transition observable (comme le ferait l'autorité de jeton).</summary>
    public void Declencher(EtatAuthentification e)
    {
        Etat = e;
        EtatChange?.Invoke(this, e);
    }
}
```

**INTERDITS de cette tâche :** aucun `using System.Windows.*` dans les deux fichiers de `Services/`.
Ne PAS modifier `ServicesLayerPurityTests.cs` ni son allow-list. Ne PAS placer `EtatAuthentification`
dans `Chronos.Models` (ce n'est pas une donnée d'usage ; l'y mettre inviterait plus tard à l'agréger
dans `UsageSnapshot`, exactement ce qui est rejeté). Aucun câblage DI ici.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo && dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -v q --nologo` → **0 erreur**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests"` → **vert**.
    - `git diff -- tests/Chronos.Tests/ServicesLayerPurityTests.cs` → **vide** (la garde n'a pas été assouplie).
    - `grep -c "NonConnecte\|Connecte\|HorsLigne\|Deconnecte" src/Chronos/Services/EtatAuthentification.cs` → **≥ 4**.
    - `grep -n "System.Windows" src/Chronos/Services/EtatAuthentification.cs src/Chronos/Services/IAuthStatus.cs` → **aucun résultat**.
    - `grep -c "EventHandler<EtatAuthentification>" src/Chronos/Services/IAuthStatus.cs` → **1**.
    - `grep -c "ReinitialiserApresLogin" src/Chronos/Services/IAuthStatus.cs tests/Chronos.Tests/Fakes/FakeAuthStatus.cs` → **1** dans chacun.
    - `grep -c "public sealed class FakeAuthStatus" tests/Chronos.Tests/Fakes/FakeAuthStatus.cs` → **1** (public, pas internal).
  </acceptance_criteria>

  <done>
Le vocabulaire d'état existe, il est neutre, il est observable, et il a son faux. Rien n'est encore
câblé ; la garde de pureté est verte sans avoir été touchée.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RefreshAsync rend sa CAUSE — et le [Theory] de la Wave 0 est réécrit, pas supprimé</name>

  <read_first>
    - src/Chronos/Services/ChronosOAuthClient.cs (intégralement, en particulier PostTokenAsync lignes 95-120)
    - src/Chronos/Services/ChronosOAuthUsageProvider.cs (lignes 56-76 — l'unique appelant)
    - tests/Chronos.Tests/ChronosOAuthClientTests.cs (tel que livré par le plan 17-01)
    - .planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md (§ Pattern 3, § Pitfall 3, § Code Example 3)
  </read_first>

  <files>
src/Chronos/Services/ChronosOAuthClient.cs
src/Chronos/Services/ChronosOAuthUsageProvider.cs
tests/Chronos.Tests/ChronosOAuthClientTests.cs
  </files>

  <behavior>
    - 200 nominal → `Issue == Succes`, `Jetons` non null, `RefreshToken == "REF-2"` (rotation),
      `ExpiresAt == horlogeFigee.UtcNow.AddSeconds(3600)` **exactement** (l'horloge est injectée).
    - 400 `{"error":"invalid_grant"}` → `Issue == IdentifiantsRejetes`, `Jetons == null`.
    - 401 `{"error":"invalid_client"}` → `Issue == IdentifiantsRejetes`.
    - **429 `{"error":{"type":"rate_limit_error"}}` → `Issue == EchecTemporaire`.** Mode d'échec RÉEL et
      vérifié le 2026-09-09 sur l'endpoint : le classer « déconnecté » serait une fausse alerte.
    - 500 → `Issue == EchecTemporaire`.
    - 200 à corps tronqué → `Issue == IdentifiantsRejetes` (la rotation a déjà eu lieu côté serveur :
      l'ancien refresh token est mort, le nouveau est perdu — réessayer est vain).
    - 200 sans `refresh_token` → `Issue == IdentifiantsRejetes` (même raisonnement).
    - `HttpRequestException` **avant** toute réponse → `Issue == EchecTemporaire`.
    - `TaskCanceledException` (timeout) → `Issue == EchecTemporaire`.
    - **Sécurité structurelle** : `ResultatRafraichissement` n'expose **aucune** propriété de type
      `string` — impossible d'y glisser un corps HTTP ou un message d'exception.
    - **Sécurité** : l'URL de la requête reste `https://console.anthropic.com/v1/oauth/token` et ne
      contient jamais le refresh token.
    - `ExchangeCodeAsync` continue de renvoyer `OAuthTokens?` et son comportement est inchangé
      (`Views/OAuthLogin.cs` en dépend).
  </behavior>

  <action>
1. Dans `src/Chronos/Services/ChronosOAuthClient.cs`, **au-dessus** de la classe (à côté du record
   `OAuthTokens`, ligne 11), ajouter le vocabulaire de résultat :

```csharp
/// <summary>Issue d'un rafraîchissement de jeton. Deux branches et pas trois : ce qui compte pour
/// l'utilisateur est « faut-il que je fasse quelque chose ? » (IdentifiantsRejetes) ou « faut-il
/// simplement attendre ? » (EchecTemporaire).</summary>
public enum IssueRafraichissement
{
    /// <summary>Jetons obtenus (avec rotation du refresh token).</summary>
    Succes,
    /// <summary>Le serveur a refusé les identifiants (RFC 6749 §5.2 invalid_grant / invalid_client), ou
    /// a répondu 200 avec un corps inexploitable — auquel cas la rotation a DÉJÀ eu lieu de son côté et
    /// l'ancien refresh token est définitivement mort. Aucun réessai ne peut aider : il faut un login.</summary>
    IdentifiantsRejetes,
    /// <summary>Réseau, timeout, 429 (rate_limit_error — mode d'échec RÉEL et vérifié de ce point de
    /// terminaison) ou 5xx. Le jeton est peut-être parfaitement bon : réessayer plus tard, avec recul.</summary>
    EchecTemporaire,
}

/// <summary>
/// Résultat d'un rafraîchissement. SÉCURITÉ — ce type ne transporte NI le corps HTTP, NI les en-têtes,
/// NI le message d'exception : un corps d'erreur peut échoïser la requête, donc le refresh token.
/// Seuls l'issue (deux valeurs) et les jetons obtenus en sortent. Ne JAMAIS y ajouter de champ texte.
/// </summary>
public sealed record ResultatRafraichissement(IssueRafraichissement Issue, OAuthTokens? Jetons);
```

2. Rendre l'expiration déterministe. Remplacer le ctor (ligne 36) par une surcharge à **paramètre
   optionnel en dernière position** — 0 site d'appel cassé (même levier que celui retenu pour
   `DiagnosticService`) :

```csharp
    private readonly HttpClient _http;
    private readonly IClock _horloge;

    /// <param name="clock">Horloge injectable : rend ExpiresAt déterministe sous test. Par défaut
    /// l'horloge système — aucun site d'appel existant n'est cassé.</param>
    public ChronosOAuthClient(HttpClient http, IClock? clock = null)
    {
        _http = http;
        _horloge = clock ?? new SystemClock();
    }
```
   Puis remplacer `DateTimeOffset.UtcNow.AddSeconds(expiresIn)` (ligne 117) par
   `_horloge.UtcNow.AddSeconds(expiresIn)`.

3. Scinder le `catch` fourre-tout. Extraire d'abord la lecture du flux en méthode privée réutilisée par
   les deux chemins (ne PAS dupliquer le parsing) :

```csharp
    // Lit le corps d'une réponse 2xx. null = corps inexploitable (tronqué, champ manquant).
    private async Task<OAuthTokens?> LireJetonsAsync(HttpResponseMessage resp, CancellationToken ct)
    {
        await using var s = await resp.Content.ReadAsStreamAsync(ct);
        using var doc = await JsonDocument.ParseAsync(s, cancellationToken: ct);
        var root = doc.RootElement;

        var access = root.TryGetProperty("access_token", out var a) ? a.GetString() : null;
        var refresh = root.TryGetProperty("refresh_token", out var r) ? r.GetString() : null;
        if (string.IsNullOrEmpty(access) || string.IsNullOrEmpty(refresh)) return null;

        var expiresIn = root.TryGetProperty("expires_in", out var e) && e.TryGetInt64(out var secs) ? secs : 3600;
        return new OAuthTokens(access!, refresh!, _horloge.UtcNow.AddSeconds(expiresIn));
    }
```
   `PostTokenAsync` (utilisé par `ExchangeCodeAsync`) l'appelle désormais à la place de son parsing
   inline, et **garde** son `catch { return null; }` : l'échange de code n'a pas de cause à distinguer.

4. Réécrire `RefreshAsync` (remplace intégralement les lignes 80-85) :

```csharp
    /// <summary>
    /// Rafraîchit les jetons via le refresh_token. Le serveur fait TOURNER le refresh token : celui du
    /// résultat remplace l'ancien et doit être persisté par l'appelant avant toute autre action.
    ///
    /// Rend une CAUSE et non un null muet : sans elle, distinguer « compte révoqué » de « wifi coupé »
    /// est impossible, et TOK-02 avec. SÉCURITÉ : ni le corps, ni les en-têtes, ni le message
    /// d'exception ne sortent d'ici.
    /// </summary>
    public async Task<ResultatRafraichissement> RefreshAsync(string refreshToken, CancellationToken ct = default)
    {
        var body = new { grant_type = "refresh_token", refresh_token = refreshToken, client_id = ClientId };
        HttpResponseMessage? resp = null;
        try
        {
            using var req = new HttpRequestMessage(HttpMethod.Post, TokenUrl)
            {
                Content = new StringContent(JsonSerializer.Serialize(body), Encoding.UTF8, "application/json"),
            };
            using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
            cts.CancelAfter(TimeSpan.FromSeconds(15));
            resp = await _http.SendAsync(req, cts.Token);

            var code = (int)resp.StatusCode;
            // RFC 6749 §5.2 : invalid_grant / invalid_client => seule une reconnexion répare.
            if (code is 400 or 401) return new(IssueRafraichissement.IdentifiantsRejetes, null);
            // 429 rate_limit_error (constaté en direct sur ce point de terminaison), 5xx, 408…
            if (!resp.IsSuccessStatusCode) return new(IssueRafraichissement.EchecTemporaire, null);

            var jetons = await LireJetonsAsync(resp, cts.Token);
            // 200 + corps inexploitable : le serveur a DÉJÀ roulé le refresh token, l'ancien est mort et
            // le nouveau est perdu. Réessayer est vain — déconnexion honnête plutôt que boucle inutile.
            return jetons is null
                ? new(IssueRafraichissement.IdentifiantsRejetes, null)
                : new(IssueRafraichissement.Succes, jetons);
        }
        catch (Exception ex) when (ex is HttpRequestException or TaskCanceledException
                                     or OperationCanceledException or JsonException or IOException)
        {
            // Si une réponse 2xx avait déjà été reçue, la rotation a pu avoir lieu → même raisonnement.
            return resp is { IsSuccessStatusCode: true }
                ? new(IssueRafraichissement.IdentifiantsRejetes, null)
                : new(IssueRafraichissement.EchecTemporaire, null);
        }
        finally { resp?.Dispose(); }
    }
```
   NOTE : la méthode HTTP reste `POST` et l'URL reste la constante privée `TokenUrl`
   (`https://console.anthropic.com/v1/oauth/token`) — ne rien y changer.
   Ajouter `using System.IO;` si `IOException` n'est pas encore résolu dans ce fichier.

5. **Garder la solution compilable** — adapter l'UNIQUE appelant,
   `src/Chronos/Services/ChronosOAuthUsageProvider.cs:63`, d'une seule ligne :

```csharp
            // ADAPTATION MINIMALE (plan 17-02). Ce bloc de rafraîchissement paresseux est SUPPRIMÉ
            // en entier par le plan 17-04 : le provider deviendra un simple consommateur de l'autorité.
            var refreshed = (await _client.RefreshAsync(tokens.RefreshToken, ct)).Jetons;
```
   **Ne rien changer d'autre dans ce fichier à ce commit.**

6. Réécrire le `[Theory]` de la Wave 0 dans `tests/Chronos.Tests/ChronosOAuthClientTests.cs`.
   **RÉÉCRIRE, jamais supprimer** : le nom change, le fichier garde le même nombre de cas + 3.

```csharp
    // Le helper unique du plan 17-01 : SEULE sa ligne de retour change.
    private static async Task<ResultatRafraichissement> RefreshAvec(FakeHttpMessageHandler handler, IClock? horloge = null)
    {
        var client = new ChronosOAuthClient(new HttpClient(handler), horloge);
        return await client.RefreshAsync("REF-VIEUX-BIDON");
    }

    /// <summary>
    /// TOK-02 — remplace `Toutes_les_causes_d_echec_se_confondent_en_un_seul_null` (plan 17-01, qui
    /// documentait le défaut). Chaque cause appelle désormais une réaction différente : reconnexion
    /// pour l'une, patience pour l'autre. Le cas 429 est le plus important : c'est un mode d'échec
    /// RÉEL et vérifié du point de terminaison de jeton — le classer « déconnecté » afficherait une
    /// fausse alerte sur un compte parfaitement sain.
    /// </summary>
    [Theory]
    [InlineData(400, """{"error":"invalid_grant"}""",              IssueRafraichissement.IdentifiantsRejetes)]
    [InlineData(401, """{"error":"invalid_client"}""",             IssueRafraichissement.IdentifiantsRejetes)]
    [InlineData(429, """{"error":{"type":"rate_limit_error"}}""",  IssueRafraichissement.EchecTemporaire)]
    [InlineData(500, "",                                            IssueRafraichissement.EchecTemporaire)]
    [InlineData(200, """{"access_token":"ACC-2\"""",               IssueRafraichissement.IdentifiantsRejetes)]
    [InlineData(200, """{"access_token":"ACC-2"}""",               IssueRafraichissement.IdentifiantsRejetes)]
    public async Task Chaque_cause_d_echec_est_desormais_distinguee(int statut, string corps, IssueRafraichissement attendue)
    {
        var res = await RefreshAvec(FakeHttpMessageHandler.Json((HttpStatusCode)statut, corps));
        Assert.Equal(attendue, res.Issue);
        Assert.Null(res.Jetons);
    }
```

   Adapter de la même façon les tests unitaires livrés par 17-01 :
   - `Refresh_200_rend_des_jetons_ET_fait_tourner_le_refresh_token` → assertion sur
     `res.Issue == IssueRafraichissement.Succes` et, en injectant
     `new FakeClock(new DateTimeOffset(2026, 9, 9, 12, 0, 0, TimeSpan.Zero))`,
     `Assert.Equal(horloge.UtcNow.AddSeconds(3600), res.Jetons!.ExpiresAt)` — **égalité exacte**,
     l'`InRange` approximatif de la Wave 0 n'a plus lieu d'être.
   - `Refresh_exception_reseau_ne_leve_pas` / `Refresh_timeout_ne_leve_pas` → `EchecTemporaire`.
   - `Le_refresh_token_ne_transite_JAMAIS_par_l_URL` → inchangé.

   Ajouter la garde de sécurité structurelle :

```csharp
    /// <summary>SÉCURITÉ : impossible de faire fuiter un corps HTTP ou un message d'exception par ce
    /// type — il ne contient aucune propriété texte. Ce test échoue si quelqu'un ajoute un champ.</summary>
    [Fact]
    public void Le_resultat_de_rafraichissement_ne_peut_transporter_aucun_texte()
    {
        var proprietes = typeof(ResultatRafraichissement).GetProperties();
        Assert.DoesNotContain(proprietes, p => p.PropertyType == typeof(string));
        Assert.Equal(2, proprietes.Length);   // Issue + Jetons, rien d'autre
    }
```

**INTERDITS de cette tâche :** ne PAS changer la signature d'`ExchangeCodeAsync`. Ne PAS toucher à
`Views/OAuthLogin.cs`. Ne PAS toucher à `ChronosOAuthStore.cs`. Ne PAS supprimer de test. Ne PAS
modifier `ChronosOAuthUsageProvider.cs` au-delà de la seule ligne 63. Ne JAMAIS faire figurer le corps
de la réponse, un en-tête ou `ex.Message` dans un type, un log ou une exception.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo && dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthClientTests|FullyQualifiedName~ChronosOAuthUsageProviderTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -v q --nologo` → **0 erreur** (la solution compile à ce commit).
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthClientTests"` →
      **0 échec**, **≥ 16** tests exécutés.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthUsageProviderTests"` →
      **0 échec** (les tests du plan 17-01 restent verts : le comportement observable du provider n'a pas changé).
    - `grep -c "Task<ResultatRafraichissement> RefreshAsync" src/Chronos/Services/ChronosOAuthClient.cs` → **1**.
    - `grep -c "Task<OAuthTokens?> ExchangeCodeAsync" src/Chronos/Services/ChronosOAuthClient.cs` → **1**
      (signature préservée).
    - `grep -c "catch { return null; }" src/Chronos/Services/ChronosOAuthClient.cs` → **1**
      (celui de `PostTokenAsync`, pour l'échange de code uniquement — il n'y en avait qu'un avant).
    - `grep -c "IssueRafraichissement" src/Chronos/Services/ChronosOAuthClient.cs` → **≥ 6**.
    - `grep -n "Toutes_les_causes_d_echec_se_confondent_en_un_seul_null" tests/Chronos.Tests/` →
      **aucun résultat** (réécrit) ; `grep -c "Chaque_cause_d_echec_est_desormais_distinguee" tests/Chronos.Tests/ChronosOAuthClientTests.cs` → **1**.
    - `grep -c "\.Jetons" src/Chronos/Services/ChronosOAuthUsageProvider.cs` → **1** (l'adaptation minimale).
    - `git diff --stat src/Chronos/Services/ChronosOAuthUsageProvider.cs` → **≤ 4 lignes changées**.
    - `git diff --name-only -- src/Chronos/Views/OAuthLogin.cs src/Chronos/Services/ChronosOAuthStore.cs` → **vide**.
    - `grep -rn "ex.Message\|ReadAsStringAsync" src/Chronos/Services/ChronosOAuthClient.cs` → **aucun résultat**
      (aucun texte de réponse ne peut sortir).
  </acceptance_criteria>

  <done>
`RefreshAsync` distingue « identifiants rejetés » de « échec temporaire », un 429 est classé temporaire,
un 200 inexploitable est classé rejet définitif, et rien de textuel ne peut fuiter. La solution compile,
les tests de la Wave 0 sont réécrits (aucun supprimé), la suite est verte.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln -v q --nologo` → **0 erreur**.
2. Suite complète : `dotnet test Chronos.sln -v q --nologo` → **0 échec**, total **≥ 438 + 3**
   (aucun test supprimé, 3 ajoutés nets : la garde de type, +1 cas de `[Theory]`, +1 réécrit).
3. Pureté : `--filter "FullyQualifiedName~ServicesLayerPurityTests"` → **vert**, fichier de garde
   **non modifié** (`git diff` vide).
4. Composition : `--filter "FullyQualifiedName~CompositionRootTests"` → **vert** (aucun câblage n'a bougé).
5. Login intact : `git diff --name-only -- src/Chronos/Views/` → **vide**.
6. Aucun jeton en clair :
   `grep -rniE "accessToken|refreshToken" src/Chronos --include=*.cs | grep -iE "log|Console|WriteLine|Append"`
   → **aucun résultat**.
7. Aucun paquet ajouté : `git diff -- src/Chronos/Chronos.csproj tests/Chronos.Tests/Chronos.Tests.csproj`
   → **vide**.
</verification>

<success_criteria>
- `EtatAuthentification` (4 valeurs) et `IAuthStatus` (Etat + EtatChange + ReinitialiserApresLogin)
  existent dans `Chronos.Services`, sans aucun type WPF.
- `FakeAuthStatus` existe, public, avec `Declencher` et `ReinitCount`.
- `RefreshAsync` renvoie `ResultatRafraichissement` ; les 6 modes d'échec sont classés conformément à la
  table de décision ; 429 = `EchecTemporaire` ; 200 inexploitable = `IdentifiantsRejetes`.
- Aucun texte (corps, en-tête, message d'exception) ne peut sortir du résultat — prouvé par un test réflexif.
- `ExchangeCodeAsync` et `Views/OAuthLogin.cs` intacts.
- Solution compilable, suite verte, aucun test supprimé.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-02-SUMMARY.md`.
</output>
