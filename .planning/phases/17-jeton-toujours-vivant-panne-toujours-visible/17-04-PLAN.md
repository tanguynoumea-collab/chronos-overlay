---
phase: 17-jeton-toujours-vivant-panne-toujours-visible
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - src/Chronos/Services/ChronosOAuthUsageProvider.cs
  - src/Chronos/Services/ChronosOAuthStore.cs
  - src/Chronos/Services/DiagnosticService.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
  - tests/Chronos.Tests/DiagnosticServiceTests.cs
autonomous: true
requirements: [TOK-01, TOK-02]

must_haves:
  truths:
    - "Il n'existe plus qu'UN SEUL rafraîchisseur dans l'application : le provider d'usage a perdu son refresh paresseux et ne connaît plus ni le coffre ni le client OAuth."
    - "Un 401 sur l'endpoint d'usage invalide le jeton, rejoue l'appel UNE seule fois avec un jeton frais, puis déclare « déconnecté » — le serveur peut révoquer avant l'ExpiresAt local, le préventif seul ne suffit pas."
    - "Un second 401 après rejeu ne relance pas de boucle : une garde locale interdit tout rejeu supplémentaire."
    - "Une réponse 2xx exploitée déverrouille l'état d'authentification : la pastille pourra disparaître dès qu'un chiffre exact est de nouveau obtenu."
    - "Le graphe DI réel résout l'autorité, le service de fond et le provider, et l'exe démarre : la garde de composition reproduit la sous-chaîne d'App.xaml.cs sur un coffre en dossier temporaire."
    - "Le diagnostic nomme l'état d'authentification RÉEL au lieu de se contenter de « le fichier existe » — IsLoggedIn n'a jamais signifié « authentifié »."
  artifacts:
    - path: "src/Chronos/Services/ChronosOAuthUsageProvider.cs"
      provides: "Provider consommateur de l'autorité : plus aucun refresh, rejeu unique sur 401, signalement d'état"
      contains: "ChronosTokenAuthority"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Câblage DI : autorité singleton, alias IAuthStatus, service de fond hébergé"
      contains: "AddHostedService"
    - path: "src/Chronos/Services/DiagnosticService.cs"
      provides: "Ligne d'état d'authentification réel, via un paramètre ctor OPTIONNEL (0 site cassé)"
      contains: "IAuthStatus? authStatus = null"
  key_links:
    - from: "src/Chronos/Services/ChronosOAuthUsageProvider.cs"
      to: "ChronosTokenAuthority.GetAccessTokenAsync"
      via: "seule source de jeton du provider"
      pattern: "_autorite\\.GetAccessTokenAsync"
    - from: "src/Chronos/Services/ChronosOAuthUsageProvider.cs"
      to: "ChronosTokenAuthority.InvaliderAccessToken"
      via: "chemin 401 → invalidation → rejeu unique"
      pattern: "InvaliderAccessToken"
    - from: "src/Chronos/App.xaml.cs"
      to: "TokenRefreshService"
      via: "AddSingleton + AddHostedService sur la MÊME instance"
      pattern: "AddHostedService\\(sp => sp.GetRequiredService<TokenRefreshService>\\(\\)\\)"
    - from: "src/Chronos/App.xaml.cs"
      to: "IAuthStatus"
      via: "alias de la MÊME instance d'autorité (jamais une seconde)"
      pattern: "AddSingleton<IAuthStatus>"
---

<objective>
Brancher l'autorité de jeton dans l'application réelle et **supprimer le second rafraîchisseur**.

Purpose: à ce stade, l'autorité existe et tourne, mais `ChronosOAuthUsageProvider` conserve son refresh
paresseux. Deux rafraîchisseurs coexistants = deux rotations concurrentes du refresh token = un
`invalid_grant` sur le second = une **fausse déconnexion** sur un compte sain. Il n'y a donc pas de
demi-mesure possible : le provider doit perdre le droit de rafraîchir dans le même geste où l'autorité
est câblée. C'est aussi ici qu'on ferme le trou signalé par la recherche : le serveur peut révoquer un
jeton **avant** son `ExpiresAt` local (cas documenté `claude-code#54443`), donc le rafraîchissement
préventif seul ne suffit pas — le chemin « 401 → invalider → un seul rejeu → sinon Deconnecte » est
obligatoire, pas un raffinement.

Output: provider réécrit en consommateur, `App.xaml.cs` câblé (autorité + alias `IAuthStatus` + service
hébergé), `DiagnosticService` qui dit enfin l'état réel, et les trois fichiers de test correspondants.

**Deux tâches, deux commits, et la solution compile à chacun des deux.** La réécriture du ctor du
provider et l'adaptation de son unique site de construction dans `App.xaml.cs` sont **le même geste** :
les séparer produirait un commit intermédiaire où `dotnet build` échoue — et où `dotnet test` ne peut
même pas s'exécuter, puisque `tests/Chronos.Tests` référence le projet `Chronos`. C'est l'invariant
« solution compilable à chaque commit atomique » tenu par les phases 15 et 16 et par le plan 17-02
(tâche 2, étape 5 : « Garder la solution compilable — adapter l'UNIQUE appelant »).

À l'issue de ce plan, TOK-01 est **fonctionnellement acquis** : un exe laissé tourner voit son jeton
renouvelé tout seul. Ce qui manque encore est uniquement la **visibilité** (plan 17-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-VALIDATION.md
@.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrats DISPONIBLES après les plans 17-02 et 17-03. -->

```csharp
// src/Chronos/Services/ChronosTokenAuthority.cs (plan 17-03)
public sealed class ChronosTokenAuthority : IAuthStatus, IDisposable
{
    public ChronosTokenAuthority(ChronosOAuthStore coffre, ChronosOAuthClient client, IClock horloge);
    public EtatAuthentification Etat { get; }
    public event EventHandler<EtatAuthentification>? EtatChange;
    public Task<string?> GetAccessTokenAsync(CancellationToken ct = default);  // SEULE sortie de jeton
    public void InvaliderAccessToken();      // après un 401 sur l'usage : force un refresh au prochain appel
    public void SignalerSucces();            // un 2xx exploité déverrouille l'état
    public void SignalerRefusServeur();      // 401/403 sur un jeton FRAIS = refus de compte
    public void ReinitialiserApresLogin();
    public void Dispose();
}

// src/Chronos/Services/TokenRefreshService.cs (plan 17-03)
public sealed class TokenRefreshService : IHostedService, IDisposable
{
    public TokenRefreshService(ChronosTokenAuthority autorite);
    public Task StartAsync(CancellationToken ct);
    public Task TickAsync();
    public Task StopAsync(CancellationToken ct);
}

// src/Chronos/Services/EtatAuthentification.cs (plan 17-02)
public enum EtatAuthentification { NonConnecte, Connecte, HorsLigne, Deconnecte }
```

**Les 4 sites d'`App.xaml.cs` qui mentionnent la sous-chaîne OAuth** (numéros de ligne VÉRIFIÉS dans le
fichier au 2026-09-09) :

| Lignes | Contenu | Sort dans ce plan |
|---|---|---|
| 279-282 | commentaire « v2.1 », `AddSingleton<ChronosOAuthStore>`, `AddSingleton(... new ChronosOAuthClient(new HttpClient()))` | **conservés** (le coffre et le client restent nécessaires à l'autorité ET au login) ; le client gagne son `IClock` |
| **283-287** | `new ChronosOAuthUsageProvider(store, client, http, clock)` — **ancien ctor à 4 arguments** | **SEUL site de construction du provider** : réécrit à 3 arguments **dans la tâche 1**, sinon la solution ne compile plus |
| 288-290 | `AddSingleton<IOAuthLogin>(... new Views.OAuthLogin(client, store))` | **inchangé** — le login continue de parler au coffre et au client directement (c'est lui l'écrivain du premier jeton) |
| 300 | `primary: sp.GetRequiredService<ChronosOAuthUsageProvider>()` dans le composite | **inchangé** — c'est une RÉSOLUTION par type, pas une construction : elle survit au changement de ctor |
| 320-325 | `new DiagnosticService(...)` à 5 arguments | **tâche 2** : gagne un 6ᵉ argument, APRÈS que le paramètre optionnel a été créé |

**Câblage DI ACTUEL à modifier** — `src/Chronos/App.xaml.cs`, lignes 279-290 (extrait littéral) :

```csharp
        // v2.1 : SOURCE EXACTE PRIMAIRE = login OAuth propre à Chronos (jeton obtenu par login navigateur,
        // rafraîchi tout seul, stocké chiffré DPAPI). Marche que l'utilisateur soit en app bureau OU terminal.
        services.AddSingleton<ChronosOAuthStore>(_ => new ChronosOAuthStore());
        services.AddSingleton(_ => new ChronosOAuthClient(new HttpClient()));
        services.AddSingleton(sp => new ChronosOAuthUsageProvider(
            sp.GetRequiredService<ChronosOAuthStore>(),
            sp.GetRequiredService<ChronosOAuthClient>(),
            new HttpClient(),
            sp.GetRequiredService<IClock>()));
        services.AddSingleton<IOAuthLogin>(sp => new Views.OAuthLogin(
            sp.GetRequiredService<ChronosOAuthClient>(),
            sp.GetRequiredService<ChronosOAuthStore>()));
```

**Motif d'instance unique réexposée** déjà employé dans le même fichier (lignes 316-317) — à copier :

```csharp
        services.AddSingleton<RefreshOrchestrator>();
        services.AddHostedService(sp => sp.GetRequiredService<RefreshOrchestrator>());
```

**Ctor de `DiagnosticService` ACTUEL** (`DiagnosticService.cs:30-31`) — 8 sites de construction
(1 en prod, 7 en tests sur 5 fichiers). Le paramètre ajouté DOIT être **optionnel et en dernière
position** pour ramener le rayon de souffle à **0** :

```csharp
    public DiagnosticService(IClaudeTokenReader tokenReader, ChronosPaths paths,
                             SettingsService settings, IUsageProvider composite, IClock clock)
```

**Assertions de `DiagnosticServiceTests` à préserver MOT POUR MOT** (lignes 42-59) :
`"Diagnostic"`, `"Usage exact (OAuth)"`, `"Token déchiffré : OUI"`, `"Token déchiffré : NON"`,
`"estimé"`, `"Conseil"`, et `Assert.DoesNotContain("SECRET-TOKEN", report)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Le provider perd son refresh, gagne le rejeu unique sur 401, et App.xaml.cs suit dans le MÊME commit</name>

  <read_first>
    - src/Chronos/Services/ChronosOAuthUsageProvider.cs (intégralement, 144 lignes)
    - src/Chronos/Services/ChronosTokenAuthority.cs et TokenRefreshService.cs (livrés par 17-03)
    - src/Chronos/App.xaml.cs (lignes 250-330 — toute la section des sources exactes)
    - src/Chronos/Services/ChronosOAuthStore.cs (le champ `_path` à exposer en lecture)
    - tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs (livré par 17-01, adapté par 17-02)
    - tests/Chronos.Tests/CompositionRootTests.cs (intégralement — le conteneur miroir et ses gardes)
    - .planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-RESEARCH.md
      (§ Pattern 3b, § Pitfall 2, § Open Question 2)
  </read_first>

  <files>
src/Chronos/Services/ChronosOAuthUsageProvider.cs
src/Chronos/Services/ChronosOAuthStore.cs
src/Chronos/App.xaml.cs
tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs
tests/Chronos.Tests/CompositionRootTests.cs
  </files>

  <behavior>
    - Autorité sans jeton → `UsageSnapshot.Empty`, **aucune** requête d'usage (`usage.SendCount == 0`).
    - 200 nominal → snapshot exact identique à l'existant (0.42 / 0.63, `Exact`, `SourceCapturedAt`),
      **et** `autorite.Etat == Connecte` (le succès déverrouille l'état).
    - En-têtes et URL inchangés (les tests de la Wave 0 restent verts sans retouche d'assertion).
    - **401 puis 401** → `usage.SendCount == 2` (un rejeu et **un seul**), `autorite.Etat == Deconnecte`,
      snapshot `Empty`. Un 3ᵉ `GetAsync` ne relance **pas** de nouveau rejeu.
    - **401 puis 200** → `usage.SendCount == 2`, snapshot exact rendu, `autorite.Etat == Connecte` :
      le jeton avait été révoqué avant son `ExpiresAt` local, le rejeu répare tout seul.
    - **403** → `autorite.Etat == Deconnecte`, sans rejeu (`usage.SendCount == 1`) : scope/abonnement,
      un rafraîchissement n'y changera rien.
    - **429 / 5xx / exception réseau** → comportement inchangé (`Empty`, recul), et
      `autorite.Etat` n'est **PAS** mis à `Deconnecte` (ce serait la fausse alerte que TOK-02 interdit).
    - **Le provider ne rafraîchit plus** : avec un jeton expiré dans le coffre, le nombre de requêtes
      vers le point de terminaison de jeton est **exactement 1** (celle de l'autorité), pas 2.
    - **Le graphe DI réel se résout** : `ChronosTokenAuthority` et `IAuthStatus` sont LA MÊME instance,
      `TokenRefreshService` est enregistré comme `IHostedService`, le provider se construit — et le
      coffre du test pointe dans `Path.GetTempPath()`, jamais dans le vrai `%APPDATA%\Chronos\`.
  </behavior>

  <action>
**Périmètre de cette tâche — un seul commit, la solution compile à sa frontière.** Le ctor du provider
change de forme ; son unique site de construction (`App.xaml.cs:283-287`) est adapté ici même. Les
séparer laisserait un commit où `dotnet build` échoue et où `dotnet test` ne peut PAS s'exécuter du tout
(`tests/Chronos.Tests` a un `ProjectReference` vers `Chronos`). Même motif que le plan 17-02, tâche 2,
étape 5.

1. Réécrire l'en-tête de `src/Chronos/Services/ChronosOAuthUsageProvider.cs` :

   - **Supprimer** les champs `_store`, `_client` et la constante `RefreshMargin` (ligne 30).
     Conserver `MinInterval`, `Backoff429`, `CacheUsable`, `Timeout`, `UsageUrl`, `UserAgent`.
   - Nouveau ctor :

```csharp
    private readonly ChronosTokenAuthority _autorite;
    private readonly HttpClient _http;
    private readonly IClock _clock;

    public ChronosOAuthUsageProvider(ChronosTokenAuthority autorite, HttpClient http, IClock clock)
    {
        _autorite = autorite;
        _http = http;
        _clock = clock;
    }
```
   - Mettre à jour la XML-doc de classe : le provider **ne rafraîchit plus** ; il demande un jeton valide
     à l'autorité unique. Écrire explicitement le POURQUOI : « deux rafraîchisseurs concurrents
     présentant le même refresh token produisent un invalid_grant sur le second, donc une fausse
     déconnexion (bug claude-code#25609). »

2. Réécrire `GetAsync` (remplace les lignes 49-119). Supprimer intégralement le bloc de rafraîchissement
   paresseux (lignes 60-75) :

```csharp
    public async Task<UsageSnapshot> GetAsync(CancellationToken ct = default)
    {
        var now = _clock.UtcNow;

        if (now < _nextAllowedCall && _cached is not null)
            return _cached;

        // SOURCE UNIQUE DE JETON. Le provider ne connaît ni le coffre, ni le refresh token, ni le
        // client OAuth : l'autorité sérialise, rafraîchit et classe les échecs pour tout le monde.
        var jeton = await _autorite.GetAccessTokenAsync(ct);
        if (jeton is null)
            return ServeCachedOr(UsageSnapshot.Empty, now);   // état déjà publié par l'autorité

        try
        {
            var resp = await EnvoyerAsync(jeton, ct);
            var rejoue = false;

            // Le serveur peut révoquer un jeton AVANT son ExpiresAt local (cas documenté
            // claude-code#54443) : le rafraîchissement préventif ne suffit donc pas. UN SEUL rejeu —
            // sans cette garde, on boucle refresh+401 jusqu'à déclencher un 429 sur l'endpoint de jeton.
            if ((int)resp.StatusCode == 401 && !rejoue)
            {
                resp.Dispose();
                rejoue = true;
                _autorite.InvaliderAccessToken();
                var frais = await _autorite.GetAccessTokenAsync(ct);
                if (frais is null)
                {
                    _nextAllowedCall = now + MinInterval;
                    return ServeCachedOr(UsageSnapshot.Empty, now);
                }
                resp = await EnvoyerAsync(frais, ct);
            }

            using (resp)
            {
                var code = (int)resp.StatusCode;

                if (code is 401 or 403)
                {
                    // Jeton FRAIS et pourtant refusé (ou scope insuffisant) : ce n'est pas un problème
                    // de fraîcheur, c'est un refus de compte. Seule une reconnexion répare.
                    _autorite.SignalerRefusServeur();
                    _nextAllowedCall = now + MinInterval;
                    return ServeCachedOr(UsageSnapshot.Empty, now);
                }
                if (code == 429)
                {
                    // TEMPORAIRE : ne JAMAIS déclarer une déconnexion sur un rate-limit (fausse alerte).
                    _nextAllowedCall = now + Backoff429;
                    return ServeCachedOr(UsageSnapshot.Empty, now);
                }
                if (!resp.IsSuccessStatusCode)
                {
                    _nextAllowedCall = now + MinInterval;
                    return ServeCachedOr(UsageSnapshot.Empty, now);
                }

                await using var stream = await resp.Content.ReadAsStreamAsync(ct);
                using var doc = await JsonDocument.ParseAsync(stream, cancellationToken: ct);
                var root = doc.RootElement;

                var snap = new UsageSnapshot
                {
                    FiveHour = Read(root, "five_hour", WindowKind.FiveHour, TimeSpan.FromHours(5)),
                    SevenDay = Read(root, "seven_day", WindowKind.SevenDay, TimeSpan.FromDays(7)),
                    SourceCapturedAt = now,
                };
                _cached = snap; _cachedAt = now; _nextAllowedCall = now + MinInterval;
                _autorite.SignalerSucces();   // un 2xx exploité déverrouille l'état (critère de succès 3)
                return snap;
            }
        }
        catch (Exception ex) when (ex is HttpRequestException or TaskCanceledException
                                     or OperationCanceledException or JsonException or IOException)
        {
            // Réseau/timeout : surtout PAS de « déconnecté » — c'est peut-être seulement le wifi.
            _nextAllowedCall = now + MinInterval;
            return ServeCachedOr(UsageSnapshot.Empty, now);
        }
    }

    // Envoi d'UNE requête d'usage avec le jeton fourni. SÉCURITÉ : le jeton ne vit qu'en variable
    // locale, le temps de construire l'en-tête Authorization — jamais journalisé, jamais en URL.
    private async Task<HttpResponseMessage> EnvoyerAsync(string jeton, CancellationToken ct)
    {
        using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
        cts.CancelAfter(Timeout);

        using var req = new HttpRequestMessage(HttpMethod.Get, UsageUrl);
        req.Headers.TryAddWithoutValidation("Authorization", "Bearer " + jeton);
        req.Headers.TryAddWithoutValidation("anthropic-beta", "oauth-2025-04-20");
        req.Headers.TryAddWithoutValidation("User-Agent", UserAgent);

        return await _http.SendAsync(req, cts.Token);
    }
```
   ATTENTION : `rejoue` est déclaré **avant** le `if` et n'est jamais remis à `false` — c'est la garde
   qui interdit le second rejeu. Si le compilateur signale que `!rejoue` est toujours vrai à cet endroit,
   c'est acceptable : garder la variable et la condition, elles documentent l'invariant et protègent une
   éventuelle transformation ultérieure en boucle. Alternative acceptée : extraire le rejeu dans une
   méthode privée `RejouerUneFoisAsync`.
   `ServeCachedOr` et `Read` sont **inchangés**.

3. `src/Chronos/Services/ChronosOAuthStore.cs` — ajouter **une seule** propriété, additive
   (motif identique à `LastExactStore.Path`, exigé par la garde anti-accident des tests) :

```csharp
    /// <summary>Chemin du coffre. Exposé pour les GARDES DE TEST (« ce test écrit-il bien dans un
    /// dossier temporaire ? ») — SÉCURITÉ : c'est le CHEMIN qui sort, jamais le contenu.</summary>
    public string Path => _path;
```
   Attention aux collisions de nom avec `System.IO.Path` dans ce fichier : qualifier les appels
   existants en `System.IO.Path.Combine(...)` / `System.IO.Path.GetDirectoryName(...)` si nécessaire.
   **Ne rien changer d'autre** dans ce fichier (`Save`, `Load`, `Clear` restent mot pour mot) — et en
   particulier ne JAMAIS appeler `.Clear()` sur le coffre depuis quoi que ce soit de ce plan.

4. `src/Chronos/App.xaml.cs` — remplacer le bloc « v2.1 : SOURCE EXACTE PRIMAIRE » (**lignes 279-290**,
   les deux lignes de commentaire incluses) par le bloc ci-dessous. C'est ici qu'est adapté l'**unique
   site de construction** du provider (ancien `new ChronosOAuthUsageProvider(...)` à 4 arguments,
   lignes 283-287) :

```csharp
        // v2.1 : SOURCE EXACTE PRIMAIRE = login OAuth propre à Chronos (jeton obtenu par login
        // navigateur, stocké chiffré DPAPI). Marche que l'utilisateur soit en app bureau OU terminal.
        services.AddSingleton<ChronosOAuthStore>(_ => new ChronosOAuthStore());
        services.AddSingleton(sp => new ChronosOAuthClient(new HttpClient(), sp.GetRequiredService<IClock>()));

        // TOK-01/TOK-02 — AUTORITÉ UNIQUE DE JETON. Un seul objet du processus a le droit d'appeler
        // RefreshAsync et d'écrire dans le coffre. Deux rafraîchisseurs présenteraient le même refresh
        // token et le second récolterait un invalid_grant : une FAUSSE déconnexion sur un compte sain
        // (bug documenté anthropics/claude-code#25609). Enregistrée UNE fois et réexposée comme
        // IAuthStatus sur la MÊME instance — surtout pas une seconde autorité.
        services.AddSingleton(sp => new ChronosTokenAuthority(
            sp.GetRequiredService<ChronosOAuthStore>(),
            sp.GetRequiredService<ChronosOAuthClient>(),
            sp.GetRequiredService<IClock>()));
        services.AddSingleton<IAuthStatus>(sp => sp.GetRequiredService<ChronosTokenAuthority>());

        // TOK-01 — horloge de fond du jeton : tick 60 s, PREMIER TICK IMMÉDIAT. Même motif d'instance
        // unique réexposée en IHostedService que RefreshOrchestrator ci-dessous.
        services.AddSingleton(sp => new TokenRefreshService(sp.GetRequiredService<ChronosTokenAuthority>()));
        services.AddHostedService(sp => sp.GetRequiredService<TokenRefreshService>());

        // Le provider d'usage n'est plus qu'un CONSOMMATEUR de l'autorité : il ne connaît ni le coffre,
        // ni le refresh token, ni le client OAuth.
        services.AddSingleton(sp => new ChronosOAuthUsageProvider(
            sp.GetRequiredService<ChronosTokenAuthority>(),
            new HttpClient(),
            sp.GetRequiredService<IClock>()));

        // Le login reste l'écrivain du PREMIER jeton : il parle au coffre et au client directement.
        services.AddSingleton<IOAuthLogin>(sp => new Views.OAuthLogin(
            sp.GetRequiredService<ChronosOAuthClient>(),
            sp.GetRequiredService<ChronosOAuthStore>()));
```

   Le reste du fichier ne bouge pas dans cette tâche :
   - **ligne 300** — `primary: sp.GetRequiredService<ChronosOAuthUsageProvider>()` : c'est une
     RÉSOLUTION par type, pas une construction. Elle reste valide telle quelle : **ne pas y toucher**.
   - **lignes 292-306** — bloc `LastExactUsageProvider` / `CompositeUsageProvider` : la doctrine de
     choix de source reste la phase 19. **Ne pas y toucher.**
   - **lignes 320-325** — `new DiagnosticService(...)` : son 6ᵉ argument est ajouté par la **tâche 2**,
     qui crée d'abord le paramètre optionnel correspondant. Le toucher ici casserait la compilation.

5. Adapter `tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` :

   - **Réécrire le helper unique** `Provider(...)` (point de changement prévu dès le plan 17-01) :

```csharp
    private static (ChronosOAuthUsageProvider provider, ChronosTokenAuthority autorite) Provider(
        DateTimeOffset? expiration, FakeHttpMessageHandler usage, FakeClock horloge,
        FakeHttpMessageHandler? jeton = null)
    {
        var chemin = CheminCoffreTemp();
        var coffre = new ChronosOAuthStore(chemin);
        if (expiration is { } e) coffre.Save(new OAuthTokens("ACC-VALIDE", "REF-VALIDE", e));
        var client = new ChronosOAuthClient(
            new HttpClient(jeton ?? FakeHttpMessageHandler.Json(HttpStatusCode.OK,
                """{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}""")),
            horloge);
        var autorite = new ChronosTokenAuthority(coffre, client, horloge);
        return (new ChronosOAuthUsageProvider(autorite, new HttpClient(usage), horloge), autorite);
    }
```
     Les tests existants s'adaptent en destructurant le tuple ; **aucune assertion de comportement HTTP
     n'est modifiée** (en-têtes, unités, 429, cache, exception réseau restent tels quels).
     `CheminCoffreTemp()` reste basé sur `Path.GetTempPath()` : **aucun test ne lit ni n'écrit**
     `%APPDATA%\Chronos\oauth.dat`.

   - **RÉÉCRIRE** (ne pas supprimer) `Un_401_est_aujourd_hui_totalement_muet` en :

```csharp
    /// <summary>
    /// TOK-02 — remplace `Un_401_est_aujourd_hui_totalement_muet` (plan 17-01, qui documentait le 401
    /// muet de deux mois). Le serveur peut révoquer un jeton avant son ExpiresAt local : on invalide,
    /// on rejoue UNE fois avec un jeton frais, et si le refus persiste on le DIT.
    /// </summary>
    [Fact]
    public async Task Un_401_persistant_declenche_UN_seul_rejeu_puis_declare_Deconnecte()
    {
        var usage = FakeHttpMessageHandler.Json(HttpStatusCode.Unauthorized, """{"error":"unauthorized"}""");
        var (p, autorite) = Provider(Maintenant.AddHours(1), usage, new FakeClock(Maintenant));

        var snap = await p.GetAsync();

        Assert.Equal(2, usage.SendCount);                              // un rejeu, UN SEUL
        Assert.Equal(EtatAuthentification.Deconnecte, autorite.Etat);  // plus jamais muet
        Assert.Equal(SourceReliability.Unavailable, snap.FiveHour.Reliability);
    }
```
     Et ajouter `Un_401_suivi_d_un_200_rend_des_chiffres_exacts_sans_rien_signaler` : responder à
     compteur (`new FakeHttpMessageHandler(_ => premier ? 401 : 200)`), `SendCount == 2`,
     `autorite.Etat == Connecte`, `snap.FiveHour.Utilization == 0.42`.

   - **RÉÉCRIRE** `Le_rafraichissement_est_aujourd_hui_paresseux_declenche_par_le_GetAsync` en :

```csharp
    /// <summary>
    /// TOK-01 — remplace le test qui documentait le refresh PARESSEUX. Le provider ne rafraîchit plus :
    /// il n'existe qu'un seul rafraîchisseur dans l'application. Deux auraient produit deux rotations
    /// concurrentes du refresh token, donc une FAUSSE déconnexion (bug claude-code#25609).
    /// </summary>
    [Fact]
    public async Task Le_provider_ne_rafraichit_plus_lui_meme_il_consomme_l_autorite()
    {
        var jeton = FakeHttpMessageHandler.Json(HttpStatusCode.OK,
            """{"access_token":"ACC-2","refresh_token":"REF-2","expires_in":3600}""");
        var usage = FakeHttpMessageHandler.Json(HttpStatusCode.OK, CorpsNominal);
        var (p, _) = Provider(Maintenant.AddMinutes(-1), usage, new FakeClock(Maintenant), jeton);

        await p.GetAsync();
        await p.GetAsync();   // second appel : le jeton est frais, aucune nouvelle rotation

        Assert.Equal(1, jeton.SendCount);   // UNE rotation au total, portée par l'autorité
    }
```

   - Ajouter `Un_429_ne_declare_JAMAIS_une_deconnexion` :
     `Assert.NotEqual(EtatAuthentification.Deconnecte, autorite.Etat)`.
   - Ajouter `Un_403_declare_Deconnecte_sans_rejeu` : `usage.SendCount == 1`.

6. `tests/Chronos.Tests/CompositionRootTests.cs` — deux modifications.

   a) Dans `Host_resout_et_dispose_les_singletons`, à côté de l'enregistrement existant d'`IOAuthLogin`
      (ligne ~89), ajouter :

```csharp
        // Phase 17 : le ctor de MainViewModel dépendra d'IAuthStatus (plan 17-05). L'enregistrer dès
        // maintenant évite une rupture de garde au plan suivant ; le faux suffit ici.
        services.AddSingleton<IAuthStatus>(_ => new FakeAuthStatus());
```

   b) Ajouter une garde DI RÉELLE, sur le modèle des gardes analogues des phases 13 et 15 — elle attrape
      un graphe qui compile mais plante au démarrage :

```csharp
    /// <summary>
    /// GARDE DI RÉELLE (phase 17). Reproduit la sous-chaîne OAuth d'App.ConfigureServices : coffre
    /// (sur chemin TEMPORAIRE), client, autorité unique, service de fond hébergé, provider consommateur.
    /// Prouve trois choses qu'un `dotnet build` ne prouve pas : que le graphe se résout, que
    /// ChronosTokenAuthority et IAuthStatus sont bien LA MÊME instance (une seconde autorité
    /// rouvrirait la course de rotation du refresh token), et que le service de fond est bien
    /// enregistré comme IHostedService.
    /// </summary>
    [Fact]
    public void Le_graphe_DI_resout_l_autorite_de_jeton_et_son_service_de_fond()
    {
        var dir = System.IO.Path.Combine(System.IO.Path.GetTempPath(), "ChronosDiTest_" + System.Guid.NewGuid().ToString("N"));
        System.IO.Directory.CreateDirectory(dir);

        var services = new ServiceCollection();
        services.AddSingleton<IClock>(_ => new SystemClock());
        services.AddSingleton(_ => new ChronosOAuthStore(System.IO.Path.Combine(dir, "oauth.dat")));
        services.AddSingleton(sp => new ChronosOAuthClient(new HttpClient(), sp.GetRequiredService<IClock>()));
        services.AddSingleton(sp => new ChronosTokenAuthority(
            sp.GetRequiredService<ChronosOAuthStore>(),
            sp.GetRequiredService<ChronosOAuthClient>(),
            sp.GetRequiredService<IClock>()));
        services.AddSingleton<IAuthStatus>(sp => sp.GetRequiredService<ChronosTokenAuthority>());
        services.AddSingleton(sp => new TokenRefreshService(sp.GetRequiredService<ChronosTokenAuthority>()));
        services.AddHostedService(sp => sp.GetRequiredService<TokenRefreshService>());
        services.AddSingleton(sp => new ChronosOAuthUsageProvider(
            sp.GetRequiredService<ChronosTokenAuthority>(),
            new HttpClient(),
            sp.GetRequiredService<IClock>()));

        using var provider = services.BuildServiceProvider();

        // Garde anti-accident : aucun test n'écrit dans le vrai %APPDATA%\Chronos.
        Assert.StartsWith(System.IO.Path.GetTempPath(), provider.GetRequiredService<ChronosOAuthStore>().Path);

        var autorite = provider.GetRequiredService<ChronosTokenAuthority>();
        Assert.NotNull(autorite);
        Assert.Same(autorite, provider.GetRequiredService<IAuthStatus>());   // UNE seule autorité
        Assert.NotNull(provider.GetRequiredService<ChronosOAuthUsageProvider>());
        Assert.Contains(provider.GetServices<IHostedService>(), s => s is TokenRefreshService);
    }
```
      Ajouter `[Collection("XAML WPF")]` **uniquement si** la classe le porte déjà (elle le porte depuis
      la phase 16) — ne pas modifier l'attribut de classe.

**INTERDITS de cette tâche :** ne PAS laisser une seule occurrence de `RefreshAsync`, `ChronosOAuthStore`
ou `ChronosOAuthClient` dans `ChronosOAuthUsageProvider.cs`. Ne PAS supprimer de test (réécrire). Ne PAS
toucher à `ClaudeOAuthUsageProvider.cs`, `GatedOAuthUsageProvider.cs`, `CompositeUsageProvider.cs`,
`LastExactUsageProvider.cs`, ni au bloc composite d'`App.xaml.cs`. Ne PAS toucher à l'enregistrement de
`DiagnosticService` (tâche 2). Ne PAS enregistrer deux instances de `ChronosTokenAuthority`. Ne PAS
modifier la logique de `ChronosOAuthStore` au-delà de la propriété `Path`, et ne JAMAIS appeler
`.Clear()` sur le coffre. Le test DI ne doit **jamais** appeler `GetAccessTokenAsync` : aucune requête
réseau ne part de cette garde, et aucun test n'appelle l'endpoint de refresh avec le refresh token RÉEL.
Aucun test ne lit ni n'écrit `%APPDATA%\Chronos\oauth.dat`. Aucun jeton logué ni écrit en clair.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo && dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthUsageProviderTests|FullyQualifiedName~CompositionRootTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -v q --nologo` → **0 erreur** (la solution recompile intégralement à ce
      commit : le ctor et son unique site de construction ont changé ensemble).
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ChronosOAuthUsageProviderTests|FullyQualifiedName~CompositionRootTests"` →
      **0 échec**, **≥ 18** tests exécutés (**≥ 14** côté provider, **≥ 4** côté composition).
    - `grep -cE "RefreshAsync|ChronosOAuthStore|ChronosOAuthClient|RefreshMargin" src/Chronos/Services/ChronosOAuthUsageProvider.cs` → **0**.
    - `grep -c "_autorite.GetAccessTokenAsync" src/Chronos/Services/ChronosOAuthUsageProvider.cs` → **2**
      (appel initial + rejeu).
    - `grep -c "InvaliderAccessToken" src/Chronos/Services/ChronosOAuthUsageProvider.cs` → **1**.
    - `grep -c "SignalerSucces" src/Chronos/Services/ChronosOAuthUsageProvider.cs` → **1** ;
      `grep -c "SignalerRefusServeur"` → **1**.
    - `grep -c "AddSingleton<IAuthStatus>" src/Chronos/App.xaml.cs` → **1**.
    - `grep -c "new ChronosTokenAuthority(" src/Chronos/App.xaml.cs` → **1** (une seule autorité).
    - `grep -c "AddHostedService" src/Chronos/App.xaml.cs` → **3**
      (`DesktopUiaPollService`, `RefreshOrchestrator`, `TokenRefreshService`).
    - `grep -c "new ChronosOAuthUsageProvider(" src/Chronos/App.xaml.cs` → **1**, et le bloc ne
      mentionne plus `ChronosOAuthStore` ni `ChronosOAuthClient` en argument :
      `sed -n '/new ChronosOAuthUsageProvider(/,/));/p' src/Chronos/App.xaml.cs | grep -cE "ChronosOAuthStore|ChronosOAuthClient"` → **0**.
    - `grep -c "new CompositeUsageProvider(" src/Chronos/App.xaml.cs` → **2** (imbrication inchangée) ;
      `grep -c "new LastExactUsageProvider(" src/Chronos/App.xaml.cs` → **1** ;
      `grep -c "new Views.OAuthLogin(" src/Chronos/App.xaml.cs` → **1** (login intact).
    - `sed -n '/new DiagnosticService(/,/));/p' src/Chronos/App.xaml.cs | grep -c "IAuthStatus"` → **0**
      (le diagnostic est la tâche 2, il n'a pas été touché ici).
    - `grep -c "public string Path => _path;" src/Chronos/Services/ChronosOAuthStore.cs` → **1**, et
      `git diff --stat src/Chronos/Services/ChronosOAuthStore.cs` → **≤ 8 lignes changées** ;
      `git diff -- src/Chronos/Services/ChronosOAuthStore.cs | grep -c "Clear"` → **0**.
    - `grep -c "new ChronosOAuthUsageProvider(" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → **1**
      (le helper reste le point unique).
    - `grep -n "Un_401_est_aujourd_hui_totalement_muet\|Le_rafraichissement_est_aujourd_hui_paresseux" tests/` →
      **aucun résultat** (réécrits) ; `grep -c "Un_401_persistant_declenche_UN_seul_rejeu_puis_declare_Deconnecte" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs` → **1**.
    - `grep -c "Le_graphe_DI_resout_l_autorite_de_jeton_et_son_service_de_fond" tests/Chronos.Tests/CompositionRootTests.cs` → **1** ;
      `grep -c "Assert.Same(autorite" tests/Chronos.Tests/CompositionRootTests.cs` → **1**.
    - `git diff --name-only -- src/Chronos/Services/CompositeUsageProvider.cs src/Chronos/Services/LastExactUsageProvider.cs src/Chronos/Services/ClaudeOAuthUsageProvider.cs src/Chronos/Services/GatedOAuthUsageProvider.cs src/Chronos/Views/OAuthLogin.cs` → **vide**.
    - `grep -rn "APPDATA\|ApplicationData" tests/Chronos.Tests/ChronosOAuthUsageProviderTests.cs tests/Chronos.Tests/CompositionRootTests.cs` → **aucun résultat**
      (les coffres de test vivent dans `Path.GetTempPath()`).
    - Après la suite : le coffre réel `%APPDATA%\Chronos\oauth.dat` a la **même taille et le même
      horodatage** qu'avant.
  </acceptance_criteria>

  <done>
Le provider ne sait plus rafraîchir : il demande, il rejoue une fois sur 401, il signale succès et refus.
Il n'existe plus qu'un seul rafraîchisseur dans le code, et l'exe démarre avec une autorité unique
réexposée comme `IAuthStatus` plus une horloge de fond hébergée. La solution compile à ce commit, la
garde DI prouve l'unicité de l'instance sur un coffre temporaire, les tests de la Wave 0 ont été
réécrits, aucun supprimé.
  </done>
</task>

<task type="auto">
  <name>Task 2: Le diagnostic dit l'état d'authentification RÉEL — paramètre optionnel, 0 site cassé</name>

  <read_first>
    - src/Chronos/Services/DiagnosticService.cs (lignes 20-40 pour le ctor, 86-93 pour la section OAuth Chronos)
    - tests/Chronos.Tests/DiagnosticServiceTests.cs (intégralement — les 7 assertions de chaîne à préserver)
    - src/Chronos/Services/EtatAuthentification.cs (plan 17-02)
    - src/Chronos/App.xaml.cs (lignes 319-325 — l'enregistrement du diagnostic, seul site de production)
  </read_first>

  <files>
src/Chronos/Services/DiagnosticService.cs
src/Chronos/App.xaml.cs
tests/Chronos.Tests/DiagnosticServiceTests.cs
  </files>

  <behavior>
    - Sans autorité injectée (paramètre omis) : le rapport contient
      `"État d'authentification : (inconnu"` et **aucune** exception.
    - Autorité en `Deconnecte` : le rapport contient
      `"État d'authentification : DÉCONNECTÉ"`.
    - Autorité en `HorsLigne` : le rapport contient `"État d'authentification : HORS LIGNE"`.
    - Les sept assertions existantes restent vraies **mot pour mot** : `"Diagnostic"`,
      `"Usage exact (OAuth)"`, `"Token déchiffré : OUI"`, `"Token déchiffré : NON"`, `"estimé"`,
      `"Conseil"`, et le token n'apparaît jamais.
    - Aucun site de construction existant n'est cassé : les 8 appels à `new DiagnosticService(...)`
      à 5 arguments compilent tels quels.
  </behavior>

  <action>
1. `src/Chronos/Services/DiagnosticService.cs` — ajouter le champ et **un paramètre optionnel en
   DERNIÈRE position**. C'est le levier qui ramène 8 sites de construction cassés à **0**, et qui
   permet d'ajouter le 6ᵉ argument d'`App.xaml.cs` (étape 4) **dans le même commit sans jamais casser
   la compilation** :

```csharp
    private readonly IAuthStatus? _authStatus;

    /// <param name="authStatus">État d'authentification réel (autorité de jeton). OPTIONNEL et en
    /// dernière position à dessein : les 8 sites de construction existants (1 en production, 7 en
    /// tests) compilent sans retouche, et la DI passe le vrai service.</param>
    public DiagnosticService(IClaudeTokenReader tokenReader, ChronosPaths paths,
                             SettingsService settings, IUsageProvider composite, IClock clock,
                             IAuthStatus? authStatus = null)
    {
        // ... affectations existantes inchangées ...
        _authStatus = authStatus;
    }
```

2. Dans `BuildReportAsync`, section `[Source exacte — login OAuth Chronos]` (juste après le
   `sb.AppendLine("  Connecté : " + ...)` des lignes 89-91, avant le `sb.AppendLine();`), insérer :

```csharp
        // TOK-02 : « le fichier existe » n'a JAMAIS voulu dire « authentifié ». Le jeton de cette
        // machine a expiré le 2026-07-12 alors que oauth.dat était bien présent : c'est exactement le
        // silence que la phase 17 brise. On affiche donc l'état RÉEL, pas la présence d'un fichier.
        sb.AppendLine("  État d'authentification : " + LibelleAuth(_authStatus?.Etat));
```

   Et ajouter la fonction de libellé (statique privée, français) :

```csharp
    private static string LibelleAuth(EtatAuthentification? etat) => etat switch
    {
        EtatAuthentification.Connecte    => "CONNECTÉ (jeton valide, chiffres exacts en circulation)",
        EtatAuthentification.HorsLigne   => "HORS LIGNE (réseau ou serveur indisponible — le jeton est peut-être bon)",
        EtatAuthentification.Deconnecte  => "DÉCONNECTÉ (le serveur a refusé les identifiants — reconnexion nécessaire)",
        EtatAuthentification.NonConnecte => "jamais connecté (clic droit → « Se connecter à Claude »)",
        _                                => "(inconnu — autorité de jeton non injectée)",
    };
```

   **Ne modifier AUCUNE autre chaîne du rapport.**

3. `tests/Chronos.Tests/DiagnosticServiceTests.cs` — ajouter **un** test, en réutilisant le montage
   du test existant à `Token = null` (celui-ci n'émet **aucune** requête réseau : la sonde
   `api.anthropic.com` est gardée par `if (token is not null)`, donc le test reste rapide) :

```csharp
    /// <summary>TOK-02 : le rapport nomme l'état d'authentification RÉEL, pas la seule présence
    /// du fichier oauth.dat — c'est cette confusion qui a laissé l'utilisateur deux mois dans le noir.</summary>
    [Fact]
    public async Task Le_rapport_nomme_l_etat_d_authentification_reel()
    {
        var paths = /* même montage temp que le test existant */;
        var settings = new SettingsService(paths);
        var auth = new FakeAuthStatus { Etat = EtatAuthentification.Deconnecte };
        var diag = new DiagnosticService(new FakeClaudeTokenReader { Token = null }, paths,
                                         settings, new StubProvider(UsageSnapshot.Empty),
                                         new FakeClock(DateTimeOffset.UtcNow), auth);

        var report = await diag.BuildReportAsync();

        Assert.Contains("État d'authentification : DÉCONNECTÉ", report);
        Assert.Contains("Token déchiffré : NON", report);   // assertion existante préservée
    }
```
   Ne modifier **aucun** des deux tests existants. Le montage temp reste sous `Path.GetTempPath()` :
   aucun test n'écrit dans le vrai `%APPDATA%\Chronos\`.

4. `src/Chronos/App.xaml.cs` — ajouter enfin le 6ᵉ argument à l'enregistrement de `DiagnosticService`
   (**lignes 319-325**, seul site de production). Ce geste vient APRÈS l'ajout du paramètre optionnel
   de l'étape 1, donc la solution compile à chaque instant de cette tâche comme à sa frontière de
   commit. `IAuthStatus` est déjà enregistré par la tâche 1 :

```csharp
        services.AddSingleton(sp => new DiagnosticService(
            sp.GetRequiredService<IClaudeTokenReader>(),
            sp.GetRequiredService<ChronosPaths>(),
            sp.GetRequiredService<SettingsService>(),
            sp.GetRequiredService<IUsageProvider>(),
            sp.GetRequiredService<IClock>(),
            sp.GetRequiredService<IAuthStatus>()));   // TOK-02 : le rapport dit l'état RÉEL
```

**INTERDITS de cette tâche :** ne PAS rendre le paramètre obligatoire (8 sites seraient cassés pour
rien). Ne PAS modifier les chaînes `"Token déchiffré : OUI"`, `"Token déchiffré : NON"`,
`"Usage exact (OAuth)"`, `"Conseil"`, `"estimé"`. Ne PAS écrire le jeton, ni son expiration brute,
ni un fragment de refresh token dans le rapport. Ne PAS ajouter d'appel réseau au diagnostic — et en
particulier aucun appel à l'endpoint de refresh. Ne PAS toucher au bloc OAuth d'`App.xaml.cs` livré par
la tâche 1, ni au bloc composite. Ne PAS appeler `.Clear()` sur le coffre.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo && dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~DiagnosticServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -v q --nologo` → **0 erreur**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~DiagnosticServiceTests"` →
      **0 échec**, **3** tests (2 existants + 1 nouveau).
    - `grep -c "IAuthStatus? authStatus = null" src/Chronos/Services/DiagnosticService.cs` → **1**.
    - `grep -c "État d'authentification" src/Chronos/Services/DiagnosticService.cs` → **1**.
    - `grep -c "Token déchiffré : " src/Chronos/Services/DiagnosticService.cs` → **1**
      (chaîne préservée mot pour mot).
    - `grep -rc "new DiagnosticService(" src/Chronos tests/Chronos.Tests` → **9** au total
      (8 pré-existants + 1 nouveau test) et **aucun** ne provoque d'erreur de compilation.
    - `sed -n '/new DiagnosticService(/,/));/p' src/Chronos/App.xaml.cs | grep -c "IAuthStatus"` → **1**.
    - `grep -rn "SECRET-TOKEN" tests/Chronos.Tests/DiagnosticServiceTests.cs` → toujours présent
      (le test anti-fuite n'a pas été affaibli).
    - `git diff --name-only -- src/Chronos/Services/ChronosOAuthUsageProvider.cs src/Chronos/Services/ChronosOAuthStore.cs` → **vide**
      pour ce commit (la tâche 1 les a déjà livrés ; cette tâche ne les retouche pas).
  </acceptance_criteria>

  <done>
Le diagnostic ne se contente plus de dire « le fichier existe » : il nomme l'état d'authentification réel.
Aucun site de construction n'a été cassé, aucune assertion existante n'a bougé, la solution compile.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln -v q --nologo` → **0 erreur** (2 avertissements xUnit2031 pré-existants
   dans `DesktopUiaSessionSourceTests` tolérés, hors périmètre). La solution compile aussi à la
   frontière du commit de la tâche 1 — c'est l'invariant de la phase.
2. Suite complète : `dotnet test Chronos.sln -v q --nologo` → **0 échec**, total **≥ 473**
   (fin de 17-03 + ≥ 8 nets). **Aucun test supprimé.**
3. Un seul rafraîchisseur dans tout le dépôt :
   `grep -rln "RefreshAsync" src/Chronos --include=*.cs` → **exactement 2 fichiers**
   (`ChronosOAuthClient.cs` qui la définit, `ChronosTokenAuthority.cs` qui l'appelle).
4. Une seule autorité : `grep -rc "new ChronosTokenAuthority(" src/Chronos` → **1**.
5. Gardes : `--filter "FullyQualifiedName~ServicesLayerPurityTests"` → **vert**, fichier non modifié ;
   `--filter "FullyQualifiedName~CompositionRootTests"` → **vert**.
6. Non-régression du pipeline d'usage :
   `--filter "FullyQualifiedName~CompositeUsageProviderTests|FullyQualifiedName~LastExactUsageProviderTests|FullyQualifiedName~GatedOAuthUsageProviderTests|FullyQualifiedName~ClaudeOAuthUsageProviderTests"` → **vert**.
7. Aucun jeton en clair :
   `grep -rniE "accessToken|refreshToken" src/Chronos --include=*.cs | grep -iE "log|Console|WriteLine|Append"`
   → **aucun résultat**.
8. Le coffre réel est intact : `ls -l "$APPDATA/Chronos/oauth.dat"` avant/après la suite → identique
   (aucun test n'a touché au vrai `%APPDATA%\Chronos\`, aucun appel de refresh avec le jeton RÉEL).
9. Aucun paquet ajouté : `git diff -- src/Chronos/Chronos.csproj tests/Chronos.Tests/Chronos.Tests.csproj` → **vide**.
</verification>

<success_criteria>
- `ChronosOAuthUsageProvider` ne contient plus aucune référence à `RefreshAsync`, `ChronosOAuthStore`
  ou `ChronosOAuthClient` : il consomme `ChronosTokenAuthority.GetAccessTokenAsync`.
- Le chemin « 401 → invalider → un seul rejeu → sinon Deconnecte » est implémenté et testé, et un 429
  ne déclare jamais de déconnexion.
- `App.xaml.cs` enregistre une **unique** autorité, l'alias `IAuthStatus` sur la même instance, et
  `TokenRefreshService` en `IHostedService` ; la garde DI le prouve (`Assert.Same`).
- `DiagnosticService` nomme l'état réel via un paramètre optionnel : 0 site de construction cassé,
  0 assertion existante modifiée.
- **La solution compile à chacun des deux commits du plan** : aucun état intermédiaire non compilable.
- Suite complète verte, aucun test supprimé, coffre réel intact.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/17-jeton-toujours-vivant-panne-toujours-visible/17-04-SUMMARY.md`.
</output>
