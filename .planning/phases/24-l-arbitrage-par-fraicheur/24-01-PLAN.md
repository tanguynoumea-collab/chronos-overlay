---
phase: 24-l-arbitrage-par-fraicheur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/ArbitrageSessions.cs
  - src/Chronos/Services/LectureSessions.cs
  - src/Chronos/Services/SessionMonitor.cs
  - tests/Chronos.Tests/ArbitrageSessionsTests.cs
  - tests/Chronos.Tests/InspectionSessionsTests.cs
  - tests/Chronos.Tests/SessionsTests.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
autonomous: true
requirements: [FUS-01]

must_haves:
  truths:
    - "Une session dont le transcript a bougé il y a 10 s est annoncée « en cours », même si un fichier de hook vieux de 7 h dit « à toi »."
    - "Un hook Working de 25 min ne transforme plus un transcript frais et correct en « inconnu »."
    - "Pour des données identiques, permuter l'ordre des signaux ne change pas un seul état retenu ni un seul désaccord rendu."
    - "À âge égal, une règle NOMMÉE tranche (la source la plus spécifique), jamais le hasard d'un parcours de collection."
    - "Un signal plus ancien mais plus spécifique (permission_prompt) n'écrase jamais un signal plus récent."
    - "Un fragment illisible n'entre pas dans l'arbitrage : c'est une absence de signal, ni un vieux signal ni un signal retenu."
  artifacts:
    - path: "src/Chronos/Services/ArbitrageSessions.cs"
      provides: "SourceSession, SignalSession, DesaccordSources, ResultatArbitrage, ArbitrageSessions.Trancher — arbitrage PUR par fraîcheur"
      min_lines: 70
      contains: "public static ResultatArbitrage Trancher"
    - path: "tests/Chronos.Tests/ArbitrageSessionsTests.cs"
      provides: "Test de PERMUTATION exhaustif (720 permutations), égalité d'âge, précision vs fraîcheur"
      min_lines: 130
    - path: "src/Chronos/Services/LectureSessions.cs"
      provides: "LectureSessions.Desaccords — le 4e champ, chemin de FUS-02"
      contains: "Desaccords"
    - path: "src/Chronos/Services/SessionMonitor.cs"
      provides: "Inspecter collecte des SIGNAUX et délègue l'arbitrage ; plus aucune réaffectation d'entrée de dictionnaire"
      contains: "ArbitrageSessions.Trancher("
  key_links:
    - from: "src/Chronos/Services/SessionMonitor.cs"
      to: "ArbitrageSessions.Trancher"
      via: "appel unique dans Inspecter, sur la liste des signaux collectés"
      pattern: "ArbitrageSessions\\.Trancher\\("
    - from: "src/Chronos/Services/SessionMonitor.cs"
      to: "LectureSessions.Desaccords"
      via: "4e argument de construction de LectureSessions"
      pattern: "arbitrage\\.Desaccords"
    - from: "tests/Chronos.Tests/GardesPerimetreTests.cs"
      to: "src/Chronos/Services/SessionMonitor.cs"
      via: "garde de non-retour par lecture de SOURCE (l'idiome d'écrasement ne revient pas)"
      pattern: "Le_moniteur_n_arbitre_plus_par_ordre_d_insertion"
---

<objective>
**FUS-01 — un signal ne peut en écraser un autre que s'il est plus récent.**

Aujourd'hui `SessionMonitor.Inspecter` fusionne les sources en réaffectant une entrée d'un dictionnaire
indexé par identifiant : étape 1 les transcripts, étape 2 les fichiers de hook. Les hooks gagnent donc
**toujours**, quel que soit leur âge. Mesuré le 2026-09-12, vérité terrain « le modèle travaille, transcript
écrit il y a 10 s » :

| Entrée | État rendu aujourd'hui | Verdict |
|---|---|---|
| transcript seul | `Working` | correct |
| + hook `Working` de 25 min | `Unknown` | **le périmé écrase le frais ET correct** |
| + hook `WaitingTurn` de 7 h | `WaitingTurn` | **« tour fini » pendant que ça travaille** |
| + hook `WaitingAttention` de 7 h | `WaitingAttention` | **« à toi » sans aucune permission demandée** |
| + hook `WaitingTurn` de 9 h (> `DropAfter`) | `Working` | le hook s'efface — bon résultat, mauvaise raison |

**Un signal de 7 heures bat un signal de 10 secondes.** C'est la cause racine n°1 du milestone.

Purpose: rendre l'arbitrage **indépendant de l'ordre** et le doter d'une règle d'égalité **nommée et testée**,
sans changer ce que les sources produisent (le contrat d'événements est la phase 25).
Output: un service PUR `ArbitrageSessions`, un `SessionMonitor.Inspecter` qui ne fait plus que **collecter**
des signaux, un 4e champ `Desaccords` sur `LectureSessions` (que le plan 24-02 affichera), et une garde de
non-retour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-l-arbitrage-par-fraicheur/24-CONTEXT.md
@.planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md
@.planning/debug/widget-sessions-statuts.md
@src/Chronos/Services/SessionMonitor.cs
@src/Chronos/Services/LectureSessions.cs
@src/Chronos/Services/SessionSnapshot.cs
@src/Chronos/Services/AffichageSessions.cs
@tests/Chronos.Tests/InspectionSessionsTests.cs

<interfaces>
<!-- Contrats déjà en place. L'exécutant les utilise TELS QUELS : aucune exploration du dépôt requise. -->

`src/Chronos/Services/SessionSnapshot.cs` :
```csharp
public enum SessionActivity { Working, WaitingAttention, WaitingTurn, Unknown }

public sealed record SessionSnapshot(
    string SessionId, string Project, SessionActivity Activity,
    string? Reason, System.DateTimeOffset UpdatedAt);
```

`src/Chronos/Services/ISessionSource.cs` :
```csharp
public interface ISessionSource
{
    IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);
}
```

`src/Chronos/Services/AffichageSessions.cs` (producteur UNIQUE du barème d'ordre — à appeler, jamais à recopier) :
```csharp
public static IReadOnlyList<SessionSnapshot> Ordonner(IEnumerable<SessionSnapshot> sessions);
public static int    Urgence(SessionActivity a);   // WaitingAttention=0, WaitingTurn=1, Working=2, _=3
public static string Etat(SessionActivity a);      // « à toi » / « tour fini » / « en cours » / « inconnu »
public static string Age(System.TimeSpan d);
```

`src/Chronos/Services/LectureSessions.cs` (état ACTUEL, à étendre d'un 4e champ) :
```csharp
public enum MotifMasquage { Archivee, Traitee }
public sealed record SessionMasquee(SessionSnapshot Session, MotifMasquage Motif);
public sealed record LectureSessions(
    IReadOnlyList<SessionSnapshot> Visibles,
    IReadOnlyList<SessionMasquee>  Masquees,
    int FichiersEcartesParAnciennete);
```

`src/Chronos/Services/SessionMonitor.cs` (seuils EXISTANTS — ne pas y toucher, ils relèvent de la phase 25) :
```csharp
private static readonly TimeSpan StaleWorking = TimeSpan.FromMinutes(20);
private static readonly TimeSpan DropAfter    = TimeSpan.FromHours(8);
public IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now) => Inspecter(now).Visibles;
public LectureSessions Inspecter(DateTimeOffset now);
private static SessionSnapshot? TryRead(string file, DateTimeOffset now, out bool perimee);
```

`src/Chronos/Services/SessionTreatmentTracker.cs` :
```csharp
public void Observe(IReadOnlyList<SessionSnapshot> raw, System.DateTimeOffset now);
```

Aides de test déjà disponibles dans `tests/Chronos.Tests/InspectionSessionsTests.cs` :
```csharp
private static string TempDir();                 // + garde anti-accident Assert.StartsWith(Path.GetTempPath(), d)
private static string TempFichier();
private sealed class SourceFixe : ISessionSource { public SourceFixe(params SessionSnapshot[] snaps); }
private static readonly DateTimeOffset Maintenant = new(2026, 9, 12, 13, 28, 0, TimeSpan.Zero);
private static SessionSnapshot Snap(string id, SessionActivity a, DateTimeOffset maj);
// Écriture d'un fichier d'état au format RÉEL :
SessionHookProcessor.BuildStateJson(id, projet, SessionActivity a, string? reason, long updatedAtMs)
```
</interfaces>
</context>

<constraints>
**À NE PAS CASSER (acquis mesurés, revérifiés à chaque tâche) :**
- Phase 22 : `Read(now) => Inspecter(now).Visibles` reste l'**unique** implémentation des filtres ;
  **zéro** `new SessionMonitor` dans `DiagnosticService.cs` ; `?? new ` y reste à **2**.
- Phase 23 : `EcritureEtatSession` et `BalayageMagasinSessions` ne sont **pas** touchés par ce plan.
- Phase 21 : `TranscriptSessionSource : ISessionSource`, gardes de périmètre vertes.
- **Baseline : 763 tests verts, suite ~4 s.** Aucun test supprimé. Un renommage est autorisé (voir Task 2)
  et doit être **nommé** dans le SUMMARY.

**CONTRAINTES DURES :**
- `ArbitrageSessions` est **PUR** : aucune E/S, aucune horloge, aucun type WPF, aucun `DateTimeOffset.UtcNow`
  — ni dans le service, ni dans son fichier de tests.
- `NormalisationUniqueTests` balaie `Services/` et `Models/` **commentaires compris** : dans tout fichier
  **nouveau** de `Services/`, aucun `FromUnixTimeMilliseconds` / `FromUnixTimeSeconds` /
  `DateTimeOffset.TryParse`, et aucune division ni multiplication par cent écrite en clair. `SessionMonitor.cs`
  est **exempté nominativement** par cette garde (horodatage de session, jamais un quota) : sa conversion
  existante reste en place.
- **Ne pas anticiper la phase 25** : `StaleWorking` (20 min) et `DropAfter` (8 h) gardent leurs valeurs et leur
  rôle. **Ne pas anticiper la phase 26** : `SessionTreatmentTracker`, `TreatedStore` et `ArchiveStore` ne sont
  ni modifiés ni remplacés.
- Compilabilité à **chaque** commit ; aucun « échec de build attendu ».
- Commentaires et libellés en **français**.

**SÉCURITÉ (à revérifier avant et après chaque tâche) :**
- Ne PAS toucher `%APPDATA%\Chronos\sessions` (66 entrées) ni `archived.json` (84 octets). Tous les tests
  écrivent sous `Path.GetTempPath()`, garde anti-accident **avant** toute construction.
- Ne PAS lancer ni tuer l'overlay (`Chronos-v3.0.2.exe`, pid 119412) — `tasklist` seul.
- `oauth.dat` : taille seule (518 octets), **jamais le mtime**.
- Aucune requête réseau réelle. Aucune dépendance NuGet ajoutée.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : ArbitrageSessions — la fraîcheur tranche, et l'égalité a une règle nommée</name>
  <files>src/Chronos/Services/ArbitrageSessions.cs, tests/Chronos.Tests/ArbitrageSessionsTests.cs</files>

  <read_first>
    - src/Chronos/Services/SessionSnapshot.cs
    - src/Chronos/Services/AffichageSessions.cs
    - src/Chronos/Services/LectureSessions.cs (le style de XML-doc attendu dans ce dépôt)
    - src/Chronos/Services/BalayageMagasinSessions.cs (précédent de service neutre, pur, documenté — phase 23)
    - tests/Chronos.Tests/InspectionSessionsTests.cs
    - tests/Chronos.Tests/NormalisationUniqueTests.cs (la liste des motifs interdits et des exemptions)
  </read_first>

  <behavior>
    Comportement attendu de `ArbitrageSessions.Trancher(IEnumerable<SignalSession>)`, testé AVANT
    implémentation (étape ROUGE contre un squelette compilable qui lève `NotImplementedException`) :

    - **T1 — la fraîcheur d'abord.** Hook `WaitingAttention` à `T-7h` + transcript `Working` à `T-10s`
      → retenu = `Working`, source retenue = `Transcript`.
    - **T2 — le périmé n'écrase plus le frais.** Hook `Unknown` à `T-25min` + transcript `Working` à `T-10s`
      → retenu = `Working`.
    - **T3 — la précision ne bat pas la fraîcheur.** Hook `WaitingAttention` portant
      `Reason = "permission_prompt"` à `T-1min` + transcript `Working` à `T-10s` → retenu = `Working`,
      `SourceRetenue == SourceSession.Transcript`, et le désaccord porte
      `SourceEcartee == SourceSession.Hook` et `EtatEcarte == SessionActivity.WaitingAttention`.
      **`DesaccordSources` ne porte PAS le motif** : le `permission_prompt` est la mise en situation,
      pas un champ asserté. N'ajouter AUCUN champ au contrat — le plan 24-02 le consomme tel quel.
    - **T4 — égalité d'âge : règle NOMMÉE.** Hook `WaitingAttention` à `T` + transcript `WaitingTurn` à `T`
      → retenu = `WaitingAttention` (source la plus SPÉCIFIQUE), `EcartAge == TimeSpan.Zero`. Le même test
      rejoue l'entrée dans l'ordre INVERSE et obtient le même résultat.
    - **T5 — PERMUTATION EXHAUSTIVE (le critère n°2 de la phase).** Un corpus de **6** signaux couvrant
      4 sessions (une tranchée par la fraîcheur, une par l'égalité d'âge, une transcript seule, une hook
      seule) est soumis dans ses **720** permutations. Le résultat canonisé (`Retenus` en séquence +
      `Desaccords` en séquence) doit être **strictement identique** aux 720 passages, et égal à une valeur
      attendue écrite en dur dans le test. Deux assertions anti-garde-muette : `Assert.Equal(720, vues)` et
      `Assert.Single(distincts)`.
    - **T6 — un désaccord dit tout.** Le désaccord porte l'identifiant, la source retenue, l'état retenu,
      la source écartée, l'état écarté et l'écart d'âge.
    - **T7 — pas de désaccord sans contradiction.** Deux signaux de sources différentes annonçant le MÊME
      état ne produisent **aucun** désaccord (un doublon n'est pas un désaccord).
    - **T8 — aucun écart d'âge négatif.** Sur tout le corpus de T5, `EcartAge >= TimeSpan.Zero`.
    - **T9 — l'entrée vide rend deux listes vides**, jamais null.
  </behavior>

  <action>
**CONSIGNE DE RÉDACTION** : ne jamais écrire littéralement `DateTimeOffset.UtcNow`,
`FromUnixTimeMilliseconds`, `Dictionary<string, SessionSnapshot>` ni `byId[` dans un commentaire ou une
XML-doc de ces fichiers — les gardes lisent le TEXTE, commentaires compris. Dire « l'horloge du système »,
« une entrée indexée par identifiant ».

**ROUGE.** Créer d'abord `src/Chronos/Services/ArbitrageSessions.cs` en SQUELETTE compilable
(`Trancher` lève `System.NotImplementedException`), puis `tests/Chronos.Tests/ArbitrageSessionsTests.cs`
complet. Lancer la suite : les tests comportementaux doivent échouer, le build doit être **propre**.
Commit `test(24-01): l'arbitrage par fraîcheur, ses permutations et sa règle d'égalité`.

**VERT.** Écrire l'implémentation ci-dessous, telle quelle. Commit
`feat(24-01): l'arbitrage se fait sur la FRAÎCHEUR, plus sur l'ordre d'insertion (FUS-01)`.

`src/Chronos/Services/ArbitrageSessions.cs` (nouveau, ~110 lignes, minimum 70) :

```csharp
using System.Collections.Generic;
using System.Linq;

namespace Chronos.Services;

/// <summary>D'OÙ vient un signal de session. L'ORDRE DE DÉCLARATION EST SIGNIFIANT : il sert de
/// départage à ÂGE ÉGAL, et seulement là (voir <see cref="ArbitrageSessions"/>).</summary>
public enum SourceSession
{
    /// <summary>Fichier d'état écrit par un hook. SPÉCIFIQUE : lui seul sait dire « une permission est
    /// demandée ». C'est ce qui le départage d'un transcript de MÊME âge — jamais ce qui le hisse
    /// au-dessus d'un signal plus récent.</summary>
    Hook,

    /// <summary>Transcript JSONL. Universel et continu, mais muet sur la permission.</summary>
    Transcript,
}

/// <summary>Ce qu'UNE source dit d'UNE session, à un instant qu'elle porte elle-même
/// (<see cref="SessionSnapshot.UpdatedAt"/>). Un signal n'est pas un verdict : c'est une déposition.</summary>
public sealed record SignalSession(SourceSession Source, SessionSnapshot Session);

/// <summary>
/// FUS-02 — deux sources ont parlé de la même session et ne disent PAS la même chose. Ce n'est ni une
/// erreur ni un masquage : la session est bel et bien affichée, c'est l'un de ses signaux qui a perdu.
///
/// <para>Le relevé du 2026-09-12 est l'exemple type : un fichier de hook figé depuis 7 h annonçait
/// « à toi » pendant qu'un transcript de 10 s prouvait le contraire. Le désaccord existait, il gagnait,
/// et rien ne le disait. <see cref="EcartAge"/> est la grandeur qui rend une source FIGÉE
/// diagnosticable par l'utilisateur seul.</para>
/// </summary>
public sealed record DesaccordSources(
    string SessionId,
    SourceSession SourceRetenue, SessionActivity EtatRetenu,
    SourceSession SourceEcartee, SessionActivity EtatEcarte,
    System.TimeSpan EcartAge);

/// <summary>Ce que l'arbitrage retient, et ce qu'il a écarté en le DISANT.</summary>
public sealed record ResultatArbitrage(
    IReadOnlyList<SessionSnapshot> Retenus,
    IReadOnlyList<DesaccordSources> Desaccords);

/// <summary>
/// FUS-01 — quand deux sources parlent de la même session, c'est la plus RÉCENTE qui gagne.
///
/// <para>Ce que ce type remplace : une fusion qui réaffectait une entrée indexée par identifiant, source
/// après source. Le dernier écrivain l'emportait, donc l'ordre du code faisait loi. Mesuré : un signal de
/// 7 heures battait un signal de 10 secondes.</para>
///
/// <para>LA DOCTRINE : un état ancien n'est pas une vérité plus solide parce qu'il est plus détaillé. La
/// précision ne bat JAMAIS la fraîcheur. Elle ne sert qu'à départager deux signaux du MÊME âge — et ce
/// départage est une règle écrite, testée et nommée, pas le hasard d'un parcours de collection.</para>
///
/// <para>Le vainqueur est le maximum d'un ordre TOTAL sur le CONTENU des signaux :
/// <list type="number">
///   <item>l'instant du signal, décroissant — LA FRAÎCHEUR ;</item>
///   <item>à âge égal, le rang de la source (la plus spécifique d'abord) ;</item>
///   <item>puis le rang d'urgence de l'état, par <see cref="AffichageSessions.Urgence"/> — appelé, jamais recopié ;</item>
///   <item>puis le motif, puis le projet, en comparaison ordinale.</item>
/// </list>
/// Ces critères épuisent tous les champs de <see cref="SessionSnapshot"/> hors l'identifiant, qui est la
/// clé de regroupement. Deux signaux encore ex aequo sont donc identiques champ pour champ : le choix ne
/// PEUT plus dépendre de l'ordre d'entrée. C'est ce qui rend le test de permutation vrai par construction
/// et non par chance.</para>
///
/// PUR : aucune E/S, aucune horloge, aucun type WPF. Les instants comparés sont ceux que les signaux
/// portent — on ne demande jamais l'heure à qui que ce soit.
/// </summary>
public static class ArbitrageSessions
{
    /// <summary>Tranche entre les signaux, session par session. Rend les retenus ET les désaccords.</summary>
    public static ResultatArbitrage Trancher(IEnumerable<SignalSession> signaux)
    {
        var retenus = new List<SessionSnapshot>();
        var desaccords = new List<DesaccordSources>();

        // Le regroupement ET la sortie sont ordonnés par identifiant : la SÉQUENCE rendue est elle aussi
        // indépendante de l'ordre d'entrée, sinon « le résultat ne dépend plus de l'ordre » ne vaudrait
        // que pour les états, pas pour les lignes.
        foreach (var groupe in signaux.GroupBy(s => s.Session.SessionId, System.StringComparer.Ordinal)
                                      .OrderBy(g => g.Key, System.StringComparer.Ordinal))
        {
            var classes = groupe.OrderBy(s => s, Comparateur).ToList();
            var vainqueur = classes[0];
            retenus.Add(vainqueur.Session);

            foreach (var ecarte in classes.Skip(1))
            {
                // Un DOUBLON n'est pas un désaccord : deux sources d'accord ne contredisent personne.
                if (ecarte.Session.Activity == vainqueur.Session.Activity) continue;

                desaccords.Add(new DesaccordSources(
                    groupe.Key,
                    vainqueur.Source, vainqueur.Session.Activity,
                    ecarte.Source, ecarte.Session.Activity,
                    vainqueur.Session.UpdatedAt - ecarte.Session.UpdatedAt));
            }
        }

        return new ResultatArbitrage(retenus, desaccords);
    }

    private static readonly IComparer<SignalSession> Comparateur =
        Comparer<SignalSession>.Create(Departager);

    // Ordre TOTAL sur le contenu. Le rang 1 est la phase entière ; les rangs 2 à 5 n'existent que pour
    // qu'aucun ex aequo ne soit laissé à la position dans la collection.
    private static int Departager(SignalSession a, SignalSession b)
    {
        var c = b.Session.UpdatedAt.CompareTo(a.Session.UpdatedAt);   // 1. LA FRAÎCHEUR
        if (c != 0) return c;

        c = ((int)a.Source).CompareTo((int)b.Source);                 // 2. à âge ÉGAL : la plus spécifique
        if (c != 0) return c;

        c = AffichageSessions.Urgence(a.Session.Activity)             // 3. ce qui réclame une intervention
                             .CompareTo(AffichageSessions.Urgence(b.Session.Activity));
        if (c != 0) return c;

        c = System.StringComparer.Ordinal.Compare(a.Session.Reason ?? "", b.Session.Reason ?? "");
        if (c != 0) return c;                                         // 4. le motif

        return System.StringComparer.Ordinal.Compare(a.Session.Project, b.Session.Project);  // 5. le projet
    }
}
```

`tests/Chronos.Tests/ArbitrageSessionsTests.cs` (nouveau, ~210 lignes, minimum 130, **9 tests**) :

- En-tête `using Chronos.Services; using Xunit;` + XML-doc de classe rappelant le relevé du 2026-09-12
  et le fait que ces tests sont **purs** (aucune E/S, aucun dossier temporaire, aucune horloge).
- `private static readonly DateTimeOffset T = new(2026, 9, 12, 13, 28, 0, TimeSpan.Zero);`
  **Aucun `DateTimeOffset.UtcNow` dans ce fichier** : tous les instants dérivent de `T`.
- Fabriques :
  ```csharp
  private static SignalSession Hook(string id, SessionActivity a, DateTimeOffset maj, string? motif = null)
      => new(SourceSession.Hook, new SessionSnapshot(id, "Proj-" + id, a, motif, maj));

  private static SignalSession Transcript(string id, SessionActivity a, DateTimeOffset maj)
      => new(SourceSession.Transcript, new SessionSnapshot(id, "Proj-" + id, a, null, maj));
  ```
- Corpus de permutation (T5) — **6 signaux, 4 sessions** :
  ```csharp
  private static SignalSession[] Corpus() => new[]
  {
      Hook("s1", SessionActivity.WaitingAttention, T.AddHours(-7), "permission_prompt"),
      Transcript("s1", SessionActivity.Working, T.AddSeconds(-10)),   // la fraîcheur tranche
      Hook("s2", SessionActivity.WaitingAttention, T.AddMinutes(-1)),
      Transcript("s2", SessionActivity.WaitingTurn, T.AddMinutes(-1)),// l'ÉGALITÉ d'âge tranche
      Transcript("s3", SessionActivity.WaitingTurn, T.AddMinutes(-5)),// seule : aucun désaccord
      Hook("s4", SessionActivity.Working, T.AddMinutes(-2)),          // seule : aucun désaccord
  };
  ```
- Générateur de permutations (récursif, local au fichier) :
  ```csharp
  private static IEnumerable<T[]> Permutations<T>(T[] source)
  {
      if (source.Length <= 1) { yield return source; yield break; }
      for (var i = 0; i < source.Length; i++)
      {
          var tete = source[i];
          var reste = source.Take(i).Concat(source.Skip(i + 1)).ToArray();
          foreach (var suite in Permutations(reste))
              yield return new[] { tete }.Concat(suite).ToArray();
      }
  }
  ```
- Canonisation (une chaîne unique par résultat, pour comparer 720 sorties sans ambiguïté) :
  ```csharp
  private static string Canonique(ResultatArbitrage r)
      => string.Join(" | ", r.Retenus.Select(s => $"{s.SessionId}={s.Activity}@{s.UpdatedAt:O}"))
       + " || "
       + string.Join(" | ", r.Desaccords.Select(d =>
             $"{d.SessionId}:{d.SourceRetenue}/{d.EtatRetenu}>{d.SourceEcartee}/{d.EtatEcarte}+{d.EcartAge}"));
  ```
- Le test de permutation :
  ```csharp
  [Fact]
  public void Permuter_l_ordre_des_signaux_ne_change_pas_un_seul_etat_ni_un_seul_desaccord()
  {
      var attendu = Canonique(ArbitrageSessions.Trancher(Corpus()));
      var distincts = new HashSet<string>(StringComparer.Ordinal);
      var vues = 0;

      foreach (var permutation in Permutations(Corpus()))
      {
          distincts.Add(Canonique(ArbitrageSessions.Trancher(permutation)));
          vues++;
      }

      Assert.Equal(720, vues);          // 6! — une garde qui n'énumérerait rien serait muette
      Assert.Single(distincts);         // LE critère n°2 de la phase
      Assert.Equal(attendu, distincts.Single());

      // …et le résultat n'est pas n'importe lequel : la fraîcheur a bien tranché s1.
      Assert.Contains("s1=Working", attendu);
      Assert.Contains("s2=WaitingAttention", attendu);
  }
  ```
- Les 8 autres tests suivent `<behavior>`, un `[Fact]` par point, nommés en français et sans abréviation
  (`Un_permission_prompt_plus_ancien_ne_bat_pas_un_signal_plus_recent`,
  `A_age_egal_la_source_la_plus_specifique_tranche_et_l_ordre_inverse_donne_le_meme_resultat`,
  `Deux_sources_d_accord_ne_produisent_aucun_desaccord`,
  `Aucun_ecart_d_age_n_est_negatif_sur_tout_le_corpus`,
  `L_entree_vide_rend_deux_listes_vides`, …).
  </action>

  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~ArbitrageSessions"</automated>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/Chronos/Services/ArbitrageSessions.cs` → présent ; `wc -l` ≥ **70**.
    - `test -f tests/Chronos.Tests/ArbitrageSessionsTests.cs` → présent ; `wc -l` ≥ **130**.
    - `grep -c "\[Fact\]\|\[Theory\]" tests/Chronos.Tests/ArbitrageSessionsTests.cs` → **≥ 9**.
    - `grep -cF "public static ResultatArbitrage Trancher" src/Chronos/Services/ArbitrageSessions.cs` → **1**.
    - `grep -cF "DateTimeOffset.UtcNow" src/Chronos/Services/ArbitrageSessions.cs` → **0**.
    - `grep -cF "DateTimeOffset.UtcNow" tests/Chronos.Tests/ArbitrageSessionsTests.cs` → **0**.
    - `grep -cE "FromUnixTimeMilliseconds|FromUnixTimeSeconds|DateTimeOffset\.TryParse" src/Chronos/Services/ArbitrageSessions.cs` → **0**.
    - `grep -cF "AffichageSessions.Urgence(" src/Chronos/Services/ArbitrageSessions.cs` → **2**
      (les deux opérandes du rang 3 ; le renvoi `<see cref=…/>` de la doctrine n'a pas de parenthèse
      et n'est donc pas compté).
    - `grep -cE "WaitingAttention\s*=>\s*0|WaitingTurn\s*=>\s*1|Working\s*=>\s*2" src/Chronos/Services/ArbitrageSessions.cs` → **0**
      (le barème est APPELÉ, jamais recopié — c'est ce que le critère précédent croyait mesurer).
    - `grep -cE "System\.IO|FileStream|Directory\." src/Chronos/Services/ArbitrageSessions.cs` → **0** (service PUR).
    - `grep -cF "Assert.Equal(720, vues)" tests/Chronos.Tests/ArbitrageSessionsTests.cs` → **1**.
    - `grep -cF "Assert.Single(distincts)" tests/Chronos.Tests/ArbitrageSessionsTests.cs` → **1**.
    - Étape ROUGE : le SUMMARY consigne le **compte exact** d'échecs et de succès au commit `test(24-01)`,
      build **0 erreur / 0 avertissement**.
    - Suite complète : **0 échec**, total **> 763**, aucun test supprimé.
    - `NormalisationUniqueTests`, `ServicesLayerPurityTests`, `GardesDoctrineTests`, `CompositionRootTests`
      → **toutes vertes** (elles balaient le nouveau fichier).
  </acceptance_criteria>

  <done>
`ArbitrageSessions.Trancher` rend un résultat **identique pour les 720 permutations** du corpus, tranche par
la fraîcheur, départage l'égalité d'âge par une règle nommée, et rend les désaccords avec leur écart d'âge.
Le service est pur : ni horloge, ni disque, ni conversion d'époque.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Inspecter COLLECTE des signaux et délègue l'arbitrage — les inversions mesurées tombent</name>
  <files>src/Chronos/Services/SessionMonitor.cs, src/Chronos/Services/LectureSessions.cs, tests/Chronos.Tests/InspectionSessionsTests.cs, tests/Chronos.Tests/SessionsTests.cs</files>

  <read_first>
    - src/Chronos/Services/SessionMonitor.cs (intégralité — c'est le fichier refondu)
    - src/Chronos/Services/ArbitrageSessions.cs (livré en Task 1)
    - src/Chronos/Services/LectureSessions.cs
    - tests/Chronos.Tests/InspectionSessionsTests.cs (intégralité)
    - tests/Chronos.Tests/SessionsTests.cs (bloc « --- SessionMonitor --- » et suivants, à partir de la ligne 225)
    - .planning/debug/widget-sessions-statuts.md (l'entrée d'Evidence du 2026-09-12T13:36Z)
  </read_first>

  <behavior>
    Tests ajoutés à `tests/Chronos.Tests/InspectionSessionsTests.cs`, rejouant **les cinq lignes mesurées**
    contre les classes réelles. Vérité terrain commune : le modèle travaille, transcript à `Maintenant - 10 s`.

    | # | Test | Entrée ajoutée au transcript frais | Attendu |
    |---|---|---|---|
    | 1 | `Un_transcript_frais_seul_est_annonce_en_cours` | — | `Working`, 0 désaccord |
    | 2 | `Un_hook_Working_de_25_min_ne_rend_plus_un_transcript_frais_inconnu` | hook `Working` à `-25 min` | `Working`, **1 désaccord** (`Unknown` écarté) |
    | 3 | `Un_hook_WaitingTurn_de_7_h_ne_bat_plus_un_transcript_de_10_s` | hook `WaitingTurn` à `-7 h` | `Working`, 1 désaccord |
    | 4 | `Un_hook_WaitingAttention_de_7_h_ne_bat_plus_un_transcript_de_10_s` | hook `WaitingAttention`, motif `permission_prompt`, à `-7 h` | `Working`, 1 désaccord |
    | 5 | `Un_hook_de_9_h_n_est_plus_candidat_du_tout_et_ne_produit_aucun_desaccord` | hook `WaitingTurn` à `-9 h` (> `DropAfter`) | `Working`, **0 désaccord**, `FichiersEcartesParAnciennete == 1` |

    **Honnêteté sur la falsifiabilité** (à consigner tel quel dans le SUMMARY) : les tests **2, 3 et 4**
    échouent contre le code d'avant ce plan — ce sont les trois inversions du ROADMAP. Les tests **1 et 5**
    rendaient déjà le bon état : le test 1 est un **témoin**, et le test 5 est un **contrôle de
    non-régression** dont la seule assertion neuve est `0 désaccord` (le hook de 9 h n'est jamais candidat).
    Prétendre que les cinq seraient rouges avant le correctif serait précisément le genre d'affirmation non
    observée que ce milestone bannit.

    Deux tests supplémentaires, la contrepartie assumée de la phase 23 :

    - `Un_fragment_illisible_est_une_ABSENCE_de_signal_jamais_un_vieux_signal` — un fichier de hook
      **tronqué** (écriture directe surprise en cours : `{"session_id":"frag","project":"P","activ`) pour la
      MÊME session qu'un transcript frais → état = celui du transcript, `FichiersEcartesParAnciennete == 0`,
      **0 désaccord**. Le fragment n'est ni un vieux signal qui perdrait, ni un signal retenu : il n'existe pas.
    - `Un_hook_sans_updated_at_ne_gagne_jamais` — fichier JSON **valide** mais sans `updated_at` →
      n'est jamais retenu contre un transcript frais (il n'entre pas dans l'arbitrage : `TryRead` le date à
      `DateTimeOffset.MinValue`, donc `DropAfter` le fait tomber et il est compté comme écarté pour ancienneté).

    Et le versant « ordre » au niveau du moniteur :

    - `Permuter_l_ordre_rendu_par_la_source_ne_change_pas_l_affichage` — corpus IMPOSÉ, choisi pour que
      l'ancien code soit ROUGE : deux sessions EX AEQUO sur (urgence, horodatage), qu'un tri stable
      ne peut départager autrement que par l'ordre d'entrée.

      ```csharp
      private static SessionSnapshot[] Trois() => new[]
      {
          Snap("b-deux",   SessionActivity.Working,     Maintenant.AddMinutes(-1)),  // ex aequo…
          Snap("a-un",     SessionActivity.Working,     Maintenant.AddMinutes(-1)),  // …avec b-deux
          Snap("c-trois",  SessionActivity.WaitingTurn, Maintenant.AddMinutes(-1)),
      };
      ```

      Pour chacune des **6** permutations, instancier `new SessionMonitor(TempDir(), new SourceFixe(p),
      new ArchiveStore(TempFichier()))` et canoniser DEUX séquences : `Inspecter(Maintenant).Visibles`
      (la couche que l'arbitrage contrôle) ET `AffichageSessions.Ordonner(...)` (ce que l'écran montre).
      `Assert.Equal(6, vues)` et `Assert.Single(distincts)` sur chacune.

      **Falsifiabilité (à consigner au SUMMARY) :** ce test est ROUGE contre le code d'avant ce plan —
      une entrée indexée par identifiant rend l'ordre d'insertion et le tri de `Ordonner` est stable, donc
      les deux sessions ex aequo sortent dans l'ordre où la source les a rendues. C'est un **4e test
      rouge-avant**, à ajouter à la ligne « Qualification » de 24-VALIDATION.md.
  </behavior>

  <action>
**ROUGE.** Écrire d'abord les 8 tests ci-dessus dans `tests/Chronos.Tests/InspectionSessionsTests.cs`
(ils référencent `lecture.Desaccords`, donc ils NE COMPILENT PAS tant que le champ n'existe pas).
Pour tenir la règle « aucun échec de build attendu », l'ordre est : **(a)** ajouter le champ à
`LectureSessions` et le brancher dans `SessionMonitor` avec la **liste vide**, ce qui compile et laisse les
inversions en place ; **(b)** ajouter les tests → ROUGE comportemental sur 3 d'entre eux ; **(c)** refondre
`Inspecter` → VERT. Trois commits.

**(a)** `src/Chronos/Services/LectureSessions.cs` — ajouter le 4e champ positionnel et sa XML-doc :

```csharp
/// <para><see cref="Desaccords"/> (FUS-02) n'est PAS un motif de masquage et ne doit jamais être compté
/// comme tel : la session concernée est AFFICHÉE. Ce qui a été écarté, c'est l'un de ses signaux. Le relevé
/// du 2026-09-12 est le cas d'école : un fichier de hook figé depuis 7 h annonçait « à toi » pendant qu'un
/// transcript de 10 s prouvait le contraire — le désaccord gagnait, et rien ne le disait.</para>
public sealed record LectureSessions(
    IReadOnlyList<SessionSnapshot> Visibles,
    IReadOnlyList<SessionMasquee>  Masquees,
    int FichiersEcartesParAnciennete,
    IReadOnlyList<DesaccordSources> Desaccords);
```

Commit intermédiaire autorisé et recommandé : `feat(24-01): LectureSessions porte les désaccords de sources`.

**(c)** `src/Chronos/Services/SessionMonitor.cs` — remplacer le corps d'`Inspecter` (étapes 1, 2 et 2.b)
par la version ci-dessous. **Ne toucher à rien d'autre** : `TryRead`, `StaleWorking`, `DropAfter`, `Read`,
le constructeur et le bloc des filtres (étape 3) restent **mot pour mot** ce qu'ils sont.

```csharp
    public LectureSessions Inspecter(System.DateTimeOffset now)
    {
        // 1 & 2) COLLECTE. Les deux sources déposent leurs signaux dans une même liste, et l'ordre de cette
        //        collecte n'a plus AUCUNE conséquence (FUS-01) : c'est ArbitrageSessions qui tranche, sur la
        //        FRAÎCHEUR. Avant ce plan, chaque source réécrivait une entrée indexée par identifiant — le
        //        dernier passage gagnait, donc l'ordre du code faisait loi, et un signal de 7 heures battait
        //        un signal de 10 secondes.
        var signaux = new List<SignalSession>();

        foreach (var t in _transcripts.Read(now))
            signaux.Add(new SignalSession(SourceSession.Transcript, t));

        string[] files;
        try { files = System.IO.Directory.Exists(_dir) ? System.IO.Directory.GetFiles(_dir, "*.json") : System.Array.Empty<string>(); }
        catch { files = System.Array.Empty<string>(); }

        // Un fichier écarté pour son ÂGE est un fait observable, pas un non-événement : c'est l'écart entre
        // « 54 fichiers sur disque » et « 1 ligne à l'écran ». Un fichier ILLISIBLE n'est pas compté ici —
        // il n'a pas été écarté pour son âge, il n'a pas été lu.
        var ecartesParAnciennete = 0;
        foreach (var f in files)
        {
            var snap = TryRead(f, now, out var perimee);
            if (perimee) { ecartesParAnciennete++; continue; }

            // Un FRAGMENT — la contrepartie assumée de l'écriture directe de la phase 23, qui tronque la
            // cible avant de la réécrire — rend null SANS être périmé. C'est une ABSENCE de signal, et elle
            // se traite comme telle : le fragment n'est pas déposé. Le compter comme un signal sans date en
            // ferait un « très vieux signal » perdant contre n'importe quoi — une fausse déposition, là où
            // il n'y a rien à déposer.
            if (snap is not null) signaux.Add(new SignalSession(SourceSession.Hook, snap));
        }

        var arbitrage = ArbitrageSessions.Trancher(signaux);

        // 2.b) Le détecteur de traitement observe les snapshots RETENUS (+ horloge) et met à jour
        //      TreatedStore. Best-effort : ne casse JAMAIS le pipeline. Sa logique n'est pas touchée ici —
        //      elle est le sujet de la phase 26 ; il observe simplement, désormais, un état arbitré.
        var raw = arbitrage.Retenus;
        try { _tracker?.Observe(raw, now); } catch { }

        // 3) Filtres : archivées (permanent, NET-04) PUIS traitées (réversible). [bloc INCHANGÉ]
        var archived = _archive.Load();
        var treatedMap = _treated?.Load();
        var visibles = new List<SessionSnapshot>(raw.Count);
        var masquees = new List<SessionMasquee>();
        foreach (var s in raw)
        {
            if (archived.Contains(s.SessionId)) { masquees.Add(new SessionMasquee(s, MotifMasquage.Archivee)); continue; }
            if (treatedMap is not null && treatedMap.ContainsKey(s.SessionId)) { masquees.Add(new SessionMasquee(s, MotifMasquage.Traitee)); continue; }
            visibles.Add(s);
        }
        return new LectureSessions(visibles, masquees, ecartesParAnciennete, arbitrage.Desaccords);
    }
```

Mettre aussi à jour la XML-doc d'`Inspecter` : remplacer la phrase
« fichiers d'état des HOOKS … plus précis (permission), PRIORITAIRES quand présents » par
« … plus précis (permission), mais JAMAIS prioritaires du seul fait d'être des hooks : c'est le signal le
plus RÉCENT qui gagne (FUS-01) ». Retirer `using System.Linq;` **seulement** si plus aucun usage ne subsiste
(vérifier : le build doit rester sans avertissement).

**Renommage à faire, et à nommer dans le SUMMARY.** `tests/Chronos.Tests/SessionsTests.cs` porte
`Monitor_fusionne_transcripts_et_hooks_hook_prioritaire`. Ce test reste **vert** (le hook y est à `now`, le
transcript à `now - 1 min` : le hook gagne, désormais par la FRAÎCHEUR), mais son nom affirme une doctrine
que la phase supprime. Le renommer en
`Monitor_fusionne_transcripts_et_hooks_le_plus_recent_gagne`, remplacer le commentaire
`// hook prioritaire` par `// le plus RÉCENT gagne — ici c'est le hook`, et le commentaire d'en-tête
`// Même session_id des deux côtés : le hook (WaitingAttention) doit primer sur le transcript (Working).`
par `// Même session_id des deux côtés : le hook est plus RÉCENT d'une minute, c'est à ce titre — et à ce
titre seul — qu'il l'emporte.` **Aucun autre test de ce fichier n'est modifié ni supprimé.**

**Risque à contrôler explicitement.** `ArbitrageSessions.Trancher` rend ses retenus triés par identifiant ;
l'ancienne fusion rendait l'ordre d'insertion. Aucun test connu n'asserte cet ordre brut (le widget et le
rapport passent tous deux par `AffichageSessions.Ordonner`), mais si un test préexistant venait à échouer
pour cette raison, le corriger et l'**inscrire nominativement** dans le SUMMARY, jamais l'absorber.

Commits : `test(24-01): les cinq lignes mesurées, le fragment et la permutation au niveau du moniteur`
puis `feat(24-01): Inspecter collecte des signaux et délègue l'arbitrage (FUS-01)`.
  </action>

  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~Inspection|FullyQualifiedName~Sessions|FullyQualifiedName~Arbitrage|FullyQualifiedName~Treated|FullyQualifiedName~Balayage|FullyQualifiedName~Affichage"</automated>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "ArbitrageSessions.Trancher(" src/Chronos/Services/SessionMonitor.cs` → **1**.
    - `grep -cF "arbitrage.Desaccords" src/Chronos/Services/SessionMonitor.cs` → **1**.
    - `grep -cF "byId[" src/Chronos/Services/SessionMonitor.cs` → **0** (l'idiome d'écrasement a disparu ;
      aucun commentaire ni XML-doc de ce plan n'écrit cette chaîne).
    - `grep -cF "Dictionary<string, SessionSnapshot>" src/Chronos/Services/SessionMonitor.cs` → **0**
      (idem : la prose du fichier dit « une entrée indexée par identifiant », jamais le type).
    - `grep -cF "new SignalSession(SourceSession.Transcript" src/Chronos/Services/SessionMonitor.cs` → **1**.
    - `grep -cF "new SignalSession(SourceSession.Hook" src/Chronos/Services/SessionMonitor.cs` → **1**.
    - `grep -cF "=> Inspecter(now).Visibles;" src/Chronos/Services/SessionMonitor.cs` → **1** (acquis phase 22).
    - `grep -cF "StaleWorking = System.TimeSpan.FromMinutes(20)" src/Chronos/Services/SessionMonitor.cs` → **1**
      et `grep -cF "DropAfter = System.TimeSpan.FromHours(8)" …` → **1** (phase 25 non anticipée).
    - `grep -cF "IReadOnlyList<DesaccordSources> Desaccords" src/Chronos/Services/LectureSessions.cs` → **1**.
    - `grep -cF "hook_prioritaire" tests/Chronos.Tests/SessionsTests.cs` → **0**.
    - `grep -cF "Monitor_fusionne_transcripts_et_hooks_le_plus_recent_gagne" tests/Chronos.Tests/SessionsTests.cs` → **1**.
    - `grep -c "\[Fact\]" tests/Chronos.Tests/InspectionSessionsTests.cs` → **≥ 14** (6 préexistants + 8 ajoutés).
    - `grep -cF "Assert.Single(distincts)" tests/Chronos.Tests/InspectionSessionsTests.cs` → **1**.
    - `git diff --stat` de la tâche ne mentionne **que** : `SessionMonitor.cs`, `LectureSessions.cs`,
      `InspectionSessionsTests.cs`, `SessionsTests.cs`.
    - `git diff --stat -- src/Chronos/Services/SessionTreatmentTracker.cs src/Chronos/Services/TreatedStore.cs src/Chronos/Services/ArchiveStore.cs src/Chronos/Services/DiagnosticService.cs` → **vide**
      (phases 25 et 26 non anticipées ; le diagnostic est le sujet du plan 24-02).
    - Suite complète : **0 échec**, total **> 763**, **aucun test supprimé**. Deux exécutions consécutives
      au même total.
  </acceptance_criteria>

  <done>
Les trois inversions du ROADMAP sont rouges avant le correctif et vertes après. Un transcript de 10 s bat un
hook de 7 h. Un fragment illisible n'entre pas dans l'arbitrage. Permuter l'ordre rendu par la source ne
change pas une seule ligne affichée. `LectureSessions` porte les désaccords, prêts pour le plan 24-02.
  </done>
</task>

<task type="auto">
  <name>Task 3 : deux gardes de non-retour — l'ordre d'insertion ne revient pas, l'arbitrage reste pur</name>
  <files>tests/Chronos.Tests/GardesPerimetreTests.cs</files>

  <read_first>
    - tests/Chronos.Tests/GardesPerimetreTests.cs (intégralité — le style des gardes et `CheminSources()`)
    - src/Chronos/Services/ArbitrageSessions.cs
    - src/Chronos/Services/SessionMonitor.cs
  </read_first>

  <action>
Ajouter **deux** `[Fact]` à `tests/Chronos.Tests/GardesPerimetreTests.cs`, à la suite des gardes de la
phase 23, dans le même style (contrôle de SOURCE + garde anti-muette).

Pourquoi une garde de SOURCE et pas seulement un test de comportement : le comportement d'aujourd'hui peut
être rétabli demain par un « petit raccourci » qui réintroduit l'écrasement pour une source jugée « plus
fiable ». Le test de permutation le verrait ; une relecture, non. Les deux ensemble ferment la porte.

```csharp
    /// <summary>
    /// GARDE DE NON-RETOUR (FUS-01, phase 24) — le moniteur ne fusionne plus en réaffectant une entrée
    /// indexée par identifiant, source après source.
    ///
    /// <para>Le défaut mesuré le 2026-09-12 n'était pas une faute de frappe mais une classe d'erreur :
    /// l'ordre du code faisait loi. Étape 1 les transcripts, étape 2 les hooks — donc un signal de 7 heures
    /// battait un signal de 10 secondes, et « à toi » s'affichait pendant que le modèle travaillait.</para>
    ///
    /// <para>Cette garde vaut surtout pour l'AVENIR : les phases 25 et 26 ajoutent des signaux et une notion
    /// de « traité » adossée à la fraîcheur. Qu'une source redevienne prioritaire par construction un seul
    /// commit, et les deux phases suivantes reposeraient à nouveau sur un arbitrage faussé, sans rien signaler.</para>
    /// </summary>
    [Fact]
    public void Le_moniteur_n_arbitre_plus_par_ordre_d_insertion()
    {
        var fichier = Path.Combine(CheminSources(), "Services", "SessionMonitor.cs");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        // Une garde qui lirait un fichier vide, ou dont la méthode aurait disparu, serait muette.
        Assert.Contains("public LectureSessions Inspecter(", texte, StringComparison.Ordinal);
        Assert.Contains("ArbitrageSessions.Trancher(", texte, StringComparison.Ordinal);

        // L'idiome EXACT de l'écrasement mesuré.
        Assert.DoesNotContain("byId[", texte, StringComparison.Ordinal);
        Assert.DoesNotContain("Dictionary<string, SessionSnapshot>", texte, StringComparison.Ordinal);
    }

    /// <summary>
    /// GARDE DE PURETÉ (FUS-01). L'arbitrage ne compare QUE les instants que les signaux portent. Qu'il
    /// acquière une horloge, un chemin ou un magasin, et il pourrait « rafraîchir » un signal muet — c'est-à-dire
    /// présenter comme observé ce qui ne l'a pas été, le seul interdit absolu de ce milestone.
    /// La réflexion ne lit pas les commentaires : elle lit la surface du type.
    /// </summary>
    [Fact]
    public void L_arbitrage_ne_connait_ni_horloge_ni_magasin_ni_chemin()
    {
        var t = typeof(Chronos.Services.ArbitrageSessions);

        Assert.True(t.IsAbstract && t.IsSealed, "ArbitrageSessions doit rester une classe statique");

        var declarees = t.GetMethods(BindingFlags.Public | BindingFlags.Static | BindingFlags.DeclaredOnly);
        var trancher = Assert.Single(declarees);                       // une seule porte d'entrée
        Assert.Equal("Trancher", trancher.Name);
        var parametre = Assert.Single(trancher.GetParameters());       // …et un seul argument
        Assert.Equal(typeof(IEnumerable<Chronos.Services.SignalSession>), parametre.ParameterType);

        // Aucun champ statique ne peut cacher une horloge, un magasin de verdict ou un chemin de dossier.
        var contrebande = t.GetFields(BindingFlags.Static | BindingFlags.Public | BindingFlags.NonPublic)
            .Where(f => f.FieldType == typeof(Chronos.Services.IClock)
                     || f.FieldType == typeof(Chronos.Services.TreatedStore)
                     || f.FieldType == typeof(Chronos.Services.ArchiveStore)
                     || f.FieldType == typeof(string))
            .Select(f => f.Name)
            .ToList();

        Assert.True(contrebande.Count == 0,
            "L'arbitrage doit rester pur : ni horloge, ni magasin de verdict, ni chemin.\n  "
            + string.Join("\n  ", contrebande));
    }
```

`GardesPerimetreTests.cs` importe déjà `System.IO`, `System.Reflection` et `Xunit` ; ajouter
`using System.Collections.Generic;` si le compilateur le réclame pour `IEnumerable<>`.

**Falsification exigée, puis révocation.** Comme en 23-01 et 23-02, jouer la mutation via un **alias
temporaire** pour que le dépôt reste compilable (une erreur de compilation ferait échouer *toute*
l'invocation `dotnet test` et ne dirait **rien** de la garde) :

1. Dans `SessionMonitor.cs`, renommer l'appel en `ArbitrageSessions.TrancherMUTANT(` et ajouter à
   `ArbitrageSessions` l'alias `public static ResultatArbitrage TrancherMUTANT(IEnumerable<SignalSession> s) => Trancher(s);`.
2. Mesurer : **deux** tests doivent échouer — `Le_moniteur_n_arbitre_plus_par_ordre_d_insertion` (l'appel
   a changé de nom) **et** `L_arbitrage_ne_connait_ni_horloge_ni_magasin_ni_chemin` (l'alias ajoute une
   seconde porte d'entrée publique et casse son `Assert.Single(declarees)`). Consigner les deux ; c'est une
   double falsification, pas une surprise. Consigner le compte exact d'échecs/succès sur le filtre
   `GardesPerimetre`.
3. Révoquer alias **et** mutation ensemble, puis prouver la révocation :
   `git diff --stat -- src/Chronos/Services/` **vide** après révocation, ET
   `grep -rlF "MUTANT" --include=*.cs src/Chronos tests/Chronos.Tests | wc -l` → **0**.
   (`--include=*.cs` est indispensable : sans lui le balayage traverse `bin/` et `obj/`, où le nom muté
   survit dans les assemblies compilés, et `grep -rc` rend une ligne PAR FICHIER, pas un nombre.)

Commit `test(24-01): deux gardes de non-retour sur l'arbitrage par fraîcheur`.
  </action>

  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~GardesPerimetre"</automated>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "Le_moniteur_n_arbitre_plus_par_ordre_d_insertion" tests/Chronos.Tests/GardesPerimetreTests.cs` → **1**.
    - `grep -cF "L_arbitrage_ne_connait_ni_horloge_ni_magasin_ni_chemin" tests/Chronos.Tests/GardesPerimetreTests.cs` → **1**.
    - Filtre `GardesPerimetre` : **10 tests, 0 échec** (8 préexistants + 2).
    - Mutation jouée et **mesurée** : le SUMMARY porte le nom des **deux** tests tombés
      (`Le_moniteur_n_arbitre_plus_par_ordre_d_insertion` et
      `L_arbitrage_ne_connait_ni_horloge_ni_magasin_ni_chemin`) et le compte exact d'échecs.
    - Révocation prouvée : `git diff --stat -- src/Chronos/Services/` **vide** après révocation, ET
      `grep -rlF "MUTANT" --include=*.cs src/Chronos tests/Chronos.Tests | wc -l` → **0**.
      (`--include=*.cs` est indispensable : sans lui le balayage traverse `bin/` et `obj/`, où le nom
      muté survit dans les assemblies compilés, et `grep -rc` rend une ligne PAR FICHIER, pas un nombre.)
    - `git diff --stat` de la tâche ne mentionne que `tests/Chronos.Tests/GardesPerimetreTests.cs`.
    - Suite complète : **0 échec**, total **> 763**.
  </acceptance_criteria>

  <done>
Deux gardes de non-retour couvrent FUS-01 : l'une interdit le retour de l'idiome d'écrasement par lecture de
la source, l'autre interdit à l'arbitrage d'acquérir une horloge, un magasin ou un chemin. La première a été
falsifiée par mutation réelle puis révoquée, avec ses chiffres.
  </done>
</task>

</tasks>

<verification>
1. **Suite complète verte, deux fois de suite, au même total** :
   `dotnet test Chronos.sln -c Debug --nologo -v q` → 0 échec, total > 763, ~4 s.
2. **Les inversions mesurées** : filtre `Inspection` → les cinq lignes du relevé sont chacune un test nommé,
   avec pour chacune sa qualification honnête (rouge-avant / témoin / contrôle) inscrite au SUMMARY.
3. **Le critère n°2, par construction et par mesure** : 720 permutations au niveau de l'arbitrage,
   6 permutations au niveau du moniteur, une seule sortie distincte à chaque fois.
4. **Le critère n°4** : l'égalité d'âge est tranchée par une règle nommée et testée dans les deux sens
   d'entrée ; un `permission_prompt` plus ancien perd contre un signal plus récent.
5. **Le fragment** : `FichiersEcartesParAnciennete == 0` et **0 désaccord** — ni vieux signal, ni signal retenu.
6. **Acquis intacts** :
   `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` → 0 ;
   `grep -cF "?? new " src/Chronos/Services/DiagnosticService.cs` → 2 ;
   `grep -cF "=> Inspecter(now).Visibles;" src/Chronos/Services/SessionMonitor.cs` → 1 ;
   `NormalisationUnique` 3/3, `ServicesLayerPurity` 2/2, `CompositionRoot` 5/5, `GardesDoctrine` 8/8.
7. **Sécurité** : `ls "$APPDATA/Chronos/sessions" | wc -l` → **66** (avant ET après) ;
   `archived.json` → **84** octets ; `oauth.dat` → **518** octets (taille seule) ;
   `tasklist | grep Chronos` → l'overlay pid 119412 vivant, ni lancé ni tué ;
   `git diff -- '*.csproj'` → vide ; 0 dépendance NuGet ajoutée.
</verification>

<success_criteria>
- `ArbitrageSessions.Trancher` existe, est pur, et rend un résultat identique aux **720** permutations du corpus.
- `SessionMonitor.Inspecter` ne contient plus l'idiome d'écrasement et délègue à l'arbitrage.
- `LectureSessions` porte `Desaccords`, alimenté par l'arbitrage.
- Les 3 inversions du ROADMAP sont couvertes par des tests rouges avant le correctif ; les 2 autres lignes
  mesurées sont couvertes et **qualifiées** honnêtement.
- Un fragment illisible est traité comme une ABSENCE de signal.
- Deux gardes de non-retour, dont une falsifiée par mutation réelle puis révoquée.
- Suite : 0 échec, total > 763, aucun test supprimé, un seul renommage, nommé.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/24-l-arbitrage-par-fraicheur/24-01-SUMMARY.md`.

Y consigner impérativement :
- le compte **exact** de l'étape ROUGE (échecs / succès / total), et le nom des tests verts dès le rouge ;
- la qualification de chacune des **cinq** lignes mesurées (rouge-avant, témoin, contrôle) ;
- les chiffres de `grep` **réellement obtenus**, en regard des valeurs prédites — un écart se **documente**,
  il ne s'absorbe pas (leçon 22-03) ;
- le renommage de `Monitor_fusionne_transcripts_et_hooks_hook_prioritaire`, nommément ;
- la mutation de falsification, le test tombé, et la preuve de révocation ;
- les invariants de sécurité mesurés avant et après.
</output>
