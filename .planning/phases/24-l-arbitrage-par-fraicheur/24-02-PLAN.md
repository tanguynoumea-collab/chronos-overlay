---
phase: 24-l-arbitrage-par-fraicheur
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/Chronos/Services/AffichageSessions.cs
  - src/Chronos/Services/DiagnosticService.cs
  - tests/Chronos.Tests/AffichageSessionsTests.cs
  - tests/Chronos.Tests/DiagnosticServiceTests.cs
  - .planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FUS-02]

must_haves:
  truths:
    - "Quand deux sources se contredisent, le diagnostic nomme la source RETENUE, la source ÉCARTÉE et l'écart d'âge entre les deux."
    - "Chaque source est nommée avec le dossier qu'on peut aller ouvrir — l'utilisateur diagnostique seul une source figée."
    - "Un désaccord n'est jamais présenté comme un masquage : la session concernée reste comptée parmi les affichées."
    - "Quand aucune source n'en contredit une autre, le rapport le dit au lieu de se taire."
    - "Sans moniteur injecté, le rapport ne raconte aucun désaccord."
  artifacts:
    - path: "src/Chronos/Services/AffichageSessions.cs"
      provides: "AffichageSessions.Ecart — un ÉCART d'âge, pas une ancienneté"
      contains: "public static string Ecart"
    - path: "src/Chronos/Services/DiagnosticService.cs"
      provides: "Bloc « Désaccords entre sources » + LibelleSource (la source nommée AVEC son dossier)"
      contains: "Désaccords entre sources"
    - path: ".planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md"
      provides: "Carte de vérification de la phase, remplie et mesurée, status: validated"
      contains: "status: validated"
  key_links:
    - from: "src/Chronos/Services/DiagnosticService.cs"
      to: "LectureSessions.Desaccords"
      via: "la MÊME lecture partagée que les visibles et les masquées (_moniteurSessions.Inspecter)"
      pattern: "lecture\\.Desaccords"
    - from: "src/Chronos/Services/DiagnosticService.cs"
      to: "AffichageSessions.Ecart"
      via: "mise en forme de l'écart d'âge par le producteur unique, jamais recopiée"
      pattern: "AffichageSessions\\.Ecart\\("
---

<objective>
**FUS-02 — les désaccords entre sources deviennent traçables dans le diagnostic.**

Le plan 24-01 a rendu l'arbitrage honnête et lui a fait produire ses `Desaccords`. Personne ne les lit
encore. Ce plan les affiche, par le **même chemin** que les masquages de la phase 22 : rendus par
`Inspecter`, affichés par le diagnostic, sur **l'instance partagée** du conteneur.

Le cas d'usage exact : une source **figée**. Le 2026-09-12, un fichier de hook n'avait plus bougé depuis
7 heures et annonçait « à toi » pendant que le transcript prouvait le contraire. Après 24-01, le bon état
s'affiche — mais rien ne dit à l'utilisateur **qu'une de ses sources est morte**. Une ligne qui nomme la
source écartée et l'écart d'âge transforme un symptôme en diagnostic.

Purpose: rendre un désaccord lisible sans jamais le confondre avec un masquage, puis refermer la phase sur
une carte de vérification mesurée.
Output: un bloc de rapport, un libellé de source, un formateur d'écart, leurs tests, et `24-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-l-arbitrage-par-fraicheur/24-CONTEXT.md
@.planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md
@.planning/phases/24-l-arbitrage-par-fraicheur/24-01-SUMMARY.md
@src/Chronos/Services/DiagnosticService.cs
@src/Chronos/Services/AffichageSessions.cs
@src/Chronos/Services/ArbitrageSessions.cs
@tests/Chronos.Tests/DiagnosticServiceTests.cs

<interfaces>
<!-- Contrats livrés par le plan 24-01, à consommer TELS QUELS. -->

```csharp
public enum SourceSession { Hook, Transcript }

public sealed record DesaccordSources(
    string SessionId,
    SourceSession SourceRetenue, SessionActivity EtatRetenu,
    SourceSession SourceEcartee, SessionActivity EtatEcarte,
    System.TimeSpan EcartAge);              // toujours >= TimeSpan.Zero

public sealed record LectureSessions(
    IReadOnlyList<SessionSnapshot>  Visibles,
    IReadOnlyList<SessionMasquee>   Masquees,
    int                             FichiersEcartesParAnciennete,
    IReadOnlyList<DesaccordSources> Desaccords);
```

Déjà dans `AffichageSessions` (producteur UNIQUE de mise en forme — à appeler, jamais à recopier) :
```csharp
public static string Etat(SessionActivity a);   // « à toi » / « tour fini » / « en cours » / « inconnu »
public static string Age(System.TimeSpan d);    // « à l'instant » / « il y a N min » / « il y a N h »
public static int    Urgence(SessionActivity a);
```

Déjà dans `DiagnosticService` (privés, à réutiliser tels quels) :
```csharp
private static string Court(string id);                     // ligne 590 — 8 premiers caractères
private static string LibelleMotif(MotifMasquage m);        // lignes 581-586 — le filtre NOMMÉ AVEC SON FICHIER
```

Aides de test déjà disponibles dans `tests/Chronos.Tests/DiagnosticServiceTests.cs` :
```csharp
private static readonly DateTimeOffset T22;                       // instant figé du bloc phase 22
private static string TempDir22();                                // + garde Assert.StartsWith(Path.GetTempPath(), d)
private static string TempFichier22();
private sealed class SourceFixe22 : ISessionSource { … }
private static async Task<string> Rapport(SessionMonitor? moniteur);
private static void EcrireEtat(string dir, string id, string projet, SessionActivity a, DateTimeOffset maj);
private static SessionMonitor MoniteurSur(string dir);
private static List<string> LignesFichiersEtat(string report);    // isole le SEUL bloc « Fichiers d'état »
```
</interfaces>
</context>

<constraints>
**Justification du changement de `DiagnosticService.cs` — exigée par le contexte de phase.**
La phase 22 garantit que le rapport décrit **le moniteur qui tourne**, pas un moniteur reconstruit : tout
changement de **câblage** du widget s'y reflète sans qu'une ligne n'y change. Elle ne garantit pas — et ne
peut pas garantir — qu'une information **qui n'existait pas** s'imprime toute seule. Les désaccords sont une
information neuve ; il faut donc des lignes neuves.

Alternative envisagée et **rejetée** : faire passer un désaccord pour un masquage, en élargissant le
vocabulaire de masquage existant. Ce serait un mensonge structurel — la session concernée est **affichée**,
ce n'est pas elle qui est écartée mais l'un de ses signaux — et cela rouvrirait exactement la confusion
« absente » / « écartée par tel filtre » que la phase 22 a fermée.

Le changement reste donc **minimal et additif** : six lignes dans `BuildReportAsync`, une méthode de libellé
de source, un formateur d'écart dans la couche d'affichage partagée. **Aucune ligne existante n'est
supprimée ni réécrite.**

**À NE PAS CASSER :**
- Phase 22 : `new SessionMonitor` absent de `DiagnosticService.cs` ; `_moniteurSessions.Inspecter(_clock.UtcNow)` présent.
- Phase 20 : `?? new ` reste à **2** dans `DiagnosticService.cs`.
- Phase 22 : le nom du type de motif de masquage apparaît **3 fois** dans `DiagnosticService.cs` (une
  signature + deux branches) et ce compte doit rester **inchangé** — **aucun commentaire ni libellé ajouté
  par ce plan ne doit écrire ce nom** ; la prose dit « le vocabulaire de masquage ».
- Phase 24-01 : `GardesPerimetre` reste à **10/10** ; `SessionMonitor.cs`, `ArbitrageSessions.cs`,
  `LectureSessions.cs` ne sont **pas** touchés par ce plan.
- `AffichageSessions.cs` est dans `Services/` et **n'est pas exempté** de `NormalisationUniqueTests` :
  aucune conversion d'époque, aucune division ni multiplication par cent, commentaires compris.
- Aucun type WPF dans `Services/`. UI et commentaires en **français**.
- **Baseline d'entrée de plan** : le total mesuré en fin de 24-01 (consigné dans `24-01-SUMMARY.md`).
  Aucun test supprimé, 0 échec.
- **Ne pas anticiper** les phases 25 et 26 : aucun nouvel événement, aucune notion de « traité ».

**SÉCURITÉ :** identique au plan 24-01 — dossier `sessions\` (66 entrées) et `archived.json` (84 octets)
intouchés, tests sous `Path.GetTempPath()` uniquement, overlay pid 119412 ni lancé ni tué, `oauth.dat`
taille seule (518 octets) et **jamais** son mtime, aucune requête réseau, aucune dépendance NuGet.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : le rapport nomme la source retenue, la source écartée et l'écart d'âge</name>
  <files>src/Chronos/Services/AffichageSessions.cs, src/Chronos/Services/DiagnosticService.cs, tests/Chronos.Tests/AffichageSessionsTests.cs, tests/Chronos.Tests/DiagnosticServiceTests.cs</files>

  <read_first>
    - src/Chronos/Services/DiagnosticService.cs (lignes 515-600 : le bloc du moniteur, `LibelleMotif`, `Court`)
    - src/Chronos/Services/AffichageSessions.cs (intégralité — 60 lignes)
    - src/Chronos/Services/ArbitrageSessions.cs (le contrat `DesaccordSources`)
    - tests/Chronos.Tests/DiagnosticServiceTests.cs (lignes 515-630 : `Rapport`, `TempDir22`, `SourceFixe22`, `EcrireEtat`)
    - tests/Chronos.Tests/AffichageSessionsTests.cs (le style des `[Theory]` de libellé)
    - tests/Chronos.Tests/NormalisationUniqueTests.cs (les motifs interdits qui balaient `AffichageSessions.cs`)
  </read_first>

  <behavior>
    - **Le formateur d'écart.** `AffichageSessions.Ecart(TimeSpan)` rend `"0 s"` pour zéro, puis `"45 s"`,
      `"7 min"`, `"7 h"`. Il ne dit **jamais** « il y a » ni « à l'instant » : « il y a 7 h » situe un
      instant, « 7 h » mesure une distance, et c'est la distance qui rend un désaccord lisible. Testé par
      `[Theory]` sur 4 valeurs, dont **0 s** — le cas d'égalité d'âge, qui doit se voir.
    - **Un désaccord se lit en une ligne.** Transcript `Working` à `T-10 s` + fichier de hook
      `WaitingAttention` / `permission_prompt` à `T-7 h`, même identifiant. Le rapport contient
      `Désaccords entre sources : 1` et **une seule** ligne portant l'identifiant court, où figurent
      **tous** ces éléments : `retenu`, le libellé du transcript, `en cours`, `écarté`, le libellé du
      fichier de hook, `à toi`, et l'écart d'âge.
    - **La session reste AFFICHÉE.** Le même rapport annonce `Sessions AFFICHÉES par le widget : 1` et
      `Sessions MASQUÉES par un filtre : 0`. Un désaccord n'est pas un masquage — assertion explicite.
    - **Le silence se dit.** Sans contradiction (une seule source), le rapport porte
      `Désaccords entre sources : 0` **et** la ligne `(aucun — aucune source n'en contredit une autre en ce moment)`.
    - **Sans moniteur, rien n'est raconté.** `Rapport(null)` ne contient **pas** `Désaccords entre sources`.
      Le test existant `Sans_moniteur_le_rapport_dit_qu_il_n_a_rien_observe` est **étendu** d'un
      `Assert.DoesNotContain` ; aucune de ses lignes n'est supprimée.
    - **Chaque source est nommée avec son dossier.** Le libellé du hook contient `Chronos\sessions`, celui
      du transcript contient `.claude/projects`. C'est ce qui permet d'aller ouvrir la source figée.
  </behavior>

  <action>
Ordre d'exécution — trois commits, **chacun compilable** :
**(a)** `AffichageSessions.Ecart` + ses tests → vert immédiat ;
**(b)** les tests du rapport → **ROUGE** comportemental ;
**(c)** le bloc de rapport + `LibelleSource` → **VERT**.

***

**(a)** `src/Chronos/Services/AffichageSessions.cs` — ajouter à la suite de `Age` (la méthode se termine
à la ligne 55, juste avant l'accolade fermante de la classe) :

```csharp
    /// <summary>Un ÉCART d'âge entre deux signaux, et non une ancienneté. « il y a 7 h » situe un instant,
    /// « 7 h » mesure une distance : confondre les deux rendrait un désaccord illisible, alors que c'est
    /// précisément l'écart qui dit à l'utilisateur qu'une de ses sources est FIGÉE. Zéro se dit « 0 s » et
    /// non « à l'instant » — c'est le cas d'égalité d'âge, il doit se voir.</summary>
    public static string Ecart(System.TimeSpan d)
    {
        if (d < System.TimeSpan.FromSeconds(60)) return $"{(int)d.TotalSeconds} s";
        if (d < System.TimeSpan.FromHours(1)) return $"{(int)d.TotalMinutes} min";
        return $"{(int)d.TotalHours} h";
    }
```

Tests dans `tests/Chronos.Tests/AffichageSessionsTests.cs`, à la suite de
`L_anciennete_se_dit_au_grain_du_widget` :

```csharp
    /// <summary>FUS-02 — un écart d'âge n'est pas une ancienneté, et le zéro (égalité d'âge) doit se voir.</summary>
    [Theory]
    [InlineData(0, "0 s")]
    [InlineData(45, "45 s")]
    [InlineData(420, "7 min")]
    [InlineData(25200, "7 h")]
    public void L_ecart_d_age_se_dit_en_distance_pas_en_instant(int secondes, string attendu)
        => Assert.Equal(attendu, AffichageSessions.Ecart(TimeSpan.FromSeconds(secondes)));
```

Commit `feat(24-02): un écart d'âge se dit en distance, pas en instant`.

***

**(b) puis (c)** `src/Chronos/Services/DiagnosticService.cs`.

Insérer le bloc ci-dessous **après la ligne 548**
(`sb.AppendLine("    (aucune — aucun filtre n'écarte de session en ce moment)");`) et **avant** l'accolade
fermante du `try` — donc dans le **même** `try` que les visibles et les masquées, sur la **même** lecture :

```csharp
                // FUS-02 — un DÉSACCORD n'est pas un écartement de session : celle-ci est AFFICHÉE, juste
                // au-dessus. Ce qui a été écarté, c'est l'un de ses SIGNAUX. Les ranger avec le vocabulaire
                // de masquage rouvrirait la confusion « absente » / « écartée par tel filtre » que la
                // phase 22 a fermée, et noierait le seul cas qui compte ici : une SOURCE FIGÉE.
                // Cas fondateur, mesuré le 2026-09-12 : un fichier de hook immobile depuis 7 h annonçait
                // « à toi » pendant qu'un transcript de 10 s prouvait le contraire. Le hook gagnait, et rien
                // ne le disait. Depuis la phase 24 il perd — mais sans cette ligne, l'utilisateur n'aurait
                // toujours aucun moyen d'apprendre que sa source est morte.
                sb.AppendLine($"  Désaccords entre sources : {lecture.Desaccords.Count}");
                foreach (var d in lecture.Desaccords)
                    sb.AppendLine($"    · {Court(d.SessionId)} — retenu {LibelleSource(d.SourceRetenue)}"
                                + $" « {AffichageSessions.Etat(d.EtatRetenu)} » ; écarté {LibelleSource(d.SourceEcartee)}"
                                + $" « {AffichageSessions.Etat(d.EtatEcarte)} », plus ancien de {AffichageSessions.Ecart(d.EcartAge)}");
                if (lecture.Desaccords.Count == 0)
                    sb.AppendLine("    (aucun — aucune source n'en contredit une autre en ce moment)");
```

Puis, **juste après** la méthode `LibelleMotif` (elle se termine à la ligne 586), ajouter :

```csharp
    // La source NOMMÉE AVEC SON DOSSIER, exactement comme un filtre est nommé avec son fichier : « hook »
    // n'apprend rien, « fichier de hook (%APPDATA%\Chronos\sessions) » dit où aller regarder quand une
    // source se fige. C'est toute la différence entre constater un désaccord et pouvoir le diagnostiquer seul.
    private static string LibelleSource(SourceSession s) => s switch
    {
        SourceSession.Hook       => @"fichier de hook (%APPDATA%\Chronos\sessions)",
        SourceSession.Transcript => "transcript (~/.claude/projects)",
        _                        => "une source non nommée",
    };
```

Tests dans `tests/Chronos.Tests/DiagnosticServiceTests.cs`, à la suite de
`Une_session_archivee_est_annoncee_masquee_par_le_magasin_d_archives`. Réutiliser `TempDir22`,
`TempFichier22`, `SourceFixe22` et `Rapport` — **ne créer aucune nouvelle aide** :

```csharp
    // --- Phase 24 (FUS-02) : les désaccords entre sources deviennent lisibles ---

    /// <summary>Le relevé du 2026-09-12, rejoué de bout en bout : un fichier de hook figé depuis 7 h contre
    /// un transcript de 10 s. La phase 24 fait gagner le transcript ; cette ligne-ci dit à l'utilisateur
    /// POURQUOI — et surtout que sa source de hooks ne bouge plus.</summary>
    [Fact]
    public async Task Un_desaccord_nomme_la_source_retenue_la_source_ecartee_et_l_ecart_d_age()
    {
        const string Id = "e465420e-83e0-428f-97f1-f0174c0848fc";
        var dir = TempDir22();
        System.IO.File.WriteAllText(System.IO.Path.Combine(dir, Id + ".json"),
            SessionHookProcessor.BuildStateJson(Id, "PROJET ADVANCED SHEET", SessionActivity.WaitingAttention,
                "permission_prompt", T22.AddHours(-7).ToUnixTimeMilliseconds()));

        var moniteur = new SessionMonitor(dir,
            new SourceFixe22(new SessionSnapshot(Id, "PROJET ADVANCED SHEET", SessionActivity.Working,
                                                 null, T22.AddSeconds(-10))),
            new ArchiveStore(TempFichier22()));

        var report = await Rapport(moniteur);

        Assert.Contains("Désaccords entre sources : 1", report);

        // La ligne à puce du désaccord, isolée par ses deux marqueurs : l'identifiant ET le mot « retenu ».
        var ligne = report.Split('\n').Single(l => l.Contains("e465420e") && l.Contains("retenu "));
        Assert.Contains("transcript (~/.claude/projects)", ligne);
        Assert.Contains("en cours", ligne);
        Assert.Contains(@"fichier de hook (%APPDATA%\Chronos\sessions)", ligne);
        Assert.Contains("à toi", ligne);
        Assert.Contains("plus ancien de 6 h", ligne);   // 7 h moins 10 s → 6 h 59 min 50 s, tronqué à l'heure

        // Un désaccord n'est PAS un masquage : la session est bel et bien à l'écran.
        Assert.Contains("Sessions AFFICHÉES par le widget : 1", report);
        Assert.Contains("Sessions MASQUÉES par un filtre : 0", report);
    }

    /// <summary>Le silence se DIT. Un rapport muet sur les désaccords laisserait croire qu'il n'a pas regardé —
    /// c'est la même faute, une octave plus bas, que celle que la phase 22 a corrigée sur les masquages.</summary>
    [Fact]
    public async Task Sans_contradiction_le_rapport_annonce_zero_desaccord_et_le_dit()
    {
        var moniteur = new SessionMonitor(TempDir22(),
            new SourceFixe22(new SessionSnapshot("solo-0001", "Projet", SessionActivity.Working, null, T22)),
            new ArchiveStore(TempFichier22()));

        var report = await Rapport(moniteur);

        Assert.Contains("Désaccords entre sources : 0", report);
        Assert.Contains("aucune source n'en contredit une autre", report);
    }
```

Et **étendre** (sans rien supprimer) `Sans_moniteur_le_rapport_dit_qu_il_n_a_rien_observe` d'une assertion :

```csharp
        Assert.DoesNotContain("Désaccords entre sources", report);
```

Commits : `test(24-02): les désaccords entre sources doivent être lisibles dans le rapport`
puis `feat(24-02): le rapport nomme la source retenue, l'écartée et l'écart d'âge (FUS-02)`.

**Attention au chiffre `6 h`.** L'écart vaut `7 h − 10 s`, soit 6 h 59 min 50 s ; `Ecart` tronque à l'heure
inférieure → `"6 h"`. Si l'exécution rend autre chose, **ne pas ajuster le test en silence** : mesurer,
comprendre, et inscrire l'écart et sa raison dans le SUMMARY (leçon 22-03).
  </action>

  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~Diagnostic|FullyQualifiedName~Affichage"</automated>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "public static string Ecart" src/Chronos/Services/AffichageSessions.cs` → **1**.
    - `grep -cE "FromUnixTimeMilliseconds|FromUnixTimeSeconds|DateTimeOffset\.TryParse" src/Chronos/Services/AffichageSessions.cs` → **0**.
    - `grep -cF "L_ecart_d_age_se_dit_en_distance_pas_en_instant" tests/Chronos.Tests/AffichageSessionsTests.cs` → **1**.
    - `grep -cF "Désaccords entre sources" src/Chronos/Services/DiagnosticService.cs` → **1**
      (le seul `AppendLine` du compte ; le commentaire fourni n'écrit PAS cette chaîne, et le libellé de
      vacuité non plus).
    - `grep -cF "lecture.Desaccords" src/Chronos/Services/DiagnosticService.cs` → **3**
      (le compte, le `foreach`, le test de vacuité).
    - `grep -cF "AffichageSessions.Ecart(" src/Chronos/Services/DiagnosticService.cs` → **1**.
    - `grep -cF "private static string LibelleSource(SourceSession s)" src/Chronos/Services/DiagnosticService.cs` → **1**.
    - `grep -cF "LibelleSource(" src/Chronos/Services/DiagnosticService.cs` → **2** (la signature + la ligne
      du `AppendLine` qui l'appelle deux fois sur une seule et même ligne logique — recompter après écriture
      et inscrire le **chiffre réel** dans le SUMMARY si le formatage sur trois lignes en produit **3**).
    - `grep -cF "MotifMasquage" src/Chronos/Services/DiagnosticService.cs` → **3, INCHANGÉ** (aucun
      commentaire ni libellé de ce plan n'écrit ce nom).
    - `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` → **0** (acquis phase 22).
    - `grep -cF "?? new " src/Chronos/Services/DiagnosticService.cs` → **2** (acquis phase 20).
    - `grep -cF "_moniteurSessions.Inspecter(_clock.UtcNow)" src/Chronos/Services/DiagnosticService.cs` → **1**.
    - `git diff -U0 -- tests/Chronos.Tests/DiagnosticServiceTests.cs | grep -c "^-[^-]"` → **0**
      (aucune ligne préexistante supprimée : l'extension du test sans moniteur est purement additive).
    - `git diff --stat -- src/Chronos/Services/SessionMonitor.cs src/Chronos/Services/ArbitrageSessions.cs src/Chronos/Services/LectureSessions.cs` → **vide**.
    - Filtres `Diagnostic` et `Affichage` : **0 échec**.
    - Suite complète : **0 échec**, total **>** celui mesuré en fin de 24-01, aucun test supprimé.
    - `GardesPerimetre` **10/10**, `NormalisationUnique` **3/3**, `ServicesLayerPurity` **2/2**,
      `CompositionRoot` **5/5**, `GardesDoctrine` **8/8**.
  </acceptance_criteria>

  <done>
Le rapport de diagnostic annonce le nombre de désaccords, et pour chacun : l'identifiant court, la source
retenue **avec son dossier**, son état, la source écartée **avec son dossier**, son état, et l'écart d'âge.
La session reste comptée parmi les **affichées**, jamais parmi les masquées. Sans moniteur, rien n'est
raconté. Aucune ligne préexistante de `DiagnosticService.cs` n'a été supprimée ni réécrite.
  </done>
</task>

<task type="auto">
  <name>Task 2 : la carte de vérification de la phase, remplie et mesurée</name>
  <files>.planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md (le squelette à remplir)
    - .planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-VALIDATION.md (le modèle de la phase précédente)
    - .planning/phases/24-l-arbitrage-par-fraicheur/24-01-SUMMARY.md (les chiffres de la vague 1)
    - .planning/ROADMAP.md (les quatre critères de succès de la phase 24, lignes 193-212)
    - .planning/REQUIREMENTS.md (FUS-01, FUS-02 et le tableau de traçabilité, lignes 113-114)
  </read_first>

  <action>
Remplir `.planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md`, section par section :

1. **Frontmatter** : `status: validated`, `validated: <date du jour>`.
2. **Infrastructure de test** : baseline d'entrée **763**, total mesuré après 24-01, total mesuré après
   24-02, **deux exécutions consécutives au même total**, durée de la suite.
3. **Les quatre critères du ROADMAP**, un par un, chacun avec **sa preuve nommée** (nom exact du test ou
   commande exacte) et **son résultat mesuré**. Aucune case ne reste en `(à mesurer)`.
4. **Le tableau des cinq lignes mesurées** du relevé du 2026-09-12, avec pour chacune le test qui la couvre
   et sa **qualification honnête** :
   - *rouge avant le correctif* — les trois inversions du ROADMAP (hook `Working` de 25 min, hook
     `WaitingTurn` de 7 h, hook `WaitingAttention` de 7 h) ;
   - *témoin* — le transcript seul ;
   - *contrôle de non-régression* — le hook de 9 h, dont le résultat était déjà `Working` et dont la seule
     assertion neuve est « 0 désaccord ».
   **Ne jamais écrire que les cinq étaient rouges : trois l'étaient.** C'est la doctrine du milestone
   appliquée à la carte elle-même.
5. **Ce qui rendrait cette phase creuse** — deux livraisons qui produisent le même écran le jour J :

   | Livraison | Ce qu'on lit le jour J | Ce qui se passe la semaine suivante |
   |---|---|---|
   | Inverser la priorité : « le transcript prime toujours » | les inversions mesurées disparaissent | une vraie demande de permission — que seul le hook sait exprimer — est écrasée par un transcript à peine plus récent : le défaut a changé de sens, pas de nature |
   | Arbitrer sur la FRAÎCHEUR, égalité tranchée par une règle nommée | les inversions disparaissent | un signal ne perd que parce qu'il est plus vieux, et on peut lire pourquoi dans le rapport |

   Puis les preuves structurelles qui les distinguent, avec leurs valeurs attendues :
   ```sh
   grep -cF "byId["                       src/Chronos/Services/SessionMonitor.cs      # attendu : 0
   grep -cF "ArbitrageSessions.Trancher(" src/Chronos/Services/SessionMonitor.cs      # attendu : 1
   grep -cF "DateTimeOffset.UtcNow"       src/Chronos/Services/ArbitrageSessions.cs   # attendu : 0
   grep -cF "AffichageSessions.Urgence"   src/Chronos/Services/ArbitrageSessions.cs   # attendu : 1
   grep -cF "lecture.Desaccords"          src/Chronos/Services/DiagnosticService.cs   # attendu : 3
   grep -cF "new SessionMonitor"          src/Chronos/Services/DiagnosticService.cs   # attendu : 0
   ```
6. **Le tableau des mutations de falsification** : la mutation jouée en 24-01 Task 3 (alias temporaire), le
   test tombé, le compte exact d'échecs/succès sur le filtre, et la preuve de révocation.
7. **Les invariants de sécurité**, mesurés avant et après : `sessions\` **66** entrées, `archived.json`
   **84** octets, `oauth.dat` **518** octets (**taille seule, jamais le mtime**), overlay pid 119412 vivant
   (`tasklist` seul), `git diff -- '*.csproj'` vide, **0** dépendance NuGet, **0** requête réseau.
8. **`À VÉRIFIER PAR L'UTILISATEUR`** — ce que seul un vrai lancement établit :
   - **critère n°1 in vivo** : une session en cours de travail s'affiche « en cours » même si un vieux
     fichier de hook traîne dans le dossier ;
   - **critère n°3 in vivo** : **clic droit → Diagnostic…**, lire le bloc « Désaccords entre sources » ;
     une source figée doit y être **nommée avec son dossier** et son écart d'âge ;
   - **critère n°2 in vivo** : aucun état affiché ne doit changer d'un tick à l'autre sans qu'une donnée
     ait changé ;
   - **le rappel décisif** : *rien de ce milestone ne s'exécute tant que l'exe n'est pas republié* — les
     hooks de `~/.claude/settings.json` pointent l'exe **v3.0.2** actuellement en cours d'exécution,
     antérieur à tout le milestone. Republier (version **dans** l'exe **et** dans le nom du fichier publié)
     reste à faire en fin de milestone ;
   - les reliquats hérités des phases 21, 22 et 23, à présenter **groupés** en fin de milestone.
9. `.planning/REQUIREMENTS.md` : cocher **FUS-01** et **FUS-02** (`- [x]`), et passer leurs deux lignes du
   tableau de traçabilité (lignes 113-114) de `Pending` à `Complete`.

Commit `docs(24-02): la carte de vérification de la phase 24, remplie et mesurée`.
  </action>

  <verify>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; grep -c "(à mesurer)" .planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md; grep -c "status: validated" .planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md; grep -c "À VÉRIFIER PAR L'UTILISATEUR" .planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md</automated>
    <automated>cd "C:/Users/Tanguy/Documents/PROGRAMMES/DEV/PROJET OVERLAY" &amp;&amp; dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "(à mesurer)" .planning/phases/24-l-arbitrage-par-fraicheur/24-VALIDATION.md` → **0**.
    - `grep -c "status: validated" …/24-VALIDATION.md` → **1**.
    - `grep -c "À VÉRIFIER PAR L'UTILISATEUR" …/24-VALIDATION.md` → **1**.
    - `grep -c "contrôle de non-régression" …/24-VALIDATION.md` → **≥ 1** (la qualification honnête de la
      cinquième ligne mesurée est écrite noir sur blanc).
    - Les **quatre** critères du ROADMAP apparaissent chacun avec un nom de test ou une commande, et un
      résultat mesuré : `grep -c "^| *[1-4] " …/24-VALIDATION.md` → **≥ 4** (ou l'équivalent selon la mise
      en forme retenue ; le critère de fond est : aucune case vide).
    - `grep -cF "- [x] **FUS-01**" .planning/REQUIREMENTS.md` → **1** et `… **FUS-02**` → **1**.
    - `grep -c "| FUS-0[12] | Phase 24 | Pending |" .planning/REQUIREMENTS.md` → **0**.
    - `git diff --stat` de la tâche ne mentionne que `24-VALIDATION.md` et `REQUIREMENTS.md`.
    - Suite complète revérifiée après écriture des documents : **0 échec**, même total qu'en fin de Task 1.
  </acceptance_criteria>

  <done>
`24-VALIDATION.md` porte `status: validated`, les quatre critères du ROADMAP avec leur preuve nommée et leur
résultat mesuré, le tableau des cinq lignes du relevé avec leur qualification honnête, les preuves
structurelles, la mutation et sa révocation, les invariants de sécurité, et la liste de ce qui reste à voir
de ses yeux. FUS-01 et FUS-02 sont cochées dans `REQUIREMENTS.md`.
  </done>
</task>

</tasks>

<verification>
1. **Suite complète verte, deux fois de suite, au même total** :
   `dotnet test Chronos.sln -c Debug --nologo -v q` → 0 échec, aucun test supprimé.
2. **FUS-02 par le comportement** : le rapport porte le bloc « Désaccords entre sources », avec le compte,
   une ligne par désaccord nommant les deux sources **et leurs dossiers**, les deux états et l'écart d'âge.
3. **FUS-02 par la structure** : la même lecture partagée alimente visibles, masquées et désaccords
   (`_moniteurSessions.Inspecter(_clock.UtcNow)` → **1**, `new SessionMonitor` → **0**).
4. **Un désaccord n'est pas un masquage** : la session concernée est comptée dans « Sessions AFFICHÉES ».
5. **Acquis intacts** : `?? new ` → 2 ; le compte du type de motif de masquage → 3, inchangé ;
   `GardesPerimetre` 10/10 ; `NormalisationUnique` 3/3 ; `ServicesLayerPurity` 2/2 ;
   `CompositionRoot` 5/5 ; `GardesDoctrine` 8/8.
6. **Sécurité** : `ls "$APPDATA/Chronos/sessions" | wc -l` → **66** (avant ET après) ;
   `archived.json` → **84** octets ; `oauth.dat` → **518** octets (taille seule) ;
   overlay pid 119412 vivant, ni lancé ni tué ; `git diff -- '*.csproj'` vide ; 0 dépendance NuGet.
</verification>

<success_criteria>
- `AffichageSessions.Ecart` existe, dit une distance et non un instant, et affiche `0 s` pour l'égalité d'âge.
- Le rapport de diagnostic nomme, pour chaque désaccord : la source retenue, la source écartée, et l'écart
  d'âge — chaque source assortie du dossier à ouvrir.
- L'absence de désaccord est **dite**, pas tue.
- Sans moniteur injecté, aucun désaccord n'est raconté.
- `DiagnosticService.cs` ne perd ni ne réécrit une seule ligne existante ; le changement est additif et justifié.
- `24-VALIDATION.md` est rempli, mesuré, `status: validated`, avec la qualification honnête des cinq lignes.
- FUS-01 et FUS-02 cochées dans `REQUIREMENTS.md`.
- Suite : 0 échec, aucun test supprimé.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/24-l-arbitrage-par-fraicheur/24-02-SUMMARY.md`.

Y consigner impérativement :
- le compte **exact** de l'étape ROUGE (échecs / succès / total) au commit `test(24-02)` ;
- les chiffres de `grep` **réellement obtenus** en regard des valeurs prédites — en particulier
  `LibelleSource(` (prédit 2, à recompter selon le formatage réel) et `Désaccords entre sources` (prédit 1) :
  un écart se **documente**, il ne s'absorbe pas (leçon 22-03) ;
- la valeur réelle rendue par `Ecart` pour `7 h − 10 s` ;
- la **justification** du changement de `DiagnosticService.cs` et la preuve qu'il est purement additif
  (`git diff -U0 | grep -c "^-[^-]"` sur le fichier) ;
- l'état de clôture de la phase : FUS-01 et FUS-02 couvertes, ce qui reste à vérifier in vivo, et le rappel
  que **rien ne s'exécute tant que l'exe n'est pas republié**.
</output>
