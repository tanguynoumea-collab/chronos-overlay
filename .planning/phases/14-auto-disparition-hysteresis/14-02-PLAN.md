---
phase: 14-auto-disparition-hysteresis
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/Chronos/Services/WindowsForegroundWatch.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
autonomous: true
requirements: [NET-02]
must_haves:
  truths:
    - "En conditions réelles, la fenêtre Claude au premier plan de l'OS pilote la branche NET-02 (acquittement par focus)"
    - "Si le focus est indisponible, NET-02 ne déclenche pas, sans erreur ni faux traitement"
    - "Le graphe DI complet (avec le focus réel injecté dans SessionMonitor) se résout au démarrage"
  artifacts:
    - path: "src/Chronos/Services/WindowsForegroundWatch.cs"
      provides: "Implémentation RÉELLE d'IForegroundWatch (Win32 GetForegroundWindow, titre contient « Claude »), best-effort, ne lève jamais"
      contains: "class WindowsForegroundWatch"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "SessionMonitor"
      via: "injection de WindowsForegroundWatch comme 7e paramètre (foreground) => NET-02 devient vivant"
      pattern: "WindowsForegroundWatch"
    - from: "tests/Chronos.Tests/CompositionRootTests.cs"
      to: "graphe DI bureau"
      via: "garde de résolution incluant IForegroundWatch + SessionMonitor avec focus injecté"
      pattern: "IForegroundWatch"
---

<objective>
Rendre la branche NET-02 VIVANTE en conditions réelles : fournir l'implémentation OS-dépendante d'`IForegroundWatch`
(`WindowsForegroundWatch`, via Win32 `GetForegroundWindow`), la câbler dans la DI et l'injecter dans
`SessionMonitor` (7e paramètre `foreground`). Étendre la garde DI réelle pour couvrir le nouveau service.

Purpose: NET-02 — une session bureau en attente gardée au premier plan de l'OS ≥ ~2,5 s est acquittée et
disparaît. La logique a été prouvée au plan 01 (faux focus) ; ici on branche le focus RÉEL, partie OS-dépendante
minimale, best-effort (indisponible → NET-02 ne déclenche pas, sans erreur).

Output: `WindowsForegroundWatch.cs` (neuf), `App.xaml.cs` (DI), `CompositionRootTests.cs` (garde étendue).
Suite `dotnet test` verte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-auto-disparition-hysteresis/14-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contrats livrés par le plan 01 (déjà sur disque quand ce plan s'exécute). -->

From src/Chronos/Services/IForegroundWatch.cs (plan 01) :
```csharp
public interface IForegroundWatch { bool IsClaudeForeground(); } // best-effort, non bloquant, NE LÈVE JAMAIS
```

From src/Chronos/Services/SessionMonitor.cs (plan 01) — ctor déjà étendu :
```csharp
public SessionMonitor(string? sessionsDir = null, TranscriptSessionSource? transcripts = null,
    ArchiveStore? archive = null, ISessionSource? desktop = null,
    TreatedStore? treated = null, SessionTreatmentTracker? tracker = null, IForegroundWatch? foreground = null);
// Read appelle _foreground?.IsClaudeForeground() (try/catch) et passe le bool à tracker.Observe.
```

Patron d'un service réel OS-dépendant NON unit-testé : `src/Chronos/Services/WindowsUiaTreeProvider.cs`
(dégradation silencieuse, jamais d'exception qui remonte ; correction jugée en exécution, garde DI par résolution).

Câblage DI actuel (App.xaml.cs, après plan 01) :
```csharp
services.AddSingleton(_ => new TreatedStore());
services.AddSingleton(sp => new SessionTreatmentTracker(sp.GetRequiredService<TreatedStore>()));
services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
    sp.GetRequiredService<DesktopUiaSessionSource>(),
    sp.GetRequiredService<TreatedStore>(),
    sp.GetRequiredService<SessionTreatmentTracker>()));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WindowsForegroundWatch (focus réel Win32) + injection DI</name>
  <read_first>src/Chronos/Services/WindowsUiaTreeProvider.cs, src/Chronos/Services/IForegroundWatch.cs, src/Chronos/App.xaml.cs</read_first>
  <files>src/Chronos/Services/WindowsForegroundWatch.cs, src/Chronos/App.xaml.cs</files>
  <action>
Créer `src/Chronos/Services/WindowsForegroundWatch.cs` — `namespace Chronos.Services;`,
`public sealed class WindowsForegroundWatch : IForegroundWatch`. Implémentation RÉELLE et OS-dépendante,
MINCE et SANS logique métier (comme `WindowsUiaTreeProvider`). AUCUN type WPF (Win32 P/Invoke pur → couche
neutre, `ServicesLayerPurityTests` reste vert). NE LÈVE JAMAIS.

Principe (aligné sur la façon dont `WindowsUiaTreeProvider.FindClaudeWindow` reconnaît la fenêtre) : la fenêtre
Claude est au premier plan si le TITRE de la fenêtre de premier plan de l'OS contient « Claude » (insensible à
la casse). Rapide (quelques µs), non bloquant → appelable depuis le chemin synchrone `Read` (thread UI) sans
risque de figer l'overlay (cf. CLAUDE.md, coûts de composition). Best-effort.

P/Invoke (`using System.Runtime.InteropServices;`, `using System.Text;`) :
```csharp
[DllImport("user32.dll")]
private static extern System.IntPtr GetForegroundWindow();

[DllImport("user32.dll", CharSet = CharSet.Unicode)]
private static extern int GetWindowText(System.IntPtr hWnd, StringBuilder text, int count);
```

`public bool IsClaudeForeground()` :
- Tout le corps sous `try { ... } catch { return false; }` (best-effort : indisponible/erreur → false, la branche
  NET-02 ne déclenche simplement pas — décision d'honnêteté du CONTEXT.md).
- `var h = GetForegroundWindow(); if (h == System.IntPtr.Zero) return false;`
- Lire le titre : `var sb = new StringBuilder(256); var len = GetWindowText(h, sb, sb.Capacity); if (len <= 0) return false;`
- `return sb.ToString().IndexOf("Claude", System.StringComparison.OrdinalIgnoreCase) >= 0;`

Commentaire XML de classe (en FRANÇAIS) : explique que ce type N'EST PAS unit-testé (dépend d'une vraie
fenêtre + de l'OS, comme `WindowsUiaTreeProvider`) ; sa correction se juge en exécution ; la garde DI (test de
résolution) prouve seulement le câblage. Rappeler qu'un titre contenant « Claude » est le MÊME critère souple
que la découverte de fenêtre UIA de la Phase 13 (cohérence, robustesse aux versions).

Puis `src/Chronos/App.xaml.cs`, bloc « Source BUREAU (UIA) » : enregistrer le service et l'INJECTER dans
`SessionMonitor` (7e paramètre `foreground`) :
```csharp
services.AddSingleton<IForegroundWatch>(_ => new WindowsForegroundWatch());
```
(à déclarer AVANT le `SessionMonitor` qui le consomme) et étendre l'enregistrement de `SessionMonitor` :
```csharp
services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
    sp.GetRequiredService<DesktopUiaSessionSource>(),
    sp.GetRequiredService<TreatedStore>(),
    sp.GetRequiredService<SessionTreatmentTracker>(),
    sp.GetRequiredService<IForegroundWatch>()));
```
  </action>
  <verify>
    <automated>grep -Eq "class WindowsForegroundWatch : IForegroundWatch" src/Chronos/Services/WindowsForegroundWatch.cs && grep -Eq "GetForegroundWindow" src/Chronos/Services/WindowsForegroundWatch.cs && grep -Eq "IForegroundWatch" src/Chronos/App.xaml.cs && grep -Eq "GetRequiredService<IForegroundWatch>" src/Chronos/App.xaml.cs && dotnet build src/Chronos/Chronos.csproj -c Debug</automated>
  </verify>
  <done>
`WindowsForegroundWatch` compile (P/Invoke Win32, ne lève jamais, best-effort titre « Claude »). DI enregistre
`IForegroundWatch` et l'injecte comme 7e paramètre de `SessionMonitor` → NET-02 devient vivant. 0 avertissement
/ 0 erreur au build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Étendre la garde DI réelle (résolution avec focus injecté)</name>
  <read_first>tests/Chronos.Tests/CompositionRootTests.cs</read_first>
  <files>tests/Chronos.Tests/CompositionRootTests.cs</files>
  <action>
Dans `tests/Chronos.Tests/CompositionRootTests.cs`, mettre à jour le test existant
`Le_graphe_DI_resout_les_services_bureau_UIA` pour REFLÉTER EXACTEMENT la sous-chaîne bureau d'`App.xaml.cs`
après ce plan (sinon la garde ne couvre plus la réalité). Ajouter, dans le même style que les enregistrements
déjà présents :
```csharp
services.AddSingleton(_ => new TreatedStore(
    System.IO.Path.Combine(System.IO.Path.GetTempPath(), "ChronosTreated_" + System.Guid.NewGuid().ToString("N") + ".json")));
services.AddSingleton(sp => new SessionTreatmentTracker(sp.GetRequiredService<TreatedStore>()));
services.AddSingleton<IForegroundWatch>(_ => new WindowsForegroundWatch());
```
et remplacer l'enregistrement `SessionMonitor` du test par la forme complète à 7 paramètres :
```csharp
services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
    sp.GetRequiredService<DesktopUiaSessionSource>(),
    sp.GetRequiredService<TreatedStore>(),
    sp.GetRequiredService<SessionTreatmentTracker>(),
    sp.GetRequiredService<IForegroundWatch>()));
```
Conserver les assertions existantes (`SessionMonitor` + `DesktopUiaPollService` résolus sans exception). Mettre à
jour le commentaire XML du test pour mentionner que la garde couvre désormais aussi `TreatedStore`,
`SessionTreatmentTracker` et `IForegroundWatch` (attrape une DI mal ordonnée qui ne planterait qu'au démarrage).
  </action>
  <verify>
    <automated>grep -Eq "GetRequiredService<IForegroundWatch>" tests/Chronos.Tests/CompositionRootTests.cs && dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --filter "FullyQualifiedName~CompositionRootTests"</automated>
  </verify>
  <done>
La garde DI réelle enregistre la sous-chaîne bureau COMPLÈTE (TreatedStore + tracker + IForegroundWatch + focus
injecté dans SessionMonitor) et prouve la résolution sans exception de `SessionMonitor` et `DesktopUiaPollService`.
Tout `CompositionRootTests` est vert.
  </done>
</task>

</tasks>

<verification>
- `dotnet build src/Chronos/Chronos.csproj -c Debug` : 0 avertissement / 0 erreur.
- `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj` : tous verts (aucune régression sur les 304 + les
  tests neufs du plan 01).
- `ServicesLayerPurityTests` reste vert : `WindowsForegroundWatch` est neutre (Win32 P/Invoke, aucun assembly WPF).
- Câblage NET-02 réel prouvé par la garde DI (résolution de `SessionMonitor` avec `IForegroundWatch` injecté).
</verification>

<success_criteria>
- NET-02 vivant en prod : la fenêtre Claude au premier plan de l'OS (titre « Claude ») pilote l'acquittement par
  focus déjà implémenté au plan 01.
- Focus indisponible/erreur → `IsClaudeForeground()` renvoie false → NET-02 ne déclenche pas, sans exception ni
  faux traitement (honnêteté/robustesse du CONTEXT.md).
- Le graphe DI complet (focus réel injecté dans `SessionMonitor`) se résout au démarrage (garde réelle verte).
</success_criteria>

<output>
After completion, create `.planning/phases/14-auto-disparition-hysteresis/14-02-SUMMARY.md`
</output>
