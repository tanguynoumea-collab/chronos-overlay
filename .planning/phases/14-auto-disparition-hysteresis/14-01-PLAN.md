---
phase: 14-auto-disparition-hysteresis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/TreatedStore.cs
  - src/Chronos/Services/IForegroundWatch.cs
  - src/Chronos/Services/SessionTreatmentTracker.cs
  - src/Chronos/Services/SessionMonitor.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/TreatedSessionsTests.cs
autonomous: true
requirements: [NET-01, NET-03, NET-04]
must_haves:
  truths:
    - "Une session en attente qui repasse en Working (répondu) disparaît de la liste"
    - "Une session traitée qui repart en attente (nouvel épisode) réapparaît"
    - "Une session archivée reste masquée pour toujours, même si elle repart en attente (distincte de traité)"
    - "La logique d'acquittement par focus (NET-02) est prouvée avec un faux focus + horloge injectée, même si le focus RÉEL est câblé au plan 02"
  artifacts:
    - path: "src/Chronos/Services/TreatedStore.cs"
      provides: "Magasin RÉVERSIBLE des sessions traitées (map SessionId->treatedWaitingTs, TTL 6 h, tmp+move, tolérant)"
      contains: "class TreatedStore"
    - path: "src/Chronos/Services/SessionTreatmentTracker.cs"
      provides: "Détecteur STATEFUL d'hystérésis (NET-01 répondu, NET-02 focus, NET-03 réapparition/purge) — logique pure"
      contains: "class SessionTreatmentTracker"
    - path: "src/Chronos/Services/IForegroundWatch.cs"
      provides: "Seam neutre du focus premier-plan OS (bool IsClaudeForeground())"
      contains: "interface IForegroundWatch"
    - path: "src/Chronos/Services/SessionMonitor.cs"
      provides: "Observe(tracker) + filtre treated au même endroit que archived"
      contains: "_tracker"
  key_links:
    - from: "src/Chronos/Services/SessionMonitor.cs"
      to: "SessionTreatmentTracker.Observe"
      via: "appel dans Read avant le filtre, sur les snapshots BRUTS fusionnés + focus + now"
      pattern: "_tracker\\?\\.Observe"
    - from: "src/Chronos/Services/SessionMonitor.cs"
      to: "TreatedStore.Load"
      via: "filtre de masquage (session présente dans treated => cachée)"
      pattern: "_treated\\?\\.Load"
    - from: "src/Chronos/App.xaml.cs"
      to: "SessionMonitor"
      via: "injection DI de TreatedStore + SessionTreatmentTracker (foreground=null tant que plan 02 non fait)"
      pattern: "SessionTreatmentTracker"
---

<objective>
Livrer le cœur de l'auto-disparition par hystérésis : un magasin RÉVERSIBLE `TreatedStore`, un détecteur
STATEFUL `SessionTreatmentTracker` (logique pure, testable sans fenêtre Claude), et le branchement du filtre
`treated` dans `SessionMonitor.Read` au même endroit que le filtre `archived`. Câblage DI du magasin + tracker
dans `App.xaml.cs` (le focus RÉEL NET-02 est branché au plan 02 ; ici `foreground=null` → branche NET-02
dormante mais son code et sa LOGIQUE sont présents et testés via un faux focus).

Purpose: Faire disparaître automatiquement les sessions « répondues » (NET-01), les faire réapparaître sur un
nouvel épisode d'attente (NET-03), en gardant l'archivage manuel distinct et permanent (NET-04). Prouver aussi
la logique de l'acquittement par focus (NET-02) avec un faux focus + horloge, prête à être câblée au plan 02.

Output: `TreatedStore.cs`, `SessionTreatmentTracker.cs`, `IForegroundWatch.cs` (neufs), `SessionMonitor.cs` +
`App.xaml.cs` (modifiés), `TreatedSessionsTests.cs` (neuf). Suite `dotnet test` verte (304 existants + neufs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-auto-disparition-hysteresis/14-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contrats existants que l'exécuteur utilise DIRECTEMENT — pas d'exploration nécessaire. -->

From src/Chronos/Services/SessionSnapshot.cs :
```csharp
public enum SessionActivity { Working, WaitingAttention, WaitingTurn, Unknown }
public enum SessionKind { Unknown, Chat, Code, Cowork }
public enum SessionOrigin { Cli, Desktop }
public sealed record SessionSnapshot(
    string SessionId, string Project, SessionActivity Activity, string? Reason,
    System.DateTimeOffset UpdatedAt,
    SessionKind Kind = SessionKind.Unknown, SessionOrigin Origin = SessionOrigin.Cli);
// « En attente » == Activity is WaitingTurn or WaitingAttention.
```

From src/Chronos/Services/ISessionSource.cs :
```csharp
public interface ISessionSource { IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now); }
```

From src/Chronos/Services/ArchiveStore.cs (PATRON à copier — permanent, non réversible) :
```csharp
public sealed class ArchiveStore {
    private static readonly TimeSpan Ttl = TimeSpan.FromHours(6);
    public ArchiveStore(string? path = null); // défaut %APPDATA%\Chronos\archived.json
    public ISet<string> Load();               // ids valides (TTL non dépassé)
    public void Add(string sessionId);        // atomique tmp+move, purge expirés
}
```

From src/Chronos/Services/SessionMonitor.cs — ctor ACTUEL (à étendre en fin, non cassant) :
```csharp
public SessionMonitor(string? sessionsDir = null, TranscriptSessionSource? transcripts = null,
    ArchiveStore? archive = null, ISessionSource? desktop = null);
public IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);
// Read : (1) transcripts -> byId ; (2) hooks (surcharge) ; (2.b) desktop (fusion) ; (3) filtre archived -> retour.
```

Clés de la source bureau (Phase 13) : foreground = `desktop:foreground:<kind>` (Origin=Desktop) ;
sidebar = `desktop:<kind>:<nom>`. La branche NET-02 (acquittement focus) ne vise QUE le premier plan
(`desktop:foreground:...`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TreatedStore (réversible) + seam IForegroundWatch</name>
  <read_first>src/Chronos/Services/ArchiveStore.cs, src/Chronos/Services/IUiaTreeProvider.cs</read_first>
  <files>src/Chronos/Services/TreatedStore.cs, src/Chronos/Services/IForegroundWatch.cs</files>
  <action>
Créer `src/Chronos/Services/TreatedStore.cs` — calqué EXACTEMENT sur `ArchiveStore` (mêmes patterns : TTL,
tmp+move, `JsonDocument` tolérant, chemins `%APPDATA%` via `Environment.GetFolderPath`), mais RÉVERSIBLE.
`namespace Chronos.Services;`, `public sealed class TreatedStore`. Aucun type WPF (couche neutre).

- Champ `private static readonly System.TimeSpan Ttl = System.TimeSpan.FromHours(6);` (même ordre de grandeur
  qu'`ArchiveStore`, borne la croissance du fichier).
- Champ `private readonly string _path;`
- Ctor `public TreatedStore(string? path = null)` → défaut
  `Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData), "Chronos", "treated.json")`.
- `public IReadOnlyDictionary<string, long> Load()` : lit la map `{ sessionId : treatedWaitingTs(ms) }`,
  ne garde QUE les entrées dont `now - ts < Ttl.TotalMilliseconds` (now = `DateTimeOffset.UtcNow.ToUnixTimeMilliseconds()`).
  Fichier absent/corrompu/racine non-objet → `Dictionary<string,long>` VIDE, jamais d'exception (try/catch englobant).
- `public void Set(string sessionId, long treatedWaitingTs)` : idempotent. Si `string.IsNullOrEmpty(sessionId)`
  → return. Recharge la map existante (en purgeant les expirés comme `ArchiveStore.Add`), fait
  `map[sessionId] = treatedWaitingTs`, réécrit atomiquement (`tmp = _path + ".tmp-" + Environment.ProcessId`,
  `File.WriteAllText(tmp, JsonSerializer.Serialize(map))`, `File.Move(tmp, _path, overwrite: true)`),
  `Directory.CreateDirectory` du dossier avant. Tout en try/catch tolérant.
- `public void Remove(string sessionId)` : c'est le point RÉVERSIBLE (NET-03) absent d'`ArchiveStore`. Recharge
  la map (purge expirés), `map.Remove(sessionId)`, réécrit atomiquement de la MÊME façon. Si la map devient vide,
  réécrire `{}` est acceptable. Tolérant.

Commentaire XML de classe (en FRANÇAIS) expliquant : magasin AUTO-géré et RÉVERSIBLE des sessions « traitées »,
DISTINCT d'`ArchiveStore` (archivé = permanent NET-04 ; traité = réversible via `Remove`).

Créer `src/Chronos/Services/IForegroundWatch.cs` — seam NEUTRE du focus premier-plan OS :
`public interface IForegroundWatch { bool IsClaudeForeground(); }` avec commentaire XML français : « La fenêtre
de l'app bureau Claude est-elle au premier plan de l'OS ? Best-effort, non bloquant, NE LÈVE JAMAIS ; si
indisponible → false (la branche NET-02 ne déclenche simplement pas). » L'implémentation réelle
(`WindowsForegroundWatch`) est livrée au plan 02.
  </action>
  <verify>
    <automated>grep -Eq "class TreatedStore" src/Chronos/Services/TreatedStore.cs && grep -Eq "public void Remove" src/Chronos/Services/TreatedStore.cs && grep -Eq "FromHours\(6\)" src/Chronos/Services/TreatedStore.cs && grep -Eq "interface IForegroundWatch" src/Chronos/Services/IForegroundWatch.cs && dotnet build src/Chronos/Chronos.csproj -c Debug</automated>
  </verify>
  <done>
`TreatedStore` compile avec `Load()`/`Set()`/`Remove()`, TTL 6 h, écriture atomique tmp+move, tolérance totale.
`IForegroundWatch` déclaré (interface neutre). 0 avertissement / 0 erreur au build.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SessionTreatmentTracker (hystérésis pure) + tests</name>
  <read_first>src/Chronos/Services/SessionSnapshot.cs, src/Chronos/Services/DesktopUiaSessionSource.cs, tests/Chronos.Tests/Fakes/FakeClock.cs</read_first>
  <files>src/Chronos/Services/SessionTreatmentTracker.cs, tests/Chronos.Tests/TreatedSessionsTests.cs</files>
  <behavior>
    - NET-01 (répondu) : une session vue « en attente » au cycle précédent, PRÉSENTE et non-attente maintenant
      (Working/Unknown) → `TreatedStore.Set(id, tsEpisode)`. La DISPARITION seule (session absente ce cycle) ne
      déclenche PAS le traitement (on garde le dernier état connu).
    - NET-02 (acquitté par focus) — logique testée ici avec un faux focus : une session premier-plan bureau
      (`Origin==Desktop` ET `SessionId` commence par `desktop:foreground:`) ACTUELLEMENT en attente, avec
      `claudeForeground==true` de façon CONTINUE depuis `>= 2,5 s` → `Set(id, tsEpisode)`. Toute interruption du
      focus (focus false, ou plus en attente) REMET à zéro le compteur (debounce anti-survol).
    - NET-03 (réapparition/purge) : une session actuellement en attente dont l'épisode d'attente courant est
      PLUS RÉCENT que le `treatedWaitingTs` mémorisé → `TreatedStore.Remove(id)` (elle réapparaîtra).
    - « Épisode d'attente » = horodatage (`now`) du moment où la session est passée EN attente depuis un état
      non-attente (ou depuis l'absence avec dernier état non-attente) ; STABLE tant qu'elle reste en attente.
      NB : indispensable car le snapshot bureau porte `UpdatedAt == now` à chaque poll → on ne peut PAS se fier
      à `UpdatedAt` pour dater l'épisode (sinon réapparition à chaque tick). Le tracker maintient donc lui-même
      l'horodatage d'épisode et possède l'ajout ET la purge.
  </behavior>
  <action>
Créer `src/Chronos/Services/SessionTreatmentTracker.cs` — `namespace Chronos.Services;`,
`public sealed class SessionTreatmentTracker`. STATEFUL, PUR (aucune I/O hors `TreatedStore`), aucun type WPF.

Constantes / champs :
- `private static readonly System.TimeSpan FocusAckDelay = System.TimeSpan.FromSeconds(2.5);` (debounce NET-02).
- `private readonly TreatedStore _store;`
- `private readonly System.Collections.Generic.Dictionary<string, SessionActivity> _lastActivity = new();`
  (dernier état VU par session ; persiste tant que la session existe, pas réinitialisé sur absence).
- `private readonly System.Collections.Generic.Dictionary<string, long> _waitingSince = new();`
  (horodatage ms d'épisode d'attente courant, stable pendant l'épisode).
- `private readonly System.Collections.Generic.Dictionary<string, System.DateTimeOffset> _focusSince = new();`
  (début de la tenue continue focus+attente pour la branche NET-02).
- Ctor `public SessionTreatmentTracker(TreatedStore store) => _store = store ?? throw new System.ArgumentNullException(nameof(store));`

Helpers privés :
- `private static bool IsWaiting(SessionActivity a) => a is SessionActivity.WaitingTurn or SessionActivity.WaitingAttention;`
- `private static bool IsForegroundDesktop(SessionSnapshot s) => s.Origin == SessionOrigin.Desktop && s.SessionId.StartsWith("desktop:foreground:", System.StringComparison.Ordinal);`

Méthode `public void Observe(IReadOnlyList<SessionSnapshot> raw, bool claudeForeground, System.DateTimeOffset now)` :
- `var nowMs = now.ToUnixTimeMilliseconds();`
- `var treated = _store.Load();` (une seule lecture par cycle, pour les tests de réapparition).
- Pour chaque `s` dans `raw` :
  - `var id = s.SessionId; var isWaiting = IsWaiting(s.Activity);`
  - `var wasWaiting = _lastActivity.TryGetValue(id, out var prev) && IsWaiting(prev);`
  - NET-01 : `if (wasWaiting && !isWaiting) { _store.Set(id, _waitingSince.TryGetValue(id, out var ep) ? ep : s.UpdatedAt.ToUnixTimeMilliseconds()); _waitingSince.Remove(id); _focusSince.Remove(id); }`
  - Suivi d'épisode + NET-02 :
    - `if (isWaiting) {`
      - `if (!wasWaiting) _waitingSince[id] = nowMs;` (nouvel épisode)
      - `if (claudeForeground && IsForegroundDesktop(s)) {`
        - `if (!_focusSince.ContainsKey(id)) _focusSince[id] = now;`
        - `else if (now - _focusSince[id] >= FocusAckDelay) _store.Set(id, _waitingSince[id]);`
      - `} else { _focusSince.Remove(id); }`
    - `} else { _focusSince.Remove(id); }`
  - NET-03 (réapparition) : `if (isWaiting && treated.TryGetValue(id, out var tts) && _waitingSince.TryGetValue(id, out var cur) && cur > tts) _store.Remove(id);`
  - `_lastActivity[id] = s.Activity;`

Écrire les commentaires en FRANÇAIS, en reliant chaque bloc à NET-01/02/03 et à la décision du CONTEXT.md.

Créer `tests/Chronos.Tests/TreatedSessionsTests.cs` (`namespace Chronos.Tests;`, xUnit). Helpers privés :
- `private static string TempFile() => Path.Combine(Path.GetTempPath(), "chronos-treated-" + Guid.NewGuid().ToString("N") + ".json");`
- `private static SessionSnapshot Cli(string id, SessionActivity a, DateTimeOffset t) => new(id, "Proj", a, null, t);`
- `private static SessionSnapshot Fg(SessionActivity a, DateTimeOffset t) => new("desktop:foreground:chat", "Claude (bureau)", a, null, t, SessionKind.Chat, SessionOrigin.Desktop);`

Tests (tracker DIRECT — aucune fenêtre Claude, aucun UIA) :
1. `TreatedStore_set_load_remove_et_purge_TTL` : `Set("a",100)` → `Load()` contient "a"=100 ; `Remove("a")` →
   absent ; une entrée avec ts vieux de > 6 h n'est pas rendue par `Load()`.
2. `NET01_repondu_marque_traitee` : t0 Observe [Cli("s",WaitingTurn,t0)] focus=false → store vide ;
   t1 Observe [Cli("s",Working,t1)] → store contient "s".
3. `NET01_disparition_seule_ne_traite_pas` : t0 [Cli("s",WaitingTurn,t0)] ; t1 [] (absente) → store VIDE
   (la disparition n'est pas un « répondu »).
4. `NET03_reapparition_purge_l_entree` : après NET01 (store contient "s"), Observe
   [Cli("s",WaitingTurn,t2)] avec t2 > t1 → `_store.Load()` ne contient PLUS "s" (nouvel épisode > traitedTs).
5. `NET02_focus_continu_2_5s_acquitte` : Observe [Fg(WaitingTurn,t)] claudeForeground=true à t0, t0+1.5s
   (pas encore traité), t0+3s (>=2,5s → store contient "desktop:foreground:chat").
6. `NET02_interruption_focus_remet_a_zero` : focus=true t0, focus=false t0+1s, focus=true t0+2s, focus=true
   t0+3s → PAS traité (le compteur est reparti à t0+2s, il ne reste que 1 s < 2,5 s).
7. `NET02_ne_declenche_pas_sans_focus` : Observe [Fg(WaitingTurn,...)] focus=false sur 5 cycles couvrant
   > 2,5 s → store VIDE.

Utiliser un `TreatedStore(TempFile())` par test. Aucune dépendance à `IClock` (le `now` est passé à `Observe`).
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj --filter "FullyQualifiedName~TreatedSessionsTests"</automated>
  </verify>
  <done>
`SessionTreatmentTracker` compile ; les 7 tests passent : NET-01 (répondu), non-déclenchement sur disparition
seule, NET-03 (réapparition/purge), NET-02 (acquittement à 2,5 s, reset sur interruption, aucun déclenchement
sans focus), et le round-trip/TTL de `TreatedStore`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Filtre treated dans SessionMonitor.Read + câblage DI + tests d'intégration</name>
  <read_first>src/Chronos/Services/SessionMonitor.cs, src/Chronos/App.xaml.cs, tests/Chronos.Tests/SessionsTests.cs</read_first>
  <files>src/Chronos/Services/SessionMonitor.cs, src/Chronos/App.xaml.cs, tests/Chronos.Tests/TreatedSessionsTests.cs</files>
  <action>
1) `src/Chronos/Services/SessionMonitor.cs` — étendre le ctor EN FIN (non cassant), ajouter l'observation +
le filtre `treated` au MÊME endroit que `archived` :
- Ajouter les paramètres optionnels : `TreatedStore? treated = null, SessionTreatmentTracker? tracker = null, IForegroundWatch? foreground = null`.
- Champs : `_treated`, `_tracker`, `_foreground` (affectés depuis les paramètres, sans instancier de défaut —
  null = fonctionnalité désactivée, non-régression).
- Dans `Read`, APRÈS l'étape (2.b) source bureau et AVANT le filtre `archived` :
  ```csharp
  // 2.c) Hystérésis (Phase 14) : le tracker observe les snapshots BRUTS fusionnés (+ focus + now) et met à
  //      jour TreatedStore (ajout NET-01/NET-02, purge NET-03). Best-effort : ne casse JAMAIS le pipeline.
  var raw = byId.Values.ToList();
  bool foreground = false;
  try { foreground = _foreground?.IsClaudeForeground() ?? false; } catch { foreground = false; }
  try { _tracker?.Observe(raw, foreground, now); } catch { }
  ```
- Remplacer le filtre final `archived` par le double filtre (archived PERMANENT puis treated présent = masqué) :
  ```csharp
  // 3) Filtres : archivées (permanent, NET-04) PUIS traitées (réversible). Le tracker possède l'ajout ET la
  //    purge des entrées treated ; ici on MASQUE simplement toute session encore présente dans le magasin.
  var archived = _archive.Load();
  var treatedMap = _treated?.Load();
  var result = new List<SessionSnapshot>(byId.Count);
  foreach (var s in byId.Values)
  {
      if (archived.Contains(s.SessionId)) continue;                       // NET-04 : permanent, jamais réversible
      if (treatedMap is not null && treatedMap.ContainsKey(s.SessionId)) continue; // traité => caché
      result.Add(s);
  }
  return result;
  ```
  NE PAS ré-implémenter la comparaison `treatedWaitingTs >= UpdatedAt` dans le filtre : elle serait FAUSSE pour
  les sessions bureau (`UpdatedAt == now` à chaque poll ferait réapparaître à chaque tick). La réversibilité
  NET-03 est portée par le tracker (purge sur nouvel épisode). Documenter ce choix en commentaire français.

2) `src/Chronos/App.xaml.cs` — dans `ConfigureServices`, bloc « Source BUREAU (UIA) », enregistrer le magasin +
le tracker et les injecter dans `SessionMonitor` (le paramètre `foreground` reste NON fourni ici → NET-02
dormant jusqu'au plan 02, aucune régression) :
```csharp
services.AddSingleton(_ => new TreatedStore());
services.AddSingleton(sp => new SessionTreatmentTracker(sp.GetRequiredService<TreatedStore>()));
```
Et modifier l'enregistrement existant de `SessionMonitor` pour passer les deux nouveaux services :
```csharp
services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
    sp.GetRequiredService<DesktopUiaSessionSource>(),
    sp.GetRequiredService<TreatedStore>(),
    sp.GetRequiredService<SessionTreatmentTracker>()));
```
(Déclarer `TreatedStore` et `SessionTreatmentTracker` AVANT le `SessionMonitor` qui les consomme.)

3) Ajouter à `tests/Chronos.Tests/TreatedSessionsTests.cs` les tests d'INTÉGRATION via `SessionMonitor` (un
faux `IForegroundWatch` et un faux `ISessionSource` en local — copier le patron `FakeSessionSource` de
`SessionsTests.cs`, liste fixe de snapshots) :
- Faux focus : `private sealed class FakeForegroundWatch : IForegroundWatch { public bool Value; public bool IsClaudeForeground() => Value; }`
- Construire `SessionMonitor` avec des chemins temp isolés :
  `new SessionMonitor(TempDir(), new TranscriptSessionSource(TempDir()), new ArchiveStore(TempFile()), fakeSource, treatedStore, tracker, fakeFocus)`.
- Test `NET01_le_monitor_masque_apres_reponse` : cycle1 la session CLI en attente est VISIBLE ; cycle2 (même
  id repassé Working via un faux source) → ABSENTE de `Read`.
- Test `NET03_le_monitor_la_reaffiche_sur_nouvel_episode` : après masquage, un nouvel épisode d'attente
  (UpdatedAt/now plus récents) → la session REDEVIENT visible dans `Read`, et `TreatedStore.Load()` ne la
  contient plus.
- Test `NET04_archivee_reste_masquee_meme_en_attente` : une session archivée (`archive.Add(id)`) qui repart en
  attente avec un épisode plus récent RESTE masquée (contraste direct avec NET-03 : archivé ≠ traité,
  permanent vs réversible).
- Test `NET02_le_monitor_masque_apres_focus_2_5s` : faux focus=true + faux source rendant une session
  `desktop:foreground:chat` en WaitingTurn ; `Read(t0)`, `Read(t0+1.5s)` la montrent, `Read(t0+3s)` la masque.
- Test `Monitor_sans_tracker_ni_treated_ne_regresse_pas` : `SessionMonitor` construit SANS les nouveaux
  paramètres (comme aujourd'hui) → comportement identique, aucune exception.

Créer/mutualiser les helpers `TempDir()` (répertoire temp) et `FakeSessionSource` mutable (permettant de
changer la liste rendue entre deux `Read` pour simuler l'évolution des cycles).
  </action>
  <verify>
    <automated>grep -Eq "_tracker\?\.Observe" src/Chronos/Services/SessionMonitor.cs && grep -Eq "treatedMap" src/Chronos/Services/SessionMonitor.cs && grep -Eq "SessionTreatmentTracker" src/Chronos/App.xaml.cs && grep -Eq "new TreatedStore" src/Chronos/App.xaml.cs && dotnet test tests/Chronos.Tests/Chronos.Tests.csproj</automated>
  </verify>
  <done>
`SessionMonitor.Read` invoque `tracker.Observe` sur les snapshots bruts + focus + now, puis masque les sessions
présentes dans `TreatedStore` (après le filtre archived permanent). DI câble `TreatedStore` + `SessionTreatmentTracker`
dans `SessionMonitor` (foreground non fourni). Toute la suite `dotnet test` est verte (304 existants + nouveaux),
NET-01/03/04 prouvés bout-en-bout au niveau `SessionMonitor` et NET-02 prouvé via faux focus.
  </done>
</task>

</tasks>

<verification>
- `dotnet build src/Chronos/Chronos.csproj -c Debug` : 0 avertissement / 0 erreur.
- `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj` : tous verts (304 existants conservés + neufs).
- `ServicesLayerPurityTests` reste vert : `TreatedStore`, `SessionTreatmentTracker`, `IForegroundWatch` sont
  neutres (aucun assembly WPF, aucune signature publique non neutre).
- Réversibilité NET-03 explicitement testée (réapparition + purge de l'entrée) ET distinction traité≠archivé
  NET-04 explicitement testée (archivé reste masqué même en attente plus récente).
</verification>

<success_criteria>
- NET-01 : une session répondue (attente→Working) disparaît de `SessionMonitor.Read`.
- NET-03 : une session traitée qui repart en attente (nouvel épisode) réapparaît et son entrée est purgée.
- NET-04 : une session archivée reste masquée pour toujours, distincte du magasin « traité ».
- La LOGIQUE NET-02 (focus ≥ 2,5 s, reset sur interruption, aucun déclenchement sans focus) est prouvée avec un
  faux focus + horloge, prête à recevoir le focus RÉEL au plan 02.
- Aucune régression : 304 tests existants toujours verts ; `SessionMonitor` sans les nouveaux paramètres se
  comporte comme avant.
</success_criteria>

<output>
After completion, create `.planning/phases/14-auto-disparition-hysteresis/14-01-SUMMARY.md`
</output>
