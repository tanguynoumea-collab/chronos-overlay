---
phase: 23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/Chronos/Services/BalayageMagasinSessions.cs
  - tests/Chronos.Tests/BalayageMagasinSessionsTests.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
  - .planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-VALIDATION.md
autonomous: true
requirements: [CYC-01]

must_haves:
  truths:
    - "Au lancement, les fichiers d'état périmés et les fichiers temporaires orphelins disparaissent du dossier, sans geste de l'utilisateur"
    - "Une session vivante depuis plusieurs jours survit au nettoyage, même si son fichier d'état est ancien"
    - "Une session dont l'état a été balayé disparaît du widget sans être annoncée « terminée » ni « traitée »"
    - "Un fichier d'état que le widget affiche encore n'est jamais balayé"
    - "S'il n'y a rien à retirer, aucun fichier n'est touché — date de dernière écriture comprise"
    - "Le nombre rendu par le balayage est ce qui a été RETIRÉ, pas ce qui a été repéré"
  artifacts:
    - path: "src/Chronos/Services/BalayageMagasinSessions.cs"
      provides: "Deux gestes distincts — retirer les débris temporaires, balayer les états périmés — sur horloge INJECTÉE"
      exports: ["BalayageMagasinSessions", "BilanBalayage"]
      contains: "IClock horloge"
      min_lines: 90
    - path: "tests/Chronos.Tests/BalayageMagasinSessionsTests.cs"
      provides: "Le critère double (âge + attestation de vie) et la preuve que balayer ne conclut rien"
      min_lines: 130
    - path: ".planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-VALIDATION.md"
      provides: "Carte de vérification de la phase, remplie et mesurée"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "BalayageMagasinSessions.Balayer"
      via: "OnStartup, mode overlay uniquement, best-effort"
      pattern: "GetRequiredService<BalayageMagasinSessions>\\(\\)\\.Balayer\\(\\)"
    - from: "src/Chronos/App.xaml.cs (enregistrement DI)"
      to: "SessionMonitor.Directory"
      via: "le balayage lit le dossier DU MONITEUR, jamais un second chemin déduit"
      pattern: "GetRequiredService<SessionMonitor>\\(\\)\\.Directory"
    - from: "src/Chronos/Services/BalayageMagasinSessions.cs"
      to: "ISessionSource"
      via: "attestation de vie — le critère d'âge n'est pas le seul"
      pattern: "ISessionSource"
---

<objective>
CYC-01 — `%APPDATA%\Chronos\sessions` cesse de ne faire que grandir.

État mesuré le 2026-09-12 : **66 entrées** — 54 `.json` + **12 fichiers temporaires orphelins**, dont cinq
portant le MÊME `session_id` avec cinq identifiants de processus différents (débris du fan-out des 25 hooks).
**51 des 54 ont plus de 24 h, 48 ont plus de 7 jours, l'âge médian est de ~37 jours**, et 32 se déclarent
encore « Working ». La cause est structurelle : la SEULE suppression d'un fichier d'état dans tout `src/` est
conditionnée à `SessionEnd`, dont les valeurs de `reason` (`clear`, `logout`, `prompt_input_exit`, `other`)
**ne couvrent ni terminal tué, ni crash, ni redémarrage machine** — et dans ces cas-là le hook ne s'exécute
tout simplement pas. Aucun balayage d'expiration n'existe nulle part.

**La doctrine mord ici, et c'est le critère n°4 du ROADMAP : expirer, c'est ne plus savoir.** Une session
balayée doit **disparaître**, jamais être annoncée « terminée » ni « traitée ». Balayer ne doit pas devenir
une façon détournée de conclure. Corollaire opératoire : **le critère d'âge ne peut pas être le seul**, sinon
une session vivante depuis plusieurs jours serait effacée.

Purpose: sans ce plan, la phase 24 arbitrerait par fraîcheur sur un magasin de 37 jours d'âge médian dont
32 entrées se déclarent faussement actives.

Output: `Services/BalayageMagasinSessions.cs` (horloge injectée dès la conception), son fichier de tests, le
câblage au démarrage, deux gardes, et la carte de vérification de la phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-CONTEXT.md
@.planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-01-SUMMARY.md
@.planning/debug/widget-sessions-statuts.md
@CLAUDE.md

<interfaces>
<!-- Contrats déjà présents dans le dépôt. L'exécutant les utilise TELS QUELS. -->

```csharp
// src/Chronos/Services/IClock.cs
public interface IClock { DateTimeOffset UtcNow { get; } }

// src/Chronos/Services/ISessionSource.cs
public interface ISessionSource
{
    IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);
}

// src/Chronos/Services/SessionSnapshot.cs
public enum SessionActivity { Working, WaitingAttention, WaitingTurn, Unknown }
public sealed record SessionSnapshot(string SessionId, string Project, SessionActivity Activity,
                                     string? Reason, System.DateTimeOffset UpdatedAt);

// src/Chronos/Services/LectureSessions.cs
public enum MotifMasquage { Archivee, Traitee }
public sealed record SessionMasquee(SessionSnapshot Session, MotifMasquage Motif);
public sealed record LectureSessions(IReadOnlyList<SessionSnapshot> Visibles,
                                     IReadOnlyList<SessionMasquee> Masquees,
                                     int FichiersEcartesParAnciennete);

// src/Chronos/Services/SessionMonitor.cs
public sealed class SessionMonitor
{
    public SessionMonitor(string? sessionsDir = null, ISessionSource? transcripts = null,
                          ArchiveStore? archive = null, TreatedStore? treated = null,
                          SessionTreatmentTracker? tracker = null);
    public string Directory { get; }
    public IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);   // => Inspecter(now).Visibles
    public LectureSessions Inspecter(System.DateTimeOffset now);
}
// Seuils INTERNES du moniteur, à connaître sans les modifier :
//   StaleWorking = 20 min  (Working périmé -> Unknown)
//   DropAfter    =  8 h    (fichier écarté de l'affichage ; compté dans FichiersEcartesParAnciennete)

// src/Chronos/Services/UsageNormalization.cs — LE point unique de conversion d'époque
public static DateTimeOffset? InstantDepuisEpochMillisecondes(long? millisecondes);

// src/Chronos/Services/SessionHookProcessor.cs — le format RÉEL d'un fichier d'état
public static string BuildStateJson(string sessionId, string project, SessionActivity activity,
                                    string? reason, long updatedAtMs);
// -> {"session_id":…,"project":…,"activity":…,("reason":…,)"updated_at":<epoch ms>}
```

Schéma des noms de fichiers dans le magasin :
- état : `<session_id>.json`
- débris : `<session_id>.json.tmp-<pid>` (produits par les exes antérieurs, toujours installés comme hooks)
</interfaces>

<pieges_a_ne_pas_reproduire>
**1. Le piège de test à retardement (trois plans de la phase 22 s'y sont heurtés).** `TreatedStore` et
`ArchiveStore` purgent à 6 h sur l'horloge **SYSTÈME**, sans horloge injectable. Écrire un horodatage
littéral figé rend des tests rouges à une heure donnée du soir sans qu'aucun code ne bouge.
**Remède imposé ici : l'horloge est INJECTÉE dès la conception** (`IClock`), et **tout** horodatage écrit par
un test — champ `updated_at` ET date de dernière écriture du fichier — est **ancré sur cette horloge**, jamais
sur celle du système.

**2. `NormalisationUniqueTests` balaie le TEXTE de `Services/`, commentaires compris**, et
`BalayageMagasinSessions.cs` **n'est pas exempté**. Interdits : `/ 100`, `* 100`, `FromUnixTimeSeconds`,
`FromUnixTimeMilliseconds`, `DateTimeOffset.TryParse`. On convertit donc l'époque par
`UsageNormalization.InstantDepuisEpochMillisecondes`, comme `DiagnosticService` l. 483.

**3. CS0136 (leçon 22-03).** Ne pas masquer un identifiant déjà en portée. Nommer explicitement.

**4. Ce code SUPPRIME des fichiers.** Tout test doit passer par un dossier temporaire vérifié par
`Assert.StartsWith(Path.GetTempPath(), …)` **avant** toute construction. Aucun test ne doit pouvoir viser
`%APPDATA%\Chronos\sessions` (66 entrées réelles, données de l'utilisateur ET matière d'enquête).
</pieges_a_ne_pas_reproduire>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : deux gestes distincts — retirer les débris, balayer ce dont plus rien n'atteste la vie</name>

  <read_first>
    - src/Chronos/Services/ArchiveStore.cs l. 74-123 (`PurgerPrefixe` : LE MODÈLE — purger n'est pas expirer ;
      rien à retirer ⇒ le fichier n'est pas réécrit ; le nombre rendu est une observation)
    - src/Chronos/Services/SessionMonitor.cs l. 72-155 (`Inspecter`, `TryRead`, `DropAfter = 8 h`, la règle
      de repli du `session_id` sur le nom de fichier)
    - src/Chronos/Services/UsageNormalization.cs l. 90-110 (`InstantDepuisEpochMillisecondes`)
    - src/Chronos/Services/DiagnosticService.cs l. 455-511 (le même besoin, résolu sans conversion locale)
    - tests/Chronos.Tests/InspectionSessionsTests.cs l. 19-46 (idiomes `TempDir()`, `SourceFixe`,
      `Maintenant`, et le commentaire sur le TTL mesuré à l'horloge réelle)
    - tests/Chronos.Tests/Fakes/FakeClock.cs
    - tests/Chronos.Tests/NormalisationUniqueTests.cs l. 27-44
  </read_first>

  <files>
    src/Chronos/Services/BalayageMagasinSessions.cs (créé),
    tests/Chronos.Tests/BalayageMagasinSessionsTests.cs (créé)
  </files>

  <behavior>
    - Un fichier d'état dont la date dépasse `ExpirationEtat` (72 h) **et** dont aucune source n'atteste la
      vie est **retiré**.
    - Un fichier d'état de 40 jours dont le `session_id` est **attesté vivant** par `ISessionSource` est
      **conservé** — c'est le critère n°4 : le critère d'âge n'est pas le seul.
    - Un fichier d'état de 10 h (au-delà du `DropAfter` de 8 h du moniteur, en deçà de 72 h) est **conservé** :
      le balayage ne court pas derrière le filtre d'affichage.
    - Un fichier `<sid>.json.tmp-<pid>` de plus d'`AgeMinimalTemporaire` (1 h) est **retiré** ; un débris
      vieux de deux minutes est **épargné** (un hook est peut-être en train d'écrire). Les **cinq** débris
      mesurés portant le même `session_id` partent tous les cinq.
    - Un fichier d'état **illisible** est daté par sa date d'écriture — un fait observé sur le fichier — et
      **jamais** par une date inventée : illisible et récent ⇒ conservé ; illisible et vieux de 40 jours ⇒
      retiré.
    - **Rien à retirer ⇒ rien n'est touché** : la date de dernière écriture et le contenu des survivants sont
      identiques après le balayage.
    - Le bilan rendu compte ce qui a été **effectivement supprimé** : une suppression qui échoue n'est pas
      comptée comme retirée.
    - Après balayage d'un état, `SessionMonitor.Inspecter` ne rend la session **ni dans `Visibles`, ni dans
      `Masquees`** : aucun verdict n'a été posé, elle a simplement disparu.
    - Dossier absent ⇒ bilan nul, aucune exception.
  </behavior>

  <action>
**ÉTAPE ROUGE** — écrire `tests/Chronos.Tests/BalayageMagasinSessionsTests.cs` contre un SQUELETTE
compilable (signatures réelles, corps `throw new System.NotImplementedException();`). La compilabilité à
chaque commit est non négociable. Commit `test(23-02): …` avec le rouge et son compte.

**ÉTAPE VERTE** — créer `src/Chronos/Services/BalayageMagasinSessions.cs` :

```csharp
using System.Collections.Generic;
using System.IO;
using System.Text.Json;

namespace Chronos.Services;

/// <summary>
/// Ce qu'un balayage a RÉELLEMENT retiré, et ce qu'il a laissé. Trois OBSERVATIONS, jamais des intentions :
/// une suppression qui échoue ne figure pas dans les retirés (précédent <see cref="ArchiveStore.PurgerPrefixe"/>,
/// où annoncer ce qu'on a repéré plutôt que ce qu'on a fait aurait menti sur le seul canal disponible).
/// </summary>
public sealed record BilanBalayage(int EtatsRetires, int TemporairesRetires, int EtatsConserves);

/// <summary>
/// CYC-01 — le magasin d'états de session cesse de ne faire que grandir.
///
/// <para>Pourquoi il fallait un balayage : la seule suppression d'un fichier d'état repose sur l'événement
/// de fin de session, dont aucune valeur de motif ne couvre un terminal tué, un plantage ou un redémarrage
/// machine — dans ces cas, le hook ne s'exécute pas du tout. Mesuré le 2026-09-12 : 54 fichiers, dont 48 de
/// plus de sept jours, âge médian 37 jours, 32 se déclarant encore au travail ; plus 12 fichiers temporaires
/// abandonnés, dont cinq pour une même session.</para>
///
/// <para>DEUX GESTES DISTINCTS, comme purger et expirer le sont dans
/// <see cref="ArchiveStore.PurgerPrefixe"/> :
/// retirer des DÉBRIS (des fichiers temporaires qui ne sont l'état de personne — ils n'ont jamais atteint
/// leur destination) n'est pas la même chose que balayer un ÉTAT (qui, lui, a été vrai).</para>
///
/// <para>LA DOCTRINE : expirer, c'est ne plus savoir. Une session balayée disparaît ; elle n'est
/// PAS déclarée terminée, ni traitée. Ce type ne connaît donc aucun magasin de verdict, et une garde par
/// réflexion le vérifie. Conséquence directe : le critère d'ÂGE ne peut pas être le seul, sinon une session
/// vivante depuis plusieurs jours serait effacée. Un état n'est balayé que si, en plus d'être vieux, plus
/// aucune source n'atteste que sa session vit.</para>
///
/// <para>Le seuil de <see cref="ExpirationEtat"/> vaut neuf fois celui au-delà duquel le moniteur cesse
/// d'afficher un fichier : on ne balaie jamais quelque chose que le widget pourrait encore montrer, et le
/// passé récent reste lisible dans le rapport de diagnostic.</para>
///
/// <para>L'horloge est INJECTÉE, et ce n'est pas une coquetterie : deux magasins de ce dépôt comparent un
/// horodatage à l'horloge du système sans pouvoir être pilotés, et trois plans de la phase 22 ont buté sur
/// des tests qui viraient au rouge à une heure donnée du soir sans qu'aucun code ne bouge.</para>
///
/// Aucun type WPF (couche neutre). Ne lève jamais : un balayage best-effort ne doit pas empêcher un démarrage.
/// </summary>
public sealed class BalayageMagasinSessions
{
    /// <summary>Au-delà, un état sans attestation de vie est balayé. Neuf fois le seuil d'affichage du moniteur.</summary>
    public static readonly System.TimeSpan ExpirationEtat = System.TimeSpan.FromHours(72);

    /// <summary>En deçà, un fichier temporaire est épargné : un hook est peut-être en train d'écrire
    /// (latence mesurée d'un hook : 584-611 ms, soit six mille fois moins).</summary>
    public static readonly System.TimeSpan AgeMinimalTemporaire = System.TimeSpan.FromHours(1);

    private const string MarqueurTemporaire = ".json.tmp-";

    private readonly string _dossier;
    private readonly ISessionSource _attestationDeVie;
    private readonly IClock _horloge;

    public BalayageMagasinSessions(string dossier, ISessionSource attestationDeVie, IClock horloge)
    {
        _dossier = dossier;
        _attestationDeVie = attestationDeVie;
        _horloge = horloge;
    }

    /// <summary>Le dossier balayé. Exposé pour qu'un test puisse VÉRIFIER qu'il est temporaire avant
    /// d'appeler <see cref="Balayer"/> : ce code supprime des fichiers.</summary>
    public string Dossier => _dossier;

    public BilanBalayage Balayer()
    {
        var maintenant = _horloge.UtcNow;
        if (!Directory.Exists(_dossier)) return new BilanBalayage(0, 0, 0);

        string[] entrees;
        try { entrees = Directory.GetFiles(_dossier); }
        catch { return new BilanBalayage(0, 0, 0); }

        // 1) DÉBRIS. Un fichier temporaire n'est l'état de personne : il n'a jamais atteint sa destination.
        //    Aucun critère d'attestation ne s'y applique — il n'y a rien à attester.
        var temporairesRetires = 0;
        foreach (var entree in entrees)
        {
            if (!Path.GetFileName(entree).Contains(MarqueurTemporaire, System.StringComparison.Ordinal)) continue;
            if (maintenant - DateEcriture(entree) < AgeMinimalTemporaire) continue;
            try { File.Delete(entree); temporairesRetires++; } catch { }
        }

        // 2) ATTESTATION DE VIE. Lue UNE fois, à l'instant du balayage. Best-effort : une source en panne
        //    n'atteste rien : le balayage retombe alors sur le seul critère d'âge. C'est le cas le moins
        //    favorable, et il est assumé — 72 h sans le moindre événement de hook ET sans transcript lisible.
        var vivantes = new HashSet<string>(System.StringComparer.Ordinal);
        try { foreach (var s in _attestationDeVie.Read(maintenant)) vivantes.Add(s.SessionId); }
        catch { }

        // 3) ÉTATS. Vieux ET sans attestation de vie. Les deux, jamais l'un seul.
        var etatsRetires = 0;
        var etatsConserves = 0;
        foreach (var entree in entrees)
        {
            if (!entree.EndsWith(".json", System.StringComparison.OrdinalIgnoreCase)) continue;

            var (identifiant, date) = Dater(entree);
            if (vivantes.Contains(identifiant)) { etatsConserves++; continue; }
            if (maintenant - date <= ExpirationEtat) { etatsConserves++; continue; }

            try { File.Delete(entree); etatsRetires++; }
            catch { etatsConserves++; }   // pas supprimé = pas retiré : le bilan reste une observation
        }

        return new BilanBalayage(etatsRetires, temporairesRetires, etatsConserves);
    }

    // Identifiant et date d'un fichier d'état. Même règle de repli que le moniteur pour l'identifiant (le
    // nom du fichier), et pour la date : la valeur DÉCLARÉE d'abord ; à défaut, la date d'écriture du
    // fichier — un fait observé sur le fichier, jamais une date inventée.
    private static (string Identifiant, System.DateTimeOffset Date) Dater(string fichier)
    {
        var identifiant = Path.GetFileNameWithoutExtension(fichier);
        System.DateTimeOffset? declaree = null;
        try
        {
            using var flux = new FileStream(fichier, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
            using var doc = JsonDocument.Parse(flux);
            var racine = doc.RootElement;
            if (racine.ValueKind == JsonValueKind.Object)
            {
                if (racine.TryGetProperty("session_id", out var sid) && sid.ValueKind == JsonValueKind.String
                    && !string.IsNullOrEmpty(sid.GetString()))
                    identifiant = sid.GetString()!;
                if (racine.TryGetProperty("updated_at", out var ua) && ua.TryGetInt64(out var ms))
                    declaree = UsageNormalization.InstantDepuisEpochMillisecondes(ms);
            }
        }
        catch { }
        return (identifiant, declaree ?? DateEcriture(fichier));
    }

    // Date d'écriture du fichier. Illisible -> instant maximal, donc âge négatif, donc JAMAIS balayé :
    // ne pas savoir quand un fichier a été écrit n'autorise pas à le supprimer.
    private static System.DateTimeOffset DateEcriture(string fichier)
    {
        try { return new System.DateTimeOffset(File.GetLastWriteTimeUtc(fichier), System.TimeSpan.Zero); }
        catch { return System.DateTimeOffset.MaxValue; }
    }
}
```

**Contraintes de rédaction, mesurées par les critères d'acceptation :**
- ce fichier ne doit contenir **ni** `DateTimeOffset.UtcNow`, **ni** `FromUnixTimeMilliseconds`, **ni**
  `TreatedStore`, **ni** `ArchiveStore` ailleurs que dans les deux `<see cref="ArchiveStore.PurgerPrefixe"/>`
  du XML-doc — c'est-à-dire **exactement 2** occurrences de `ArchiveStore` et **0** de `TreatedStore` ;
- les commentaires parlent de « l'horloge du système » et de « l'événement de fin de session » en toutes
  lettres, jamais par leur nom d'API.

**Fichier de tests** `tests/Chronos.Tests/BalayageMagasinSessionsTests.cs`. Aides obligatoires :

```csharp
    private static readonly DateTimeOffset Maintenant = new(2026, 9, 12, 13, 0, 0, TimeSpan.Zero);

    private static string TempDossier()
    {
        var d = Path.Combine(Path.GetTempPath(), "chronos-balayage-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(d);
        Assert.StartsWith(Path.GetTempPath(), d);   // ce code SUPPRIME des fichiers
        return d;
    }

    private static BalayageMagasinSessions Balayeur(string dossier, params SessionSnapshot[] vivantes)
    {
        var b = new BalayageMagasinSessions(dossier, new SourceFixe(vivantes), new FakeClock(Maintenant));
        Assert.StartsWith(Path.GetTempPath(), b.Dossier);   // garde anti-accident, AVANT tout balayage
        return b;
    }

    // TOUT horodatage est ancré sur l'horloge INJECTÉE (Maintenant), jamais sur celle du système : c'est
    // le remède au piège qui a rendu rouges trois plans de la phase 22 sans qu'aucun code ne bouge.
    private static string EcrireEtat(string dossier, string sid, SessionActivity activite, TimeSpan age)
    {
        var instant = Maintenant - age;
        var f = Path.Combine(dossier, sid + ".json");
        File.WriteAllText(f, SessionHookProcessor.BuildStateJson(
            sid, "Projet-" + sid, activite, null, instant.ToUnixTimeMilliseconds()));
        File.SetLastWriteTimeUtc(f, instant.UtcDateTime);
        return f;
    }

    private static string EcrireDebris(string dossier, string sid, int pid, TimeSpan age)
    {
        var f = Path.Combine(dossier, sid + ".json.tmp-" + pid);
        File.WriteAllText(f, "{}");
        File.SetLastWriteTimeUtc(f, (Maintenant - age).UtcDateTime);
        return f;
    }

    private sealed class SourceFixe : ISessionSource
    {
        private readonly IReadOnlyList<SessionSnapshot> _snaps;
        public SourceFixe(params SessionSnapshot[] snaps) => _snaps = snaps;
        public IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now) => _snaps;
    }
```

Les huit tests :

1. `Un_etat_perime_et_sans_attestation_est_retire_un_etat_encore_affichable_reste` — quatre fichiers :
   40 jours, 10 jours, **10 h** (au-delà du seuil d'affichage de 8 h, en deçà de 72 h) et 1 h. Attendu :
   `EtatsRetires == 2`, `EtatsConserves == 2`, et les deux fichiers récents existent toujours.
2. `Une_session_vivante_depuis_des_jours_survit_au_nettoyage` — un fichier de 40 jours dont le `session_id`
   est rendu par la source (`Snap("vivante", SessionActivity.Working, Maintenant)`). Attendu :
   `EtatsRetires == 0`, le fichier existe toujours. **C'est le critère n°4 : l'âge n'est pas le seul critère.**
3. `Les_debris_temporaires_partent_et_un_debris_tout_frais_est_epargne` — rejouer le cas MESURÉ : cinq
   `<sid>.json.tmp-<pid>` pour un même `sid` (cinq pid distincts), âgés de 3 jours, plus un débris de
   2 minutes. Attendu : `TemporairesRetires == 5`, le débris frais existe toujours.
4. `Un_etat_illisible_est_date_par_son_ecriture_jamais_par_une_date_inventee` — deux fichiers contenant
   `"ceci n'est pas du JSON {{{"`, l'un daté d'1 h, l'autre de 40 jours (par `File.SetLastWriteTimeUtc`
   ancré sur `Maintenant`). Attendu : le récent reste, le vieux part.
5. `Rien_a_retirer_ne_touche_rien` — un seul fichier d'1 h ; mémoriser `File.GetLastWriteTimeUtc` et le
   contenu, balayer, vérifier `new BilanBalayage(0, 0, 1)` et que **date et contenu sont inchangés**
   (précédent `Sans_fantome_le_fichier_de_l_utilisateur_n_est_pas_reecrit`).
6. `Un_etat_balaye_disparait_sans_etre_declare_traite_ni_archive` — **le test de doctrine**. Écrire un état
   de 40 jours ; construire
   `new SessionMonitor(dossier, new SourceFixe(), new ArchiveStore(cheminTempA), new TreatedStore(cheminTempT))`
   sur des chemins temporaires **inexistants** ; balayer ; puis `Inspecter(Maintenant)` doit rendre
   `Visibles` **vide**, `Masquees` **vide** et `FichiersEcartesParAnciennete == 0` — et
   `File.Exists(cheminTempA) == false`, `File.Exists(cheminTempT) == false` : le balayage n'a écrit **aucun
   verdict** nulle part. (Aucun horodatage n'est déposé dans ces deux magasins, donc leur TTL sur horloge
   système n'entre jamais en jeu — pas de test à retardement.)
7. `Le_balayage_ne_connait_aucun_magasin_de_verdict` — garde par RÉFLEXION, insensible au texte des
   commentaires :

```csharp
        var t = typeof(BalayageMagasinSessions);
        var champs = t.GetFields(BindingFlags.Instance | BindingFlags.NonPublic | BindingFlags.Public);
        Assert.NotEmpty(champs);                       // une garde qui ne verrait aucun champ serait muette
        Assert.DoesNotContain(champs, f => f.FieldType == typeof(TreatedStore)
                                        || f.FieldType == typeof(ArchiveStore));

        var parametres = t.GetConstructors().SelectMany(c => c.GetParameters()).ToList();
        Assert.NotEmpty(parametres);
        Assert.DoesNotContain(parametres, p => p.ParameterType == typeof(TreatedStore)
                                            || p.ParameterType == typeof(ArchiveStore));

        // L'horloge est injectable : c'est la condition pour que ce mécanisme n'ait pas de test à retardement.
        Assert.Contains(parametres, p => p.ParameterType == typeof(IClock));
```

8. `Dossier_absent_rend_un_bilan_nul_sans_exception` — pointer un sous-dossier temporaire non créé.

Commit `feat(23-02): …` une fois le vert obtenu.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~BalayageMagasinSessions"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` → **0 erreur, 0 avertissement**.
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, total **>** celui mesuré en fin de
      plan 23-01, **aucun test supprimé**.
    - `grep -cF "public BalayageMagasinSessions(string dossier, ISessionSource attestationDeVie, IClock horloge)" src/Chronos/Services/BalayageMagasinSessions.cs` → **1**.
    - `grep -cF "_horloge.UtcNow" src/Chronos/Services/BalayageMagasinSessions.cs` → **1**.
    - `grep -cF "DateTimeOffset.UtcNow" src/Chronos/Services/BalayageMagasinSessions.cs` → **0**
      (aucune lecture de l'horloge système : c'est LA condition anti-test-à-retardement).
    - `grep -cF "FromUnixTimeMilliseconds" src/Chronos/Services/BalayageMagasinSessions.cs` → **0**, et
      `grep -cF "UsageNormalization.InstantDepuisEpochMillisecondes" …` → **1**.
    - `grep -cF "TreatedStore" src/Chronos/Services/BalayageMagasinSessions.cs` → **0**.
    - `grep -cF "ArchiveStore" src/Chronos/Services/BalayageMagasinSessions.cs` → **2** — les deux
      `<see cref="ArchiveStore.PurgerPrefixe"/>` du XML-doc, qui citent le précédent ; **0 usage de code**,
      vérifié en relisant les deux lignes.
    - `grep -cF "SessionActivity" src/Chronos/Services/BalayageMagasinSessions.cs` → **0** (le balayage ne
      regarde pas l'état déclaré : balayer n'est pas conclure).
    - `grep -cF "Assert.StartsWith(Path.GetTempPath()" tests/Chronos.Tests/BalayageMagasinSessionsTests.cs` → **≥ 2**.
    - `grep -cF "DateTimeOffset.UtcNow" tests/Chronos.Tests/BalayageMagasinSessionsTests.cs` → **0**
      (tout horodatage de test est ancré sur `Maintenant`).
    - `grep -cF "new FakeClock(Maintenant)" tests/Chronos.Tests/BalayageMagasinSessionsTests.cs` → **1**.
    - `dotnet test … --filter "FullyQualifiedName~NormalisationUnique|FullyQualifiedName~ServicesLayerPurity|FullyQualifiedName~GardesDoctrine"` → **0 échec**.
    - `ls "$APPDATA/Chronos/sessions" | wc -l` → **66** ; aucun fichier du vrai `%APPDATA%\Chronos` supprimé.
  </acceptance_criteria>

  <done>
    `BalayageMagasinSessions` existe avec une horloge injectée, deux gestes distincts et un critère double ;
    huit tests le prouvent, dont la survie d'une session vivante depuis 40 jours et la disparition sans
    verdict d'un état balayé.
  </done>
</task>

<task type="auto">
  <name>Task 2 : le balayage s'exécute au démarrage, sur le dossier DU MONITEUR, et deux gardes le tiennent</name>

  <read_first>
    - src/Chronos/App.xaml.cs l. 86-118 (`OnStartup` : réconciliation, purge `PurgerPrefixe`, mode overlay
      uniquement) et l. 216-242 (enregistrements DI de la chaîne de sessions)
    - src/Chronos/Services/BalayageMagasinSessions.cs (créé en Task 1)
    - tests/Chronos.Tests/GardesPerimetreTests.cs l. 76-94 (la garde de câblage `PurgerPrefixe`, à imiter)
    - tests/Chronos.Tests/CompositionRootTests.cs l. 128-167 (`Le_graphe_DI_resout_la_chaine_de_sessions`)
  </read_first>

  <files>
    src/Chronos/App.xaml.cs,
    tests/Chronos.Tests/GardesPerimetreTests.cs,
    tests/Chronos.Tests/CompositionRootTests.cs
  </files>

  <action>
**1) Enregistrement DI**, dans `ConfigureServices`, immédiatement APRÈS l'enregistrement de `SessionMonitor`
(actuellement l. 226-228) :

```csharp
        // CYC-01 — balayage du magasin d'états au démarrage. Le dossier balayé est celui DU MONITEUR du
        // widget, jamais un second chemin déduit : deux chemins pour un seul widget rouvriraient l'écart
        // que la phase 22 vient de fermer, et un balayage qui se tromperait de dossier effacerait les
        // fichiers de quelqu'un d'autre. L'attestation de vie vient des transcripts — c'est ce qui permet
        // à une session vivante depuis plusieurs jours de survivre au nettoyage.
        services.AddSingleton(sp => new BalayageMagasinSessions(
            sp.GetRequiredService<SessionMonitor>().Directory,
            new TranscriptSessionSource(),
            sp.GetRequiredService<IClock>()));
```

**2) Appel au démarrage**, dans `OnStartup`, juste APRÈS le bloc `PurgerPrefixe("desktop:")` (l. 109-110) et
AVANT `OfferOnFirstRun()` :

```csharp
        // CYC-01 — le magasin d'états de session est balayé une fois par lancement. Même régime que la purge
        // ci-dessus : mode OVERLAY uniquement (les modes --hook et --statusline sortent bien plus haut),
        // best-effort et silencieux, il ne peut pas empêcher le démarrage. Mesuré le 2026-09-12 : 54 états,
        // dont 48 de plus de sept jours, plus 12 fichiers temporaires abandonnés.
        //
        // Expirer, c'est ne plus savoir : ce qui est balayé disparaît, rien n'est déclaré terminé ni traité.
        try { _host.Services.GetRequiredService<BalayageMagasinSessions>().Balayer(); }
        catch { }
```

**3) Garde de câblage** dans `tests/Chronos.Tests/GardesPerimetreTests.cs`, à la suite des gardes existantes :

```csharp
    /// <summary>
    /// GARDE DE CÂBLAGE (CYC-01, phase 23). Un balayage parfaitement testé mais jamais appelé ne balaie
    /// rien, et le défaut ne se verrait que chez l'utilisateur, sur ses propres données, en silence — c'est
    /// exactement ce qui s'est produit pendant deux mois avec le magasin de sessions. La seconde assertion
    /// est la plus importante : le balayage doit viser le dossier DU MONITEUR du widget, jamais un chemin
    /// déduit dans son coin.
    /// </summary>
    [Fact]
    public void Le_demarrage_balaie_le_magasin_de_sessions_sur_le_dossier_du_moniteur()
    {
        var fichier = Path.Combine(CheminSources(), "App.xaml.cs");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        Assert.Contains("OnStartup", texte, StringComparison.Ordinal);   // garde muette sinon
        Assert.Contains("GetRequiredService<BalayageMagasinSessions>().Balayer()", texte, StringComparison.Ordinal);
        Assert.Contains("GetRequiredService<SessionMonitor>().Directory", texte, StringComparison.Ordinal);
    }
```

**4) Garde DI** — étendre `Le_graphe_DI_resout_la_chaine_de_sessions` dans `CompositionRootTests.cs`.
Le miroir enregistre aujourd'hui `new SessionMonitor(null, …)`, dont le dossier serait le **vrai**
`%APPDATA%\Chronos\sessions`. Le balayeur en dérivant son dossier, la garde anti-accident du fichier (déjà
appliquée à `ArchiveStore` et `TreatedStore`) doit s'étendre au moniteur :

```csharp
        // Dossier TEMPORAIRE et non « null » comme en production : le balayeur enregistré ci-dessous dérive
        // son dossier de CE moniteur, et ce code supprime des fichiers. Aucune garde DI ne doit pouvoir
        // viser le vrai magasin de l'utilisateur.
        var dossierSessions = System.IO.Path.Combine(System.IO.Path.GetTempPath(),
            "ChronosSessions_" + System.Guid.NewGuid().ToString("N"));
        services.AddSingleton(sp => new SessionMonitor(dossierSessions, null, sp.GetRequiredService<ArchiveStore>(),
            sp.GetRequiredService<TreatedStore>(),
            sp.GetRequiredService<SessionTreatmentTracker>()));

        services.AddSingleton<IClock, SystemClock>();
        services.AddSingleton(sp => new BalayageMagasinSessions(
            sp.GetRequiredService<SessionMonitor>().Directory,
            new TranscriptSessionSource(),
            sp.GetRequiredService<IClock>()));
```

et, après `BuildServiceProvider()`, ajouter :

```csharp
        var balayeur = provider.GetRequiredService<BalayageMagasinSessions>();
        Assert.NotNull(balayeur);
        Assert.StartsWith(System.IO.Path.GetTempPath(), balayeur.Dossier);   // garde anti-accident
        Assert.Equal(provider.GetRequiredService<SessionMonitor>().Directory, balayeur.Dossier);
```

**Ne JAMAIS appeler `Balayer()` dans ce test** — la garde DI prouve la résolution du graphe, pas le
comportement. Mettre à jour le XML-doc du test pour mentionner le balayeur et la raison du dossier
temporaire — en écrivant « le balayeur » en toutes lettres, **jamais** le nom de sa méthode : un critère
d'acceptation mesure qu'aucune occurrence de `Balayer()` ne subsiste dans ce fichier de tests.

**Falsification obligatoire avant commit** : muter `.Balayer()` en `.BalayerMUTANT()` dans `App.xaml.cs`,
relancer `Le_demarrage_balaie_le_magasin_de_sessions_sur_le_dossier_du_moniteur`, **constater et nommer
l'échec dans le SUMMARY**, révoquer, revérifier par `grep`.

**Ne PAS toucher `DiagnosticService.cs`.** Le rapport compte et liste les fichiers du dossier du moniteur à
chaque appel : il reflétera le balayage **sans qu'une ligne ne change**. C'est la preuve du partage
d'instance de la phase 22, et l'absence de ce fichier du diff est un critère d'acceptation.

Commit `feat(23-02): …`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~GardesPerimetre|FullyQualifiedName~CompositionRoot|FullyQualifiedName~BalayageMagasinSessions"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` → **0 erreur, 0 avertissement**.
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, aucun test supprimé, deux exécutions
      consécutives au même total.
    - `grep -cF "GetRequiredService<BalayageMagasinSessions>().Balayer()" src/Chronos/App.xaml.cs` → **1**.
    - `grep -cF "GetRequiredService<SessionMonitor>().Directory" src/Chronos/App.xaml.cs` → **1**.
    - `grep -cF "GetRequiredService<SessionMonitor>()" src/Chronos/App.xaml.cs` → **3** (il y en avait 2 —
      contrôleur du widget et diagnostic ; le troisième est l'enregistrement du balayeur).
    - `grep -cF "new BalayageMagasinSessions(" src/Chronos/App.xaml.cs` → **1**.
    - `git diff --name-only` sur ce plan **ne contient pas** `src/Chronos/Services/DiagnosticService.cs` :
      le diagnostic reflète le balayage sans qu'une ligne ne change.
    - `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` → **0** (acquis phase 22).
    - `grep -cF "?? new " src/Chronos/Services/DiagnosticService.cs` → **2** (acquis phase 20).
    - `grep -cF "=> Inspecter(now).Visibles;" src/Chronos/Services/SessionMonitor.cs` → **1** (acquis phase 22).
    - `grep -cF "Balayer()" tests/Chronos.Tests/CompositionRootTests.cs` → **0** (la garde DI ne balaie rien).
    - `grep -cF "Path.GetTempPath()" tests/Chronos.Tests/CompositionRootTests.cs` **augmente de ≥ 2** par
      rapport à l'état d'entrée de tâche (dossier de sessions + assertion anti-accident) — relever les deux
      chiffres dans le SUMMARY plutôt que d'en fixer un à l'avance.
    - Toutes les gardes nommées de la phase vertes :
      `dotnet test … --filter "FullyQualifiedName~ServicesLayerPurity|FullyQualifiedName~CompositionRoot|FullyQualifiedName~NormalisationUnique|FullyQualifiedName~GardesDoctrine|FullyQualifiedName~GardesPerimetre|FullyQualifiedName~Inspection|FullyQualifiedName~Diagnostic"`
      → **0 échec**, et `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte` verte.
    - La mutation `.BalayerMUTANT()` a été **jouée**, l'échec est consigné nominativement, la mutation
      révoquée, le `grep` de contrôle rend **1**.
    - Invariants de sécurité : `ls "$APPDATA/Chronos/sessions" | wc -l` → **66** ;
      `stat -c '%s' "$APPDATA/Chronos/archived.json"` → **84** ;
      `stat -c '%s' "$APPDATA/Chronos/oauth.dat"` → **518** (**taille seule, jamais le mtime**) ;
      overlay pid 119412 vivant, **ni lancé ni tué** ; `git diff -- '*.csproj'` **vide**.
  </acceptance_criteria>

  <done>
    Le balayage s'exécute au démarrage en mode overlay, sur le dossier du moniteur du widget, best-effort ;
    une garde de câblage prouvée falsifiable et une garde DI anti-accident le tiennent ; `DiagnosticService.cs`
    est absent du diff.
  </done>
</task>

<task type="auto">
  <name>Task 3 : la carte de vérification de la phase, remplie et mesurée</name>

  <read_first>
    - .planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-VALIDATION.md (le squelette à remplir)
    - .planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md (le format de référence)
    - .planning/ROADMAP.md, section « Phase 23 » (les 4 critères de succès, mot pour mot)
    - .planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-01-SUMMARY.md
  </read_first>

  <files>
    .planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-VALIDATION.md
  </files>

  <action>
Remplir la carte `23-VALIDATION.md` : chaque `(à mesurer)` est remplacé par un **résultat réellement
obtenu**, jamais par une prédiction recopiée du plan.

1. **Test Infrastructure** — totaux mesurés après chaque plan, sur **deux exécutions consécutives**.
2. **Critères de succès du ROADMAP** — les quatre, chacun avec sa preuve NOMMÉE (nom de test ou commande)
   et son résultat. Le critère n°1 (« le magasin se résorbe ») et le n°2 (« un terminal tué ne laisse plus de
   trace éternelle ») ne sont **pas** entièrement vérifiables ici : la phase s'interdit de lancer l'overlay.
   Les inscrire en section « À VÉRIFIER PAR L'UTILISATEUR » avec la commande exacte et le chiffre attendu —
   et dire clairement que le test unitaire prouve le mécanisme, pas que le dossier de l'utilisateur a changé
   (précédent 21-04, `archived.json`).
3. **Falsification des gardes** — les deux mutations jouées (23-01 et 23-02), avec l'échec constaté.
4. **Gardes à ne pas casser** — le tableau, avec les chiffres relevés.
5. **Ce que cette phase NE fait PAS** — à écrire explicitement, avec la raison :
   - la **fusion par fraîcheur** reste par ordre d'insertion (phase 24) ;
   - le **contrat d'événements** reste à 5 hooks, `SessionEnd` compris (phase 25) ;
   - `TreatedStore` et le TTL d'`ArchiveStore` sont **intacts** (phase 26) — vérifiable :
     `git diff --name-only` de toute la phase ne contient ni `TreatedStore.cs` ni `ArchiveStore.cs` ;
   - **`ArchiveStore.Add` conserve son écriture par fichier temporaire.** Le noter comme un choix, avec sa
     raison : CYC-02 porte sur « l'écriture d'un **état de hook** », et `Add` n'a aujourd'hui **aucun canal**
     pour rendre un échec constatable (geste de menu, retour ignoré par l'appelant). Changer sa mécanique
     sans lui donner de canal produirait un correctif invérifiable — exactement le genre de geste que ce
     milestone combat. Relève de l'optimisation fine des écritures concurrentes, **hors périmètre v1.6**
     (poids mesuré : ~0,7 événement perdu sur 5 000).
6. **Contrepartie assumée de l'écriture directe** — un lecteur malchanceux peut lire un fragment pendant un
   cycle de 2 s, contre une perte définitive auparavant. À écrire, pas à taire.
7. **Limite connue du balayage** — il s'exécute **une fois par lancement**. Sur une machine qui reste
   allumée des semaines, le magasin croît entre deux lancements ; la borne est reprise au lancement suivant.
   Écrire la condition qui rendrait ce choix faux (un magasin qui, au prochain lancement, dépasserait
   nettement quelques jours d'états), plutôt que de présenter le choix comme sans conséquence.
8. **Invariants de sécurité** — mesurés et consignés : `sessions/` = 66 entrées **inchangées**,
   `archived.json` = 84 octets, `oauth.dat` = **518 octets, taille seule**, overlay pid 119412 ni lancé ni
   tué, aucune dépendance NuGet, aucun écrit hors `src/`, `tests/`, `.planning/`.
9. Passer le frontmatter à `status: validated` et `validated: <date>`.

Commit `docs(23-02): …`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "(à mesurer)" .planning/phases/23-*/23-VALIDATION.md` → **0**.
    - `grep -cF "À VÉRIFIER PAR L'UTILISATEUR" .planning/phases/23-*/23-VALIDATION.md` → **1**.
    - `grep -c "^status: validated" .planning/phases/23-*/23-VALIDATION.md` → **1**.
    - Les **quatre** critères de succès du ROADMAP apparaissent dans la carte, chacun avec une preuve nommée
      et un résultat ; le critère n°4 cite le test `Un_etat_balaye_disparait_sans_etre_declare_traite_ni_archive`
      **et** le test `Une_session_vivante_depuis_des_jours_survit_au_nettoyage`.
    - `git diff --name-only` de toute la phase **ne contient ni** `src/Chronos/Services/TreatedStore.cs`,
      **ni** `src/Chronos/Services/ArchiveStore.cs`, **ni** `src/Chronos/Services/DiagnosticService.cs`,
      **ni** `src/Chronos/Services/SessionMonitor.cs`.
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, deux exécutions consécutives au même total.
  </acceptance_criteria>

  <done>
    `23-VALIDATION.md` porte les 4 critères du ROADMAP avec leurs preuves et résultats mesurés, les deux
    mutations de falsification, les limites assumées, ce que la phase ne fait pas, et les invariants de
    sécurité relevés.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln -c Debug` → 0 erreur, 0 avertissement.
2. `dotnet test Chronos.sln -c Debug --nologo -v q` → 0 échec, deux exécutions consécutives, aucun test supprimé.
3. Le critère n°4 est tenu par deux tests nommés et une garde par réflexion : un état balayé disparaît sans
   verdict, une session vivante depuis 40 jours survit.
4. L'horloge est injectable dans le seul mécanisme neuf de cette phase : `DateTimeOffset.UtcNow` = 0
   occurrence dans `BalayageMagasinSessions.cs` **et** dans son fichier de tests.
5. Aucun fichier du vrai `%APPDATA%\Chronos\` n'a été supprimé ni modifié ; l'overlay n'a été ni lancé ni tué.
</verification>

<success_criteria>
- CYC-01 est couverte : les états périmés sans attestation de vie et les débris temporaires sont balayés au
  démarrage, sur le dossier du moniteur du widget.
- Balayer ne conclut rien : aucun magasin de verdict n'est connu du balayeur, et une session balayée
  disparaît sans motif de masquage.
- Le critère d'âge n'est pas le seul : une session attestée vivante survit quel que soit l'âge de son fichier.
- Le diagnostic reflète le balayage sans qu'une ligne de `DiagnosticService.cs` ne change.
- `23-VALIDATION.md` est rempli et mesuré ; les limites du choix sont écrites, pas tues.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-02-SUMMARY.md`.

Y consigner : les comptes de tests plan par plan, l'échec constaté à la mutation de falsification, les
chiffres de `grep` **réellement obtenus** (et non ceux prédits par le plan — leçon 22-03 : un écart se
documente, il ne s'absorbe pas), et la clôture de la phase 23.
</output>
