---
phase: 23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/EcritureEtatSession.cs
  - tests/Chronos.Tests/EcritureEtatSessionTests.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
autonomous: true
requirements: [CYC-02]

must_haves:
  truths:
    - "Un état de hook écrit PENDANT que le widget tient le fichier ouvert en lecture est bien écrit, et relu ensuite"
    - "Un hook qui ne peut PAS écrire l'état le dit sur la sortie d'erreur et rend un code de sortie non nul"
    - "Le mode --hook ne laisse plus aucun fichier temporaire derrière lui"
    - "Un échec d'écriture ne casse jamais la session Claude Code (jamais de code de sortie 2, jamais d'exception)"
  artifacts:
    - path: "src/Chronos/Services/EcritureEtatSession.cs"
      provides: "Écriture DIRECTE de l'état de session + résultat porteur de sa CAUSE d'échec"
      exports: ["EcritureEtatSession.Appliquer", "ResultatEcritureEtat"]
      min_lines: 60
    - path: "tests/Chronos.Tests/EcritureEtatSessionTests.cs"
      provides: "Le scénario mesuré du lecteur concurrent, rejoué 200 fois, plus la preuve que l'échec est RENDU"
      min_lines: 90
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "EcritureEtatSession.Appliquer"
      via: "RunSessionHook, mode --hook"
      pattern: "EcritureEtatSession\\.Appliquer\\("
    - from: "src/Chronos/App.xaml.cs"
      to: "sortie d'erreur du processus de hook"
      via: "flux d'erreur standard en UTF-8 + code de sortie rendu par RunSessionHook"
      pattern: "OpenStandardError"
---

<objective>
CYC-02 — une écriture d'état de hook ne peut plus disparaître sans que personne ne le sache.

Aujourd'hui `App.RunSessionHook` écrit un fichier temporaire puis le déplace par-dessus la cible. Mesuré le
2026-09-12 : contre un lecteur qui tient le fichier (et `SessionMonitor.TryRead` ouvre en
`FileShare.ReadWrite`), **290 écritures sur 500 sont perdues**, l'exception est avalée par un `catch {}`, et
le fichier temporaire reste — d'où les **12 orphelins** du magasin. Ajouter `FileShare.Delete` côté lecteur
**ne corrige pas** (200/500). L'**écriture directe** a été mesurée à **0 perte sur 500**.

Ce plan fait deux choses, dans cet ordre :
1. il sort l'écriture d'`App.xaml.cs` — fichier WPF que nul test ne peut instancier, ce qui est précisément
   la raison pour laquelle ce défaut a vécu si longtemps sans couverture ;
2. il rend l'échec **constatable** : la méthode rend sa CAUSE, le hook l'écrit sur la sortie d'erreur et sort
   avec un code non nul.

Purpose: la phase 24 arbitrera par fraîcheur, c'est-à-dire en comparant des horodatages. Une écriture perdue
en silence fait paraître un état **plus vieux qu'il n'est** : réparer l'écriture est une dépendance, pas un
ménage. Et le balayage du plan 23-02 datera les fichiers — il a besoin que ces dates soient celles des
événements réels.

Output: `Services/EcritureEtatSession.cs` (neutre, testable), son fichier de tests, un `RunSessionHook` qui
rend un code de sortie, et une garde de câblage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-CONTEXT.md
@.planning/debug/widget-sessions-statuts.md
@CLAUDE.md

<interfaces>
<!-- Contrats déjà présents dans le dépôt. L'exécutant les utilise TELS QUELS, sans exploration. -->

Depuis `src/Chronos/Services/SessionHookProcessor.cs` :
```csharp
public sealed record SessionHookResult(string? SessionId, bool Delete, string? StateJson, bool Ignore)
{
    public static SessionHookResult Ignored { get; }
}

public static class SessionHookProcessor
{
    public static SessionHookResult Process(string? eventName, string? stdinJson, long nowMs);
    public static string BuildStateJson(string sessionId, string project, SessionActivity activity,
                                        string? reason, long updatedAtMs);
    internal static string ProjectFromCwd(string? cwd);
}
```

Depuis `src/Chronos/Services/SessionMonitor.cs` (le LECTEUR concurrent, ligne 127 — c'est lui qui fait
échouer l'écriture actuelle) :
```csharp
using var fs = new FileStream(file, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
```

Depuis `src/Chronos/App.xaml.cs` — l'état AVANT ce plan :
```csharp
// l. 29-35
int hookIdx = System.Array.FindIndex(e.Args, a => string.Equals(a, "--hook", StringComparison.OrdinalIgnoreCase));
if (hookIdx >= 0)
{
    RunSessionHook(hookIdx + 1 < e.Args.Length ? e.Args[hookIdx + 1] : null);
    Environment.Exit(0);
    return;
}

// l. 122-151
private static void RunSessionHook(string? eventName) { /* … écriture par fichier temporaire … */ }
```

Motif d'encodage DÉJÀ établi dans ce fichier (`RunStatusLineBridge`, l. 156-179) : on n'écrit jamais par
`Console.Out`/`Console.Error`, dont l'encodage OEM par défaut mutile les caractères non-ASCII. On passe par
le flux brut et un `UTF8Encoding(false)`.
</interfaces>

<contrat_externe>
Codes de sortie d'un hook Claude Code (contrat documenté, relevé dans l'enquête du 2026-09-12) :
- **0** — succès. `stdout` n'est montré qu'en mode transcript.
- **2** — erreur BLOQUANTE : `stderr` est renvoyé à Claude, l'action est bloquée. **INTERDIT ICI.**
- **tout autre code** — erreur NON bloquante : `stderr` est montré à l'utilisateur, l'exécution continue.

C'est exactement le canal cherché par CYC-02 : « constatable » sans « casser la session ». On sort donc
**1**, jamais 2.
</contrat_externe>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : l'écriture d'état quitte la couche WPF, devient directe, et rend sa cause d'échec</name>

  <read_first>
    - src/Chronos/Services/SessionHookProcessor.cs (le record `SessionHookResult`, le format écrit)
    - src/Chronos/Services/SessionMonitor.cs l. 119-155 (`TryRead` : le lecteur concurrent, `FileShare.ReadWrite`)
    - src/Chronos/Services/ArchiveStore.cs l. 74-123 (`PurgerPrefixe` : le MODÈLE — un nombre rendu est une
      observation ; écriture directe motivée par la même mesure)
    - tests/Chronos.Tests/ArchiveStorePurgeTests.cs (idiome `TempFichier()` + `Assert.StartsWith(Path.GetTempPath(), …)`)
    - tests/Chronos.Tests/NormalisationUniqueTests.cs l. 27-44 (les motifs INTERDITS dans `Services/`)
  </read_first>

  <files>
    src/Chronos/Services/EcritureEtatSession.cs (créé),
    tests/Chronos.Tests/EcritureEtatSessionTests.cs (créé)
  </files>

  <behavior>
    - Un événement ignoré (`Ignore`) ou sans `SessionId` n'écrit rien et n'est PAS un échec.
    - `Delete` sur un fichier présent le supprime ; `Delete` sur un fichier absent réussit (rien à faire n'est
      pas un échec — précédent `PurgerPrefixe`).
    - Écriture sous LECTEUR CONCURRENT : un `FileStream(fichier, FileMode.Open, FileAccess.Read,
      FileShare.ReadWrite)` reste ouvert pendant 200 écritures successives → **200 succès, 0 échec**, et le
      contenu final sur disque est le DERNIER écrit. C'est le scénario mesuré à 290 pertes sur 500.
    - Écriture IMPOSSIBLE (cible tenue en `FileShare.None`) → `Reussi == false`, `Cause` non vide et
      non générique, **aucune exception ne remonte**.
    - Après une écriture réussie, le dossier contient EXACTEMENT un fichier, `<session_id>.json` : plus
      aucun débris temporaire (12 orphelins mesurés sur la machine).
  </behavior>

  <action>
**ÉTAPE ROUGE** — écrire d'abord `tests/Chronos.Tests/EcritureEtatSessionTests.cs` contre un SQUELETTE
compilable (`throw new System.NotImplementedException();`). La compilabilité à chaque commit est non
négociable : `tests/Chronos.Tests` porte un `ProjectReference` vers `Chronos`, donc une erreur de compilation
fait échouer **toute** l'invocation `dotnet test`, pas le test visé (précédents 18-01 et 22-03). Commit
`test(23-01): …` avec le rouge constaté et son compte.

**ÉTAPE VERTE** — créer `src/Chronos/Services/EcritureEtatSession.cs` :

```csharp
using System.IO;

namespace Chronos.Services;

/// <summary>
/// Ce qu'une tentative d'écriture d'état a RÉELLEMENT produit. Un booléen seul dirait qu'il s'est passé
/// quelque chose sans dire quoi : la <see cref="Cause"/> est la seule chose qui distingue « rien à faire »
/// d'« impossible » (même motif qu'au plan 17-02, où le rafraîchissement de jeton a cessé de rendre un null
/// muet). Elle n'est jamais fabriquée : c'est le type et le message de l'exception réellement levée.
/// </summary>
public sealed record ResultatEcritureEtat(bool Reussi, string? Cause)
{
    public static ResultatEcritureEtat Reussie { get; } = new(true, null);
    public static ResultatEcritureEtat Echouee(string cause) => new(false, cause);
}

/// <summary>
/// CYC-02 — applique au disque l'ordre rendu par <see cref="SessionHookProcessor"/>, et rend le résultat
/// au lieu de l'avaler.
///
/// <para>Deux défauts distincts sont corrigés ici. Le premier est mécanique : l'écriture passait par un
/// fichier temporaire déplacé par-dessus la cible, et ce déplacement échoue en
/// <c>UnauthorizedAccessException</c> dès qu'un lecteur tient le fichier — or
/// <see cref="SessionMonitor"/> le lit toutes les deux secondes en partage lecture-écriture. Mesuré le
/// 2026-09-12 : 290 écritures perdues sur 500 ; élargir le partage côté lecteur ne corrige pas (200/500) ;
/// l'écriture directe : 0 sur 500. Les fichiers temporaires abandonnés étaient indexés par identifiant de
/// processus, donc ils se recouvraient : les 12 orphelins relevés SOUS-ESTIMENT massivement le nombre
/// d'écritures perdues.</para>
///
/// <para>Le second est le silence : l'échec disparaissait dans un <c>catch</c> vide. Ici il est RENDU.</para>
///
/// <para>Contrepartie ASSUMÉE et mesurable : l'écriture directe tronque la cible avant de la réécrire, donc
/// un lecteur malchanceux peut lire un fragment. Le moniteur ignore un fichier illisible et le relira deux
/// secondes plus tard. Perdre un état pendant un cycle de lecture n'est pas du même ordre que le perdre
/// définitivement.</para>
///
/// <para>Ce code vit dans <c>Services/</c> et non dans <c>App.xaml.cs</c> pour une raison précise : le point
/// d'entrée WPF n'est pas instanciable sous test, et c'est là que ce défaut a vécu sans couverture.</para>
/// </summary>
public static class EcritureEtatSession
{
    public static ResultatEcritureEtat Appliquer(string dossier, SessionHookResult resultat)
    {
        // Rien à faire n'est pas un échec (précédent ArchiveStore.PurgerPrefixe).
        if (resultat.Ignore || string.IsNullOrEmpty(resultat.SessionId)) return ResultatEcritureEtat.Reussie;

        try
        {
            Directory.CreateDirectory(dossier);
            var fichier = Path.Combine(dossier, resultat.SessionId + ".json");

            if (resultat.Delete)
            {
                if (File.Exists(fichier)) File.Delete(fichier);   // déjà absent = déjà dans l'état voulu
                return ResultatEcritureEtat.Reussie;
            }
            if (resultat.StateJson is null) return ResultatEcritureEtat.Reussie;

            var octets = new System.Text.UTF8Encoding(false).GetBytes(resultat.StateJson);
            using var flux = new FileStream(fichier, FileMode.Create, FileAccess.Write, FileShare.Read);
            flux.Write(octets, 0, octets.Length);
            flux.Flush();
            return ResultatEcritureEtat.Reussie;
        }
        catch (System.Exception ex)
        {
            // Jamais de relance : un hook ne doit pas casser la session Claude Code. Mais plus jamais de
            // silence non plus — l'appelant reçoit de quoi le dire.
            return ResultatEcritureEtat.Echouee(ex.GetType().Name + " : " + ex.Message);
        }
    }
}
```

**Contraintes de rédaction, à respecter à la lettre** (elles sont mesurées par les critères d'acceptation) :
- le fichier ne doit contenir `FileMode.Create` **qu'une fois**, dans l'écriture — les commentaires parlent
  de « l'écriture directe », jamais de l'API ;
- le fichier ne doit contenir **ni** le nom de l'API de déplacement de fichier, **ni** le fragment de suffixe
  temporaire : les commentaires disent « fichier temporaire déplacé par-dessus la cible » en toutes lettres ;
- `Services/` est balayé par `NormalisationUniqueTests` (commentaires compris) : **aucun** `/ 100`,
  `* 100`, `FromUnixTimeSeconds`, `FromUnixTimeMilliseconds`, `DateTimeOffset.TryParse`. Ce fichier n'a
  aucune raison d'en contenir ; ne pas en introduire par inadvertance dans un commentaire.

**Fichier de tests** `tests/Chronos.Tests/EcritureEtatSessionTests.cs` — tous les tests écrivent dans un
dossier temporaire, avec la garde anti-accident du dépôt :

```csharp
private static string TempDossier()
{
    var d = Path.Combine(Path.GetTempPath(), "chronos-ecriture-" + Guid.NewGuid().ToString("N"));
    Directory.CreateDirectory(d);
    Assert.StartsWith(Path.GetTempPath(), d);   // ce code SUPPRIME et TRONQUE des fichiers
    return d;
}
```

Les six tests, nommés en français comme le reste du dépôt :

1. `Sous_un_lecteur_concurrent_aucune_ecriture_n_est_perdue` — ouvrir la cible en
   `new FileStream(fichier, FileMode.Open, FileAccess.Read, FileShare.ReadWrite)` (le mode EXACT de
   `SessionMonitor.TryRead`) et la garder ouverte pendant **200** appels à `Appliquer`, chacun avec un
   `updated_at` différent obtenu par `SessionHookProcessor.Process("Notification", stdin, ms)`. Compter les
   `Reussi == false` → `Assert.Equal(0, echecs)`. Après fermeture du lecteur, le contenu du fichier porte le
   **dernier** `updated_at` écrit.
2. `Un_echec_d_ecriture_est_rendu_avec_sa_cause_et_ne_leve_jamais` — tenir la cible en
   `FileShare.None`, appeler `Appliquer`, vérifier `Reussi == false`, `Cause` non vide, et qu'aucune
   exception n'a traversé (le simple fait que le test aille jusqu'à l'assertion le prouve).
3. `SessionEnd_supprime_le_fichier_et_un_fichier_absent_n_est_pas_un_echec` — deux cas dans un test.
4. `Un_evenement_ignore_n_ecrit_rien_et_n_est_pas_un_echec` — `SessionHookResult.Ignored` →
   `Reussi == true` et `Directory.GetFiles(dossier)` vide.
5. `Une_ecriture_ne_laisse_aucun_debris` — après un `Appliquer` réussi,
   `Assert.Equal(new[] { sid + ".json" }, Directory.GetFiles(dossier).Select(Path.GetFileName))`.
   C'est la garde des 12 orphelins mesurés.
6. `Le_contenu_ecrit_est_relisible_par_le_moniteur` — écrire via `Appliquer`, puis construire
   `new SessionMonitor(dossier, new SourceVide(), new ArchiveStore(Path.Combine(TempDossier(), "a.json")))`
   et vérifier que `Read(maintenant)` rend bien la session. `SourceVide` est une classe privée imbriquée
   implémentant `ISessionSource` et rendant une liste vide (idiome `SourceFixe` d'`InspectionSessionsTests`).
   L'instant `maintenant` est un littéral fixe et l'`updated_at` écrit en dérive : rien n'est comparé à
   l'horloge du système.

**Piège de compilation à éviter** (leçon 22-03, CS0136) : ne pas réutiliser un identifiant déjà en portée.
Nommer les itérateurs et variables locales de façon distincte (`fic`, `octets`, `flux`, `verrou`).

Commit `feat(23-01): …` une fois le vert obtenu et la suite complète relancée.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~EcritureEtatSession"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` → **0 erreur, 0 avertissement** (le dépôt construit à zéro
      avertissement ; introduire le premier entamerait cet invariant — leçon 21-04, xUnit2013).
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, total **> 747** (baseline d'entrée de
      phase), **aucun test supprimé**.
    - `grep -cF "FileMode.Create" src/Chronos/Services/EcritureEtatSession.cs` → **1**.
    - `grep -cF "FileShare.Read)" src/Chronos/Services/EcritureEtatSession.cs` → **1**.
    - `grep -cF "File.Move" src/Chronos/Services/EcritureEtatSession.cs` → **0**.
    - `grep -cF ".tmp-" src/Chronos/Services/EcritureEtatSession.cs` → **0**.
    - `grep -cF "catch { }" src/Chronos/Services/EcritureEtatSession.cs` → **0** (aucun `catch` vide : c'est
      le défaut que ce fichier corrige).
    - `grep -cF "FromUnixTimeMilliseconds" src/Chronos/Services/EcritureEtatSession.cs` → **0**.
    - `grep -cF "ResultatEcritureEtat.Echouee(" src/Chronos/Services/EcritureEtatSession.cs` → **1**.
    - `grep -cF "Assert.Equal(0, echecs)" tests/Chronos.Tests/EcritureEtatSessionTests.cs` → **1**.
    - `grep -cF "Path.GetTempPath()" tests/Chronos.Tests/EcritureEtatSessionTests.cs` → **≥ 2** (le chemin
      construit et la garde anti-accident).
    - `git status --porcelain` ne montre **aucun** fichier hors `src/`, `tests/`, `.planning/`.
    - `ls "$APPDATA/Chronos/sessions" | wc -l` → **66** (aucun test n'a touché aux données réelles).
  </acceptance_criteria>

  <done>
    `EcritureEtatSession.Appliquer` existe, écrit directement, rend un résultat porteur de sa cause, et six
    tests le prouvent — dont les 200 écritures sous lecteur concurrent, sans une seule perte.
  </done>
</task>

<task type="auto">
  <name>Task 2 : le mode --hook consomme le résultat, dit son échec et rend un code de sortie</name>

  <read_first>
    - src/Chronos/App.xaml.cs l. 27-35 (l'aiguillage `--hook`) et l. 120-179 (`RunSessionHook` et le motif
      d'encodage UTF-8 de `RunStatusLineBridge`)
    - src/Chronos/Services/EcritureEtatSession.cs (créé en Task 1)
    - tests/Chronos.Tests/GardesPerimetreTests.cs (le motif de garde de CÂBLAGE sur le texte source,
      l. 76-94 : `OnStartup` n'est pas instanciable sous test)
  </read_first>

  <files>
    src/Chronos/App.xaml.cs,
    tests/Chronos.Tests/GardesPerimetreTests.cs
  </files>

  <action>
**1) `RunSessionHook` rend désormais un code de sortie.** Remplacer la signature
`private static void RunSessionHook(string? eventName)` par
`private static int RunSessionHook(string? eventName)`, et le corps par :

```csharp
    // Exécuté en mode --hook : lit le JSON stdin de Claude Code et applique l'ordre au fichier d'état de la
    // session dans %APPDATA%\Chronos\sessions\<id>.json. Rend le CODE DE SORTIE du processus.
    //
    // CYC-02 — deux changements de fond par rapport à la version d'origine.
    // (a) L'écriture est sortie d'ici : elle vit dans Services/EcritureEtatSession, donc elle est testable.
    //     Ce fichier-ci ne l'est pas — il monte WPF — et c'est exactement pourquoi le défaut y a survécu.
    // (b) L'échec n'est plus avalé. Contrat des hooks Claude Code : 0 = succès, 2 = erreur BLOQUANTE
    //     (stderr renvoyé à Claude, action bloquée), tout autre code = erreur NON bloquante (stderr montré
    //     à l'utilisateur, la session continue). On sort donc 1, JAMAIS 2 : un hook ne doit pas casser la
    //     session Claude Code, mais il n'a aucune raison de mentir sur ce qu'il n'a pas pu faire.
    private static int RunSessionHook(string? eventName)
    {
        try
        {
            var utf8 = new System.Text.UTF8Encoding(false);
            string input;
            using (var sr = new System.IO.StreamReader(Console.OpenStandardInput(), utf8))
                input = sr.ReadToEnd();

            var res = SessionHookProcessor.Process(eventName, input, DateTimeOffset.UtcNow.ToUnixTimeMilliseconds());

            var dossier = System.IO.Path.Combine(
                Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData), "Chronos", "sessions");

            var ecriture = EcritureEtatSession.Appliquer(dossier, res);
            if (ecriture.Reussi) return 0;

            SignalerSurErreurStandard($"Chronos --hook {eventName} : état de session non écrit — {ecriture.Cause}");
            return 1;
        }
        catch (Exception ex)
        {
            SignalerSurErreurStandard($"Chronos --hook {eventName} : {ex.GetType().Name} — {ex.Message}");
            return 1;
        }
    }

    // Écrit une ligne sur le flux d'erreur du processus en UTF-8 STRICT. Pas via Console.Error : son
    // encodage OEM par défaut mutilerait les accents, exactement la leçon déjà tirée pour la barre de
    // statut (RunStatusLineBridge). Ne lève jamais : signaler un échec ne doit pas en produire un second.
    private static void SignalerSurErreurStandard(string message)
    {
        try
        {
            var utf8 = new System.Text.UTF8Encoding(encoderShouldEmitUTF8Identifier: false);
            var octets = utf8.GetBytes(message + Environment.NewLine);
            using var flux = Console.OpenStandardError();
            flux.Write(octets, 0, octets.Length);
            flux.Flush();
        }
        catch { }
    }
```

**2) L'aiguillage transmet le code.** Remplacer le bloc des lignes 30-35 par :

```csharp
        if (hookIdx >= 0)
        {
            Environment.Exit(RunSessionHook(hookIdx + 1 < e.Args.Length ? e.Args[hookIdx + 1] : null));
            return;
        }
```

**3) Garde de câblage** dans `tests/Chronos.Tests/GardesPerimetreTests.cs`, à la suite des gardes
existantes, avec le même XML-doc explicatif que ses voisines :

```csharp
    /// <summary>
    /// GARDE DE CÂBLAGE (CYC-02, phase 23). <c>EcritureEtatSession</c> peut être parfaitement testée et
    /// n'être jamais appelée : le hook continuerait alors de déplacer un fichier temporaire par-dessus la
    /// cible, de perdre une écriture sur deux sous lecteur concurrent, et de le taire. Contrôle de SOURCE :
    /// <c>OnStartup</c> monte un host WPF, il n'est pas instanciable sous test.
    /// </summary>
    [Fact]
    public void Le_mode_hook_ecrit_par_le_service_teste_et_signale_son_echec()
    {
        var fichier = Path.Combine(CheminSources(), "App.xaml.cs");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        // Une garde qui lirait un fichier vide serait muette.
        Assert.Contains("RunSessionHook", texte, StringComparison.Ordinal);

        Assert.Contains("EcritureEtatSession.Appliquer(", texte, StringComparison.Ordinal);
        Assert.Contains("OpenStandardError", texte, StringComparison.Ordinal);

        // Le défaut mesuré : un fichier temporaire déplacé par-dessus la cible, et un échec avalé.
        Assert.DoesNotContain("System.IO.File.Move", texte, StringComparison.Ordinal);
        Assert.DoesNotContain(".tmp-", texte, StringComparison.Ordinal);
    }
```

**Contrainte de rédaction** : ni `App.xaml.cs` ni son commentaire ne doivent contenir le fragment `.tmp-`
ni la chaîne `System.IO.File.Move` — c'est ce que mesurent les critères ci-dessous et la garde elle-même.

**Falsification obligatoire avant commit** (précédents 21-03 et 21-04) : muter
`EcritureEtatSession.Appliquer(` en `EcritureEtatSession.AppliquerMUTANT(` dans `App.xaml.cs`, relancer la
garde, **constater l'échec, le nommer dans le SUMMARY**, révoquer la mutation, revérifier par `grep`. Une
garde qui ne peut pas échouer ne garde rien.

Commit `feat(23-01): …`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~GardesPerimetre|FullyQualifiedName~EcritureEtatSession|FullyQualifiedName~Sessions"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` → **0 erreur, 0 avertissement**.
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, total **> 747**, aucun test supprimé.
    - `grep -cF "EcritureEtatSession.Appliquer(" src/Chronos/App.xaml.cs` → **1**.
    - `grep -cF "OpenStandardError" src/Chronos/App.xaml.cs` → **1**.
    - `grep -cF "System.IO.File.Move" src/Chronos/App.xaml.cs` → **0**.
    - `grep -cF ".tmp-" src/Chronos/App.xaml.cs` → **0**.
    - `grep -cF "private static int RunSessionHook" src/Chronos/App.xaml.cs` → **1**.
    - `grep -cF "Environment.Exit(RunSessionHook(" src/Chronos/App.xaml.cs` → **1**.
    - `grep -cF "Environment.Exit(0)" src/Chronos/App.xaml.cs` → **1** (il en restait 2 avant ce plan ;
      celui du mode `--statusline` subsiste, celui du mode `--hook` est remplacé).
    - `grep -c "return 2;" src/Chronos/App.xaml.cs` → **0** (jamais le code BLOQUANT).
    - Gardes à ne pas casser, vertes :
      `dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~ServicesLayerPurity|FullyQualifiedName~CompositionRoot|FullyQualifiedName~NormalisationUnique|FullyQualifiedName~GardesDoctrine|FullyQualifiedName~GardesPerimetre"`
      → **0 échec**.
    - `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` → **0** (acquis phase 22).
    - `grep -cF "?? new " src/Chronos/Services/DiagnosticService.cs` → **2** (acquis phase 20).
    - `grep -cF "=> Inspecter(now).Visibles;" src/Chronos/Services/SessionMonitor.cs` → **1** (acquis phase 22).
    - La mutation de falsification a été **jouée** : l'échec de `Le_mode_hook_ecrit_par_le_service_teste_et_signale_son_echec`
      est consigné nominativement dans le SUMMARY, et le `grep` de contrôle rend **1** après révocation.
    - `ls "$APPDATA/Chronos/sessions" | wc -l` → **66** ; `stat -c '%s' "$APPDATA/Chronos/archived.json"` → **84**.
    - L'overlay `Chronos-v3.0.2.exe` (pid 119412) n'a été **ni lancé ni tué**.
  </acceptance_criteria>

  <done>
    Le mode `--hook` écrit par le service testé, ne fabrique plus de fichier temporaire, écrit sa cause
    d'échec sur le flux d'erreur en UTF-8 et sort avec 1 — jamais 2. Une garde de câblage prouvée falsifiable
    l'empêche de revenir en arrière.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln -c Debug` → 0 erreur, 0 avertissement.
2. `dotnet test Chronos.sln -c Debug --nologo -v q` → 0 échec, total > 747, deux exécutions consécutives
   (la seconde attrape les tests sensibles au chargeur BAML).
3. Le scénario mesuré est rejoué et rendu : 200 écritures sous lecteur `FileShare.ReadWrite`, 0 perte.
4. Aucun échec ne peut plus être silencieux : `catch { }` = 0 dans `EcritureEtatSession.cs`, et le hook
   dispose d'un canal (flux d'erreur + code de sortie 1).
5. Invariants de sécurité : `sessions/` = 66 entrées, `archived.json` = 84 octets, `oauth.dat` = 518 octets
   (**taille seule, jamais le mtime**), overlay pid 119412 vivant et jamais touché, `git status --porcelain`
   sans écrit hors périmètre, `git diff -- '*.csproj'` vide.
</verification>

<success_criteria>
- CYC-02 est couverte sur son défaut mesuré : l'écriture directe remplace le déplacement de fichier
  temporaire, et l'échec est constatable au lieu d'être avalé.
- L'écriture est sortie de la couche WPF et couverte par six tests, dont le scénario du lecteur concurrent.
- Le mode `--hook` ne laisse plus aucun débris, et ne peut pas bloquer une session Claude Code.
- La garde de câblage a été falsifiée avant d'être committée.
- 0 échec, aucun test supprimé, aucune dépendance NuGet ajoutée, aucune donnée utilisateur touchée.
</success_criteria>

<output>
Après complétion, créer
`.planning/phases/23-un-magasin-qui-ne-croit-plus-et-n-oublie-plus/23-01-SUMMARY.md`.

Y consigner explicitement : le compte du rouge, le résultat des 200 écritures sous lecteur concurrent,
l'échec constaté lors de la mutation de falsification, et la **contrepartie assumée** de l'écriture directe
(lecture possiblement fragmentaire pendant un cycle de 2 s, contre une perte définitive auparavant).
</output>
