---
phase: 12-refonte-du-cadran-3-anneaux-remplissage-compacit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/ViewModels/WindowGaugeViewModel.cs
  - src/Chronos/Text/PercentFormatter.cs
  - src/Chronos/Rendering/DayTimeline.cs
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/Controls/TickRing.cs
  - tests/Chronos.Tests/WindowGaugeViewModelTests.cs
  - tests/Chronos.Tests/DayTimelineTests.cs
  - tests/Chronos.Tests/TickRingTests.cs
  - tests/Chronos.Tests/MainViewModelTests.cs
autonomous: true
requirements: [VIS-01, VIS-05, JOUR-01, JOUR-02]
must_haves:
  truths:
    - "FractionElapsed = clamp(1 − FractionRemaining, 0, 1) est exposée par WindowGaugeViewModel et recalculée à chaque Interpolate"
    - "UtilizationText rend « 80 % » (exact), « ~80 % » (estimé), «» (null) — l'honnêteté est préservée"
    - "DayTimeline.Fraction rend minutes-depuis-minuit-local / 1440 (18 h → 0.75, minuit → 0)"
    - "DayTimeline.ResetAngles projette les resets 5 h du jour local sur l'axe 24 h (angle = fraction-jour × 360°)"
    - "TickRing accepte une liste d'angles explicites et dessine un trait par angle"
    - "MainViewModel expose DayFraction + DayResetAngles, recalculés à chaque Interpolate"
    - "Les 188 tests existants restent verts"
  artifacts:
    - path: "src/Chronos/Text/PercentFormatter.cs"
      provides: "Formatage honnête du pourcentage d'utilization (FR, arrondi entier)"
      contains: "class PercentFormatter"
    - path: "src/Chronos/Rendering/DayTimeline.cs"
      provides: "Math pure timeline 24 h (fraction du jour + angles des resets)"
      contains: "class DayTimeline"
    - path: "src/Chronos/ViewModels/WindowGaugeViewModel.cs"
      provides: "FractionElapsed + UtilizationText + HasUtilizationText"
      contains: "FractionElapsed"
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "DayFraction + DayResetAngles pour l'anneau 24 h"
      contains: "DayResetAngles"
    - path: "src/Chronos/Controls/TickRing.cs"
      provides: "DP Angles (ticks à angles arbitraires) en plus des ticks réguliers"
      contains: "AnglesProperty"
  key_links:
    - from: "WindowGaugeViewModel.Interpolate"
      to: "FractionElapsed"
      via: "FractionElapsed = Math.Clamp(1 - FractionRemaining, 0, 1)"
      pattern: "1\\s*-\\s*FractionRemaining"
    - from: "MainViewModel.Interpolate"
      to: "DayTimeline.Fraction / DayTimeline.ResetAngles"
      via: "now.ToLocalTime() + _last.FiveHour.ResetsAt"
      pattern: "DayTimeline\\.(Fraction|ResetAngles)"
---

<objective>
Créer TOUTE la logique pure nouvelle de la refonte du cadran, testée avant toute recomposition XAML :
inversion du remplissage (FractionElapsed), formatage honnête du % (UtilizationText), math de la timeline
24 h (fraction du jour + angles des resets 5 h projetés), et l'extension TickRing pour des ticks à angles
arbitraires. Aucun XAML n'est touché ici — le plan 12-02 consommera ces propriétés/contrôles.

Purpose: Isoler la math testable (pattern ArcGeometry/RampColor de Phase 5) pour que la composition visuelle
du plan 02 ne fasse que du binding, sans logique cachée dans le XAML.
Output: PercentFormatter, DayTimeline, FractionElapsed/UtilizationText sur WindowGaugeViewModel,
DayFraction/DayResetAngles sur MainViewModel, TickRing.Angles — le tout couvert par des tests unitaires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-refonte-du-cadran-3-anneaux-remplissage-compacit/12-CONTEXT.md

<interfaces>
<!-- Contrats réels extraits du code — l'exécuteur les utilise directement, sans exploration. -->

// src/Chronos/ViewModels/WindowGaugeViewModel.cs (ObservableObject, CommunityToolkit.Mvvm)
[ObservableProperty] private double _fractionRemaining;   // 0..1, posé dans Interpolate
[ObservableProperty] private double? _utilization;         // 0..1 ou null, posé dans Apply
[ObservableProperty] private bool _isEstimated;            // posé dans Apply (Reliability == Estimated)
public void Apply(WindowState s) { ... }                   // thread UI, pose Utilization/IsEstimated/…
public void Interpolate(DateTimeOffset now) { FractionRemaining = ...; CountdownText = ...; }

// src/Chronos/Models/WindowState.cs
public DateTimeOffset? ResetsAt { get; init; }
public double? Utilization { get; init; }

// src/Chronos/ViewModels/MainViewModel.cs
public WindowGaugeViewModel FiveHour { get; }              // fenêtre 5 h
private UsageSnapshot? _last;                              // dernier snapshot appliqué → _last.FiveHour.ResetsAt
internal void Interpolate(DateTimeOffset now) { FiveHour.Interpolate(now); SevenDay.Interpolate(now); ... }
// _clock.UtcNow est UTC ; pour l'heure locale utiliser now.ToLocalTime()

// src/Chronos/Rendering/ArcGeometry.cs (repère Y-inversé, 0° = 12 h, horaire)
public static Point PointAt(Point center, double radius, double deg);

// src/Chronos/Controls/TickRing.cs (Shape) — DP existantes : Count, Radius, TickLength, StartAngle
// DefiningGeometry boucle for i in 0..Count : angle = StartAngle + i*360/Count → LineGeometry(inner,outer)

// src/Chronos/Text/CountdownFormatter.cs — modèle de formatter pur FR (littéraux fixes, aucune CultureInfo)

// tests : xUnit ; [Fact] pour la math pure, [WpfFact] (Xunit.StaFact) dès qu'on construit des Geometry WPF.
// FakeClock(DateTimeOffset now) : IClock injectable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : FractionElapsed + PercentFormatter + UtilizationText (VIS-01, VIS-05)</name>
  <files>src/Chronos/Text/PercentFormatter.cs, src/Chronos/ViewModels/WindowGaugeViewModel.cs, tests/Chronos.Tests/WindowGaugeViewModelTests.cs</files>
  <read_first>
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (Apply + Interpolate)
    - src/Chronos/Text/CountdownFormatter.cs (modèle de formatter pur FR)
    - tests/Chronos.Tests/WindowGaugeViewModelTests.cs (style [Fact])
  </read_first>
  <behavior>
    PercentFormatter.Format(double? utilization, bool isEstimated) :
    - Test « UtilizationText_null_rend_vide » : Format(null, false) == "" ET Format(null, true) == ""
    - Test « UtilizationText_exact_rend_80pourcent » : Format(0.80, false) == "80 %" (espace avant %)
    - Test « UtilizationText_estime_prefixe_tilde » : Format(0.80, true) == "~80 %"
    - Test « UtilizationText_arrondi_entier » : Format(0.804, false) == "80 %" ; Format(0.806, false) == "81 %"
    - Test « UtilizationText_plein_100 » : Format(1.0, false) == "100 %"
    FractionElapsed (WindowGaugeViewModel) :
    - Test « Elapsed_inverse_le_remplissage » : après Interpolate avec FractionRemaining = 0.25 → FractionElapsed == 0.75
    - Test « Elapsed_clampe_0_1 » : FractionRemaining bornée [0..1] → FractionElapsed ∈ [0..1] (jamais négatif)
    UtilizationText posé par Apply :
    - Test « UtilizationText_pose_par_Apply_exact » : Apply(Exact, Utilization=0.8) → UtilizationText == "80 %", HasUtilizationText == true
    - Test « UtilizationText_absent_si_null » : Apply(util null) → UtilizationText == "", HasUtilizationText == false
  </behavior>
  <action>
    1) Créer src/Chronos/Text/PercentFormatter.cs — classe statique pure (commentaires FR, aucune CultureInfo,
       modèle CountdownFormatter). Signature : `public static string Format(double? utilization, bool isEstimated)`.
       - utilization null → "" (honnêteté : pas de plafond → pas de %).
       - sinon pct = (int)Math.Round(utilization.Value * 100, MidpointRounding.AwayFromZero) ;
         préfixe = isEstimated ? "~" : "" ; retourner $"{préfixe}{pct} %" (espace normal avant %).
    2) WindowGaugeViewModel : ajouter `[ObservableProperty] private double _fractionElapsed;`. Dans Interpolate,
       APRÈS le calcul de FractionRemaining, ajouter :
       `FractionElapsed = System.Math.Clamp(1.0 - FractionRemaining, 0.0, 1.0);` (VIS-01, inversion, clamp).
       NE PAS supprimer FractionRemaining (toujours utilisée par le calcul).
    3) WindowGaugeViewModel : ajouter `[ObservableProperty] private string _utilizationText = "";` et
       `[ObservableProperty] private bool _hasUtilizationText;`. Dans Apply, APRÈS le calcul de IsEstimated :
       `UtilizationText = PercentFormatter.Format(s.Utilization, IsEstimated);`
       `HasUtilizationText = s.Utilization is not null;` (pilote la visibilité du séparateur « · » côté XAML,
       même pattern que HasTokens).
    4) Écrire les tests du bloc <behavior> dans WindowGaugeViewModelTests.cs (noms de méthodes contenant
       « Elapsed » et « UtilizationText » pour les filtres). RED d'abord (build échoue tant que les props/format
       n'existent pas), puis GREEN.
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~Elapsed|FullyQualifiedName~UtilizationText"</automated>
  </verify>
  <acceptance_criteria>
    - PercentFormatter.Format couvre null/exact/estimé/arrondi/100 % (tests verts).
    - FractionElapsed == clamp(1 − FractionRemaining) vérifié après Interpolate.
    - UtilizationText + HasUtilizationText posés par Apply, vérifiés exact/null.
    - `dotnet build Chronos.sln -c Debug` réussit.
  </acceptance_criteria>
  <done>FractionElapsed, UtilizationText, HasUtilizationText exposés et testés ; PercentFormatter pur créé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : DayTimeline (fraction jour + angles resets) + exposition MainViewModel (JOUR-01, JOUR-02)</name>
  <files>src/Chronos/Rendering/DayTimeline.cs, src/Chronos/ViewModels/MainViewModel.cs, tests/Chronos.Tests/DayTimelineTests.cs, tests/Chronos.Tests/MainViewModelTests.cs</files>
  <read_first>
    - src/Chronos/Rendering/ArcGeometry.cs (repère 0° = 12 h horaire ; math pure comme modèle)
    - src/Chronos/ViewModels/MainViewModel.cs (Interpolate, _last, _clock)
    - tests/Chronos.Tests/Fakes/FakeClock.cs
    - tests/Chronos.Tests/MainViewModelTests.cs (montage du VM en test)
  </read_first>
  <behavior>
    DayTimeline.Fraction(DateTimeOffset localNow) — travaille sur .TimeOfDay du VALUE fourni (indépendant du
    fuseau machine ; l'appelant convertit via ToLocalTime) :
    - Test « DayTimeline_minuit_rend_zero » : Fraction(2026-07-09T00:00 +02:00) == 0.0
    - Test « DayTimeline_18h_rend_075 » : Fraction(2026-07-09T18:00 +02:00) == 0.75 (précision 1e-9)
    - Test « DayTimeline_borne_sous_1 » : Fraction(23:59:59) < 1.0 et ≥ 0.999
    DayTimeline.ResetAngles(DateTimeOffset localNow, DateTimeOffset? localReset5h) → IReadOnlyList&lt;double&gt; :
    - Test « DayTicks_null_rend_liste_vide » : ResetAngles(now, null) est vide.
    - Test « DayTicks_resets_du_jour_projetes » : localNow = 09-07 18:00, reset = 09-07 03:00 (offset +02:00)
      → angles [45, 120, 195, 270, 345] (resets locaux 03/08/13/18/23 h → h/24×360°), triés croissants.
    - Test « DayTicks_offset_de_phase » : reset = 09-07 01:00 → angles [15, 90, 165, 240, 315].
    MainViewModel expose la timeline :
    - Test « DayFraction_et_angles_poses_par_Interpolate » : après ApplySnapshot avec FiveHour.ResetsAt connu
      puis Interpolate(now), DayFraction ≈ attendu et DayResetAngles non vide ; si _last.FiveHour.ResetsAt null
      → DayResetAngles vide.
  </behavior>
  <action>
    1) Créer src/Chronos/Rendering/DayTimeline.cs — classe statique PURE (commentaires FR). 0° = minuit au
       sommet (cohérent ArcGeometry : 0° = 12 h horaire). Deux méthodes :
       - `public static double Fraction(DateTimeOffset localNow)` :
         `return localNow.TimeOfDay.TotalMinutes / 1440.0;` (lit le .TimeOfDay du value → déterministe en test).
       - `public static IReadOnlyList<double> ResetAngles(DateTimeOffset localNow, DateTimeOffset? localReset5h)` :
         si localReset5h null → `Array.Empty<double>()`. Sinon :
         * base = localReset5h.Value ; step = TimeSpan.FromHours(5).
         * Ramener au 1er reset ≥ minuit du jour de localNow : soustraire/ajouter des pas de 5 h tant que
           reset.Date != localNow.Date ; puis énumérer en avançant de 5 h TANT QUE reset.Date == localNow.Date.
         * Pour chaque reset retenu : angle = reset.TimeOfDay.TotalMinutes / 1440.0 * 360.0.
         * Retourner la liste triée croissante (List&lt;double&gt;).
         Comparer les .Date des DateTimeOffset (composante offset-locale du value → indépendant fuseau machine).
    2) MainViewModel : ajouter `[ObservableProperty] private double _dayFraction;` et
       `[ObservableProperty] private System.Collections.Generic.IReadOnlyList<double> _dayResetAngles = System.Array.Empty<double>();`.
       Dans Interpolate(now), après FiveHour/SevenDay.Interpolate :
       `var localNow = now.ToLocalTime();`
       `DayFraction = DayTimeline.Fraction(localNow);`
       `DayResetAngles = DayTimeline.ResetAngles(localNow, _last?.FiveHour.ResetsAt?.ToLocalTime());`
       (JOUR-01/02 ; la couleur 24 h réutilisera FiveHour.Utilization côté XAML → JOUR-03 au plan 02.)
    3) Écrire DayTimelineTests.cs ([Fact], noms contenant « DayTimeline » et « DayTicks ») + un test dans
       MainViewModelTests.cs (montage existant du VM). RED puis GREEN.
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~DayTimeline|FullyQualifiedName~DayTicks|FullyQualifiedName~DayFraction"</automated>
  </verify>
  <acceptance_criteria>
    - Fraction : minuit → 0, 18 h → 0.75, borne < 1 (tests verts).
    - ResetAngles : null → vide ; resets du jour projetés en angles triés (exemples [45,120,195,270,345] et [15,90,165,240,315]).
    - MainViewModel.Interpolate pose DayFraction + DayResetAngles ; angles vides si ResetsAt 5 h inconnu.
    - `dotnet build Chronos.sln -c Debug` réussit.
  </acceptance_criteria>
  <done>DayTimeline pure testée ; MainViewModel expose DayFraction + DayResetAngles rafraîchis chaque seconde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : TickRing.Angles — ticks à angles arbitraires (JOUR-02)</name>
  <files>src/Chronos/Controls/TickRing.cs, tests/Chronos.Tests/TickRingTests.cs</files>
  <read_first>
    - src/Chronos/Controls/TickRing.cs (DefiningGeometry actuel, boucle Count)
    - src/Chronos/Rendering/ArcGeometry.cs (PointAt)
    - tests/Chronos.Tests/ArcGeometryTests.cs (usage [WpfFact], Xunit.StaFact)
  </read_first>
  <behavior>
    - Test « TickRing_Angles_dessine_un_trait_par_angle » ([WpfFact]) : une instance TickRing avec
      Width=Height=100, Radius=40, TickLength=6, Angles=[45,120,195,270,345] → DefiningGeometry est un
      GeometryGroup de 5 LineGeometry (un par angle).
    - Test « TickRing_Angles_vide_ou_null_ne_dessine_rien » : Angles = liste vide → GeometryGroup à 0 enfant.
    - Test « TickRing_sans_Angles_garde_comportement_regulier » : Angles null + Count=12 → 12 traits (non-régression).
  </behavior>
  <action>
    Étendre TickRing (extension minime, PAS de nouveau contrôle) :
    1) Ajouter une DP `Angles` de type `System.Collections.Generic.IReadOnlyList<double>` (défaut null),
       FrameworkPropertyMetadataOptions.AffectsRender (redessin auto au changement de binding).
    2) Dans DefiningGeometry : SI Angles n'est pas null → dessiner un LineGeometry(inner, outer) pour CHAQUE
       angle de la liste (inner = PointAt(center, Radius − TickLength, a), outer = PointAt(center, Radius, a)) ;
       SINON conserver la boucle régulière `Count` existante à l'identique (non-régression décorative).
       Choix de conception : la liste d'angles est le contrat honnête pour JOUR-02 car 24 h / 5 h = 4,8 resets/jour
       (non entier) → des ticks « réguliers + offset » produiraient un dernier écart faux ; on dessine donc
       exactement les resets du jour fournis par DayTimeline.ResetAngles.
    3) Écrire TickRingTests.cs ([WpfFact]). RED puis GREEN.
  </action>
  <verify>
    <automated>dotnet test Chronos.sln -c Debug --filter "FullyQualifiedName~TickRing"</automated>
  </verify>
  <acceptance_criteria>
    - Angles=[5 valeurs] → 5 LineGeometry ; Angles vide → 0 ; Angles null → boucle Count intacte.
    - DP Angles marquée AffectsRender.
    - Suite complète verte : `dotnet test Chronos.sln -c Debug` (aucune régression sur les 188 tests).
  </acceptance_criteria>
  <done>TickRing peut rendre des ticks à angles arbitraires via la DP Angles, sans casser les ticks réguliers.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit après chaque tâche.
- `dotnet test Chronos.sln -c Debug` : les 188 tests existants + les nouveaux passent (aucune régression).
- FractionElapsed, UtilizationText/HasUtilizationText, DayFraction, DayResetAngles, TickRing.Angles existent
  et sont couverts par des tests.
</verification>

<success_criteria>
- VIS-01 (logique) : FractionElapsed = clamp(1 − FractionRemaining) exposée et testée.
- VIS-05 (logique) : UtilizationText honnête (exact/estimé/null) exposé et testé.
- JOUR-01 (logique) : DayTimeline.Fraction + MainViewModel.DayFraction (18 h → 0.75) testés.
- JOUR-02 (logique) : DayTimeline.ResetAngles + MainViewModel.DayResetAngles + TickRing.Angles testés.
- Aucun XAML modifié ; aucune source de données touchée ; 188 tests toujours verts.
</success_criteria>

<output>
Après complétion, créer .planning/phases/12-refonte-du-cadran-3-anneaux-remplissage-compacit/12-01-SUMMARY.md
</output>
