---
phase: 12-refonte-du-cadran-3-anneaux-remplissage-compacit
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/Chronos/Views/MainWindow.xaml
  - src/Chronos/Resources/DesignTokens.xaml
autonomous: false
requirements: [VIS-01, VIS-02, VIS-05, JOUR-01, JOUR-02, JOUR-03, TAILLE-01]
must_haves:
  truths:
    - "L'overlay mesure 170×170 px, centre 85,85, sans chevauchement des 3 anneaux ni du texte"
    - "Ordre centre → extérieur : hebdo (rayon min) → 5 h (milieu) → 24 h (rayon max)"
    - "Les arcs de valeur 5 h et hebdo bindent FractionElapsed (se REMPLISSENT vers le reset)"
    - "L'anneau 24 h se remplit selon DayFraction et prend la couleur de FiveHour.Utilization via UtilBrush"
    - "Des ticks marquent les resets 5 h projetés (TickRing.Angles ← DayResetAngles)"
    - "Chaque countdown affiche son % à côté (UtilizationText), séparateur visible seulement si % présent"
    - "Le drag et le snap continuent de fonctionner à 170 px"
  artifacts:
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "Cadran refondu : 3 anneaux réordonnés + anneau 24 h + % au centre + resize 170"
      contains: "Width=\"170\""
    - path: "src/Chronos/Resources/DesignTokens.xaml"
      provides: "Token de piste pour l'anneau 24 h"
      contains: "Piste24h"
  key_links:
    - from: "MainWindow.xaml arcs valeur"
      to: "FiveHour.FractionElapsed / SevenDay.FractionElapsed"
      via: "Binding Fraction"
      pattern: "FractionElapsed"
    - from: "MainWindow.xaml anneau 24 h"
      to: "DayFraction + FiveHour.Utilization + DayResetAngles"
      via: "Binding Fraction / Stroke(UtilBrush) / TickRing.Angles"
      pattern: "DayFraction|DayResetAngles"
---

<objective>
Recomposer le cadran dans MainWindow.xaml pour livrer visuellement la refonte : inversion du remplissage
(bind FractionElapsed), 3 anneaux réordonnés (hebdo interne → 5 h milieu → 24 h externe), nouvel anneau
timeline 24 h coloré et gradué aux resets, % d'utilization à côté de chaque countdown, et resize à 170 px
sans chevauchement. Consomme exclusivement les propriétés/contrôles livrés par le plan 12-01 — aucune
nouvelle logique ici, uniquement du binding et de la géométrie XAML. Se termine par un checkpoint visuel.

Purpose: Rendre la refonte réelle à l'écran tout en préservant l'honnêteté (% « ~ » si estimé, absent si null)
et la mécanique fenêtre (drag/snap) inchangée.
Output: MainWindow.xaml recomposé (170 px, 3 anneaux + 24 h + %), DesignTokens.xaml enrichi, validation visuelle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-refonte-du-cadran-3-anneaux-remplissage-compacit/12-CONTEXT.md
@src/Chronos/Views/MainWindow.xaml
@src/Chronos/Resources/DesignTokens.xaml

<interfaces>
<!-- Propriétés/contrôles livrés par le plan 12-01 — à binder directement. -->
// WindowGaugeViewModel : FractionElapsed (double 0..1), UtilizationText (string), HasUtilizationText (bool),
//                        Utilization (double?), CountdownText, IsEstimated, Exhausted, TokensText, HasTokens
// MainViewModel        : FiveHour, SevenDay (WindowGaugeViewModel), DayFraction (double 0..1),
//                        DayResetAngles (IReadOnlyList<double>)
// TickRing (Shape)     : DP Angles (IReadOnlyList<double>) → un tick par angle ; Radius, TickLength, Stroke, StrokeThickness
// RingArc (Shape)      : Fraction (0..1), Radius, StrokeThickness, Stroke ; Fraction=1 = piste pleine
// Converters (DesignTokens.xaml) : UtilBrush (UtilizationToBrushConverter), BoolToVis (BooleanToVisibilityConverter)
// Repère : 0° = 12 h (sommet), sens horaire — cohérent entre les 3 anneaux et la timeline 24 h.
</interfaces>

<geometry>
<!-- Valeurs CONCRÈTES pour 170 px (centre 85,85). Départ des checkpoints ; ajustables en UAT. -->
Fenêtre : Width=170 Height=170.
Ellipse de fond : Width=156 Height=156 (rayon 78), Rim StrokeThickness=2.
Anneaux (StrokeThickness=9 chacun, gap ~5 px entre bords) :
  - hebdo  (interne) : Radius=44
  - 5 h    (milieu)  : Radius=58
  - 24 h   (externe) : Radius=72
Ticks resets 24 h : TickRing Radius=76, TickLength=6 (pointe vers l'intérieur, bord externe du 24 h).
Zone texte centrale : diamètre ~78 (rayon hebdo interne 44 − 4,5 ≈ 39). Polices réduites (voir Task 2).
</geometry>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Réordonner les 3 anneaux + anneau 24 h + resize 170 (VIS-01, VIS-02, JOUR-01/02/03, TAILLE-01)</name>
  <files>src/Chronos/Views/MainWindow.xaml, src/Chronos/Resources/DesignTokens.xaml</files>
  <read_first>
    - src/Chronos/Views/MainWindow.xaml (structure actuelle : Ellipse, TickRing, pistes, arcs valeur)
    - src/Chronos/Resources/DesignTokens.xaml (tokens Piste5h/PisteHebdo, UtilBrush)
  </read_first>
  <action>
    DesignTokens.xaml :
    - Ajouter un token de piste pour l'anneau 24 h, ex. `&lt;SolidColorBrush x:Key="Piste24h" Color="#242330"/&gt;`
      (nuance sombre proche de Piste5h/PisteHebdo ; commentaire FR). Ajouter aussi, si besoin, un token
      `Tick24h` pour la couleur des ticks resets (réutiliser TickMajeur #46454F sinon).

    MainWindow.xaml :
    1) Fenêtre : Width="170" Height="170". Ellipse de fond : Width="156" Height="156" (centrée), Rim inchangé.
    2) Réordonner et redimensionner les anneaux, du fond vers le premier plan, ordre centre → extérieur
       (VIS-02) — INVERSION de l'ordre actuel (hebdo était intérieur mais 5 h extérieur ; on garde hebdo au
       plus petit rayon, 5 h au milieu, 24 h au plus grand). Pour CHAQUE anneau : d'abord la piste
       (RingArc Fraction="1"), puis l'arc de valeur par-dessus.
       - Piste hebdo : RingArc Fraction="1" Radius="44" StrokeThickness="9" Stroke="{StaticResource PisteHebdo}".
       - Piste 5 h   : RingArc Fraction="1" Radius="58" StrokeThickness="9" Stroke="{StaticResource Piste5h}".
       - Piste 24 h  : RingArc Fraction="1" Radius="72" StrokeThickness="9" Stroke="{StaticResource Piste24h}".
    3) Arcs de valeur — binder FractionElapsed (VIS-01, remplissage inversé) :
       - Hebdo : RingArc x:Name="ArcHebdo" Radius="44" StrokeThickness="9" StrokeStartLineCap/EndLineCap="Round"
         Fraction="{Binding SevenDay.FractionElapsed}"
         Stroke="{Binding SevenDay.Utilization, Converter={StaticResource UtilBrush}}".
       - 5 h : RingArc x:Name="ArcCinqHeures" Radius="58" StrokeThickness="9" caps Round
         Fraction="{Binding FiveHour.FractionElapsed}"
         Stroke="{Binding FiveHour.Utilization, Converter={StaticResource UtilBrush}}".
       - 24 h : RingArc x:Name="ArcVingtQuatreHeures" Radius="72" StrokeThickness="9" caps Round
         Fraction="{Binding DayFraction}" (JOUR-01)
         Stroke="{Binding FiveHour.Utilization, Converter={StaticResource UtilBrush}}" (JOUR-03, même rampe que 5 h).
    4) Ticks resets 24 h (JOUR-02) : `&lt;ctrl:TickRing Radius="76" TickLength="6"
         Angles="{Binding DayResetAngles}" Stroke="{StaticResource TickMajeur}" StrokeThickness="2"/&gt;`
       placé au-dessus de l'anneau 24 h.
    5) Graduations décoratives existantes (TickRing Count=60 / Count=12, Radius=96) : les SUPPRIMER ou les
       rescaler sous 170 px pour éviter le fouillis (discrétion — à 170 px, privilégier la lisibilité ;
       si conservées, Radius ≤ 78 pour rester dans le fond). Recommandation : supprimer les décoratives,
       ne garder que les ticks resets porteurs de sens.
    6) NE PAS toucher au ContextMenu, ni au code-behind drag/snap (la logique de snap se base sur le Rect
       fenêtre réel → fonctionne à 170 px sans changement).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Width="170"' MainWindow.xaml` et `grep 'Height="170"'` présents (TAILLE-01).
    - Ordre des RingArc valeur : ArcHebdo (Radius 44) avant ArcCinqHeures (Radius 58) avant ArcVingtQuatreHeures (Radius 72) (VIS-02).
    - ArcHebdo/ArcCinqHeures bindent FractionElapsed (VIS-01) ; ArcVingtQuatreHeures binde DayFraction (JOUR-01)
      et Stroke = FiveHour.Utilization via UtilBrush (JOUR-03).
    - TickRing Angles="{Binding DayResetAngles}" présent (JOUR-02) ; token Piste24h ajouté.
    - `dotnet build Chronos.sln -c Debug` réussit ; les 188+ tests restent verts (`dotnet test`).
  </acceptance_criteria>
  <done>Les 3 anneaux réordonnés + l'anneau 24 h coloré/gradué s'affichent à 170 px, arcs bindés sur FractionElapsed/DayFraction.</done>
</task>

<task type="auto">
  <name>Task 2 : % au centre à côté de chaque countdown + resize des polices (VIS-05, TAILLE-01)</name>
  <files>src/Chronos/Views/MainWindow.xaml</files>
  <read_first>
    - src/Chronos/Views/MainWindow.xaml (bloc central StackPanel : countdown, badges, tokens)
  </read_first>
  <action>
    Recomposer le bloc central pour afficher le % À CÔTÉ de chaque countdown (VIS-05) et réduire les polices
    pour tenir dans ~78 px sans chevauchement (TAILLE-01) :
    1) Groupe 5 h — remplacer le TextBlock countdown seul par un StackPanel Orientation="Horizontal"
       HorizontalAlignment="Center" contenant :
       - TextBlock Text="{Binding FiveHour.CountdownText}" FontSize="18" FontWeight="SemiBold"
         Foreground="{StaticResource TextePrincipal}".
       - TextBlock Text=" · " FontSize="18" Foreground="{StaticResource TexteSecondaire}"
         Visibility="{Binding FiveHour.HasUtilizationText, Converter={StaticResource BoolToVis}}" (séparateur
         visible seulement si % présent → honnêteté : rien affiché quand Utilization null).
       - TextBlock Text="{Binding FiveHour.UtilizationText}" FontSize="18"
         Foreground="{StaticResource TexteSecondaire}".
    2) Groupe hebdo — même structure horizontale, FontSize="12", countdown en
       Foreground="{StaticResource TexteSecondaireClair}", séparateur/`%` lié à SevenDay.HasUtilizationText /
       SevenDay.UtilizationText.
    3) Conserver les badges existants (« estimée », « quota épuisé », tokens NET-02, staleness, indisponible)
       mais réduire leurs FontSize (ex. 9 → 8) pour la compacité. Le « ~ » du % porte déjà l'info estimée :
       garder le badge « estimée » (non redondant lourd) — discrétion, simplifier seulement si visuellement chargé.
    4) Vérifier qu'aucune ligne ne déborde du diamètre ~78 px (zone hebdo interne). Ajuster FontSize si besoin
       au checkpoint (Task 3).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'FiveHour.UtilizationText' MainWindow.xaml` et `grep 'SevenDay.UtilizationText'` présents (VIS-05).
    - Séparateur « · » lié à HasUtilizationText (absent si Utilization null).
    - Polices réduites (5 h ≈ 18, hebdo ≈ 12) pour 170 px.
    - `dotnet build Chronos.sln -c Debug` réussit.
  </acceptance_criteria>
  <done>Chaque countdown affiche « countdown · % » honnête (rien si null, « ~ » si estimé), lisible à 170 px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : Checkpoint visuel — cadran refondu à 170 px</name>
  <files>src/Chronos/Views/MainWindow.xaml</files>
  <action>Lancer l'overlay et valider visuellement la refonte (voir how-to-verify). Aucune ecriture de code
    tant que l'utilisateur n'a pas signale d'ajustement ; le cas echeant, ajuster rayons/epaisseurs/FontSize
    dans MainWindow.xaml puis re-verifier.</action>
  <verify><automated>dotnet build Chronos.sln -c Debug</automated></verify>
  <done>L'utilisateur confirme (approuve) remplissage inverse, ordre des anneaux, anneau 24 h, %, compacite et drag/snap.</done>
  <what-built>
    Cadran refondu : arcs qui se REMPLISSENT vers le reset (FractionElapsed), 3 anneaux réordonnés
    (hebdo interne → 5 h milieu → 24 h externe), anneau timeline 24 h coloré (utilization 5 h) et gradué
    aux resets, % d'utilization à côté de chaque countdown, overlay à 170 px.
  </what-built>
  <how-to-verify>
    1) Lancer : `dotnet run --project src/Chronos/Chronos.csproj -c Debug` (ou l'exe build). La fenêtre
       doit apparaître à ~170 px sans crash.
    2) Remplissage inversé (VIS-01) : vérifier que les arcs sont PLUS remplis quand il reste PEU de temps
       avant le reset (vides en début de fenêtre). Comparer 5 h et hebdo.
    3) Ordre des anneaux (VIS-02) : du centre vers l'extérieur = hebdo, 5 h, 24 h — sans chevauchement.
    4) Anneau 24 h (JOUR-01/02/03) : rempli de minuit à maintenant (à 18 h ≈ 3/4), ticks aux resets 5 h
       cohérents avec l'heure réelle, couleur = celle de l'anneau 5 h.
    5) % au centre (VIS-05) : « countdown · % » lisible ; « ~ » si source estimée ; PAS de % si utilization
       absente (badge « estimée » cohérent).
    6) Compacité (TAILLE-01) : 3 anneaux + 2 lignes de texte lisibles, aucun chevauchement ; tester le
       drag (clic gauche) et le snap dans un coin — placement correct à 170 px.
    Si un réglage visuel gêne (police trop large, gap d'anneau, longueur de tick), ajuster les valeurs
    concrètes du plan (rayons/épaisseurs/FontSize) et re-vérifier.
  </how-to-verify>
  <resume-signal>Taper « approuvé » ou décrire les ajustements visuels à appliquer.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit ; `dotnet test Chronos.sln -c Debug` reste vert (188+).
- MainWindow.xaml : Width/Height 170, 3 RingArc valeur réordonnés (hebdo 44 → 5 h 58 → 24 h 72),
  anneau 24 h bindé DayFraction + couleur FiveHour.Utilization, TickRing Angles ← DayResetAngles,
  UtilizationText affiché pour les deux fenêtres.
- Validation visuelle humaine (Task 3) confirme remplissage inversé, ordre, 24 h, %, compacité, drag/snap.
</verification>

<success_criteria>
- VIS-01 : arcs 5 h et hebdo bindent FractionElapsed → se remplissent vers le reset.
- VIS-02 : ordre hebdo (44) → 5 h (58) → 24 h (72), sans chevauchement.
- JOUR-01/02/03 : anneau 24 h rempli selon DayFraction, gradué aux resets (DayResetAngles), coloré selon FiveHour.Utilization.
- VIS-05 : % honnête à côté de chaque countdown (~ si estimé, absent si null).
- TAILLE-01 : overlay 170 px lisible, sans chevauchement, drag/snap fonctionnels.
</success_criteria>

<output>
Après complétion, créer .planning/phases/12-refonte-du-cadran-3-anneaux-remplissage-compacit/12-02-SUMMARY.md
</output>
