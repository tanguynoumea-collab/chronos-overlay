---
phase: 21-p-rim-tre-le-widget-ne-parle-que-de-claude-code
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - src/Chronos/Services/ArchiveStore.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/ArchiveStorePurgeTests.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
autonomous: true
requirements: [SRC-02]

must_haves:
  truths:
    - "Les entrées fantômes présentes dans archived.json sont RETIRÉES DU FICHIER au premier lancement, pas seulement écartées à la lecture"
    - "Une session Claude Code archivée par l'utilisateur survit intacte à la purge, horodatage compris"
    - "Une entrée simplement expirée n'est pas supprimée par la purge : purger et expirer sont deux gestes distincts"
    - "Quand il n'y a rien à purger, le fichier de l'utilisateur n'est pas réécrit"
    - "La purge ne rend un nombre de retraits que si l'écriture a réellement eu lieu"
  artifacts:
    - path: "src/Chronos/Services/ArchiveStore.cs"
      provides: "PurgerPrefixe — retrait par préfixe, sans filtre de TTL, sans réécriture inutile"
      contains: "public int PurgerPrefixe(string prefixe)"
    - path: "tests/Chronos.Tests/ArchiveStorePurgeTests.cs"
      provides: "Le contenu RÉEL mesuré de archived.json rejoué en test"
      contains: "desktop:foreground:unknown"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "ArchiveStore.PurgerPrefixe"
      via: "un appel best-effort au démarrage, mode overlay uniquement"
      pattern: "PurgerPrefixe\\(\"desktop:\"\\)"
---

<objective>
SRC-02 : ce qui restait du contournement manuel de l'utilisateur s'efface.

`%APPDATA%\Chronos\archived.json` contient — mesuré le 2026-09-12, 84 octets, mtime du 12 juillet — **deux
entrées et rien d'autre** :

```json
{"desktop:foreground:unknown":1783867136065,"desktop:foreground:code":1783867137990}
```

`ArchiveStore.Load()` les écarte déjà, par TTL. Ce n'est pas suffisant, et la distinction est le cœur de
cette exigence : **écarter à la lecture n'est pas retirer**. Ces deux lignes prouvent que l'utilisateur
avait dû archiver ces fantômes **à la main**, parce qu'ils portaient un horodatage rafraîchi à chaque poll
— ils ne vieillissaient jamais, ils ne pouvaient jamais expirer, et la seule action possible était le menu
contextuel. Tant que ces lignes restent dans son fichier, son geste de contournement reste gravé dans ses
données.

Purpose : dernier plan de la phase. Il ferme SRC-02 et porte le **bilan nominatif du recul de couverture**,
qui est le livrable de vérification de cette phase de démolition.

Output : `ArchiveStore.PurgerPrefixe`, son câblage au démarrage, 7 tests, et le bilan chiffré.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-CONTEXT.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-01-SUMMARY.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-02-SUMMARY.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-03-SUMMARY.md

<interfaces>
<!-- Contrats existants, extraits du dépôt le 2026-09-12. NE PAS explorer le code pour les retrouver. -->

```csharp
// src/Chronos/Services/ArchiveStore.cs — 73 lignes, DEUX membres publics aujourd'hui
public sealed class ArchiveStore
{
    private static readonly System.TimeSpan Ttl = System.TimeSpan.FromHours(6);
    private readonly string _path;
    public ArchiveStore(string? path = null)
        => _path = path ?? Path.Combine(Environment.GetFolderPath(SpecialFolder.ApplicationData), "Chronos", "archived.json");

    public ISet<string> Load();          // identifiants encore valides (TTL non dépassé)
    public void Add(string sessionId);   // archive + purge des expirées, écriture par tmp + File.Move
}
```

```csharp
// src/Chronos/App.xaml.cs — OnStartup, mode OVERLAY uniquement (les modes --hook et --statusline sortent
// bien plus haut, l. 20 et 30). Le point d'insertion est JUSTE APRÈS ce bloc :
        try
        {
            _host.Services.GetRequiredService<ClaudeSettingsReconciler>()
                 .Reconcile(settings.SessionsWidgetEnabled);
        }
        catch { }

        // ← ICI

        _host.Services.GetRequiredService<IStatusLineSetup>().OfferOnFirstRun();
        _host.Services.GetRequiredService<ISessionsController>().ShowIfEnabled();

// ArchiveStore est déjà un singleton du conteneur (l. 205) : GetRequiredService<ArchiveStore>() résout.
```

```csharp
// tests/Chronos.Tests/GardesPerimetreTests.cs — créé au plan 03. L'aide y est déjà :
internal static string CheminSources()   // chemin des sources INJECTÉ par MSBuild, jamais deviné
```

Contenu RÉEL de `%APPDATA%\Chronos\archived.json`, relevé le 2026-09-12 (84 octets, mtime 2026-07-12) :
```json
{"desktop:foreground:unknown":1783867136065,"desktop:foreground:code":1783867137990}
```
Et celui de `treated.json`, pour mémoire : **aucune** entrée préfixée — deux UUID seulement. SRC-02 ne
porte donc que sur `archived.json`.

Mesure de la phase 23, à NE PAS utiliser ici : l'écriture par `tmp` + `File.Move` échoue 200 fois sur 500
contre un lecteur concurrent, l'écriture directe 0 fois sur 500. `Add` garde son écriture actuelle
(c'est CYC-02, phase 23) ; la nouvelle méthode, elle, écrit directement — un geste unique au démarrage,
sans lecteur concurrent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : PurgerPrefixe — retirer du fichier, sans expirer au passage et sans réécrire pour rien</name>
  <files>src/Chronos/Services/ArchiveStore.cs, tests/Chronos.Tests/ArchiveStorePurgeTests.cs</files>

  <read_first>
    - src/Chronos/Services/ArchiveStore.cs (INTÉGRALEMENT, 73 lignes — `Load` applique un TTL de 6 h,
      `Add` réécrit en filtrant les expirées : deux comportements dont la nouvelle méthode doit se
      DÉMARQUER)
    - tests/Chronos.Tests/SessionsTests.cs (l. 303-322 `Archive_retire_la_session_de_l_affichage` — la
      seule couverture existante d'`ArchiveStore`, pour ne pas la dupliquer)
    - .planning/debug/widget-sessions-statuts.md (l'entrée d'évidence du 2026-09-12T13:38Z : « ArchiveStore
      applique un TTL de 6 h alors que son contrat annoncé est permanent »)
  </read_first>

  <behavior>
    - Un `archived.json` contenant EXACTEMENT les deux entrées mesurées → `PurgerPrefixe("desktop:")` rend
      **2**, et le fichier contient ensuite `{}`.
    - Une entrée d'UUID (session Claude Code réellement archivée par l'utilisateur) survit à la purge avec
      son horodatage INCHANGÉ.
    - Une entrée non préfixée mais EXPIRÉE (7 h) reste dans le fichier après la purge : purger n'est pas
      expirer. `Load()` continue de ne pas la rendre — ce sont deux questions distinctes.
    - Aucune entrée préfixée → rend **0** et le fichier N'EST PAS réécrit (date de dernière écriture
      inchangée).
    - Fichier absent, vide, ou contenant du non-JSON → rend **0**, aucune exception.
    - Le préfixe `desktop:` couvre aussi les identifiants de type `desktop:session:…`.
  </behavior>

  <action>
Écrire d'abord les tests, les voir échouer, puis implémenter.

**1. `tests/Chronos.Tests/ArchiveStorePurgeTests.cs`** (nouveau fichier).

```csharp
using System.IO;
using System.Text.Json;
using Chronos.Services;
using Xunit;

namespace Chronos.Tests;

/// <summary>
/// SRC-02 — les entrées fantômes de l'ancienne source app-bureau sont RETIRÉES du fichier d'archives, pas
/// seulement écartées à la lecture.
///
/// Contenu RÉEL relevé le 2026-09-12 dans %APPDATA%\Chronos\archived.json (84 octets, mtime du 12 juillet) :
/// deux entrées « desktop:foreground:… » et rien d'autre. Elles prouvent que l'utilisateur avait dû
/// archiver ces fantômes À LA MAIN : leur horodatage étant rafraîchi à chaque poll, elles ne vieillissaient
/// jamais et ne pouvaient jamais expirer. Les ignorer à la lecture laisse son contournement gravé dans ses
/// données ; les retirer l'efface.
///
/// Tous les tests écrivent dans un fichier TEMPORAIRE : aucun ne touche le vrai %APPDATA%\Chronos.
/// </summary>
public class ArchiveStorePurgeTests
{
    private static string TempFichier()
    {
        var d = Path.Combine(Path.GetTempPath(), "chronos-arch-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(d);
        var f = Path.Combine(d, "archived.json");
        Assert.StartsWith(Path.GetTempPath(), f);
        return f;
    }

    // Le contenu EXACT mesuré sur la machine de l'utilisateur le 2026-09-12.
    private const string ContenuReel =
        """{"desktop:foreground:unknown":1783867136065,"desktop:foreground:code":1783867137990}""";

    [Fact]
    public void Les_deux_fantomes_mesures_sont_retires_du_fichier()
    {
        var f = TempFichier();
        File.WriteAllText(f, ContenuReel);

        var retires = new ArchiveStore(f).PurgerPrefixe("desktop:");

        Assert.Equal(2, retires);
        using var doc = JsonDocument.Parse(File.ReadAllText(f));
        Assert.Equal(0, doc.RootElement.EnumerateObject().Count());   // le fichier est vidé, pas supprimé
        Assert.Empty(new ArchiveStore(f).Load());
    }

    [Fact]
    public void Une_session_Claude_Code_archivee_survit_intacte()
    {
        var f = TempFichier();
        var recent = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds();
        File.WriteAllText(f, $$"""{"e465420e-83e0-428f-97f1-f0174c0848fc":{{recent}},"desktop:foreground:code":1783867137990}""");

        Assert.Equal(1, new ArchiveStore(f).PurgerPrefixe("desktop:"));

        using var doc = JsonDocument.Parse(File.ReadAllText(f));
        Assert.Equal(recent, doc.RootElement.GetProperty("e465420e-83e0-428f-97f1-f0174c0848fc").GetInt64());
        Assert.False(doc.RootElement.TryGetProperty("desktop:foreground:code", out _));
        Assert.Contains("e465420e-83e0-428f-97f1-f0174c0848fc", new ArchiveStore(f).Load());
    }

    [Fact]
    public void Purger_n_est_pas_expirer_une_entree_vieille_reste_dans_le_fichier()
    {
        var f = TempFichier();
        var vieux = DateTimeOffset.UtcNow.AddHours(-7).ToUnixTimeMilliseconds();   // au-delà du TTL de 6 h
        File.WriteAllText(f, $$"""{"une-vieille-session":{{vieux}},"desktop:session:X":1783867137990}""");

        Assert.Equal(1, new ArchiveStore(f).PurgerPrefixe("desktop:"));

        using var doc = JsonDocument.Parse(File.ReadAllText(f));
        Assert.True(doc.RootElement.TryGetProperty("une-vieille-session", out var v));
        Assert.Equal(vieux, v.GetInt64());                              // la purge ne l'a PAS touchée…
        Assert.DoesNotContain("une-vieille-session", new ArchiveStore(f).Load());  // …et Load ne la rend pas
    }

    [Fact]
    public void Sans_fantome_le_fichier_de_l_utilisateur_n_est_pas_reecrit()
    {
        var f = TempFichier();
        File.WriteAllText(f, """{"une-session":1783867137990}""");
        var avant = new DateTime(2026, 7, 12, 10, 0, 0, DateTimeKind.Utc);
        File.SetLastWriteTimeUtc(f, avant);

        Assert.Equal(0, new ArchiveStore(f).PurgerPrefixe("desktop:"));

        Assert.Equal(avant, File.GetLastWriteTimeUtc(f));   // pas touché : rien à faire = ne rien faire
    }

    [Fact]
    public void Fichier_absent_ou_illisible_rend_zero_sans_exception()
    {
        var absent = TempFichier();                       // créé nulle part : seul le dossier existe
        Assert.Equal(0, new ArchiveStore(absent).PurgerPrefixe("desktop:"));

        var corrompu = TempFichier();
        File.WriteAllText(corrompu, "ceci n'est pas du JSON {{{");
        Assert.Equal(0, new ArchiveStore(corrompu).PurgerPrefixe("desktop:"));

        var tableau = TempFichier();
        File.WriteAllText(tableau, "[1,2,3]");            // JSON valide, mais pas un objet
        Assert.Equal(0, new ArchiveStore(tableau).PurgerPrefixe("desktop:"));
    }

    [Fact]
    public void Un_prefixe_vide_ne_purge_rien()
    {
        // Garde-fou : un préfixe vide correspondrait à TOUTES les entrées et viderait le fichier de
        // l'utilisateur. Ce n'est jamais une intention légitime.
        var f = TempFichier();
        File.WriteAllText(f, """{"une-session":1783867137990}""");

        Assert.Equal(0, new ArchiveStore(f).PurgerPrefixe(""));
        Assert.Contains("une-session", File.ReadAllText(f));
    }
}
```

**2. `src/Chronos/Services/ArchiveStore.cs`** — ajouter la méthode APRÈS `Add`, sans toucher à `Load` ni
à `Add` :

```csharp
    /// <summary>
    /// SRC-02 (phase 21) — retire DU FICHIER toute entrée dont l'identifiant commence par
    /// <paramref name="prefixe"/>, et rend le nombre d'entrées effectivement retirées ET écrites.
    ///
    /// <para>Distinct de <see cref="Load"/>, qui se contente d'ÉCARTER à la lecture. Écarter n'est pas
    /// retirer : les deux entrées de l'ancienne source app-bureau relevées le 2026-09-12 dans
    /// archived.json prouvent que l'utilisateur avait dû archiver ces fantômes à la main, faute qu'ils
    /// puissent vieillir. Tant qu'elles restent dans le fichier, son contournement reste gravé dans ses
    /// données.</para>
    ///
    /// <para>Distinct aussi d'<see cref="Add"/>, qui applique au passage la purge des entrées expirées :
    /// ici, AUCUN filtre de TTL. Purger un préfixe et expirer une entrée sont deux gestes différents ;
    /// les confondre ferait disparaître des archives que l'utilisateur n'a pas demandé de retirer.</para>
    ///
    /// <para>Rien à retirer → le fichier n'est PAS réécrit (ni date, ni contenu). Écriture DIRECTE et non
    /// par fichier temporaire : geste unique au démarrage, sans lecteur concurrent (l'écriture par
    /// tmp + Move a été mesurée à 200 échecs sur 500 contre un lecteur, la directe à 0 sur 500).</para>
    ///
    /// <para>Le nombre rendu n'est pas une intention mais une OBSERVATION : si l'écriture échoue, la
    /// méthode rend 0, parce que rien n'a été retiré.</para>
    /// </summary>
    public int PurgerPrefixe(string prefixe)
    {
        if (string.IsNullOrEmpty(prefixe)) return 0;   // un préfixe vide viderait tout : jamais légitime

        var restantes = new Dictionary<string, long>();
        var retirees = 0;
        var ecrit = false;
        try
        {
            if (!File.Exists(_path)) return 0;
            using (var doc = JsonDocument.Parse(File.ReadAllText(_path)))
            {
                if (doc.RootElement.ValueKind != JsonValueKind.Object) return 0;
                foreach (var p in doc.RootElement.EnumerateObject())
                {
                    if (!p.Value.TryGetInt64(out var ts)) continue;   // entrée illisible : ignorée, non conservée
                    if (p.Name.StartsWith(prefixe, System.StringComparison.Ordinal)) { retirees++; continue; }
                    restantes[p.Name] = ts;                           // conservée TELLE QUELLE, sans TTL
                }
            }
            if (retirees == 0) return 0;                              // rien à faire = ne rien écrire

            Directory.CreateDirectory(Path.GetDirectoryName(_path)!);
            File.WriteAllText(_path, JsonSerializer.Serialize(restantes));
            ecrit = true;
        }
        catch { }
        return ecrit ? retirees : 0;
    }
```
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~ArchiveStorePurgeTests|FullyQualifiedName~SessionsTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public int PurgerPrefixe(string prefixe)" src/Chronos/Services/ArchiveStore.cs` rend **1**
    - `grep -c "Ttl" src/Chronos/Services/ArchiveStore.cs` rend **3** — inchangé : la constante + ses deux
      usages dans `Load` et `Add`. `PurgerPrefixe` n'en fait AUCUN usage
    - `grep -c "File.Move" src/Chronos/Services/ArchiveStore.cs` rend **1** — `Add` conserve son écriture
      actuelle ; la corriger est CYC-02, phase 23
    - `grep -c "ecrit ? retirees : 0" src/Chronos/Services/ArchiveStore.cs` rend **1** — le nombre rendu est
      une observation, pas une intention
    - `grep -c "desktop:foreground:unknown" tests/Chronos.Tests/ArchiveStorePurgeTests.cs` rend **1** —
      le contenu RÉEL mesuré est rejoué, pas un cas inventé
    - Les 6 tests d'`ArchiveStorePurgeTests` passent
    - Les tests préexistants de `SessionsTests` passent sans retouche :
      `git diff --name-only` ne contient PAS `tests/Chronos.Tests/SessionsTests.cs`
  </acceptance_criteria>

  <done>
`ArchiveStore` sait retirer des entrées par préfixe, sans expirer au passage, sans réécrire pour rien, et
sans annoncer un retrait qui n'a pas eu lieu.
  </done>
</task>

<task type="auto">
  <name>Task 2 : la purge s'exécute au démarrage, et une garde prouve qu'elle est câblée</name>
  <files>src/Chronos/App.xaml.cs, tests/Chronos.Tests/GardesPerimetreTests.cs</files>

  <read_first>
    - src/Chronos/App.xaml.cs (l. 60-106 — `OnStartup` en mode overlay ; le bloc `ClaudeSettingsReconciler`
      l. 86-97 et son commentaire, qui explique POURQUOI ce point du démarrage est le bon : les modes
      `--hook` et `--statusline` sortent bien plus haut et ne doivent jamais l'atteindre)
    - src/Chronos/Services/ArchiveStore.cs (la méthode créée en tâche 1)
    - tests/Chronos.Tests/GardesPerimetreTests.cs (créé au plan 03 — l'aide `CheminSources()` y est déjà)
  </read_first>

  <action>
**1. `src/Chronos/App.xaml.cs`** — insérer, JUSTE APRÈS le `catch { }` du bloc `ClaudeSettingsReconciler`
et AVANT `OfferOnFirstRun()` :

```csharp
        // SRC-02 — les identifiants synthétiques de l'ancienne source app-bureau (préfixe « desktop: »)
        // sont retirés DU FICHIER archived.json, pas seulement ignorés à la lecture. Mesuré le 2026-09-12 :
        // archived.json ne contenait QUE deux de ces entrées, datées de juillet. Elles prouvent que
        // l'utilisateur avait dû les archiver À LA MAIN — leur horodatage était rafraîchi à chaque poll,
        // donc elles ne vieillissaient jamais et ne pouvaient jamais expirer. Les laisser dans le fichier,
        // c'est laisser son contournement gravé dans ses données.
        //
        // Même régime que la réconciliation ci-dessus : mode OVERLAY uniquement (les modes --hook et
        // --statusline sortent bien plus haut, l. 20 et 30), best-effort et silencieux — ne peut pas
        // empêcher le démarrage. Idempotent : une fois le fichier propre, l'appel suivant ne réécrit rien.
        try { _host.Services.GetRequiredService<ArchiveStore>().PurgerPrefixe("desktop:"); }
        catch { }
```

**Ne rien changer d'autre** dans `OnStartup` : ni l'ordre des appels qui suivent, ni `ShowIfEnabled`.

**2. `tests/Chronos.Tests/GardesPerimetreTests.cs`** — ajouter la garde de câblage. Une méthode publique
non appelée ne purge rien, et cela ne se verrait qu'à l'usage, sur la machine de l'utilisateur :

```csharp
    /// <summary>
    /// GARDE DE CÂBLAGE (SRC-02). <c>PurgerPrefixe</c> peut être parfaitement testée et n'être jamais
    /// appelée : le défaut ne se verrait alors que chez l'utilisateur, sur ses propres données, et
    /// silencieusement. Le démarrage en mode overlay doit invoquer la purge du préfixe de l'ancienne
    /// source app-bureau. Contrôle de SOURCE : <c>OnStartup</c> monte un host WPF complet, il n'est pas
    /// instanciable sous test sans lancer l'application — ce que la phase interdit.
    /// </summary>
    [Fact]
    public void Le_demarrage_purge_les_identifiants_fantomes_du_magasin_d_archives()
    {
        var fichier = Path.Combine(CheminSources(), "App.xaml.cs");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        // Une garde qui lirait un fichier vide serait muette.
        Assert.Contains("OnStartup", texte, StringComparison.Ordinal);
        Assert.Contains("PurgerPrefixe(\"desktop:\")", texte, StringComparison.Ordinal);
    }
```
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~GardesPerimetreTests|FullyQualifiedName~CompositionRootTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'PurgerPrefixe("desktop:")' src/Chronos/App.xaml.cs` rend **1** — un seul point d'appel
    - `grep -n 'PurgerPrefixe' src/Chronos/App.xaml.cs` renvoie une ligne située APRÈS celle de
      `ClaudeSettingsReconciler` et AVANT celle de `OfferOnFirstRun` (ordre vérifiable à l'œil sur la sortie)
    - `grep -c "ShowIfEnabled" src/Chronos/App.xaml.cs` rend **1** — l'ordre de fin de démarrage est intact
    - `grep -c "Le_demarrage_purge_les_identifiants_fantomes_du_magasin_d_archives" tests/Chronos.Tests/GardesPerimetreTests.cs` rend **1**
    - `dotnet build Chronos.sln -c Debug` réussit
    - Les gardes `CompositionRootTests` et `GardesPerimetreTests` sont vertes
  </acceptance_criteria>

  <done>
Au prochain lancement de l'overlay, les deux entrées de juillet disparaissent du fichier de l'utilisateur
sans qu'il ait à toucher quoi que ce soit — et une garde échoue si l'appel disparaît.
  </done>
</task>

<task type="auto">
  <name>Task 3 : le bilan nominatif du recul de couverture — le livrable de vérification de la phase</name>
  <files>.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-VALIDATION.md</files>

  <read_first>
    - .planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-VALIDATION.md
      (INTÉGRALEMENT — la carte de vérification par tâche et le tableau « Bilan nominatif » à remplir)
    - .planning/phases/21-…/21-02-SUMMARY.md et 21-03-SUMMARY.md (les listes nominatives déjà consignées)
    - .planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-VERIFICATION.md
      (le PRÉCÉDENT : la phase 16 du milestone v1.5 a déjà fait accepter un recul de couverture justifié)
  </read_first>

  <action>
Cette phase a une contrainte de vérification renversée : **le recul du nombre de tests est le livrable**,
pas une régression. Le critère opérationnel n'est PAS « ≥ 752 » mais **0 échec + justification NOMINATIVE
de chaque test supprimé**. Ce plan produit cette justification.

**1. Mesurer, deux fois.**

```sh
dotnet test Chronos.sln -c Debug --nologo -v q
dotnet test Chronos.sln -c Debug --nologo -v q     # deux passes : le BAML impose la seconde
```

**2. Remplir le tableau « Bilan nominatif du recul de couverture » de `21-VALIDATION.md`**, une ligne par
test supprimé, avec pour chacun **le code disparu qu'il couvrait**. Les 49 suppressions attendues :

| Origine | Nombre | Ce qu'ils couvraient, qui n'existe plus |
|---|---|---|
| `DesktopUiaSessionSourceTests.cs` (fichier entier) | 36 | `DesktopUiaSessionSource`, `UiaLabels`, `UiaNode`, `DesktopHealth` |
| `DesktopUiaPollServiceTests.cs` (fichier entier) | 4 | `DesktopUiaPollService` |
| `SessionsTests.cs` — 5 tests nommés dans le SUMMARY 21-02 | 5 | l'étape de repli app-bureau de `SessionMonitor.Read`, le libellé de type |
| `TreatedSessionsTests.cs` — 4 tests `NET02_*` | 4 | la branche d'acquittement par focus de `SessionTreatmentTracker` |

Pour les deux fichiers entiers, l'énumération nominative des 40 méthodes est obtenue par
`git show HEAD~N:tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs | grep "public .*()"`. La reporter
telle quelle : « justification nominative » veut dire que chaque nom figure, pas qu'un fichier est cité.

**3. Consigner les 16 tests AJOUTÉS** et leur exigence : 4 (SRC-03, plan 01), 2 + 1 + 2 (gardes et garde
visuelle, plan 03), 6 + 1 (SRC-02, plan 04).

**4. Vérifier et consigner les invariants de sécurité** — ils font partie du livrable :

```sh
ls "$APPDATA/Chronos/sessions" | wc -l        # doit rendre 66 (54 .json + 12 .tmp) — INCHANGÉ
ls -l "$APPDATA/Chronos/oauth.dat"            # doit rendre 518 octets, mtime 1783863147 — INCHANGÉ
```

L'overlay `Chronos-v3.0.2.exe` (pid 119412) **n'a été ni lancé ni tué**. Aucune requête réseau n'a été
émise depuis un test.

**5. Ouvrir la section « À VÉRIFIER PAR L'UTILISATEUR »** de `21-VALIDATION.md`, qui remplace les
checkpoints que la contrainte de phase interdit :

- Au prochain **redémarrage volontaire** de l'overlay : `%APPDATA%\Chronos\archived.json` doit être passé
  de 84 octets à `{}` (2 octets). C'est la preuve in vivo de SRC-02, et elle n'est observable qu'après un
  lancement.
- Galerie `--sessions` : parcourir les 8 styles. Un élément Pastilles doit lire `état · détail`, sans point
  médian en fin de ligne.
- Après quelques heures d'usage avec l'app bureau Claude ouverte au premier plan : plus aucune ligne
  préfixée `desktop:` dans le widget, et plus rien à archiver à la main (critère de succès 1).
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, sur **deux exécutions consécutives**
    - Total attendu : **719** (752 − 49 + 16). Un écart n'est pas à corriger en ajustant le chiffre : il
      doit être expliqué ligne à ligne dans le bilan
    - `grep -c "^|" .planning/phases/21-…/21-VALIDATION.md` : le tableau du bilan contient **au moins 49
      lignes de test supprimé**, chacune portant un nom de méthode, pas un nom de fichier seul
    - Les 5 gardes nommées de la phase sont vertes : `ServicesLayerPurityTests`, `CompositionRootTests`,
      `NormalisationUniqueTests`, `GardesDoctrineTests`,
      `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte`
    - `ls "$APPDATA/Chronos/sessions" | wc -l` rend **66** — aucune donnée utilisateur touchée
    - `git status --porcelain` ne montre AUCUNE modification hors `src/`, `tests/` et `.planning/`
  </acceptance_criteria>

  <done>
La phase peut être vérifiée sur son vrai critère : zéro échec, et chacun des 49 tests disparus nommé, avec
le code supprimé qu'il couvrait.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -c Debug --nologo -v q` **deux fois** → **0 échec**, **719** tests.
2. `grep -c 'PurgerPrefixe("desktop:")' src/Chronos/App.xaml.cs` rend **1**.
3. `21-VALIDATION.md` porte les 49 noms de tests supprimés et les 16 ajoutés.
4. `%APPDATA%\Chronos\` : `sessions/` toujours à 66 entrées, `oauth.dat` toujours 518 octets,
   `archived.json` **inchangé** (la purge ne s'exécute qu'au prochain lancement volontaire de l'overlay,
   par l'utilisateur — aucun test n'écrit dans le vrai profil).
</verification>

<success_criteria>
- SRC-02 clos : les entrées fantômes sont retirées du fichier, pas seulement ignorées ; une session
  réellement archivée et une entrée simplement expirée survivent toutes deux à la purge.
- La purge est câblée au démarrage et gardée contre le décâblage.
- Le recul de couverture est justifié nominativement, test par test.
- Aucune donnée de l'utilisateur modifiée, aucune instance de l'overlay lancée ou tuée.
</success_criteria>

<output>
Créer `.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-04-SUMMARY.md`, et mettre à
jour `.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-VALIDATION.md` : bilan
nominatif rempli + section « À VÉRIFIER PAR L'UTILISATEUR ».
</output>
