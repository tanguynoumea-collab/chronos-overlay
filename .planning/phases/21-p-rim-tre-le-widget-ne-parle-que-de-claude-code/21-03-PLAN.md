---
phase: 21-p-rim-tre-le-widget-ne-parle-que-de-claude-code
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/Chronos/Services/DesktopUiaSessionSource.cs
  - src/Chronos/Services/WindowsUiaTreeProvider.cs
  - src/Chronos/Services/UiaLabels.cs
  - src/Chronos/Services/DesktopUiaPollService.cs
  - src/Chronos/Services/WindowsForegroundWatch.cs
  - src/Chronos/Services/UiaNode.cs
  - src/Chronos/Services/IForegroundWatch.cs
  - src/Chronos/Services/IUiaTreeProvider.cs
  - src/Chronos/Services/SessionSnapshot.cs
  - src/Chronos/ViewModels/SessionsViewModel.cs
  - src/Chronos/ViewModels/SessionsPreviewViewModel.cs
  - src/Chronos/Resources/SessionStyles.xaml
  - src/Chronos/Views/SessionsController.cs
  - tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs
  - tests/Chronos.Tests/DesktopUiaPollServiceTests.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
  - tests/Chronos.Tests/SessionStylesBindingTests.cs
autonomous: true
requirements: [SRC-01]

must_haves:
  truths:
    - "Les 765 lignes de la source app-bureau et de l'observateur de focus ne sont plus dans le dépôt"
    - "Un instantané de session porte cinq champs : plus de typologie d'app bureau à inventer"
    - "Le widget n'annonce plus à l'utilisateur qu'il couvre l'app bureau — parce qu'il ne la couvre plus"
    - "Le retrait du libellé de type ne laisse ni case vide ni séparateur orphelin dans aucun des 8 styles"
    - "Une garde échoue si un type de la source app-bureau ou de la typologie réapparaît dans l'assembly"
  artifacts:
    - path: "src/Chronos/Services/SessionSnapshot.cs"
      provides: "Instantané NEUTRE à 5 champs, sans typologie ni origine"
      contains: "public sealed record SessionSnapshot"
      max_lines: 22
    - path: "tests/Chronos.Tests/GardesPerimetreTests.cs"
      provides: "Garde de non-retour par réflexion sur l'assembly + contrôle de source sur le XAML"
      contains: "class GardesPerimetreTests"
    - path: "tests/Chronos.Tests/SessionStylesBindingTests.cs"
      provides: "Les 8 styles × 9 thèmes chargés, mesurés et disposés sans exception ni taille dégénérée"
      contains: "class SessionStylesBindingTests"
  key_links:
    - from: "src/Chronos/Resources/SessionStyles.xaml"
      to: "le style Pastilles"
      via: "un seul séparateur visible par élément, plus aucun binding de libellé de type"
      pattern: "Text=\"  ·  \""
    - from: "tests/Chronos.Tests/SessionStylesBindingTests.cs"
      to: "ThemeCatalog.All × SessionStyle"
      via: "double boucle sur le catalogue, jamais sur une liste en dur"
      pattern: "ThemeCatalog\\.All"
---

<objective>
SRC-01, second des deux gestes : **supprimer**. Le plan 02 a rendu les huit fichiers orphelins ; ici on les
retire du dépôt, puis on retire ce qui n'existait que pour eux — la typologie d'app bureau portée par
`SessionSnapshot`, son libellé dans le widget, et la promesse faite à l'utilisateur que le widget « couvre
l'app bureau ».

**Correction MESURÉE du contexte de phase.** Le CONTEXT annonce que « les 8 styles bindent le libellé de
type ». C'est faux : `grep -c "KindLabel" src/Chronos/Resources/SessionStyles.xaml` rend **3**, et les trois
occurrences sont dans le SEUL template `TplPastilles` (l. 70-79). Les sept autres styles ne bindent rien de
tel. La modification visuelle est donc confinée à un template — mais la **vérification**, elle, reste sur
les 8 styles × 9 thèmes, parce que c'est la seule façon de prouver qu'on n'a rien décalé ailleurs.

Purpose : après ce plan, le dépôt ne contient plus une ligne qui prétende savoir ce que fait l'app bureau
Claude. Et une garde par réflexion rend le retour en arrière détectable au build, pas au démarrage.

Output : 10 fichiers supprimés (765 lignes de `src/`, 529 de tests, 40 tests), un `SessionSnapshot` à 5
champs, deux nouvelles gardes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-CONTEXT.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-02-SUMMARY.md

<interfaces>
<!-- Contrats existants, extraits du dépôt le 2026-09-12. NE PAS explorer le code pour les retrouver. -->

```csharp
// src/Chronos/Services/SessionSnapshot.cs — état APRÈS le plan 02, état VOULU après la tâche 2
// (les enums SessionActivity reste ; SessionKind et SessionOrigin disparaissent) :
public enum SessionActivity { Working, WaitingAttention, WaitingTurn, Unknown }   // ← RESTE tel quel
public sealed record SessionSnapshot(
    string SessionId, string Project, SessionActivity Activity, string? Reason,
    System.DateTimeOffset UpdatedAt);                                            // ← 5 champs
```

```csharp
// src/Chronos/ViewModels/SessionsViewModel.cs — les 3 sites du libellé de type
l. 24   [ObservableProperty] private string _kindLabel = "";
l. 151  it.KindLabel = KindText(s.Kind);
l. 170-178  private static string KindText(SessionKind k) => k switch { … };
// TOUT le reste de SessionItemVm reste : Project, StateText, Detail, StateBrush, IsWaiting,
// IsAttention, IsTurn, IsWorking, IsGhost, ArchiveCommand.
```

```xml
<!-- src/Chronos/Resources/SessionStyles.xaml — l. 66-80, le SEUL endroit du fichier concerné.
     Les lignes 70 à 79 (le second séparateur + le libellé de type, avec leurs deux Style/Trigger)
     disparaissent. La l. 68 (premier séparateur, entre StateText et Detail) RESTE. -->
<StackPanel Orientation="Horizontal" Margin="0,1,0,0">
    <TextBlock Text="{Binding StateText}" .../>
    <TextBlock Text="  ·  " .../>                    <!-- l. 68 — RESTE -->
    <TextBlock Text="{Binding Detail}" .../>
    <TextBlock Text="  ·  " ...>  …trigger…          <!-- l. 70-74 — SUPPRIMÉ -->
    <TextBlock Text="{Binding KindLabel}" ...>  …trigger…  <!-- l. 75-79 — SUPPRIMÉ -->
</StackPanel>
```

Mesures de référence du fichier XAML AVANT modification (à retrouver après) :
```
grep -c 'DataTemplate x:Key='      → 8      (inchangé après)
grep -c 'Text="  ·  "'             → 3      (2 après : l. 68 Pastilles + l. 113 Marge)
grep -c 'KindLabel'                → 3      (0 après)
```

```csharp
// tests/Chronos.Tests/GardesDoctrineTests.cs — le MOTIF de garde structurelle à recopier.
// Le chemin des sources est INJECTÉ par MSBuild (attribut AssemblyMetadata posé dans le .csproj de
// tests, l. 25-28) et jamais deviné : Assembly.Location est VIDE en mono-fichier.
private static string CheminSources()
    => typeof(GardesDoctrineTests).Assembly
           .GetCustomAttributes<AssemblyMetadataAttribute>()
           .FirstOrDefault(a => a.Key == "CheminSourcesChronos")?.Value ?? "";

// tests/Chronos.Tests/ServicesLayerPurityTests.cs — le MOTIF de garde par RÉFLEXION à recopier :
var asm = typeof(Chronos.Services.IUsageProvider).Assembly;
var revenants = asm.GetTypes().Where(t => t.Name.StartsWith("Budget", StringComparison.Ordinal))…
Assert.Empty(revenants);
```

```csharp
// Pour la garde visuelle (tâche 3) — tout existe déjà :
[CollectionDefinition("XAML WPF", DisableParallelization = true)]   // tests/Chronos.Tests/XamlWpfCollection.cs
public SessionsWindow(SessionsViewModel vm)                         // src/Chronos/Views/SessionsWindow.xaml.cs
public void ApplyThemeBrushes(Chronos.Theming.ChronosTheme theme)   // idem, l. 27
public static readonly IReadOnlyList<ChronosTheme> All              // Chronos.Theming.ThemeCatalog — 9 thèmes
public enum SessionStyle { Pastilles, Marge, Jetons, Sonar, Facade, Etagere, Annonciateur, Veilleurs }  // Chronos.Services (ChronosSettings.cs l. 22)
public SessionsViewModel(SessionMonitor monitor, IClock clock, ArchiveStore archive)
public void Refresh(System.DateTimeOffset now)                      // recalcule Items, pur hors I/O du monitor
[ObservableProperty] private SessionStyle _style;                   // pilote IsStyleX → sélection du template
internal sealed class FakeClock : IClock { public DateTimeOffset UtcNow { get; set; } … }  // tests/Chronos.Tests/Fakes/FakeClock.cs
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : les dix fichiers quittent le dépôt, et une garde par réflexion interdit leur retour</name>
  <files>src/Chronos/Services/DesktopUiaSessionSource.cs, src/Chronos/Services/WindowsUiaTreeProvider.cs, src/Chronos/Services/UiaLabels.cs, src/Chronos/Services/DesktopUiaPollService.cs, src/Chronos/Services/WindowsForegroundWatch.cs, src/Chronos/Services/UiaNode.cs, src/Chronos/Services/IForegroundWatch.cs, src/Chronos/Services/IUiaTreeProvider.cs, tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs, tests/Chronos.Tests/DesktopUiaPollServiceTests.cs, tests/Chronos.Tests/GardesPerimetreTests.cs</files>

  <read_first>
    - .planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-02-SUMMARY.md (pour confirmer
      que le débranchement est bien acquis avant de supprimer quoi que ce soit)
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (INTÉGRALEMENT, 80 lignes — le motif de garde par
      réflexion `Aucun_type_de_plafond_ne_subsiste_dans_l_assembly`, précédent exact de phase 16)
    - tests/Chronos.Tests/GardesDoctrineTests.cs (l. 390-403 — l'aide `CheminSources()` et son commentaire)
    - tests/Chronos.Tests/Chronos.Tests.csproj (l. 20-29 — l'attribut MSBuild qui injecte le chemin)
  </read_first>

  <action>
**1. Vérifier d'abord, supprimer ensuite.** Avant toute suppression, confirmer que le plan 02 a bien
débranché : la commande suivante ne doit rendre QUE les dix fichiers concernés, et aucun autre.

```sh
grep -rln "DesktopUiaSessionSource\|DesktopUiaPollService\|IUiaTreeProvider\|WindowsUiaTreeProvider\|UiaLabels\|UiaNode\|IForegroundWatch\|WindowsForegroundWatch\|DesktopHealth" src tests
```

Si un onzième fichier apparaît, **s'arrêter et l'adapter avant de supprimer** — une suppression avec un
site d'appel restant casse la compilation de TOUTE la solution de tests.

**2. Supprimer les dix fichiers** (`git rm`), 765 lignes de `src/` + 529 lignes de tests mesurées le
2026-09-12 :

| Fichier | Lignes |
|---|---|
| `src/Chronos/Services/DesktopUiaSessionSource.cs` | 276 |
| `src/Chronos/Services/WindowsUiaTreeProvider.cs` | 198 |
| `src/Chronos/Services/UiaLabels.cs` | 93 |
| `src/Chronos/Services/DesktopUiaPollService.cs` | 86 |
| `src/Chronos/Services/WindowsForegroundWatch.cs` | 59 |
| `src/Chronos/Services/UiaNode.cs` | 24 |
| `src/Chronos/Services/IForegroundWatch.cs` | 16 |
| `src/Chronos/Services/IUiaTreeProvider.cs` | 13 |
| `tests/Chronos.Tests/DesktopUiaSessionSourceTests.cs` | 423 — **36 tests** |
| `tests/Chronos.Tests/DesktopUiaPollServiceTests.cs` | 106 — **4 tests** |

Les 40 tests supprimés sont justifiés nominativement : ils instancient exclusivement
`DesktopUiaSessionSource` / `DesktopUiaPollService` et assertent sur `DesktopHealth`, trois types qui
n'existent plus. Aucun d'eux ne couvre une ligne survivante.

**3. Créer `tests/Chronos.Tests/GardesPerimetreTests.cs`** — la garde de non-retour.

```csharp
using System.IO;
using System.Reflection;
using Xunit;

namespace Chronos.Tests;

/// <summary>
/// GARDE DE NON-RETOUR de la phase 21 (SRC-01). Le widget de sessions ne parle que de Claude Code : la
/// source app-bureau par UI Automation, son poll de fond et l'observateur de focus OS ont été retirés.
///
/// La garde est par RÉFLEXION sur l'assembly, comme le précédent
/// <c>Aucun_type_de_plafond_ne_subsiste_dans_l_assembly</c> (phase 16) : elle attrape un rétablissement
/// depuis l'historique, une réimplémentation sous un autre fichier, ou un copier-coller — trois voies
/// qu'une recherche textuelle sur un chemin de fichier manquerait.
///
/// Pourquoi cette suppression n'est PAS du ménage opportuniste : l'acquittement par focus exigeait une
/// session d'origine « app bureau » ET un identifiant synthétique de cette source. Une session Claude Code
/// a une origine ligne de commande et un UUID — le mécanisme ne l'atteignait jamais, et le sondage de
/// focus OS était payé à chaque tick pour un résultat jamais lu.
///
/// Ces tests ne lisent que l'assembly et des fichiers du dépôt : aucun réseau, aucun %APPDATA%, aucun jeton.
/// </summary>
public class GardesPerimetreTests
{
    [Fact]
    public void Aucun_type_de_la_source_app_bureau_ne_subsiste_dans_l_assembly()
    {
        var asm = typeof(Chronos.Services.IUsageProvider).Assembly;

        var revenants = asm.GetTypes()
            .Where(t => t.Name.Contains("Uia", StringComparison.Ordinal)
                     || t.Name.EndsWith("ForegroundWatch", StringComparison.Ordinal)
                     || t.Name == "DesktopHealth")
            .Select(t => t.FullName)
            .ToList();

        Assert.True(revenants.Count == 0,
            "La source app-bureau par UI Automation a été retirée en phase 21 (SRC-01) : le widget ne "
            + "montre que des sessions Claude Code. Ces types ne doivent pas revenir.\n  "
            + string.Join("\n  ", revenants!));
    }

    // Une garde qui ne verrait AUCUN type serait muette (assembly mal résolu, réflexion cassée).
    [Fact]
    public void La_garde_voit_bien_l_assembly_Chronos()
    {
        var asm = typeof(Chronos.Services.IUsageProvider).Assembly;
        Assert.True(asm.GetTypes().Length >= 50, "réflexion muette : l'assembly Chronos n'est pas résolu");
        Assert.Contains(asm.GetTypes(), t => t.Name == "SessionMonitor");
    }

    /// <summary>Le chemin des sources est INJECTÉ par MSBuild, jamais deviné (Assembly.Location est VIDE
    /// en publication mono-fichier). Motif recopié de <c>GardesDoctrineTests</c>.</summary>
    internal static string CheminSources()
        => typeof(GardesPerimetreTests).Assembly
               .GetCustomAttributes<AssemblyMetadataAttribute>()
               .FirstOrDefault(a => a.Key == "CheminSourcesChronos")?.Value
           ?? "";
}
```

Ce fichier de test **cite** les noms supprimés dans des littéraux : c'est normal et voulu, une garde doit
nommer ce qu'elle interdit. Les critères de zéro-occurrence de ce plan portent tous sur `src/`, jamais sur
`tests/`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - Les 8 fichiers de `src/Chronos/Services/` listés ci-dessus n'existent plus :
      `ls src/Chronos/Services/ | grep -ci "uia\|foregroundwatch"` rend **0**
    - Les 2 fichiers de tests n'existent plus :
      `ls tests/Chronos.Tests/ | grep -c "DesktopUia"` rend **0**
    - `grep -rn "DesktopUia\|IUiaTreeProvider\|WindowsUia\|UiaLabels\|UiaNode\|IForegroundWatch\|WindowsForegroundWatch\|DesktopHealth" --include=*.cs --include=*.xaml src | wc -l` rend **0**
    - `grep -c "class GardesPerimetreTests" tests/Chronos.Tests/GardesPerimetreTests.cs` rend **1**
    - `dotnet build Chronos.sln -c Debug` réussit
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**. Total attendu : **709**
      (747 − 40 supprimés + 2 nouvelles gardes). Un écart est un signal à instruire
  </acceptance_criteria>

  <done>
765 lignes de `src/` et 529 de tests ont quitté le dépôt, chaque test supprimé est justifié par la
disparition du type qu'il instanciait, et un retour en arrière ferait rougir une garde au build.
  </done>
</task>

<task type="auto">
  <name>Task 2 : un instantané de session redevient cinq champs, et le widget cesse de promettre l'app bureau</name>
  <files>src/Chronos/Services/SessionSnapshot.cs, src/Chronos/ViewModels/SessionsViewModel.cs, src/Chronos/ViewModels/SessionsPreviewViewModel.cs, src/Chronos/Resources/SessionStyles.xaml, src/Chronos/Views/SessionsController.cs, tests/Chronos.Tests/GardesPerimetreTests.cs</files>

  <read_first>
    - src/Chronos/Services/SessionSnapshot.cs (INTÉGRALEMENT, 39 lignes — les deux enums de typologie et
      le commentaire « AJOUTÉS EN FIN avec valeurs par défaut » qui explique pourquoi ils étaient optionnels)
    - src/Chronos/ViewModels/SessionsViewModel.cs (l. 14-42 `SessionItemVm`, l. 133-160 `Refresh`,
      l. 170-178 `KindText`)
    - src/Chronos/ViewModels/SessionsPreviewViewModel.cs (INTÉGRALEMENT, 59 lignes — la galerie
      `--sessions` ; le paramètre `kind` traverse `Add` jusqu'aux items d'échantillon)
    - src/Chronos/Resources/SessionStyles.xaml (l. 45-88 — le template `TplPastilles` EN ENTIER, pour
      voir exactement quelle `StackPanel` horizontale est touchée)
    - src/Chronos/Views/SessionsController.cs (l. 40-56 — le message d'activation du widget)
  </read_first>

  <action>
**1. `src/Chronos/Services/SessionSnapshot.cs`**

Supprimer les deux enums de typologie et d'origine ainsi que les deux paramètres correspondants du record.
Le fichier doit se réduire à `SessionActivity` (inchangé) + un record à 5 champs :

```csharp
namespace Chronos.Services;

/// <summary>État d'activité d'une session Claude Code, du point de vue « ai-je quelque chose à faire ? ».</summary>
public enum SessionActivity
{
    Working,            // l'assistant travaille (prompt soumis, session démarrée)
    WaitingAttention,   // réclame une intervention MAINTENANT (permission, question, inactif)
    WaitingTurn,        // a fini son tour, attend ton prochain message
    Unknown,            // signal périmé / indéterminé (jamais présenté comme « en attente »)
}

/// <summary>
/// Instantané NEUTRE d'une session Claude Code (pour l'UI). Aucun type WPF.
///
/// <para>Phase 21 (SRC-01) — le record est revenu à cinq champs. Les deux champs de typologie ajoutés en
/// phase 13 (quel mode de l'app bureau héberge la session, et de quelle surface elle vient) n'avaient de
/// producteur que la source app-bureau : sans elle, ils n'auraient plus jamais valu autre chose que leur
/// défaut. Un champ qui ne peut plus varier est un champ qui ment par omission.</para>
/// </summary>
public sealed record SessionSnapshot(
    string SessionId,
    string Project,
    SessionActivity Activity,
    string? Reason,
    System.DateTimeOffset UpdatedAt);
```

**Attention à la cohérence** : ne PAS écrire les noms des deux enums supprimés dans le commentaire — le
critère d'acceptation exige zéro occurrence dans `src/`. Les désigner par leur rôle, comme ci-dessus.

**2. `src/Chronos/ViewModels/SessionsViewModel.cs`**

- Supprimer la propriété observable du libellé de type (l. 24) dans `SessionItemVm`.
- Supprimer la ligne d'affectation dans `Refresh` (l. 151).
- Supprimer la fonction de mapping `KindText` et son commentaire (l. 170-178).
- Ne RIEN toucher d'autre : `Describe`, `Rank`, `Age`, `SetTheme`, les 8 propriétés `IsStyleX`,
  `RowOrientation` restent tels quels.

**3. `src/Chronos/ViewModels/SessionsPreviewViewModel.cs`**

Supprimer le paramètre `kind` de la méthode privée `Add`, l'initialisation correspondante (l. 43), et les
arguments `kind:` des cinq appels (l. 27-31). Les cinq échantillons conservent leurs quatre états
(« à toi », « tour fini », « en cours » ×2, « inconnu ») : la galerie doit rester représentative.

**4. `src/Chronos/Resources/SessionStyles.xaml`**

Supprimer **exactement les lignes 70 à 79** — le second séparateur `<TextBlock Text="  ·  ">` avec son
`Style`/`DataTrigger`, et le `<TextBlock Text="{Binding KindLabel}">` avec le sien. **Ne pas toucher à la
ligne 68** (premier séparateur, entre l'état et le détail) : elle sépare deux textes qui restent tous les
deux affichés. Après modification, la `StackPanel` horizontale de `TplPastilles` contient exactement trois
enfants : état, séparateur, détail.

**Aucun des sept autres templates n'est modifié.** `git diff --numstat` sur ce fichier doit rendre
`0 insertions, 10 deletions`.

**5. `src/Chronos/Views/SessionsController.cs`**

Le message d'activation (l. 47) promet à l'utilisateur : « Il affiche tes sessions Claude Code ACTIVES
(app bureau incluse), détectées via leurs transcripts. » La parenthèse devient fausse. Nouveau texte :

```csharp
                "Widget de sessions activé.\n\nIl affiche tes sessions Claude Code ACTIVES, détectées via\n" +
                "leurs transcripts. Une session apparaît dès qu'elle a de l'activité récente et indique si\n" +
                "elle a fini son tour (elle t'attend) ou travaille encore.",
```

**6. `tests/Chronos.Tests/GardesPerimetreTests.cs`** — étendre la garde à ce qui vient de disparaître.

Ajouter à la garde par réflexion les deux noms de la typologie, en modifiant le prédicat existant :

```csharp
            .Where(t => t.Name.Contains("Uia", StringComparison.Ordinal)
                     || t.Name.EndsWith("ForegroundWatch", StringComparison.Ordinal)
                     || t.Name == "DesktopHealth"
                     || t.Name == "SessionKind"
                     || t.Name == "SessionOrigin")
```

Et ajouter un test de source pour le XAML — la réflexion ne voit pas un binding :

```csharp
    /// <summary>
    /// Le libellé de type de session (Chat / Code / Cowork) était bindé dans le SEUL template Pastilles —
    /// mesuré : 3 occurrences, toutes entre les lignes 70 et 79 de SessionStyles.xaml, contrairement à ce
    /// qu'annonçait le contexte de phase. Son producteur a disparu avec la source app-bureau : un binding
    /// survivant afficherait une case vide ou décalerait la rangée. La réflexion ne voit pas un binding XAML,
    /// d'où ce contrôle de SOURCE.
    /// </summary>
    [Fact]
    public void Aucun_style_de_session_ne_binde_plus_un_libelle_de_type()
    {
        var fichier = Path.Combine(CheminSources(), "Resources", "SessionStyles.xaml");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        // Une garde qui lirait un fichier vide serait muette : on exige les 8 templates.
        var templates = System.Text.RegularExpressions.Regex.Matches(texte, "DataTemplate x:Key=").Count;
        Assert.Equal(8, templates);

        Assert.DoesNotContain("KindLabel", texte, StringComparison.Ordinal);
    }
```
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "SessionKind\|SessionOrigin\|KindLabel\|KindText" --include=*.cs --include=*.xaml src | wc -l` rend **0**
    - `wc -l < src/Chronos/Services/SessionSnapshot.cs` rend **au plus 22**
    - `grep -c "record SessionSnapshot" src/Chronos/Services/SessionSnapshot.cs` rend **1** et
      `grep -c "System.DateTimeOffset UpdatedAt);" src/Chronos/Services/SessionSnapshot.cs` rend **1**
      (le record se termine bien sur le 5e champ)
    - `grep -c "enum SessionActivity" src/Chronos/Services/SessionSnapshot.cs` rend **1** — cet enum SURVIT
    - `grep -c 'Text="  ·  "' src/Chronos/Resources/SessionStyles.xaml` rend **2** (Pastilles l. 68 +
      Marge) ; il en restait **3** avant
    - `grep -c 'DataTemplate x:Key=' src/Chronos/Resources/SessionStyles.xaml` rend **8** — aucun template
      n'a été perdu
    - `git diff --numstat src/Chronos/Resources/SessionStyles.xaml` rend `0	10` — zéro ajout, dix
      suppressions, donc aucun des sept autres styles touché
    - `grep -ci "app bureau incluse" src/Chronos/Views/SessionsController.cs` rend **0**
    - `grep -c "Claude Code ACTIVES" src/Chronos/Views/SessionsController.cs` rend **1**
    - `grep -c "SessionKind" tests/Chronos.Tests/GardesPerimetreTests.cs` rend **1** (le littéral de la
      garde — c'est dans `tests/`, hors périmètre des critères de zéro-occurrence)
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, **710** tests (709 + 1 garde XAML)
  </acceptance_criteria>

  <done>
`SessionSnapshot` est revenu à cinq champs, le widget ne binde plus un libellé sans producteur, et le
message d'activation ne promet plus une couverture que le code n'assure plus.
  </done>
</task>

<task type="auto">
  <name>Task 3 : les 8 styles × 9 thèmes, chargés, mesurés et disposés — la preuve qu'aucune case n'est vide</name>
  <files>tests/Chronos.Tests/SessionStylesBindingTests.cs</files>

  <read_first>
    - tests/Chronos.Tests/CadranBindingTests.cs (l. 1-80 — le motif BAML EXACT : `[Collection("XAML WPF")]`,
      `[WpfFact]`, chemins temporaires, `Measure`/`Arrange` pour déclencher les bindings)
    - tests/Chronos.Tests/XamlWpfCollection.cs (INTÉGRALEMENT — POURQUOI la sérialisation est obligatoire :
      le chargeur BAML n'est pas sûr en accès concurrent, le symptôme est intermittent et trompeur)
    - src/Chronos/Views/SessionsWindow.xaml (INTÉGRALEMENT, 33 lignes — le `ContentControl` et ses 8
      `DataTrigger` sur `IsStyleX`)
    - src/Chronos/Views/SessionsWindow.xaml.cs (INTÉGRALEMENT, 32 lignes — ctor et `ApplyThemeBrushes`)
    - src/Chronos/Theming/ChronosTheme.cs (l. 75-85 `SessionBrushTokens`, l. 140-165 `ThemeCatalog.All`)
  </read_first>

  <behavior>
    - Pour chacune des 72 combinaisons (8 styles × 9 thèmes), la fenêtre du widget se charge, se mesure et
      se dispose sans lever, avec une taille désirée strictement positive.
    - Dans le style Pastilles, chaque élément de session affiche EXACTEMENT un séparateur « · » visible —
      celui qui sépare l'état du détail. Deux séparateurs signaleraient un orphelin laissé par le retrait
      du libellé de type ; zéro signalerait qu'on a supprimé le mauvais.
    - La double boucle parcourt `ThemeCatalog.All` et `Enum.GetValues<SessionStyle>()`, jamais une liste
      écrite à la main : un 10e thème ou un 9e style entre dans la couverture sans qu'on touche au test.
  </behavior>

  <action>
Créer `tests/Chronos.Tests/SessionStylesBindingTests.cs`.

```csharp
using System.IO;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Media;
using Chronos.Services;
using Chronos.Theming;
using Chronos.ViewModels;
using Chronos.Views;
using Xunit;

namespace Chronos.Tests;

/// <summary>
/// Critère de succès 4 de la phase 21 : « aucun trou visuel ». Le retrait du libellé de type (bindé dans
/// le seul template Pastilles, l. 70-79 de SessionStyles.xaml) ne doit laisser ni case vide ni décalage,
/// sur les 8 styles de session et les 9 thèmes — 72 combinaisons.
///
/// SUBSTITUTION ASSUMÉE : la contrainte de phase interdit de lancer l'overlay (une instance est en cours
/// d'utilisation). Ce test est la vérification NON DESTRUCTIVE qui remplace le contrôle à l'œil : il charge
/// le vrai BAML, applique les vrais pinceaux du thème et fait faire à WPF sa passe de mise en page.
///
/// Aucun accès au vrai %APPDATA%\Chronos : le moniteur pointe des dossiers temporaires et sa source de
/// base est substituée.
/// </summary>
[Collection("XAML WPF")]   // le chargeur BAML n'est pas sûr en accès concurrent — voir XamlWpfCollection
public class SessionStylesBindingTests
{
    private static readonly DateTimeOffset Maintenant = new(2026, 9, 12, 12, 0, 0, TimeSpan.Zero);

    private static string TempDir()
    {
        var d = Path.Combine(Path.GetTempPath(), "chronos-styles-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(d);
        Assert.StartsWith(Path.GetTempPath(), d);
        return d;
    }

    // Source substituée : rend une liste FIXE couvrant les QUATRE états, pour qu'aucun template ne soit
    // mesuré à vide (une fenêtre sans élément passerait le test sans rien prouver).
    private sealed class SourceFixe : ISessionSource
    {
        private readonly IReadOnlyList<SessionSnapshot> _snaps;
        public SourceFixe(params SessionSnapshot[] snaps) => _snaps = snaps;
        public IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now) => _snaps;
    }

    private static SessionsViewModel Vm()
    {
        var source = new SourceFixe(
            new SessionSnapshot("s1", "overlay",       SessionActivity.WaitingAttention, "permission_prompt", Maintenant),
            new SessionSnapshot("s2", "api-migration", SessionActivity.WaitingTurn,      null, Maintenant.AddMinutes(-3)),
            new SessionSnapshot("s3", "chronos",       SessionActivity.Working,          null, Maintenant),
            new SessionSnapshot("s4", "legacy",        SessionActivity.Unknown,          null, Maintenant.AddMinutes(-12)));

        var monitor = new SessionMonitor(TempDir(), source, new ArchiveStore(Path.Combine(TempDir(), "a.json")));
        var vm = new SessionsViewModel(monitor, new FakeClock(Maintenant), new ArchiveStore(Path.Combine(TempDir(), "b.json")));
        vm.Refresh(Maintenant);
        Assert.Equal(4, vm.Items.Count);   // une fenêtre vide ne prouverait rien
        return vm;
    }

    // Parcourt l'arbre VISUEL et rend les TextBlock effectivement visibles.
    private static IEnumerable<TextBlock> TextesVisibles(DependencyObject racine)
    {
        var n = VisualTreeHelper.GetChildrenCount(racine);
        for (var i = 0; i < n; i++)
        {
            var enfant = VisualTreeHelper.GetChild(racine, i);
            if (enfant is TextBlock tb && tb.Visibility == Visibility.Visible) yield return tb;
            foreach (var petit in TextesVisibles(enfant)) yield return petit;
        }
    }

    [WpfFact]
    public void Les_8_styles_et_les_9_themes_se_chargent_se_mesurent_et_se_disposent()
    {
        var styles = Enum.GetValues<SessionStyle>();
        var themes = ThemeCatalog.All;

        Assert.Equal(8, styles.Length);      // garde anti-muette : la matrice est bien 8 × 9
        Assert.Equal(9, themes.Count);

        foreach (var theme in themes)
        foreach (var style in styles)
        {
            var vm = Vm();
            vm.Style = style;
            vm.SetTheme(theme);

            var fenetre = new SessionsWindow(vm);
            fenetre.ApplyThemeBrushes(theme);
            fenetre.Measure(new Size(400, 400));
            fenetre.Arrange(new Rect(0, 0, 400, 400));
            fenetre.UpdateLayout();

            Assert.True(fenetre.DesiredSize.Width > 0 && fenetre.DesiredSize.Height > 0,
                $"taille dégénérée pour le style {style} et le thème {theme.Key} : {fenetre.DesiredSize}");
        }
    }

    [WpfFact]
    public void Le_style_Pastilles_n_a_plus_qu_un_seul_separateur_visible_par_session()
    {
        var vm = Vm();
        vm.Style = SessionStyle.Pastilles;
        vm.SetTheme(ThemeCatalog.Default);

        var fenetre = new SessionsWindow(vm);
        fenetre.ApplyThemeBrushes(ThemeCatalog.Default);
        fenetre.Measure(new Size(400, 400));
        fenetre.Arrange(new Rect(0, 0, 400, 400));
        fenetre.UpdateLayout();

        var separateurs = TextesVisibles(fenetre).Count(tb => tb.Text is "  ·  ");

        // 4 sessions × 1 séparateur (état · détail). Deux par session = un orphelin laissé par le retrait
        // du libellé de type ; zéro = on a supprimé le mauvais TextBlock.
        Assert.Equal(4, separateurs);
    }
}
```

Si `Enum.GetValues<SessionStyle>()` ou `ThemeCatalog.All` exigent un `using` supplémentaire, l'ajouter ;
ne pas remplacer le parcours du catalogue par une liste écrite à la main.

**Pas de checkpoint humain.** La contrainte de phase interdit de lancer l'overlay (instance en cours
d'utilisation, pid 119412). Consigner dans le SUMMARY, sous une section **« À VÉRIFIER PAR L'UTILISATEUR »** :
« au prochain redémarrage volontaire de l'overlay, ouvrir la galerie `--sessions` et vérifier à l'œil les
8 styles — la rangée d'un élément Pastilles doit lire `état · détail`, sans point médian en fin de ligne. »
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~SessionStylesBindingTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ThemeCatalog.All" tests/Chronos.Tests/SessionStylesBindingTests.cs` rend **au moins 1**
    - `grep -c "Enum.GetValues<SessionStyle>()" tests/Chronos.Tests/SessionStylesBindingTests.cs` rend **au moins 1**
    - `grep -c 'Collection("XAML WPF")' tests/Chronos.Tests/SessionStylesBindingTests.cs` rend **1** —
      sans cette sérialisation, le chargeur BAML produit un échec INTERMITTENT et trompeur
    - `grep -c "WpfFact" tests/Chronos.Tests/SessionStylesBindingTests.cs` rend **2**
    - Les 2 tests passent ; le premier parcourt bien 72 combinaisons (8 × 9, asserté dans le test lui-même)
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, **712** tests (710 + 2)
    - Deux exécutions consécutives de la suite complète sont vertes (le BAML impose cette vérification :
      un défaut de parallélisme ne se voit pas à la première passe)
  </acceptance_criteria>

  <done>
Les 72 combinaisons se chargent et se mesurent sans exception, et le style touché n'a plus qu'un séparateur
par ligne — la disparition du libellé de type ne laisse ni case vide ni décalage.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -c Debug --nologo -v q` **deux fois de suite** → **0 échec**, **712** tests.
2. `grep -rn "DesktopUia\|Uia\|IForegroundWatch\|SessionKind\|SessionOrigin\|KindLabel" --include=*.cs --include=*.xaml src | wc -l`
   rend **0**.
3. `git status --porcelain` montre 10 fichiers supprimés (`D`), 5 modifiés, 2 ajoutés.
4. Gardes nommément vérifiées vertes : `ServicesLayerPurityTests`, `CompositionRootTests`,
   `NormalisationUniqueTests`, `GardesDoctrineTests`,
   `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte`.
5. **AUCUN fichier de `%APPDATA%\Chronos\sessions\` n'a été touché** : `ls "$APPDATA/Chronos/sessions" | wc -l`
   doit toujours rendre **66** (54 `.json` + 12 `.tmp`). Le balayage est la phase 23.
</verification>

<success_criteria>
- Les 765 lignes de `src/` et 529 de tests ont quitté le dépôt ; 40 tests supprimés, tous justifiés par la
  disparition du type qu'ils instanciaient.
- `SessionSnapshot` a 5 champs ; plus aucun binding de libellé de type dans les 8 styles.
- Le message d'activation du widget ne promet plus l'app bureau.
- Les 72 combinaisons style × thème se chargent et se mesurent ; Pastilles n'a qu'un séparateur par ligne.
- Deux gardes de non-retour (réflexion + source XAML) feraient échouer un rétablissement.
</success_criteria>

<output>
Créer `.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-03-SUMMARY.md`, avec :
- le total de tests mesuré après chaque tâche,
- la section **« À VÉRIFIER PAR L'UTILISATEUR »** (galerie `--sessions` au prochain redémarrage volontaire),
- la correction du contexte : le libellé de type n'était bindé que dans **1** des 8 styles, pas dans les 8.
</output>
