---
phase: 21-p-rim-tre-le-widget-ne-parle-que-de-claude-code
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/Chronos/Services/SessionMonitor.cs
  - src/Chronos/Services/SessionTreatmentTracker.cs
  - src/Chronos/App.xaml.cs
  - src/Chronos/Services/DiagnosticService.cs
  - src/Chronos/Services/IInventaireMachine.cs
  - src/Chronos/Services/InventaireMachine.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
  - tests/Chronos.Tests/TreatedSessionsTests.cs
  - tests/Chronos.Tests/SessionsTests.cs
  - tests/Chronos.Tests/Fakes/FakeInventaireMachine.cs
  - tests/Chronos.Tests/TokenRefreshServiceTests.cs
autonomous: true
requirements: [SRC-01]

must_haves:
  truths:
    - "Plus aucun consommateur du dépôt n'appelle la source app-bureau ni l'observateur de focus"
    - "Le moniteur du widget se construit avec cinq arguments et ne fusionne plus qu'une source de base et les fichiers de hooks"
    - "Le détecteur de traitement n'a plus de branche d'acquittement par focus — celle qui n'atteignait jamais une session Claude Code"
    - "Le diagnostic n'imprime plus de section app-bureau et n'appelle plus de sondage UI Automation"
    - "La garde de composition prouve encore que le graphe de sessions se résout dans le bon ordre"
  artifacts:
    - path: "src/Chronos/Services/SessionMonitor.cs"
      provides: "Constructeur à 5 paramètres, Read sans étape de repli app-bureau"
      contains: "TreatedStore? treated = null, SessionTreatmentTracker? tracker = null)"
    - path: "src/Chronos/Services/SessionTreatmentTracker.cs"
      provides: "Observe à deux paramètres (snapshots + horloge), NET-01 et NET-03 seuls"
      contains: "public void Observe(IReadOnlyList<SessionSnapshot> raw, System.DateTimeOffset now)"
    - path: "tests/Chronos.Tests/CompositionRootTests.cs"
      provides: "Garde DI de la chaîne de sessions, réduite au périmètre Claude Code"
      contains: "Le_graphe_DI_resout_la_chaine_de_sessions"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "SessionMonitor"
      via: "un unique enregistrement DI à 5 arguments"
      pattern: "new SessionMonitor\\(null, null,"
    - from: "src/Chronos/Services/SessionMonitor.cs"
      to: "SessionTreatmentTracker.Observe"
      via: "appel à deux arguments, best-effort sous try/catch"
      pattern: "_tracker\\?\\.Observe\\(raw, now\\)"
---

<objective>
SRC-01, premier des deux gestes : **débrancher**. Aucun fichier n'est supprimé ici. On coupe tous les
consommateurs de la source app-bureau et de l'hystérésis par focus, de sorte qu'à la fin de ce plan les
huit fichiers à supprimer soient des **orphelins** : plus personne ne les référence, hors leurs propres
tests.

Purpose : c'est la seule découpe qui garantit la contrainte dure de la phase — **la solution compile à
chaque commit**. `tests/Chronos.Tests` a un `ProjectReference` vers `Chronos` : une signature changée sans
ses sites d'appel fait échouer TOUTE l'invocation `dotnet test`, pas seulement le test concerné. Chaque
tâche de ce plan change donc une signature ET tous ses appelants dans le même commit.

Pourquoi l'observateur de focus tombe avec la source app-bureau, et ce n'est pas du ménage opportuniste :
`SessionTreatmentTracker.IsForegroundDesktop` exige `Origin == Desktop` **et** un identifiant préfixé
`desktop:foreground:`. Une session Claude Code a `Origin = Cli` et un UUID. La branche NET-02
**n'atteignait donc structurellement jamais** une session de terminal, et le P/Invoke de focus était
consommé à chaque tick pour un résultat jamais lu. C'est du code mort **prouvé**, pas soupçonné.

Output : un moniteur à 5 arguments, un détecteur à deux branches, un diagnostic sans section app-bureau,
et une garde de composition réécrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-CONTEXT.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-01-SUMMARY.md

<interfaces>
<!-- Contrats existants, extraits du dépôt le 2026-09-12. NE PAS explorer le code pour les retrouver. -->

```csharp
// src/Chronos/Services/SessionMonitor.cs — constructeur ACTUEL (7 paramètres, tous optionnels)
public SessionMonitor(string? sessionsDir = null, TranscriptSessionSource? transcripts = null,
    ArchiveStore? archive = null, ISessionSource? desktop = null,
    TreatedStore? treated = null, SessionTreatmentTracker? tracker = null, IForegroundWatch? foreground = null)

// Champs : _dir, _transcripts, _archive, _desktop, _treated, _tracker, _foreground
// Read(now) enchaîne : 1) transcripts → 2) fichiers de hooks → 2.b) repli app-bureau (l. 86-100)
//                    → 2.c) tracker (l. 102-107) → 3) filtres archived puis treated (l. 109-123)
```

```csharp
// src/Chronos/Services/SessionTreatmentTracker.cs — membres ACTUELS
private static readonly System.TimeSpan FocusAckDelay = System.TimeSpan.FromSeconds(2.5);
private readonly Dictionary<string, SessionActivity> _lastActivity = new();   // ← RESTE
private readonly Dictionary<string, long> _waitingSince = new();              // ← RESTE
private readonly Dictionary<string, System.DateTimeOffset> _focusSince = new();
private static bool IsWaiting(SessionActivity a) => …                         // ← RESTE
private static bool IsForegroundDesktop(SessionSnapshot s) => …
public void Observe(IReadOnlyList<SessionSnapshot> raw, bool claudeForeground, System.DateTimeOffset now)
```

```csharp
// src/Chronos/Services/IInventaireMachine.cs — DEUX membres ; seul le premier survit
IReadOnlyList<string> CoffresOAuth(string racine);
(IReadOnlyList<SessionSnapshot> Sessions, DesktopHealth Sante) SessionsBureau(DateTimeOffset now);
```

Les TROIS sites d'appel positionnels de `SessionMonitor` hors tests de composition, vérifiés par grep :
```
src/Chronos/App.xaml.cs:223      new SessionMonitor(null, null, archive, desktop, treated, tracker, foreground)
src/Chronos/Services/DiagnosticService.cs:463   new SessionMonitor()            ← INCHANGÉ (0 argument)
tests/Chronos.Tests/SessionsTests.cs            new SessionMonitor(dir, transcripts, archive)  ← INCHANGÉ (3 args)
tests/Chronos.Tests/TreatedSessionsTests.cs:156 new SessionMonitor(dir, transcripts, archive, source, treated, tracker, focus)
```
Le 4e argument disparaissant, **seuls** les appels à 4 arguments ou plus sont à retoucher.

Tests de `SessionsTests.cs` qui disparaissent avec la branche de repli app-bureau — noms EXACTS et
numéros de ligne actuels :
```
l. 326  private sealed class FakeSessionSource : ISessionSource      (l'aide, 6 lignes)
l. 336  Monitor_source_bureau_ignoree_si_sessions_locales_presentes
l. 364  Monitor_source_bureau_utilisee_en_repli_si_aucune_session_locale
l. 380  Monitor_sans_source_bureau_ne_regresse_pas                    ← SURVIT, renommé
l. 396  Monitor_archive_une_session_bureau
l. 415  Widget_affiche_le_type_de_session_bureau            [WpfFact]
l. 437  Widget_mappe_chaque_type_bureau_vers_son_libelle    [WpfFact]
```
Tests de `TreatedSessionsTests.cs` qui disparaissent avec la branche d'acquittement par focus :
```
l. 18   private static SessionSnapshot Fg(…)            (l'aide)
l. 92   NET02_focus_continu_2_5s_acquitte
l. 107  NET02_interruption_focus_remet_a_zero
l. 123  NET02_ne_declenche_pas_sans_focus
l. 140  private sealed class FakeForegroundWatch        (l'aide)
l. 221  NET02_le_monitor_masque_apres_focus_2_5s
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : la couture — moniteur, détecteur, composition et leurs tests, en un seul commit compilable</name>
  <files>src/Chronos/Services/SessionMonitor.cs, src/Chronos/Services/SessionTreatmentTracker.cs, src/Chronos/App.xaml.cs, tests/Chronos.Tests/CompositionRootTests.cs, tests/Chronos.Tests/TreatedSessionsTests.cs, tests/Chronos.Tests/SessionsTests.cs</files>

  <read_first>
    - src/Chronos/Services/SessionMonitor.cs (INTÉGRALEMENT, 162 lignes — surtout le commentaire de
      l'étape 2.b l. 86-92, qui explique POURQUOI le repli app-bureau était un anti-doublon : c'est cette
      raison d'être qui disparaît)
    - src/Chronos/Services/SessionTreatmentTracker.cs (INTÉGRALEMENT, 103 lignes — l. 47-49
      `IsForegroundDesktop` : la preuve du code mort tient en trois lignes)
    - src/Chronos/App.xaml.cs (l. 200-250 — le bloc « Source BUREAU (UIA) », le bloc hystérésis,
      l'enregistrement de `SessionMonitor` et celui du poll de fond)
    - tests/Chronos.Tests/CompositionRootTests.cs (l. 128-175 — la garde DI et son commentaire, qui
      explique pourquoi une DI mal ordonnée compile et ne plante qu'au démarrage)
    - tests/Chronos.Tests/TreatedSessionsTests.cs (INTÉGRALEMENT, 252 lignes)
    - tests/Chronos.Tests/SessionsTests.cs (l. 303-455)
  </read_first>

  <action>
**Un seul commit.** Les six fichiers changent ensemble parce que `tests/Chronos.Tests` référence le projet
`Chronos` : laisser un seul site d'appel non adapté ferait échouer TOUTE l'invocation `dotnet test`.

**1. `src/Chronos/Services/SessionMonitor.cs`**

Constructeur, champs, et l'étape 2.b :

```csharp
    private readonly string _dir;
    private readonly ISessionSource _transcripts;
    private readonly ArchiveStore _archive;
    private readonly TreatedStore? _treated;
    private readonly SessionTreatmentTracker? _tracker;

    public SessionMonitor(string? sessionsDir = null, ISessionSource? transcripts = null,
        ArchiveStore? archive = null,
        TreatedStore? treated = null, SessionTreatmentTracker? tracker = null)
    {
        _dir = sessionsDir ?? Path.Combine(
            System.Environment.GetFolderPath(System.Environment.SpecialFolder.ApplicationData), "Chronos", "sessions");
        _transcripts = transcripts ?? new TranscriptSessionSource();
        _archive = archive ?? new ArchiveStore();
        _treated = treated;
        _tracker = tracker;
    }
```

Noter le changement de TYPE du 2e paramètre : `TranscriptSessionSource?` → `ISessionSource?`. Le défaut
reste `new TranscriptSessionSource()` (le plan 01 lui a fait porter le contrat). Aucun appelant à 0, 1, 2
ou 3 arguments n'est affecté.

Dans `Read` : **supprimer intégralement** le bloc de l'étape 2.b (l. 86-100 actuelles, du commentaire
« 2.b) Source BUREAU (UIA) » jusqu'à l'accolade fermante du `if (_desktop is not null && byId.Count == 0)`).

Remplacer l'étape 2.c (l. 102-107) par :

```csharp
        // 2.b) Le détecteur de traitement observe les snapshots BRUTS fusionnés (+ horloge) et met à jour
        //      TreatedStore (ajout NET-01, purge NET-03). Best-effort : ne casse JAMAIS le pipeline.
        var raw = byId.Values.ToList();
        try { _tracker?.Observe(raw, now); } catch { }
```

Mettre à jour le commentaire XML de `Read` (l. 63-68) : il annonce aujourd'hui « FUSIONNE deux sources »
en précisant que les transcripts « couvrent l'app BUREAU (base) ». Réécrire la promesse au périmètre réel :

```csharp
    /// <summary>
    /// Instantané courant des sessions Claude Code (staleness appliquée). FUSIONNE deux sources par
    /// session_id :
    ///   • transcripts (~/.claude/projects) — la base, universelle ;
    ///   • fichiers d'état des HOOKS (%APPDATA%\Chronos\sessions) — plus précis (permission),
    ///     PRIORITAIRES quand présents.
    /// </summary>
```

Enfin, remplacer le commentaire des champs d'hystérésis (l. 30-40, celui qui parle d'une branche « dormante
mais présente » et d'un paramètre « ajouté en fin ») par une note courte :

```csharp
    // Hystérésis (phase 14, réduite en phase 21) : nuls par défaut = fonctionnalité désactivée. Quand ils
    // sont fournis, le détecteur observe les snapshots BRUTS et alimente TreatedStore ; le filtre masque
    // ensuite les sessions traitées.
```

**Attention aux noms** : aucun des identifiants supprimés (`_desktop`, `_foreground`, le type de
l'observateur de focus, les types de la source app-bureau) ne doit subsister dans ce fichier, **commentaires
compris**. Parler de « la source app-bureau » en toutes lettres, jamais par son nom de type.

**2. `src/Chronos/Services/SessionTreatmentTracker.cs`**

Supprimer : la constante `FocusAckDelay`, le dictionnaire `_focusSince`, la méthode `IsForegroundDesktop`,
le paramètre `claudeForeground` d'`Observe`, et les deux blocs qui les utilisaient (le `if (claudeForeground
&& IsForegroundDesktop(s)) { … } else { … }` à l'intérieur du `if (isWaiting)`, et le `else { _focusSince.Remove(id); }`
final). Ce qui reste :

```csharp
    public void Observe(IReadOnlyList<SessionSnapshot> raw, System.DateTimeOffset now)
    {
        var nowMs = now.ToUnixTimeMilliseconds();
        var treated = _store.Load(); // une seule lecture par cycle (sert au test de réapparition NET-03)

        foreach (var s in raw)
        {
            var id = s.SessionId;
            var isWaiting = IsWaiting(s.Activity);
            var wasWaiting = _lastActivity.TryGetValue(id, out var prev) && IsWaiting(prev);

            // NET-01 (répondu) : présente et passée d'attente → non-attente ce cycle → traitée.
            if (wasWaiting && !isWaiting)
            {
                _store.Set(id, _waitingSince.TryGetValue(id, out var ep) ? ep : s.UpdatedAt.ToUnixTimeMilliseconds());
                _waitingSince.Remove(id);
            }

            // Suivi de l'épisode d'attente courant (horodatage STABLE pendant l'épisode).
            if (isWaiting && !wasWaiting) _waitingSince[id] = nowMs;

            // NET-03 (réapparition/purge) : épisode d'attente courant PLUS RÉCENT que le traitement mémorisé.
            if (isWaiting && treated.TryGetValue(id, out var tts)
                          && _waitingSince.TryGetValue(id, out var cur) && cur > tts)
                _store.Remove(id);

            _lastActivity[id] = s.Activity;
        }
    }
```

Dans le commentaire XML de la classe, remplacer la puce NET-02 par son **épitaphe** — la raison mécanique,
pas un « supprimé car inutilisé » :

```
///   • NET-02 (acquitté par focus) : RETIRÉE en phase 21. Elle exigeait une session d'origine « app bureau »
///     ET un identifiant synthétique de cette source. Une session Claude Code a une origine ligne de commande
///     et un UUID : la branche ne l'atteignait JAMAIS, et le sondage de focus OS était payé à chaque tick
///     pour un résultat jamais lu. Code mort PROUVÉ, pas soupçonné.
```

Supprimer aussi, dans ce même commentaire, la phrase qui justifie l'horodatage d'épisode par « le snapshot
bureau porte UpdatedAt == now à chaque poll » : cette raison n'existe plus. La remplacer par la raison qui
reste vraie : l'épisode doit être STABLE pendant toute la durée de l'attente, sinon NET-03 purgerait à
chaque cycle.

**3. `src/Chronos/App.xaml.cs`**

Supprimer le bloc « Source BUREAU (UIA) » (l. 207-212 : le commentaire de 5 lignes + les 2
`services.AddSingleton`), la ligne d'enregistrement de l'observateur de focus (l. 221), et le bloc du poll
de fond (l. 228-233 : commentaire + `AddSingleton` + `AddHostedService`).

Le bloc restant devient EXACTEMENT :

```csharp
        // Hystérésis (phase 14, réduite en phase 21) : magasin RÉVERSIBLE des sessions traitées + détecteur
        // STATEFUL. Déclarés AVANT le SessionMonitor qui les consomme. L'acquittement par focus est tombé
        // avec la source app-bureau : il exigeait une origine qu'aucune session Claude Code ne porte (SRC-01).
        services.AddSingleton(_ => new TreatedStore());
        services.AddSingleton(sp => new SessionTreatmentTracker(sp.GetRequiredService<TreatedStore>()));

        services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
            sp.GetRequiredService<TreatedStore>(),
            sp.GetRequiredService<SessionTreatmentTracker>()));
```

**Ne PAS toucher** à `services.AddSingleton(_ => new ArchiveStore());` (l. 205), ni à
`SessionHookInstaller`, `ClaudeSettingsReconciler`, `ISessionsController`, ni à quoi que ce soit d'autre.
Aucun nom de type supprimé ne doit subsister dans ce fichier, commentaires compris.

**4. `tests/Chronos.Tests/CompositionRootTests.cs`**

Renommer `Le_graphe_DI_resout_les_services_bureau_UIA` en `Le_graphe_DI_resout_la_chaine_de_sessions` et
réduire son corps à la chaîne qui subsiste :

```csharp
        var services = new ServiceCollection();

        // Chaîne de sessions EXACTEMENT comme dans App.xaml.cs (ArchiveStore + hystérésis + moniteur).
        services.AddSingleton(_ => new ArchiveStore(
            System.IO.Path.Combine(System.IO.Path.GetTempPath(), "ChronosArch_" + System.Guid.NewGuid().ToString("N") + ".json")));
        services.AddSingleton(_ => new TreatedStore(
            System.IO.Path.Combine(System.IO.Path.GetTempPath(), "ChronosTreated_" + System.Guid.NewGuid().ToString("N") + ".json")));
        services.AddSingleton(sp => new SessionTreatmentTracker(sp.GetRequiredService<TreatedStore>()));
        services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
            sp.GetRequiredService<TreatedStore>(),
            sp.GetRequiredService<SessionTreatmentTracker>()));

        var provider = services.BuildServiceProvider();

        // Résolution sans exception = graphe câblé dans le BON ORDRE (aucun service manquant/mal ordonné).
        Assert.NotNull(provider.GetRequiredService<SessionMonitor>());

        provider.Dispose();
```

Le commentaire XML garde sa raison d'être (« une DI mal ordonnée COMPILERAIT et ne planterait qu'au
DÉMARRAGE ») mais doit cesser de nommer les types supprimés : parler de « la sous-chaîne app-bureau
(phases 13-14), retirée en phase 21 » et de ce que la garde couvre désormais — `ArchiveStore`,
`TreatedStore`, `SessionTreatmentTracker`, `SessionMonitor`.

L'enregistrement `services.AddSingleton<IClock, SystemClock>()` de ce test ne servait qu'au poll de fond :
le retirer si plus rien ne le consomme.

**5. `tests/Chronos.Tests/TreatedSessionsTests.cs`**

Supprimer : l'aide `Fg` (l. 18), la classe `FakeForegroundWatch` (l. 139-144), et les QUATRE tests
`NET02_focus_continu_2_5s_acquitte`, `NET02_interruption_focus_remet_a_zero`,
`NET02_ne_declenche_pas_sans_focus`, `NET02_le_monitor_masque_apres_focus_2_5s`.

`MutableSource` **reste** : elle implémente `ISessionSource` et devient la source de BASE du moniteur
(2e argument) au lieu d'une source app-bureau (4e argument). Réécrire `BuildMonitor` :

```csharp
    // MutableSource alimente désormais la source de BASE du moniteur (2e argument) : c'est le point de
    // substitution que le contrat ISessionSource existe pour offrir.
    private static SessionMonitor BuildMonitor(MutableSource source, TreatedStore treated,
        SessionTreatmentTracker tracker, ArchiveStore? archive = null)
        => new SessionMonitor(TempDir(), source, archive ?? new ArchiveStore(TempFile()), treated, tracker);
```

Adapter les quatre appels survivants (`NET01_le_monitor_masque_apres_reponse`,
`NET03_le_monitor_la_reaffiche_sur_nouvel_episode`, `NET04_archivee_reste_masquee_meme_en_attente`,
`Monitor_sans_tracker_ni_treated_ne_regresse_pas`) : retirer l'argument de focus, et pour le dernier
déplacer `source` du 4e au 2e argument :

```csharp
        var monitor = new SessionMonitor(TempDir(), source, new ArchiveStore(TempFile()));
```

Adapter les appels directs `tracker.Observe(…, false, t)` → `tracker.Observe(…, t)` dans les trois tests
`NET01_*` / `NET03_*` qui subsistent.

Mettre à jour le commentaire XML de la classe : elle ne prouve plus NET-02, et il n'y a plus de « faux
focus ». Dire ce qui reste : NET-01 (répondu), NET-03 (réapparition/purge), NET-04 (archivage permanent),
et le branchement dans `SessionMonitor`.

**6. `tests/Chronos.Tests/SessionsTests.cs`**

Supprimer l'aide `FakeSessionSource` (l. 326-334) et les CINQ tests suivants :

| Test supprimé | Ce qu'il couvrait, et qui disparaît |
|---|---|
| `Monitor_source_bureau_ignoree_si_sessions_locales_presentes` | l'étape 2.b (anti-doublon du repli app-bureau) |
| `Monitor_source_bureau_utilisee_en_repli_si_aucune_session_locale` | l'étape 2.b (le repli lui-même) |
| `Monitor_archive_une_session_bureau` | l'archivage d'une clé synthétique de la source app-bureau — l'archivage générique reste couvert par `Archive_retire_la_session_de_l_affichage` (l. 304) |
| `Widget_affiche_le_type_de_session_bureau` | le libellé de type, retiré au plan 03 |
| `Widget_mappe_chaque_type_bureau_vers_son_libelle` | idem |

Renommer `Monitor_sans_source_bureau_ne_regresse_pas` (l. 380) en
`Monitor_lit_une_session_du_transcript_sans_hook` et réécrire son commentaire : il ne prouve plus une
non-régression par rapport à une source absente, il prouve le cas nominal (transcript seul, aucun hook).
Son corps ne change pas.

Mettre à jour le commentaire XML de la classe s'il mentionne la détection app-bureau.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` réussit — **aucun « échec de build attendu » n'est admis**
    - `grep -c "TreatedStore? treated = null, SessionTreatmentTracker? tracker = null)" src/Chronos/Services/SessionMonitor.cs` rend **1**
      (le constructeur se termine bien sur le 5e paramètre)
    - `grep -c "_desktop\|_foreground\|IForegroundWatch\|ISessionSource? desktop" src/Chronos/Services/SessionMonitor.cs` rend **0**
    - `grep -c "ISessionSource? transcripts" src/Chronos/Services/SessionMonitor.cs` rend **1**
    - `grep -c "_tracker?.Observe(raw, now)" src/Chronos/Services/SessionMonitor.cs` rend **1**
    - `grep -c "FocusAckDelay\|_focusSince\|IsForegroundDesktop\|claudeForeground" src/Chronos/Services/SessionTreatmentTracker.cs` rend **0**
    - `grep -c "NET-02" src/Chronos/Services/SessionTreatmentTracker.cs` rend **1** — l'épitaphe, et elle seule
    - `grep -c "Uia\|IForegroundWatch\|DesktopUia" src/Chronos/App.xaml.cs` rend **0**
    - `grep -c "new SessionMonitor(null, null," src/Chronos/App.xaml.cs` rend **1**
    - `grep -c "AddHostedService" src/Chronos/App.xaml.cs` rend **2** — il y en avait TROIS (poll de fond
      l. 233, `TokenRefreshService` l. 304, `RefreshOrchestrator` l. 369) ; seul celui du poll de fond disparaît
    - `grep -c "Le_graphe_DI_resout_la_chaine_de_sessions" tests/Chronos.Tests/CompositionRootTests.cs` rend **1**
    - `grep -c "DesktopUiaPollService\|IUiaTreeProvider\|WindowsForegroundWatch\|WindowsUiaTreeProvider" tests/Chronos.Tests/CompositionRootTests.cs` rend **0**
    - `grep -c "FakeForegroundWatch\|NET02_" tests/Chronos.Tests/TreatedSessionsTests.cs` rend **0**
    - `grep -c "MutableSource" tests/Chronos.Tests/TreatedSessionsTests.cs` rend **au moins 3**
      (déclaration + `BuildMonitor` + au moins un test) — la substitution par le contrat SURVIT
    - `grep -c "FakeSessionSource\|source_bureau\|session_bureau\|type_bureau" tests/Chronos.Tests/SessionsTests.cs` rend **0**
    - `grep -c "Monitor_lit_une_session_du_transcript_sans_hook" tests/Chronos.Tests/SessionsTests.cs` rend **1**
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**. Total attendu : **747** (756 − 9 :
      5 de `SessionsTests` + 4 de `TreatedSessionsTests`). Un écart est un signal à instruire
    - Les gardes suivantes restent vertes, nommément : `ServicesLayerPurityTests`,
      `NormalisationUniqueTests`, `GardesDoctrineTests`,
      `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte`
  </acceptance_criteria>

  <done>
Le moniteur, le détecteur et la composition ne connaissent plus la source app-bureau ni le focus OS. La
solution compile, la suite est verte, et les huit fichiers à supprimer ne sont plus référencés que par
leurs propres tests.
  </done>
</task>

<task type="auto">
  <name>Task 2 : l'instrument de mesure cesse de sonder une source qui n'existera plus</name>
  <files>src/Chronos/Services/DiagnosticService.cs, src/Chronos/Services/IInventaireMachine.cs, src/Chronos/Services/InventaireMachine.cs, tests/Chronos.Tests/Fakes/FakeInventaireMachine.cs, tests/Chronos.Tests/TokenRefreshServiceTests.cs</files>

  <read_first>
    - src/Chronos/Services/DiagnosticService.cs (l. 455-485 — le bloc « Sessions détectées (widget) » puis
      le bloc « Sessions BUREAU (UIA, one-shot) » ; seul le SECOND disparaît)
    - src/Chronos/Services/IInventaireMachine.cs (INTÉGRALEMENT, 23 lignes — son commentaire chiffre les
      DEUX sondages chers : coffres OAuth 17 703 ms, poll UIA 936 ms)
    - src/Chronos/Services/InventaireMachine.cs (INTÉGRALEMENT, 62 lignes)
    - tests/Chronos.Tests/Fakes/FakeInventaireMachine.cs (INTÉGRALEMENT, 25 lignes)
    - tests/Chronos.Tests/TokenRefreshServiceTests.cs (l. 78-82 — l'unique occurrence, dans un commentaire
      XML qui cite un motif de code d'un fichier qui va disparaître)
    - tests/Chronos.Tests/DiagnosticServiceTests.cs (vérifier par grep qu'AUCUNE assertion ne porte sur la
      section app-bureau du rapport — c'était le cas au 2026-09-12)
  </read_first>

  <action>
**Attention à la limite de périmètre** : la phase 22 recâblera le diagnostic sur le moniteur *du widget*
(OBS-01). Cette tâche **ne fait que retirer** ce qui sonde une source supprimée. Elle ne touche NI au bloc
« Sessions détectées (widget) » (l. 460-470), NI au `new SessionMonitor()` de la l. 463, NI au
`files.Take(8)` de la l. 440 — ce sont les sujets de la phase 22, et les corriger ici les corrigerait deux
fois.

**1. `src/Chronos/Services/DiagnosticService.cs`**

Supprimer le bloc « Sessions BUREAU (UIA, one-shot) » **en entier** (l. 472-484 : le commentaire de 3
lignes, le `try { … }` et son `catch`). La ligne `sb.AppendLine();` qui suit immédiatement est la
séparation de section : la CONSERVER.

Le champ `_machine` et le paramètre `machine` du constructeur **restent** : `CoffresOAuth` continue de les
utiliser (c'est le sondage à 17 703 ms, 94 % du coût du rapport — sa couture sous test est la raison d'être
de l'interface).

**2. `src/Chronos/Services/IInventaireMachine.cs`**

Supprimer le membre `SessionsBureau` et son commentaire XML. Corriger le commentaire de l'interface : il
annonce « les DEUX sondages d'environnement MESURÉS comme chers » — il n'en reste qu'un. Nouvelle rédaction :

```csharp
/// <summary>
/// Le sondage d'environnement MESURÉ comme cher dans <see cref="DiagnosticService.BuildReportAsync"/>
/// (2026-09-12, machine cible) : recherche des coffres OAuth, 17 703 ms — 94 % du coût d'un rapport. Tout
/// le reste du rapport coûte moins de 50 ms cumulés et n'a aucun besoin de couture.
///
/// <para>Phase 21 : le second membre (poll de la source app-bureau, 936 ms) a disparu avec cette source.</para>
///
/// Cette interface n'existe QUE pour rendre ce sondage substituable sous test. Elle n'est pas un point
/// d'extension : n'y ajouter un membre que si une mesure montre qu'il coûte des secondes.
///
/// Type NEUTRE (aucun type WPF) : la garde <c>ServicesLayerPurityTests</c> s'applique.
/// </summary>
```

**3. `src/Chronos/Services/InventaireMachine.cs`**

Supprimer la méthode `SessionsBureau` (l. 54-61) et son commentaire. `CoffresOAuth` reste intacte, bornes
comprises (profondeur 3, 5 résultats, liste noire, 2 Mo) — « les toucher ferait dériver le coût mesuré ».

**4. `tests/Chronos.Tests/Fakes/FakeInventaireMachine.cs`**

Supprimer l'implémentation de `SessionsBureau` (l. 23-24). Corriger le commentaire XML : il chiffre les
deux sondages ; ne garder que celui qui reste, et ajouter une phrase de phase 21 disant que le second a
disparu avec sa source. La classe conserve `CoffresOAuth`.

**5. `tests/Chronos.Tests/TokenRefreshServiceTests.cs`**

L'occurrence est **fortuite** : un commentaire XML (l. 80-81) cite un fichier qui disparaît comme
précédent de motif (« dueTime zéro »). Remplacer la parenthèse
`(dueTime zéro, motif <c>DesktopUiaPollService.StartAsync:44</c>)` par
`(dueTime zéro : le premier tick part à la souscription, pas après le premier intervalle)`.
Aucun code de test n'est modifié ; aucun test n'est supprimé.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~DiagnosticServiceTests|FullyQualifiedName~TokenRefreshServiceTests|FullyQualifiedName~ServicesLayerPurityTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "SessionsBureau" src/Chronos/Services/IInventaireMachine.cs` rend **0**
    - `grep -c "SessionsBureau" src/Chronos/Services/InventaireMachine.cs` rend **0**
    - `grep -c "SessionsBureau" src/Chronos/Services/DiagnosticService.cs` rend **0**
    - `grep -rc "SessionsBureau\|DesktopHealth" tests/Chronos.Tests/Fakes/FakeInventaireMachine.cs` rend **0**
    - `grep -c "CoffresOAuth" src/Chronos/Services/IInventaireMachine.cs` rend **1** — le sondage cher
      SURVIT, avec sa couture
    - `grep -c "_machine" src/Chronos/Services/DiagnosticService.cs` rend **au moins 2** (le champ +
      l'appel `CoffresOAuth`) : le champ n'est PAS supprimé
    - `grep -c "new SessionMonitor()" src/Chronos/Services/DiagnosticService.cs` rend **1** — la phase 22
      seule a le droit d'y toucher ; le laisser ici est DÉLIBÉRÉ
    - `grep -c "Take(8)" src/Chronos/Services/DiagnosticService.cs` rend **au moins 1** — même raison
    - `grep -c "DesktopUiaPollService" tests/Chronos.Tests/TokenRefreshServiceTests.cs` rend **0**
    - `dotnet test … --filter …` → **0 échec** ; le nombre de tests de ces trois classes est **inchangé**
      par rapport au plan 01 (aucun test n'est supprimé par cette tâche)
  </acceptance_criteria>

  <done>
Le rapport de diagnostic n'imprime plus de section app-bureau et ne paie plus l'appel COM correspondant ;
`IInventaireMachine` ne coud plus qu'un seul sondage, celui qui coûte réellement.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, **747** tests.
2. `grep -rn "IForegroundWatch\|ISessionSource? desktop\|SessionsBureau" src/ --include=*.cs` ne doit plus
   renvoyer que les **déclarations** dans les fichiers encore présents mais orphelins
   (`IForegroundWatch.cs`, `WindowsForegroundWatch.cs`) — aucun **consommateur**.
3. Les huit fichiers à supprimer au plan 03 ne sont plus référencés que par leurs deux fichiers de tests :
   `grep -rln "DesktopUiaSessionSource\|DesktopUiaPollService\|IUiaTreeProvider\|WindowsUiaTreeProvider\|UiaLabels\|UiaNode\|IForegroundWatch\|WindowsForegroundWatch" src tests`
   doit rendre EXACTEMENT ces dix fichiers et rien d'autre.
</verification>

<success_criteria>
- Le graphe DI de production ne contient plus ni source app-bureau, ni observateur de focus, ni poll de fond.
- `SessionMonitor` prend 5 arguments ; `SessionTreatmentTracker.Observe` en prend 2.
- Neuf tests ont disparu, chacun justifié nominativement par la disparition du code qu'il couvrait.
- La solution compile et la suite est verte à chaque commit de ce plan.
</success_criteria>

<output>
Créer `.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-02-SUMMARY.md`.
Y consigner la LISTE NOMINATIVE des 9 tests supprimés avec, pour chacun, le code supprimé qu'il couvrait —
c'est la matière du bilan de couverture du plan 04.
</output>
