---
phase: 21-p-rim-tre-le-widget-ne-parle-que-de-claude-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/TranscriptSessionSource.cs
  - src/Chronos/Services/ISessionSource.cs
  - tests/Chronos.Tests/TranscriptSousAgentsTests.cs
autonomous: true
requirements: [SRC-03]

must_haves:
  truths:
    - "Douze transcripts de sous-agents chauds simultanément ne font plus disparaître la vraie session du widget"
    - "La limite de douze porte sur les sessions RETENUES, jamais sur les fichiers examinés"
    - "Un transcript de sous-agent mal rangé (hors dossier subagents) reste écarté : le champ isSidechain garde l'autorité"
    - "Une erreur d'énumération du dossier des transcripts rend une liste vide au lieu de remonter en exception"
  artifacts:
    - path: "src/Chronos/Services/TranscriptSessionSource.cs"
      provides: "Énumération pré-filtrée des sous-agents + limite appliquée après classification"
      contains: "EstSousAgent"
    - path: "tests/Chronos.Tests/TranscriptSousAgentsTests.cs"
      provides: "Le scénario mesuré (12 sous-agents chauds) rejoué en test"
      contains: "class TranscriptSousAgentsTests"
  key_links:
    - from: "src/Chronos/Services/TranscriptSessionSource.cs"
      to: "la limite MaxSessions"
      via: "un break dans la boucle de classification, plus aucun Take dans l'énumération"
      pattern: "result\\.Count >= MaxSessions"
    - from: "src/Chronos/Services/TranscriptSessionSource.cs"
      to: "ISessionSource"
      via: "déclaration d'implémentation (la signature de Read est déjà exactement celle du contrat)"
      pattern: "TranscriptSessionSource : ISessionSource"
---

<objective>
SRC-03 : la source transcripts cesse de s'aveugler pendant les vagues de sous-agents.

Mesuré le 2026-09-12 sur `~/.claude/projects` : **870 transcripts dont 819 `agent-*.jsonl` (94 %)**, tous
rangés sous `<uuid-de-session>/subagents/`. Le `Take(12)` s'applique **avant** le filtre `isSidechain` :
chaque sous-agent chaud consomme un des douze emplacements puis est jeté par `Classify`. À l'instant de la
mesure, **4 des 5 fichiers chauds étaient des sous-agents** ; avec douze sous-agents chauds, la vraie
session disparaît **complètement**.

Purpose : ce plan est en vague 1 parce qu'il ne touche aucun fichier de la démolition qui suit — et parce
qu'il pose la seule pièce dont le plan 02 a besoin pour ne pas casser à la compilation : la déclaration
`TranscriptSessionSource : ISessionSource`, qui permettra à `SessionMonitor` de prendre sa source de base
par le contrat plutôt que par le type concret.

Output : une source transcripts qui rend jusqu'à douze **sessions**, et quatre tests qui rejouent le
scénario mesuré.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-CONTEXT.md

<interfaces>
<!-- Contrats existants, extraits du dépôt. NE PAS explorer le code pour les retrouver. -->

```csharp
// src/Chronos/Services/ISessionSource.cs — le contrat, déjà présent
public interface ISessionSource
{
    IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);
}

// src/Chronos/Services/TranscriptSessionSource.cs — signature ACTUELLE de Read : DÉJÀ celle du contrat.
// Déclarer l'implémentation ne change donc AUCUN corps de méthode.
public IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now)

// Constantes privées existantes (à ne pas renommer) :
private static readonly System.TimeSpan ActiveWindow = System.TimeSpan.FromMinutes(15);
private const int MaxSessions = 12;
private const int TailBytes = 64 * 1024;
```

```csharp
// src/Chronos/Services/SessionSnapshot.cs — forme ACTUELLE (les deux derniers champs tombent au plan 03 ;
// ce plan ne les touche pas et n'en construit aucun).
public sealed record SessionSnapshot(
    string SessionId, string Project, SessionActivity Activity, string? Reason,
    System.DateTimeOffset UpdatedAt,
    SessionKind Kind = SessionKind.Unknown, SessionOrigin Origin = SessionOrigin.Cli);
```

Agencement RÉEL de `~/.claude/projects`, vérifié le 2026-09-12 :
```
C--Users-…-PROJET-ADVANCED-SHEET/
  4b4e90cb-….jsonl                              ← session RÉELLE (51 fichiers au total)
  4b4e90cb-…/subagents/agent-a0026c4e….jsonl    ← sous-agent (819 fichiers au total)
```

Aides de test déjà écrites dans `tests/Chronos.Tests/SessionsTests.cs` (motif à RECOPIER dans le nouveau
fichier, pas à réutiliser — les membres y sont `private`) :
```csharp
private static string TempDir() { var d = Path.Combine(Path.GetTempPath(), "chronos-sess-" + Guid.NewGuid().ToString("N")); Directory.CreateDirectory(d); return d; }

private static string WriteTranscript(string root, string session, string[] lines, TimeSpan ago)
{
    var projDir = Path.Combine(root, "C--dev-MonProjet");
    Directory.CreateDirectory(projDir);
    var f = Path.Combine(projDir, session + ".jsonl");
    File.WriteAllText(f, string.Join("\n", lines) + "\n");
    File.SetLastWriteTimeUtc(f, DateTime.UtcNow - ago);
    return f;
}

private const string CwdLine = """{"type":"user","cwd":"C:\\dev\\MonProjet","message":{"role":"user","content":[{"type":"text","text":"salut"}]}}""";
private const string AssistantToolUse = """{"type":"assistant","message":{"role":"assistant","stop_reason":"tool_use","content":[{"type":"tool_use","name":"Bash"}]}}""";
private const string AssistantEndTurn = """{"type":"assistant","message":{"role":"assistant","stop_reason":"end_turn","content":[{"type":"text","text":"fini"}]}}""";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : la limite de douze porte sur les sessions retenues, plus sur les fichiers examinés</name>
  <files>src/Chronos/Services/TranscriptSessionSource.cs, tests/Chronos.Tests/TranscriptSousAgentsTests.cs</files>

  <read_first>
    - src/Chronos/Services/TranscriptSessionSource.cs (INTÉGRALEMENT — l. 33-55 `Read`, l. 57-96 `Classify`,
      et surtout la l. 79 `if (o.TryGetProperty("isSidechain", …)) continue;` : c'est le filtre de CONTENU
      qui reste l'autorité, le pré-filtre de chemin n'est qu'une économie)
    - tests/Chronos.Tests/SessionsTests.cs (l. 224-243 : `TempDir`, `WriteTranscript`, `CwdLine`,
      `AssistantToolUse`, `AssistantEndTurn` — le motif à recopier ; l. 246-284 : les trois tests
      `Transcript_*` existants, qui montrent comment on monte une racine temporaire)
    - .planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-CONTEXT.md (section
      « SRC-03 — l'étranglement de la source transcripts »)
  </read_first>

  <behavior>
    - Douze transcripts de sous-agents chauds (sous `<uuid>/subagents/`, écrits il y a 1 min) + une vraie
      session légèrement plus ancienne (2 min) → `Read` rend EXACTEMENT une session, celle de la vraie.
    - Quinze vraies sessions toutes chaudes → `Read` rend EXACTEMENT douze entrées, les douze plus récentes.
    - Un fichier `agent-zzz.jsonl` posé DIRECTEMENT dans le dossier de projet (donc hors `subagents/`), au
      contenu parfaitement normal, est écarté : le préfixe `agent-` suffit.
    - Un fichier au nom quelconque, hors `subagents/`, dont TOUTES les lignes portent `isSidechain: true`
      n'est pas rendu : le filtre de contenu garde l'autorité et ne consomme aucun emplacement.
  </behavior>

  <action>
Écrire d'abord `tests/Chronos.Tests/TranscriptSousAgentsTests.cs`, VOIR LES TESTS ÉCHOUER, puis corriger
`TranscriptSessionSource`.

**1. Le test (nouveau fichier, aucune modification de `SessionsTests.cs`).**

```csharp
using System.IO;
using Chronos.Services;
using Xunit;

namespace Chronos.Tests;

/// <summary>
/// SRC-03 — la source transcripts cesse de s'aveugler pendant les vagues de sous-agents.
///
/// Fait MESURÉ le 2026-09-12 sur ~/.claude/projects : 870 transcripts, dont 819 « agent-*.jsonl »
/// rangés sous « &lt;uuid-de-session&gt;/subagents/ » — 94 %. La limite de douze fichiers était appliquée
/// AVANT le filtre isSidechain : chaque sous-agent chaud consommait un emplacement puis était jeté.
/// 4 des 5 fichiers chauds à l'instant de la mesure étaient des sous-agents.
///
/// Ces tests montent une racine de projets TEMPORAIRE : aucun ne lit le vrai ~/.claude/projects.
/// </summary>
public class TranscriptSousAgentsTests
{
    private static string TempRoot()
    {
        var d = Path.Combine(Path.GetTempPath(), "chronos-souagents-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(d);
        Assert.StartsWith(Path.GetTempPath(), d);   // aucun test n'écrit hors du dossier temporaire
        return d;
    }

    private const string CwdLine = """{"type":"user","cwd":"C:\\dev\\MonProjet","message":{"role":"user","content":[{"type":"text","text":"salut"}]}}""";
    private const string AssistantEndTurn = """{"type":"assistant","message":{"role":"assistant","stop_reason":"end_turn","content":[{"type":"text","text":"fini"}]}}""";
    private const string SousAgentToolUse = """{"type":"assistant","isSidechain":true,"message":{"role":"assistant","stop_reason":"tool_use","content":[{"type":"tool_use","name":"Bash"}]}}""";
    private const string SousAgentUser = """{"type":"user","isSidechain":true,"cwd":"C:\\dev\\MonProjet","message":{"role":"user","content":[{"type":"text","text":"va"}]}}""";

    // Écrit un fichier .jsonl à un CHEMIN RELATIF donné sous la racine, avec une date de dernière
    // écriture choisie (c'est elle que la fenêtre d'activité de 15 min regarde).
    private static void Ecrire(string root, string cheminRelatif, string[] lignes, TimeSpan age)
    {
        var f = Path.Combine(root, cheminRelatif);
        Directory.CreateDirectory(Path.GetDirectoryName(f)!);
        File.WriteAllText(f, string.Join("\n", lignes) + "\n");
        File.SetLastWriteTimeUtc(f, DateTime.UtcNow - age);
    }

    [Fact]
    public void Douze_sous_agents_chauds_ne_font_plus_disparaitre_la_vraie_session()
    {
        var root = TempRoot();
        var now = DateTimeOffset.UtcNow;

        // La vraie session est VOLONTAIREMENT la plus ANCIENNE : avec l'ancien Take(12) appliqué au tri
        // par fraîcheur, les douze sous-agents la repoussaient en 13e position et elle disparaissait.
        for (var i = 0; i < 12; i++)
            Ecrire(root, Path.Combine("C--dev-MonProjet", "sess-uuid", "subagents", $"agent-{i:D2}.jsonl"),
                   new[] { SousAgentUser, SousAgentToolUse }, TimeSpan.FromSeconds(10 + i));
        Ecrire(root, Path.Combine("C--dev-MonProjet", "sess-uuid.jsonl"),
               new[] { CwdLine, AssistantEndTurn }, TimeSpan.FromMinutes(2));

        var snaps = new TranscriptSessionSource(root).Read(now);

        Assert.Single(snaps);
        Assert.Equal("sess-uuid", snaps[0].SessionId);
    }

    [Fact]
    public void La_limite_de_douze_porte_sur_les_sessions_retenues()
    {
        var root = TempRoot();
        var now = DateTimeOffset.UtcNow;

        for (var i = 0; i < 15; i++)
            Ecrire(root, Path.Combine("C--dev-MonProjet", $"vraie-{i:D2}.jsonl"),
                   new[] { CwdLine, AssistantEndTurn }, TimeSpan.FromSeconds(i));

        var snaps = new TranscriptSessionSource(root).Read(now);

        Assert.Equal(12, snaps.Count);                                   // la limite tient
        Assert.Contains(snaps, s => s.SessionId == "vraie-00");          // la plus récente
        Assert.DoesNotContain(snaps, s => s.SessionId == "vraie-14");    // la plus ancienne, écartée
    }

    [Fact]
    public void Un_fichier_agent_hors_dossier_subagents_est_ecarte_par_son_nom()
    {
        var root = TempRoot();
        var now = DateTimeOffset.UtcNow;

        // Contenu PARFAITEMENT normal (aucun isSidechain) : seul le préfixe « agent- » le trahit.
        Ecrire(root, Path.Combine("C--dev-MonProjet", "agent-zzz.jsonl"),
               new[] { CwdLine, AssistantEndTurn }, TimeSpan.FromMinutes(1));

        Assert.Empty(new TranscriptSessionSource(root).Read(now));
    }

    [Fact]
    public void Le_champ_isSidechain_garde_l_autorite_sur_un_fichier_mal_range()
    {
        var root = TempRoot();
        var now = DateTimeOffset.UtcNow;

        // Nom banal, hors « subagents/ » : le pré-filtre de chemin ne le voit pas. Le filtre de CONTENU,
        // lui, le reconnaît — et il ne doit consommer aucun des douze emplacements.
        Ecrire(root, Path.Combine("C--dev-MonProjet", "range-n-importe-ou.jsonl"),
               new[] { SousAgentUser, SousAgentToolUse }, TimeSpan.FromMinutes(1));
        Ecrire(root, Path.Combine("C--dev-MonProjet", "vraie.jsonl"),
               new[] { CwdLine, AssistantEndTurn }, TimeSpan.FromMinutes(2));

        var snaps = new TranscriptSessionSource(root).Read(now);

        Assert.Single(snaps);
        Assert.Equal("vraie", snaps[0].SessionId);
    }
}
```

**2. La correction, dans `src/Chronos/Services/TranscriptSessionSource.cs`.**

Remplacer INTÉGRALEMENT le corps de `Read` (l. 33-55 actuelles) par :

```csharp
    public IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now)
    {
        var result = new List<SessionSnapshot>();
        if (!Directory.Exists(_projectsRoot)) return result;

        List<FileInfo> recents;
        try
        {
            recents = new DirectoryInfo(_projectsRoot)
                .EnumerateFiles("*.jsonl", SearchOption.AllDirectories)
                .Where(f => !EstSousAgent(f))
                .Where(f => now - new System.DateTimeOffset(f.LastWriteTimeUtc, System.TimeSpan.Zero) < ActiveWindow)
                .OrderByDescending(f => f.LastWriteTimeUtc)
                .ToList();   // MATÉRIALISÉ ici, dans le try : l'énumération était paresseuse, donc une erreur
                             // d'accès disque survenait DANS le foreach, hors de ce catch, et remontait.
        }
        catch { return result; }

        foreach (var fi in recents)
        {
            var snap = Classify(fi, now);
            if (snap is null) continue;                 // rien d'exploitable → ne consomme AUCUN emplacement
            result.Add(snap);
            if (result.Count >= MaxSessions) break;     // SRC-03 : la limite porte sur les sessions RETENUES
        }
        return result;
    }

    // SRC-03 — reconnaît un transcript de SOUS-AGENT par son chemin, avant tout I/O de contenu.
    // Mesuré le 2026-09-12 sur la machine cible : 819 des 870 transcripts (94 %) sont des
    // « <uuid-de-session>/subagents/agent-*.jsonl ». Ce pré-filtre est une ÉCONOMIE, pas l'autorité :
    // l'autorité reste le champ isSidechain lu ligne à ligne dans Classify, qui rattrape un fichier
    // mal rangé ou un agencement de dossiers qui changerait chez Anthropic.
    private static bool EstSousAgent(FileInfo f)
        => string.Equals(f.Directory?.Name, "subagents", System.StringComparison.OrdinalIgnoreCase)
           || f.Name.StartsWith("agent-", System.StringComparison.Ordinal);
```

Mettre à jour le commentaire XML de la classe (l. 8-20) : remplacer la phrase d'ouverture
« ce que l'app DESKTOP écrit aussi (pas seulement le terminal). Ne dépend d'AUCUN hook → couvre l'usage
bureau. » par une formulation qui ne promet plus rien sur l'app bureau, et AJOUTER un paragraphe SRC-03 :

```
/// <para>SRC-03 (phase 21) — la limite de <see cref="MaxSessions"/> porte sur les sessions RETENUES, pas
/// sur les fichiers examinés. Auparavant elle était appliquée à l'énumération, donc AVANT le filtre des
/// sous-agents : 94 % des transcripts étant des sous-agents (mesure du 2026-09-12 : 819 sur 870), une
/// vague d'agents parallèles consommait les douze emplacements et faisait disparaître la vraie session.</para>
```

**Ne PAS** toucher à `Classify`, `HasToolUse`, `ReadTail`, ni aux trois constantes.
  </action>

  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --nologo -v q --filter "FullyQualifiedName~TranscriptSousAgentsTests|FullyQualifiedName~SessionsTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "\.Take(MaxSessions)" src/Chronos/Services/TranscriptSessionSource.cs` rend **0**
    - `grep -c "result.Count >= MaxSessions" src/Chronos/Services/TranscriptSessionSource.cs` rend **1**
    - `grep -c "EstSousAgent" src/Chronos/Services/TranscriptSessionSource.cs` rend **2**
      (la déclaration + l'unique appel dans le `Where`)
    - `grep -c "isSidechain" src/Chronos/Services/TranscriptSessionSource.cs` rend **au moins 1** —
      le filtre de contenu de `Classify` est INTACT, il n'est pas remplacé par le pré-filtre de chemin
    - `grep -c "private const int MaxSessions = 12" src/Chronos/Services/TranscriptSessionSource.cs` rend **1**
      (la valeur ne change pas ; seul son point d'application change)
    - Les 4 tests de `TranscriptSousAgentsTests` passent
    - Les 11 tests préexistants de `SessionsTests` passent sans une seule retouche de ce fichier :
      `git diff --name-only` ne contient PAS `tests/Chronos.Tests/SessionsTests.cs`
  </acceptance_criteria>

  <done>
Une vague de douze sous-agents chauds ne fait plus disparaître la session réelle, et le scénario mesuré le
2026-09-12 est rejoué par un test qui échouerait si la limite repassait avant le filtre.
  </done>
</task>

<task type="auto">
  <name>Task 2 : TranscriptSessionSource porte le contrat ISessionSource</name>
  <files>src/Chronos/Services/TranscriptSessionSource.cs, src/Chronos/Services/ISessionSource.cs</files>

  <read_first>
    - src/Chronos/Services/ISessionSource.cs (INTÉGRALEMENT — 14 lignes ; son commentaire annonce
      « transcripts CLI, app bureau UIA, … », moitié qui devient fausse à la fin de cette phase)
    - src/Chronos/Services/TranscriptSessionSource.cs (la ligne de déclaration de classe et la signature
      de `Read`, modifiées en tâche 1)
  </read_first>

  <action>
Deux modifications, aucune ligne de corps de méthode touchée.

**1. `src/Chronos/Services/TranscriptSessionSource.cs`** — déclarer l'implémentation :

```csharp
public sealed class TranscriptSessionSource : ISessionSource
```

La signature de `Read` est DÉJÀ exactement celle du contrat (`IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now)`) :
aucun `override`, aucun renommage, aucun changement de visibilité n'est nécessaire.

**Pourquoi ici, dans un plan SRC-03, et pas dans la démolition qui suit** — à écrire en commentaire d'une
ligne au-dessus de la déclaration :

```csharp
// Le contrat ISessionSource n'avait qu'une seule implémentation, la source app-bureau, qui disparaît en
// phase 21. Le porter ici évite deux choses : une interface orpheline, et — c'est le point mécanique —
// un SessionMonitor qui dépendrait du TYPE CONCRET de sa source de base, donc intestable par substitution.
```

**2. `src/Chronos/Services/ISessionSource.cs`** — corriger le commentaire XML, qui promet une source
app-bureau qui n'existera plus. Remplacer les l. 5-9 par :

```csharp
/// <summary>
/// Contrat COMMUN d'une source de sessions. <see cref="Read"/> retourne l'instantané courant des sessions
/// connues, de façon NON bloquante ; les sources sont fusionnées dans <c>SessionMonitor</c>.
///
/// <para>Phase 21 — le périmètre du widget est Claude Code, et lui seul. La seule implémentation de
/// production est <see cref="TranscriptSessionSource"/> (lecture des transcripts JSONL). L'interface est
/// conservée parce qu'elle est le point de substitution des tests du moniteur, pas parce qu'on prévoit
/// d'autres sources.</para>
/// </summary>
```

**Ne PAS** modifier la signature de `Read` ni le commentaire de la méthode.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "class TranscriptSessionSource : ISessionSource" src/Chronos/Services/TranscriptSessionSource.cs` rend **1**
    - `grep -ci "app bureau UIA" src/Chronos/Services/ISessionSource.cs` rend **0**
    - `grep -c "TranscriptSessionSource" src/Chronos/Services/ISessionSource.cs` rend **1**
      (la nouvelle référence `<see cref="…"/>`)
    - `dotnet build Chronos.sln -c Debug` réussit : la signature de `Read` satisfaisait DÉJÀ le contrat,
      aucun site d'appel existant n'est cassé
    - La suite complète est verte — **0 échec**. Le total attendu à ce stade est **756** (752 + les 4 tests
      de la tâche 1) ; s'il diffère, c'est un signal à instruire, pas une valeur à ajuster
  </acceptance_criteria>

  <done>
`SessionMonitor` pourra prendre sa source de base par le contrat au plan 02, et `ISessionSource` ne restera
pas une interface sans implémentation après la démolition.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, 756 tests.
2. `grep -rn "Take(MaxSessions)" src/` → aucun résultat.
3. Contrôle de non-régression du coût : la fenêtre d'activité de 15 min borne toujours l'ensemble classifié
   AVANT la boucle. Sur la machine cible, elle ramenait 5 fichiers chauds sur 870 ; le pré-filtre de chemin
   en retire d'office 94 %.
</verification>

<success_criteria>
- Le scénario mesuré (12 sous-agents chauds) laisse la vraie session visible — prouvé par test.
- La limite de douze est intacte en valeur, déplacée en point d'application.
- Le filtre `isSidechain` de `Classify` n'a pas été remplacé par le pré-filtre de chemin : il le double.
- `TranscriptSessionSource` implémente `ISessionSource` ; la solution compile et la suite est verte.
</success_criteria>

<output>
Créer `.planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-01-SUMMARY.md`.
Y consigner le nombre de tests exact après ce plan (attendu 756) — les plans 02 à 04 s'en servent comme
point de départ du bilan nominatif de couverture.
</output>
