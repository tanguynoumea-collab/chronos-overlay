---
phase: 01-fondations-architecture-squelette-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Chronos.sln
  - .gitignore
  - src/Chronos/Chronos.csproj
  - src/Chronos/app.manifest
  - src/Chronos/App.xaml
  - src/Chronos/App.xaml.cs
  - src/Chronos/Services/IUiDispatcher.cs
  - src/Chronos/Services/WpfUiDispatcher.cs
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/Views/MainWindow.xaml
  - src/Chronos/Views/MainWindow.xaml.cs
  - tests/Chronos.Tests/Chronos.Tests.csproj
  - tests/Chronos.Tests/CompositionRootTests.cs
  - tests/Chronos.Tests/OverlayWindowConfigTests.cs
autonomous: true
requirements: [FEN-01]

must_haves:
  truths:
    - "Au lancement, une fenêtre carrée borderless, transparente et sans entrée dans la barre des tâches apparaît sur le bureau (placeholder sombre visible)"
    - "La fenêtre ne vole pas le focus au démarrage (ShowActivated=False, Show() sans Activate)"
    - "Le host résout MainWindow et MainViewModel par DI sans exception et dispose les Singletons IDisposable à la fermeture"
  artifacts:
    - path: "src/Chronos/Chronos.csproj"
      provides: "Projet WPF net8.0-windows, UseWPF, propriétés de publish conditionnées"
      contains: "net8.0-windows"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Composition root Generic Host (pas de StartupUri)"
      contains: "GetRequiredService<MainWindow>"
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "Fenêtre overlay conforme FEN-01 + placeholder"
      contains: "AllowsTransparency=\"True\""
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "ViewModel racine ObservableObject"
      contains: "ObservableObject"
    - path: "src/Chronos/Services/IUiDispatcher.cs"
      provides: "Abstraction de marshaling UI (couche Services sans type WPF)"
      exports: ["CheckAccess", "Post"]
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "src/Chronos/Views/MainWindow.xaml.cs"
      via: "GetRequiredService<MainWindow>() puis Show()"
      pattern: "GetRequiredService<MainWindow>"
    - from: "src/Chronos/Views/MainWindow.xaml.cs"
      to: "src/Chronos/ViewModels/MainViewModel.cs"
      via: "injection ctor + DataContext"
      pattern: "DataContext = viewModel"
---

<objective>
Poser la fondation buildable de Chronos : la solution, le projet applicatif WPF `net8.0-windows`,
le projet de tests xUnit, la composition root Generic Host dans `App` (sans `StartupUri`), les
abstractions de la couche Services, et la fenêtre overlay conforme FEN-01 (borderless, transparente,
topmost, hors barre des tâches, sans vol de focus, carrée avec placeholder visible).

Purpose : sans ce squelette câblé par DI, aucune phase ultérieure ne peut se poser. Cette phase livre
un squelette qui compile, une fenêtre observable, et un cycle de vie DI possédé/disposé (success criterion 3).
Output : arborescence `src/Chronos/` + `tests/Chronos.Tests/`, app qui affiche une fenêtre overlay vide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fondations-architecture-squelette-overlay/01-CONTEXT.md
@.planning/phases/01-fondations-architecture-squelette-overlay/01-RESEARCH.md
@.planning/phases/01-fondations-architecture-squelette-overlay/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md

Note : le RESEARCH.md contient le code de référence VERBATIM de chaque fichier (section « Code Examples »).
L'exécuteur transcrit ce code, il n'invente rien. Skills UI à activer sur la tâche fenêtre : `frontend-design` + `windows-wpf`.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Scaffold solution + projet applicatif + projet de tests (squelette compilable)</name>
  <files>Chronos.sln, .gitignore, src/Chronos/Chronos.csproj, src/Chronos/app.manifest, src/Chronos/App.xaml, src/Chronos/App.xaml.cs, tests/Chronos.Tests/Chronos.Tests.csproj</files>
  <read_first>
    - .planning/research/STACK.md (csproj autoritatif, packages, propriétés de publish conditionnées)
    - .planning/phases/01-fondations-architecture-squelette-overlay/01-RESEARCH.md (sections « Structure de solution physique », « Chronos.csproj », « app.manifest », « App.xaml »)
    - CLAUDE.md (conventions : dossiers Models/Views/ViewModels/Services, commentaires en français)
  </read_first>
  <action>
    Créer l'arborescence `src/Chronos/` + `tests/Chronos.Tests/` sous la racine du dépôt, et la rendre compilable.

    1. `.gitignore` (racine) : gabarit VisualStudio/.NET a minima `bin/`, `obj/`, `.vs/`, `*.user`.

    2. `src/Chronos/Chronos.csproj` — VERBATIM depuis RESEARCH.md § « Chronos.csproj » :
    ```xml
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <OutputType>WinExe</OutputType>
        <TargetFramework>net8.0-windows</TargetFramework>
        <UseWPF>true</UseWPF>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <LangVersion>latest</LangVersion>
        <RootNamespace>Chronos</RootNamespace>
        <AssemblyName>Chronos</AssemblyName>
        <ApplicationManifest>app.manifest</ApplicationManifest>
      </PropertyGroup>
      <!-- Propriétés de PUBLICATION uniquement (Phase 7) — jamais actives en build/debug -->
      <PropertyGroup Condition="'$(PublishSingleFile)' == 'true'">
        <SelfContained>true</SelfContained>
        <RuntimeIdentifier>win-x64</RuntimeIdentifier>
        <PublishSingleFile>true</PublishSingleFile>
        <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
        <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
        <PublishTrimmed>false</PublishTrimmed>
      </PropertyGroup>
      <ItemGroup>
        <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
        <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.1" />
      </ItemGroup>
    </Project>
    ```
    IMPORTANT : les propriétés de publish DOIVENT rester dans le `<PropertyGroup Condition="'$(PublishSingleFile)' == 'true'">` conditionné, JAMAIS dans le PropertyGroup inconditionnel (décision verrouillée — sinon build/debug lents). Au restore, confirmer le dernier patch `8.0.x` de `Microsoft.Extensions.Hosting` (8.0.1 attendu).

    3. `src/Chronos/app.manifest` — VERBATIM depuis RESEARCH.md § « app.manifest » (DPI PerMonitorV2 + supportedOS Win10/11).

    4. Créer les dossiers de la structure MVVM avec un `.gitkeep` dans chacun encore vide : `src/Chronos/Models/`, `src/Chronos/Services/`, `src/Chronos/Interop/`, `src/Chronos/ViewModels/`, `src/Chronos/Views/`, `src/Chronos/Controls/`, `src/Chronos/Converters/`.

    5. `src/Chronos/App.xaml` — VERBATIM RESEARCH.md § « App.xaml » : `<Application x:Class="Chronos.App" ...>` AVEC `<Application.Resources/>` et SANS `StartupUri` (décision verrouillée : la fenêtre est résolue par la composition root, pas par StartupUri).

    6. `src/Chronos/App.xaml.cs` — version MINIMALE compilable pour cette tâche (le corps complet du cycle de vie sera rempli en Task 3) :
    ```csharp
    using System.Windows;
    using Microsoft.Extensions.Hosting;

    namespace Chronos;

    public partial class App : Application
    {
        private IHost _host = null!;
        // Le cycle de vie complet (build host, StartAsync, résolution MainWindow, dispose) est câblé en Task 3.
    }
    ```

    7. `tests/Chronos.Tests/Chronos.Tests.csproj` : projet `net8.0-windows`, `<UseWPF>true</UseWPF>` (tests STA construisent des `Window`), `IsPackable=false`, référence `<ProjectReference Include="..\..\src\Chronos\Chronos.csproj" />`, packages `Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, `Xunit.StaFact` (confirmer la dernière version de chaque au restore).

    8. Créer `Chronos.sln` à la racine et y ajouter les deux projets :
    `dotnet new sln -n Chronos` puis `dotnet sln add src/Chronos/Chronos.csproj tests/Chronos.Tests/Chronos.Tests.csproj`.

    Commentaires de code en français (convention CLAUDE.md).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` se termine avec « Build succeeded » et 0 erreur.
    - `grep -q "net8.0-windows" src/Chronos/Chronos.csproj` retourne vrai.
    - `grep -q "Condition=\"'\$(PublishSingleFile)' == 'true'\"" src/Chronos/Chronos.csproj` retourne vrai (publish conditionné).
    - `grep -q "StartupUri" src/Chronos/App.xaml` retourne FAUX (StartupUri absent).
    - `grep -q "PerMonitorV2" src/Chronos/app.manifest` retourne vrai.
    - `dotnet sln Chronos.sln list` liste `src/Chronos/Chronos.csproj` et `tests/Chronos.Tests/Chronos.Tests.csproj`.
    - Les 7 dossiers MVVM existent sous `src/Chronos/`.
  </acceptance_criteria>
  <done>La solution compile en Debug ; les deux projets sont dans la sln ; le csproj est correct dès le départ (TFM net8.0-windows, publish conditionné) ; App.xaml n'a pas de StartupUri.</done>
</task>

<task type="auto">
  <name>Task 2 : Abstractions Services + ViewModel + fenêtre overlay conforme (FEN-01)</name>
  <files>src/Chronos/Services/IUiDispatcher.cs, src/Chronos/Services/WpfUiDispatcher.cs, src/Chronos/ViewModels/MainViewModel.cs, src/Chronos/Views/MainWindow.xaml, src/Chronos/Views/MainWindow.xaml.cs</files>
  <read_first>
    - .planning/phases/01-fondations-architecture-squelette-overlay/01-RESEARCH.md (sections « Pattern 2 IUiDispatcher », « Pattern 3 Fenêtre overlay », « Code Examples » IUiDispatcher/WpfUiDispatcher/MainWindow.xaml/MainViewModel)
    - .planning/research/ARCHITECTURE.md (Pattern 4 marshaling, structure interne Chronos)
    - src/Chronos/Chronos.csproj (confirmer que CommunityToolkit.Mvvm est référencé)
    - CLAUDE.md (skills UI : frontend-design + windows-wpf ; UI et commentaires en français)
  </read_first>
  <action>
    Tâche UI — activer les skills `frontend-design` et `windows-wpf`.

    1. `src/Chronos/Services/IUiDispatcher.cs` — contrat minimal, AUCUN type WPF dans la signature (VERBATIM RESEARCH.md) :
    ```csharp
    namespace Chronos.Services;

    /// <summary>Point unique de franchissement vers le thread UI (testable, sans type WPF côté Services).</summary>
    public interface IUiDispatcher
    {
        bool CheckAccess();
        void Post(Action action);
    }
    ```

    2. `src/Chronos/Services/WpfUiDispatcher.cs` — impl WPF (wrap `Dispatcher`), VERBATIM RESEARCH.md : ctor prend un `System.Windows.Threading.Dispatcher`, `CheckAccess()` délègue, `Post()` fait `if (_dispatcher.CheckAccess()) action(); else _dispatcher.BeginInvoke(action);`.

    3. `src/Chronos/ViewModels/MainViewModel.cs` — quasi vide en Phase 1 (VERBATIM RESEARCH.md) :
    ```csharp
    using CommunityToolkit.Mvvm.ComponentModel;

    namespace Chronos.ViewModels;

    /// <summary>ViewModel racine. Vide en Phase 1 (le pipeline données arrive en Phases 3-4).</summary>
    public sealed partial class MainViewModel : ObservableObject
    {
    }
    ```

    4. `src/Chronos/Views/MainWindow.xaml` — overlay conforme FEN-01, VERBATIM RESEARCH.md § « MainWindow.xaml ». La combinaison EXACTE (l'oubli d'un membre casse l'overlay) :
    ```xml
    <Window x:Class="Chronos.Views.MainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            WindowStyle="None" AllowsTransparency="True" Background="Transparent"
            Topmost="True" ShowInTaskbar="False" ShowActivated="False"
            ResizeMode="NoResize" SizeToContent="Manual"
            Width="220" Height="220" WindowStartupLocation="Manual">
        <!-- Placeholder visible de l'empreinte du cadran (remplacé en Phase 5).
             Pas d'ombre/blur/animation : AllowsTransparency force le rendu logiciel. -->
        <Grid>
            <Ellipse Width="200" Height="200" Fill="#CC1E1E1E"/>
        </Grid>
    </Window>
    ```
    Ne PAS ajouter d'ombre, blur ou animation (AllowsTransparency force le rendu logiciel). `Background="Transparent"` (jamais `null` : garde le hit-test).

    5. `src/Chronos/Views/MainWindow.xaml.cs` — code-behind : ctor prend `MainViewModel` par injection, fixe `DataContext = viewModel` (MVVM). NE PAS injecter de TopmostGuard ici (arrive en Plan 02). Ajouter un placement de départ simple non persisté (coin supérieur droit) via `SystemParameters.WorkArea` dans un handler `Loaded`, avec une marge de 24 px :
    ```csharp
    using System.Windows;
    using Chronos.ViewModels;

    namespace Chronos.Views;

    public partial class MainWindow : Window
    {
        public MainWindow(MainViewModel viewModel)
        {
            InitializeComponent();
            DataContext = viewModel;          // MVVM : la vue reçoit son VM par injection
            Loaded += PlacerCoinSuperieurDroit;
        }

        // Placement de départ observable, non persisté (la persistance/snap = Phase 6).
        private void PlacerCoinSuperieurDroit(object sender, RoutedEventArgs e)
        {
            var zone = SystemParameters.WorkArea;
            Left = zone.Right - Width - 24;
            Top = zone.Top + 24;
        }
    }
    ```
    Commentaires en français.
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` : « Build succeeded », 0 erreur.
    - `grep -q "WindowStyle=\"None\"" src/Chronos/Views/MainWindow.xaml` retourne vrai.
    - `grep -q "AllowsTransparency=\"True\"" src/Chronos/Views/MainWindow.xaml` retourne vrai.
    - `grep -q "Background=\"Transparent\"" src/Chronos/Views/MainWindow.xaml` retourne vrai.
    - `grep -q "Topmost=\"True\"" src/Chronos/Views/MainWindow.xaml` retourne vrai.
    - `grep -q "ShowInTaskbar=\"False\"" src/Chronos/Views/MainWindow.xaml` retourne vrai.
    - `grep -q "ShowActivated=\"False\"" src/Chronos/Views/MainWindow.xaml` retourne vrai.
    - `grep -q "DataContext = viewModel" src/Chronos/Views/MainWindow.xaml.cs` retourne vrai.
    - `grep -q "ObservableObject" src/Chronos/ViewModels/MainViewModel.cs` retourne vrai.
    - `IUiDispatcher.cs` ne contient aucun `using System.Windows` (couche Services neutre) : `grep -q "System.Windows" src/Chronos/Services/IUiDispatcher.cs` retourne FAUX.
  </acceptance_criteria>
  <done>La fenêtre overlay porte les 6 propriétés FEN-01 ; MainWindow reçoit son VM par injection ; IUiDispatcher est un contrat neutre sans type WPF ; tout compile.</done>
</task>

<task type="auto">
  <name>Task 3 : Câblage composition root Generic Host complet + tests (SC3 + FEN-01)</name>
  <files>src/Chronos/App.xaml.cs, tests/Chronos.Tests/CompositionRootTests.cs, tests/Chronos.Tests/OverlayWindowConfigTests.cs</files>
  <read_first>
    - src/Chronos/App.xaml.cs (état minimal de Task 1 — à compléter, ne pas repartir de zéro)
    - .planning/phases/01-fondations-architecture-squelette-overlay/01-RESEARCH.md (sections « Pattern 1 séquence de vie », « App.xaml.cs Code Example », « Validation Architecture » détails des tests)
    - src/Chronos/Views/MainWindow.xaml.cs (signature du ctor : prend MainViewModel)
    - src/Chronos/Services/IUiDispatcher.cs, src/Chronos/Services/WpfUiDispatcher.cs (types à enregistrer)
  </read_first>
  <action>
    1. Compléter `src/Chronos/App.xaml.cs` avec le cycle de vie Generic Host COMPLET (VERBATIM RESEARCH.md § « App.xaml.cs ») :
    ```csharp
    using System.Windows;
    using Chronos.Services;
    using Chronos.ViewModels;
    using Chronos.Views;
    using Microsoft.Extensions.DependencyInjection;
    using Microsoft.Extensions.Hosting;

    namespace Chronos;

    public partial class App : Application
    {
        private IHost _host = null!;

        protected override async void OnStartup(StartupEventArgs e)
        {
            base.OnStartup(e);

            var builder = Host.CreateApplicationBuilder();
            ConfigureServices(builder.Services);
            _host = builder.Build();

            await _host.StartAsync();                    // démarre les IHostedService (aucun en Phase 1)

            var window = _host.Services.GetRequiredService<MainWindow>();
            window.Show();                               // ShowActivated=False (XAML) → pas de vol de focus
        }

        protected override void OnExit(ExitEventArgs e)
        {
            // Blocage volontaire : dispose déterministe des Singletons IDisposable
            // (évite le piège async-void qui n'attend pas StopAsync).
            _host.StopAsync().GetAwaiter().GetResult();
            _host.Dispose();
            base.OnExit(e);
        }

        private static void ConfigureServices(IServiceCollection services)
        {
            services.AddSingleton<IUiDispatcher>(_ => new WpfUiDispatcher(Current.Dispatcher));
            services.AddSingleton<MainViewModel>();
            services.AddSingleton<MainWindow>();
        }
    }
    ```
    NB : `TopmostGuard` n'est PAS encore enregistré ici (Plan 02, ROB-04). `MainWindow` et `MainViewModel` en Singleton (fenêtre unique). Retrait de StartupUri déjà acté (App.xaml).

    2. `tests/Chronos.Tests/OverlayWindowConfigTests.cs` — `[WpfFact]` (Xunit.StaFact, contexte STA) : construire `new MainWindow(new MainViewModel())` sur le thread STA et asserter chaque propriété FEN-01 :
    `WindowStyle == WindowStyle.None`, `AllowsTransparency == true`, `Topmost == true`, `ShowInTaskbar == false`, `ShowActivated == false`, `ResizeMode == ResizeMode.NoResize`. Utiliser `[WpfFact]` de `Xunit.StaFact` (namespace `Xunit`).

    3. `tests/Chronos.Tests/CompositionRootTests.cs` — `[WpfFact]` : reproduire les enregistrements de `ConfigureServices` dans un `ServiceCollection` de test (STA pour construire la `Window`), enregistrer EN PLUS un `IDisposable` marqueur (classe locale exposant un `bool Disposed`), `BuildServiceProvider()`, puis :
    - `provider.GetRequiredService<MainWindow>()` et `provider.GetRequiredService<MainViewModel>()` ne lèvent pas (preuve de résolution — success criterion 3 partie « lance ») ;
    - récupérer le marqueur, appeler `provider.Dispose()`, asserter `marqueur.Disposed == true` (preuve de disposition — success criterion 3 partie « ferme proprement »).
    Enregistrer `IUiDispatcher` via `AddSingleton<IUiDispatcher>(_ => new WpfUiDispatcher(...))` — sur STA, `System.Windows.Threading.Dispatcher.CurrentDispatcher` fournit un dispatcher valide.

    Commentaires en français. Chaque test porte un nom explicite (ex. `Fenetre_expose_les_proprietes_overlay`, `Host_resout_et_dispose_les_singletons`).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug && dotnet test tests/Chronos.Tests -c Debug --filter "FullyQualifiedName~OverlayWindowConfig|FullyQualifiedName~CompositionRoot"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` : « Build succeeded ».
    - `dotnet test ... --filter "FullyQualifiedName~OverlayWindowConfig|FullyQualifiedName~CompositionRoot"` : tous les tests passent (Passed! avec Failed: 0).
    - `grep -q "GetRequiredService<MainWindow>" src/Chronos/App.xaml.cs` retourne vrai.
    - `grep -q "AddSingleton<MainWindow>" src/Chronos/App.xaml.cs` retourne vrai.
    - `grep -q "StopAsync().GetAwaiter().GetResult()" src/Chronos/App.xaml.cs` retourne vrai (dispose déterministe).
    - `grep -q "WpfFact" tests/Chronos.Tests/OverlayWindowConfigTests.cs` retourne vrai.
    - `grep -q "Disposed" tests/Chronos.Tests/CompositionRootTests.cs` retourne vrai (preuve de disposition).
  </acceptance_criteria>
  <done>La composition root résout MainWindow/MainViewModel par DI et les dispose à la sortie ; les tests OverlayWindowConfig (FEN-01) et CompositionRoot (SC3) sont verts.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit.
- `dotnet test tests/Chronos.Tests -c Debug --filter "FullyQualifiedName~OverlayWindowConfig|FullyQualifiedName~CompositionRoot"` : tous verts.
- App.xaml sans StartupUri ; App.xaml.cs = composition root Generic Host (StartAsync → GetRequiredService<MainWindow> → Show ; OnExit StopAsync + Dispose bloquant).
- MainWindow porte les 6 propriétés FEN-01 + placeholder visible ; IUiDispatcher sans type WPF.
</verification>

<success_criteria>
- Le squelette compile et les deux projets sont dans `Chronos.sln`.
- La fenêtre overlay est conforme FEN-01 (borderless, transparente, topmost, hors barre des tâches, non activée, carrée, placeholder visible).
- Le host résout et dispose les Singletons (success criterion 3 automatiquement prouvé par CompositionRootTests).
- `dotnet run --project src/Chronos` affiche la fenêtre (vérification visuelle en Plan 03).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondations-architecture-squelette-overlay/01-01-SUMMARY.md`.
</output>
