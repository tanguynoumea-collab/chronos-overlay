---
phase: 01-fondations-architecture-squelette-overlay
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: [FEN-01, ROB-04]

must_haves:
  truths:
    - "Une fenêtre carrée sombre sans bordure apparaît sur le bureau, sans entrée dans la barre des tâches ni dans Alt+Tab"
    - "L'overlay ne prend pas le focus au démarrage : l'application active au lancement le reste"
    - "L'overlay reste au premier plan après ouverture d'une autre fenêtre par-dessus (réaffirmation 2 s)"
    - "L'application se ferme proprement, aucun processus Chronos résiduel"
  artifacts: []
  key_links: []
---

<objective>
Valider par observation humaine les deux success criteria intrinsèquement visuels de la Phase 1 :
l'apparence/comportement de la fenêtre overlay (FEN-01) et la persistance du premier plan dans le
temps (ROB-04). Les tests automatisés couvrent déjà les propriétés et les flags P/Invoke ; ce gate
couvre ce qui n'est pas automatisable de façon fiable (rendu réel, Z-order dans le temps, focus).

Purpose : le RESEARCH assume honnêtement que « reste au-dessus après ouverture d'autres fenêtres » et
« n'apparaît pas dans la barre des tâches » se vérifient à l'œil. Ce plan isole ce contrôle manuel du
travail d'implémentation (les plans 01 et 02 restent autonomes).
Output : phase gate coché avant `/gsd:verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-fondations-architecture-squelette-overlay/01-VALIDATION.md
@.planning/phases/01-fondations-architecture-squelette-overlay/01-01-SUMMARY.md
@.planning/phases/01-fondations-architecture-squelette-overlay/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 : Smoke test visuel de l'overlay (FEN-01 + ROB-04 + SC3)</name>
  <files>aucun (plan de vérification — aucune modification de fichier)</files>
  <what-built>
    Un squelette WPF complet : composition root Generic Host (App.xaml.cs sans StartupUri), fenêtre
    overlay borderless/transparente/topmost carrée avec placeholder sombre, et réaffirmation périodique
    du Topmost (TopmostGuard, ~2 s, sans vol de focus). Les tests OverlayWindowConfig, CompositionRoot
    et TopmostGuard sont verts.
  </what-built>
  <action>
    Aucune écriture de code — vérification manuelle. Exécuter le smoke test décrit dans how-to-verify :
    lancer l'app, observer les 4 comportements (fenêtre borderless hors barre des tâches, pas de vol de
    focus au démarrage, persistance au premier plan après 3 s, fermeture propre sans processus résiduel),
    puis renvoyer le verdict via le resume-signal.
  </action>
  <how-to-verify>
    Préalable : `dotnet test Chronos.sln -c Debug` doit être vert (suite complète), puis lancer l'app.

    1. Lancer : `dotnet run --project src/Chronos`.
       Attendu (Succès #1 / FEN-01) : une fenêtre CARRÉE SOMBRE (cercle placeholder) apparaît sur le
       bureau, SANS bordure ni barre de titre, dans le coin supérieur droit. Vérifier qu'elle
       N'APPARAÎT PAS dans la barre des tâches, ni dans la liste Alt+Tab.

    2. Au moment du lancement, garder le focus sur une autre application (ex. l'éditeur/terminal courant).
       Attendu (Succès #2 / focus) : l'overlay NE PREND PAS le focus ; l'application active le reste
       (le curseur/la sélection de l'app précédente n'est pas volé).

    3. Ouvrir une fenêtre quelconque (Explorateur, navigateur) PAR-DESSUS l'overlay, attendre ≥ 3 s.
       Attendu (Succès #2 / ROB-04) : l'overlay REVIENT / RESTE au premier plan (réaffirmation 2 s),
       sans faire clignoter ni voler le focus de la fenêtre ouverte.

    4. Fermer l'application (fermer le processus / Ctrl+C dans le terminal `dotnet run`).
       Attendu (Succès #3) : arrêt propre, AUCUN processus `Chronos` résiduel
       (vérifier via `tasklist | findstr Chronos` → aucune ligne, ou le Gestionnaire des tâches).

    Note : un jeu/vidéo en plein écran EXCLUSIF peut légitimement passer devant — c'est une limite
    Windows, pas un défaut (documenté dans PITFALLS). Ne pas tester ce cas comme un échec.
  </how-to-verify>
  <verify>Les 4 points de la checklist how-to-verify sont observés conformes ; `dotnet test Chronos.sln -c Debug` est vert au préalable.</verify>
  <done>L'utilisateur a tapé « approved » (les 4 comportements sont conformes) ; la phase est prête pour /gsd:verify-work.</done>
  <resume-signal>Taper « approved » si les 4 points sont conformes, sinon décrire précisément l'écart observé (point concerné + comportement réel).</resume-signal>
</task>

</tasks>

<verification>
- Les 4 points de la checklist smoke sont conformes.
- La suite `dotnet test Chronos.sln -c Debug` est verte au préalable.
</verification>

<success_criteria>
- Success criterion 1 (fenêtre borderless/transparente hors barre des tâches) : confirmé visuellement.
- Success criterion 2 (reste au-dessus, pas de vol de focus) : confirmé visuellement.
- Success criterion 3 (lancement/fermeture propres) : confirmé (aucun processus résiduel) + prouvé par CompositionRootTests.
- La Phase 1 est prête pour `/gsd:verify-work`.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondations-architecture-squelette-overlay/01-03-SUMMARY.md`.
</output>
