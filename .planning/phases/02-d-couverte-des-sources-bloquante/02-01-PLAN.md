---
phase: 02-d-couverte-des-sources-bloquante
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/data-sources.md
autonomous: true
requirements: [DAT-01]

must_haves:
  truths:
    - "Un lecteur de docs/data-sources.md sait exactement où se trouve l'objet d'usage : le bloc `rate_limits` du contrat JSON statusLine de Claude Code, et sait qu'aucun fichier ne le persiste sur disque (accès via un pont statusLine → fichier)"
    - "Le schéma de la source primaire est documenté sans ambiguïté : five_hour/seven_day avec used_percentage (0..100, PAS 0..1) et resets_at (Unix epoch SECONDES, PAS ISO ni millisecondes), avec un échantillon réel anonymisé"
    - "La source de repli JSONL est documentée : localisation ~/.claude/projects/<slug>/<uuid>.jsonl, objet message.usage (input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens), timestamp ISO 8601 UTC, layout subagents/ en v2.1.202"
    - "Le mapping vers UsageSnapshot (Phase 3) est explicite : used_percentage/100 → Utilization ; resets_at epoch s → ResetsAt (DateTimeOffset.FromUnixTimeSeconds)"
    - "Les hypothèses et points de fragilité sont consignés pour guider IUsageProvider : API privée de facto, écart de version 2.1.87 disque / 2.1.202 runtime, staleness hors session active, conditions de présence Pro/Max + absence indépendante des fenêtres, reset hebdo dérivant"
    - "Les échantillons du document sont anonymisés : aucun token, UUID réel, identifiant de compte ou contenu de conversation"
  artifacts:
    - path: "docs/data-sources.md"
      provides: "Documentation empirique complète des sources de données (primaire statusLine/rate_limits + repli JSONL), livrable bloquant DAT-01"
      contains: "rate_limits"
      min_lines: 90
  key_links:
    - from: "docs/data-sources.md (source primaire, champ used_percentage 0..100)"
      to: "UsageSnapshot.Utilization (Phase 3)"
      via: "conversion documentée used_percentage / 100.0"
      pattern: "used_percentage"
    - from: "docs/data-sources.md (source primaire, champ resets_at epoch s)"
      to: "UsageSnapshot.ResetsAt (Phase 3)"
      via: "conversion documentée DateTimeOffset.FromUnixTimeSeconds"
      pattern: "resets_at"
---

<objective>
Produire `docs/data-sources.md` : la documentation empirique de la méthode d'obtention de l'objet
d'usage Claude Code (five_hour/seven_day : used_percentage + resets_at), préalable STRICT et BLOQUANT
à tout code de provider (Phase 3). Cette phase est DOCUMENTAIRE — le livrable est un document, PAS du
code de provider. Aucun provider, aucune classe C#, aucun script ne doit être écrit ici.

Tout le contenu factuel a déjà été découvert et est consigné dans `02-RESEARCH.md` (la source d'usage a
été localisée : bloc `rate_limits` du contrat statusLine documenté ; le repli JSONL a été échantillonné
réellement). L'exécuteur STRUCTURE et RÉDIGE le document à partir de RESEARCH.md — il ne redécouvre rien.

Purpose : sans cette caractérisation écrite et vérifiée de la source (localisation, schéma corrigé,
mécanisme d'accès, fragilités), l'abstraction IUsageProvider de la Phase 3 se bâtirait sur des hypothèses
fausses (le champ `utilization` 0..1 n'existe pas — c'est `used_percentage` 0..100).
Output : `docs/data-sources.md` (français), couvrant les 3 Success Criteria de la phase et DAT-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-d-couverte-des-sources-bloquante/02-CONTEXT.md
@.planning/phases/02-d-couverte-des-sources-bloquante/02-RESEARCH.md
@.planning/phases/02-d-couverte-des-sources-bloquante/02-VALIDATION.md
@CLAUDE.md

Note : `02-RESEARCH.md` contient TOUT le contenu factuel (schéma statusLine verbatim, échantillon JSONL réel
anonymisé, corrections de champs, alternatives évaluées, pitfalls). L'exécuteur transcrit et structure ce
contenu ; il n'invente aucune donnée et ne relance aucune investigation. Document et échantillons en français,
échantillons anonymisés (valeurs synthétiques plausibles). Lecture seule stricte sous ~/.claude si une
re-vérification ponctuelle est souhaitée — ne JAMAIS modifier un fichier de ~/.claude, ne jamais lire
`.credentials.json`, ne jamais logger de contenu de conversation.
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Rédiger la source primaire (rate_limits/statusLine), la source de repli (JSONL) et le mapping UsageSnapshot</name>
  <files>docs/data-sources.md</files>
  <read_first>
    - .planning/phases/02-d-couverte-des-sources-bloquante/02-RESEARCH.md (sections « Summary », « Sondage empirique réalisé » §3 schéma statusLine + §4 échantillon JSONL, « Architecture Patterns » structure imposée + Pattern 1 pont, « Standard Stack » mécanismes de source)
    - .planning/phases/02-d-couverte-des-sources-bloquante/02-CONTEXT.md (contraintes d'honnêteté, anonymisation, structure JSONL à documenter)
    - CLAUDE.md (document en français ; ne jamais présenter une estimation comme exacte)
  </read_first>
  <action>
    Créer le dossier `docs/` à la racine du dépôt (s'il n'existe pas) et écrire `docs/data-sources.md`.
    Titre H1 : `# Chronos — Sources de données`. Ajouter un en-tête daté : « Capturé le 2026-07-08 —
    Claude Code runtime 2.1.202 / binaire disque 2.1.87 / doc officielle statusLine courante à cette date. »
    Puis avertissement : « API privée de facto : schéma susceptible de changer à toute MAJ de Claude Code
    (voir section Hypothèses & points de fragilité). »

    Rédiger les sections 1 à 3 ci-dessous (chaque titre de section en `## `). Transcrire le contenu factuel
    depuis RESEARCH.md — ne rien inventer.

    === `## 1. Source primaire — objet d'usage (rate_limits via statusLine)` ===
    - **Localisation exacte** : le bloc `rate_limits` du contrat JSON de la fonctionnalité **statusLine**
      de Claude Code (point d'extension officiellement documenté : code.claude.com/docs/en/statusline).
      Préciser explicitement : cet objet n'est persisté dans AUCUN fichier sur disque (vérifié
      exhaustivement — 0 occurrence structurée sous ~/.claude et ~/.claude.json). Il ne transite que
      transitoirement par le **stdin** de la commande statusLine, pendant qu'une session Claude Code tourne.
    - **Mécanisme d'accès (pont statusLine → fichier)** : documenter que statusLine ne « rend » pas un
      fichier ; Claude Code POUSSE le JSON sur stdin. Pour qu'un overlay externe le consomme, il faut un
      **pont** : une commande statusLine (script ou mode CLI de Chronos) qui lit stdin, extrait `rate_limits`,
      et l'écrit atomiquement dans un fichier watchable (ex. `%APPDATA%\Chronos\usage.json`) que l'overlay
      surveille (aligné RAF-01 FileSystemWatcher). Préciser : le pont doit RÉ-ÉMETTRE la barre existante sur
      stdout (non destructif — ce poste a déjà `gsd-statusline.js`) et n'ajouter que l'écriture du fichier.
      Marquer clairement « à documenter ici, à CODER en Phase 3 — pas de code dans cette phase ».
    - **Schéma des champs** : documenter le bloc verbatim (extrait du binaire + confirmé doc officielle) :
      `rate_limits.five_hour.used_percentage` (nombre, **0 à 100**, décimales possibles) et
      `rate_limits.five_hour.resets_at` (**Unix epoch SECONDES**), idem `seven_day`.
      **CORRECTION MAJEURE à énoncer explicitement** : la modélisation projet parlait d'un champ
      `utilization` (0..1) — ce nom N'EXISTE PAS dans la source. Le champ réel est `used_percentage` (0..100).
    - **Échantillon réel anonymisé** : insérer un bloc de code JSON avec valeurs synthétiques plausibles :
      ```jsonc
      "rate_limits": {
        "five_hour": { "used_percentage": 23.5, "resets_at": 1738425600 },
        "seven_day": { "used_percentage": 41.2, "resets_at": 1738857600 }
      }
      ```
    - **Conditions de présence** : `rate_limits` n'apparaît que pour les abonnés Claude.ai (Pro/Max), APRÈS
      la 1re réponse API de la session ; chaque fenêtre (`five_hour`, `seven_day`) peut être indépendamment
      absente. Conséquence : dégrader vers « indisponible » / repli, jamais inventer de valeur.
    - **Fréquence de mise à jour** : rafraîchi à chaque rendu de la barre statusLine, donc UNIQUEMENT pendant
      une session Claude Code active (best-effort, cadence interne non documentée — ne pas en dépendre).
    - **SourceReliability = Fiable.**

    === `## 2. Source de repli — estimation par transcripts JSONL` ===
    - **Localisation** : `~/.claude/projects/<slug-projet>/<session-uuid>.jsonl` (transcripts de session).
    - **Schéma d'une ligne `assistant`** : lister les clés de haut niveau (cwd, entrypoint, gitBranch,
      isSidechain, message, parentUuid, requestId, sessionId, timestamp, type, userType, uuid, version).
      Filtrer sur `o["type"] == "assistant"` (et `message.role == "assistant"`).
    - **Objet `message.usage` (cœur du repli, DAT-05)** : insérer l'échantillon anonymisé :
      ```jsonc
      "usage": {
        "input_tokens": 20863,
        "output_tokens": 1496,
        "cache_creation_input_tokens": 7814,
        "cache_read_input_tokens": 30962,
        "server_tool_use": { "web_search_requests": 0, "web_fetch_requests": 0 },
        "service_tier": "standard",
        "cache_creation": { "ephemeral_1h_input_tokens": 7814, "ephemeral_5m_input_tokens": 0 }
      }
      ```
      Préciser que l'estimation somme les tokens ; les plafonds ne sont pas publiés → structurellement
      approximatif (d'où « Estimé »).
    - **Format des timestamps** : `o["timestamp"]` = **ISO 8601 UTC** avec millisecondes et suffixe `Z`,
      ex. `"2026-07-08T12:20:42.428Z"`. AVERTISSEMENT explicite : deux formats de temps distincts à ne PAS
      confondre — primaire `resets_at` = epoch secondes ; repli `timestamp` = ISO 8601 UTC.
    - **Taille typique + implications perf** : ~3 Ko/ligne ; ex. 1.1 Mo / 336 lignes pour une session ;
      fichiers plurimégaoctets sur longues sessions → lecture en **streaming**, `FileShare.ReadWrite`
      (le fichier est en cours d'écriture), tolérance de la dernière ligne partielle (Phase 3, ROB-02).
    - **Blocs sous-agents (note V2-01, différé — ne pas coder)** : en v2.1.202 les sous-agents ne sont PLUS
      des blocs `tool_use` name=Task inline. Ils vivent dans
      `~/.claude/projects/<slug>/<session-uuid>/subagents/` : `agent-<id>.jsonl` (lignes assistant avec usage,
      `isSidechain: true`) + `agent-<id>.meta.json` (`{ agentType, description, spawnDepth, toolUseId }`).
      Consigner comme piste V2-01 : la future bande sous-agents lira ce dossier, pas des blocs Task inline.
    - **SourceReliability = Estimé.**

    === `## 3. Mapping vers UsageSnapshot (Phase 3)` ===
    Table de correspondance source → modèle neutre (guide IUsageProvider) :
    - `rate_limits.<window>.used_percentage` (0..100) → `UsageSnapshot.Utilization` (0..1) via `/ 100.0`.
    - `rate_limits.<window>.resets_at` (epoch s) → `UsageSnapshot.ResetsAt` via `DateTimeOffset.FromUnixTimeSeconds`.
    - fenêtre/objet absent → `SourceReliability` bascule sur repli/indisponible (jamais de valeur inventée).
    - repli JSONL `message.usage.*_tokens` → estimation `Utilization` marquée `SourceReliability = Estimé`.
    Rappeler : `utilization` (0..1) est un champ FANTÔME côté source — n'exister que côté modèle après conversion.

    Convention : commentaires et prose en français. NE PAS écrire de provider ni de vrai script de pont
    (seule une esquisse illustrative en bloc de code commentée « à ne pas implémenter en Phase 2 » est tolérée
    si utile à la compréhension du pont).
  </action>
  <verify>
    <automated>test -f "docs/data-sources.md" && grep -Eiq 'rate_limits' docs/data-sources.md && grep -Eiq 'statusline' docs/data-sources.md && grep -q 'used_percentage' docs/data-sources.md && grep -q 'resets_at' docs/data-sources.md && grep -Eiq 'epoch' docs/data-sources.md && grep -q 'five_hour' docs/data-sources.md && grep -q 'seven_day' docs/data-sources.md && grep -Eiq 'jsonl' docs/data-sources.md && grep -q 'input_tokens' docs/data-sources.md && grep -q 'ISO 8601' docs/data-sources.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/data-sources.md` retourne vrai (fichier créé, dossier docs/ existant).
    - `grep -Eiq 'rate_limits' docs/data-sources.md` retourne vrai (objet localisé).
    - `grep -Eiq 'statusline' docs/data-sources.md` retourne vrai (mécanisme d'accès nommé).
    - `grep -q 'used_percentage' docs/data-sources.md` retourne vrai (champ réel, PAS `utilization`).
    - `grep -q 'resets_at' docs/data-sources.md` ET `grep -Eiq 'epoch' docs/data-sources.md` retournent vrai (format timestamp primaire).
    - `grep -q 'five_hour' docs/data-sources.md` ET `grep -q 'seven_day' docs/data-sources.md` retournent vrai (les deux fenêtres).
    - `grep -Eiq 'jsonl' docs/data-sources.md` ET `grep -q 'input_tokens' docs/data-sources.md` retournent vrai (repli documenté).
    - `grep -q 'ISO 8601' docs/data-sources.md` retourne vrai (format timestamp repli distinct).
    - `grep -Eiq 'subagents' docs/data-sources.md` retourne vrai (piste V2-01 layout v2.1.202).
    - `grep -Eiq 'pont|bridge' docs/data-sources.md` retourne vrai (mécanisme d'accès statusLine → fichier).
    - `grep -Eiq 'Fiable' docs/data-sources.md` ET `grep -Eiq 'Estim' docs/data-sources.md` retournent vrai (SourceReliability des deux sources).
  </acceptance_criteria>
  <done>docs/data-sources.md existe avec les sections 1-3 : source primaire (rate_limits/statusLine localisé, schéma used_percentage 0..100 + resets_at epoch s, échantillon anonymisé, pont d'accès, conditions de présence, Fiable), source de repli JSONL (localisation, message.usage tokens, timestamp ISO 8601, perf, subagents/, Estimé), et mapping vers UsageSnapshot (conversions /100 et FromUnixTimeSeconds).</done>
</task>

<task type="auto">
  <name>Task 2 : Rédiger les hypothèses & points de fragilité, la reproductibilité, et vérifier l'anonymisation</name>
  <files>docs/data-sources.md</files>
  <read_first>
    - docs/data-sources.md (sections 1-3 rédigées en Task 1 — à COMPLÉTER, ne pas réécrire)
    - .planning/phases/02-d-couverte-des-sources-bloquante/02-RESEARCH.md (sections « Common Pitfalls », « Open Questions », « State of the Art », « Sondage empirique réalisé » §1/§2/§5, « Metadata » confidence)
    - .planning/phases/02-d-couverte-des-sources-bloquante/02-VALIDATION.md (Human Verification : aucun secret/ID de compte dans les échantillons)
  </read_first>
  <action>
    APPENDER les sections 4 et 5 à `docs/data-sources.md` (ne pas toucher les sections 1-3). Transcrire depuis
    RESEARCH.md — ne rien inventer.

    === `## 4. Hypothèses & points de fragilité` ===
    Consigner explicitement, en liste, chaque risque identifié (guide direct pour l'abstraction IUsageProvider) :
    - **API privée de facto** : bien que le contrat statusLine soit documenté, `rate_limits` n'est pas un
      contrat de données garanti — renommage/déplacement de champ possible à toute MAJ. Recommandation :
      test de contrat sur échantillon en Phase 3, dégradation vers « indisponible » si champ/fenêtre absent.
    - **Écart de version** : binaire disque **2.1.87** vs runtime actif **2.1.202** ; schéma confirmé identique
      entre 2.1.87 et doc officielle courante, mais 2.1.202 non vérifié champ par champ (confiance MEDIUM).
      Recommandation : dater la capture (fait en en-tête) et revalider à chaque MAJ majeure.
    - **Staleness hors session active** : l'usage n'est rafraîchi que quand une session Claude tourne et rend
      sa barre ; overlay ouvert sans session → `used_percentage` figé au dernier connu (à marquer potentiellement
      périmé). Le `resets_at` (epoch) permet néanmoins d'interpoler le compte à rebours (aligné RAF-03).
    - **Conditions de présence** : rappeler Pro/Max + après 1re réponse API + fenêtres indépendamment absentes
      → le provider doit gérer l'absence sans crash (ROB-01) et basculer sur le repli JSONL (DAT-06).
    - **Reset hebdo dérivant** : la fenêtre 7 j dérive (~72 h, ancrage non documenté) → traiter `resets_at` tel
      que fourni, best-effort et recalibrable (ROB-03).
    - **Faux positifs JSONL** : les chaînes « five_hour »/« seven_day » trouvées dans les transcripts sont de la
      PROSE (ce projet en discute), PAS un objet d'usage loggé — exiger un objet structuré, jamais une chaîne
      dans un champ `content`/`text`. Rappeler : aucun objet d'usage n'est matérialisé sur disque.
    - **Sécurité** : ne jamais lire/logger `.credentials.json` (tokens OAuth) ni le contenu des conversations —
      ne compter que tokens/métadonnées ; lecture seule stricte sous le profil utilisateur.

    === `## 5. Reproductibilité — recapture (lecture seule stricte)` ===
    Documenter la méthode pour re-vérifier la source à une future version (sans code de provider) :
    - Vérifier la config statusLine active : lire `~/.claude/settings.json` (clé `statusLine.command`).
    - Confirmer l'absence de persistance : grep ciblé d'un objet structuré `"used_percentage":` /
      `"utilization":` sous `~/.claude` et `~/.claude.json` (attendu : 0 occurrence structurée).
    - Confirmer le schéma courant : doc officielle `code.claude.com/docs/en/statusline` (table des champs) ;
      à défaut, chaînes printables embarquées du binaire `~/.claude/downloads/claude-<ver>-win32-x64.exe`.
    - Ré-échantillonner le repli : dernières lignes `type=assistant` d'un `~/.claude/projects/<slug>/<uuid>.jsonl`
      pour `message.usage` + `timestamp`, et lister `subagents/` pour le layout sous-agents.
    - Rappel impératif : NE MODIFIER aucun fichier de ~/.claude ; anonymiser toute capture avant de la coller ici.
    Ajouter une courte note de traçabilité des sources (doc officielle statusLine + binaire 2.1.87 + sondage
    filesystem + échantillon JSONL réel), avec les niveaux de confiance (localisation/schéma HIGH ;
    stabilité inter-versions MEDIUM ; structure JSONL HIGH).

    === Revue d'anonymisation (obligatoire avant de terminer) ===
    Relire l'INTÉGRALITÉ du document et garantir qu'AUCUN de ces éléments n'y figure : token/clé OAuth, UUID de
    session réel, identifiant de compte, e-mail, chemin absolu contenant un vrai nom d'utilisateur autre qu'un
    placeholder (`<slug>`, `<uuid>`, `%USERPROFILE%`), contenu de conversation. Toutes les valeurs numériques
    d'usage doivent être synthétiques (23.5, 41.2, epochs plausibles). Corriger sur place le cas échéant.
  </action>
  <verify>
    <automated>grep -Eiq 'fragilit' docs/data-sources.md && grep -Eiq 'version' docs/data-sources.md && grep -Eiq 'staleness|périm|figé' docs/data-sources.md && grep -Eiq 'hypoth' docs/data-sources.md && grep -Eiq 'reproductib|recapture' docs/data-sources.md && ! grep -Eq '(sk-ant|eyJ|BEGIN [A-Z ]*PRIVATE KEY)' docs/data-sources.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eiq 'fragilit' docs/data-sources.md` retourne vrai (points de fragilité consignés).
    - `grep -Eiq 'hypoth' docs/data-sources.md` retourne vrai (hypothèses consignées).
    - `grep -Eiq 'version' docs/data-sources.md` retourne vrai (écart 2.1.87/2.1.202 documenté).
    - `grep -Eiq 'staleness|périm|figé' docs/data-sources.md` retourne vrai (staleness hors session).
    - `grep -Eiq 'reproductib|recapture' docs/data-sources.md` retourne vrai (section reproductibilité).
    - `grep -Eiq 'IUsageProvider|Phase 3' docs/data-sources.md` retourne vrai (fragilités reliées à l'abstraction).
    - `grep -Eq '(sk-ant|eyJ|BEGIN [A-Z ]*PRIVATE KEY)' docs/data-sources.md` retourne FAUX (aucun secret évident).
    - Le document compte au moins 5 sections `## ` : `grep -c '^## ' docs/data-sources.md` >= 5.
  </acceptance_criteria>
  <done>docs/data-sources.md est complet : sections 4 (hypothèses & fragilités reliées à IUsageProvider : API privée de facto, écart de version, staleness, présence conditionnelle, reset hebdo, faux positifs, sécurité) et 5 (reproductibilité en lecture seule + traçabilité/confiance) ajoutées ; revue d'anonymisation passée (aucun secret, UUID réel ou contenu de conversation).</done>
</task>

</tasks>

<verification>
- `test -f docs/data-sources.md` : le livrable existe.
- Localisation objet d'usage : `grep -Eiq 'rate_limits' docs/data-sources.md` ET `grep -Eiq 'statusline' docs/data-sources.md`.
- Schéma corrigé : `grep -q 'used_percentage'` ET `grep -q 'resets_at'` ET `grep -Eiq 'epoch'`.
- Repli documenté : `grep -Eiq 'jsonl'` ET `grep -q 'input_tokens'` ET `grep -q 'ISO 8601'`.
- Fragilités : `grep -Eiq 'fragilit'` ET `grep -Eiq 'version'` ET `grep -Eiq 'staleness|périm|figé'`.
- Anonymisation : `grep -Eq '(sk-ant|eyJ|BEGIN [A-Z ]*PRIVATE KEY)' docs/data-sources.md` retourne FAUX.
- Au moins 5 sections : `grep -c '^## ' docs/data-sources.md` >= 5.
</verification>

<success_criteria>
- SC1 : docs/data-sources.md existe et localise précisément l'objet d'usage (bloc rate_limits du contrat statusLine) avec un échantillon réel anonymisé.
- SC2 : le schéma des champs (five_hour/seven_day : used_percentage 0..100 + resets_at epoch s) et la structure des transcripts JSONL (message.usage tokens, timestamp ISO 8601, subagents/) sont documentés.
- SC3 : les hypothèses et points de fragilité (API privée de facto, écart de version, staleness, présence conditionnelle) sont consignés pour guider IUsageProvider (Phase 3).
- DAT-01 satisfait ; aucun code de provider écrit (phase documentaire).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-d-couverte-des-sources-bloquante/02-01-SUMMARY.md`.
</output>
