---
phase: 22-un-instrument-de-mesure-qui-ne-ment-plus
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/Chronos/Services/DiagnosticService.cs
  - tests/Chronos.Tests/DiagnosticServiceTests.cs
  - .planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "Les fichiers d'état listés sont ceux qui attendent et les plus récents, jamais les huit premiers de l'alphabet"
    - "Le rapport dit combien de fichiers il n'a pas listés, au lieu de laisser croire qu'il les a tous montrés"
    - "Le dossier inspecté est celui que le moniteur du widget lit réellement"
    - "Un fichier d'état sans horodatage lisible est annoncé de date inconnue, jamais daté d'office"
    - "La phase est vérifiable : chaque critère de succès a une preuve nommée et une commande"
  artifacts:
    - path: "src/Chronos/Services/DiagnosticService.cs"
      provides: "Sélection par pertinence des fichiers d'état (attente d'abord, puis les plus récents)"
      contains: "MaxFichiersEtat"
    - path: ".planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md"
      provides: "Carte de vérification par tâche, résultats mesurés, invariants de sécurité"
      contains: "À VÉRIFIER PAR L'UTILISATEUR"
  key_links:
    - from: "src/Chronos/Services/DiagnosticService.cs"
      to: "SessionMonitor.Directory"
      via: "le dossier listé est celui du moniteur du widget quand il est injecté"
      pattern: "_moniteurSessions\\?\\.Directory"
    - from: "src/Chronos/Services/DiagnosticService.cs"
      to: "AffichageSessions.Urgence"
      via: "l'ordre de pertinence des fichiers d'état est celui du widget"
      pattern: "AffichageSessions\\.Urgence"
---

<objective>
OBS-02 — **le rapport cesse de choisir ses exemples par ordre alphabétique.**

`DiagnosticService.cs:440` liste `files.Take(8)` sur la sortie de `Directory.GetFiles` : les **huit premiers
fichiers par ordre alphabétique d'UUID**. Sur la machine mesurée le 2026-09-12 — 54 fichiers d'état dont 48
de plus de sept jours — les huit retenus étaient tous vieux de plusieurs semaines. Un échantillon tiré par
le hasard d'un nom de fichier, présenté comme un état des lieux. C'est cette liste que l'utilisateur lisait
quand il croyait voir « des sessions mortes depuis des semaines » dans son widget.

La correction est une **sélection par pertinence** : d'abord ce qui attend une intervention, ensuite le plus
récent — le même critère d'urgence que le widget, pris à la même source (`AffichageSessions.Urgence`). Et le
rapport dit combien de fichiers il n'a pas montrés, au lieu de laisser croire qu'il les a tous montrés.

Ce plan ferme aussi la phase : il produit `22-VALIDATION.md`, la carte qui rend les quatre critères de
succès vérifiables un à un.

Purpose : dernier plan de la phase. Il touche le même fichier que le plan 22-02, d'où la vague 3.

Output : une sélection pertinente, un compte des fichiers non listés, et la carte de vérification remplie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-CONTEXT.md
@.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-01-SUMMARY.md
@.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-02-SUMMARY.md

<interfaces>
<!-- Contrats existants, extraits du dépôt le 2026-09-12. NE PAS explorer le code pour les retrouver. -->

```csharp
// src/Chronos/Services/DiagnosticService.cs, bloc à remplacer — l. 434-456, TEL QUEL :
        // Fichiers d'état écrits par le mode --hook.
        var sessDir = Path.Combine(Path.GetDirectoryName(_paths.UsageFile)!, "sessions");
        try
        {
            var files = Directory.Exists(sessDir) ? Directory.GetFiles(sessDir, "*.json") : System.Array.Empty<string>();
            sb.AppendLine($"  Fichiers d'état ({sessDir}) : {files.Length}");
            foreach (var f in files.Take(8))
            {
                try
                {
                    using var d = JsonDocument.Parse(File.ReadAllText(f));
                    var r = d.RootElement;
                    string P(string k) => r.TryGetProperty(k, out var v) ? v.ToString() : "?";
                    // HDR-05 : plus aucune conversion d'unité locale — tout passe par UsageNormalization.
                    var age = r.TryGetProperty("updated_at", out var ua) && ua.TryGetInt64(out var ms)
                        && UsageNormalization.InstantDepuisEpochMillisecondes(ms) is { } maj
                        ? $"{(_clock.UtcNow - maj).TotalMinutes:F0} min" : "?";
                    sb.AppendLine($"    · {P("project")} — {P("activity")} (maj il y a {age})");
                }
                catch { }
            }
            if (files.Length == 0)
                sb.AppendLine("    (aucun — normal en app bureau : la détection passe par les transcripts, pas les hooks)");
        }
        catch { }
```

```csharp
// src/Chronos/Services/UsageNormalization.cs — l. 98, LE point unique de conversion. Obligatoire ici :
// DiagnosticService.cs n'est PAS exempté de NormalisationUniqueTests, qui interdit FromUnixTimeMilliseconds
// hors des quatre fichiers nommés.
    public static DateTimeOffset? InstantDepuisEpochMillisecondes(long? millisecondes);
```

```csharp
// Disponible depuis la vague 1 / 2 :
public static class AffichageSessions
{
    public static int Urgence(SessionActivity a);   // 0 = WaitingAttention … 3 = Unknown
    public static string Age(System.TimeSpan d);    // « à l'instant » / « il y a N min » / « il y a N h »
}
public sealed class SessionMonitor { public string Directory { get; } }
// Champ posé au plan 22-02 : private readonly SessionMonitor? _moniteurSessions;
```

Format RÉEL d'un fichier d'état, écrit par `SessionHookProcessor.BuildStateJson` :
`{"session_id":"…","project":"…","activity":"WaitingAttention","reason":"permission_prompt","updated_at":1789181509266}`

Mesures du 2026-09-12 sur la machine cible, qui motivent ce plan :
- `%APPDATA%\Chronos\sessions` : **54** fichiers `.json` (+ 12 `.tmp` orphelins), dont **48** de plus de
  sept jours. Les huit premiers par ordre alphabétique étaient tous vieux de plusieurs semaines.
- Une passe de lecture complète des 54 fichiers coûte **15,15 ms** : lire tout le dossier pour n'en afficher
  que huit est sans conséquence mesurable sur un rapport qui dure plusieurs secondes.
- **NE PAS anticiper la phase 23** : les `.tmp` orphelins et le balayage des états expirés lui appartiennent
  (CYC-01). Ici on LIT et on CHOISIT mieux ; on ne supprime rien, on ne répare aucune écriture.

**SÉCURITÉ, rappel de phase :** aucun test n'écrit dans le vrai `%APPDATA%\Chronos` ; les 66 entrées de
`sessions\` et les 84 octets d'`archived.json` restent intouchés ; l'overlay `Chronos-v3.0.2.exe`
(pid 119412) n'est ni lancé ni tué ; `oauth.dat` se vérifie par sa taille (**518** octets) et JAMAIS par
son mtime — le rafraîchissement préventif de la phase 17 le fait légitimement bouger toutes les 60 s.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : les fichiers d'état listés sont ceux qui comptent, et le rapport dit ce qu'il ne montre pas</name>
  <files>src/Chronos/Services/DiagnosticService.cs, tests/Chronos.Tests/DiagnosticServiceTests.cs</files>

  <read_first>
    - src/Chronos/Services/DiagnosticService.cs (l. 396-500 — la section « Widget sessions Claude Code »
      telle qu'elle est APRÈS le plan 22-02 : le bloc des fichiers d'état est le seul qui n'a pas encore été
      repris, et le champ `_moniteurSessions` existe déjà)
    - src/Chronos/Services/UsageNormalization.cs (l. 98 `InstantDepuisEpochMillisecondes` — obligatoire,
      `DiagnosticService.cs` n'est pas exempté de `NormalisationUniqueTests`)
    - src/Chronos/Services/SessionHookProcessor.cs (`BuildStateJson` — le format exact des fichiers écrits,
      pour que les tests posent de vraies données et non une forme inventée)
    - tests/Chronos.Tests/DiagnosticServiceTests.cs (les aides `TempPaths`, `TempDir22`, `Rapport` ajoutées
      au plan 22-02, à réutiliser)
  </read_first>

  <behavior>
    - Dossier contenant 10 fichiers dont un seul en `WaitingAttention` daté du plus ancien : ce fichier est
      listé **en premier**, malgré son âge — l'attente prime sur la fraîcheur, exactement comme au widget.
    - À rang d'urgence égal, le plus récent d'abord.
    - 10 fichiers → 8 listés + une ligne annonçant les **2** non listés. Le rapport ne laisse jamais croire
      qu'il a tout montré.
    - 8 fichiers ou moins → aucune ligne de reste.
    - Le compte total annoncé reste celui de TOUS les fichiers du dossier (`Fichiers d'état (…) : 10`).
    - Un fichier sans `updated_at` lisible est listé avec « date inconnue » et relégué au dernier rang, pas
      daté d'office à l'instant courant.
    - Un fichier au JSON corrompu est ignoré sans faire échouer le rapport.
    - Quand le moniteur est injecté, le dossier annoncé est `_moniteurSessions.Directory`.
    - Dossier vide → une ligne le dit, et elle ne parle plus de l'app bureau (source retirée en phase 21).
  </behavior>

  <action>
Écrire d'abord les tests, les voir échouer, puis implémenter.

**1. `src/Chronos/Services/DiagnosticService.cs`** — remplacer le bloc l. 434-456 EN ENTIER par :

```csharp
        // Fichiers d'état écrits par le mode --hook. OBS-02 — les fichiers PERTINENTS : ce qui attend
        // d'abord, puis le plus récent. L'ancien tri était celui de Directory.GetFiles, c'est-à-dire
        // l'ordre alphabétique des UUID. Sur la machine mesurée le 2026-09-12 (54 fichiers, dont 48 de
        // plus de sept jours), les huit retenus étaient tous vieux de plusieurs semaines : un échantillon
        // tiré par le hasard d'un nom de fichier, présenté comme un état des lieux. C'est cette liste-là
        // que l'utilisateur lisait quand il croyait voir des sessions mortes dans son widget.
        //
        // Le dossier lu est celui du MONITEUR quand il est injecté : lire un autre dossier que celui du
        // widget rouvrirait exactement l'écart qu'OBS-01 vient de fermer.
        var sessDir = _moniteurSessions?.Directory
                      ?? Path.Combine(Path.GetDirectoryName(_paths.UsageFile)!, "sessions");
        try
        {
            var files = Directory.Exists(sessDir) ? Directory.GetFiles(sessDir, "*.json") : System.Array.Empty<string>();
            sb.AppendLine($"  Fichiers d'état ({sessDir}) : {files.Length}");

            // Lire les 54 fichiers coûte 15,15 ms (mesure du 2026-09-12) : négligeable sur un rapport qui
            // dure des secondes, et c'est le prix d'un choix motivé plutôt que d'un tirage alphabétique.
            var lus = new List<(string Projet, string Activite, System.DateTimeOffset? Maj, int Urgence)>();
            foreach (var f in files)
            {
                try
                {
                    using var d = JsonDocument.Parse(File.ReadAllText(f));
                    var r = d.RootElement;
                    string P(string k) => r.TryGetProperty(k, out var v) ? v.ToString() : "?";
                    // HDR-05 : plus aucune conversion d'unité locale — tout passe par UsageNormalization.
                    var maj = r.TryGetProperty("updated_at", out var ua) && ua.TryGetInt64(out var ms)
                        ? UsageNormalization.InstantDepuisEpochMillisecondes(ms)
                        : null;
                    var activite = P("activity");
                    // Le rang d'urgence vient de la couche partagée avec le widget : une activité que le
                    // moniteur ne saurait pas relire est reléguée au dernier rang plutôt que devinée.
                    var urgence = System.Enum.TryParse<SessionActivity>(activite, ignoreCase: true, out var a)
                        ? AffichageSessions.Urgence(a) : 3;
                    lus.Add((P("project"), activite, maj, maj is null ? 3 : urgence));
                }
                catch { }   // fichier illisible : ignoré, jamais fatal au rapport
            }

            foreach (var e in lus.OrderBy(e => e.Urgence)
                                 .ThenByDescending(e => e.Maj ?? System.DateTimeOffset.MinValue)
                                 .Take(MaxFichiersEtat))
            {
                // Une date absente reste absente. La remplacer par l'instant courant ferait passer un
                // fichier muet pour un fichier frais — exactement ce que la doctrine du milestone interdit.
                var age = e.Maj is { } maj ? "maj " + AffichageSessions.Age(_clock.UtcNow - maj) : "date inconnue";
                sb.AppendLine($"    · {e.Projet} — {e.Activite} ({age})");
            }
            if (lus.Count > MaxFichiersEtat)
                sb.AppendLine($"    … et {lus.Count - MaxFichiersEtat} autre(s) non listé(s) : ni en attente, ni parmi les plus récents");
            if (files.Length == 0)
                sb.AppendLine("    (aucun — les hooks n'ont encore rien écrit dans ce dossier)");
        }
        catch { }
```

Déclarer la borne en constante, à côté de `UsageUrl` (l. 22) :

```csharp
    // Combien de fichiers d'état le rapport détaille. Une borne de LISIBILITÉ, pas une troncature muette :
    // le reste est annoncé sur une ligne dédiée, et le compte total du dossier est donné avant la liste.
    private const int MaxFichiersEtat = 8;
```

**2. `tests/Chronos.Tests/DiagnosticServiceTests.cs`** — ajouter en fin de classe. Réutiliser `TempDir22`,
`TempFichier22` et `Rapport` du plan 22-02 :

```csharp
    // --- Phase 22 (OBS-02) : des fichiers d'état PERTINENTS, pas les huit premiers de l'alphabet ---

    private static void EcrireEtat(string dir, string id, string projet, SessionActivity a, DateTimeOffset maj)
        => System.IO.File.WriteAllText(System.IO.Path.Combine(dir, id + ".json"),
               SessionHookProcessor.BuildStateJson(id, projet, a, null, maj.ToUnixTimeMilliseconds()));

    private static SessionMonitor MoniteurSur(string dir)
        => new(dir, new SourceFixe22(), new ArchiveStore(TempFichier22()));

    /// <summary>OBS-02 — le défaut mesuré : sur 54 fichiers dont 48 vieux de plus de sept jours, le rapport
    /// montrait les huit premiers par ordre alphabétique d'UUID. Ici les identifiants sont choisis pour que
    /// l'ordre alphabétique et l'ordre de pertinence soient OPPOSÉS : « aaa… » est le plus vieux et le plus
    /// muet, « zzz… » est la session qui attend. Un tri alphabétique la reléguerait hors de la liste.</summary>
    [Fact]
    public async Task Les_fichiers_listes_sont_ceux_qui_attendent_et_les_plus_recents()
    {
        var dir = TempDir22();
        for (var i = 0; i < 9; i++)
            EcrireEtat(dir, $"aaa-{i:D2}", $"vieux-{i}", SessionActivity.Unknown, T22.AddDays(-30 - i));
        EcrireEtat(dir, "zzz-attente", "PROJET QUI ATTEND", SessionActivity.WaitingAttention, T22.AddHours(-40));

        var report = await Rapport(MoniteurSur(dir));

        Assert.Contains("Fichiers d'état", report);
        Assert.Contains($"({dir}) : 10", report);   // le compte TOTAL reste celui du dossier
        // Le bloc des fichiers d'état PRÉCÈDE celui des sessions dans le rapport : les premières lignes
        // à puce sont donc bien les siennes.
        var lignes = report.Split('\n').Where(l => l.TrimStart().StartsWith("· ")).ToList();
        Assert.Contains("PROJET QUI ATTEND", lignes[0]);        // l'attente passe devant, malgré son âge
        Assert.Contains("… et 2 autre(s) non listé(s)", report);
    }

    [Fact]
    public async Task Huit_fichiers_ou_moins_ne_produisent_aucune_ligne_de_reste()
    {
        var dir = TempDir22();
        for (var i = 0; i < 8; i++)
            EcrireEtat(dir, $"s-{i:D2}", $"p-{i}", SessionActivity.Working, T22.AddMinutes(-i));

        var report = await Rapport(MoniteurSur(dir));

        Assert.DoesNotContain("non listé(s)", report);
        for (var i = 0; i < 8; i++) Assert.Contains($"p-{i}", report);
    }

    /// <summary>Doctrine du milestone : une date absente reste absente. Lui attribuer l'instant courant
    /// ferait passer un fichier muet pour un fichier frais, et le placerait en tête de la liste.</summary>
    [Fact]
    public async Task Un_fichier_sans_horodatage_est_annonce_de_date_inconnue_et_relegue()
    {
        var dir = TempDir22();
        System.IO.File.WriteAllText(System.IO.Path.Combine(dir, "muet.json"),
            """{"session_id":"muet","project":"SANS DATE","activity":"WaitingAttention"}""");
        EcrireEtat(dir, "date", "AVEC DATE", SessionActivity.Working, T22.AddMinutes(-3));

        var report = await Rapport(MoniteurSur(dir));

        var lignes = report.Split('\n').Where(l => l.TrimStart().StartsWith("· ")).ToList();
        Assert.Contains("AVEC DATE", lignes[0]);                          // le daté passe devant
        Assert.Contains("SANS DATE", lignes[1]);
        Assert.Contains("date inconnue", lignes[1]);
    }

    [Fact]
    public async Task Un_fichier_corrompu_est_ignore_sans_casser_le_rapport()
    {
        var dir = TempDir22();
        System.IO.File.WriteAllText(System.IO.Path.Combine(dir, "corrompu.json"), "pas du JSON {{{");
        EcrireEtat(dir, "bon", "PROJET SAIN", SessionActivity.Working, T22);

        var report = await Rapport(MoniteurSur(dir));

        Assert.Contains($"({dir}) : 2", report);    // 2 fichiers sur disque…
        Assert.Contains("PROJET SAIN", report);     // …1 seul lisible, et le rapport tient debout
        Assert.Contains("[Conseil]", report);
    }

    /// <summary>Le rapport inspecte le dossier que le MONITEUR lit, pas un dossier déduit d'un autre
    /// chemin : deux dossiers pour un seul widget rouvriraient l'écart qu'OBS-01 vient de fermer.</summary>
    [Fact]
    public async Task Le_dossier_inspecte_est_celui_du_moniteur_du_widget()
    {
        var dir = TempDir22();
        EcrireEtat(dir, "s1", "PROJET DU MONITEUR", SessionActivity.Working, T22);

        var report = await Rapport(MoniteurSur(dir));

        Assert.Contains(dir, report);
        Assert.Contains("PROJET DU MONITEUR", report);
    }
```
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~DiagnosticServiceTests|FullyQualifiedName~NormalisationUniqueTests|FullyQualifiedName~GardesPerimetreTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "files.Take(8)" src/Chronos/Services/DiagnosticService.cs` rend **0** — le tirage alphabétique
      a disparu
    - `grep -cF "Take(8)" src/Chronos/Services/DiagnosticService.cs` rend **1** — il en restait 2 après le
      plan 22-02 ; la seule survivante est la description de forme d'un blob d'identifiant (`DescribeBlobShape`)
    - `grep -cF "private const int MaxFichiersEtat = 8;" src/Chronos/Services/DiagnosticService.cs` rend **1**
    - `grep -cF "Take(MaxFichiersEtat)" src/Chronos/Services/DiagnosticService.cs` rend **1**
    - `grep -cF "AffichageSessions.Urgence" src/Chronos/Services/DiagnosticService.cs` rend **1** — l'ordre
      d'urgence vient de la couche partagée, il n'est pas recopié
    - `grep -cF "_moniteurSessions?.Directory" src/Chronos/Services/DiagnosticService.cs` rend **1**
    - `grep -cF "InstantDepuisEpochMillisecondes" src/Chronos/Services/DiagnosticService.cs` rend **2** —
      INCHANGÉ : `DiagnosticService.cs` n'est pas exempté de `NormalisationUniqueTests`, aucune conversion
      locale n'a été introduite
    - `grep -cF "normal en app bureau" src/Chronos/Services/DiagnosticService.cs` rend **0** — la source
      app-bureau a été retirée en phase 21, ce conseil ne veut plus rien dire
    - `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` rend **0** — la garde de
      non-retour du plan 22-02 reste satisfaite
    - Les 5 nouveaux tests passent, et les tests préexistants n'ont pas été retouchés :
      `git diff -U0 tests/Chronos.Tests/DiagnosticServiceTests.cs | grep -c "^-[^-]"` rend **0**
    - `NormalisationUniqueTests` et `GardesPerimetreTests` sont verts
  </acceptance_criteria>

  <done>
Le rapport montre les fichiers d'état qui attendent et les plus récents, annonce combien il n'en montre pas,
et inspecte le dossier que le widget lit réellement.
  </done>
</task>

<task type="auto">
  <name>Task 2 : la carte de vérification de la phase, remplie et mesurée</name>
  <files>.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md</files>

  <read_first>
    - .planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md (INTÉGRALEMENT — la
      carte à remplir : colonnes « Résultat mesuré » vides, section des invariants, section utilisateur)
    - .planning/phases/22-.../22-01-SUMMARY.md et 22-02-SUMMARY.md (les mesures déjà consignées, dont les
      trois mutations de falsification du plan 22-02)
    - .planning/ROADMAP.md (section « Phase 22 » — les quatre critères de succès, mot pour mot)
    - .planning/phases/21-p-rim-tre-le-widget-ne-parle-que-de-claude-code/21-VALIDATION.md (le PRÉCÉDENT de
      forme : carte par tâche, invariants de sécurité, section « À VÉRIFIER PAR L'UTILISATEUR »)
  </read_first>

  <action>
**1. Mesurer, deux fois** (le chargeur BAML produit des échecs intermittents invisibles à la première passe) :

```sh
dotnet test Chronos.sln -c Debug --nologo -v q
dotnet test Chronos.sln -c Debug --nologo -v q
```

Consigner le total obtenu et le temps. Repère d'entrée de phase : **719 tests / 0 échec / ~5 s**. Le critère
est **0 échec ET aucun test supprimé** : cette phase n'ajoute que des tests. Un total inférieur à 719 doit
être expliqué nominativement, jamais absorbé en ajustant le chiffre attendu.

**2. Remplir les quatre lignes du tableau « Critères de succès »** de `22-VALIDATION.md`, chacune avec la
preuve NOMMÉE (test ou commande) et le résultat mesuré :

| Critère ROADMAP | Preuve |
|---|---|
| 1. Diagnostic et widget disent la même chose | `Le_rapport_decrit_les_sessions_du_moniteur_qu_on_lui_donne`, `Le_widget_affiche_ce_que_la_couche_neutre_produit`, `Read_rend_exactement_les_sessions_visibles_d_Inspecter` + `grep -cF "GetRequiredService<SessionMonitor>()" src/Chronos/App.xaml.cs` → 2 |
| 2. Ce qui est masqué est dit, et pourquoi | `Le_cas_e465420e_devient_lisible_en_une_lecture`, `Le_cas_e465420e_se_lit_en_une_ligne_du_rapport`, `Une_session_archivee_est_annoncee_masquee_par_le_magasin_d_archives` |
| 3. Des sessions pertinentes | `Les_fichiers_listes_sont_ceux_qui_attendent_et_les_plus_recents`, `Le_rapport_ne_tronque_plus_la_liste_des_sessions_affichees` + `grep -cF "files.Take(8)"` → 0 |
| 4. Non-retour garanti | `Le_diagnostic_ne_fabrique_aucun_moniteur_de_sessions`, `Le_diagnostic_recoit_le_moniteur_du_conteneur`, `Assert.Same` de `Le_graphe_DI_resout_la_chaine_de_sessions` — **les trois mutations de falsification du plan 22-02, observées rouges puis révoquées** |

**3. Consigner la preuve de PARTAGE D'INSTANCE**, qui est la condition de placement de la phase. Écrire noir
sur blanc, avec les commandes et leurs sorties :

```sh
grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs    # attendu : 0
grep -cF "GetRequiredService<SessionMonitor>()" src/Chronos/App.xaml.cs    # attendu : 2
grep -cF "=> Inspecter(now).Visibles;" src/Chronos/Services/SessionMonitor.cs   # attendu : 1
```

Et la conséquence, qui est le livrable de long terme de cette phase : **les phases 23 à 26 pourront changer
le câblage du widget sans qu'une ligne de `DiagnosticService.cs` ne change.** Le noter comme une attente
vérifiable à la fin du milestone, pas comme une promesse.

**4. Vérifier et consigner les invariants de sécurité.** Ils font partie du livrable :

```sh
ls "$APPDATA/Chronos/sessions" | wc -l        # attendu : 66 (54 .json + 12 .tmp) — INCHANGÉ
wc -c < "$APPDATA/Chronos/archived.json"      # attendu : 84 — INCHANGÉ
wc -c < "$APPDATA/Chronos/oauth.dat"          # attendu : 518 — TAILLE SEULE, jamais le mtime
git status --porcelain                        # aucune modification hors src/, tests/, .planning/
```

L'overlay `Chronos-v3.0.2.exe` (pid 119412) **n'a été ni lancé ni tué**. Aucune requête réseau n'a été émise
depuis un test (tous les montages posent `Token = null`, et la sonde est gardée par `if (token is not null)`).

**5. Remplir la section « À VÉRIFIER PAR L'UTILISATEUR »** — ce que seul un vrai lancement peut établir, et
que la contrainte de phase interdit de provoquer :

- Ouvrir **clic droit → Diagnostic…** pendant que le widget de sessions est affiché, et comparer **ligne à
  ligne** : mêmes projets, mêmes états, mêmes âges, même ordre (critère n°1, in vivo).
- Dans la section « Sessions MASQUÉES par un filtre » : vérifier que toute session qui manque à l'écran y
  figure avec le nom du fichier qui la masque. Cas attendu sur cette machine : `e465420e`
  (PROJET ADVANCED SHEET), masquée par `treated.json` (critère n°2, in vivo).
- Dans « Fichiers d'état » : les lignes listées doivent être des sessions récentes ou en attente, plus des
  entrées vieilles de plusieurs semaines, et la ligne « … et N autre(s) non listé(s) » doit apparaître
  (critère n°3, in vivo — attendu ~46 non listés sur 54).
- Reporter aussi, à l'occasion, les vérifications in vivo héritées de la phase 21 :
  purge réelle d'`archived.json` (84 octets → `{}`), 8 styles × 9 thèmes, absence de lignes `desktop:`.

**6. Mettre à jour la ligne de la phase 22 dans `.planning/ROADMAP.md`** (tableau « Progress ») : plans
complétés, statut, date.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, sur **deux exécutions consécutives**
    - Total **≥ 719** : cette phase n'a supprimé aucun test. Tout écart est expliqué nominativement dans la
      carte, jamais absorbé en ajustant le chiffre attendu
    - Les six gardes nommées de la phase sont vertes : `ServicesLayerPurityTests`, `CompositionRootTests`,
      `NormalisationUniqueTests`, `GardesDoctrineTests`,
      `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte`, `GardesPerimetreTests`
    - `grep -cF "Résultat mesuré" .planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md`
      rend **au moins 1**, et AUCUNE cellule de résultat ne reste sur le marqueur `(à mesurer)` :
      `grep -cF "(à mesurer)" …/22-VALIDATION.md` rend **0**
    - `grep -cF "À VÉRIFIER PAR L'UTILISATEUR" …/22-VALIDATION.md` rend **1**
    - `ls "$APPDATA/Chronos/sessions" | wc -l` rend **66**, `wc -c < "$APPDATA/Chronos/archived.json"`
      rend **84**, `wc -c < "$APPDATA/Chronos/oauth.dat"` rend **518** (taille seule — le mtime n'est PAS
      vérifié : le refresh préventif de la phase 17 le fait bouger toutes les 60 s)
    - `git status --porcelain` ne montre AUCUNE modification hors `src/`, `tests/` et `.planning/`
    - La ligne « 22. Un instrument de mesure qui ne ment plus » du tableau Progress de `ROADMAP.md` est à jour
  </acceptance_criteria>

  <done>
Les quatre critères de succès de la phase ont chacun une preuve nommée et un résultat mesuré ; le partage
d'instance est établi par trois commandes ; l'utilisateur sait quoi regarder au prochain lancement.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -c Debug --nologo -v q` **deux fois** → **0 échec**, total **≥ 719**.
2. `grep -cF "files.Take(8)" src/Chronos/Services/DiagnosticService.cs` rend **0** ;
   `grep -cF "Take(8)" …` rend **1**.
3. `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` rend **0**.
4. `22-VALIDATION.md` est rempli : quatre critères, quatre preuves nommées, aucun `(à mesurer)` restant.
5. `%APPDATA%\Chronos\` : `sessions/` à **66** entrées, `archived.json` à **84** octets, `oauth.dat` à
   **518** octets (taille seule). Overlay `Chronos-v3.0.2.exe` (pid 119412) ni lancé ni tué.
</verification>

<success_criteria>
- OBS-02 clos : les fichiers d'état listés sont ceux qui attendent et les plus récents ; le nombre de
  fichiers non listés est annoncé ; le dossier inspecté est celui du moniteur du widget.
- Une date absente reste absente ; un fichier corrompu ne casse pas le rapport.
- La phase est vérifiable critère par critère, avec des preuves nommées.
- Aucune donnée de l'utilisateur modifiée, aucune instance de l'overlay lancée ou tuée, aucune requête réseau.
</success_criteria>

<output>
Créer `.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-03-SUMMARY.md`, et livrer
`.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-VALIDATION.md` rempli.
</output>
