---
phase: 22-un-instrument-de-mesure-qui-ne-ment-plus
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/Chronos/Services/DiagnosticService.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/DiagnosticServiceTests.cs
  - tests/Chronos.Tests/GardesPerimetreTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
autonomous: true
requirements: [OBS-01, OBS-02]

must_haves:
  truths:
    - "Le rapport décrit le moniteur DU CONTENEUR — la même instance que celle que consomme le widget"
    - "Une session écartée apparaît dans le rapport comme masquée par tel filtre nommé, au lieu d'être absente"
    - "Le cas e465420e se lit en une ligne : identifiant, projet, état, âge, filtre qui le masque"
    - "La liste des sessions affichées n'est plus tronquée à huit et suit l'ordre du widget"
    - "Le diagnostic ne peut plus fabriquer son propre moniteur, et une garde falsifiable le prouve"
    - "Sans moniteur injecté, le rapport dit qu'il n'a rien observé au lieu d'inventer une lecture"
  artifacts:
    - path: "src/Chronos/Services/DiagnosticService.cs"
      provides: "Paramètre optionnel moniteurSessions + section widget alimentée par Inspecter"
      contains: "SessionMonitor? moniteurSessions = null"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Partage d'instance : le singleton SessionMonitor est passé au DiagnosticService"
      contains: "moniteurSessions: sp.GetRequiredService<SessionMonitor>()"
    - path: "tests/Chronos.Tests/GardesPerimetreTests.cs"
      provides: "Deux gardes de non-retour : aucune fabrication locale, et le moniteur vient du conteneur"
      contains: "Le_diagnostic_ne_fabrique_aucun_moniteur_de_sessions"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "DiagnosticService"
      via: "argument nommé moniteurSessions, résolu depuis le conteneur"
      pattern: "moniteurSessions: sp\\.GetRequiredService<SessionMonitor>\\(\\)"
    - from: "src/Chronos/Services/DiagnosticService.cs"
      to: "SessionMonitor.Inspecter"
      via: "une seule interrogation, à l'horloge du rapport"
      pattern: "_moniteurSessions\\.Inspecter\\(_clock\\.UtcNow\\)"
---

<objective>
OBS-01 — **le diagnostic cesse de décrire un système qui ne tourne nulle part.**

`DiagnosticService.cs:463` appelle aujourd'hui le constructeur nu du moniteur : sans le magasin d'archives
de l'application, sans le filtre « traité », sans le détecteur d'hystérésis. Le rapport ne décrit donc pas
le widget — il décrit un jumeau imaginaire. C'est très probablement **pourquoi le problème n'a jamais été
élucidé** : l'utilisateur lisait « des sessions mortes depuis des semaines » dans le **rapport**, jamais
dans le widget, et les deux ne parlaient pas du même objet.

La correction est un **PARTAGE D'INSTANCE**, jamais une copie de comportement. Le diagnostic interroge le
`SessionMonitor` du conteneur DI — celui que `Views.SessionsController` donne au widget. Conséquence voulue,
et c'est l'argument qui justifie de placer cette phase en n°2 du milestone : **les phases 23 à 26 changeront
le widget sans qu'une seule ligne du diagnostic ne change**, et elles seront vérifiées avec un instrument
qui ne ment plus.

Ce plan livre aussi le critère n°2 — **dire ce qui est masqué ET pourquoi** — en consommant la lecture
complète posée en vague 1, et le critère n°4 — **le non-retour**, prouvé par deux gardes falsifiables.

Purpose : c'est le cœur de la phase. Les trois autres plans l'outillent ou le complètent.

Output : un paramètre optionnel de plus, une section de rapport réécrite, un argument de DI, deux gardes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-CONTEXT.md
@.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-01-SUMMARY.md

<interfaces>
<!-- Contrats existants, extraits du dépôt le 2026-09-12, plus ceux créés en vague 1. -->

```csharp
// src/Chronos/Services/DiagnosticService.cs — 616 lignes. Constructeur ACTUEL (l. 45-58) :
    public DiagnosticService(IClaudeTokenReader tokenReader, ChronosPaths paths,
                             SettingsService settings, IUsageProvider composite, IClock clock,
                             IAuthStatus? authStatus = null, IEtatServeur? etatServeur = null,
                             IInventaireMachine? machine = null)
    {
        _tokenReader = tokenReader; _paths = paths; _settings = settings;
        _composite = composite; _clock = clock;
        _authStatus = authStatus; _etatServeur = etatServeur;
        _machine = machine ?? new InventaireMachine();
    }
```

**PROTOCOLE ÉTABLI, à suivre sans le rediscuter** : tout nouvel argument de `DiagnosticService` est
**optionnel et en DERNIÈRE position**. Précédents : `authStatus` (phase 17), `etatServeur` (phase 18),
`machine` (phase 20). Coût mesuré : **0 site de construction cassé** — 1 en production, 10 en tests.
Ne PAS toucher au repli `machine ?? new InventaireMachine()` : c'est lui qui rend les deux sondages chers
substituables et fait tourner la suite en 5 s au lieu de 2 min 12 (acquis de la phase 20).

```csharp
// src/Chronos/Services/DiagnosticService.cs, section à remplacer — l. 460-470, TELLE QUELLE :
        // Ce que le widget AFFICHE réellement (transcripts ~/.claude/projects + hooks, fusionnés + staleness).
        try
        {
            var detected = new SessionMonitor().Read(_clock.UtcNow);
            sb.AppendLine($"  Sessions détectées (widget) : {detected.Count}");
            foreach (var d in detected.Take(8))
                sb.AppendLine($"    · {d.Project} — {d.Activity} (maj il y a {(_clock.UtcNow - d.UpdatedAt).TotalMinutes:F0} min)");
            if (detected.Count == 0)
                sb.AppendLine("    → aucune session active récente (< 15 min). Utilise une session Claude Code puis rouvre ce diagnostic.");
        }
        catch (Exception ex) { sb.AppendLine("  (détection sessions impossible : " + ex.GetType().Name + ")"); }

        sb.AppendLine();

        // 5) Conseil
        sb.AppendLine("[Conseil]");
```

`DiagnosticService.cs` déclare `using System.Diagnostics; System.IO; System.Net.Http; System.Text;
System.Text.Json; Chronos.Models; Chronos.Text;` et vit dans `namespace Chronos.Services` — `SessionMonitor`,
`LectureSessions`, `MotifMasquage` et `AffichageSessions` y sont donc accessibles sans `using` supplémentaire.
`System.Linq` arrive par les `ImplicitUsings` (le fichier utilise déjà `.Take`, `.Select`, `.Distinct`).

```csharp
// Créés en vague 1 (plan 22-01) — À CONSOMMER, PAS À REDÉFINIR :
public enum MotifMasquage { Archivee, Traitee }
public sealed record SessionMasquee(SessionSnapshot Session, MotifMasquage Motif);
public sealed record LectureSessions(
    IReadOnlyList<SessionSnapshot> Visibles,
    IReadOnlyList<SessionMasquee> Masquees,
    int FichiersEcartesParAnciennete);

public sealed class SessionMonitor
{
    public string Directory { get; }
    public IReadOnlyList<SessionSnapshot> Read(System.DateTimeOffset now);   // => Inspecter(now).Visibles
    public LectureSessions Inspecter(System.DateTimeOffset now);
}

public static class AffichageSessions
{
    public static IReadOnlyList<SessionSnapshot> Ordonner(IEnumerable<SessionSnapshot> sessions);
    public static int Urgence(SessionActivity a);
    public static string Etat(SessionActivity a);       // « à toi » / « tour fini » / « en cours » / « inconnu »
    public static string Age(System.TimeSpan d);        // « à l'instant » / « il y a N min » / « il y a N h »
}
```

```csharp
// src/Chronos/App.xaml.cs — l. 226-228 : le SINGLETON à partager (déjà consommé par le widget l. 237-242) :
        services.AddSingleton(sp => new SessionMonitor(null, null, sp.GetRequiredService<ArchiveStore>(),
            sp.GetRequiredService<TreatedStore>(),
            sp.GetRequiredService<SessionTreatmentTracker>()));

        services.AddSingleton<ISessionsController>(sp => new Views.SessionsController(
            sp.GetRequiredService<SessionHookInstaller>(), sp.GetRequiredService<SettingsService>(),
            sp.GetRequiredService<SessionMonitor>(),        // ← LE widget lit CETTE instance
            sp.GetRequiredService<IClock>(), sp.GetRequiredService<ArchiveStore>()));

// src/Chronos/App.xaml.cs — l. 367-374 : l'enregistrement À COMPLÉTER :
        services.AddSingleton(sp => new DiagnosticService(
            sp.GetRequiredService<IClaudeTokenReader>(),
            sp.GetRequiredService<ChronosPaths>(),
            sp.GetRequiredService<SettingsService>(),
            sp.GetRequiredService<IUsageProvider>(),
            sp.GetRequiredService<IClock>(),
            sp.GetRequiredService<IAuthStatus>(),     // TOK-02 : le rapport dit l'état RÉEL
            sp.GetRequiredService<IEtatServeur>()));  // HDR-03/HDR-04 : le rapport nomme ce que la sonde reçoit
```

```csharp
// tests/Chronos.Tests/GardesPerimetreTests.cs — 103 lignes, l'aide est déjà là :
internal static string CheminSources();   // chemin des sources INJECTÉ par MSBuild, jamais deviné
// Elle rend la racine de src/Chronos : Path.Combine(CheminSources(), "Services", "DiagnosticService.cs")
// et Path.Combine(CheminSources(), "App.xaml.cs") sont les deux chemins utiles ici.
```

```csharp
// tests/Chronos.Tests/DiagnosticServiceTests.cs — outillage existant :
private static ChronosPaths TempPaths();       // usage.json + projects sous %TEMP%, JAMAIS %APPDATA%
private sealed class StubProvider : IUsageProvider { ... }
// Les 10 sites existants passent `machine: new FakeInventaireMachine()` en argument NOMMÉ : ajouter un
// paramètre APRÈS `machine` ne casse aucun d'eux.
// tests/Chronos.Tests/Fakes/ contient FakeClock, FakeClaudeTokenReader, FakeAuthStatus,
// FakeEtatServeur, FakeInventaireMachine.
```

**EFFET DE BORD ASSUMÉ, à comprendre avant d'implémenter** : `Inspecter` invoque
`_tracker?.Observe(raw, now)` comme `Read` l'a toujours fait. Le rapport de démarrage
(`LogStartupAsync`, App.xaml.cs l. 75) déclenchera donc une observation du détecteur **quelques dizaines de
millisecondes avant** celle du widget (`ShowIfEnabled`, l. 117), au lieu de laisser le widget être le
premier. C'est le prix — et le sens — du partage d'instance : le diagnostic paie exactement ce que le widget
paie. Le détecteur n'agit que sur des TRANSITIONS entre deux cycles ; une observation supplémentaire au
même instant ne produit aucune transition. **Ne PAS contourner cet effet en fabriquant un moniteur sans
détecteur** : ce serait revenir au défaut que ce plan corrige.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : le rapport lit le moniteur qu'on lui donne, dit ce qui est masqué et nomme le filtre</name>
  <files>src/Chronos/Services/DiagnosticService.cs, tests/Chronos.Tests/DiagnosticServiceTests.cs</files>

  <read_first>
    - src/Chronos/Services/DiagnosticService.cs (l. 1-60 — les `using`, les champs, le constructeur et ses
      XML-doc qui documentent le protocole du paramètre optionnel en dernière position ; puis l. 396-475 —
      toute la section « Widget sessions Claude Code » jusqu'à `[Conseil]`)
    - src/Chronos/Services/LectureSessions.cs et src/Chronos/Services/AffichageSessions.cs (créés en vague 1)
    - tests/Chronos.Tests/DiagnosticServiceTests.cs (l. 1-100 — `TempPaths`, `StubProvider`, et la façon
      dont les 10 sites existants passent `machine:` en argument nommé)
    - .planning/debug/widget-sessions-statuts.md (entrée du 2026-09-12T13:38Z — le constat qui motive ce plan)
  </read_first>

  <behavior>
    - Sans moniteur injecté, le rapport contient « MONITEUR NON INJECTÉ » et **aucune** liste de sessions :
      rien n'a été observé, donc rien n'est affirmé.
    - Avec un moniteur injecté portant une session visible, le rapport nomme le projet, l'état au libellé du
      widget (« à toi ») et l'âge au libellé du widget (« il y a 5 min »).
    - Avec une session masquée par le magasin « traité », le rapport porte une ligne contenant l'identifiant
      court, le projet, et « masquée par treated.json ».
    - Avec une session archivée, la ligne contient « masquée par archived.json ».
    - **Cas `e465420e`** : le rapport contient sur une seule ligne l'identifiant court `e465420e`, le projet
      `PROJET ADVANCED SHEET`, l'état « à toi » et « masquée par treated.json » — et par ailleurs le nombre
      de fichiers de hook écartés pour ancienneté.
    - Neuf sessions visibles → les neuf sont listées (plus aucune troncature à huit) et l'attente est en tête.
    - Un moniteur qui lève à la lecture ne casse pas le rapport : la ligne d'échec nomme le type d'exception.
  </behavior>

  <action>
Écrire d'abord les tests, les voir échouer, puis implémenter. Le fichier reste compilable à chaque commit.

**1. `src/Chronos/Services/DiagnosticService.cs` — le champ, le paramètre, l'affectation.**

Ajouter le champ après `_machine` (l. 31) :

```csharp
    private readonly SessionMonitor? _moniteurSessions;
```

Ajouter le paramètre en DERNIÈRE position et son XML-doc, à la suite de celui de `machine` :

```csharp
    /// <param name="moniteurSessions">OBS-01 — LE moniteur du widget, partagé par le conteneur DI, jamais
    /// un second exemplaire. Le rapport décrivait jusqu'ici un moniteur fabriqué ici même, donc nu : sans le
    /// magasin d'archives de l'application, sans le filtre « traité », sans le détecteur d'hystérésis. Il
    /// décrivait un jumeau imaginaire — et c'est très probablement ce qui a rendu le défaut du widget
    /// inélucidable, l'utilisateur lisant dans le RAPPORT des sessions que le widget ne montrait pas.
    ///
    /// <para>Le partage d'instance n'est pas un détail d'implémentation : il est la raison pour laquelle le
    /// rapport suivra chaque changement futur du câblage du widget sans qu'une ligne de ce fichier ne
    /// change. Une copie du comportement du widget rouvrirait l'écart le lendemain.</para>
    ///
    /// <para>AUCUN REPLI, contrairement à <paramref name="machine"/> : nul n'est absent, le rapport dit
    /// qu'il n'a rien observé. Fabriquer un moniteur de secours ici, c'est exactement le défaut corrigé.
    /// OPTIONNEL et en DERNIÈRE position à dessein : les 11 sites de construction préexistants compilent
    /// sans retouche. Précédents : authStatus (17), etatServeur (18), machine (20).</para></param>
```

Compléter la signature et le corps :

```csharp
                             IInventaireMachine? machine = null,
                             SessionMonitor? moniteurSessions = null)
    {
        ...
        _machine = machine ?? new InventaireMachine();
        _moniteurSessions = moniteurSessions;   // pas de repli : voir le XML-doc ci-dessus
    }
```

**2. Remplacer la section l. 460-470** (depuis le commentaire « Ce que le widget AFFICHE réellement »
jusqu'au `catch` inclus) par :

```csharp
        // OBS-01 — Ce que le widget AFFICHE, lu sur LE moniteur du widget (l'instance du conteneur DI), à
        // l'instant du rapport. Ce fichier ne fabrique plus de moniteur : celui qu'il bâtissait était nu —
        // sans magasin d'archives, sans filtre « traité », sans détecteur — donc le rapport décrivait un
        // système qui ne tournait nulle part. Conséquence du partage d'instance : tout changement futur du
        // câblage du widget se reflète ici SANS qu'une ligne de ce fichier ne change.
        if (_moniteurSessions is null)
            sb.AppendLine("  Sessions (widget) : MONITEUR NON INJECTÉ — rien n'a été observé ici.");
        else
        {
            try
            {
                var lecture = _moniteurSessions.Inspecter(_clock.UtcNow);

                sb.AppendLine($"  Fichiers de hook écartés (trop anciens) : {lecture.FichiersEcartesParAnciennete}");

                // Pas de troncature : le widget n'en applique aucune, et comparer ligne à ligne un rapport
                // tronqué avec un écran complet, c'est reconstruire l'écart que ce plan ferme.
                sb.AppendLine($"  Sessions AFFICHÉES par le widget : {lecture.Visibles.Count}");
                foreach (var d in AffichageSessions.Ordonner(lecture.Visibles))
                    sb.AppendLine($"    · {Court(d.SessionId)} {d.Project} — {AffichageSessions.Etat(d.Activity)}"
                                + $" ({AffichageSessions.Age(_clock.UtcNow - d.UpdatedAt)})");
                if (lecture.Visibles.Count == 0)
                    sb.AppendLine("    → aucune session à l'écran. Si tu en attendais une, lis la liste des MASQUÉES juste en dessous.");

                // Le critère n°2 de la phase. Une session écartée n'est pas absente : elle est écartée PAR
                // QUELQUE CHOSE, et ce quelque chose a un nom et un fichier que l'utilisateur peut ouvrir.
                // Cas fondateur, mesuré le 2026-09-12 : e465420e, session vivante en attente de permission
                // depuis 10 h, écartée par treated.json — invisible du widget ET du rapport.
                sb.AppendLine($"  Sessions MASQUÉES par un filtre : {lecture.Masquees.Count}");
                foreach (var m in lecture.Masquees
                             .OrderBy(m => AffichageSessions.Urgence(m.Session.Activity))
                             .ThenByDescending(m => m.Session.UpdatedAt))
                    sb.AppendLine($"    · {Court(m.Session.SessionId)} {m.Session.Project} — {AffichageSessions.Etat(m.Session.Activity)}"
                                + $" ({AffichageSessions.Age(_clock.UtcNow - m.Session.UpdatedAt)}) — masquée par {LibelleMotif(m.Motif)}");
                if (lecture.Masquees.Count == 0)
                    sb.AppendLine("    (aucune — aucun filtre n'écarte de session en ce moment)");
            }
            catch (Exception ex) { sb.AppendLine("  (lecture du moniteur impossible : " + ex.GetType().Name + ")"); }
        }
```

**3. Ajouter les deux aides privées**, juste après `LibelleAuth` (vers l. 480) :

```csharp
    // Le filtre qui écarte, NOMMÉ AVEC SON FICHIER : « absent » n'apprend rien, « écarté par treated.json »
    // dit quoi ouvrir. C'est toute la différence entre un défaut inélucidable et un défaut diagnosticable.
    private static string LibelleMotif(MotifMasquage m) => m switch
    {
        MotifMasquage.Archivee => "archived.json (archivage — geste explicite de l'utilisateur)",
        MotifMasquage.Traitee  => "treated.json (hystérésis « traité » — posée automatiquement)",
        _                      => "un filtre non nommé",
    };

    // Huit premiers caractères de l'identifiant : assez pour retrouver le fichier d'état correspondant dans
    // %APPDATA%\Chronos\sessions, assez court pour que la ligne reste lisible.
    private static string Court(string id) => id.Length <= 8 ? id : id[..8];
```

**4. `tests/Chronos.Tests/DiagnosticServiceTests.cs`** — ajouter en fin de classe, sans toucher aux tests
existants :

```csharp
    // --- Phase 22 (OBS-01/OBS-02) : le rapport décrit LE moniteur du widget, et nomme ce qui est masqué ---
    //
    // SÉCURITÉ, commune à ces tests : FakeClaudeTokenReader à Token = null (la sonde réseau est gardée par
    // `if (token is not null)`, aucune requête ne part), FakeInventaireMachine (aucun balayage de coffre),
    // et TOUS les chemins de moniteur/magasins sont sous %TEMP%. Aucun n'écrit dans %APPDATA%\Chronos.

    private static readonly DateTimeOffset T22 = new(2026, 9, 12, 13, 28, 0, TimeSpan.Zero);

    private sealed class SourceFixe22 : ISessionSource
    {
        private readonly IReadOnlyList<SessionSnapshot> _snaps;
        public SourceFixe22(params SessionSnapshot[] snaps) => _snaps = snaps;
        public IReadOnlyList<SessionSnapshot> Read(DateTimeOffset now) => _snaps;
    }

    private static string TempDir22()
    {
        var d = System.IO.Path.Combine(System.IO.Path.GetTempPath(), "chronos-diag22-" + Guid.NewGuid().ToString("N"));
        System.IO.Directory.CreateDirectory(d);
        Assert.StartsWith(System.IO.Path.GetTempPath(), d);
        return d;
    }

    private static string TempFichier22() => System.IO.Path.Combine(TempDir22(), "magasin.json");

    private static async Task<string> Rapport(SessionMonitor? moniteur)
    {
        var paths = TempPaths();
        var diag = new DiagnosticService(new FakeClaudeTokenReader { Token = null }, paths,
                                         new SettingsService(paths), new StubProvider(UsageSnapshot.Empty),
                                         new FakeClock(T22), machine: new FakeInventaireMachine(),
                                         moniteurSessions: moniteur);
        return await diag.BuildReportAsync();
    }

    /// <summary>Doctrine du milestone appliquée au rapport lui-même : sans moniteur, on ne raconte pas une
    /// lecture. L'ancien code fabriquait ici un moniteur nu et présentait sa sortie comme « ce que le widget
    /// affiche » — un mensonge poli, et probablement la raison pour laquelle le défaut n'a jamais été élucidé.</summary>
    [Fact]
    public async Task Sans_moniteur_le_rapport_dit_qu_il_n_a_rien_observe()
    {
        var report = await Rapport(null);

        Assert.Contains("MONITEUR NON INJECTÉ", report);
        Assert.DoesNotContain("Sessions AFFICHÉES par le widget", report);
    }

    [Fact]
    public async Task Le_rapport_decrit_les_sessions_du_moniteur_qu_on_lui_donne()
    {
        var moniteur = new SessionMonitor(TempDir22(),
            new SourceFixe22(new SessionSnapshot("s-att-0001", "overlay", SessionActivity.WaitingAttention,
                                                 "permission_prompt", T22.AddMinutes(-5))),
            new ArchiveStore(TempFichier22()));

        var report = await Rapport(moniteur);

        Assert.Contains("Sessions AFFICHÉES par le widget : 1", report);
        Assert.Contains("overlay — à toi (il y a 5 min)", report);   // libellés IDENTIQUES à ceux du widget
    }

    /// <summary>Critère n°2 de la phase — ce qui est masqué est dit, ET on dit par quoi. Reconstitution du
    /// relevé du 2026-09-12T13:28Z : e465420e (PROJET ADVANCED SHEET), session VIVANTE en attente de
    /// permission, que treated.json cachait pour 6 h. Le widget ne la montrait nulle part ; le rapport non
    /// plus. Une seule ligne doit désormais suffire à la retrouver.</summary>
    [Fact]
    public async Task Le_cas_e465420e_se_lit_en_une_ligne_du_rapport()
    {
        const string Id = "e465420e-83e0-428f-97f1-f0174c0848fc";
        var treated = new TreatedStore(TempFichier22());
        treated.Set(Id, T22.ToUnixTimeMilliseconds());

        var moniteur = new SessionMonitor(TempDir22(),
            new SourceFixe22(new SessionSnapshot(Id, "PROJET ADVANCED SHEET",
                                                 SessionActivity.WaitingAttention, "permission_prompt",
                                                 T22.AddMinutes(-10))),
            new ArchiveStore(TempFichier22()), treated);

        var report = await Rapport(moniteur);

        Assert.Contains("Sessions AFFICHÉES par le widget : 0", report);
        var ligne = report.Split('\n').Single(l => l.Contains("e465420e"));
        Assert.Contains("PROJET ADVANCED SHEET", ligne);
        Assert.Contains("à toi", ligne);
        Assert.Contains("masquée par treated.json", ligne);
    }

    [Fact]
    public async Task Une_session_archivee_est_annoncee_masquee_par_le_magasin_d_archives()
    {
        var archive = new ArchiveStore(TempFichier22());
        archive.Add("arch-0001-xxxx");
        var moniteur = new SessionMonitor(TempDir22(),
            new SourceFixe22(new SessionSnapshot("arch-0001-xxxx", "vieux-projet", SessionActivity.WaitingTurn,
                                                 null, T22)),
            archive);

        var report = await Rapport(moniteur);

        Assert.Contains("Sessions MASQUÉES par un filtre : 1", report);
        Assert.Contains("masquée par archived.json", report);
    }

    /// <summary>OBS-02 — la troncature à huit disparaît : comparer ligne à ligne un rapport tronqué avec un
    /// écran complet reconstruirait l'écart que cette phase ferme. Neuf sessions, neuf lignes, attente en tête.</summary>
    [Fact]
    public async Task Le_rapport_ne_tronque_plus_la_liste_des_sessions_affichees()
    {
        var snaps = Enumerable.Range(0, 9)
            .Select(i => new SessionSnapshot($"sess-{i:D4}-xx", $"projet-{i}",
                        i == 8 ? SessionActivity.WaitingAttention : SessionActivity.Working,
                        null, T22.AddMinutes(-i)))
            .ToArray();

        var report = await Rapport(new SessionMonitor(TempDir22(), new SourceFixe22(snaps),
                                                      new ArchiveStore(TempFichier22())));

        Assert.Contains("Sessions AFFICHÉES par le widget : 9", report);
        for (var i = 0; i < 9; i++) Assert.Contains($"projet-{i}", report);

        var lignes = report.Split('\n').Where(l => l.Contains("projet-")).ToList();
        Assert.Equal(9, lignes.Count);
        Assert.Contains("projet-8", lignes[0]);   // l'attente passe devant, comme dans le widget
    }
```
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~DiagnosticServiceTests|FullyQualifiedName~CadranBindingTests|FullyQualifiedName~NormalisationUniqueTests|FullyQualifiedName~ServicesLayerPurityTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "SessionMonitor? moniteurSessions = null" src/Chronos/Services/DiagnosticService.cs` rend **1**
      — le paramètre est bien optionnel et en dernière position
    - `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` rend **0** — le fichier ne
      fabrique plus aucun moniteur, ni en code ni en commentaire (les XML-doc de ce plan sont rédigés sans
      cette formule, à dessein)
    - `grep -cF "_moniteurSessions.Inspecter(_clock.UtcNow)" src/Chronos/Services/DiagnosticService.cs` rend **1**
      — une seule interrogation, à l'horloge du rapport
    - `grep -cF "?? new " src/Chronos/Services/DiagnosticService.cs` rend **2** — INCHANGÉ : la mention en
      XML-doc (l. 42) et le repli réel de `machine` (l. 57). Aucun nouveau repli n'est introduit
    - `grep -cF "Take(8)" src/Chronos/Services/DiagnosticService.cs` rend **2** — il en restait 3 ; celle de
      la liste des sessions disparaît. Les deux survivantes sont la liste des fichiers d'état (plan 22-03)
      et la description de forme d'un blob d'identifiant
    - `grep -cF "masquée par" src/Chronos/Services/DiagnosticService.cs` rend **1** — la SEULE occurrence
      est la ligne de rapport. Les commentaires de ce plan écrivent volontairement « écartée par » / « le
      filtre qui écarte » pour ne pas gonfler ce compte
    - `grep -cF "private static string LibelleMotif" src/Chronos/Services/DiagnosticService.cs` rend **1**
    - Les 5 nouveaux tests de `DiagnosticServiceTests` passent, et les 10 sites de construction
      préexistants n'ont PAS été retouchés :
      `git diff -U0 tests/Chronos.Tests/DiagnosticServiceTests.cs | grep -c "^-[^-]"` rend **0**
      (le motif `^-[^-]` ignore l'en-tête `--- a/…` que `git diff` émet toujours)
    - `NormalisationUniqueTests` et `ServicesLayerPurityTests` sont verts
  </acceptance_criteria>

  <done>
Le rapport décrit le moniteur qu'on lui donne, liste sans troncature ce que le widget montre, et nomme le
filtre qui masque chaque session écartée — le cas `e465420e` tient sur une ligne.
  </done>
</task>

<task type="auto">
  <name>Task 2 : partage d'instance câblé, et deux gardes falsifiables qui interdisent le retour en arrière</name>
  <files>src/Chronos/App.xaml.cs, tests/Chronos.Tests/GardesPerimetreTests.cs, tests/Chronos.Tests/CompositionRootTests.cs</files>

  <read_first>
    - src/Chronos/App.xaml.cs (l. 216-242 — l'enregistrement du `SessionMonitor` singleton et sa
      consommation par `Views.SessionsController` ; puis l. 366-375 — l'enregistrement du `DiagnosticService`
      à compléter)
    - src/Chronos/Services/DiagnosticService.cs (le constructeur issu de la tâche 1)
    - tests/Chronos.Tests/GardesPerimetreTests.cs (INTÉGRALEMENT, 103 lignes — le motif des gardes de SOURCE
      de la phase 21 et l'aide `CheminSources()`)
    - tests/Chronos.Tests/CompositionRootTests.cs (l. 128-162 —
      `Le_graphe_DI_resout_la_chaine_de_sessions`, miroir exact d'`App.xaml.cs`, à compléter)
  </read_first>

  <action>
**1. `src/Chronos/App.xaml.cs`** — compléter l'enregistrement du `DiagnosticService` (l. 367-374). L'argument
est NOMMÉ parce qu'il faut sauter `machine`, dont la production garde volontairement le repli réel :

```csharp
            sp.GetRequiredService<IEtatServeur>(),     // HDR-03/HDR-04 : le rapport nomme ce que la sonde reçoit
            // OBS-01 — LE moniteur du widget, pas un second exemplaire. PARTAGE D'INSTANCE : le singleton
            // que Views.SessionsController consomme (l. 240) est exactement celui que le rapport interroge.
            // C'est ce qui rend le rapport vrai à chaque commit ultérieur PAR CONSTRUCTION : les phases 23
            // à 26 changeront le câblage du widget sans qu'une ligne du diagnostic ne change. Une copie du
            // comportement du widget aurait rouvert l'écart dès la phase suivante.
            // `machine` est sauté par argument NOMMÉ : la production conserve son repli réel (phase 20).
            moniteurSessions: sp.GetRequiredService<SessionMonitor>()));
```

Ne rien changer d'autre : ni l'ordre des enregistrements, ni `OnStartup`.

**2. `tests/Chronos.Tests/GardesPerimetreTests.cs`** — ajouter les DEUX gardes de non-retour (critère n°4
de la phase). Contrôle de SOURCE dans les deux cas : `OnStartup` monte un host WPF complet et n'est pas
instanciable sous test, et la réflexion ne voit pas un appel de constructeur.

```csharp
    /// <summary>
    /// GARDE DE NON-RETOUR (OBS-01, phase 22) — le diagnostic ne peut plus fabriquer son propre moniteur.
    ///
    /// <para>Le défaut corrigé n'était pas une faute de frappe mais une classe d'erreur : un rapport qui
    /// décrit un système reconstruit à la volée, donc différent de celui qui tourne. Relevé le 2026-09-12 :
    /// le moniteur fabriqué dans le rapport n'avait ni magasin d'archives, ni filtre « traité », ni
    /// détecteur. L'utilisateur lisait des sessions vieilles de plusieurs semaines dans le RAPPORT, jamais
    /// dans le widget — c'est très probablement pourquoi le problème n'a jamais été élucidé.</para>
    ///
    /// <para>Cette garde vaut surtout pour l'AVENIR : les phases 23 à 26 modifient le widget, et elles sont
    /// vérifiées avec cet instrument. Qu'il redevienne autonome un seul commit, et elles seraient vérifiées
    /// avec un instrument à nouveau faussé, sans que rien ne le signale.</para>
    /// </summary>
    [Fact]
    public void Le_diagnostic_ne_fabrique_aucun_moniteur_de_sessions()
    {
        var fichier = Path.Combine(CheminSources(), "Services", "DiagnosticService.cs");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        // Une garde qui lirait un fichier vide, ou un fichier où la section a disparu, serait muette.
        Assert.Contains("BuildReportAsync", texte, StringComparison.Ordinal);
        Assert.Contains("_moniteurSessions.Inspecter(_clock.UtcNow)", texte, StringComparison.Ordinal);

        Assert.DoesNotContain("new SessionMonitor", texte, StringComparison.Ordinal);
    }

    /// <summary>
    /// GARDE DE PARTAGE D'INSTANCE (OBS-01). La garde précédente empêche la FABRICATION ; celle-ci exige
    /// l'INJECTION. Sans elle, supprimer l'argument de composition laisserait le rapport parfaitement
    /// honnête — il dirait « moniteur non injecté » — et parfaitement inutile, en silence.
    ///
    /// <para>L'assertion porte sur le fragment d'enregistrement du DiagnosticService et non sur le fichier
    /// entier : `GetRequiredService&lt;SessionMonitor&gt;()` figure aussi dans l'enregistrement du contrôleur
    /// du widget, et une recherche globale resterait verte alors que le diagnostic aurait été débranché.</para>
    /// </summary>
    [Fact]
    public void Le_diagnostic_recoit_le_moniteur_du_conteneur()
    {
        var fichier = Path.Combine(CheminSources(), "App.xaml.cs");
        Assert.True(File.Exists(fichier), $"Fichier introuvable : {fichier}");

        var texte = File.ReadAllText(fichier);

        var debut = texte.IndexOf("new DiagnosticService(", StringComparison.Ordinal);
        Assert.True(debut >= 0, "Enregistrement du DiagnosticService introuvable dans App.xaml.cs");
        var fin = texte.IndexOf("));", debut, StringComparison.Ordinal);
        Assert.True(fin > debut, "Fin de l'enregistrement du DiagnosticService introuvable");

        var enregistrement = texte[debut..fin];
        Assert.Contains("moniteurSessions: sp.GetRequiredService<SessionMonitor>()", enregistrement,
                        StringComparison.Ordinal);
    }
```

**3. `tests/Chronos.Tests/CompositionRootTests.cs`** — dans
`Le_graphe_DI_resout_la_chaine_de_sessions`, après le `Assert.NotNull` existant, ajouter la preuve que le
partage est possible, c'est-à-dire que l'enregistrement est bien un SINGLETON :

```csharp
        // OBS-01 — le partage d'instance repose entièrement sur la portée : passer ce moniteur en transient
        // donnerait au diagnostic un exemplaire distinct de celui du widget, avec ses propres magasins
        // rechargés et son propre détecteur sans mémoire. Le rapport redeviendrait faux, sans rien casser
        // au build ni au démarrage.
        Assert.Same(provider.GetRequiredService<SessionMonitor>(), provider.GetRequiredService<SessionMonitor>());
```

**4. FALSIFIER LES GARDES AVANT DE COMMITTER** — motif établi au plan 21-03, obligatoire ici parce que le
critère n°4 de la phase exige une garde *falsifiable*, pas seulement *verte*. Trois mutations, chacune
appliquée, observée rouge, puis RÉVOQUÉE :

  a. Dans `App.xaml.cs`, supprimer la ligne `moniteurSessions: …` (et fermer la parenthèse)
     → `Le_diagnostic_recoit_le_moniteur_du_conteneur` doit ÉCHOUER. Révoquer.
  b. Dans `DiagnosticService.cs`, remplacer `_moniteurSessions.Inspecter(...)` par une construction locale
     `new SessionMonitor().Inspecter(...)` → `Le_diagnostic_ne_fabrique_aucun_moniteur_de_sessions` doit
     ÉCHOUER. Révoquer.
  c. Dans `CompositionRootTests`, passer le `SessionMonitor` en `AddTransient` → le nouvel `Assert.Same`
     doit ÉCHOUER. Révoquer.

Consigner les trois observations dans le SUMMARY. Après révocation, `git diff` sur `src/` ne doit montrer
que les modifications prévues par ce plan.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -c Debug --nologo -v q --filter "FullyQualifiedName~GardesPerimetreTests|FullyQualifiedName~CompositionRootTests|FullyQualifiedName~DiagnosticServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "GetRequiredService<SessionMonitor>()" src/Chronos/App.xaml.cs` rend **2** — une fois pour le
      contrôleur du widget, une fois pour le diagnostic : c'est le partage d'instance, lisible à l'œil
    - `grep -cF "moniteurSessions: sp.GetRequiredService<SessionMonitor>()" src/Chronos/App.xaml.cs` rend **1**
    - `grep -cF "AddSingleton(sp => new SessionMonitor(" src/Chronos/App.xaml.cs` rend **1** — la portée
      reste SINGLETON, sans quoi le partage n'existe pas
    - `grep -cF "Le_diagnostic_ne_fabrique_aucun_moniteur_de_sessions" tests/Chronos.Tests/GardesPerimetreTests.cs` rend **1**
    - `grep -cF "Le_diagnostic_recoit_le_moniteur_du_conteneur" tests/Chronos.Tests/GardesPerimetreTests.cs` rend **1**
    - `grep -cF "Assert.Same(provider.GetRequiredService<SessionMonitor>()" tests/Chronos.Tests/CompositionRootTests.cs` rend **1**
    - `dotnet build Chronos.sln -c Debug` réussit
    - Les trois mutations de falsification ont été observées ROUGES puis révoquées, et le SUMMARY les nomme
    - `CompositionRootTests`, `GardesPerimetreTests`, `ServicesLayerPurityTests`, `NormalisationUniqueTests`,
      `GardesDoctrineTests` et `La_sonde_d_en_tetes_est_le_PRIMAIRE_de_la_chaine_exacte` sont verts
  </acceptance_criteria>

  <done>
Le rapport de l'application interroge l'instance exacte que lit le widget, et deux gardes prouvées
falsifiables empêchent le diagnostic de redevenir autonome au cours des phases 23 à 26.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test Chronos.sln -c Debug --nologo -v q` → **0 échec**, total **≥ 719** (aucun test supprimé).
2. `grep -cF "new SessionMonitor" src/Chronos/Services/DiagnosticService.cs` rend **0**.
3. `grep -cF "GetRequiredService<SessionMonitor>()" src/Chronos/App.xaml.cs` rend **2**.
4. Les trois mutations de falsification sont consignées au SUMMARY, et `git status --porcelain` ne montre
   aucun reliquat de mutation.
5. `%APPDATA%\Chronos\sessions` toujours à **66** entrées, `archived.json` toujours **84** octets,
   `oauth.dat` toujours **518** octets (taille seulement — jamais le mtime). L'overlay
   `Chronos-v3.0.2.exe` (pid 119412) n'a été ni lancé ni tué.
</verification>

<success_criteria>
- OBS-01 est livré en PARTAGE D'INSTANCE : le rapport interroge le `SessionMonitor` du conteneur, celui-là
  même que consomme le widget.
- Une session écartée est annoncée « masquée par » un filtre nommé avec son fichier ; le cas `e465420e`
  tient sur une ligne.
- La liste des sessions affichées n'est plus tronquée et suit l'ordre du widget.
- Sans moniteur injecté, le rapport dit qu'il n'a rien observé — il n'invente pas une lecture.
- Deux gardes, prouvées falsifiables par mutation réelle, interdisent le retour en arrière.
</success_criteria>

<output>
Créer `.planning/phases/22-un-instrument-de-mesure-qui-ne-ment-plus/22-02-SUMMARY.md`, en y consignant
nominativement les trois mutations de falsification et leur révocation.
</output>
