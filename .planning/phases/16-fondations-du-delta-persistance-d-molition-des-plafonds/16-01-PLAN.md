---
phase: 16-fondations-du-delta-persistance-d-molition-des-plafonds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/App.xaml.cs
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/Views/SettingsWindow.xaml
  - src/Chronos/Services/RefreshOrchestrator.cs
  - src/Chronos/Services/GatedOAuthUsageProvider.cs
  - src/Chronos/Services/IBudgetPrompt.cs
  - src/Chronos/Views/BudgetPrompt.cs
  - src/Chronos/Views/BudgetDialog.xaml
  - src/Chronos/Views/BudgetDialog.xaml.cs
  - src/Chronos/ViewModels/BudgetDialogViewModel.cs
  - src/Chronos/Services/BudgetAutoCalibrator.cs
  - src/Chronos/Services/BudgetCalibration.cs
  - tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs
  - tests/Chronos.Tests/MainViewModelTests.cs
  - tests/Chronos.Tests/CadranBindingTests.cs
  - tests/Chronos.Tests/OverlayWindowConfigTests.cs
  - tests/Chronos.Tests/ThemingTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
  - tests/Chronos.Tests/BudgetAutoCalibratorTests.cs
  - tests/Chronos.Tests/BudgetCalibrationTests.cs
autonomous: true
requirements: [DEL-05]

must_haves:
  truths:
    - "L'utilisateur ouvre les réglages et ne voit plus aucune entrée « Plafonds… » : aucun dialogue de calibration n'est atteignable."
    - "L'application démarre sans exception de résolution DI (le point d'accroche `GetRequiredService<BudgetAutoCalibrator>()` a disparu d'App.OnStartup)."
    - "Aucun type de calibration de plafond (`IBudgetPrompt`, `BudgetPrompt`, `BudgetDialog`, `BudgetDialogViewModel`, `BudgetAutoCalibrator`, `BudgetCalibration`) n'existe plus dans le dépôt."
    - "La solution compile et la suite de tests reste à 0 échec après chacun des trois commits."
  artifacts:
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "ViewModel sans dépendance IBudgetPrompt ni commande CalibrateBudgets"
      absent_pattern: "IBudgetPrompt|CalibrateBudgets|BudgetSelection"
    - path: "src/Chronos/Views/SettingsWindow.xaml"
      provides: "UniformGrid RÉGLAGES à 3 boutons"
      absent_pattern: "Plafonds|CalibrateBudgetsCommand"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Composition racine sans IBudgetPrompt ni BudgetAutoCalibrator"
      absent_pattern: "BudgetAutoCalibrator|IBudgetPrompt"
    - path: "tests/Chronos.Tests/CompositionRootTests.cs"
      provides: "Garde DI sans les enregistrements de calibration"
      absent_pattern: "BudgetAutoCalibrator|IBudgetPrompt|FakeBudgetPrompt"
  key_links:
    - from: "src/Chronos/App.xaml.cs (OnStartup)"
      to: "_host.StartAsync()"
      via: "aucune résolution forcée de BudgetAutoCalibrator ne subsiste avant StartAsync"
      pattern: "GetRequiredService<MainViewModel>"
    - from: "src/Chronos/Views/SettingsWindow.xaml"
      to: "MainViewModel"
      via: "les 3 commandes restantes (RecalibrateCommand, ToggleStatusLineSourceCommand, Diagnostic_Click) sont toujours liées"
      pattern: "RecalibrateCommand"
---

<objective>
Démolir le sous-système de calibration des plafonds : l'interface utilisateur (bouton, dialogue, prompt),
le calibrateur automatique et la logique pure de déduction — soit 7 fichiers de production, 3 fichiers de
test et 6 points d'accroche localisés à la ligne près par la recherche.

Purpose: DEL-05. La calibration des plafonds est la cause racine des pourcentages faux après le passage
Max x5 → Max x20 (`FiveHourTokenBudget` figé en source `Manual`, `ApplyAuto` refusant par conception de
l'écraser). Supprimer le code rend le bug structurellement impossible, au lieu de le corriger.

Output: une base de code où plus aucun type ne sait déduire, saisir ou appliquer un plafond de tokens.
Les six CHAMPS de `ChronosSettings` survivent volontairement à ce plan (leur dernier lecteur métier,
`JsonlEstimationProvider`, ne disparaît qu'au plan 16-03) — c'est ce qui garantit qu'on compile à chaque
commit. Leur suppression est le plan 16-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contrats que l'exécuteur doit connaître SANS explorer la base de code. -->

Signature ACTUELLE du ctor de MainViewModel (src/Chronos/ViewModels/MainViewModel.cs:184-190) —
12 paramètres, le 7e (`IBudgetPrompt budgetPrompt`) doit disparaître → arité 11 :

```csharp
public MainViewModel(
    RefreshOrchestrator orchestrator, IUiDispatcher ui, IClock clock,
    IWindowController controller, IAutostartService autostart,
    IRecalibrationPrompt prompt, IBudgetPrompt budgetPrompt, SettingsService settings,
    DiagnosticService diagnostic, IStatusLineSetup statusLineSetup, IOAuthLogin oauthLogin,
    ISessionsController sessions)
```

Types SUPPRIMÉS par ce plan (aucun ne doit subsister) :
```csharp
public sealed record BudgetSelection(long? FiveHour, long? Weekly);          // Services/IBudgetPrompt.cs
public interface IBudgetPrompt { BudgetSelection? Ask(long?, long?); }        // Services/IBudgetPrompt.cs
public sealed class BudgetPrompt : IBudgetPrompt                             // Views/BudgetPrompt.cs
public sealed partial class BudgetDialog : Window                            // Views/BudgetDialog.xaml(.cs)
public sealed partial class BudgetDialogViewModel : ObservableObject         // ViewModels/
public sealed class BudgetAutoCalibrator : IDisposable                       // Services/
public static class BudgetCalibration                                        // Services/
```

Type CONSERVÉ malgré tout (ne PAS supprimer dans ce plan) :
```csharp
public enum BudgetSource { None, Manual, Auto }   // Services/BudgetSource.cs
```
Raison : `ChronosSettings.FiveHourBudgetSource` / `WeeklyBudgetSource` le référencent encore. Le supprimer
ici casserait la compilation. Il part au plan 16-04, APRÈS les six champs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer l'entrée « Plafonds… », la commande et le dialogue (geste atomique — arité du ctor)</name>

  <read_first>
    - src/Chronos/ViewModels/MainViewModel.cs (lignes 20-35, 178-200, 320-350)
    - src/Chronos/Views/SettingsWindow.xaml (lignes 300-315)
    - src/Chronos/App.xaml.cs (lignes 195-205)
    - src/Chronos/Services/IBudgetPrompt.cs
    - tests/Chronos.Tests/MainViewModelTests.cs (lignes 60-90, 105-115, 238-250, 378-440)
    - tests/Chronos.Tests/CadranBindingTests.cs (lignes 28-40)
    - tests/Chronos.Tests/OverlayWindowConfigTests.cs (lignes 18-30)
    - tests/Chronos.Tests/ThemingTests.cs (lignes 90-102)
    - tests/Chronos.Tests/CompositionRootTests.cs (lignes 55-62)
  </read_first>

  <files>
src/Chronos/ViewModels/MainViewModel.cs
src/Chronos/Views/SettingsWindow.xaml
src/Chronos/App.xaml.cs
src/Chronos/Services/IBudgetPrompt.cs (supprimé)
src/Chronos/Views/BudgetPrompt.cs (supprimé)
src/Chronos/Views/BudgetDialog.xaml (supprimé)
src/Chronos/Views/BudgetDialog.xaml.cs (supprimé)
src/Chronos/ViewModels/BudgetDialogViewModel.cs (supprimé)
tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs (supprimé)
tests/Chronos.Tests/MainViewModelTests.cs
tests/Chronos.Tests/CadranBindingTests.cs
tests/Chronos.Tests/OverlayWindowConfigTests.cs
tests/Chronos.Tests/ThemingTests.cs
tests/Chronos.Tests/CompositionRootTests.cs
  </files>

  <action>
GESTE ATOMIQUE UNIQUE : retirer le paramètre `IBudgetPrompt` du ctor de `MainViewModel` change son
arité (12 → 11). Les 6 sites de construction en test doivent changer dans le MÊME commit, sinon la
solution ne compile pas. Ne PAS scinder.

1. `src/Chronos/ViewModels/MainViewModel.cs` :
   - supprimer le champ ligne 28 : `private readonly IBudgetPrompt _budgetPrompt;`
   - supprimer le 7e paramètre `IBudgetPrompt budgetPrompt,` de la signature du ctor (ligne 185)
   - supprimer l'affectation ligne 194 : `_budgetPrompt = budgetPrompt;`
   - supprimer intégralement le bloc XML-doc + `[RelayCommand] private void CalibrateBudgets()`
     (lignes 325-348), y compris les 6 affectations `FiveHourTokenBudget` / `FiveHourBudgetSource` /
     `FiveHourBudgetCalibratedAt` / `WeeklyTokenBudget` / `WeeklyBudgetSource` / `WeeklyBudgetCalibratedAt`
   - ligne 195, remplacer le commentaire `// mémorisé pour re-déclencher un recalcul après calibration (CAL-01)`
     par `// mémorisé pour re-déclencher un recalcul immédiat (bascule de source, login OAuth)`.
     Le champ `_orchestrator` RESTE : il est encore utilisé lignes 359 et 393.
   - vérifier qu'aucun `using` ne devient inutilisé ; ne pas toucher aux autres commandes.

2. `src/Chronos/Views/SettingsWindow.xaml` : supprimer les 2 lignes 306-307
   (`<Button Content="Plafonds…" Style="{StaticResource Flat}" Margin="4,0,0,6" FontSize="12"` +
   `Command="{Binding CalibrateBudgetsCommand}"/>`). La `<UniformGrid Columns="2">` passe de 4 à 3
   enfants — c'est ATTENDU, une case reste vide en bas à droite (décision de la recherche : ne pas
   surinvestir, la phase 20 retouche l'UI). Ajuster la marge du bouton « Recalibrer hebdo… » n'est PAS
   requis. Ne pas changer `Columns="2"`.

3. `src/Chronos/App.xaml.cs` : supprimer les lignes 200-201
   (`// CAL-01 : dialogue de calibration manuelle des plafonds…` +
   `services.AddSingleton<IBudgetPrompt, BudgetPrompt>();`).

4. Supprimer les fichiers : `src/Chronos/Services/IBudgetPrompt.cs`, `src/Chronos/Views/BudgetPrompt.cs`,
   `src/Chronos/Views/BudgetDialog.xaml`, `src/Chronos/Views/BudgetDialog.xaml.cs`,
   `src/Chronos/ViewModels/BudgetDialogViewModel.cs`, `tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs`
   (via `git rm`).

5. `tests/Chronos.Tests/MainViewModelTests.cs` :
   - supprimer les 3 tests de calibration : `CalibrateBudgets_persiste_le_plafond_saisi_en_Manual_et_None_pour_le_champ_vide`
     (~381), `CalibrateBudgets_annule_ne_persiste_rien` (~401),
     `CalibrateBudgets_n_ecrase_pas_les_reglages_persistes_par_un_autre_writer` (~421), commentaires
     `--- CAL-01 …` / `--- GAP-1 …` associés compris
   - helper `Build` (ligne 62-69) : retirer le paramètre `FakeBudgetPrompt budgetPrompt` et son passage
     au ctor ligne 68
   - helper `NewVmFull` (lignes 72-84) : retirer `out FakeBudgetPrompt budgetPrompt`, l'affectation
     `budgetPrompt = new FakeBudgetPrompt();` et l'argument passé à `Build`
   - `NewVm` (ligne 88) : passer de 5 à 4 `out _`
   - ligne 111 : retirer `new FakeBudgetPrompt(),` de l'appel direct au ctor
   - ligne 244 : retirer `new FakeBudgetPrompt(),` de l'appel à `Build`

6. Retirer `new FakeBudgetPrompt(),` des appels directs au ctor de `MainViewModel` dans :
   `tests/Chronos.Tests/CadranBindingTests.cs:35`, `tests/Chronos.Tests/OverlayWindowConfigTests.cs:24`,
   `tests/Chronos.Tests/ThemingTests.cs:97`. Aucune assertion ne change dans ces trois fichiers.

7. `tests/Chronos.Tests/CompositionRootTests.cs` : supprimer les lignes 58-60 (commentaire `09-02 : …
   IBudgetPrompt …` + `services.AddSingleton<IBudgetPrompt>(_ => new FakeBudgetPrompt());`).

8. PIÈGE OBLIGATOIRE (Pitfall 3 de la recherche) : `BudgetDialog.xaml` est supprimé, or 3 copies de
   `obj/**/Views/BudgetDialog.g.cs` existent dans l'arbre de travail. Exécuter
   `dotnet clean Chronos.sln` AVANT le premier build, sinon risque d'erreur fantôme sur une
   `partial class BudgetDialog` orpheline.
  </action>

  <verify>
    <automated>dotnet clean Chronos.sln --nologo &amp;&amp; dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln` : 0 erreur.
    - `dotnet test Chronos.sln` : 0 échec.
    - `grep -rn "IBudgetPrompt\|BudgetSelection\|CalibrateBudgets\|BudgetDialog\|FakeBudgetPrompt" src/ tests/ --include=*.cs --include=*.xaml` → **0 résultat**.
    - `grep -c "Plafonds" src/Chronos/Views/SettingsWindow.xaml` → **0**.
    - `git status --porcelain` liste bien 6 fichiers en `D` : `Services/IBudgetPrompt.cs`, `Views/BudgetPrompt.cs`, `Views/BudgetDialog.xaml`, `Views/BudgetDialog.xaml.cs`, `ViewModels/BudgetDialogViewModel.cs`, `tests/Chronos.Tests/Fakes/FakeBudgetPrompt.cs`.
    - `find src/Chronos/obj -name "BudgetDialog.g.cs"` → **0 résultat** après `dotnet clean`.
    - `grep -n "_orchestrator" src/Chronos/ViewModels/MainViewModel.cs` → au moins 3 résultats (le champ survit, il sert encore).
    - `grep -c "<Button" src/Chronos/Views/SettingsWindow.xaml` diminue exactement de 1 par rapport à HEAD.
  </acceptance_criteria>

  <done>
Le bouton « Plafonds… », la commande `CalibrateBudgetsCommand`, l'interface `IBudgetPrompt`, son
implémentation WPF, le dialogue et son VM n'existent plus. Le ctor de `MainViewModel` a 11 paramètres.
La solution compile et la suite est à 0 échec (attendu : baseline − 3 tests supprimés).
  </done>
</task>

<task type="auto">
  <name>Task 2: Supprimer le calibrateur automatique et la logique pure de déduction</name>

  <read_first>
    - src/Chronos/Services/BudgetAutoCalibrator.cs
    - src/Chronos/Services/BudgetCalibration.cs
    - src/Chronos/App.xaml.cs (lignes 68-76 et 315-330)
    - tests/Chronos.Tests/CompositionRootTests.cs (lignes 60-70 et 90-100, après Task 1)
  </read_first>

  <files>
src/Chronos/App.xaml.cs
src/Chronos/Services/BudgetAutoCalibrator.cs (supprimé)
src/Chronos/Services/BudgetCalibration.cs (supprimé)
tests/Chronos.Tests/BudgetAutoCalibratorTests.cs (supprimé)
tests/Chronos.Tests/BudgetCalibrationTests.cs (supprimé)
tests/Chronos.Tests/CompositionRootTests.cs
  </files>

  <action>
ORDRE IMPOSÉ (Pitfall 2 de la recherche) : `BudgetAutoCalibrator` d'abord (il appelle
`BudgetCalibration`), `BudgetCalibration` ensuite. `BudgetSource.cs` N'EST PAS touché ici — il est encore
référencé par `ChronosSettings`, sa suppression est le plan 16-04.

1. `src/Chronos/App.xaml.cs` — supprimer les DEUX points d'accroche :
   - lignes 71-73 : le commentaire `// CAL-02 : même raison — résoudre le calibrateur AVANT StartAsync…`
     ET la ligne `_ = _host.Services.GetRequiredService<BudgetAutoCalibrator>();`. C'est le point qui
     ferait lever la DI au démarrage s'il survivait à la suppression du type.
     ATTENTION : la ligne voisine `_ = _host.Services.GetRequiredService<MainViewModel>();` et son
     commentaire (Pitfall 3, abonnement du VM) DOIVENT rester intacts.
   - lignes 318-325 : le commentaire `// CAL-02 : calibrateur auto opportuniste. tokenSource = …`
     et tout le bloc `services.AddSingleton(sp => new BudgetAutoCalibrator( … ));`
   - ligne ~77 (commentaire du log de démarrage) : remplacer
     `(token/OAuth/sources/plafonds)` par `(token/OAuth/sources)`.

2. Supprimer via `git rm` : `src/Chronos/Services/BudgetAutoCalibrator.cs`,
   `src/Chronos/Services/BudgetCalibration.cs`,
   `tests/Chronos.Tests/BudgetAutoCalibratorTests.cs` (3 tests),
   `tests/Chronos.Tests/BudgetCalibrationTests.cs` (15 tests).
   Justification de la suppression des 18 tests : ils couvrent exclusivement `Deduce` / `ApplyAuto` /
   l'abonnement du calibrateur, c'est-à-dire du code qui n'existe plus. Aucune couverture d'un
   comportement survivant n'est perdue.

3. `tests/Chronos.Tests/CompositionRootTests.cs` :
   - supprimer le bloc de commentaire `// 09-02 : le calibrateur auto (CAL-02) est résolu eager…` et
     l'enregistrement `services.AddSingleton(sp => new BudgetAutoCalibrator( … ));` (4 lignes d'arguments)
   - supprimer l'assertion `Assert.NotNull(provider.GetRequiredService<BudgetAutoCalibrator>());`
   - laisser intactes les assertions sur `MainWindow`, `MainViewModel` et `MarqueurDisposable` : la
     preuve de disposition des Singletons `IDisposable` repose sur `MarqueurDisposable`, pas sur le
     calibrateur.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln` : 0 erreur.
    - `dotnet test Chronos.sln` : 0 échec.
    - `grep -rn "BudgetAutoCalibrator\|BudgetCalibration" src/ tests/ --include=*.cs` → **0 résultat**.
    - `grep -c "GetRequiredService<MainViewModel>" src/Chronos/App.xaml.cs` → **1** (le point d'accroche du VM survit).
    - `ls src/Chronos/Services/BudgetSource.cs` → le fichier EXISTE encore (suppression différée au plan 16-04).
    - `grep -rn "plafonds" src/Chronos/App.xaml.cs` → **0 résultat**.
    - `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~CompositionRootTests"` → 3 tests, 0 échec.
  </acceptance_criteria>

  <done>
`BudgetAutoCalibrator` et `BudgetCalibration` n'existent plus, ni en code, ni en DI, ni comme résolution
forcée avant `StartAsync`. La garde `CompositionRootTests` est verte sans eux. Attendu cumulé depuis la
baseline : −21 tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reformuler les commentaires orphelins référençant la calibration</name>

  <read_first>
    - src/Chronos/Services/RefreshOrchestrator.cs (lignes 88-100)
    - src/Chronos/Services/GatedOAuthUsageProvider.cs (lignes 1-15)
  </read_first>

  <files>
src/Chronos/Services/RefreshOrchestrator.cs
src/Chronos/Services/GatedOAuthUsageProvider.cs
  </files>

  <action>
Hygiène documentaire : deux XML-doc décrivent un comportement qui n'existe plus. Aucun changement de
code exécutable — uniquement des commentaires. La méthode `RequestRefresh` RESTE (elle est appelée
lignes 359 et 393 de `MainViewModel`).

1. `src/Chronos/Services/RefreshOrchestrator.cs:93-95` — remplacer le XML-doc de `RequestRefresh` par :

```csharp
    /// <summary>Déclenche un recalcul immédiat — ex. après bascule de la source exacte (portillon OAuth)
    /// ou après un login : le prochain GetAsync relit les réglages frais sans redémarrage. Type neutre
    /// (void) → garde de pureté inchangée.</summary>
```

2. `src/Chronos/Services/GatedOAuthUsageProvider.cs:6-7` — dans le XML-doc de la classe, remplacer
   `FRAIS depuis settings.json (comme JsonlEstimationProvider relit ses plafonds).` par
   `FRAIS depuis settings.json à chaque appel (aucun cache de réglage).`
   La suppression de la mention `JsonlEstimationProvider` est volontaire : ce type est renommé au plan
   16-03, et corriger le commentaire ici évite que 16-03 ait à rouvrir ce fichier.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "CAL-01\|CAL-02" src/Chronos --include=*.cs --include=*.xaml` (hors `obj/`) → **0 résultat**.
    - `grep -n "JsonlEstimationProvider" src/Chronos/Services/GatedOAuthUsageProvider.cs` → **0 résultat**.
    - `grep -c "public void RequestRefresh" src/Chronos/Services/RefreshOrchestrator.cs` → **1** (la méthode survit).
    - `dotnet test Chronos.sln -v q --nologo` : 0 échec.
  </acceptance_criteria>

  <done>
Plus aucun commentaire du code de production ne référence CAL-01 / CAL-02 ni la calibration des plafonds.
La solution compile, 0 échec.
  </done>
</task>

</tasks>

<verification>
1. `dotnet clean Chronos.sln && dotnet build Chronos.sln -v q --nologo` → 0 erreur, 0 nouveau warning.
2. `dotnet test Chronos.sln -v q --nologo` → **0 échec**. Total attendu ≈ **382** (baseline 405 − 15
   `BudgetCalibrationTests` − 3 `BudgetAutoCalibratorTests` − 3 `MainViewModelTests`, soit −21). Ce
   recul est LÉGITIME : chaque test supprimé couvrait du code supprimé. Le critère est 0 échec, pas ≥ 405.
3. `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert.
4. `dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~CompositionRootTests"` → 3 tests verts.
5. `grep -rn "IBudgetPrompt\|BudgetSelection\|BudgetDialog\|BudgetAutoCalibrator\|BudgetCalibration\|CalibrateBudgets" src/ tests/ --include=*.cs --include=*.xaml` → 0 résultat.
6. `grep -rn "BudgetSource" src/ --include=*.cs` → exactement 3 résultats attendus (déclaration
   `BudgetSource.cs` + 2 propriétés de `ChronosSettings.cs`) — preuve que la suppression s'est arrêtée
   au bon endroit et que le plan 16-04 a encore son objet.
</verification>

<success_criteria>
- Le sous-système de calibration (UI + calibrateur + logique pure) a disparu du code, de la DI et de la
  fenêtre de réglages.
- Trois commits atomiques, chacun laissant la solution compilable et la suite à 0 échec.
- `BudgetSource.cs` et les six champs de `ChronosSettings` sont intacts (leur suppression est le plan 16-04).
- Aucun fichier partagé avec le plan 16-02 n'a été touché (`ChronosPaths.cs`, `Models/WindowState.cs`,
  `LastExact*.cs` et leurs tests) — les deux plans de la vague 1 restent parallélisables.
</success_criteria>

<output>
After completion, create `.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-01-SUMMARY.md`
</output>
