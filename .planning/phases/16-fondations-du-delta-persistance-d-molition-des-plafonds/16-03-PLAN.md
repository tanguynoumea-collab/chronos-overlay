---
phase: 16-fondations-du-delta-persistance-d-molition-des-plafonds
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/Chronos/Services/ITranscriptActivitySource.cs
  - src/Chronos/Services/TranscriptActivityProvider.cs
  - src/Chronos/Services/JsonlEstimationProvider.cs
  - src/Chronos/Services/FiveHourWindowInference.cs
  - src/Chronos/Services/WeeklyWindow.cs
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/TranscriptActivityProviderTests.cs
  - tests/Chronos.Tests/TranscriptActivityLogTests.cs
  - tests/Chronos.Tests/JsonlEstimationProviderTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
autonomous: true
requirements: [DEL-01, DEL-02, EXA-01]

must_haves:
  truths:
    - "Pour un instant T donné, Chronos sait dire s'il y a eu une réponse assistant depuis T (booléen), sans produire aucun pourcentage."
    - "Pour un instant T donné, Chronos sait dire combien de tokens ont été consommés depuis T par Claude Code."
    - "Aucun type du dépôt ne sait plus fabriquer une Utilization à partir d'un comptage de tokens."
    - "Le journal signale son horizon : une interrogation portant sur un T plus ancien que le filtre mtime de 8 jours est déclarée non couverte."
    - "L'application démarre : le graphe DI se résout avec le décorateur de persistance en tête de chaîne et sans le provider de transcripts dans la chaîne composite."
  artifacts:
    - path: "src/Chronos/Services/ITranscriptActivitySource.cs"
      provides: "Contrat de delta DEL-01/DEL-02 — n'hérite PAS de IUsageProvider"
      exports: ["ITranscriptActivitySource", "TranscriptActivity", "TranscriptActivityLog"]
    - path: "src/Chronos/Services/TranscriptActivityProvider.cs"
      provides: "Parcours disque tolérant des JSONL, une seule passe, sans plafond ni utilization"
      absent_pattern: "IUsageProvider|Utilization|UsageSnapshot|TokenBudget"
    - path: "src/Chronos/App.xaml.cs"
      provides: "Chaîne composite se terminant sur ClaudeUsageObjectProvider, coiffée du décorateur LastExactUsageProvider"
      contains: "LastExactUsageProvider"
      absent_pattern: "JsonlEstimationProvider"
    - path: "tests/Chronos.Tests/TranscriptActivityLogTests.cs"
      provides: "Preuve PURE de Since()/Covers() sans aucun accès disque"
  key_links:
    - from: "src/Chronos/App.xaml.cs"
      to: "LastExactUsageProvider"
      via: "AddSingleton<IUsageProvider> enveloppant le composite"
      pattern: "new LastExactUsageProvider\\("
    - from: "src/Chronos/App.xaml.cs"
      to: "ITranscriptActivitySource"
      via: "AddSingleton<ITranscriptActivitySource> sur TranscriptActivityProvider"
      pattern: "AddSingleton<ITranscriptActivitySource>"
    - from: "tests/Chronos.Tests/CompositionRootTests.cs"
      to: "LastExactUsageProvider"
      via: "la garde DI reproduit le nouveau câblage de tête de chaîne"
      pattern: "LastExactUsageProvider"
---

<objective>
Transformer `JsonlEstimationProvider` en source de DELTA (`TranscriptActivityProvider`) et recâbler la
chaîne de providers : sortie du JSONL de la chaîne composite, branchement du décorateur de persistance
en tête.

Purpose: DEL-01 + DEL-02 + achèvement d'EXA-01. Les limites Anthropic pondèrent par modèle : `tokens /
plafond` restera faux même avec le bon plafond. Les transcripts ne savent répondre qu'à deux questions
bornées — *activité depuis T ?* et *tokens depuis T ?*. En faisant du provider un type qui n'implémente
PAS `IUsageProvider`, EXA-04 devient structurellement vrai dès la phase 16 : plus aucun type du dépôt ne
peut fabriquer une utilization à partir d'un comptage.

Output: `ITranscriptActivitySource` / `TranscriptActivity` / `TranscriptActivityLog`,
`TranscriptActivityProvider`, la chaîne DI recâblée, et 2 fichiers de test remplaçant
`JsonlEstimationProviderTests`.

DEUX DISPARITIONS VISUELLES ATTENDUES, non régressives — à ne PAS traiter comme des bugs à la
vérification de phase (analyse chiffrée §Q1 de la recherche sur l'état réel de la machine) :
1. **L'anneau hebdo perd sa couleur** (devient gris, `Utilization = null`). Il était le seul consommateur
   de `tokens / 5 817 635 413`, un plafond calibré sous Max x5 et faux d'un facteur ~4. Perdre une
   couleur mensongère est le BUT de ce milestone. Son compte à rebours SURVIT (il vient de
   `WeeklyRecalibration` + `WeeklyAnchor`, pas du plafond).
2. **Le texte « ≈ N tokens » disparaît** (plus aucun producteur de `SourceReliability.Estimated`).
L'anneau 5 h n'est PAS touché : il reste alimenté par `usage.json` marqué `Exact`. Le bandeau « données
indisponibles » ne se déclenche pas (il exige les DEUX fenêtres `Unavailable`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-RESEARCH.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-01-SUMMARY.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrats à connaître SANS explorer la base de code. -->

Livrés par le plan 16-02 (à consommer ici) :
```csharp
public sealed class LastExactStore
{
    public const int SchemaVersion = 1;
    public LastExactStore(string path);
    public string Path { get; }
    public void Save(UsageSnapshot snapshot);
    public LastExactWindows? Load(DateTimeOffset now);
}
public sealed record LastExactWindows(WindowState? FiveHour, WindowState? SevenDay);

public sealed class LastExactUsageProvider : IUsageProvider
{
    public LastExactUsageProvider(IUsageProvider inner, LastExactStore store, IClock clock);
}
public string ChronosPaths.LastExactFile { get; }   // %APPDATA%\Chronos\last-exact.json
```

Constructeurs existants nécessaires au recâblage :
```csharp
public ClaudeUsageObjectProvider(ChronosPaths paths, IClock clock);
public GatedOAuthUsageProvider(ClaudeOAuthUsageProvider inner, SettingsService settings);
public CompositeUsageProvider(IUsageProvider primary, IUsageProvider fallback);
public JsonlEstimationProvider(ChronosPaths paths, IClock clock, SettingsService settings);  // → devient TranscriptActivityProvider(paths, clock) : PLUS de SettingsService
```

Bloc DI ACTUEL à remplacer (`src/Chronos/App.xaml.cs`, lignes ~291-296 après le plan 16-01) :
```csharp
services.AddSingleton<IUsageProvider>(sp => new CompositeUsageProvider(
    primary:  sp.GetRequiredService<ChronosOAuthUsageProvider>(),
    fallback: new CompositeUsageProvider(
        primary:  sp.GetRequiredService<GatedOAuthUsageProvider>(),
        fallback: new CompositeUsageProvider(
            primary:  sp.GetRequiredService<ClaudeUsageObjectProvider>(),
            fallback: sp.GetRequiredService<JsonlEstimationProvider>()))));
```

Fixtures de test existantes (`tests/Chronos.Tests/TestData/`), horloge figée `now = 2026-07-08T12:00:00Z` :
- `sample-valid.jsonl` — 3 messages assistant : **1550 tokens @ 2026-07-08T11:30:00Z**,
  **600 @ 2026-07-05T12:00:00Z**, **9999 @ 2026-06-01T12:00:00Z**
- `sample-inactive.jsonl` — 1 message assistant : **600 tokens @ 2026-07-08T06:00:00Z**
- `sample-tolerant.jsonl` — 1 seule ligne assistant valide (**700 @ 11:00**) + prose « five_hour », ligne
  corrompue, dernière ligne tronquée
- `SubagentsRoot/` — `session.jsonl` (500) + `subagents/agent-abc.jsonl` (300) = **800**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer le contrat de delta et convertir le provider (renommage, pas réécriture)</name>

  <read_first>
    - src/Chronos/Services/JsonlEstimationProvider.cs (INTÉGRALEMENT — le parcours disque doit être conservé mot pour mot)
    - src/Chronos/Services/FiveHourWindowInference.cs
    - src/Chronos/Services/WeeklyWindow.cs
    - src/Chronos/Services/IUsageProvider.cs
    - src/Chronos/Services/IClock.cs
    - src/Chronos/Services/ChronosPaths.cs
  </read_first>

  <files>
src/Chronos/Services/ITranscriptActivitySource.cs
src/Chronos/Services/TranscriptActivityProvider.cs
src/Chronos/Services/JsonlEstimationProvider.cs (renommé)
src/Chronos/Services/FiveHourWindowInference.cs
src/Chronos/Services/WeeklyWindow.cs
  </files>

  <behavior>
    - `Since(t)` sur un journal construit EN MÉMOIRE (aucun fichier) contenant des entrées à
      T0/T1/T2 → renvoie la somme des tokens strictement postérieurs à `t`.
    - Borne basse STRICTEMENT exclusive : une entrée dont le timestamp vaut exactement `t` n'est PAS
      comptée (elle a déjà été comptée par le serveur au moment de la capture exacte).
    - Borne haute inclusive : une entrée dont le timestamp vaut exactement `Now` EST comptée.
    - `HasActivity == false` et `LastActivityAt == null` quand aucune entrée n'est postérieure à `t`.
    - `LastActivityAt` = timestamp de l'entrée la plus récente strictement postérieure à `t`.
    - `Covers(since)` renvoie `false` pour `since < Horizon` et `true` pour `since >= Horizon`.
    - `Horizon == Now - 8 jours`.
  </behavior>

  <action>
1. Créer `src/Chronos/Services/ITranscriptActivitySource.cs` — contrat + types de résultat. XML-doc en
   français. Aucun `using Chronos.Models` nécessaire : ce type ne produit AUCUN `UsageSnapshot`.

```csharp
namespace Chronos.Services;

/// <summary>
/// Résultat BORNÉ d'une interrogation des transcripts (DEL-01 + DEL-02). Aucun pourcentage, jamais.
///
/// <para><b>Tokens est une BORNE INFÉRIEURE de l'activité Claude Code, pas une consommation de compte.</b>
/// ~/.claude/projects/**/*.jsonl ne contient que les transcripts de Claude Code : l'usage de l'app de
/// bureau et de Cowork consomme le MÊME pool de compte mais n'y apparaît pas. De plus, les limites
/// Anthropic pondèrent par modèle — une somme de tokens n'est PAS convertible en pourcentage. La marge
/// d'incertitude qui en découle est portée par la phase 19 (DEL-04).</para>
/// </summary>
public sealed record TranscriptActivity(bool HasActivity, long Tokens, DateTimeOffset? LastActivityAt);

/// <summary>
/// Journal MATÉRIALISÉ des réponses assistant récentes. PUR : aucune E/S, interrogeable N fois.
/// Sépare l'I/O (une seule passe disque, coûteuse) de la requête (bornage par fenêtre, gratuite) —
/// les deux fenêtres 5 h et hebdo ont des bornes basses DIFFÉRENTES mais doivent répondre depuis un
/// SEUL instantané disque, sinon leurs réponses peuvent être incohérentes entre elles.
/// </summary>
public sealed class TranscriptActivityLog
{
    /// <summary>Instant de la passe disque. Borne haute INCLUSIVE des requêtes.</summary>
    public DateTimeOffset Now { get; }

    /// <summary>Borne basse au-delà de laquelle le journal ne sait RIEN (filtre mtime de 8 jours du
    /// parcours disque). Une requête dont le « depuis » est antérieur à cet horizon produirait un delta
    /// silencieusement SOUS-ÉVALUÉ : l'appelant doit alors refuser le delta plutôt que mentir.</summary>
    public DateTimeOffset Horizon { get; }

    public TranscriptActivityLog(DateTimeOffset now, DateTimeOffset horizon,
                                 IReadOnlyList<(DateTimeOffset Ts, long Tokens)> entries);

    /// <summary>Le journal couvre-t-il une interrogation depuis <paramref name="since"/> ?</summary>
    public bool Covers(DateTimeOffset since) => since >= Horizon;

    /// <summary>Activité et tokens dans <c>]since ; Now]</c>. Borne basse EXCLUSIVE : un message dont
    /// le timestamp vaut exactement l'instant du relevé exact a déjà été compté par le serveur, le
    /// compter à nouveau le compterait DEUX FOIS.</summary>
    public TranscriptActivity Since(DateTimeOffset since);
}

/// <summary>
/// Source de DELTA (DEL-01/DEL-02). N'implémente VOLONTAIREMENT PAS <see cref="IUsageProvider"/> :
/// les transcripts ne produisent plus jamais d'utilization absolue (EXA-04). Ne pas « rétablir »
/// cet héritage : c'est la garantie structurelle du milestone.
/// </summary>
public interface ITranscriptActivitySource
{
    Task<TranscriptActivityLog> ReadAsync(CancellationToken ct = default);
}
```

2. RENOMMER `src/Chronos/Services/JsonlEstimationProvider.cs` en
   `src/Chronos/Services/TranscriptActivityProvider.cs` (`git mv`, pour préserver l'historique). Ne PAS
   créer un nouveau fichier à côté : le parcours disque existant encode des bugs déjà rencontrés et
   testés, il doit survivre mot pour mot.

   À CONSERVER À L'IDENTIQUE (lignes 43-73 du fichier actuel) :
   - `FileShare.ReadWrite` au `FileStream` (Claude Code écrit le transcript en parallèle) + `catch (IOException) { continue; }`
   - `JsonDocument.Parse(line)` par ligne dans un `try` avec `catch (JsonException)` (ligne partielle / corrompue ignorée, ROB-02)
   - `IsAssistant(o)` — objet STRUCTURÉ (`type=="assistant"` ET `message.role=="assistant"`), jamais une chaîne de prose
   - `DateTimeOffset.TryParse(..., CultureInfo.InvariantCulture, DateTimeStyles.RoundtripKind, ...)`
   - `SumUsageTokens` = `input_tokens + output_tokens + cache_creation_input_tokens + cache_read_input_tokens`
   - `if (when <= now)` — filtre des timestamps futurs (horloge décalée)
   - `EnumerateJsonl` : `SearchOption.AllDirectories` (inclut `subagents/`), dossier absent → séquence vide,
     `catch (IOException)` / `catch (UnauthorizedAccessException)` → séquence vide
   - `RecentEnough` avec `cutoff = now - TimeSpan.FromDays(8)`
   - `entries.Sort((a, b) => a.Ts.CompareTo(b.Ts))`

   À SUPPRIMER :
   - `: IUsageProvider` sur la déclaration de classe
   - le champ `_settings` et le 3e paramètre `SettingsService settings` du ctor → nouvelle signature :
     `public TranscriptActivityProvider(ChronosPaths paths, IClock clock)`
   - la ligne 37 `var settings = _settings.Load();`
   - la méthode `GetAsync` et son type de retour `Task<UsageSnapshot>`
   - INTÉGRALEMENT `BuildFiveHour` (lignes 87-112) et `BuildSevenDay` (lignes 116-130) — c'est là que
     vivait `Utilization = tokens / budget`
   - les appels à `FiveHourWindowInference.InferWindowStart` et `WeeklyWindow.CurrentStart`
   - le `using Chronos.Models;`

   À AJOUTER : `public async Task<TranscriptActivityLog> ReadAsync(CancellationToken ct = default)` qui
   effectue exactement l'ancienne passe disque puis retourne
   `new TranscriptActivityLog(now, now - TimeSpan.FromDays(8), entries)`. La constante 8 jours doit être
   déclarée UNE fois (`private static readonly TimeSpan HorizonSpan = TimeSpan.FromDays(8);`) et utilisée
   à la fois pour `cutoff` et pour `Horizon` — les deux ne doivent pas pouvoir diverger.

   Réécrire le XML-doc de la classe en français : source de delta, deux questions bornées, aucun
   pourcentage, tolérance ROB-02 conservée.

3. `src/Chronos/Services/FiveHourWindowInference.cs` et `src/Chronos/Services/WeeklyWindow.cs` — NE PAS
   SUPPRIMER, malgré la disparition de leur dernier appelant de production. Ajouter en tête du XML-doc
   de chaque classe :

```
/// <remarks>ORPHELIN ASSUMÉ depuis la phase 16 : plus aucun appelant en production depuis la
/// conversion des transcripts en source de delta. CONSERVÉ VOLONTAIREMENT — logique pure, couverte
/// par ses tests, et candidate à réemploi en phase 19 (borne basse d'un delta quand ResetsAt est
/// inconnu). Ne pas supprimer sans décision explicite.</remarks>
```
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln` : la compilation ÉCHOUE de façon attendue à cette étape UNIQUEMENT sur les références restantes à `JsonlEstimationProvider` (`App.xaml.cs`, `CompositionRootTests.cs`, `JsonlEstimationProviderTests.cs`) — ces trois-là sont traités aux tâches 2 et 3. Aucune AUTRE erreur ne doit apparaître.
    - `ls src/Chronos/Services/JsonlEstimationProvider.cs` → **absent** ; `ls src/Chronos/Services/TranscriptActivityProvider.cs` → **présent**.
    - `git log --follow --oneline -- src/Chronos/Services/TranscriptActivityProvider.cs | wc -l` → **> 1** (preuve du `git mv`, historique préservé).
    - `grep -n "IUsageProvider\|UsageSnapshot\|Utilization\|TokenBudget\|SettingsService" src/Chronos/Services/TranscriptActivityProvider.cs` → **0 résultat**.
    - `grep -c "FileShare.ReadWrite" src/Chronos/Services/TranscriptActivityProvider.cs` → **1**.
    - `grep -c "cache_read_input_tokens" src/Chronos/Services/TranscriptActivityProvider.cs` → **1**.
    - `grep -c "SearchOption.AllDirectories" src/Chronos/Services/TranscriptActivityProvider.cs` → **1**.
    - `grep -c "TimeSpan.FromDays(8)" src/Chronos/Services/TranscriptActivityProvider.cs` → **1** (constante unique, `cutoff` et `Horizon` ne peuvent pas diverger).
    - `grep -n "interface ITranscriptActivitySource" src/Chronos/Services/ITranscriptActivitySource.cs` → présent, et la ligne ne contient **pas** `: IUsageProvider`.
    - `grep -c "ORPHELIN ASSUMÉ" src/Chronos/Services/FiveHourWindowInference.cs src/Chronos/Services/WeeklyWindow.cs` → **1** chacun ; les deux fichiers EXISTENT toujours.
  </acceptance_criteria>

  <done>
Le contrat de delta existe et le provider de transcripts a été renommé, amputé de toute production
d'utilization et de sa dépendance à `SettingsService`, en conservant son parcours disque mot pour mot.
Les deux calculateurs purs devenus orphelins sont documentés, pas supprimés.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Réécrire les tests JSONL en preuves de delta (DEL-01 / DEL-02) et en tests purs du journal</name>

  <read_first>
    - tests/Chronos.Tests/JsonlEstimationProviderTests.cs (INTÉGRALEMENT — helpers d'isolation à reprendre)
    - tests/Chronos.Tests/TestData/sample-valid.jsonl
    - tests/Chronos.Tests/TestData/sample-inactive.jsonl
    - tests/Chronos.Tests/TestData/sample-tolerant.jsonl
    - src/Chronos/Services/ITranscriptActivitySource.cs (produit par la tâche 1)
    - src/Chronos/Services/TranscriptActivityProvider.cs (produit par la tâche 1)
  </read_first>

  <files>
tests/Chronos.Tests/TranscriptActivityProviderTests.cs
tests/Chronos.Tests/TranscriptActivityLogTests.cs
tests/Chronos.Tests/JsonlEstimationProviderTests.cs (supprimé)
  </files>

  <action>
Renommer `tests/Chronos.Tests/JsonlEstimationProviderTests.cs` en
`tests/Chronos.Tests/TranscriptActivityProviderTests.cs` (`git mv`) puis réécrire son contenu. Créer en
plus `tests/Chronos.Tests/TranscriptActivityLogTests.cs`.

CONSERVER les helpers d'isolation existants (`TestDataDir` via `[CallerFilePath]`, `IsolatedRootWith`)
et l'horloge figée `Now = 2026-07-08T12:00:00Z`. Le helper `ProviderFor` perd son `SettingsService` :
`new TranscriptActivityProvider(new ChronosPaths(usageTemp, projectsRoot), new FakeClock(Now))`.

**Tests SUPPRIMÉS — justification obligatoire à reporter dans le SUMMARY :**
- `Utilization_5h_estimee_avec_plafond` — supprimé parce que l'utilization absolue dérivée d'un
  comptage de tokens n'existe plus (EXA-04). Aucune couverture d'un comportement survivant n'est perdue.
- `Plafond_defini_laisse_la_fenetre_5h_Estimated_avec_utilization` — idem : il testait la relation
  plafond → couleur, disparue avec le plafond.

**Tests de `TranscriptActivityProviderTests.cs` (I/O réel sur fixtures) — 9 tests :**
1. `Tokens_depuis_T_borne_sur_la_fenetre_5h` — `sample-valid.jsonl`,
   `Since(2026-07-08T06:00:00Z)` → `Tokens == 1550`, `HasActivity == true` (DEL-02).
2. `Tokens_depuis_T_borne_sur_la_fenetre_hebdo` — `sample-valid.jsonl`,
   `Since(2026-07-01T00:00:00Z)` → `Tokens == 2150` (1550 + 600 ; le message du 2026-06-01 est hors
   fenêtre) (DEL-02).
3. `Borne_basse_strictement_exclusive` — `sample-valid.jsonl`, `Since(2026-07-08T11:30:00Z)` →
   `Tokens == 0`, `HasActivity == false` : le message de 11:30 n'est PAS recompté (Pitfall 5).
4. `Derniere_activite_exposee` — `sample-valid.jsonl`, `Since(2026-07-01T00:00:00Z)` →
   `LastActivityAt == 2026-07-08T11:30:00Z`.
5. `Aucune_activite_depuis_T` — `sample-inactive.jsonl` (unique message @ 06:00),
   `Since(2026-07-08T07:00:00Z)` → `HasActivity == false`, `Tokens == 0`, `LastActivityAt == null` (DEL-01).
6. `Tolerant_ignore_corrompue_partielle_prose_et_user_sans_exception` — CONSERVÉ, assertions adaptées :
   `sample-tolerant.jsonl`, `Since(2026-07-01T00:00:00Z)` → `Tokens == 700`, aucune exception (ROB-02).
7. `Subagents_inclus_dans_la_somme_recursive` — CONSERVÉ, assertions adaptées : `SubagentsRoot/`,
   `Since(2026-07-01T00:00:00Z)` → `Tokens == 800`.
8. `Dossier_absent_renvoie_un_journal_vide_sans_exception` — CONSERVÉ, assertions adaptées : dossier
   inexistant → `HasActivity == false`, `Tokens == 0`, aucune exception.
9. `Horizon_borne_a_huit_jours` — `Covers(Now - TimeSpan.FromDays(30)) == false` et
   `Covers(Now - TimeSpan.FromDays(1)) == true` ; `log.Horizon == Now - TimeSpan.FromDays(8)`.

Ajouter en tête de la classe une assertion de non-retour, à écrire comme un `[Fact]` :
10. `Le_provider_de_transcripts_n_est_pas_un_IUsageProvider` —
    `Assert.False(typeof(IUsageProvider).IsAssignableFrom(typeof(TranscriptActivityProvider)));`
    C'est la garantie structurelle d'EXA-04 : un futur développeur qui « rebrancherait » le provider dans
    la chaîne casserait ce test.

**Tests de `TranscriptActivityLogTests.cs` (PURS, aucun fichier) — 5 tests :** construire directement
`new TranscriptActivityLog(now, horizon, entries)` avec des entrées en mémoire et couvrir : somme sur
`]since ; Now]`, exclusivité stricte de la borne basse, inclusivité de la borne haute
(`Ts == Now` compté), `LastActivityAt` sur l'entrée la plus récente, `Covers` de part et d'autre de
`Horizon`. Ces tests sont la preuve que la logique de bornage est testable sans fixture disque
(séparation I/O pur / logique pure, Pattern 3 du dépôt).
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~TranscriptActivity"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~TranscriptActivityProviderTests"` → **10 tests, 0 échec**.
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~TranscriptActivityLogTests"` → **5 tests, 0 échec**.
    - `ls tests/Chronos.Tests/JsonlEstimationProviderTests.cs` → **absent**.
    - `grep -rn "Utilization\|EstimatedTokens\|TokenBudget\|SourceReliability" tests/Chronos.Tests/TranscriptActivityProviderTests.cs tests/Chronos.Tests/TranscriptActivityLogTests.cs` → **0 résultat**.
    - `grep -c "IsAssignableFrom(typeof(TranscriptActivityProvider))" tests/Chronos.Tests/TranscriptActivityProviderTests.cs` → **1**.
    - `grep -rn "settings.json" tests/Chronos.Tests/TranscriptActivityProviderTests.cs` → **0 résultat** (le provider ne lit plus de réglages).
    - Aucun fichier de `tests/Chronos.Tests/TestData/` n'est modifié : `git status --porcelain tests/Chronos.Tests/TestData/` → **vide**.
  </acceptance_criteria>

  <done>
DEL-01 et DEL-02 sont prouvés sur fixtures réelles et en pur. La borne basse exclusive, l'horizon 8 jours
et la tolérance ROB-02 ont chacun leur test. Une assertion réflexive interdit de rebrancher le provider
comme `IUsageProvider`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Recâbler la chaîne — sortie du JSONL, décorateur de persistance en tête, garde DI réécrite</name>

  <read_first>
    - src/Chronos/App.xaml.cs (lignes 255-300, après le plan 16-01)
    - tests/Chronos.Tests/CompositionRootTests.cs (lignes 28-60 et 85-100, après le plan 16-01)
    - src/Chronos/Services/LastExactUsageProvider.cs (produit par le plan 16-02)
    - src/Chronos/Services/LastExactStore.cs (produit par le plan 16-02)
    - src/Chronos/Services/CompositeUsageProvider.cs
  </read_first>

  <files>
src/Chronos/App.xaml.cs
tests/Chronos.Tests/CompositionRootTests.cs
  </files>

  <action>
1. `src/Chronos/App.xaml.cs` — dans `ConfigureServices` :
   - remplacer `services.AddSingleton<JsonlEstimationProvider>();` (ligne ~261) par
     l'enregistrement du contrat de delta :

```csharp
        // DEL-01/DEL-02 : les transcripts ne répondent plus qu'à deux questions bornées (activité
        // depuis T ? tokens depuis T ?) — ils ne sont PLUS un IUsageProvider et sont donc HORS de la
        // chaîne composite. Plus aucune dépendance à SettingsService : ni plafond, ni ancre hebdo.
        services.AddSingleton<ITranscriptActivitySource>(sp => new TranscriptActivityProvider(
            sp.GetRequiredService<ChronosPaths>(),
            sp.GetRequiredService<IClock>()));

        // EXA-01 : magasin persistant du dernier relevé exact (%APPDATA%\Chronos\last-exact.json).
        services.AddSingleton(sp => new LastExactStore(
            sp.GetRequiredService<ChronosPaths>().LastExactFile));
```

   - remplacer le bloc `services.AddSingleton<IUsageProvider>(sp => new CompositeUsageProvider( … ))`
     (lignes ~291-296) par EXACTEMENT :

```csharp
        // Chaîne exacte par imbrication, MEILLEURE source PAR FENÊTRE (composite) :
        //   login OAuth Chronos (exact) → OAuth coffre app (exact, gated) → pont statusLine (exact).
        // Le maillon JSONL a disparu (EXA-04) : le composite se termine sur le pont statusLine.
        // Le décorateur EXA-01 coiffe le tout : écrivain UNIQUE du dernier relevé exact, et rebouchage
        // des fenêtres Unavailable depuis le magasin. La règle Best() de CompositeUsageProvider n'est
        // PAS modifiée (c'est la phase 19).
        services.AddSingleton<IUsageProvider>(sp => new LastExactUsageProvider(
            inner: new CompositeUsageProvider(
                primary:  sp.GetRequiredService<ChronosOAuthUsageProvider>(),
                fallback: new CompositeUsageProvider(
                    primary:  sp.GetRequiredService<GatedOAuthUsageProvider>(),
                    fallback: sp.GetRequiredService<ClaudeUsageObjectProvider>())),
            store: sp.GetRequiredService<LastExactStore>(),
            clock: sp.GetRequiredService<IClock>()));
```

   - le commentaire d'en-tête du bloc « Pipeline de donnees Phase 3 » mentionnant « repli (JSONL) » doit
     être reformulé : la chaîne n'a plus de repli estimé.

2. `tests/Chronos.Tests/CompositionRootTests.cs`, test `Host_resout_et_dispose_les_singletons` — cette
   garde attrape un câblage qui COMPILE mais casse au démarrage ; elle doit refléter le nouveau graphe :
   - supprimer `services.AddSingleton<JsonlEstimationProvider>();` (ligne 37)
   - remplacer l'enregistrement d'`IUsageProvider` (lignes 38-40) par le décorateur au-dessus d'un
     composite se terminant sur `ClaudeUsageObjectProvider` :

```csharp
        services.AddSingleton(sp => new LastExactStore(
            sp.GetRequiredService<ChronosPaths>().LastExactFile));
        services.AddSingleton<IUsageProvider>(sp => new LastExactUsageProvider(
            inner: sp.GetRequiredService<ClaudeUsageObjectProvider>(),
            store: sp.GetRequiredService<LastExactStore>(),
            clock: sp.GetRequiredService<IClock>()));
```

     ATTENTION — GARDE ANTI-ACCIDENT : ce test utilise `ChronosPaths.Default()`, donc
     `LastExactFile` pointerait le VRAI `%APPDATA%\Chronos\last-exact.json`. Remplacer
     `services.AddSingleton(ChronosPaths.Default());` (ligne 35) par un `ChronosPaths` en dossier temp :

```csharp
        var tmpUsage = System.IO.Path.Combine(
            System.IO.Path.GetTempPath(), "ChronosDI_" + System.Guid.NewGuid().ToString("N"), "usage.json");
        services.AddSingleton(ChronosPaths.Default() with { UsageFile = tmpUsage });
```

     puis ajouter l'assertion `Assert.StartsWith(System.IO.Path.GetTempPath(),
     provider.GetRequiredService<LastExactStore>().Path);` — même motif anti-accident que le test
     `Le_graphe_DI_resout_le_reconciliateur_de_settings_Claude` (ligne 174).
   - ajouter `Assert.NotNull(provider.GetRequiredService<IUsageProvider>());` et
     `Assert.IsType<LastExactUsageProvider>(provider.GetRequiredService<IUsageProvider>());` — c'est la
     preuve que le décorateur est bien en TÊTE, pas enterré.
   - ajouter `services.AddSingleton<ITranscriptActivitySource>(sp => new TranscriptActivityProvider(
     sp.GetRequiredService<ChronosPaths>(), sp.GetRequiredService<IClock>()));` et
     `Assert.NotNull(provider.GetRequiredService<ITranscriptActivitySource>());`.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln` : **0 erreur**.
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec** sur la suite complète.
    - `grep -rn "JsonlEstimationProvider" src/ tests/ --include=*.cs` → **0 résultat**.
    - `grep -c "new LastExactUsageProvider(" src/Chronos/App.xaml.cs` → **1**.
    - `grep -c "AddSingleton<ITranscriptActivitySource>" src/Chronos/App.xaml.cs` → **1**.
    - `grep -c "CompositeUsageProvider" src/Chronos/App.xaml.cs` → **2** (l'imbrication passe de 3 à 2 niveaux).
    - `git diff --name-only HEAD -- src/Chronos/Services/CompositeUsageProvider.cs` → **vide** (la règle `Best()` n'est pas touchée — c'est la phase 19).
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~CompositionRootTests"` → 3 tests, 0 échec.
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~CompositeUsageProviderTests"` → 9 tests, 0 échec, **fichier non modifié**.
    - `ls "$APPDATA/Chronos/last-exact.json"` → **absent** après la suite de tests (garde anti-accident effective).
  </acceptance_criteria>

  <done>
La chaîne composite se termine sur le pont statusLine et est coiffée du décorateur de persistance.
`ITranscriptActivitySource` est enregistré hors de la chaîne. La garde DI reproduit le nouveau graphe et
prouve que le décorateur est en tête. Aucun `JsonlEstimationProvider` ne subsiste nulle part.
  </done>
</task>

</tasks>

<verification>
1. `dotnet clean Chronos.sln && dotnet build Chronos.sln -v q --nologo` → 0 erreur.
2. `dotnet test Chronos.sln -v q --nologo` → **0 échec**.
3. `dotnet test Chronos.sln --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert.
4. `dotnet test Chronos.sln --filter "FullyQualifiedName~CompositionRootTests"` → vert.
5. `dotnet test Chronos.sln --filter "FullyQualifiedName~FiveHourWindowInferenceTests"` → 7 tests verts
   et `--filter "FullyQualifiedName~WeeklyWindowTests"` → 4 tests verts : les orphelins conservés le
   restent, avec leur couverture.
6. `grep -rn "Utilization\s*=\s*.*[Bb]udget\|tokens / \|/ budget" src/ --include=*.cs` → **0 résultat** :
   plus aucun type ne dérive une utilization d'un comptage de tokens (EXA-04 structurellement acquis).
7. Contrôle de non-régression d'affichage (attendu, à consigner dans le SUMMARY, pas à corriger) :
   anneau hebdo gris, texte « ≈ N tokens » absent, anneau 5 h inchangé, bandeau « données indisponibles »
   non déclenché.
</verification>

<success_criteria>
- Les transcripts répondent aux deux questions bornées de DEL-01 et DEL-02 et ne produisent plus aucun
  pourcentage ni `UsageSnapshot`.
- Le parcours disque tolérant (FileShare, ligne partielle, prose, timestamps futurs, subagents,
  filtre mtime) est conservé mot pour mot — prouvé par le `git mv` et les tests conservés.
- La chaîne composite est recâblée et le décorateur EXA-01 est en tête, avec la garde DI qui le prouve.
- `CompositeUsageProvider.Best()` n'a pas bougé d'une ligne (phase 19).
- `FiveHourWindowInference` et `WeeklyWindow` sont conservés et documentés comme orphelins en réserve.
- Aucune dépendance NuGet ajoutée.
</success_criteria>

<output>
After completion, create `.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-03-SUMMARY.md`
</output>
