---
phase: 16-fondations-du-delta-persistance-d-molition-des-plafonds
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-03"]
files_modified:
  - src/Chronos/Services/ChronosSettings.cs
  - src/Chronos/Services/BudgetSource.cs
  - src/Chronos/Services/DiagnosticService.cs
  - tests/Chronos.Tests/SettingsServiceTests.cs
  - tests/Chronos.Tests/TestData/settings-legacy-plafonds.json
  - tests/Chronos.Tests/ServicesLayerPurityTests.cs
autonomous: true
requirements: [DEL-05, DEL-06]

must_haves:
  truths:
    - "Un settings.json de production portant les six champs de plafonds s'ouvre sans erreur : les champs obsolètes sont ignorés."
    - "Les dix-huit préférences survivantes (coin, écran, position, thème, mode et style de cadran, style de sessions, widget, ancre hebdo, source OAuth…) sont restituées à l'identique depuis ce même fichier."
    - "Plus aucun type nommé Budget* n'existe dans l'assembly Chronos — le retour du sous-système de plafonds est structurellement détecté."
    - "Le diagnostic ne mentionne plus aucun plafond ni aucune entrée « Calibrer les plafonds… »."
  artifacts:
    - path: "src/Chronos/Services/ChronosSettings.cs"
      provides: "Schéma de réglages amputé des six champs de plafonds"
      absent_pattern: "TokenBudget|BudgetSource|BudgetCalibratedAt"
    - path: "tests/Chronos.Tests/TestData/settings-legacy-plafonds.json"
      provides: "Fixture figée du settings.json réel de production (6 obsolètes + 18 survivants)"
      contains: "FiveHourBudgetSource"
    - path: "tests/Chronos.Tests/SettingsServiceTests.cs"
      provides: "Preuve DEL-06 sur fixture réelle"
      contains: "settings-legacy-plafonds.json"
    - path: "tests/Chronos.Tests/ServicesLayerPurityTests.cs"
      provides: "Garde de non-retour DEL-05 par réflexion sur l'assembly"
      contains: "Budget"
  key_links:
    - from: "tests/Chronos.Tests/SettingsServiceTests.cs"
      to: "tests/Chronos.Tests/TestData/settings-legacy-plafonds.json"
      via: "copie de la fixture dans un dossier temp puis SettingsService.Load()"
      pattern: "settings-legacy-plafonds\\.json"
    - from: "tests/Chronos.Tests/ServicesLayerPurityTests.cs"
      to: "assembly Chronos"
      via: "réflexion GetTypes() filtrée sur le préfixe Budget"
      pattern: "StartsWith\\(\"Budget\""
---

<objective>
Retirer les six champs de plafonds de `ChronosSettings`, supprimer l'enum `BudgetSource`, purger les
trois derniers points d'accroche du diagnostic, et PROUVER que les réglages existants survivent à
l'opération.

Purpose: DEL-05 (achèvement) + DEL-06. Ce plan clôt la démolition : après lui, plus aucun type nommé
`Budget*` n'existe dans l'assembly.

Découverte majeure de la recherche, **vérifiée empiriquement sur le vrai
`%APPDATA%\Chronos\settings.json` de la machine** : **DEL-06 ne demande AUCUN code**. `System.Text.Json`
ignore les membres JSON non mappés par défaut (`JsonUnmappedMemberHandling.Skip`) ; un champ absent du
type cible est skippé au niveau du LECTEUR, avant toute tentative de conversion — c'est pourquoi ni un
enum supprimé portant `"Manual"`, ni un champ de type incohérent (`{"a":[1,2,3]}`), ni une valeur
d'enum invalide ne peuvent lever. Les 18 préférences survivent intactes, l'offset `+02:00` de
`WeeklyAnchor` fait le round-trip, et les 6 champs obsolètes disparaissent passivement du fichier au
premier `Save()` (déplacement de l'overlay, changement de thème, n'importe quel toggle).

**Le livrable DEL-06 est donc un TEST avec fixture réelle, pas un migrateur.** Écrire un
`SettingsMigrator` serait du code mort à maintenir pour un problème inexistant : c'est explicitement
interdit par ce plan.

Output: `ChronosSettings` à 18 propriétés, `BudgetSource.cs` supprimé, `DiagnosticService` purgé, la
fixture `settings-legacy-plafonds.json`, le test DEL-06 et la garde de non-retour DEL-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-RESEARCH.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-01-SUMMARY.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrats à connaître SANS explorer la base de code. -->

Options de désérialisation de `SettingsService` (src/Chronos/Services/SettingsService.cs:25-32) —
**NE PAS TOUCHER**, et surtout **NE JAMAIS ajouter `JsonUnmappedMemberHandling.Disallow`**, ce serait
exactement l'inverse du besoin :
```csharp
private static readonly JsonSerializerOptions Options = new()
{
    PropertyNameCaseInsensitive = true,
    AllowTrailingCommas = true,
    ReadCommentHandling = JsonCommentHandling.Skip,
    WriteIndented = true,
    Converters = { new JsonStringEnumConverter() },
};
```
Le `catch (Exception ex) when (ex is IOException or JsonException or ArgumentException)` de `Load()`
reste adéquat et ne demande aucun élargissement.

Les 6 champs à SUPPRIMER de `ChronosSettings` (lignes 57-73) :
```csharp
public long? FiveHourTokenBudget { get; init; }
public long? WeeklyTokenBudget { get; init; }
public BudgetSource FiveHourBudgetSource { get; init; } = BudgetSource.None;
public DateTimeOffset? FiveHourBudgetCalibratedAt { get; init; }
public BudgetSource WeeklyBudgetSource { get; init; } = BudgetSource.None;
public DateTimeOffset? WeeklyBudgetCalibratedAt { get; init; }
```

Les 18 propriétés qui DOIVENT survivre (aucune ne bouge) :
`Corner`, `MonitorDeviceName`, `X`, `Y`, `Background`, `RefreshIntervalSeconds`, `WeeklyAnchor`,
`OAuthUsageEnabled`, `InnerStatusLineCommand`, `StatusLinePromptDismissed`, `ThemeKey`,
`SessionsWidgetEnabled`, `SessionsX`, `SessionsY`, `CadranMode`, `CadranStyle`, `SessionStyle`,
`VerticalLayout`.

Type à SUPPRIMER après les six champs (et jamais avant — Pitfall 2) :
```csharp
public enum BudgetSource { None, Manual, Auto }   // src/Chronos/Services/BudgetSource.cs
```

Motif de fixture existant à imiter : `tests/Chronos.Tests/TestData/claude-settings-pollue.json`
(phase 15) — état runtime réel figé comme preuve exécutable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retirer les six champs, supprimer BudgetSource et purger le diagnostic (geste atomique)</name>

  <read_first>
    - src/Chronos/Services/ChronosSettings.cs (lignes 50-80)
    - src/Chronos/Services/BudgetSource.cs
    - src/Chronos/Services/DiagnosticService.cs (lignes 275-292, 388-400, 465-475)
    - tests/Chronos.Tests/SettingsServiceTests.cs (lignes 35-75)
  </read_first>

  <files>
src/Chronos/Services/ChronosSettings.cs
src/Chronos/Services/BudgetSource.cs (supprimé)
src/Chronos/Services/DiagnosticService.cs
tests/Chronos.Tests/SettingsServiceTests.cs
  </files>

  <action>
GESTE ATOMIQUE UNIQUE : supprimer un champ d'un record casse tous ses lecteurs. `DiagnosticService` et
`SettingsServiceTests` doivent changer dans le MÊME commit. Ne PAS scinder.

ORDRE INTERNE IMPOSÉ (Pitfall 2) : les six champs d'abord, `BudgetSource.cs` ensuite. Après le plan
16-01, `ChronosSettings` est le SEUL référent de `BudgetSource` ; supprimer l'enum avant les champs
casserait la compilation.

1. `src/Chronos/Services/ChronosSettings.cs` — supprimer les 6 propriétés et leurs XML-doc
   (lignes 57-73) : `FiveHourTokenBudget`, `WeeklyTokenBudget`, `FiveHourBudgetSource`,
   `FiveHourBudgetCalibratedAt`, `WeeklyBudgetSource`, `WeeklyBudgetCalibratedAt`. Ne toucher à AUCUNE
   autre propriété, ni à leur ordre, ni à leurs valeurs par défaut. Ajouter au XML-doc du record :

```
/// <para>Phase 16 (DEL-05/DEL-06) : les six champs de plafonds ont été retirés. Un settings.json
/// antérieur qui les porte encore s'ouvre sans erreur — System.Text.Json ignore les membres non
/// mappés (JsonUnmappedMemberHandling.Skip) — et ils disparaissent du fichier au premier Save().
/// Aucune migration active n'est nécessaire ; la preuve est le test de non-régression
/// SettingsServiceTests + la fixture TestData/settings-legacy-plafonds.json.</para>
```

2. Supprimer `src/Chronos/Services/BudgetSource.cs` (`git rm`).

3. `src/Chronos/Services/DiagnosticService.cs` — trois points d'accroche :
   - lignes 283-284 : supprimer les deux `sb.AppendLine("  Plafond 5 h   : " …)` et
     `sb.AppendLine("  Plafond hebdo : " …)`. Le titre de section ligne 278
     `"[Estimation — transcripts JSONL (repli)]"` devient faux : le remplacer par
     `"[Transcripts JSONL (source de delta)]"`. Le comptage des fichiers `.jsonl` reste utile et est
     conservé tel quel.
   - ligne ~397 : supprimer intégralement la ligne
     `sb.AppendLine("  (Repli : « Calibrer les plafonds… » colore une estimation quand aucune source exacte n'est là.)");`
     Ne PAS la remplacer par un nouveau conseil : la refonte du diagnostic est EXA-06, phase 20 — ici
     on retire, on n'invente pas.
   - ligne ~471 : dans `Describe(WindowState w)`, remplacer le libellé
     `"% inconnu (pas de plafond → gris)"` par `"% inconnu"`. La branche
     `SourceReliability.Estimated` elle-même est CONSERVÉE (la valeur d'enum survit — la phase 19 en
     aura besoin comme marqueur pour « exact + delta »).
   - vérifier que `using System.Globalization;` reste nécessaire ailleurs dans le fichier avant de le
     retirer ; ne pas supprimer un `using` encore utilisé.

4. `tests/Chronos.Tests/SettingsServiceTests.cs` :
   - test `Save_puis_Load_round_trip` : retirer les deux lignes 48-49 de la fixture en mémoire
     (`FiveHourTokenBudget = 88_000,` et `WeeklyTokenBudget = 1_200_000,`). L'assertion finale
     `Assert.Equal(original, relu)` (égalité de valeur du record) est conservée telle quelle.
   - test `Load_fichier_absent_redonne_les_defauts` : retirer les deux assertions lignes 69-70
     (`Assert.Null(s.FiveHourTokenBudget);` et `Assert.Null(s.WeeklyTokenBudget);`). Les 6 autres
     assertions de défaut sont conservées.
   - AUCUN test n'est supprimé dans ce fichier : les 5 tests existants restent, seuls 4 lignes
     d'assertions/fixture disparaissent parce que les champs qu'elles visaient n'existent plus.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet build Chronos.sln` : **0 erreur**.
    - `dotnet test Chronos.sln -v q --nologo` : **0 échec**.
    - `grep -rn "TokenBudget\|BudgetSource\|BudgetCalibratedAt" src/ tests/ --include=*.cs` → **0 résultat**.
    - `ls src/Chronos/Services/BudgetSource.cs` → **absent**.
    - `grep -rn "[Pp]lafond" src/Chronos --include=*.cs --include=*.xaml` (hors `obj/`) → **0 résultat**.
    - `grep -c "public " src/Chronos/Services/ChronosSettings.cs` : le record expose exactement **18 propriétés** (`Corner`, `MonitorDeviceName`, `X`, `Y`, `Background`, `RefreshIntervalSeconds`, `WeeklyAnchor`, `OAuthUsageEnabled`, `InnerStatusLineCommand`, `StatusLinePromptDismissed`, `ThemeKey`, `SessionsWidgetEnabled`, `SessionsX`, `SessionsY`, `CadranMode`, `CadranStyle`, `SessionStyle`, `VerticalLayout`) — vérifier nominativement, pas au compteur brut.
    - `grep -n "JsonUnmappedMemberHandling" src/Chronos/Services/SettingsService.cs` → **0 résultat** (les options n'ont pas été touchées).
    - `git diff --name-only HEAD -- src/Chronos/Services/SettingsService.cs` → **vide**.
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~SettingsServiceTests"` → **5 tests, 0 échec**.
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~DiagnosticServiceTests"` → **2 tests, 0 échec** (aucune assertion sur les plafonds : ils restent verts sans modification).
  </acceptance_criteria>

  <done>
`ChronosSettings` n'a plus que ses 18 préférences, `BudgetSource` n'existe plus, et le diagnostic ne
mentionne plus ni plafond ni calibration. La solution compile, la suite est à 0 échec.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Figer la fixture du settings.json réel et prouver DEL-06</name>

  <read_first>
    - tests/Chronos.Tests/SettingsServiceTests.cs (intégralement — helpers d'isolation)
    - tests/Chronos.Tests/TestData/claude-settings-pollue.json (motif de fixture d'état réel, phase 15)
    - src/Chronos/Services/SettingsService.cs
    - src/Chronos/Services/ChronosSettings.cs (après la tâche 1)
  </read_first>

  <files>
tests/Chronos.Tests/TestData/settings-legacy-plafonds.json
tests/Chronos.Tests/SettingsServiceTests.cs
  </files>

  <behavior>
    - Charger la fixture réelle (6 champs obsolètes + 18 survivants) ne lève AUCUNE exception.
    - Les 18 préférences sont restituées EXACTEMENT aux valeurs du fichier :
      `Corner == OverlayCorner.BottomRight`, `MonitorDeviceName == @"\\.\DISPLAY2"`, `X == -182.4`,
      `Y == 921.6`, `Background == false`, `RefreshIntervalSeconds == 60`,
      `WeeklyAnchor == 2026-07-11T00:00:00+02:00` (offset compris), `OAuthUsageEnabled == true`,
      `InnerStatusLineCommand == null`, `StatusLinePromptDismissed == false`, `ThemeKey == "ardoise"`,
      `SessionsWidgetEnabled == true`, `SessionsX == -77.60000000000001`, `SessionsY == 107.2`,
      `CadranMode == CadranDisplayMode.Normal`, `CadranStyle == CadranStyle.Arcs`,
      `SessionStyle == SessionStyle.Veilleurs`, `VerticalLayout == true`.
    - Purge passive : après `Load()` puis `Save()`, le texte du fichier ne contient plus aucun des
      six noms de champ obsolètes, et contient toujours `"ThemeKey"` et `"SessionStyle"`.
    - Un champ obsolète de TYPE INCOHÉRENT (`"FiveHourTokenBudget": {"a":[1,2,3]}`) est ignoré sans
      exception, les 18 préférences restant intactes.
    - Une valeur d'ENUM INVALIDE sur un champ supprimé (`"FiveHourBudgetSource": "NimporteQuoi"`) est
      ignorée sans exception.
  </behavior>

  <action>
1. Créer `tests/Chronos.Tests/TestData/settings-legacy-plafonds.json` — copie FIDÈLE du
   `%APPDATA%\Chronos\settings.json` réel de la machine au 2026-09-09. Contenu EXACT à écrire :

```json
{
  "Corner": "BottomRight",
  "MonitorDeviceName": "\\\\.\\DISPLAY2",
  "X": -182.4,
  "Y": 921.6,
  "Background": false,
  "RefreshIntervalSeconds": 60,
  "WeeklyAnchor": "2026-07-11T00:00:00+02:00",
  "FiveHourTokenBudget": 230000000,
  "WeeklyTokenBudget": 5817635413,
  "FiveHourBudgetSource": "Manual",
  "FiveHourBudgetCalibratedAt": "2026-07-09T07:11:25.6903417+00:00",
  "WeeklyBudgetSource": "Auto",
  "WeeklyBudgetCalibratedAt": "2026-07-12T16:19:42.182414+00:00",
  "OAuthUsageEnabled": true,
  "InnerStatusLineCommand": null,
  "StatusLinePromptDismissed": false,
  "ThemeKey": "ardoise",
  "SessionsWidgetEnabled": true,
  "SessionsX": -77.60000000000001,
  "SessionsY": 107.2,
  "CadranMode": "Normal",
  "CadranStyle": "Arcs",
  "SessionStyle": "Veilleurs",
  "VerticalLayout": true
}
```

   PIÈGE À ÉVITER (Pitfall 1 de la recherche) : ne PAS fabriquer la fixture en sérialisant un
   `new ChronosSettings()`. Le code actuel ne sait PLUS produire les six champs obsolètes ; une fixture
   générée ne les contiendrait pas et le test ne prouverait rien. La fixture doit être ce fichier
   littéral, figé.

   Vérifier que le `.csproj` de test copie bien `TestData/**` en sortie, ou que le test résout le
   chemin via `[CallerFilePath]` comme le font `TranscriptActivityProviderTests` et
   `ClaudeSettingsJsonTests` — reprendre le motif déjà en place, ne pas en inventer un troisième.

2. `tests/Chronos.Tests/SettingsServiceTests.cs` — ajouter 4 tests couvrant le bloc `<behavior>` :
   - `Reglages_avec_anciens_plafonds_s_ouvrent_sans_erreur_et_conservent_les_18_preferences` — copier
     la fixture dans un dossier temp sous le nom `settings.json`, construire un `SettingsService` sur
     un `ChronosPaths` pointant ce dossier, appeler `Load()` et asserter les 18 valeurs listées dans
     `<behavior>` une par une. C'est LE test DEL-06.
   - `Les_six_champs_obsoletes_disparaissent_au_premier_Save` — `Load()` puis `Save(chargé)`, puis
     `File.ReadAllText` et asserter `DoesNotContain` sur les 6 noms de champ et `Contains` sur
     `"ThemeKey"` et `"SessionStyle"`.
   - `Champ_obsolete_de_type_incoherent_est_ignore` — fixture modifiée en mémoire avec
     `"FiveHourTokenBudget": {"a":[1,2,3]}` → `Load()` ne lève pas, `ThemeKey == "ardoise"`.
   - `Valeur_d_enum_supprimee_invalide_est_ignoree` — `"FiveHourBudgetSource": "NimporteQuoi"` →
     `Load()` ne lève pas, `Corner == OverlayCorner.BottomRight`.

   INTERDIT EXPLICITE : ne créer AUCUN type `SettingsMigrator` / `SettingsUpgrader` / méthode
   `Migrate`. Le comportement par défaut de `System.Text.Json` est le mécanisme ; le test est la preuve.
   Ne pas modifier `SettingsService.cs`.
  </action>

  <verify>
    <automated>dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~SettingsServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~SettingsServiceTests"` → **9 tests (5 existants + 4 nouveaux), 0 échec**.
    - `ls tests/Chronos.Tests/TestData/settings-legacy-plafonds.json` → **présent**.
    - `grep -c "FiveHourBudgetSource" tests/Chronos.Tests/TestData/settings-legacy-plafonds.json` → **1** (la fixture porte bien les champs obsolètes).
    - `grep -c "SessionStyle" tests/Chronos.Tests/TestData/settings-legacy-plafonds.json` → **1**.
    - `grep -c "settings-legacy-plafonds.json" tests/Chronos.Tests/SettingsServiceTests.cs` → **≥ 1** (le test DEL-06 lit bien un fichier de `TestData/`, pas une fixture générée).
    - `grep -rn "SettingsMigrator\|SettingsUpgrader\|Migrate" src/ tests/ --include=*.cs` → **0 résultat**.
    - `git diff --name-only HEAD -- src/Chronos/Services/SettingsService.cs src/Chronos/Services/ChronosSettings.cs` → **vide** (aucun code de production touché par cette tâche).
    - `ls "$APPDATA/Chronos/settings.json.tmp-"*` → **0 résultat** (les tests travaillent en dossier temp, jamais dans le profil réel).
  </acceptance_criteria>

  <done>
DEL-06 est prouvé par une fixture figée de l'état réel de production : les 6 champs obsolètes sont
ignorés, y compris avec un type incohérent et une valeur d'enum invalide, les 18 préférences sont
restituées à l'identique, et la purge est passive au premier `Save()`. Aucun migrateur n'a été écrit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Poser la garde de non-retour DEL-05 par réflexion sur l'assembly</name>

  <read_first>
    - tests/Chronos.Tests/ServicesLayerPurityTests.cs (intégralement — motif de garde réflexive)
  </read_first>

  <files>
tests/Chronos.Tests/ServicesLayerPurityTests.cs
  </files>

  <action>
Ajouter un `[Fact]` à `ServicesLayerPurityTests` — le fichier de gardes réflexives permanentes du dépôt.
Ce test échouera si quiconque réintroduit un type de plafond, y compris par un copier-coller depuis
l'historique git.

```csharp
    /// <summary>
    /// GARDE DE NON-RETOUR (DEL-05, phase 16). Le sous-système de plafonds — cause racine des
    /// pourcentages faux après le passage Max x5 → Max x20 — a été entièrement supprimé. Les limites
    /// Anthropic pondèrent par modèle : « tokens / plafond » reste faux même avec le bon plafond, la
    /// calibration n'a donc aucun avenir. Ce test échoue si un type nommé Budget* réapparaît dans
    /// l'assembly, quelle qu'en soit la voie (rétablissement depuis l'historique, nouvelle
    /// implémentation, dialogue WPF). Le remplacement légitime est la correction par DELTA
    /// (phase 19, DEL-03/DEL-04), qui ne dérive jamais un pourcentage d'un comptage de tokens.
    /// </summary>
    [Fact]
    public void Aucun_type_de_plafond_ne_subsiste_dans_l_assembly()
    {
        var asm = typeof(Chronos.Services.IUsageProvider).Assembly;

        var revenants = asm.GetTypes()
            .Where(t => t.Name.StartsWith("Budget", StringComparison.Ordinal))
            .Select(t => t.FullName)
            .ToList();

        Assert.Empty(revenants);
    }
```

Vérifier que les `using` nécessaires (`System`, `System.Linq`) sont présents en tête du fichier ou
disponibles par les usings implicites du projet de test ; ne pas ajouter de `using` inutile.

NE PAS toucher au test existant `La_couche_Services_ne_reference_aucun_assembly_WPF` ni à sa liste
`AdaptateursWpfAutorises`.
  </action>

  <verify>
    <automated>dotnet clean Chronos.sln --nologo &amp;&amp; dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~ServicesLayerPurityTests"` → **2 tests, 0 échec**.
    - `dotnet test Chronos.sln -v q --nologo` → **0 échec** sur la suite complète.
    - `grep -c "StartsWith(\"Budget\"" tests/Chronos.Tests/ServicesLayerPurityTests.cs` → **1**.
    - `grep -c "La_couche_Services_ne_reference_aucun_assembly_WPF" tests/Chronos.Tests/ServicesLayerPurityTests.cs` → **1** (le test historique est intact).
    - `grep -rn "Budget" src/Chronos --include=*.cs --include=*.xaml` (hors `obj/`) → **0 résultat**.
    - `find src/Chronos/obj src/Chronos/bin -iname "*Budget*"` → **0 résultat** après `dotnet clean`.
  </acceptance_criteria>

  <done>
Une garde permanente échoue si un type `Budget*` réapparaît dans l'assembly. La suite complète est verte
et l'arbre de build ne contient plus aucun artefact de plafond.
  </done>
</task>

</tasks>

<verification>
1. `dotnet clean Chronos.sln && dotnet build Chronos.sln -v q --nologo` → 0 erreur.
2. `dotnet test Chronos.sln -v q --nologo` → **0 échec**. Total attendu ≈ **405 ± 10** (baseline 405
   − 21 supprimés au plan 16-01 − 2 tests JSONL supprimés au plan 16-03, + ≈ 18 au plan 16-02,
   + ≈ 13 nets au plan 16-03, + 5 ici). **Le critère est 0 échec + aucune perte de couverture nette,
   PAS « ≥ 405 ».** Chaque test supprimé l'a été parce que le code qu'il couvrait n'existe plus —
   la justification est consignée dans les SUMMARY de 16-01 et 16-03.
3. `dotnet test Chronos.sln --filter "FullyQualifiedName~ServicesLayerPurityTests"` → 2 tests verts.
4. `dotnet test Chronos.sln --filter "FullyQualifiedName~CompositionRootTests"` → 3 tests verts.
5. `grep -rn "Budget" src/ tests/ --include=*.cs --include=*.xaml | grep -v "settings-legacy-plafonds\|TestData"` → 0 résultat (seule la fixture DEL-06 a le droit de porter ces noms — c'est son objet).
6. **Vérification manuelle (consignée dans `16-VALIDATION.md`)** : lancer l'app, ouvrir les réglages,
   confirmer que la `UniformGrid` « RÉGLAGES » affiche 3 boutons sans trou disgracieux ni bouton
   fantôme, et que le diagnostic ne mentionne plus aucun plafond.
</verification>

<success_criteria>
- Les six champs de plafonds, l'enum `BudgetSource` et les trois derniers points d'accroche du
  diagnostic ont disparu ; plus aucun type `Budget*` n'existe dans l'assembly.
- DEL-06 est prouvé par une fixture figée de l'état RÉEL de production, pas par une fixture générée,
  et aucun migrateur n'a été écrit — les options de `SettingsService` sont intactes.
- Les 18 préférences de l'utilisateur (coin, écran, position, thème, mode et style de cadran, style de
  sessions, widget, ancre hebdo, source OAuth) survivent à l'identique.
- La solution compile et la suite est à 0 échec à chacun des trois commits.
- Aucune dépendance NuGet ajoutée.
</success_criteria>

<output>
After completion, create `.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-04-SUMMARY.md`
</output>
