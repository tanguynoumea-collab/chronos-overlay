---
phase: 16-fondations-du-delta-persistance-d-molition-des-plafonds
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Services/ChronosPaths.cs
  - src/Chronos/Models/WindowState.cs
  - src/Chronos/Services/LastExactStore.cs
  - src/Chronos/Services/LastExactUsageProvider.cs
  - tests/Chronos.Tests/LastExactStoreTests.cs
  - tests/Chronos.Tests/LastExactUsageProviderTests.cs
autonomous: true
requirements: [EXA-01]

must_haves:
  truths:
    - "Un relevé exact écrit sur disque est relu à l'identique après recréation de l'objet magasin (le chiffre survit à un redémarrage de processus)."
    - "Un fichier last-exact.json absent, corrompu ou tronqué produit null — jamais une exception, jamais une valeur inventée."
    - "Une fenêtre dont le reset est déjà passé est REJETÉE au chargement : Chronos ne ressuscite pas un pourcentage qui ne décrit plus rien."
    - "FractionTimeRemaining n'est jamais lue du disque : elle est recalculée à partir de ResetsAt et de l'instant présent."
    - "Le décorateur écrit chaque fenêtre Exact qu'il voit passer et ne rebouche QUE les fenêtres Unavailable — il n'écrase jamais une réponse vivante."
  artifacts:
    - path: "src/Chronos/Services/LastExactStore.cs"
      provides: "Persistance atomique du dernier relevé exact, par fenêtre, schéma versionné"
      exports: ["Save", "Load", "Path"]
      min_lines: 90
    - path: "src/Chronos/Services/LastExactUsageProvider.cs"
      provides: "Décorateur IUsageProvider : écrivain unique + rebouchage des trous"
      exports: ["GetAsync"]
      min_lines: 40
    - path: "src/Chronos/Services/ChronosPaths.cs"
      provides: "Propriété calculée LastExactFile colocalisée avec usage.json"
      contains: "LastExactFile"
    - path: "src/Chronos/Models/WindowState.cs"
      provides: "Horodatage de capture PAR FENÊTRE"
      contains: "CapturedAt"
    - path: "tests/Chronos.Tests/LastExactStoreTests.cs"
      provides: "Preuve EXA-01 côté persistance"
      min_lines: 100
    - path: "tests/Chronos.Tests/LastExactUsageProviderTests.cs"
      provides: "Preuve EXA-01 côté décorateur"
      min_lines: 80
  key_links:
    - from: "src/Chronos/Services/LastExactStore.cs"
      to: "ChronosPaths.LastExactFile"
      via: "chemin injecté, jamais construit en dur"
      pattern: "LastExactFile"
    - from: "src/Chronos/Services/LastExactUsageProvider.cs"
      to: "LastExactStore"
      via: "Save des fenêtres Exact + Load pour le rebouchage"
      pattern: "_store\\.(Save|Load)"
    - from: "src/Chronos/Services/LastExactStore.cs"
      to: "WindowState.FractionRemaining"
      via: "recalcul au chargement (jamais persistée)"
      pattern: "WindowState\\.FractionRemaining"
---

<objective>
Créer le magasin persistant du dernier relevé exact et son décorateur d'écriture : les deux briques
d'EXA-01. Ce plan livre du code NEUF et ses tests, **sans aucun câblage DI** — le branchement dans
`App.ConfigureServices` est fait au plan 16-03, en même temps que le recâblage de la chaîne composite.

Purpose: EXA-01. Aujourd'hui `ChronosOAuthUsageProvider._cached` et `ClaudeOAuthUsageProvider._cached`
sont des champs d'instance en RAM : à chaque démarrage de l'exe, aucun chiffre exact n'est en mémoire et
Chronos bascule en silence. C'est le cas le plus fréquent du diagnostic et jamais identifié. Persister le
dernier relevé exact sur disque, avec son horodatage PAR FENÊTRE, est l'instant T de référence sans
lequel la correction par delta de la phase 19 est impossible.

Output: `ChronosPaths.LastExactFile`, `WindowState.CapturedAt`, `LastExactStore`,
`LastExactUsageProvider` et leurs deux fichiers de test.

Découpage volontaire — pourquoi pas de DI ici : `App.xaml.cs` et `CompositionRootTests.cs` sont modifiés
par les plans 16-01 et 16-03. Les laisser hors de ce plan rend 16-01 et 16-02 réellement parallélisables
en vague 1, sans conflit d'écriture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contrats existants que l'exécuteur doit réutiliser tels quels — ne pas réinventer. -->

```csharp
// src/Chronos/Models/WindowState.cs — état ACTUEL, à étendre d'un seul champ nullable
public sealed record WindowState
{
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }            // 0..1 ; null si inconnu
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public long? EstimatedTokens { get; init; }
    public required SourceReliability Reliability { get; init; }

    public bool Exhausted => Utilization is >= 1.0;
    public static WindowState Unavailable(WindowKind k) =>
        new() { Kind = k, Reliability = SourceReliability.Unavailable };
    public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength);
}

// src/Chronos/Models/UsageSnapshot.cs
public sealed record UsageSnapshot
{
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
    public DateTimeOffset? SourceCapturedAt { get; init; }
    public static UsageSnapshot Empty { get; }   // deux fenêtres Unavailable
}

// src/Chronos/Models/SourceReliability.cs
public enum SourceReliability { Exact, Estimated, Unavailable }
// src/Chronos/Models/WindowKind.cs → FiveHour / SevenDay

// src/Chronos/Services/IUsageProvider.cs — contrat à décorer
public interface IUsageProvider { Task<UsageSnapshot> GetAsync(CancellationToken ct = default); }

// src/Chronos/Services/IClock.cs
public interface IClock { DateTimeOffset UtcNow { get; } }

// src/Chronos/Services/ChronosPaths.cs — record positionnel + propriété CALCULÉE (motif à copier)
public sealed record ChronosPaths(string UsageFile, string ProjectsRoot)
{
    public static ChronosPaths Default();
    public string SettingsFile => Path.Combine(Path.GetDirectoryName(UsageFile)!, "settings.json");
}

// tests/Chronos.Tests/Fakes/FakeUsageProvider.cs — déjà disponible
internal sealed class FakeUsageProvider : IUsageProvider
{
    public int GetCount { get; }
    public UsageSnapshot Next = UsageSnapshot.Empty;
    public ManualResetEventSlim? Gate;
}
// tests/Chronos.Tests/Fakes/FakeClock.cs — horloge figée injectable
```

Motif d'écriture atomique CANONIQUE du dépôt (`SettingsService.cs:59-70`, `ArchiveStore.cs:62-71`) — à
copier tel quel, ne rien inventer :

```csharp
var dir = Path.GetDirectoryName(_path)!;
Directory.CreateDirectory(dir);
var json = JsonSerializer.Serialize(payload, Options);
var tmp = _path + $".tmp-{Environment.ProcessId}";   // unique par process, même volume → Move atomique
File.WriteAllText(tmp, json);
File.Move(tmp, _path, overwrite: true);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer LastExactStore — schéma versionné, écriture atomique, lecture tolérante et honnête</name>

  <read_first>
    - src/Chronos/Services/ChronosPaths.cs (intégralement — motif SettingsFile à répliquer)
    - src/Chronos/Models/WindowState.cs (intégralement)
    - src/Chronos/Models/UsageSnapshot.cs
    - src/Chronos/Services/SettingsService.cs (lignes 25-70 — options JSON + écriture atomique)
    - src/Chronos/Services/ArchiveStore.cs (lecture tolérante → neutre)
    - tests/Chronos.Tests/Fakes/FakeClock.cs
  </read_first>

  <files>
src/Chronos/Services/ChronosPaths.cs
src/Chronos/Models/WindowState.cs
src/Chronos/Services/LastExactStore.cs
tests/Chronos.Tests/LastExactStoreTests.cs
  </files>

  <behavior>
    - Round-trip : `Save` d'un snapshot dont les deux fenêtres sont `Exact` puis `Load` sur un NOUVEAU
      `LastExactStore` pointant le même fichier → les deux fenêtres reviennent avec leur `Utilization`,
      leur `ResetsAt` et leur `CapturedAt`, et `Reliability == Exact`.
    - Deux `captured_at` distincts : `Save` d'un snapshot dont la 5 h a `CapturedAt = T1` et l'hebdo
      `CapturedAt = T2` (T1 ≠ T2) → `Load` restitue T1 et T2 séparément.
    - Fenêtre non-Exact ignorée à l'écriture : `Save` d'un snapshot dont l'hebdo est `Unavailable` →
      `Load` renvoie une 5 h peuplée et une hebdo à `null`.
    - Écriture non destructive par fenêtre : `Save` d'un snapshot 5 h `Exact` + hebdo `Unavailable` APRÈS
      un `Save` où les deux étaient `Exact` → l'hebdo précédemment persistée SURVIT (on n'efface jamais
      une fenêtre exacte connue avec une absence de donnée).
    - Fichier absent → `Load` renvoie `null`, aucune exception.
    - Fichier corrompu (`"{ ceci n'est pas du JSON ]"`) → `Load` renvoie `null`, aucune exception.
    - `version` inconnue (ex. `999`) → `Load` renvoie `null`, aucune exception.
    - Fenêtre « roulée » rejetée : un fichier dont `five_hour.resets_at <= now` → `Load(now)` renvoie
      `null` POUR CETTE FENÊTRE (l'autre fenêtre, encore valide, est restituée).
    - `FractionTimeRemaining` recalculée : un fichier ne contenant AUCUN champ de fraction produit
      quand même une `FractionTimeRemaining` non nulle et cohérente avec `(resets_at − now) / longueur`.
      Et une fraction éventuellement présente dans le JSON est IGNORÉE.
    - Atomicité : après `Save`, aucun fichier `last-exact.json.tmp-*` ne subsiste dans le dossier.
    - Isolation : `new ChronosPaths(usageDansUnDossierTemp, projects).LastExactFile` pointe dans ce
      dossier temp (jamais `%APPDATA%`).
  </behavior>

  <action>
1. `src/Chronos/Services/ChronosPaths.cs` — ajouter une propriété CALCULÉE, en copiant mot pour mot le
   raisonnement documenté de `SettingsFile`. NE PAS ajouter de paramètre au ctor positionnel (cela
   casserait des dizaines de sites de construction en test) :

```csharp
    /// <summary>
    /// last-exact.json colocalisé avec usage.json (même dossier %APPDATA%\Chronos). Propriété
    /// calculée : le ctor positionnel <c>(UsageFile, ProjectsRoot)</c> reste inchangé, donc les
    /// tests qui construisent un répertoire temp obtiennent aussi un LastExactFile isolé.
    /// </summary>
    public string LastExactFile => Path.Combine(Path.GetDirectoryName(UsageFile)!, "last-exact.json");
```

2. `src/Chronos/Models/WindowState.cs` — ajouter UN champ, additif et nullable (aucun site de
   construction n'est cassé : tous utilisent des initialiseurs d'objet) :

```csharp
    /// <summary>Instant de capture de CETTE fenêtre par la source qui l'a produite. null = inconnu.
    /// Par fenêtre et non par snapshot : le composite choisit la meilleure source PAR FENÊTRE, les
    /// deux peuvent donc venir de sources différentes à des instants différents.</summary>
    public DateTimeOffset? CapturedAt { get; init; }
```

3. Créer `src/Chronos/Services/LastExactStore.cs`. Type NEUTRE (aucun `using System.Windows.*`).
   Commentaires et XML-doc en français.

   Schéma persisté (dates ISO 8601 avec offset, format `"o"`, jamais d'epoch) :

```json
{
  "version": 1,
  "five_hour": { "utilization": 0.42, "resets_at": "2026-09-09T18:30:00+00:00", "captured_at": "2026-09-09T15:12:03+00:00" },
  "seven_day": { "utilization": 0.63, "resets_at": "2026-09-14T09:00:00+00:00", "captured_at": "2026-09-09T15:12:03+00:00" }
}
```

   Surface publique EXACTE à implémenter :

```csharp
public sealed class LastExactStore
{
    public const int SchemaVersion = 1;
    public LastExactStore(string path);
    public string Path { get; }
    /// <summary>Persiste les fenêtres Exact du snapshot. Une fenêtre non-Exact laisse la valeur
    /// précédemment persistée INTACTE (on n'efface jamais un chiffre connu avec une absence).</summary>
    public void Save(UsageSnapshot snapshot);
    /// <summary>Relit les fenêtres encore valides à l'instant <paramref name="now"/>.
    /// null = rien d'exploitable (fichier absent, corrompu, version inconnue, ou toutes fenêtres rollées).</summary>
    public LastExactWindows? Load(DateTimeOffset now);
}

/// <summary>Fenêtres exactes rechargées. Chaque membre est null si absent ou invalidé.</summary>
public sealed record LastExactWindows(WindowState? FiveHour, WindowState? SevenDay);
```

   Règles NON NÉGOCIABLES (chacune justifiée par la Core Value) :
   - **`Reliability` n'est PAS persistée.** Elle est FORCÉE à `SourceReliability.Exact` à la
     reconstruction, puisque seules des fenêtres `Exact` sont écrites. Ne pas la lire du fichier :
     un fichier corrompu ou édité à la main ne doit pas pouvoir injecter un faux `Exact`.
   - **`FractionTimeRemaining` n'est PAS persistée.** Elle est recalculée au chargement via
     `WindowState.FractionRemaining(resetsAt, now, longueur)` avec `longueur = TimeSpan.FromHours(5)`
     pour `FiveHour` et `TimeSpan.FromDays(7)` pour `SevenDay`. La persister ressusciterait un chiffre
     périmé au démarrage = un chiffre inventé.
   - **`EstimatedTokens` n'est PAS persistée** (une fenêtre `Exact` n'en porte jamais).
   - **`Kind` n'est PAS persistée** : implicite dans la clé `five_hour` / `seven_day`.
   - **Garde d'honnêteté au chargement** : si `resets_at <= now`, la fenêtre a été remise à zéro depuis
     la capture ; l'utilization persistée ne décrit plus rien → renvoyer `null` pour cette fenêtre. Ce
     n'est PAS la limite d'âge d'EXA-02 (phase 19) mais une invalidation logique.
   - **Lecture tolérante** : `try { … } catch { return null; }` — fichier absent, JSON invalide, date
     illisible, `version != SchemaVersion` → `null`. Jamais d'exception qui remonte.
   - **Écriture atomique** : `Directory.CreateDirectory` → `File.WriteAllText(path + ".tmp-" +
     Environment.ProcessId)` → `File.Move(tmp, path, overwrite: true)`. Motif copié de `SettingsService`.
   - `Save` relit le fichier existant AVANT d'écrire (fusion par fenêtre), pour respecter la règle
     « une fenêtre non-Exact laisse la précédente intacte ». La relecture de fusion doit être brute
     (sans la garde `resets_at <= now`) : la fusion ne doit pas purger silencieusement une fenêtre.

4. Créer `tests/Chronos.Tests/LastExactStoreTests.cs` couvrant les 11 comportements du bloc
   `<behavior>`. Chaque test travaille dans un dossier temp unique
   (`Path.Combine(Path.GetTempPath(), "ChronosLastExact_" + Guid.NewGuid().ToString("N"))`) — aucun test
   ne doit toucher `%APPDATA%\Chronos`. Horloge figée à `new DateTimeOffset(2026, 09, 09, 12, 0, 0,
   TimeSpan.Zero)`. Tests purs → `[Fact]` classiques, pas de STA.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo --filter "FullyQualifiedName~LastExactStoreTests"</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~LastExactStoreTests"` → **≥ 10 tests, 0 échec**.
    - `grep -c "LastExactFile" src/Chronos/Services/ChronosPaths.cs` → **1**.
    - `grep -c "CapturedAt" src/Chronos/Models/WindowState.cs` → **1**.
    - `grep -n "ChronosPaths(string UsageFile, string ProjectsRoot)" src/Chronos/Services/ChronosPaths.cs` → **1 résultat** (ctor positionnel INCHANGÉ).
    - `grep -c "FractionTimeRemaining" src/Chronos/Services/LastExactStore.cs` → au plus 1, et uniquement dans une affectation issue de `WindowState.FractionRemaining(...)` — **jamais** une lecture depuis le JSON.
    - `grep -n "WindowState.FractionRemaining" src/Chronos/Services/LastExactStore.cs` → **≥ 1 résultat**.
    - `grep -n "File.Move" src/Chronos/Services/LastExactStore.cs` → **1 résultat**, avec `overwrite: true`.
    - `grep -n "System.Windows" src/Chronos/Services/LastExactStore.cs src/Chronos/Models/WindowState.cs` → **0 résultat**.
    - `grep -rn "Assembly.Location\|GetExecutingAssembly" src/Chronos/Services/LastExactStore.cs` → **0 résultat**.
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert.
  </acceptance_criteria>

  <done>
`LastExactStore` persiste et relit le dernier relevé exact par fenêtre, de façon atomique et tolérante,
en rejetant les fenêtres dont le reset est passé et en recalculant la fraction de temps restante.
`ChronosPaths.LastExactFile` et `WindowState.CapturedAt` existent. Tests verts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Créer le décorateur LastExactUsageProvider — écrivain unique et rebouchage des trous</name>

  <read_first>
    - src/Chronos/Services/LastExactStore.cs (produit par la tâche 1)
    - src/Chronos/Services/CompositeUsageProvider.cs (motif de décorateur/compositeur à imiter)
    - src/Chronos/Services/GatedOAuthUsageProvider.cs (second motif de décorateur)
    - tests/Chronos.Tests/Fakes/FakeUsageProvider.cs
    - tests/Chronos.Tests/CompositeUsageProviderTests.cs (style d'assertion à reprendre)
  </read_first>

  <files>
src/Chronos/Services/LastExactUsageProvider.cs
tests/Chronos.Tests/LastExactUsageProviderTests.cs
  </files>

  <behavior>
    - Écriture : l'inner renvoie un snapshot dont les 2 fenêtres sont `Exact` → après `GetAsync`, le
      magasin contient les 2 fenêtres (vérifié par `store.Load(now)`).
    - Passe-plat : quand l'inner renvoie 2 fenêtres `Exact`, le snapshot retourné est identique à celui
      de l'inner (mêmes `Utilization`, mêmes `ResetsAt`).
    - Rebouchage : magasin pré-rempli d'une 5 h exacte encore valide, l'inner renvoie
      `UsageSnapshot.Empty` → le snapshot retourné porte une 5 h `Exact` avec l'`Utilization` et le
      `CapturedAt` du magasin, et l'hebdo reste `Unavailable`.
    - Non-écrasement : magasin pré-rempli à 0.80, l'inner renvoie une 5 h `Exact` à 0.20 → le snapshot
      retourné vaut 0.20 (une réponse vivante n'est JAMAIS remplacée par le magasin).
    - Rebouchage refusé si la fenêtre a roulé : magasin pré-rempli d'une 5 h dont `resets_at <= now`,
      inner `Empty` → la 5 h reste `Unavailable` (aucun chiffre ressuscité).
    - Portée étroite : magasin pré-rempli, l'inner renvoie une fenêtre `Estimated` → le décorateur
      ne rebouche PAS (il ne substitue QUE sur `Unavailable`) et n'écrit PAS (il n'écrit QUE de l'`Exact`).
    - Robustesse : un magasin dont le fichier est corrompu ne fait lever aucune exception ; le snapshot
      de l'inner est renvoyé tel quel.
    - Aucune écriture inutile : deux `GetAsync` successifs avec un inner `Empty` ne créent pas de fichier
      (rien d'`Exact` à écrire).
  </behavior>

  <action>
Créer `src/Chronos/Services/LastExactUsageProvider.cs`. Type NEUTRE, XML-doc en français.

Surface publique EXACTE :

```csharp
/// <summary>
/// Décorateur d'<see cref="IUsageProvider"/> en TÊTE de chaîne (EXA-01). Écrivain UNIQUE du magasin
/// persistant : il voit exactement le snapshot fusionné qui sera affiché, donc ni les providers ni
/// l'orchestrateur n'ont à connaître la persistance.
///
/// Rôle en phase 16, et RIEN DE PLUS :
/// 1. délègue à l'inner ;
/// 2. écrit dans le magasin chaque fenêtre <see cref="SourceReliability.Exact"/> du résultat ;
/// 3. si une fenêtre du résultat est <see cref="SourceReliability.Unavailable"/> ET que le magasin
///    détient pour elle un relevé exact encore valide, il la SUBSTITUE — pur rebouchage de trou.
///
/// Il ne remplace JAMAIS une réponse vivante : la doctrine de choix de source
/// (<see cref="CompositeUsageProvider"/>) n'est pas modifiée d'une ligne. La limite d'âge (EXA-02) et
/// la correction par delta (DEL-03/04) viendront se greffer ICI, en phase 19.
///
/// Choix de conception : un DÉCORATEUR, pas un abonnement à RefreshOrchestrator.SnapshotChanged —
/// ce dernier motif exigeait une résolution forcée avant StartAsync (invisible au compilateur), et
/// c'est précisément celui que la phase 16 démolit avec BudgetAutoCalibrator.
/// </summary>
public sealed class LastExactUsageProvider : IUsageProvider
{
    public LastExactUsageProvider(IUsageProvider inner, LastExactStore store, IClock clock);
    public Task<UsageSnapshot> GetAsync(CancellationToken ct = default);
}
```

Corps de `GetAsync` :
1. `var snap = await _inner.GetAsync(ct);`
2. `var now = _clock.UtcNow;`
3. Si au moins une fenêtre est `Exact` → `_store.Save(snap)` dans un `try/catch` qui n'interrompt jamais
   le flux (une écriture disque en échec ne doit pas priver l'utilisateur de son affichage).
4. Rebouchage : `var last = _store.Load(now);` (dans le même `try/catch` tolérant). Pour chaque fenêtre
   dont `Reliability == SourceReliability.Unavailable` ET pour laquelle `last` porte une valeur non nulle,
   substituer la fenêtre du magasin. La fenêtre substituée conserve son `CapturedAt` d'ORIGINE (c'est
   l'information dont les phases 19/20 auront besoin pour dire « depuis quand »).
5. Si au moins une fenêtre a été substituée et que `snap.SourceCapturedAt is null`, laisser
   `SourceCapturedAt` tel quel — ne PAS inventer d'horodatage au niveau snapshot ; l'âge honnête est
   porté par `WindowState.CapturedAt`.
6. Retourner le snapshot (éventuellement reconstruit par `with { FiveHour = …, SevenDay = … }`).

INTERDITS explicites dans ce fichier :
- ne pas modifier `CompositeUsageProvider` ni sa règle `Best()` (c'est la phase 19) ;
- ne pas reboucher une fenêtre `Estimated` (règle volontairement la plus étroite possible en phase 16) ;
- ne pas appliquer de limite d'âge (EXA-02, phase 19) ; seule la garde `resets_at <= now` du magasin
  s'applique, et elle vit dans `LastExactStore.Load`.

Créer `tests/Chronos.Tests/LastExactUsageProviderTests.cs` couvrant les 8 comportements ci-dessus, avec
`FakeUsageProvider` (champ `Next`) et `FakeClock`, et un `LastExactStore` pointant un fichier dans un
dossier temp unique. Aucun accès à `%APPDATA%`.
  </action>

  <verify>
    <automated>dotnet build Chronos.sln -v q --nologo &amp;&amp; dotnet test Chronos.sln -v q --nologo</automated>
  </verify>

  <acceptance_criteria>
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~LastExactUsageProviderTests"` → **≥ 8 tests, 0 échec**.
    - `dotnet test Chronos.sln -v q --nologo` → **0 échec** sur la suite complète.
    - `grep -c "class LastExactUsageProvider : IUsageProvider" src/Chronos/Services/LastExactUsageProvider.cs` → **1**.
    - `grep -n "System.Windows" src/Chronos/Services/LastExactUsageProvider.cs` → **0 résultat**.
    - `git diff --name-only HEAD~1 -- src/Chronos/Services/CompositeUsageProvider.cs` → **vide** (la doctrine de choix de source n'est pas touchée).
    - `git status --porcelain src/Chronos/App.xaml.cs tests/Chronos.Tests/CompositionRootTests.cs` → **vide** (aucun câblage DI dans ce plan — le conflit avec 16-01 est évité par construction).
    - `dotnet test Chronos.sln --filter "FullyQualifiedName~CompositeUsageProviderTests"` → 9 tests verts, inchangés.
  </acceptance_criteria>

  <done>
`LastExactUsageProvider` écrit chaque fenêtre exacte et rebouche uniquement les fenêtres indisponibles à
partir d'un relevé encore valide. Ses 8 comportements sont prouvés. Aucun fichier partagé avec le plan
16-01 n'a été modifié. La suite complète est à 0 échec.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Chronos.sln -v q --nologo` → 0 erreur.
2. `dotnet test Chronos.sln -v q --nologo` → **0 échec**. Attendu : baseline + ≈ 18 nouveaux tests
   (≥ 10 pour `LastExactStoreTests`, ≥ 8 pour `LastExactUsageProviderTests`).
3. `dotnet test Chronos.sln --filter "FullyQualifiedName~ServicesLayerPurityTests"` → vert (les deux
   nouveaux types de `Services/` n'exposent aucun type WPF).
4. `git status --porcelain` : les seuls fichiers touchés sont ceux de `files_modified`. En particulier
   `src/Chronos/App.xaml.cs` et `tests/Chronos.Tests/CompositionRootTests.cs` sont INTACTS.
5. `ls "$APPDATA/Chronos/last-exact.json"` → le fichier N'EXISTE PAS après les tests (preuve que les
   tests travaillent en dossier temp isolé, sans polluer le profil réel).
</verification>

<success_criteria>
- Les deux briques d'EXA-01 existent, sont testées, et sont neutres (aucun type WPF).
- Aucun chiffre n'est inventé : fraction recalculée, fiabilité dérivée, fenêtre rollée rejetée, fichier
  illisible → `null`.
- Le magasin vit sous `%APPDATA%\Chronos\` via `ChronosPaths`, jamais par un chemin construit en dur ni
  via `Assembly.Location`.
- Aucune dépendance NuGet ajoutée.
- Zéro fichier partagé avec le plan 16-01 : les deux plans de la vague 1 sont réellement parallélisables.
- Le câblage DI est explicitement DIFFÉRÉ au plan 16-03 — ce plan livre des briques, pas encore le
  comportement de bout en bout.
</success_criteria>

<output>
After completion, create `.planning/phases/16-fondations-du-delta-persistance-d-molition-des-plafonds/16-02-SUMMARY.md`
</output>
