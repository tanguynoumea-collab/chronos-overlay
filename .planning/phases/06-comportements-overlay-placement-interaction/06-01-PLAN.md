---
phase: 06-comportements-overlay-placement-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Placement/RectD.cs
  - src/Chronos/Placement/OverlayCorner.cs
  - src/Chronos/Placement/CornerSnap.cs
  - src/Chronos/Services/ChronosSettings.cs
  - src/Chronos/Services/SettingsService.cs
  - src/Chronos/Services/WeeklyRecalibration.cs
  - src/Chronos/Services/ChronosPaths.cs
  - tests/Chronos.Tests/CornerSnapTests.cs
  - tests/Chronos.Tests/SettingsServiceTests.cs
  - tests/Chronos.Tests/WeeklyRecalibrationTests.cs
autonomous: true
requirements: [FEN-03, FEN-07, ROB-03]

must_haves:
  truths:
    - "Étant donné un rectangle fenêtre et une WorkingArea, CornerSnap.NearestCorner renvoie le coin le plus proche (4 quadrants) en respectant la marge"
    - "CornerSnap.CornerToTopLeft place la fenêtre à un coin IMPOSÉ (restauration du coin persisté)"
    - "SettingsService écrit settings.json de façon atomique et le relit en round-trip"
    - "Un settings.json corrompu ou absent redonne des ChronosSettings par défaut sans exception"
    - "WeeklyRecalibration laisse une fenêtre Exact+ResetsAt inchangée et ne synthétise un ResetsAt futur que sur le repli avec une ancre"
    - "Une fenêtre recalibrée reste Reliability=Estimated (badge « estimée » conservé)"
  artifacts:
    - path: "src/Chronos/Placement/CornerSnap.cs"
      provides: "Fonctions pures NearestCorner + ClassifyCorner + CornerToTopLeft (neutre, sans type WPF)"
      contains: "public static class CornerSnap"
    - path: "src/Chronos/Services/SettingsService.cs"
      provides: "Load tolérant + Save atomique de ChronosSettings"
      exports: ["Load", "Save"]
    - path: "src/Chronos/Services/ChronosSettings.cs"
      provides: "Schéma settings.json (coin+device = vérité, X/Y indicatifs)"
      contains: "public sealed record ChronosSettings"
    - path: "src/Chronos/Services/WeeklyRecalibration.cs"
      provides: "Recalibrage hebdo pur (repli seulement, badge estimée conservé)"
      contains: "public static class WeeklyRecalibration"
  key_links:
    - from: "SettingsService"
      to: "ChronosPaths.SettingsFile"
      via: "chemin injecté %APPDATA%/Chronos/settings.json"
      pattern: "SettingsFile"
    - from: "WeeklyRecalibration.Apply"
      to: "WindowState.Reliability"
      via: "garde Exact → inchangé, sinon ResetsAt synthétisé en restant Estimated"
      pattern: "SourceReliability.Exact"
---

<objective>
Poser les fondations NEUTRES et testables de la Phase 6 : la logique pure de placement
(« coin le plus proche » + placement à un coin imposé), la persistance atomique/tolérante de
settings.json, et le recalibrage hebdo pur. Zéro type WPF — tout est vérifiable en `[Fact]`
sans écran ni STA.

Purpose : ces trois briques sont consommées par les plans 06-03 (placement physique via
OverlayController) et 06-04 (menu + dialogue de recalibrage). Les isoler en logique pure
garantit la couverture Nyquist des règles métier (FEN-03, FEN-07, ROB-03) sans dépendre
d'un moniteur réel.
Output : namespace `Chronos.Placement` (neutre), `ChronosSettings`/`SettingsService`/
`WeeklyRecalibration` dans `Chronos.Services`, `ChronosPaths.SettingsFile`, et 3 suites de tests.

Décisions verrouillées appliquées :
- Persistance = **coin + device name comme vérité** ; X/Y purement indicatifs (résout Open Question 1).
- `RefreshIntervalSeconds` **persisté dans le schéma mais sans UI** (résout Open Question 3 ;
  l'application au démarrage est câblée en 06-03).
- Recalibrage hebdo ne s'applique **qu'au repli** ; le badge « estimée » reste (honnêteté Core Value).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-CONTEXT.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contrats existants à réutiliser tels quels (extraits du code). L'exécuteur n'a PAS besoin d'explorer. -->

Existant — src/Chronos/Services/ChronosPaths.cs (à ÉTENDRE, ne pas casser le ctor positionnel) :
```csharp
public sealed record ChronosPaths(string UsageFile, string ProjectsRoot)
{
    public static ChronosPaths Default() => new(
        UsageFile: Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData), "Chronos", "usage.json"),
        ProjectsRoot: Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), ".claude", "projects"));
}
```

Existant — src/Chronos/Models/WindowState.cs (immuable, neutre, consommé par WeeklyRecalibration) :
```csharp
public sealed record WindowState
{
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public long? EstimatedTokens { get; init; }
    public required SourceReliability Reliability { get; init; }
    public bool Exhausted => Utilization is >= 1.0;
}
public enum SourceReliability { Exact, Estimated, Unavailable }  // Chronos.Models
```

Existant — pattern d'écriture atomique (miroir C# du pont Node) à reproduire :
```csharp
var tmp = path + $".tmp-{Environment.ProcessId}";
File.WriteAllText(tmp, json);
File.Move(tmp, path, overwrite: true);  // remplacement atomique (même volume)
```

Existant — tests : xUnit + Xunit.StaFact déjà référencés dans tests/Chronos.Tests/Chronos.Tests.csproj.
`InternalsVisibleTo("Chronos.Tests")` déjà configuré. 68 tests verts — NE RIEN CASSER.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : CornerSnap — logique pure de placement aux coins (FEN-03)</name>
  <read_first>
    - src/Chronos/Interop/NativeMethods.cs (contexte : la Phase 6 posera en pixels physiques ; ici unités agnostiques)
    - 06-RESEARCH.md § Pattern 2 (code verbatim de RectD/CornerSnap) et § Anti-Patterns (ne PAS utiliser System.Windows.Rect/Point)
  </read_first>
  <behavior>
    - NearestCorner : fenêtre en haut-gauche → (workArea.X + margin, workArea.Y + margin) ; haut-droite →
      (workArea.Right - window.Width - margin, workArea.Y + margin) ; bas-gauche et bas-droite symétriques.
    - ClassifyCorner : renvoie l'OverlayCorner du quadrant du centre fenêtre (TopLeft/TopRight/BottomLeft/BottomRight).
    - CornerToTopLeft : pour un OverlayCorner IMPOSÉ, renvoie le top-left cible dans la workArea avec marge
      (TopLeft → (X+m, Y+m) ; TopRight → (Right-W-m, Y+m) ; BottomLeft → (X+m, Bottom-H-m) ; BottomRight → (Right-W-m, Bottom-H-m)).
    - La marge est respectée exactement.
  </behavior>
  <action>
    Créer le namespace NEUTRE `Chronos.Placement` (hors gate de pureté Services/Models, et de toute façon
    sans aucun type WPF — interdiction d'utiliser `System.Windows.Rect`/`Point`).

    1. `src/Chronos/Placement/RectD.cs` — `public readonly record struct RectD(double X, double Y, double Width, double Height)`
       avec `Right => X + Width`, `Bottom => Y + Height`, `CenterX => X + Width/2`, `CenterY => Y + Height/2`.
    2. `src/Chronos/Placement/OverlayCorner.cs` — `public enum OverlayCorner { TopLeft, TopRight, BottomLeft, BottomRight }`.
    3. `src/Chronos/Placement/CornerSnap.cs` — `public static class CornerSnap` avec TROIS fonctions pures :
       ```csharp
       public static (double X, double Y) NearestCorner(RectD window, RectD workArea, double margin)
       {
           bool left = window.CenterX < workArea.CenterX;
           bool top  = window.CenterY < workArea.CenterY;
           double x = left ? workArea.X + margin : workArea.Right - window.Width - margin;
           double y = top  ? workArea.Y + margin : workArea.Bottom - window.Height - margin;
           return (x, y);
       }
       public static OverlayCorner ClassifyCorner(RectD window, RectD workArea)
       {
           bool left = window.CenterX < workArea.CenterX;
           bool top  = window.CenterY < workArea.CenterY;
           return (left, top) switch
           {
               (true,  true)  => OverlayCorner.TopLeft,
               (false, true)  => OverlayCorner.TopRight,
               (true,  false) => OverlayCorner.BottomLeft,
               _              => OverlayCorner.BottomRight,
           };
       }
       // Restauration d'un coin PERSISTÉ (coin+device = vérité) — consommé par OverlayController (06-03).
       public static (double X, double Y) CornerToTopLeft(OverlayCorner corner, RectD window, RectD workArea, double margin)
       {
           bool left = corner is OverlayCorner.TopLeft or OverlayCorner.BottomLeft;
           bool top  = corner is OverlayCorner.TopLeft or OverlayCorner.TopRight;
           double x = left ? workArea.X + margin : workArea.Right - window.Width - margin;
           double y = top  ? workArea.Y + margin : workArea.Bottom - window.Height - margin;
           return (x, y);
       }
       ```
    Commentaires en français. Aucune dépendance externe.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~CornerSnapTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Chronos.Tests/CornerSnapTests.cs` couvre : NearestCorner sur 4 quadrants (coin attendu + marge),
      ClassifyCorner pour les 4 coins, CornerToTopLeft pour les 4 coins imposés.
    - Tous les tests CornerSnapTests passent ; aucun `using System.Windows` dans `Chronos.Placement`.
  </acceptance_criteria>
  <done>NearestCorner + ClassifyCorner + CornerToTopLeft purs, testés en [Fact] sans STA.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : ChronosSettings + SettingsService (persistance atomique/tolérante, FEN-07)</name>
  <read_first>
    - src/Chronos/Services/ChronosPaths.cs (record à étendre)
    - src/Chronos/Services/ClaudeUsageObjectProvider.cs (pattern de lecture JSON tolérante existant)
    - 06-RESEARCH.md § Pattern 6 (schéma ChronosSettings + écriture atomique + lecture tolérante)
    - src/Chronos/Placement/OverlayCorner.cs (créé Task 1)
  </read_first>
  <behavior>
    - Save(settings) puis Load() redonne des valeurs égales (round-trip) sur un répertoire temp injecté.
    - Load() sur fichier absent → défauts (Corner=TopRight, RefreshIntervalSeconds=60, Background=false).
    - Load() sur JSON corrompu → défauts, SANS exception.
    - Save() atomique : aucun .tmp résiduel après succès ; le dossier %APPDATA%/Chronos est créé s'il manque.
  </behavior>
  <action>
    1. Étendre `ChronosPaths` (garder le ctor positionnel `(UsageFile, ProjectsRoot)`) : propriété calculée
       `public string SettingsFile => Path.Combine(Path.GetDirectoryName(UsageFile)!, "settings.json");`.
       Les tests qui construisent `new ChronosPaths(usage, projects)` restent valides.
    2. `src/Chronos/Services/ChronosSettings.cs` — namespace `Chronos.Services`, record neutre (schéma verrouillé) :
       ```csharp
       public sealed record ChronosSettings
       {
           public OverlayCorner Corner { get; init; } = OverlayCorner.TopRight;
           public string? MonitorDeviceName { get; init; }        // \\.\DISPLAY1 (repli primaire si absent)
           public double? X { get; init; }                         // DIU indicatif (diagnostic, non fiable seul)
           public double? Y { get; init; }
           public bool Background { get; init; }
           public double RefreshIntervalSeconds { get; init; } = 60;   // persisté, appliqué au démarrage (06-03), pas d'UI
           public DateTimeOffset? WeeklyAnchor { get; init; }      // recalibrage ROB-03
       }
       ```
       (référence `Chronos.Placement.OverlayCorner` — même assembly, aucun type WPF).
    3. `src/Chronos/Services/SettingsService.cs` — `public sealed class SettingsService`, ctor `(ChronosPaths paths)` :
       - `JsonSerializerOptions` tolérant + `JsonStringEnumConverter` (OverlayCorner sérialisé en texte).
       - `public ChronosSettings Load()` : fichier absent → défauts ; try/catch `IOException`/`JsonException`/`ArgumentException` → `new ChronosSettings()`.
       - `public void Save(ChronosSettings settings)` : `Directory.CreateDirectory(dir)` ; écriture atomique temp + `File.Move(tmp, paths.SettingsFile, overwrite: true)`.
       Commentaires en français.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~SettingsServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Chronos.Tests/SettingsServiceTests.cs` (répertoire temp via `new ChronosPaths(tmpUsage, tmpProjects)`) :
      round-trip, fichier absent → défauts, JSON corrompu → défauts (pas d'exception), Save atomique (pas de .tmp résiduel), création du dossier.
    - `SettingsService`/`ChronosSettings` sans type WPF en signature (garde de pureté reste verte).
  </acceptance_criteria>
  <done>settings.json en round-trip atomique, lecture tolérante prouvée, ChronosPaths.SettingsFile disponible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : WeeklyRecalibration — recalibrage hebdo pur (ROB-03)</name>
  <read_first>
    - src/Chronos/Models/WindowState.cs, src/Chronos/Models/SourceReliability.cs
    - 06-RESEARCH.md § Pattern 7 (code verbatim) et § Pitfall 7 (le recalibrage ne doit pas « mentir »)
  </read_first>
  <behavior>
    - Exact + ResetsAt non-null → renvoyée INCHANGÉE (les chiffres exacts priment).
    - Repli (Estimated / ResetsAt null) + WeeklyAnchor null → inchangée.
    - Repli + WeeklyAnchor défini → ResetsAt = premier reset STRICTEMENT futur (ancre + n×7j) ; Reliability RESTE Estimated.
    - NextReset(anchor, now) : cycles = ceil((now-anchor)/7j), min 1.
  </behavior>
  <action>
    `src/Chronos/Services/WeeklyRecalibration.cs` — namespace `Chronos.Services`, classe statique PURE
    (aucun type WPF, aucun I/O ; consomme/produit `Chronos.Models.WindowState`) :
    ```csharp
    public static class WeeklyRecalibration
    {
        static readonly TimeSpan Week = TimeSpan.FromDays(7);
        public static WindowState Apply(WindowState weekly, DateTimeOffset? anchor, DateTimeOffset now)
        {
            if (weekly.Reliability == SourceReliability.Exact && weekly.ResetsAt is not null) return weekly;
            if (anchor is null) return weekly;
            var next = NextReset(anchor.Value, now);
            return weekly with { ResetsAt = next };   // reste Estimated → badge « estimée » conservé
        }
        static DateTimeOffset NextReset(DateTimeOffset anchor, DateTimeOffset now)
        {
            var cycles = Math.Ceiling((now - anchor) / Week);
            if (cycles < 1) cycles = 1;
            return anchor + cycles * Week;
        }
    }
    ```
    Commentaires en français expliquant la garde Exact (Pitfall 7).
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~WeeklyRecalibrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Chronos.Tests/WeeklyRecalibrationTests.cs` : (a) Exact+ResetsAt → inchangé ; (b) repli+ancre → ResetsAt > now et = ancre + n×7j ;
      (c) résultat recalibré Reliability == Estimated ; (d) repli sans ancre → inchangé.
    - Aucun type WPF, aucun I/O.
  </acceptance_criteria>
  <done>Règle de recalibrage prouvée : garde Exact respectée, ResetsAt futur synthétisé, badge « estimée » conservé.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit.
- `dotnet test Chronos.sln -c Debug` : 68 tests existants + nouveaux (CornerSnap, SettingsService, WeeklyRecalibration) verts.
- `ServicesLayerPurityTests` reste verte (aucun type WPF dans Services/Models).
- Aucun paquet NuGet ajouté.
</verification>

<success_criteria>
- Placement pur (coin le plus proche + coin imposé + classification) testé (FEN-03).
- settings.json (coin+device = vérité, RefreshIntervalSeconds, WeeklyAnchor) persisté atomiquement, relu tolérant, sans WPF (FEN-07).
- Recalibrage hebdo pur limité au repli, badge « estimée » conservé (ROB-03).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-comportements-overlay-placement-interaction/06-01-SUMMARY.md`.
</output>
