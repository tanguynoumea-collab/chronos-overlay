---
phase: 06-comportements-overlay-placement-interaction
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - src/Chronos/ViewModels/MainViewModel.cs
  - src/Chronos/Services/IRecalibrationPrompt.cs
  - src/Chronos/ViewModels/RecalibrationViewModel.cs
  - src/Chronos/Views/RecalibrationDialog.xaml
  - src/Chronos/Views/RecalibrationDialog.xaml.cs
  - src/Chronos/Views/RecalibrationPrompt.cs
  - src/Chronos/Views/MainWindow.xaml
  - src/Chronos/App.xaml.cs
  - tests/Chronos.Tests/MainViewModelTests.cs
  - tests/Chronos.Tests/CompositionRootTests.cs
  - tests/Chronos.Tests/Fakes/FakeWindowController.cs
  - tests/Chronos.Tests/Fakes/FakeAutostartService.cs
  - tests/Chronos.Tests/Fakes/FakeRecalibrationPrompt.cs
autonomous: false
requirements: [FEN-05, FEN-06, FEN-07, ROB-03, DEP-02]

must_haves:
  truths:
    - "Un clic droit ouvre un menu contextuel à 4 items : Arrière-plan (toggle), Recalibrer le reset hebdo…, Lancer au démarrage (toggle), Quitter"
    - "Quitter ferme l'application (seul point de sortie d'une fenêtre sans barre de titre ni barre des tâches)"
    - "ToggleBackground bascule l'overlay via IWindowController et persiste l'état ; le retour premier plan réaffirme le topmost"
    - "Lancer au démarrage crée/supprime le raccourci .lnk et l'item reflète l'état réel (IsEnabled)"
    - "Recalibrer applique une ancre hebdo au repli uniquement ; l'arc hebdo se met à jour en restant badgé « estimée » ; l'ancre est persistée"
  artifacts:
    - path: "src/Chronos/ViewModels/MainViewModel.cs"
      provides: "Commandes de menu [RelayCommand] + application du recalibrage dans ApplySnapshot"
      contains: "RelayCommand"
    - path: "src/Chronos/Views/MainWindow.xaml"
      provides: "ContextMenu à 4 items bindés au VM"
      contains: "ContextMenu"
    - path: "src/Chronos/Views/RecalibrationDialog.xaml"
      provides: "Dialogue minimal de recalibrage (DatePicker + « caler sur maintenant »)"
      contains: "DatePicker"
  key_links:
    - from: "MainWindow.xaml ContextMenu"
      to: "MainViewModel [RelayCommand]"
      via: "MenuItem.Command bindés (DataContext hérité du VM)"
      pattern: "Command=\"\\{Binding"
    - from: "MainViewModel.ApplySnapshot"
      to: "WeeklyRecalibration.Apply"
      via: "recalibrage du repli hebdo avant SevenDay.Apply, badge estimée conservé"
      pattern: "WeeklyRecalibration.Apply"
    - from: "MainViewModel.ToggleAutostartCommand"
      to: "IAutostartService"
      via: "Enable/Disable + IsEnabled reflété dans l'item de menu"
      pattern: "IAutostartService"
---

<objective>
Livrer le SEUL point d'accès et de sortie de l'overlay : le menu contextuel clic droit (4 items),
les commandes MVVM associées, le dialogue minimal de recalibrage hebdo, et le câblage final.
Puis vérifier l'ensemble de la Phase 6 par un checkpoint humain (drag/snap/multi-écrans/menu/
restauration/autostart) — aucune de ces interactions n'est automatisable sans écran réel.

Purpose : c'est la couche d'interaction qui relie les capacités des plans 06-01→06-03 à
l'utilisateur (FEN-06 table stake : sans « Quitter », il faut tuer le process). Le recalibrage
(ROB-03) est appliqué dans le pipeline temps réel sans jamais présenter une estimation comme exacte.
Output : `MainViewModel` avec 4 commandes, `ContextMenu` XAML, dialogue de recalibrage, DI finalisé, checkpoint UAT.

Décisions verrouillées appliquées :
- Menu clic droit = SEUL point d'accès ; items « Arrière-plan » / « Recalibrer le reset hebdo… » /
  « Lancer au démarrage » / « Quitter » ; `ContextMenu` WPF + `[RelayCommand]` (Pattern 4).
- Dialogue de recalibrage MINIMAL : `DatePicker` (pointer une date de reset connue) **+ bouton
  « Caler sur maintenant »** (résout Open Question 2 — les deux voies, minimal).
- Recalibrage appliqué au repli SEULEMENT ; badge « estimée » conservé (Pitfall 7, Core Value).

Activer les skills `frontend-design` + `windows-wpf` sur les tâches UI (menu, dialogue).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-CONTEXT.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-RESEARCH.md
@CLAUDE.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-01-SUMMARY.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-02-SUMMARY.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-03-SUMMARY.md

<interfaces>
<!-- Contrats à consommer (06-01/02/03) + existant à modifier. -->

De 06-01 — `WeeklyRecalibration.Apply(WindowState weekly, DateTimeOffset? anchor, DateTimeOffset now)` (Chronos.Services) ;
`SettingsService.Load()/Save(ChronosSettings)` ; `ChronosSettings { Corner, MonitorDeviceName, X, Y, Background, RefreshIntervalSeconds, WeeklyAnchor }`.
De 06-02 — `IAutostartService { bool IsEnabled(); void Enable(); void Disable(); }`.
De 06-03 — `IWindowController { void SendToBackground(); void BringToForeground(); void Quit(); }`.

Existant — src/Chronos/ViewModels/MainViewModel.cs (À MODIFIER) :
```csharp
public sealed partial class MainViewModel : ObservableObject
{
    public WindowGaugeViewModel FiveHour { get; }
    public WindowGaugeViewModel SevenDay { get; }
    [ObservableProperty] private bool _dataUnavailable;
    public MainViewModel(RefreshOrchestrator orchestrator, IUiDispatcher ui, IClock clock) { ... }
    internal void ApplySnapshot(UsageSnapshot snap)  // FiveHour.Apply / SevenDay.Apply / Interpolate
    internal void Interpolate(DateTimeOffset now)
    public void StartClock()
}
```

Existant — src/Chronos/Views/MainWindow.xaml : `<Grid>` racine (DataContext = VM via ctor). Le fond Ellipse capte le hit-test.
Existant — tests/Chronos.Tests/MainViewModelTests.cs : helper `NewVm(...)` construit `new MainViewModel(orch, ui, clock)` → À METTRE À JOUR.
Existant — tests/Chronos.Tests/CompositionRootTests.cs : miroir du DI → À METTRE À JOUR.
Existant — tests/Chronos.Tests/Fakes/ : FakeUiDispatcher, FakeClock, FakeUsageProvider (pattern à suivre pour les nouveaux fakes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : MainViewModel — commandes de menu + recalibrage + ContextMenu (FEN-05/06, DEP-02, ROB-03)</name>
  <files>src/Chronos/ViewModels/MainViewModel.cs, src/Chronos/Services/IRecalibrationPrompt.cs, src/Chronos/Views/MainWindow.xaml, src/Chronos/App.xaml.cs, tests/Chronos.Tests/MainViewModelTests.cs, tests/Chronos.Tests/CompositionRootTests.cs, tests/Chronos.Tests/Fakes/FakeWindowController.cs, tests/Chronos.Tests/Fakes/FakeAutostartService.cs, tests/Chronos.Tests/Fakes/FakeRecalibrationPrompt.cs</files>
  <read_first>
    - src/Chronos/ViewModels/MainViewModel.cs (ApplySnapshot, ctor)
    - src/Chronos/ViewModels/WindowGaugeViewModel.cs (Apply/IsEstimated — le badge « estimée »)
    - src/Chronos/Views/MainWindow.xaml (Grid racine)
    - 06-RESEARCH.md § Pattern 4 (ContextMenu + RelayCommand, gotcha DataContext), § Pattern 7 (recalibrage)
    - tests/Chronos.Tests/MainViewModelTests.cs + Fakes/ (pattern de fake)
  </read_first>
  <behavior>
    - ToggleBackgroundCommand : bascule IsBackground ; appelle IWindowController.SendToBackground()/BringToForeground() en conséquence.
    - ToggleAutostartCommand : appelle IAutostartService.Enable()/Disable() puis IsAutostart = IAutostartService.IsEnabled().
    - RecalibrateCommand : IRecalibrationPrompt.Ask(current) ; si non-null → persiste WeeklyAnchor (SettingsService) et ré-applique
      au dernier snapshot → SevenDay.ResetsAt change ET SevenDay reste IsEstimated (repli) ; si null → aucun changement.
    - QuitCommand : appelle IWindowController.Quit().
    - ApplySnapshot : applique WeeklyRecalibration.Apply(snap.SevenDay, WeeklyAnchor, now) AVANT SevenDay.Apply.
    - À l'initialisation : IsBackground = settings.Background ; IsAutostart = IAutostartService.IsEnabled().
  </behavior>
  <action>
    1. `src/Chronos/Services/IRecalibrationPrompt.cs` — interface NEUTRE (impl WPF en Task 2) :
       ```csharp
       public interface IRecalibrationPrompt
       {
           /// <summary>Demande une date d'ancrage hebdo. Retourne null si l'utilisateur annule.</summary>
           DateTimeOffset? Ask(DateTimeOffset? current);
       }
       ```
    2. `MainViewModel` — étendre le ctor (ne pas casser l'ordre d'abonnement Phase 4) :
       `MainViewModel(RefreshOrchestrator orchestrator, IUiDispatcher ui, IClock clock, IWindowController controller, IAutostartService autostart, IRecalibrationPrompt prompt, SettingsService settings)`.
       - Charger `_settings = settings.Load();` ; `[ObservableProperty] bool _isBackground;` `[ObservableProperty] bool _isAutostart;`.
         Initialiser `IsBackground = _settings.Background; IsAutostart = _autostart.IsEnabled();`.
       - Mémoriser le dernier snapshot : `private UsageSnapshot? _last;` (renseigné dans ApplySnapshot).
       - Dans `ApplySnapshot(snap)` : `var weekly = WeeklyRecalibration.Apply(snap.SevenDay, _settings.WeeklyAnchor, _clock.UtcNow);`
         puis `SevenDay.Apply(weekly);` (au lieu de `snap.SevenDay`). Stocker `_last = snap;`. Le reste inchangé.
       - Commandes :
         ```csharp
         [RelayCommand] private void ToggleBackground()
         {
             IsBackground = !IsBackground;
             if (IsBackground) _controller.SendToBackground(); else _controller.BringToForeground();
         }
         [RelayCommand] private void ToggleAutostart()
         {
             if (_autostart.IsEnabled()) _autostart.Disable(); else _autostart.Enable();
             IsAutostart = _autostart.IsEnabled();
         }
         [RelayCommand] private void Recalibrate()
         {
             var anchor = _prompt.Ask(_settings.WeeklyAnchor);
             if (anchor is null) return;
             _settings = _settings with { WeeklyAnchor = anchor };
             _settingsService.Save(_settings);
             if (_last is { } s) ApplySnapshot(s);   // ré-applique → arc hebdo recalé, reste « estimée »
         }
         [RelayCommand] private void Quit() => _controller.Quit();
         ```
       Commentaires en français.
    3. `MainWindow.xaml` — ajouter le `ContextMenu` sur la `<Grid>` racine (Pattern 4, DataContext hérité du VM) :
       ```xml
       <Grid.ContextMenu>
         <ContextMenu>
           <MenuItem Header="Arrière-plan" IsCheckable="True"
                     IsChecked="{Binding IsBackground}" Command="{Binding ToggleBackgroundCommand}"/>
           <MenuItem Header="Recalibrer le reset hebdo…" Command="{Binding RecalibrateCommand}"/>
           <MenuItem Header="Lancer au démarrage" IsCheckable="True"
                     IsChecked="{Binding IsAutostart}" Command="{Binding ToggleAutostartCommand}"/>
           <Separator/>
           <MenuItem Header="Quitter" Command="{Binding QuitCommand}"/>
         </ContextMenu>
       </Grid.ContextMenu>
       ```
       Ne PAS retirer les badges « estimée » existants du cadran.
    4. App.xaml.cs — enregistrer `services.AddSingleton<IAutostartService>(_ => new AutostartService());` (service de 06-02).
       (IRecalibrationPrompt sera enregistré en Task 2.)
    5. Mettre à jour les tests pour le nouveau ctor :
       - Nouveaux fakes : `Fakes/FakeWindowController.cs` (compte SendToBackground/BringToForeground/Quit),
         `Fakes/FakeAutostartService.cs` (état bool + compteurs Enable/Disable),
         `Fakes/FakeRecalibrationPrompt.cs` (retourne une DateTimeOffset? programmée).
       - `MainViewModelTests` : mettre à jour `NewVm(...)` et la construction inline pour injecter les nouveaux fakes +
         `new SettingsService(TempPaths())`. AJOUTER des tests : ToggleBackground appelle le controller + flip IsBackground ;
         ToggleAutostart appelle Enable/Disable + reflète IsEnabled ; Recalibrate (prompt→date, snapshot repli SevenDay) →
         SevenDay.CountdownText change ET IsEstimated reste vrai ; Quit appelle controller.Quit ; les 4 tests RAF existants restent verts.
       - `CompositionRootTests` : enregistrer IWindowController→OverlayController (déjà via 06-03), IAutostartService→AutostartService,
         IRecalibrationPrompt→un fake (ou impl), SettingsService, ChronosSettings pour que MainViewModel se résolve.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~MainViewModelTests|FullyQualifiedName~CompositionRootTests"</automated>
  </verify>
  <acceptance_criteria>
    - MainViewModelTests (mis à jour) verts, dont les 4 nouvelles commandes et la ré-application du recalibrage (SevenDay recalé, IsEstimated conservé).
    - CompositionRootTests vert avec le nouveau ctor de MainViewModel.
    - `ContextMenu` à 4 items présent dans MainWindow.xaml, bindé aux 4 RelayCommand ; badges « estimée » intacts.
    - `dotnet build Chronos.sln -c Debug` réussit.
  </acceptance_criteria>
  <done>Menu + commandes MVVM + recalibrage dans le pipeline, prouvés par tests (hors rendu du menu = UAT).</done>
</task>

<task type="auto">
  <name>Task 2 : Dialogue de recalibrage + prompt WPF + DI final (ROB-03, FEN-07)</name>
  <files>src/Chronos/ViewModels/RecalibrationViewModel.cs, src/Chronos/Views/RecalibrationDialog.xaml, src/Chronos/Views/RecalibrationDialog.xaml.cs, src/Chronos/Views/RecalibrationPrompt.cs, src/Chronos/App.xaml.cs</files>
  <read_first>
    - src/Chronos/Services/IRecalibrationPrompt.cs (Task 1)
    - src/Chronos/App.xaml.cs (ConfigureServices + OnStartup — mis à jour en 06-03)
    - src/Chronos/Resources/DesignTokens.xaml (tokens de style à réutiliser pour rester cohérent)
    - 06-RESEARCH.md § Pattern 7 (dialogue minimal : DatePicker + « caler sur maintenant », Owner=MainWindow, CenterOwner)
  </read_first>
  <action>
    Skills UI actifs (`frontend-design` + `windows-wpf`). Dialogue MINIMAL, sombre, cohérent avec l'overlay.
    1. `src/Chronos/ViewModels/RecalibrationViewModel.cs` — `[ObservableProperty] DateTimeOffset _selectedDate;`
       ctor `(DateTimeOffset current)` initialise SelectedDate. Commandes `[RelayCommand]` :
       `Now()` (SelectedDate = DateTimeOffset.Now puis demande fermeture-acceptée), `Validate()` (fermeture-acceptée),
       `Cancel()` (fermeture-refusée). Exposer un événement `event Action<bool>? CloseRequested;` (true=accepté).
    2. `src/Chronos/Views/RecalibrationDialog.xaml(.cs)` — petite `Window` modale (WindowStartupLocation=CenterOwner,
       ShowInTaskbar=False, taille compacte) : un `DatePicker` bindé à SelectedDate, un bouton « Caler sur maintenant »
       (NowCommand), « Valider » (ValidateCommand), « Annuler » (CancelCommand). Code-behind minimal : s'abonner à
       `CloseRequested` → `DialogResult = accepté; Close();`.
    3. `src/Chronos/Views/RecalibrationPrompt.cs` — `public sealed class RecalibrationPrompt : IRecalibrationPrompt`
       (namespace `Chronos.Views` → HORS du scan de pureté Services/Models, aucune entrée d'allow-list requise) :
       ```csharp
       public DateTimeOffset? Ask(DateTimeOffset? current)
       {
           var vm = new RecalibrationViewModel(current ?? DateTimeOffset.Now);
           var dlg = new RecalibrationDialog { DataContext = vm, Owner = System.Windows.Application.Current?.MainWindow };
           return dlg.ShowDialog() == true ? vm.SelectedDate : null;
       }
       ```
    4. App.xaml.cs :
       - `services.AddSingleton<IRecalibrationPrompt, RecalibrationPrompt>();`
       - Dans OnStartup, définir `MainWindow = window;` (Application.MainWindow) AVANT `window.Show()` pour que le dialogue
         se centre sur l'overlay (Owner). Ne pas perturber l'ordre d'abonnement/restauration existant.
    Commentaires en français.
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build Chronos.sln -c Debug` réussit ; `dotnet test Chronos.sln -c Debug` reste vert (aucune régression).
    - `IRecalibrationPrompt` résolu en DI vers `RecalibrationPrompt` ; le dialogue expose DatePicker + « Caler sur maintenant » + Valider/Annuler.
    - `RecalibrationPrompt` en `Chronos.Views` (la garde de pureté reste verte sans nouvelle entrée d'allow-list).
  </acceptance_criteria>
  <done>Dialogue minimal de recalibrage fonctionnel et câblé en DI ; l'ancre saisie alimente le pipeline (via Task 1).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : Checkpoint UAT — placement, menu, persistance, autostart (FEN-02/03/04/05/06/07, DEP-02)</name>
  <files>(aucun — vérification humaine ; lance l'application déjà construite)</files>
  <action>
    Tâche de VÉRIFICATION HUMAINE (aucune écriture de code). L'exécuteur construit et lance l'overlay, puis
    présente à l'utilisateur les étapes de <how-to-verify> et attend le signal de reprise. Toute anomalie
    signalée est consignée comme gap pour un plan de clôture (--gaps) ; ne pas « corriger en douce » sans replanifier.
  </action>
  <what-built>
    Comportements d'overlay complets : glisser + accroche au coin le plus proche (physique, multi-écrans),
    menu contextuel clic droit à 4 items, toggle arrière-plan, persistance %APPDATA%/Chronos/settings.json
    restaurée au lancement, dialogue de recalibrage hebdo, et autostart shell:startup.
  </what-built>
  <how-to-verify>
    Lancer l'overlay : `dotnet run --project src/Chronos/Chronos.csproj -c Debug`.
    1. DRAG + SNAP (FEN-02/03) : glisser l'overlay (clic gauche maintenu) puis relâcher → il s'accroche au coin
       d'écran LE PLUS PROCHE, sans chevaucher la barre des tâches (marge ~12 px). Tester les 4 coins.
    2. MULTI-ÉCRANS (FEN-04) : si un 2e moniteur est disponible (idéalement DPI différent), glisser l'overlay dessus
       et relâcher → snap correct sur le coin du BON moniteur, sans décalage ni flou. (Si pas de 2e écran : noter « non testé — 1 seul moniteur ».)
    3. MENU (FEN-06) : clic DROIT sur le cadran → menu à 4 items : « Arrière-plan », « Recalibrer le reset hebdo… »,
       « Lancer au démarrage », « Quitter ». Vérifier que « Quitter » ferme bien l'application (seul point de sortie).
    4. ARRIÈRE-PLAN (FEN-05) : cliquer « Arrière-plan » → l'overlay passe derrière les autres fenêtres et n'est plus toujours au-dessus ;
       re-cocher → il revient au premier plan (topmost réaffirmé), sans clignotement ni vol de focus.
    5. PERSISTANCE (FEN-07) : déplacer/accrocher à un coin, activer « Arrière-plan », fermer via « Quitter », relancer →
       l'overlay réapparaît au même coin/écran et dans le même mode. Vérifier %APPDATA%/Chronos/settings.json (Corner, MonitorDeviceName, Background).
    6. RECALIBRAGE (ROB-03) : si la fenêtre hebdo est en repli (badge « estimée »), « Recalibrer le reset hebdo… » →
       dialogue → choisir une date (ou « Caler sur maintenant ») → l'arc/compte à rebours hebdo se recale MAIS garde le badge « estimée ».
       (Si la source hebdo est Exacte, le recalibrage ne doit RIEN changer.)
    7. AUTOSTART (DEP-02) : cocher « Lancer au démarrage » → vérifier la présence de Chronos.lnk dans
       shell:startup (%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup) ; décocher → le raccourci disparaît. (Reboot optionnel.)
  </how-to-verify>
  <verify>Vérification manuelle par l'utilisateur (drag/snap/multi-écrans/menu/persistance/recalibrage/autostart) — pas d'automatisation possible sans écran réel.</verify>
  <resume-signal>Répondre « approuvé » si tout se comporte comme décrit, ou décrire précisément les écarts (numéro d'étape).</resume-signal>
  <done>Les 7 comportements sont approuvés par l'utilisateur, ou les écarts sont consignés pour clôture (--gaps).</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit.
- `dotnet test Chronos.sln -c Debug` : 68 existants + nouveaux tests VM/composition verts ; garde de pureté verte.
- Checkpoint humain approuvé pour FEN-02/03/04/05/06 et le round-trip de persistance (FEN-07) + autostart (DEP-02).
</verification>

<success_criteria>
- Menu clic droit à 4 items, SEUL point d'accès/sortie, « Quitter » ferme l'app (FEN-06).
- Toggle arrière-plan opérationnel de bout en bout (FEN-05).
- Position/coin/mode/ancre persistés et restaurés au lancement (FEN-07).
- Recalibrage hebdo best-effort appliqué au repli seulement, badge « estimée » conservé (ROB-03).
- Autostart shell:startup activable/désactivable depuis le menu (DEP-02).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-comportements-overlay-placement-interaction/06-04-SUMMARY.md`.
</output>
