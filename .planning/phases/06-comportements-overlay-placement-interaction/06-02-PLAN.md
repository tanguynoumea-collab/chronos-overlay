---
phase: 06-comportements-overlay-placement-interaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Interop/NativeMethods.cs
  - src/Chronos/Services/TopmostGuard.cs
  - src/Chronos/Services/IAutostartService.cs
  - src/Chronos/Services/AutostartService.cs
  - tests/Chronos.Tests/TopmostGuardTests.cs
  - tests/Chronos.Tests/AutostartServiceTests.cs
autonomous: true
requirements: [FEN-05, DEP-02]

must_haves:
  truths:
    - "TopmostGuard.Suspend() arrête la réaffirmation périodique du topmost ; Resume() la reprend et réaffirme immédiatement HWND_TOPMOST"
    - "NativeMethods expose HWND_BOTTOM, GetWindowRect, MonitorFromWindow, GetMonitorInfo (MONITORINFOEX+rcWork+szDevice), GetDpiForMonitor et EnumDisplayMonitors"
    - "AutostartService.Enable() crée un .lnk dans le dossier startup injecté pointant vers Environment.ProcessPath ; Disable() le supprime ; IsEnabled() reflète l'existence du .lnk"
  artifacts:
    - path: "src/Chronos/Interop/NativeMethods.cs"
      provides: "P/Invoke placement/arrière-plan/DPI multi-écrans"
      contains: "MonitorFromWindow"
    - path: "src/Chronos/Services/TopmostGuard.cs"
      provides: "Suspend/Resume pour le mode arrière-plan"
      exports: ["Suspend", "Resume"]
    - path: "src/Chronos/Services/AutostartService.cs"
      provides: ".lnk shell:startup sans dépendance native, dossier injectable"
      contains: "WScript.Shell"
  key_links:
    - from: "TopmostGuard.Suspend"
      to: "DispatcherTimer.Stop"
      via: "arrêt de la réaffirmation en arrière-plan (pas de re-force toutes les 2 s)"
      pattern: "_timer.Stop"
    - from: "AutostartService.Enable"
      to: "Environment.ProcessPath"
      via: "cible du raccourci = exe courant (single-file-safe, PAS Assembly.Location)"
      pattern: "Environment.ProcessPath"
---

<objective>
Étendre l'interop Win32 et les services de plomberie nécessaires au placement physique
multi-écrans (06-03) et au mode arrière-plan (06-04) : P/Invoke moniteur/DPI/fenêtre +
HWND_BOTTOM, extension de TopmostGuard avec Suspend/Resume, et service d'autostart .lnk
sans dépendance native. Aucun de ces éléments ne dépend du plan 06-01 → exécutable en parallèle.

Purpose : isoler la mécanique Win32/COM testable AVANT de câbler l'adaptateur de fenêtre
(06-03) et le menu (06-04). Suspend/Resume (FEN-05) et Enable/Disable/IsEnabled (DEP-02)
sont couverts par des tests ; le P/Invoke moniteur est consommé par OverlayController en 06-03.
Output : `NativeMethods` étendu, `TopmostGuard` avec Suspend/Resume, `IAutostartService`/`AutostartService`.

Note de cadrage — clic-traversant (WS_EX_TRANSPARENT) : explicitement **différé en V2 (V2-04)**,
AUCUNE implémentation ici. Il est incompatible avec le drag (FEN-02) et exigerait tray/hotkey
de sortie (blocker STATE.md tranché : v1 = drag + menu, pas de clic-traversant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-CONTEXT.md
@.planning/phases/06-comportements-overlay-placement-interaction/06-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contrats existants (extraits du code). NE PAS casser le test ROB-04 existant. -->

Existant — src/Chronos/Interop/NativeMethods.cs (à ÉTENDRE ; internal static) :
```csharp
internal static class NativeMethods
{
    public static readonly IntPtr HWND_TOPMOST = new(-1);
    public const uint SWP_NOSIZE     = 0x0001;
    public const uint SWP_NOMOVE     = 0x0002;
    public const uint SWP_NOACTIVATE = 0x0010;
    [DllImport("user32.dll", SetLastError = true)]
    [return: MarshalAs(UnmanagedType.Bool)]
    public static extern bool SetWindowPos(IntPtr hWnd, IntPtr hWndInsertAfter, int X, int Y, int cx, int cy, uint uFlags);
}
```

Existant — src/Chronos/Services/TopmostGuard.cs (à ÉTENDRE ; dans l'allow-list de pureté) :
```csharp
public sealed class TopmostGuard : IDisposable
{
    public delegate bool SetWindowPosFn(IntPtr hWnd, IntPtr after, int x, int y, int cx, int cy, uint flags);
    private readonly SetWindowPosFn _setWindowPos;
    private readonly DispatcherTimer _timer;   // 2 s
    private IntPtr _hwnd;
    public TopmostGuard(SetWindowPosFn? setWindowPos = null) { ... _timer.Tick += (_, _) => Reassert(); }
    public void Attach(Window window) { _hwnd = new WindowInteropHelper(window).EnsureHandle(); Reassert(); _timer.Start(); }
    public void Reassert() { /* SetWindowPos(HWND_TOPMOST, SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE) */ }
    public void Dispose() => _timer.Stop();
}
```

Existant — tests/Chronos.Tests/TopmostGuardTests.cs (à ÉTENDRE) : `[WpfFact]` avec délégué SetWindowPosFn capturant les args.
`InternalsVisibleTo("Chronos.Tests")` déjà configuré → NativeMethods internal accessible aux tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Étendre NativeMethods (moniteur, fenêtre, arrière-plan, DPI) — fondation FEN-04/FEN-05</name>
  <read_first>
    - src/Chronos/Interop/NativeMethods.cs (structure existante, flags SWP_*)
    - 06-RESEARCH.md § Code Examples « Étendre NativeMethods » (signatures verbatim) et § Pattern 3 (chaîne d'usage)
  </read_first>
  <action>
    Ajouter dans `NativeMethods` (internal static, sans casser l'existant) :
    ```csharp
    public static readonly IntPtr HWND_BOTTOM = new(1);       // envoyer au fond (arrière-plan)
    public const uint MONITOR_DEFAULTTONEAREST = 2;
    public const uint MONITORINFOF_PRIMARY = 0x1;             // repli écran primaire (FEN-04)

    [StructLayout(LayoutKind.Sequential)]
    public struct RECT { public int Left, Top, Right, Bottom; }

    [StructLayout(LayoutKind.Sequential, CharSet = CharSet.Unicode)]
    public struct MONITORINFOEX
    {
        public int cbSize;
        public RECT rcMonitor;
        public RECT rcWork;
        public uint dwFlags;
        [MarshalAs(UnmanagedType.ByValTStr, SizeConst = 32)] public string szDevice;
    }

    // rcWork/szDevice du moniteur courant. IMPORTANT : renseigner mi.cbSize = Marshal.SizeOf<MONITORINFOEX>()
    // AVANT l'appel, sinon échec silencieux.
    [DllImport("user32.dll")]
    public static extern IntPtr MonitorFromWindow(IntPtr hwnd, uint dwFlags);
    [DllImport("user32.dll", CharSet = CharSet.Unicode)]
    public static extern bool GetMonitorInfo(IntPtr hMonitor, ref MONITORINFOEX lpmi);

    // Rectangle PHYSIQUE de la fenêtre (centre fenêtre pour le calcul de coin — contourne Window.Left/Top cassés).
    [DllImport("user32.dll")]
    public static extern bool GetWindowRect(IntPtr hwnd, out RECT lpRect);

    // DPI d'un moniteur ciblé (restauration vers un moniteur précis). Win 8.1+. dpiType MDT_EFFECTIVE_DPI = 0.
    [DllImport("Shcore.dll")]
    public static extern int GetDpiForMonitor(IntPtr hmonitor, int dpiType, out uint dpiX, out uint dpiY);

    // Énumération pour retrouver un moniteur par device name / repli primaire (FEN-04).
    public delegate bool MonitorEnumProc(IntPtr hMonitor, IntPtr hdc, ref RECT lprc, IntPtr data);
    [DllImport("user32.dll")]
    public static extern bool EnumDisplayMonitors(IntPtr hdc, IntPtr clip, MonitorEnumProc callback, IntPtr data);
    ```
    Commentaires en français. Ne PAS ajouter `<UseWindowsForms>` (Anti-Pattern RESEARCH).
  </action>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Le build réussit ; `HWND_BOTTOM`, `GetWindowRect`, `MonitorFromWindow`, `GetMonitorInfo`, `MONITORINFOEX`
      (avec `rcWork`/`szDevice`), `GetDpiForMonitor`, `EnumDisplayMonitors`, `MONITORINFOF_PRIMARY` sont présents et accessibles à Chronos.Tests.
    - Aucune régression : test ROB-04 existant toujours vert.
  </acceptance_criteria>
  <done>Interop moniteur/DPI/fenêtre/arrière-plan disponible pour OverlayController (06-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : TopmostGuard.Suspend/Resume (FEN-05)</name>
  <read_first>
    - src/Chronos/Services/TopmostGuard.cs (timer 2 s, Reassert existant)
    - tests/Chronos.Tests/TopmostGuardTests.cs (pattern du délégué capturant, à étendre)
    - 06-RESEARCH.md § Code Examples « Toggle arrière-plan + Suspend/Resume »
  </read_first>
  <behavior>
    - Après Suspend(), aucun Reassert() n'est plus déclenché par le timer (réaffirmation stoppée).
    - Resume() redémarre le timer ET réaffirme immédiatement HWND_TOPMOST.
    - Le test ROB-04 existant reste vert (Reassert utilise toujours HWND_TOPMOST + SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE).
  </behavior>
  <action>
    Ajouter à `TopmostGuard` (ne pas changer sa signature de ctor ; reste dans l'allow-list de pureté) :
    ```csharp
    /// <summary>Suspend la réaffirmation topmost (mode arrière-plan) — sinon re-force le topmost toutes les 2 s.</summary>
    public void Suspend() => _timer.Stop();
    /// <summary>Reprend la réaffirmation et repose HWND_TOPMOST immédiatement (retour premier plan).</summary>
    public void Resume() { _timer.Start(); Reassert(); }
    ```
    Ne PAS utiliser le toggle `Topmost=false;Topmost=true` (Anti-Pattern : scintillement). Commentaires en français.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~TopmostGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - `TopmostGuardTests` étendu : (a) après `Suspend()`, le compteur de réaffirmations reste figé (aucun tick réaffirmé) ;
      (b) `Resume()` déclenche un `Reassert` immédiat avec `HWND_TOPMOST` (délégué capturé). Tests `[WpfFact]` (STA).
    - Le test ROB-04 d'origine reste vert.
  </acceptance_criteria>
  <done>Suspend arrête la réaffirmation, Resume la reprend + réaffirme — prouvé par délégué capturé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : AutostartService — .lnk shell:startup sans dépendance native (DEP-02)</name>
  <read_first>
    - src/Chronos/Services/ChronosPaths.cs (pattern d'injection de chemin pour testabilité)
    - 06-RESEARCH.md § Code Examples « Autostart .lnk » + § Validation Architecture note testabilité DEP-02
    - CLAUDE.md (interdiction Assembly.Location ; chemins profil utilisateur ; aucun admin)
  </read_first>
  <behavior>
    - IsEnabled() → true si le .lnk existe dans le dossier startup injecté, false sinon.
    - Disable() → supprime le .lnk s'il existe ; idempotent (aucune erreur si absent).
    - Enable() → crée le .lnk (COM WScript.Shell) ciblant Environment.ProcessPath, puis IsEnabled() == true.
    - Dossier startup INJECTÉ (ctor) → les tests utilisent un répertoire temp, jamais le vrai Startup.
  </behavior>
  <action>
    1. `src/Chronos/Services/IAutostartService.cs` :
       ```csharp
       public interface IAutostartService { bool IsEnabled(); void Enable(); void Disable(); }
       ```
    2. `src/Chronos/Services/AutostartService.cs` — impl NEUTRE (aucun type WPF ; COM late-bound `dynamic`) :
       ```csharp
       public sealed class AutostartService : IAutostartService
       {
           private readonly string _startupFolder;
           private readonly string _linkName;
           public AutostartService(string? startupFolder = null, string linkName = "Chronos.lnk")
           {
               _startupFolder = startupFolder ?? Environment.GetFolderPath(Environment.SpecialFolder.Startup);
               _linkName = linkName;
           }
           private string LinkPath => Path.Combine(_startupFolder, _linkName);
           public bool IsEnabled() => File.Exists(LinkPath);
           public void Disable() { if (File.Exists(LinkPath)) File.Delete(LinkPath); }
           public void Enable()
           {
               Directory.CreateDirectory(_startupFolder);
               var exe = Environment.ProcessPath!;               // single-file-safe (PAS Assembly.Location)
               var t = Type.GetTypeFromProgID("WScript.Shell")!; // COM late-bound, aucun NuGet
               dynamic shell = Activator.CreateInstance(t)!;
               var lnk = shell.CreateShortcut(LinkPath);
               lnk.TargetPath = exe;
               lnk.WorkingDirectory = Path.GetDirectoryName(exe);
               lnk.Description = "Chronos — overlay de quotas Claude";
               lnk.Save();
           }
       }
       ```
       `dynamic` requiert `Microsoft.CSharp` (runtime partagé, OK avec PublishTrimmed=false — STACK.md). Commentaires en français.
  </action>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~AutostartServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Chronos.Tests/AutostartServiceTests.cs` (dossier temp injecté) : IsEnabled false au départ ; Disable idempotent ;
      après création d'un .lnk factice IsEnabled == true puis Disable() le supprime.
    - Un test d'intégration Enable() réel via COM est ACCEPTABLE mais peut être `[Fact(Skip=...)]` si l'environnement n'a pas WScript.Shell ;
      la logique chemin/existence reste couverte sans COM.
    - `AutostartService`/`IAutostartService` neutres (garde de pureté verte).
  </acceptance_criteria>
  <done>Autostart .lnk pilotable, dossier injectable, ciblant ProcessPath, testé sans polluer le vrai Startup.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` réussit.
- `dotnet test Chronos.sln -c Debug` : 68 tests existants + TopmostGuard étendu + AutostartService verts.
- Garde de pureté verte (TopmostGuard déjà allow-listé ; AutostartService/IAutostartService neutres).
- Aucun NuGet ajouté ; pas de `<UseWindowsForms>`.
</verification>

<success_criteria>
- Interop moniteur/DPI/fenêtre/arrière-plan prêt pour le placement physique (FEN-04/FEN-05, consommé en 06-03).
- TopmostGuard.Suspend/Resume prouvé (FEN-05).
- Autostart .lnk sans dépendance native, testable, ciblant l'exe courant (DEP-02).
- Clic-traversant explicitement acté comme différé V2 (aucune implémentation).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-comportements-overlay-placement-interaction/06-02-SUMMARY.md`.
</output>
