---
phase: 10-lecture-du-token-client-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Chronos/Chronos.csproj
  - src/Chronos/Services/IClaudeTokenReader.cs
  - src/Chronos/Services/ClaudeTokenReader.cs
  - tests/Chronos.Tests/Fakes/V10TestVault.cs
  - tests/Chronos.Tests/ClaudeTokenReaderTests.cs
autonomous: true
requirements: [TOK-01, TOK-02, TOK-03]

must_haves:
  truths:
    - "Le lecteur déchiffre un blob v10 synthétique (clé AES de test connue) et retourne le champ `token` de l'entrée dont la clé contient `claude_code`."
    - "Fichier/clé absents, base64 invalide, blob trop court, tag GCM faux, map sans `claude_code`, entrée sans `token` → retour null SANS exception."
    - "Le lecteur ouvre les fichiers coffre en lecture seule et n'écrit AUCUN fichier (prouvé par test snapshot répertoire + grep source : aucun File.Write/Log/Console)."
    - "Le token en clair ne quitte jamais la méthode autrement que comme valeur de retour en mémoire."
  artifacts:
    - path: "src/Chronos/Services/IClaudeTokenReader.cs"
      provides: "Contrat neutre TryReadAccessToken(out DateTimeOffset? expiresAt) → string?"
      contains: "interface IClaudeTokenReader"
    - path: "src/Chronos/Services/ClaudeTokenReader.cs"
      provides: "Déchiffrement DPAPI + AES-256-GCM v10 + sélection claude_code, tolérant, lecture seule"
      min_lines: 60
      contains: "DecryptAndSelectToken"
    - path: "src/Chronos/Chronos.csproj"
      provides: "Dépendance DPAPI"
      contains: "System.Security.Cryptography.ProtectedData"
    - path: "tests/Chronos.Tests/Fakes/V10TestVault.cs"
      provides: "Fabrique un blob v10 chiffré déterministe avec une clé AES de test connue (jamais le vrai coffre)"
      contains: "V10TestVault"
    - path: "tests/Chronos.Tests/ClaudeTokenReaderTests.cs"
      provides: "TOK-01/02/03 : déchiffrement, tolérance, preuve non-écriture"
      contains: "ClaudeTokenReaderTests"
  key_links:
    - from: "src/Chronos/Services/ClaudeTokenReader.cs"
      to: "ProtectedData.Unprotect"
      via: "encrypted_key[5..] (retire préfixe DPAPI), DataProtectionScope.CurrentUser"
      pattern: "ProtectedData\\.Unprotect"
    - from: "src/Chronos/Services/ClaudeTokenReader.cs"
      to: "AesGcm.Decrypt"
      via: "nonce = blob[3..15], tag = blob[^16..], cipher = blob[15..^16]"
      pattern: "AesGcm"
    - from: "src/Chronos/Services/ClaudeTokenReader.cs"
      to: "entrée claude_code, champ token"
      via: "EnumerateObject + entry.Name.Contains(\"claude_code\") + GetProperty(\"token\")"
      pattern: "claude_code"
---

<objective>
Livrer `ClaudeTokenReader` : un service NEUTRE (couche Services, aucun type WPF) qui déchiffre le token
OAuth du coffre safeStorage de l'app bureau Claude et renvoie l'access token EN MÉMOIRE, ou `null` sur
toute anomalie — sans jamais logger, écrire, ni exposer le token, et sans jamais lever d'exception.

Couvre TOK-01 (déchiffrement config.json → clé DPAPI → AES-256-GCM → JSON MAP → champ `token`),
TOK-02 (tolérance totale aux erreurs → null), TOK-03 (aucune trace du token ; lecture seule prouvée
par test).

Purpose: isoler et prouver la surface la plus sensible (lecture/déchiffrement) AVANT tout appel réseau
(Plan 02) et toute intégration UI (Phase 11).
Output: `IClaudeTokenReader` + `ClaudeTokenReader` + fixture de test `V10TestVault` + suite de tests
TOK-01/02/03, dépendance NuGet `System.Security.Cryptography.ProtectedData` 8.0.0 ajoutée.

Le TEST DÉCISIF E2E a DÉJÀ réussi sur cette machine (déchiffrement réel + HTTP 200, cf. 10-RESEARCH.md) :
le schéma est PROUVÉ. Ce plan implémente le schéma prouvé et le couvre par des tests déterministes (blob
synthétique, PAS le vrai coffre) — aucune re-validation manuelle requise ici.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-lecture-du-token-client-endpoint/10-RESEARCH.md
@.planning/phases/10-lecture-du-token-client-endpoint/10-CONTEXT.md
@.planning/phases/10-lecture-du-token-client-endpoint/10-VALIDATION.md
@src/Chronos/Services/ClaudeUsageObjectProvider.cs
@src/Chronos/Services/ChronosPaths.cs
@src/Chronos/Services/IClock.cs

<security_constraint priority="NON_NEGOCIABLE">
Vaut pour CHAQUE tâche de ce plan.
- Le token/clé en clair vit UNIQUEMENT en variable locale (byte[]/string). JAMAIS : logué, écrit sur
  disque, mis dans un message d'exception, concaténé dans une URL, exposé dans une trace.
- Seul usage autorisé du token (Plan 02) : en-tête `Authorization`. Ici : valeur de retour en mémoire.
- Fichiers coffre (`config.json`, `Local State`) ouverts en `FileAccess.Read` STRICT. AUCUNE réécriture
  de `%APPDATA%/Claude`.
- Tolérance totale : TOUTE erreur (fichier absent, DPAPI échoue, GCM tag invalide, JSON malformé, blob
  court…) → retour `null`, JAMAIS d'exception non gérée. NE JAMAIS journaliser le détail de l'exception
  (elle pourrait contenir des fragments sensibles).
</security_constraint>

<interfaces>
<!-- Contrats existants réutilisés — pas d'exploration codebase nécessaire. -->

Cible de retour (déjà en place) — l'access token est un simple `string?` en mémoire.

InternalsVisibleTo("Chronos.Tests") est DÉJÀ actif dans Chronos.csproj → les tests peuvent cibler
directement une méthode `internal static` du reader (le cœur déchiffrement testable).

Schéma coffre PROUVÉ (10-RESEARCH.md, offsets réels) :
```
Local State (JSON) → os_crypt.encrypted_key (base64)
  → décode → préfixe ASCII "DPAPI" (5 octets) → retirer → blob DPAPI
  → ProtectedData.Unprotect(blob, null, CurrentUser) → 32 octets = clé AES-256

config.json (JSON) → oauth:tokenCache (base64)
  → décode → préfixe ASCII "v10" (3 octets)
  → nonce = octets [3..15] (12o) ; tag = 16 derniers octets ; cipher = [15 .. len-16]
  → AesGcm(clé32, 16).Decrypt(nonce, cipher, tag) → UTF-8 = JSON MAP

MAP : { "<uuid>:<uuid>:<baseUrl>:<scopes>:claude_code": { token, refreshToken, expiresAt, ... }, ... }
  → énumérer, prendre la 1re entrée dont la clé CONTIENT "claude_code",
    lire le champ **token** (PAS accessToken), et expiresAt si présent.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : NuGet DPAPI + contrat IClaudeTokenReader + fixture V10TestVault</name>
  <files>src/Chronos/Chronos.csproj, src/Chronos/Services/IClaudeTokenReader.cs, tests/Chronos.Tests/Fakes/V10TestVault.cs</files>
  <read_first>
    - src/Chronos/Chronos.csproj (bloc PackageReference existant : CommunityToolkit.Mvvm, Extensions.Hosting)
    - src/Chronos/Services/IUsageProvider.cs (style d'interface neutre + commentaire français)
    - tests/Chronos.Tests/Fakes/FakeClock.cs (style fixture interne, namespace Chronos.Tests)
  </read_first>
  <action>
    1. Ajouter la dépendance DPAPI au projet Chronos (ligne 8.0.x, vérifiée fonctionnelle en RESEARCH) :
       `dotnet add src/Chronos/Chronos.csproj package System.Security.Cryptography.ProtectedData --version 8.0.0`
       (doit produire un `<PackageReference Include="System.Security.Cryptography.ProtectedData" Version="8.0.0" />`).

    2. Créer `src/Chronos/Services/IClaudeTokenReader.cs` — contrat NEUTRE, commentaire français :
       ```csharp
       namespace Chronos.Services;

       /// <summary>Lit et déchiffre l'access token OAuth du coffre de l'app bureau Claude, en mémoire
       /// seulement (TOK-01/03). Toute anomalie → null, jamais d'exception (TOK-02). Lecture seule.</summary>
       public interface IClaudeTokenReader
       {
           /// <summary>Renvoie l'access token en clair (mémoire) + son expiration si connue, ou null
           /// sur tout échec. N'écrit rien, ne logue rien, ne lève rien.</summary>
           string? TryReadAccessToken(out DateTimeOffset? expiresAt);
       }
       ```

    3. Créer `tests/Chronos.Tests/Fakes/V10TestVault.cs` — fabrique DÉTERMINISTE d'un blob v10 chiffré
       avec une clé AES de TEST connue (jamais le vrai coffre, aucun secret réel). Sert à tester le cœur
       déchiffrement sans DPAPI ni vrai token. `internal static`, namespace `Chronos.Tests` :
       ```csharp
       using System.Security.Cryptography;
       using System.Text;

       namespace Chronos.Tests;

       /// <summary>Fabrique un blob safeStorage v10 (préfixe "v10" + nonce 12o + cipher + tag 16o)
       /// chiffré AES-256-GCM par une CLÉ DE TEST connue — pour prouver le déchiffrement du reader
       /// sans jamais toucher le vrai coffre ni un vrai token.</summary>
       internal static class V10TestVault
       {
           /// <summary>Clé AES-256 de test (32 octets fixes, publique, sans valeur réelle).</summary>
           public static byte[] TestKey => Enumerable.Range(0, 32).Select(i => (byte)i).ToArray();

           /// <summary>Chiffre `plaintextJson` en un blob v10 base64 (schéma RESEARCH : "v10" + nonce + cipher + tag).</summary>
           public static string MakeTokenCacheB64(string plaintextJson, byte[]? key = null)
           {
               key ??= TestKey;
               var nonce = new byte[12]; RandomNumberGenerator.Fill(nonce);
               var plain = Encoding.UTF8.GetBytes(plaintextJson);
               var cipher = new byte[plain.Length];
               var tag = new byte[16];
               using (var gcm = new AesGcm(key, 16)) gcm.Encrypt(nonce, plain, cipher, tag);
               var blob = new byte[3 + 12 + cipher.Length + 16];
               Encoding.ASCII.GetBytes("v10").CopyTo(blob, 0);
               nonce.CopyTo(blob, 3);
               cipher.CopyTo(blob, 15);
               tag.CopyTo(blob, 15 + cipher.Length);
               return Convert.ToBase64String(blob);
           }
       }
       ```
    Objectif de la tâche : contrats + fixture en place, la solution compile. AUCUNE logique de
    déchiffrement encore (elle vient en Tâche 2). Interface-first : les tests de la Tâche 2 se codent
    contre ce contrat.
  </action>
  <acceptance_criteria>
    - `grep -q "System.Security.Cryptography.ProtectedData" src/Chronos/Chronos.csproj` → présent.
    - `grep -q "interface IClaudeTokenReader" src/Chronos/Services/IClaudeTokenReader.cs` → présent.
    - `grep -q "class V10TestVault" tests/Chronos.Tests/Fakes/V10TestVault.cs` → présent.
    - `V10TestVault` n'utilise QUE `AesGcm` avec une clé de test générée localement (aucune constante
      ressemblant à un vrai token/clé réelle).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <done>La solution compile ; l'interface `IClaudeTokenReader`, la dépendance ProtectedData 8.0.0 et la
  fixture `V10TestVault` existent. Aucune implémentation de reader encore.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : implémenter ClaudeTokenReader + tests TOK-01/02/03</name>
  <files>src/Chronos/Services/ClaudeTokenReader.cs, tests/Chronos.Tests/ClaudeTokenReaderTests.cs</files>
  <read_first>
    - src/Chronos/Services/ClaudeUsageObjectProvider.cs (pattern lecture tolérante FileAccess.Read + try/catch ciblé + JsonDocument)
    - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs (style xUnit, [Fact], noms français, TestDataPath via CallerFilePath)
    - tests/Chronos.Tests/Fakes/V10TestVault.cs (fixture créée en Tâche 1)
    - .planning/phases/10-lecture-du-token-client-endpoint/10-RESEARCH.md §"Code Examples" (offsets verbatim) + §"Common Pitfalls"
  </read_first>
  <behavior>
    Écrire les tests AVANT l'implémentation (RED → GREEN). `ClaudeTokenReaderTests.cs` cible la méthode
    `internal static ClaudeTokenReader.DecryptAndSelectToken(byte[] aesKey, string tokenCacheB64, out DateTimeOffset? expiresAt)`
    (accessible via InternalsVisibleTo) pour les cas crypto déterministes, et le `TryReadAccessToken`
    complet (chemins injectables) pour la tolérance fichiers et la preuve de non-écriture :

    - TOK-01 (nominal) : `V10TestVault.MakeTokenCacheB64` d'une MAP JSON à 2 entrées dont une clé contient
      `claude_code` avec `{ "token": "TOK-abc", "expiresAt": "<date future ISO>" }` → `DecryptAndSelectToken(TestKey, b64, out exp)`
      retourne exactement `"TOK-abc"` et `exp` = la date parsée.
    - TOK-01 (sélection) : MAP dont la 1re entrée n'a PAS `claude_code` et la 2e l'a → retourne le token de la 2e.
    - TOK-02 : `null` SANS exception pour chacun — base64 invalide ("!!!"), blob trop court (< 31 octets),
      tag GCM faux (blob dont on flippe 1 octet du tag → CryptographicException interne), MAP sans aucune
      entrée `claude_code`, entrée `claude_code` sans champ `token`, mauvaise clé AES (déchiffrement échoue).
    - TOK-02 (fichiers) : `TryReadAccessToken` avec chemins config.json / Local State INEXISTANTS → null, aucune exception.
    - TOK-03 (non-écriture) : construire le reader avec un répertoire temp contenant des fichiers coffre
      factices, snapshot de la liste des fichiers + timestamps AVANT/APRÈS l'appel → identiques (aucune
      création/modification). Nettoyer le temp en fin de test.
  </behavior>
  <action>
    Créer `src/Chronos/Services/ClaudeTokenReader.cs` — classe scellée NEUTRE, chemins injectables pour
    la testabilité, cœur déchiffrement isolé et `internal static` :

    1. Constructeur injectable + fabrique Default() (chemins %APPDATA%/Claude, via
       `Environment.GetFolderPath(SpecialFolder.ApplicationData)`) :
       ```csharp
       public ClaudeTokenReader(string configJsonPath, string localStatePath) { ... }
       public static ClaudeTokenReader Default() => new(
           Path.Combine(appData, "Claude", "config.json"),
           Path.Combine(appData, "Claude", "Local State"));
       ```
       Implémenter `IClaudeTokenReader`.

    2. `TryReadAccessToken(out DateTimeOffset? expiresAt)` — orchestration, UN try/catch global tolérant :
       - lire `Local State` (FileStream FileMode.Open, **FileAccess.Read**), JSON → `os_crypt.encrypted_key`
         (base64) → décoder → `ProtectedData.Unprotect(bytes[5..] /* retire "DPAPI" */, null, DataProtectionScope.CurrentUser)` → clé AES 32o ;
       - lire `config.json` (FileAccess.Read), JSON → `oauth:tokenCache` (string base64) ;
       - `return DecryptAndSelectToken(aesKey, tokenCacheB64, out expiresAt);`
       - `catch (Exception)` global (voir liste Pitfall 5 : FileNotFound, DirectoryNotFound, IOException,
         JsonException, FormatException, CryptographicException, ArgumentException, IndexOutOfRange) →
         `expiresAt = null; return null;`. NE PAS journaliser l'exception.

    3. `internal static string? DecryptAndSelectToken(byte[] aesKey, string tokenCacheB64, out DateTimeOffset? expiresAt)`
       — cœur testable, schéma RESEARCH verbatim :
       ```csharp
       expiresAt = null;
       try
       {
           byte[] blob = Convert.FromBase64String(tokenCacheB64);
           if (blob.Length < 3 + 12 + 16) return null;          // trop court
           byte[] nonce = blob[3..15];                          // 12o après "v10"
           byte[] tag = blob[^16..];                            // 16o finaux
           byte[] cipher = blob[15..^16];
           byte[] plain = new byte[cipher.Length];
           using (var gcm = new AesGcm(aesKey, 16)) gcm.Decrypt(nonce, cipher, tag, plain);
           string json = Encoding.UTF8.GetString(plain);
           using var doc = JsonDocument.Parse(json);            // MAP
           foreach (var entry in doc.RootElement.EnumerateObject())
           {
               if (!entry.Name.Contains("claude_code")) continue;
               if (entry.Value.TryGetProperty("token", out var t) && t.ValueKind == JsonValueKind.String)
               {
                   if (entry.Value.TryGetProperty("expiresAt", out var e)
                       && e.ValueKind == JsonValueKind.String
                       && DateTimeOffset.TryParse(e.GetString(), CultureInfo.InvariantCulture,
                            DateTimeStyles.RoundtripKind, out var d))
                       expiresAt = d;
                   return t.GetString();                        // token en mémoire seulement
               }
           }
           return null;                                          // aucune entrée claude_code exploitable
       }
       catch (Exception) { expiresAt = null; return null; }      // GCM/base64/JSON → null (Pitfall 5)
       ```
    CONTRAINTES DE SÉCURITÉ (voir <security_constraint>) : aucun `Console.`, aucun `File.Write*`, aucun
    logger, aucune concat du token dans un message. Fichiers en `FileAccess.Read` STRICT. Le token ne
    sort que par `return`. Commentaires en français.

    Puis écrire `tests/Chronos.Tests/ClaudeTokenReaderTests.cs` couvrant tous les cas du bloc <behavior>.
  </action>
  <acceptance_criteria>
    - `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~TokenReader"` → tous verts.
    - Preuve non-écriture (source) : `grep -nE "File\.(Write|Create|Append|Open\(|Move|Delete)|StreamWriter|Console\.|Log(ger|ging)?\." src/Chronos/Services/ClaudeTokenReader.cs` → AUCUNE correspondance (le fichier n'ouvre qu'en lecture).
    - `grep -q "FileAccess.Read" src/Chronos/Services/ClaudeTokenReader.cs` → présent (lecture seule explicite).
    - `grep -q "DecryptAndSelectToken" src/Chronos/Services/ClaudeTokenReader.cs` → cœur testable exposé (internal).
    - Le test TOK-03 asserte égalité du snapshot répertoire avant/après (aucun fichier créé/modifié).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~TokenReader"</automated>
  </verify>
  <done>`ClaudeTokenReader` déchiffre un blob v10 synthétique et renvoie le champ `token` de l'entrée
  `claude_code` ; toute anomalie → null sans exception ; lecture seule prouvée (grep + test snapshot) ;
  tests TOK-01/02/03 verts.</done>
</task>

</tasks>

<verification>
- `dotnet build Chronos.sln -c Debug` : compile (NuGet ProtectedData 8.0.0 restauré).
- `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~TokenReader"` : tous verts.
- Gate wave / non-régression : `dotnet test Chronos.sln -c Debug` → 154 tests existants + nouveaux verts.
- Pureté : `dotnet test ... --filter ServicesLayerPurityTests` reste vert (le reader est neutre, aucun type WPF).
- Sécurité (grep) : `ClaudeTokenReader.cs` ne contient aucun chemin d'écriture/log/console ; ouvre en FileAccess.Read.
</verification>

<success_criteria>
- TOK-01 : blob v10 synthétique → déchiffré → entrée `claude_code` → champ `token` retourné, `expiresAt` parsé.
- TOK-02 : fichier/clé absents, base64 invalide, blob court, tag GCM faux, map sans `claude_code`, entrée
  sans `token`, mauvaise clé → null, JAMAIS d'exception.
- TOK-03 : aucune API d'écriture dans le reader (grep) ; test snapshot prouve qu'aucun fichier n'est créé/
  modifié ; token uniquement en valeur de retour mémoire.
- Non-régression : suite complète verte, pureté Services conservée.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/10-lecture-du-token-client-endpoint/10-01-SUMMARY.md`.
</output>
