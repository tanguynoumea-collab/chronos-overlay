---
phase: 10-lecture-du-token-client-endpoint
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/Chronos/Services/ClaudeOAuthUsageProvider.cs
  - tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs
  - tests/Chronos.Tests/Fakes/FakeClaudeTokenReader.cs
  - tests/Chronos.Tests/ClaudeOAuthUsageProviderTests.cs
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "Réponse OAuth valide → UsageSnapshot Exact : five_hour.utilization/100 → Utilization, resets_at ISO 8601 → ResetsAt, Reliability = Exact, FractionTimeRemaining calculé."
    - "401/403, timeout (TaskCanceled), erreur réseau (HttpRequestException) et JSON malformé → UsageSnapshot.Empty, JAMAIS d'exception non gérée."
    - "Token null OU expiresAt < now → AUCUN appel HTTP émis (provider inerte, court-circuit expiration)."
    - "L'appel est asynchrone, respecte le CancellationToken et applique un timeout court (5 s) sans bloquer l'appelant."
  artifacts:
    - path: "src/Chronos/Services/ClaudeOAuthUsageProvider.cs"
      provides: "IUsageProvider : GET /api/oauth/usage → mapping schéma OAuth → UsageSnapshot Exact, tolérant"
      min_lines: 60
      contains: "class ClaudeOAuthUsageProvider"
    - path: "tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs"
      provides: "Handler HTTP scripté (statut + corps ou exception) pour tester mapping/erreurs sans réseau"
      contains: "FakeHttpMessageHandler"
    - path: "tests/Chronos.Tests/Fakes/FakeClaudeTokenReader.cs"
      provides: "IClaudeTokenReader factice (token/expiration injectés, ou null) + compteur d'appels"
      contains: "FakeClaudeTokenReader"
    - path: "tests/Chronos.Tests/ClaudeOAuthUsageProviderTests.cs"
      provides: "API-01/02/03 : mapping, erreurs, inertie/annulation"
      contains: "ClaudeOAuthUsageProviderTests"
  key_links:
    - from: "src/Chronos/Services/ClaudeOAuthUsageProvider.cs"
      to: "IClaudeTokenReader.TryReadAccessToken"
      via: "token lu puis placé UNIQUEMENT dans l'en-tête Authorization: Bearer"
      pattern: "TryReadAccessToken"
    - from: "src/Chronos/Services/ClaudeOAuthUsageProvider.cs"
      to: "https://api.anthropic.com/api/oauth/usage"
      via: "HttpClient.SendAsync(request, cts.Token) avec anthropic-beta: oauth-2025-04-20"
      pattern: "api/oauth/usage"
    - from: "src/Chronos/Services/ClaudeOAuthUsageProvider.cs"
      to: "WindowState (Exact)"
      via: "five_hour.utilization/100 → Utilization ; resets_at ISO → ResetsAt ; Reliability=Exact"
      pattern: "SourceReliability.Exact"
---

<objective>
Livrer `ClaudeOAuthUsageProvider : IUsageProvider` : à la demande, lit le token via `IClaudeTokenReader`
(Plan 01), appelle `GET https://api.anthropic.com/api/oauth/usage` et mappe la réponse RÉELLE de
l'endpoint OAuth vers un `UsageSnapshot` **Exact** des deux fenêtres — ou `UsageSnapshot.Empty` proprement.

Couvre API-01 (client + mapping schéma OAuth réel → Exact), API-02 (401/403/réseau/timeout/malformé →
indisponible, jamais d'exception), API-03 (asynchrone, respecte CancellationToken, inerte hors ligne /
token absent / token expiré, n'impacte pas le tick 1 s).

Purpose: compléter la surface sensible isolée (réseau) et la prouver par des tests déterministes
(HttpMessageHandler mocké, réponse OAuth réelle anonymisée) AVANT l'intégration composite (Phase 11).
Output: `ClaudeOAuthUsageProvider` + `FakeHttpMessageHandler` + `FakeClaudeTokenReader` + suite
API-01/02/03.

Le TEST DÉCISIF E2E a DÉJÀ réussi (HTTP 200, chiffres réels 5 h/hebdo, cf. 10-RESEARCH.md) : le schéma de
réponse est PROUVÉ et anonymisé dans le RESEARCH. Ce plan implémente ce schéma prouvé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-lecture-du-token-client-endpoint/10-RESEARCH.md
@.planning/phases/10-lecture-du-token-client-endpoint/10-CONTEXT.md
@.planning/phases/10-lecture-du-token-client-endpoint/10-VALIDATION.md
@src/Chronos/Services/ClaudeUsageObjectProvider.cs
@src/Chronos/Models/UsageSnapshot.cs
@src/Chronos/Models/WindowState.cs
@src/Chronos/Services/IUsageProvider.cs
@src/Chronos/Services/IClock.cs
@.planning/phases/10-lecture-du-token-client-endpoint/10-01-PLAN.md

<security_constraint priority="NON_NEGOCIABLE">
Vaut pour CHAQUE tâche de ce plan.
- Le token lu via `IClaudeTokenReader` vit UNIQUEMENT en variable locale, le temps de construire l'en-tête
  `Authorization: Bearer`. JAMAIS : logué, écrit, mis dans un message d'exception, ni concaténé dans l'URL.
- Seule destination réseau autorisée : `https://api.anthropic.com`. L'URL est une constante — le token
  n'y apparaît jamais.
- Tolérance totale : 401/403, timeout, erreur réseau, JSON malformé, fenêtre absente → `UsageSnapshot.Empty`
  ou `WindowState.Unavailable`, JAMAIS d'exception non gérée, JAMAIS de valeur inventée.
- Court-circuit expiration (specifics CONTEXT) : si `expiresAt < now`, NE PAS appeler (évite un 401 inutile).
</security_constraint>

<interfaces>
<!-- Contrats consommés — définis Plan 01 + modèles existants. Pas d'exploration codebase nécessaire. -->

Depuis Plan 01 (src/Chronos/Services/IClaudeTokenReader.cs) :
```csharp
public interface IClaudeTokenReader { string? TryReadAccessToken(out DateTimeOffset? expiresAt); }
```

Modèles existants (src/Chronos/Models) — cibles du mapping :
```csharp
public sealed record UsageSnapshot {
    public required WindowState FiveHour { get; init; }
    public required WindowState SevenDay { get; init; }
    public DateTimeOffset? SourceCapturedAt { get; init; }
    public static UsageSnapshot Empty { get; }   // deux fenêtres Unavailable
}
public sealed record WindowState {
    public required WindowKind Kind { get; init; }
    public double? Utilization { get; init; }            // 0..1
    public DateTimeOffset? ResetsAt { get; init; }
    public double? FractionTimeRemaining { get; init; }
    public required SourceReliability Reliability { get; init; }
    public static WindowState Unavailable(WindowKind k);
    public static double? FractionRemaining(DateTimeOffset? resetsAt, DateTimeOffset now, TimeSpan windowLength);
}
// enum WindowKind { FiveHour, SevenDay }  |  enum SourceReliability { ... Exact, Unavailable }
public interface IUsageProvider { Task<UsageSnapshot> GetAsync(CancellationToken ct = default); }
public interface IClock { DateTimeOffset UtcNow { get; } }
```

Schéma de réponse OAuth RÉEL (10-RESEARCH.md — DIFFÉRENT de statusLine, NE PAS confondre) :
```jsonc
{
  "five_hour": { "utilization": 42.0, "resets_at": "2026-07-09T09:39:59.692687+00:00", ... },
  "seven_day": { "utilization": 73.0, "resets_at": "2026-07-10T21:59:59.692707+00:00", ... },
  "limits": [ ... ], "extra_usage": { ... }, "spend": { ... }   // ignorés en v1.2
}
```
- Fenêtres à la RACINE (pas sous `rate_limits`).
- Champ % = **`utilization`** (0..100 → diviser par 100 pour WindowState 0..1).
- `resets_at` = **chaîne ISO 8601** (offset + microsecondes) → `DateTimeOffset.Parse(..., RoundtripKind)`
  (PAS epoch secondes — c'est le piège vs `ClaudeUsageObjectProvider`).
- Ne mapper QUE `five_hour` + `seven_day` (hebdo global). Ignorer `limits[]`/sous-fenêtres (hors scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : infra de test — FakeHttpMessageHandler + FakeClaudeTokenReader</name>
  <files>tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs, tests/Chronos.Tests/Fakes/FakeClaudeTokenReader.cs</files>
  <read_first>
    - tests/Chronos.Tests/Fakes/FakeUsageProvider.cs (style fake interne thread-safe + compteur, namespace Chronos.Tests)
    - tests/Chronos.Tests/Fakes/FakeClock.cs (style minimal)
    - src/Chronos/Services/IClaudeTokenReader.cs (contrat à implémenter, créé Plan 01)
  </read_first>
  <action>
    Aucun test réseau n'existe encore dans le repo → créer les deux fakes (Wave 0 de ce plan).

    1. `tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs` — `HttpMessageHandler` scripté, permet de
       renvoyer un statut + corps JSON, OU de lever une exception (simuler réseau/timeout), et compte les
       envois (pour prouver l'inertie = 0 appel). `internal sealed`, namespace `Chronos.Tests` :
       ```csharp
       using System.Net;
       using System.Net.Http;

       namespace Chronos.Tests;

       /// <summary>Handler HTTP scripté : renvoie une réponse fixe (statut + JSON) ou lève une exception
       /// (réseau/timeout), et compte les envois — pour tester le provider sans réseau réel.</summary>
       internal sealed class FakeHttpMessageHandler : HttpMessageHandler
       {
           private readonly Func<HttpRequestMessage, HttpResponseMessage> _responder;
           public int SendCount { get; private set; }
           public HttpRequestMessage? LastRequest { get; private set; }

           public FakeHttpMessageHandler(Func<HttpRequestMessage, HttpResponseMessage> responder)
               => _responder = responder;

           /// <summary>Réponse simple : statut + corps JSON.</summary>
           public static FakeHttpMessageHandler Json(HttpStatusCode status, string body) =>
               new(_ => new HttpResponseMessage(status) { Content = new StringContent(body) });

           /// <summary>Simule une panne réseau / un timeout via l'exception fournie.</summary>
           public static FakeHttpMessageHandler Throws(Exception ex) =>
               new(_ => throw ex);

           protected override Task<HttpResponseMessage> SendAsync(HttpRequestMessage request, CancellationToken ct)
           {
               SendCount++; LastRequest = request;
               return Task.FromResult(_responder(request));
           }
       }
       ```

    2. `tests/Chronos.Tests/Fakes/FakeClaudeTokenReader.cs` — `IClaudeTokenReader` factice, token/expiration
       injectés (ou null), compte les lectures :
       ```csharp
       using Chronos.Services;

       namespace Chronos.Tests;

       /// <summary>IClaudeTokenReader factice : renvoie le token/expiration configurés (ou null).</summary>
       internal sealed class FakeClaudeTokenReader : IClaudeTokenReader
       {
           public string? Token;
           public DateTimeOffset? Expires;
           public int ReadCount { get; private set; }
           public string? TryReadAccessToken(out DateTimeOffset? expiresAt)
           { ReadCount++; expiresAt = Expires; return Token; }
       }
       ```
    Objectif : infra de test en place, la solution compile.
  </action>
  <acceptance_criteria>
    - `grep -q "class FakeHttpMessageHandler" tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs` → présent.
    - `grep -q "class FakeClaudeTokenReader" tests/Chronos.Tests/Fakes/FakeClaudeTokenReader.cs` → présent.
    - `FakeClaudeTokenReader` implémente bien `IClaudeTokenReader` (compile contre le contrat Plan 01).
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Chronos.sln -c Debug</automated>
  </verify>
  <done>Les deux fakes existent et compilent ; `FakeHttpMessageHandler` peut scripter statut/corps ou
  lever une exception et compte les envois ; `FakeClaudeTokenReader` fournit token/expiration injectables.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : implémenter ClaudeOAuthUsageProvider + tests API-01/02/03</name>
  <files>src/Chronos/Services/ClaudeOAuthUsageProvider.cs, tests/Chronos.Tests/ClaudeOAuthUsageProviderTests.cs</files>
  <read_first>
    - src/Chronos/Services/ClaudeUsageObjectProvider.cs (pattern mapping fenêtre + try/catch ciblé — MAIS schéma différent : ne pas copier used_percentage/epoch)
    - src/Chronos/Models/WindowState.cs (FractionRemaining, Unavailable, Utilization 0..1)
    - .planning/phases/10-lecture-du-token-client-endpoint/10-RESEARCH.md §"Mapping" + §"Code Examples" (appel + mapping) + §"Common Pitfalls" (2,3,5)
    - tests/Chronos.Tests/ClaudeUsageObjectProviderTests.cs (style xUnit async, [Fact], noms français, assertions Utilization/Reliability)
    - tests/Chronos.Tests/Fakes/FakeHttpMessageHandler.cs + FakeClaudeTokenReader.cs (Tâche 1)
  </read_first>
  <behavior>
    Écrire les tests AVANT l'implémentation (RED → GREEN). Injecter `FakeClaudeTokenReader`,
    `new HttpClient(FakeHttpMessageHandler)` et `FakeClock`. Réponse OAuth réelle anonymisée en fixture
    (chaîne JSON inline, schéma RESEARCH). Cas :

    - API-01 (mapping nominal) : réponse 200 avec `five_hour.utilization = 65.0`,
      `resets_at = "2026-07-09T09:39:59.692687+00:00"`, `seven_day.utilization = 92.0` →
      `snap.FiveHour.Utilization == 0.65` (à 1e-9), `snap.SevenDay.Utilization == 0.92`,
      `ResetsAt == DateTimeOffset.Parse(...)`, `Reliability == SourceReliability.Exact`,
      `FractionTimeRemaining` non-null. Vérifier aussi que la requête portait bien
      `Authorization: Bearer <token>` et `anthropic-beta: oauth-2025-04-20`
      (via `FakeHttpMessageHandler.LastRequest.Headers`).
    - API-01 (fenêtre absente) : réponse 200 où `seven_day` manque → `snap.SevenDay` = Unavailable,
      `snap.FiveHour` mappée. Aucune valeur inventée.
    - API-02 : pour chacun → `UsageSnapshot.Empty`, aucune exception :
      401, 403, 500 ; `FakeHttpMessageHandler.Throws(new HttpRequestException())` (réseau) ;
      `Throws(new TaskCanceledException())` (timeout) ; corps 200 mais JSON malformé ("{ oops").
    - API-03 (inertie token absent) : `FakeClaudeTokenReader.Token = null` → `GetAsync` retourne Empty
      ET `FakeHttpMessageHandler.SendCount == 0` (aucun appel réseau).
    - API-03 (court-circuit expiration) : token présent mais `Expires = clock.UtcNow - 1h` → Empty ET
      `SendCount == 0`.
    - API-03 (annulation) : `GetAsync(new CancellationToken(canceled: true))` → ne crashe pas
      (retourne Empty), aucune exception ne remonte.
  </behavior>
  <action>
    Créer `src/Chronos/Services/ClaudeOAuthUsageProvider.cs` — classe scellée NEUTRE implémentant
    `IUsageProvider`. Injecter `IClaudeTokenReader`, `HttpClient`, `IClock` (HttpClient injecté = testable ;
    en Phase 11 la composition fournira un HttpClient réel). Schéma RESEARCH verbatim :

    1. Constantes : `private const string UsageUrl = "https://api.anthropic.com/api/oauth/usage";`
       `private static readonly TimeSpan Timeout = TimeSpan.FromSeconds(5);`

    2. `GetAsync(CancellationToken ct)` :
       ```csharp
       var token = _tokenReader.TryReadAccessToken(out var expiresAt);
       if (string.IsNullOrEmpty(token)) return UsageSnapshot.Empty;          // inerte (API-03)
       if (expiresAt is { } exp && exp < _clock.UtcNow) return UsageSnapshot.Empty; // court-circuit expiration
       try
       {
           using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
           cts.CancelAfter(Timeout);                                          // timeout court 5 s, indépendant
           using var req = new HttpRequestMessage(HttpMethod.Get, UsageUrl);
           req.Headers.TryAddWithoutValidation("Authorization", "Bearer " + token);
           req.Headers.TryAddWithoutValidation("anthropic-beta", "oauth-2025-04-20");
           req.Headers.TryAddWithoutValidation("User-Agent", "Chronos/1.2");
           using var resp = await _http.SendAsync(req, cts.Token);
           if (!resp.IsSuccessStatusCode) return UsageSnapshot.Empty;         // 401/403/5xx → indisponible (API-02)
           await using var stream = await resp.Content.ReadAsStreamAsync(cts.Token);
           using var doc = await JsonDocument.ParseAsync(stream, cancellationToken: cts.Token);
           var root = doc.RootElement;
           return new UsageSnapshot {
               FiveHour = Read(root, "five_hour", WindowKind.FiveHour, TimeSpan.FromHours(5)),
               SevenDay = Read(root, "seven_day", WindowKind.SevenDay, TimeSpan.FromDays(7)),
               SourceCapturedAt = _clock.UtcNow,
           };
       }
       catch (Exception ex) when (ex is HttpRequestException or TaskCanceledException
                                    or OperationCanceledException or JsonException or IOException)
       { return UsageSnapshot.Empty; }                                        // réseau/timeout/malformé (API-02/03)
       ```
       (Le token, variable locale, n'apparaît QUE dans l'en-tête Authorization. Aucun log.)

    3. Helper neutre `BuildExact` / `Read` (schéma OAuth : `utilization` 0..100 → /100 ; `resets_at`
       ISO 8601 → DateTimeOffset.Parse RoundtripKind) :
       ```csharp
       private WindowState Read(JsonElement root, string name, WindowKind kind, TimeSpan len)
       {
           if (!root.TryGetProperty(name, out var w) || w.ValueKind != JsonValueKind.Object)
               return WindowState.Unavailable(kind);                          // fenêtre absente → Unavailable
           double? util = w.TryGetProperty("utilization", out var u) && u.TryGetDouble(out var p)
               ? p / 100.0 : null;                                            // 0..100 → 0..1 (Pitfall 3)
           DateTimeOffset? reset = w.TryGetProperty("resets_at", out var r)
               && r.ValueKind == JsonValueKind.String
               && DateTimeOffset.TryParse(r.GetString(), CultureInfo.InvariantCulture,
                    DateTimeStyles.RoundtripKind, out var d) ? d : null;      // ISO 8601 (Pitfall 2)
           return new WindowState {
               Kind = kind, Utilization = util, ResetsAt = reset,
               Reliability = SourceReliability.Exact,
               FractionTimeRemaining = WindowState.FractionRemaining(reset, _clock.UtcNow, len),
           };
       }
       ```
    NE PAS réutiliser le parsing de `ClaudeUsageObjectProvider` (schéma statusLine `used_percentage`/epoch
    ≠ OAuth). Rester NEUTRE (aucun type WPF) → pureté verte. Commentaires en français.

    Puis écrire `tests/Chronos.Tests/ClaudeOAuthUsageProviderTests.cs` couvrant tous les cas <behavior>.
  </action>
  <acceptance_criteria>
    - `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~OAuth"` → tous verts.
    - `grep -q "api/oauth/usage" src/Chronos/Services/ClaudeOAuthUsageProvider.cs` ET `grep -q "oauth-2025-04-20"` → endpoint + beta présents.
    - `grep -q "SourceReliability.Exact" src/Chronos/Services/ClaudeOAuthUsageProvider.cs` → mapping Exact.
    - Sécurité (source) : `grep -nE "Console\.|Log(ger|ging)?\.|File\.(Write|Create|Append)" src/Chronos/Services/ClaudeOAuthUsageProvider.cs` → AUCUNE correspondance ; le token n'apparaît que dans l'en-tête Authorization (aucune concat dans UsageUrl, qui est une constante).
    - Le test API-03 asserte `SendCount == 0` quand token null OU expiré (inertie prouvée).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~OAuth"</automated>
  </verify>
  <done>`ClaudeOAuthUsageProvider` mappe la réponse OAuth réelle en UsageSnapshot Exact ; toutes les
  erreurs (401/403/réseau/timeout/malformé) → Empty sans exception ; inerte si token null/expiré (0 appel) ;
  tests API-01/02/03 verts.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/Chronos.Tests/Chronos.Tests.csproj -c Debug --filter "FullyQualifiedName~OAuth"` : tous verts.
- Gate wave / non-régression : `dotnet test Chronos.sln -c Debug` → 154 existants + Plan 01 + Plan 02 verts.
- Pureté : `dotnet test ... --filter ServicesLayerPurityTests` reste vert (provider neutre, aucun type WPF).
- Phase gate (avant /gsd:verify-work) : suite complète verte.
</verification>

<success_criteria>
- API-01 : réponse OAuth réelle → UsageSnapshot Exact (utilization/100, resets_at ISO parsé, Exact,
  FractionTimeRemaining) ; en-têtes Authorization + anthropic-beta bien émis ; fenêtre absente → Unavailable.
- API-02 : 401/403/500, réseau, timeout, JSON malformé → UsageSnapshot.Empty, jamais d'exception.
- API-03 : token null ou expiré → 0 appel réseau (inerte) ; annulation → pas de crash ; timeout 5 s appliqué.
- Non-régression : suite complète verte, pureté Services conservée.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/10-lecture-du-token-client-endpoint/10-02-SUMMARY.md`.
</output>
